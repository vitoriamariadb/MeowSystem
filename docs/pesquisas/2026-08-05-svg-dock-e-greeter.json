{
  "summary": "Descobre por que SVGs nao renderizam no dock/applets e desenha a tela de login bonita",
  "agentCount": 7,
  "logs": [],
  "result": {
    "sintese": {
      "itens": [
        {
          "item": "greeter/avatar — o único entregável realmente pendente: virar dono do gato da tela de login",
          "causa_ou_objetivo": "MEDIDO POR MIM: md5sum /var/lib/AccountsService/icons/vitoriamaria == assets/meow-mocha-preto.svg (3a496a3f5c1e99c231ff741800ac3757), root:root 0644, ago 4 23:54. E `grep -rln 'AccountsService|SetIconFile' scripts bin lib install.sh` devolve VAZIO: nenhum script do repo sabe que esse arquivo existe. O gato já está lá, mas foi posto na mão — numa máquina limpa não existe. Polkit org.freedesktop.accounts.change-own-user-data tem allow_any=yes: NÃO precisa sudo. Confirmei IconFile legível sem privilégio: busctl devolve s \"/var/lib/AccountsService/icons/vitoriamaria\".",
          "correcao": "Novo bloco fase_avatar em scripts/greeter.sh, rodando ANTES e INDEPENDENTE de qualquer teste de sudo. (1) O PNG é ARTEFATO VERSIONADO do repo (assets/meow-<flavor>-preto-256.png, gerado pelo gerar_gato.py), nunca rasterizado dentro do conferir — senão o conferir escreve e quebra o contrato do bin/meow. (2) conferir = md5sum do arquivo apontado por IconFile (0644, leitura pura, sem sudo) contra um CONJUNTO de md5 nossos (todos os flavors × preto/painel, e o SVG e o PNG), porque hoje o que está lá é o SVG e um conferir que só aceite PNG acusa divergência num estado já desejado. (3) aplicar = SetIconFile via busctl. (4) REVERTER POR BYTES, não por caminho: o refutador #2 está certo e verifiquei — IconFile já aponta para o próprio destino (/var/lib/AccountsService/icons/vitoriamaria), então guardar a string é guardar o alvo que vamos sobrescrever; salvar cp dos bytes em $MEOW_ESTADO/greeter/avatar-anterior.bin + a string em .iconfile (vazia = nunca houve). (5) GUARDA DE PROPRIEDADE: se o avatar atual não bate com nenhum md5 nosso e não bate com $MEOW_ESTADO/greeter/avatar-nosso.md5, meow_pula 'há um avatar que não é nosso'. Sem isso o timer diário desfaz calado a escolha dela no Settings.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/gerar_gato.py",
            "/mnt/Apate/Desenvolvimento/MeowSystem/assets/",
            "/mnt/Apate/Desenvolvimento/MeowSystem/meow.conf.exemplo"
          ],
          "risco": "A crate `image` do cosmic-greeter tem bmp/hdr/ico/jpeg/png/webp e NENHUM codec svg — mas o SVG está em produção desde 04/08 23:54 e não medi se ele desenha. Entregar PNG é conservador, não conserto de falha medida. Sem a guarda de propriedade, o meow-doctor.timer (05:05 diário, confirmado em systemctl --user list-timers) vira ping-pong com a USUÁRIA, que é pior que ping-pong com o Aurora."
        },
        {
          "item": "greeter — o portão de sudo torna o módulo inerte justamente na execução automática (e reconcilia a briga dos dois frentes)",
          "causa_ou_objetivo": "FATO QUE NENHUM DOS DOIS FRENTES MEDIU, e que resolve a contradição entre eles: `sudo -n -l` mostra `(ALL : ALL) ALL` com `timestamp_timeout=60`, `timestamp_type=global` e ZERO regras NOPASSWD em /etc/sudoers*. Ou seja, sudo -n NÃO é uma propriedade da máquina — é uma credencial em cache que expira em 60 min. O frente do greeter mediu 'sudo -n falha' (sessão sem cache, verdade lá) e o refutador mediu 'sudo -n funciona' (sessão com cache, verdade aqui: confirmei exit=0, uid=0). Os dois estão certos e os dois generalizaram errado. A PROVA do dano está em /home/vitoriamaria/.local/state/meowsystem/doctor.log:224 — 'greeter  não consigo olhar /var/lib/cosmic-greeter/.config/cosmic sem sudo' e :235 'greeter: falta dependência' — a rodada automática das 05:09 cai no exit 3 todo dia. scripts/greeter.sh:71-79 é o portão.",
          "correcao": "Separar DETECÇÃO de PERMISSÃO em /mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh. Detecção da máquina = `getent passwd cosmic-greeter` (campo 6 vira GREETER_HOME, nunca cravado) + readlink de /etc/systemd/system/display-manager.service; NÃO depende de sudo. O `sudo -n test -d` decide APENAS a fase_root (tema/CosmicTk), que devolve 3 parcial com a frase 'tema da tela de login pendente: precisa de sudo'; nunca aborta o módulo. A fase_avatar roda antes, sem sudo, e é a que entrega o ganho visível.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh"
          ],
          "risco": "O comentário de scripts/greeter.sh:66-70 ('um `[ -d ]` comum devolve falso por PERMISSÃO') continua válido e não pode ser perdido na refatoração: foi um erro já cometido uma vez neste projeto. A detecção nova por getent não substitui o cuidado — só tira o sudo do caminho crítico."
        },
        {
          "item": "greeter/tema — NÃO há trabalho pendente nesta máquina; o módulo já convergiu",
          "causa_ou_objetivo": "MEDIDO POR MIM com sudo: `diff -rq assets/temas/capturados/mocha-mauve/com.system76.CosmicTheme.{Dark,Light,Mode} /var/lib/cosmic-greeter/.config/cosmic/...` devolve VAZIO nas TRÊS árvores. Contagem: Dark 68 arquivos, Light 56, Mode 2. Isso refuta a premissa do cabeçalho de greeter.sh:6-11 ('estavam TODAS VAZIAS') como estado ATUAL — era verdade em 2026-08-05 antes de rodar, virou histórico depois. O frente do greeter listou isso como lacuna por não ter medido; o refutador acertou.",
          "correcao": "Nada a escrever no tema. Ajustar só o cabeçalho de scripts/greeter.sh para marcar a medição com data ('estavam vazias em 2026-08-05 até este script rodar; conferido idêntico à captura em 2026-08-05 16h'), e registrar em docs/COSMIC-THEMING.md §4e que o vidro do MeowSystem JÁ vale no login (frosted/frosted_panel/frosted_windows são chaves do TEMA, e o greeter faz bind de ExtBackgroundEffectManagerV1/OrgKdeKwinBlurManager).",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md"
          ],
          "risco": "Baixo. Cuidado só para não transformar a medição de hoje em afirmação atemporal — foi exatamente esse o erro dos dois frentes."
        },
        {
          "item": "greeter — a pasta tem OITO árvores, não cinco, e três delas NÃO são nossas",
          "causa_ou_objetivo": "MEDIDO POR MIM: `sudo -n ls -la /var/lib/cosmic-greeter/.config/cosmic/` lista CosmicComp, CosmicGreeter, CosmicSettings.Shortcuts, CosmicSettings.WindowRules, CosmicTheme.Dark, CosmicTheme.Light, CosmicTheme.Mode, CosmicTk. O achado original afirmou 'APENAS CINCO... Nada mais' e derivaria dali um restaurar por cópia de árvore. CosmicComp/v1/xkb_config está VIVO (1 arquivo, layout br) — é o TECLADO da tela de login, escrito pelo próprio greeter em runtime. E *com.system76.CosmicSettings.Shortcuts* é literalmente o glob que lib/comum.sh:60 recusa ('os atalhos de teclado são do Ritual da Aurora').",
          "correcao": "Backup/restaurar por ALLOWLIST explícita, nunca por cópia de árvore: só CosmicTheme.{Dark,Light,Mode} e as chaves de CosmicTk que nós escrevermos. Fora, nomeadas no código: CosmicGreeter/v1/* (last_user=Some(1000), users, e as 6 pastas .atomicwrite*), CosmicComp/v1/xkb_config, CosmicSettings.Shortcuts/v1, CosmicSettings.WindowRules/v1. E fechar o furo estrutural: greeter.sh escreve com `sudo install -D` e por isso NUNCA chama meow_destino_permitido — a trava 1 não é violada, é contornada. Todo destino passa a ser validado antes, mesmo indo por sudo.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/lib/comum.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow"
          ],
          "risco": "Restaurar xkb_config é mexer no teclado da ÚNICA tela onde ela digita a senha, sem GUI para consertar. Restaurar CosmicGreeter/v1/last_user apaga qual usuário/sessão vem pré-selecionado. Os dois são irreversíveis pela interface."
        },
        {
          "item": "greeter — o 'ficou pequeno' NÃO é resolvido por interface_density; o parafuso é a escala, e ela fica fora do v1",
          "causa_ou_objetivo": "MEDIDO POR MIM: /var/lib/cosmic-greeter/.local/state/cosmic-comp/outputs.ron tem scale: 1.0 em TODOS os conectores (DP-1, DP-2, HDMI-A-1); o dela em ~/.local/state/cosmic-comp/outputs.ron tem DP-1 scale: 1.05. É por isso que o login parece menor. E CosmicTk do greeter tem ZERO arquivos — o login roda no default do libcosmic, não no Compact dela (cat do CosmicTk dela: interface_density=Compact, header_size=Compact). Logo o argumento 'copiar o dela ENCOLHE o login' combate um plano que ninguém propôs. interface_font é (family, weight, stretch, style): NÃO tem campo de tamanho.",
          "correcao": "Não vender tamanho. (1) NÃO escrever outputs.ron: é estado vivo, reescrito pelo cosmic-comp, com chave de tupla (connector, make, model) — e aqui a MESMA TV aparece como DP-1 na sessão dela e DP-2/HDMI-A-1 no greeter, então o casamento de chave é frágil por construção. (2) O `meow doctor` passa a IMPRIMIR o diagnóstico honesto: 'a tela de login roda em escala 1.0 e o teu desktop em 1.05 — é por isso que ela parece menor; ajuste ainda não automatizado'. (3) interface_density/header_size entram como enfeite declarado (folgam espaçamento), com valor PRÓPRIO (Standard/Spacious), nunca cópia cega do Compact dela, e o README dizendo que NÃO aumentam fonte. (4) Manter a guarda do fc-list para interface_font (só family sob /usr/share/fonts ou /usr/local/share/fonts): uma family que só exista em ~/.local/share/fonts cai no fallback em silêncio — mesma armadilha do icon_theme do §4e, em chave nova. Fira Sans passa.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/meow.conf.exemplo",
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/migrar_conf.py",
            "/mnt/Apate/Desenvolvimento/MeowSystem/README.md"
          ],
          "risco": "Escrever CosmicTk e ela continuar achando pequeno = módulo que prometeu e não entregou. Melhor dizer a verdade medida do que entregar padding. Registrar também que o watcher de CosmicTk do greeter nunca sobe ('Failed to create watcher', em ambas as instâncias) — nada de CosmicTk vale ao vivo, só no próximo start."
        },
        {
          "item": "greeter/wallpaper — NÃO corrigir os comentários: eles estão certos. Só ACRESCENTAR o achado do daemon",
          "causa_ou_objetivo": "MEDIDO POR MIM com journalctl -b -1 -o json agrupado por _UID/_PID/_UNIT: 11 linhas de 'failed to read wallpaper' com _UID=1000, _PID=3112, em user@1000.service + cosmic-greeter.scope e session-4.scope (= TELA DE BLOQUEIO, dentro da sessão dela); e 2 linhas com _UID=0, _PID=1062, cosmic-greeter-daemon.service (= LOGIN). O comentário de scripts/greeter.sh:12-17 cita exatamente o PID 3112 e a atribuição certa. O achado original mandava 'corrigir o comentário do wallpaper.sh e do greeter.sh' — trocaria um fato verificado por um errado, dentro do arquivo cuja razão de existir é impedir isso. Os dois refutadores acertaram.",
          "correcao": "Manter os comentários de scripts/wallpaper.sh:133-145 e scripts/greeter.sh:12-17 intactos. ACRESCENTAR uma frase (não substituir): 'e o mesmo estado é lido pelo cosmic-greeter-daemon como root no boot — quando um caminho está morto, a TELA DE LOGIN também fica sem papel de parede (medido 2026-08-04 23:39:52, PID 1062, _UID=0, cosmic-greeter-daemon.service)'. Isso promove a limpar_estado_morto de higiene de journal para correção visível. O `meow doctor` ganha o aviso de caminho morto no estado — informativo, NUNCA divergência (o cosmic-bg reescreve o arquivo a cada 5 min a partir da memória, então qualquer 'conserto' volta atrás sozinho).",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/wallpaper.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md"
          ],
          "risco": "Nenhum na escrita. O risco é o inverso: se alguém aplicar o achado original como estava, o documento de fatos medidos passa a mentir sobre o emissor."
        },
        {
          "item": "svg — a arte do Boxy nunca saiu de ~/. Não é o renderizador, não é o cache, não é style=\"\"",
          "causa_ou_objetivo": "O achado original disse '(e)(ii) o cache explica 100%'. Isso contradiz a própria evidência 11 dele (nada em MeowSystem-Icons mudou depois de 09:40) e o refutador #1 varreu md5: ZERO cópias de coquinha.svg/mimir.svg em ~/.local/share/icons, ~/.config e no repo. Cache só morde quando o CONTEÚDO de um caminho JÁ RESOLVIDO muda; se nada foi instalado, não há cache a culpar. E o nome resolvido em runtime não é o que a tabela diz: o processo é `cosmic-panel-button com.system76.CosmicAppLibrary` (argv), não CosmicPanelAppButton — hoje salvo por acidente, porque os dois arquivos são byte-idênticos (md5 327acf7d…).",
          "correcao": "Duas etapas, nesta ordem. (a) INSTALAR a arte no caminho de fato resolvido: ~/.local/share/icons/MeowSystem-Icons/scalable/apps/com.system76.CosmicAppLibrary.svg (o nome do argv), mantendo com.system76.CosmicPanelAppButton.svg igual por simetria. (b) SÓ ENTÃO o cache do §3/§4c vira obstáculo: scripts/construir_icones.sh:197-203 continua CERTO em não reiniciar o painel, mas passa a IMPRIMIR quando o conteúdo mudou e o nome não: 'o gato do dock só troca no próximo login (ou `pkill -x cosmic-panel`, que pisca a tela)'. Dizer isso é o conserto; reiniciar sozinho já custou o painel dela duas vezes.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/construir_icones.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md"
          ],
          "risco": "Reiniciar o cosmic-panel PISCA a tela dela e é proibido sem autorização. O aviso impresso é a única saída honesta. Cuidado com construir_icones.sh:103-119 (bug já documentado): qualquer lista de variantes tem de ser derivada do disco, nunca fixa, senão nasce um segundo dono da mesma linha."
        },
        {
          "item": "svg — sanear_svg.py: manter a ferramenta, JOGAR FORA a regra que a justificava",
          "causa_ou_objetivo": "VERIFIQUEI EU MESMO, com o teste A/B decisivo. a.svg = <g fill=\"#ff0000\"><path style=\"\" …>, b.svg = idêntico sem o style=\"\": rsvg-convert produz md5 IDÊNTICO (eeeba7a00226438b7498376eecdf2a09) e pixel central (255,0,0,255) nos DOIS — o vermelho do pai atravessa o style vazio. c.svg, sem fill em ancestral nenhum, dá (0,0,0,255). CONCLUSÃO: `style=\"\"` é NO-OP puro; o preto vem do valor INICIAL de fill quando ninguém declara cor. O achado original ('style vazio cai no default do SVG, que é preto puro' → 'buraco invisível' → commit 29fa004) está errado no mecanismo e no exemplo: conferi o inventário de cores e os 4 `style=\"\"` do mimir são exatamente os 4 que na coquinha valem rgb(122,124,140) — são as FEIÇÕES, desenhadas sobre o corpo rgb(95,103,119) do mimir. Quem tem risco de contraste é a coquinha (corpo rgb(7,13,24), quase o crust #11111b) — e a coquinha tem ZERO style=\"\". Mecanismo e exemplo anticorrelacionados.",
          "correcao": "Criar /mnt/Apate/Desenvolvimento/MeowSystem/scripts/sanear_svg.py (só stdlib, xml.etree; entrada=arquivo, saída=stdout, --conferir devolve 1 se mudaria). REGRAS QUE FICAM: (b) garantir width/height derivados do viewBox — MEDIDO: `grep -c 'width='` = 0 nos DOIS arquivos dela; (d) podar sodipodi:/inkscape:/boxy:/data-* e <metadata>; (c) recusar <text> sem font-family embutida, <style> com seletor complexo e BackgroundImage/BackgroundAlpha como in= de filtro — as três mensagens existem literalmente no binário. REGRA REESCRITA: `style=\"\"` vira PODA COSMÉTICA declarada como tal ('nenhum pixel muda', comprovado por md5 de render), e é PROIBIDO recusar o arquivo por causa dela — a regra original (recusa se não tiver fill=) rejeitaria o mimir.svg, que é a arte que motivou a ferramenta e que desenha perfeitamente. REGRA NOVA, que é o sintoma real: RELATÓRIO (nunca erro) dos elementos cuja cor efetiva é o preto inicial, mais um teste de CONTRASTE do maior caminho por área contra base #1e1e2e e crust #11111b — reaproveitando a lógica de silhueta do commit 29fa004. É a coquinha que reprovaria. Regra do -symbolic: contar cores RESOLVIDAS ignorando `none` e `currentColor`, senão assets/meow-symbolic.svg é acusado sem motivo.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/sanear_svg.py",
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/construir_icones.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/install.sh"
          ],
          "risco": "A prova de render usa rsvg-convert, e quem desenha no painel é resvg — um arquivo pode passar num e falhar no outro. Declarar isso no doc: a checagem detecta 'ficou vazio', não 'ficou igual ao que o COSMIC mostra'. E a dependência precisa ser MEOW_SEM_DEPENDENCIA (3), nunca falha dura: install.sh:315-332 só sabe apt-get, e a regra 6 exige valer em qualquer COSMIC."
        },
        {
          "item": "svg — construir_icones.sh escreve numa chave que NINGUÉM lê: o applet da logo não está instalado",
          "causa_ou_objetivo": "MEDIDO POR MIM: `flatpak list | grep -i logo` vazio (rc=1); `command -v cosmic-ext-applet-logomenu` não acha; nenhum .desktop de logomenu em /usr/share/applications; e li os QUATRO plugins_* — Panel/wings = [Battery, Tiling, dev.cappsy.CosmicExtAppletDrives, Workspaces] + [StatusArea, clipboard-manager, Bluetooth, Audio, Network, Power]; Panel/center = [Eyedropper, Time, Notifications]; Dock/wings = ([CosmicPanelAppButton], []); Dock/center = [CosmicAppList]. LogoMenu não aparece em nenhum. Mas ~/.config/cosmic/dev.cappsy.CosmicExtAppletLogoMenu/v1/custom_logo_path existe e vale \"…/logos/meow-mocha.svg\": o projeto escreve no vazio. Este é o bug real desta frente, e o achado original o classificou como 'configuração morta' sem tirar a consequência.",
          "correcao": "scripts/construir_icones.sh PULA o bloco da logo quando o applet não existe (teste: .desktop OU binário OU o id em algum plugins_*), imprimindo 'o applet da logo não está instalado — o gato do painel é só o do dock'. O truque de nome-com-hash + limpeza de órfãos (que de fato converge, e é a única forma de a arte aparecer AO VIVO, já que a CHAVE troca e o ARQUIVO não) fica guardado atrás do mesmo teste de presença, para quando o applet existir; a limpeza casa só meow-<flavor>-*.svg derivado do disco e JAMAIS toca gato-pop.svg, que é do Ritual da Aurora (§5). CORTAR a opção LOGO_NO_PAINEL=1: escrever em plugins_wings o id de um applet ausente reproduz o 'painel fantasma' (applet configurado que nunca vira processo é o sinal 2 do aurora-painel-fantasma.sh), numa máquina de UMA tela.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/construir_icones.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md"
          ],
          "risco": "plugins_wings é território do Ritual da Aurora pelo §5 ('a ordem dos applets é dela'). Mexer ali sem o applet instalado arrisca um painel que não sobe — e o conserto exige a tela que está em uso."
        },
        {
          "item": "svg/bandeja e applets — os dois mecanismos do achado original estão errados, e um deles esconde uma oportunidade real",
          "causa_ou_objetivo": "BANDEJA (D3): MEDIDO POR MIM — o watcher tem TRÊS itens, não dois: as 3 \":1.380\" \":1.567\" \":1.643/org/ayatana/NotificationItem/hefesto_dualsense4unix\". E o terceiro devolve IconName = s \"hefesto-dualsense4unix\" — PREENCHIDO, resolvendo em MeowSystem-Icons. Logo 'a bandeja não passa por tema... não alcançável' é falso: o app DELA é o contraexemplo, e é o próprio projeto que já o veste. APPLETS (D2): confirmei que /usr/bin/cosmic-applet-battery e -power são SYMLINKS para um único cosmic-applets; o `Icon=<id>-symbolic` do .desktop é metadado do cosmic-settings, e sombrear .desktop (o remédio implícito do achado) não repintaria pixel nenhum.",
          "correcao": "Doc, não código, exceto por uma decisão a tomar. (1) Nova §4g em docs/COSMIC-THEMING.md — 'a bandeja é IconName OU IconPixmap': quem manda IconName passa pelo tema (Hefesto = padrão-ouro, e todo app do projeto que ponha bandeja DEVE emitir IconName); quem manda IconPixmap (qBittorrent e ZapZap, IconName=\"\") é inalcançável. O `meow doctor` diz isso quando ela perguntar por que o ZapZap continua verde, em vez de deixá-la achar que é bug nosso. O Icon=meow-whatsapp continua valendo para o dock/lançador, que é onde já funciona. (2) §3 — o applet pinta nomes de ESTADO cravados no binário (battery-symbolic, network-wireless-signal-*-symbolic, display-brightness-symbolic, bluetooth-{active,disabled}-symbolic), que hoje vencem em Papirus-Dark/symbolic/{devices,status}. DECISÃO A TOMAR, não a executar às cegas: o tema PODERIA cobrir o painel declarando symbolic/devices e symbolic/status no nosso index.theme — mas continua achatado numa cor só (.symbolic(path.contains(\"-symbolic.svg\"))), então o ganho é 'silhueta Catppuccin', jamais 'logo colorida'. Escrever a escolha no §3 antes de plantar arquivo.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md",
            "/mnt/Apate/Desenvolvimento/MeowSystem/README.md"
          ],
          "risco": "Plantar nomes de ESTADO (battery-symbolic etc.) em MeowSystem-Icons sombreia o Papirus-Dark para o sistema inteiro, não só para o painel — um ícone de bateria errado é pior que um genérico. Não medi quantos nomes de estado existem nem se algum é usado fora do painel. Fazer só depois de inventário completo."
        },
        {
          "item": "svg/auditor — --conferir enxuto: as quatro asserções propostas transformariam uma auditoria 100% limpa em divergência permanente",
          "causa_ou_objetivo": "Estado atual medido: /home/vitoriamaria/.local/state/meowsystem/meow.log das 16:08 registra 'visiveis=51 pareados=51 sem=0'. As asserções A/B/C/E do plano original acusam coisas DELIBERADAS ou de OUTRO DONO — confirmei uma a uma: (A) Icon=steam do Scarlet Deer Inn foi a Steam que escreveu e jogos_steam.sh:100 copia byte a byte, declarando na linha 30 'Este script não escreve ícone nenhum'; (B) ~/.local/share/icons/steam-jogos é do Aurora (steam-gera-atalhos.sh:21, e a linha 8 diz que a pasta 'é APAGADO e recriado do zero a cada execução') e os .desktop dele usam Icon=<caminho absoluto>, que o próprio auditar_icones.sh:436,550 já pula; (C) thunderbird.png é deliberado (completar_icones.sh:178, mapa APELIDO) e RESOLVE; (E) com.vitoriamaria.HefestoDualsense4Unix NÃO é órfão — /usr/share/applications/com.vitoriamaria.HefestoDualsense4Unix.desktop existe e pede exatamente Icon=com.vitoriamaria.HefestoDualsense4Unix. E o contrato do bin/meow converte isso em mentira diária: linha 348 VERIFICAVEIS, 349 SEM_CONSERTO=\" conf \", 568 saida=\"$(\"fix_$nome\")\" — conferência sem fix_ cai em comando inexistente → 127 → falhou=1 → return MEOW_ERRO (2), que NÃO está no SuccessExitStatus=1 3 do meow-doctor.service:78. Unidade vermelha todo dia. Com um fix_ que não conserta nada é pior: saída 1 → o ExecStopPost:94 avisa 'o auto-reparo corrigiu' sem ter corrigido, exatamente o que o cabeçalho da unidade (linhas 13-14) proíbe.",
          "correcao": "Em scripts/auditar_icones.sh, o --conferir só recebe asserções que o PROJETO possa satisfazer: (D) todo SVG do tema passa no sanear_svg.py --conferir; (nova) todo Icon= que completar_icones.sh promete entregar está no tema; (nova) CosmicTk/v1/icon_theme aponta para o nosso tema e o index.theme descreve o disco (§4d); (nova) nenhum arquivo termina em -symbolic.svg com mais de uma cor resolvida. A/B/C/E saem do --conferir e viram, no máximo, linhas informativas de `meow icones auditar` (comando de mão, sem código de saída venenoso). REGRA DE OURO a escrever no bin/meow: só entra em VERIFICAVEIS o que tem fix_<nome> que de fato converge; antes de ligar no doctor, rodar duas vezes e provar rc=1 depois rc=0.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/auditar_icones.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow",
            "/mnt/Apate/Desenvolvimento/MeowSystem/systemd/"
          ],
          "risco": "Uma divergência que nunca conserta ensina a usuária a ignorar o aviso do auto-reparo — o dano é permanente e silencioso. O auditor é leitura pura hoje (auditar_icones.sh:186-187 é mktemp -d com trap) e precisa continuar."
        },
        {
          "item": "steam/Scarlet Deer Inn — conserto de uma linha, e NÃO é index.theme",
          "causa_ou_objetivo": "MEDIDO POR MIM: steam-1553260.desktop pede Icon=steam; `find ~/.local/share/icons -name 'steam_icon_1553260*'` não acha nada; e ~/.local/share/icons/hicolor/256x256/apps/ já tem 10 arquivos steam_icon_*. O achado original enquadrou como armadilha do §4d (falta index.theme em steam-jogos) — errado: NENHUM .desktop pede steam_jogo_*, então index.theme ali não muda um pixel; é pasta de estoque, não tema quebrado. É (iii) puro: nome pedido ≠ nome do arquivo.",
          "correcao": "Plantar a capa com o nome que o resolvedor procurará, em vez de editar o .desktop (a Steam reescreve o arquivo, e jogos_steam.sh copia byte a byte — editar a cópia é ping-pong interno garantido). A pasta 256x256/apps já está declarada no index.theme do hicolor do usuário e já é onde moram os 12 steam_icon_* existentes. MAS: scripts/jogos_steam.sh:30 declara 'Este script não escreve ícone nenhum' — mudar isso é mudar a promessa do script, então ou entra atrás de opção explícita no meow.conf, ou vira uma linha do `meow doctor` dizendo o comando exato para ela rodar. NÃO criar index.theme em steam-jogos: trabalho com efeito zero.",
          "arquivos": [
            "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/jogos_steam.sh",
            "/mnt/Apate/Desenvolvimento/MeowSystem/meow.conf.exemplo",
            "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md"
          ],
          "risco": "steam-gera-atalhos.sh (Aurora) apaga e recria ~/.local/share/icons/steam-jogos inteiro a cada execução; se o destino escolhido for lá dentro, some. Por isso o destino é hicolor/256x256/apps, que é nosso. E o .desktop de origem é território da Steam."
        }
      ],
      "nao_medido": [
        "RENDER NO MOTOR CERTO: não existe CLI de resvg nesta máquina e instalar é proibido. Todos os renders (meus e dos três frentes) são librsvg, que NÃO é o motor do painel. A afirmação 'o resvg também desenha esses dois' é inferida do conjunto de features vendorizadas, não de render lado a lado. O teste honesto sem instalar nada: plantar o SVG num nome que o painel realmente resolve e reiniciar o painel UMA vez — o que pisca a tela e precisa de autorização.",
        "ORDEM DA CADEIA DE TEMAS: os dois frentes mediram ordens DIFERENTES para a mesma máquina (um: MeowSystem-Icons→Papirus-Dark→breeze-dark→Cosmic→Adwaita→hicolor→Pop…; outro: …→breeze-dark→breeze→hicolor→Cosmic→Pop→…→Adwaita). Não reproduzi nenhuma das duas. A divergência levanta a suspeita, já registrada, de que a ordem venha de um HashMap e não seja determinística entre execuções. Nada que dependa da ordem deve ser decidido antes de um strace novo.",
        "AVATAR SVG NA TELA DE LOGIN: não medi se o SVG realmente desenha. O binário tem resvg vendorizado E a crate image sem codec svg; não dá para isolar qual caminho o icon_opt percorre sem rodar o greeter, que é a tela dela. O SVG está em produção desde 04/08 23:54 e o boot de hoje não gerou erro de decodificação no journal — indício, não prova.",
        "CONTEÚDO DO GetUserData: a política D-Bus (/usr/share/dbus-1/system.d/com.system76.CosmicGreeter.conf) só aceita root e o grupo cosmic-greeter, e `getent group cosmic-greeter` não tem membros. Seria a prova definitiva do que o daemon transporta (papel de parede, tema, avatar, display config). Exige `sudo busctl --system call`.",
        "COSMIC_SCALE: o literal existe no binário, mas o greetd monta o ambiente da sessão do zero e cosmic-greeter-start não exporta nada. Não medi propagação. Não usar sem medir.",
        "QUANTOS NOMES DE ESTADO o cosmic-applets pede ao todo, e se algum deles é usado fora do painel. Sem esse inventário, plantar symbolic/devices+symbolic/status em MeowSystem-Icons é sombrear o Papirus-Dark às cegas.",
        "SE A BANDEJA PREFERE IconName quando ele não é vazio: hoje os dois itens problemáticos mandam IconName=\"\", então a questão é acadêmica aqui. E não verifiquei se o ZapZap tem opção própria de trocar o ícone da bandeja — seria o único caminho para ele.",
        "O QUE A TELA DE LOGIN MOSTRA ANTES de um usuário ser selecionado: como só há um usuário, ele já vem pré-selecionado (last_user=Some(1000)) e a distinção pode nunca aparecer na prática dela.",
        "CASAMENTO DE TAMANHO na resolução de ícone: os resolvedores usados devolvem a primeira seção de Directories= que contenha o arquivo, ignorando a proximidade do tamanho pedido. O arquivo vencedor POR TEMA está certo; a subpasta relatada dentro do Papirus pode não ser a real."
      ],
      "perguntas_abertas": [
        "QUAL DOS TRÊS SINTOMAS a frase 'o svg da dock do boxy não funciona em dock' descreve? Ela nunca instalou coquinha.svg/mimir.svg em lugar nenhum (md5 varrido: zero cópias em ~/.local/share/icons, ~/.config e no repo), então não há rastro do que tentou. As três leituras estão cobertas no plano, mas a ordem de execução muda conforme a resposta.",
        "O gato do painel: ela QUER o applet dev.cappsy.CosmicExtAppletLogoMenu instalado, ou aceita que o gato seja só o do dock (que é o que existe hoje e funciona)? Instalar applet é escrita nova e mexe em plugins_wings, que o §5 dá ao Ritual da Aurora.",
        "Painel: 'logo colorida no applet' é impossível por construção (.symbolic achata numa cor). O projeto quer trocar isso por 'silhueta Catppuccin no painel' — que exige declarar symbolic/devices e symbolic/status no nosso index.theme e plantar nomes de estado — ou desiste do painel e escreve o limite no README?",
        "Avatar: trocar o SVG que já está em produção por PNG rasterizado é a escolha conservadora (a crate image do greeter não tem codec svg), mas o SVG pode estar funcionando. Trocar por precaução, ou medir primeiro no próximo login?",
        "Scarlet Deer Inn: o jogos_steam.sh promete por escrito 'não escreve ícone nenhum'. Quebrar a promessa atrás de uma opção no meow.conf, ou o doctor só imprimir o comando de uma linha para ela rodar quando quiser?",
        "'A tela de login ficou pequena': aceita a resposta honesta (escala 1.0 no login contra 1.05 no desktop, ajuste não automatizado) ou quer que o projeto assuma o outputs.ron do greeter — que é estado vivo, reescrito pelo cosmic-comp, e cujo erro deixa a tela de login em escala errada sem GUI para desfazer?",
        "qBittorrent manda IconPixmap cru. O §5 diz que o tema do qBittorrent é do Aurora — chamamos o script dele, ou isso fica fora do escopo do MeowSystem?"
      ],
      "precisa_autorizacao": [
        "SUDO (senha), para a fase_root do greeter: escrever CosmicTheme.{Dark,Light,Mode} e CosmicTk em /var/lib/cosmic-greeter/.config/cosmic. IMPORTANTE — MEDI QUE ISSO NÃO É ESTÁVEL: `sudo -n -l` mostra (ALL:ALL) ALL com timestamp_timeout=60 e ZERO NOPASSWD, ou seja o `sudo -n` que funciona agora é credencial em cache de 60 min. Foi exatamente isso que fez os dois frentes anteriores se contradizerem, e é por isso que doctor.log:224 registra 'greeter: não consigo olhar sem sudo' na rodada automática das 05:09. O padrão do etapa_pacotes se aplica: montar a lista completa ANTES, imprimir 'vou rodar como root, e só isto:' e executar numa única janela de credencial. NESTA MÁQUINA, HOJE, essa fase não tem trabalho: o tema já está byte-idêntico à captura.",
        "PISCAR A TELA (pkill -x cosmic-panel): é o ÚNICO jeito de a arte nova do dock aparecer sem relogin — o painel mantém 6 watches de inotify e nenhum sobre arquivo de ícone. O plano NÃO reinicia nada; imprime o comando e deixa a decisão com ela. Reiniciar o painel já custou o painel dela duas vezes.",
        "TROCAR O AVATAR DE LOGIN dela (SetIconFile): não precisa de sudo (polkit allow_any=yes), mas muda uma coisa que ela vê todo dia e que hoje já é o gato. Precisa do 'sim' dela como preferência, mais a guarda que impede o timer diário de desfazer uma escolha futura dela no Settings.",
        "ESCREVER EM ~/.local/share/icons/hicolor/256x256/apps/ (capa do Scarlet Deer Inn): é território nosso, mas quebra a promessa escrita em jogos_steam.sh:30 de que o script não escreve ícone nenhum. Só atrás de opção explícita no meow.conf.",
        "REDE: nenhuma. Nada no plano instala pacote. A dependência de rsvg-convert já está satisfeita (/usr/bin/rsvg-convert) e, onde faltar, o caminho é MEOW_SEM_DEPENDENCIA (3), nunca falha dura — install.sh:315-332 só sabe apt-get e a regra 6 exige valer em qualquer COSMIC.",
        "NÃO PEDIDO E NÃO NECESSÁRIO: mexer em plugins_wings (§5, é da Aurora), em CosmicComp/v1/xkb_config (o teclado da tela de login), em CosmicSettings.Shortcuts (trava 1 do lib/comum.sh) e em outputs.ron (estado vivo). Todos ficam explicitamente fora."
      ],
      "resumo_executivo": "Os DOIS achados originais foram corretamente refutados no que importa, e eu confirmei as duas refutações medindo por conta própria os dois fatos mais decisivos.\n\nFATO 1 — `style=\"\"` é NO-OP puro, não \"preto puro\". Teste A/B controlado que rodei: `<g fill=\"#ff0000\"><path style=\"\">` e o mesmo sem o atributo produzem PNG de md5 IDÊNTICO (eeeba7a00226438b7498376eecdf2a09), pixel central (255,0,0,255) nos dois — o vermelho do pai atravessa o style vazio. Só quando NENHUM ancestral declara fill o pixel sai (0,0,0,255), pelo valor inicial do CSS. Isso derruba a regra 1a, que era a razão de existir do saneador, e mostra que ela rejeitaria justamente o `~/mimir.svg` — a arte que motivou a ferramenta e que desenha perfeitamente. Conferi ainda o inventário de cores: os 4 `style=\"\"` do mimir são as FEIÇÕES sobre um corpo claro rgb(95,103,119); quem tem risco de sumir no escuro é a coquinha (corpo rgb(7,13,24), quase o crust #11111b) — e ela tem ZERO `style=\"\"`. Mecanismo e exemplo estavam anticorrelacionados.\n\nFATO 2 — a tela de login: `sudo -n` devolve exit=0/uid=0 aqui, e com ele medi que a pasta do greeter tem OITO árvores (não cinco), que `CosmicTk` está VAZIA (0 arquivos), que `CosmicTheme.{Dark,Light,Mode}` (68/56/2 arquivos) é BYTE-IDÊNTICA à captura `assets/temas/capturados/mocha-mauve` — ou seja, a parte \"tema\" do greeter já está FEITA — e que a escala do login é 1.0 contra 1.05 do desktop dela, que é a resposta real ao \"ficou pequeno\". Confirmei também, por `journalctl -o json` agrupado por _UID, que 11 das 13 linhas de \"failed to read wallpaper\" saem do uid 1000 em cosmic-greeter.scope: os comentários de `wallpaper.sh` e `greeter.sh` estão CERTOS e o achado original queria \"corrigi-los\" para o lado errado, dentro do arquivo cuja razão de existir é impedir isso.\n\nONDE OS DOIS REFUTADORES TAMBÉM ERRARAM (fato que nenhum dos três frentes mediu): `sudo -n -l` mostra `(ALL : ALL) ALL` com `timestamp_timeout=60` e ZERO regras NOPASSWD. O `sudo -n` não é propriedade da máquina — é credencial em cache que expira em 60 minutos. O frente do greeter mediu \"falha\" (sessão sem cache) e o refutador mediu \"funciona\" (sessão com cache): os dois estavam certos e os dois generalizaram. A prova do dano está em `doctor.log:224` — a rodada automática das 05:09 cai em exit 3 todo dia, e o módulo greeter é INERTE justamente na execução que deveria importar. Por isso o plano separa detecção de permissão e põe o avatar (que não precisa de sudo) antes de qualquer portão.\n\nO PLANO, em ordem de pagamento: (1) pular o bloco da logo em `construir_icones.sh` — medi que o applet LogoMenu NÃO está instalado (sem flatpak, sem binário, sem .desktop, ausente dos quatro `plugins_*`), então `custom_logo_path` é chave sem leitor e o projeto escreve no vazio; (2) o módulo do avatar, único entregável realmente pendente do greeter — o gato já está em `/var/lib/AccountsService/icons/vitoriamaria` com md5 idêntico ao asset, mas `grep -rln AccountsService` no repo devolve VAZIO: ninguém é dono, e numa máquina limpa ele não existe; (3) `sanear_svg.py` com as regras que sobreviveram (width/height, poda de namespace, as três recusas do usvg) mais a que realmente resolve o sintoma \"sumiu\" — teste de contraste contra base/crust; (4) `--conferir` ENXUTO no auditor, sem as quatro asserções que acusariam coisas deliberadas ou de outro dono e que, pelo contrato do `bin/meow` (linha 568 → 127 → falhou=1 → saída 2, fora do `SuccessExitStatus=1 3`), deixariam a unidade vermelha ou mentindo \"o auto-reparo corrigiu\" todo dia; (5) doc.\n\nNada no plano reinicia processo, abre janela ou usa rede. O que precisa do \"sim\" dela está listado à parte."
    },
    "resultados": [
      {
        "tema": "svg-dock-applets",
        "resultado": {
          "causa_ou_achado": "São TRÊS sintomas com TRÊS mecanismos distintos, e nenhum deles é o renderizador de SVG.\n\n=== (a) QUEM RENDERIZA — MEDIDO ===\n`cosmic-panel`, `cosmic-app-list` e `cosmic-panel-button` embutem **resvg + usvg + tiny-skia + svgtypes + simplecss + fontdb**, VENDORIZADOS no build `cosmic-applets-1.0.15~1785249881~24.04~ec8ffdc`. Zero ocorrências de `rsvg`/`librsvg`. A resolução de nome é da `cosmic-freedesktop-icons` (5 ocorrências), e a decisão \"symbolic\" é da `libcosmic/src/icon_theme.rs`.\nMas o resvg desta build é COMPLETO: estão presentes `usvg/src/parser/{filter,mask,clippath,use_node,marker,switch,image,text}.rs` e todas as primitivas `resvg/src/filter/*` (blur, composite, color_matrix, turbulence, lighting…). Ou seja: filtro, máscara, clipPath, `<use>`, `<image>` e `<text>` são SUPORTADOS. As únicas limitações que o binário confessa em texto são: `\" rule is not supported. Skipped.\"` e `\"' is not supported. Selector skipped.\"` (CSS do `<style>` via simplecss), `\"- filter input isn't supported and not planed.\"` (BackgroundImage/BackgroundAlpha), `\"Warning: unsupported blend mode:\"`, `\"No fonts with a … font-family.\"` e `\"Unsupported fontdb Source::File(\"`.\n\n=== (b)+(c) OS DOIS SVG DO BOXY — RENDERIZAM ===\n`~/coquinha.svg` (4137 B, 15:29) e `~/mimir.svg` (4058 B, 15:30) são o MESMO desenho (gato sobre arco Catppuccin Macchiato) com cores diferentes. Rasterizados com librsvg a 256px: **ambos saem perfeitos**. Não têm `<filter>`, `<mask>`, `<use>`, `<image>`, `<text>`, `<style>` nem namespace de editor. São 100% `<path>` + `<g transform>`. Não há nada ali que o resvg recuse.\nDois defeitos REAIS, porém, e os dois são assinatura do Boxy:\n  1. **`style=\"\"` — 4 ocorrências em mimir.svg, 0 em coquinha.svg.** Quando ela apaga o preenchimento no Boxy, ele grava `style=\"\"`, e `style` vazio NÃO é \"invisível\": cai no default do SVG, que é **preto puro**. No render, os olhos/boca do mimir saem pretos. Sobre o painel escuro (`#1e1e2e`/`#11111b`) isso é um buraco invisível — exatamente o sintoma do commit 29fa004 (\"os oito ícones sumiam sobre papel de parede claro\").\n  2. **Nenhum dos dois tem `width`/`height`** no `<svg>` — só `viewBox=\"0 0 1024 1024\"`. librsvg tolera; é o primeiro atributo a garantir num saneador.\nCONCLUSÃO: \"o SVG do Boxy não funciona no dock\" **não é o renderizador**. É o CACHE, e isso já está medido no §3/§4c do doc: o `cosmic-panel` mantém exatamente 6 watches de inotify (medido agora em `/proc/34221/fdinfo/*`), nenhum sobre arquivo de ícone nem sobre `CosmicTk/icon_theme`; e o `construir_icones.sh` (linhas 197-203) se RECUSA a reiniciar o painel quando só o conteúdo do ícone muda. Reescrever `meow-mocha.svg` ou `com.system76.CosmicPanelAppButton.svg` com a arte do Boxy não muda NADA até o próximo login. O painel dela foi reiniciado às 15:36:44 (junto com `cosmic-notifications` — o reparo do §4f), 6 min depois de ela salvar o mimir; nenhum arquivo em `~/.config/cosmic` nem em `MeowSystem-Icons` foi tocado depois das 09:40. Ou seja: os dois SVG **nunca chegaram a ser instalados em lugar nenhum**.\n\n=== (d) \"AS LOGOS DOS APPLETS NÃO REFLETEM AS DOS DOCKS\" — TRÊS CAUSAS INDEPENDENTES ===\nD1. **O applet da logo NÃO ESTÁ NO PAINEL.** `grep -rn LogoMenu ~/.config/cosmic/com.system76.CosmicPanel*` devolve VAZIO. `plugins_wings` do Panel = Battery, Tiling, Drives, Workspaces; `plugins_center` = Eyedropper, Time, Notifications; direita = StatusArea, clipboard, Bluetooth, Audio, Network, Power. Logo, `dev.cappsy.CosmicExtAppletLogoMenu/v1/custom_logo_path = \"…/logos/meow-mocha.svg\"` é **configuração morta**: não há gato nenhum no painel. Confirmado por captura de tela: o painel superior esquerdo tem 4 ícones brancos e os workspaces \"Meow\"/\"OS\", sem logo.\nD2. **11 dos 13 applets pedem nome terminado em `-symbolic`** (medido lendo `Icon=` de cada `.desktop`). E a `libcosmic` faz `.symbolic(path.contains(\"-symbolic.svg\"))` (§3; o literal `-symbolic`/`.symbolic` está no binário). Um `-symbolic.svg` é **achatado numa cor só** — logo colorida ali é impossível por construção. Pior: os 11 nomes resolvem para `/usr/share/icons/hicolor/scalable/apps/*-symbolic.svg` com **1 único candidato cada**, isto é, `MeowSystem-Icons` não tem NENHUM `*-symbolic.svg` e não participa da resolução dos applets.\nD3. **A bandeja não passa por tema nem por `.desktop`.** Os dois `StatusNotifierItem` vivos (`:1.380` qBittorrent, `:1.567` ZapZap) têm `IconName = \"\"` e `IconPixmap` com 51.535 e 58.313 bytes de ARGB cru. O applet desenha o **bitmap que o app manda pelo D-Bus**. Por isso o ZapZap continua verde-WhatsApp com o balão vermelho \"56\" na bandeja, mesmo com `Icon=meow-whatsapp` já gravado no `.desktop` e com `meow-whatsapp.svg` já em Catppuccin (`#A6E3A1` + `#1E1E2E`, conferido).\n\n=== A TABELA (entrada · nome pedido · arquivo que vence · tema · Catppuccin?) ===\nDOCK — botão:\n  AppButton      · com.system76.CosmicPanelAppButton · ~/.local/share/icons/MeowSystem-Icons/scalable/apps/com.system76.CosmicPanelAppButton.svg · MeowSystem-Icons · SIM (gato roxo, confere com a captura)\n  (mesmo arquivo, md5 327acf7d…, em CosmicAppLibrary.svg e em ~/.config/cosmic/logos/meow-mocha.svg)\nDOCK — favoritos (`CosmicAppList/v1/favorites`):\n  Arquivos       · com.system76.CosmicFiles · MeowSystem-Icons/scalable/apps/com.system76.CosmicFiles.svg · MeowSystem-Icons · SIM\n  Hefesto        · hefesto-dualsense4unix   · MeowSystem-Icons/scalable/apps/hefesto-dualsense4unix.svg · MeowSystem-Icons · SIM\nDOCK — janelas abertas:\n  qBittorrent    · org.qbittorrent.qBittorrent · /usr/share/icons/Papirus-Dark/<tam>/apps/… → link p/ Papirus/…/qbittorrent.svg · Papirus-Dark · NÃO\n  Terminal       · com.system76.CosmicTerm  · MeowSystem-Icons/scalable/apps/com.system76.CosmicTerm.svg · MeowSystem-Icons · SIM\n  Chrome         · google-chrome            · Papirus-Dark/<tam>/apps/google-chrome.svg · Papirus-Dark · NÃO\nPAINEL — applets (todos os 11 com `-symbolic`):\n  Bateria/Tiling/Drives/Eyedropper/Relógio/Notificações/Clipboard/Bluetooth/Áudio/Rede/Energia · <id>-symbolic · /usr/share/icons/hicolor/scalable/apps/<id>-symbolic.svg · hicolor (ÚLTIMO elo) · NÃO — e achatado numa cor\n  Workspaces     · com.system76.CosmicAppletWorkspaces · (mostra texto \"Meow\"/\"OS\", não ícone) · — · n/a\n  StatusArea     · com.system76.CosmicAppletStatusArea → mas o que aparece é o `IconPixmap` dos filhos · D-Bus · NÃO alcançável\nOUTROS `.desktop` separados pelo projeto:\n  WhatsApp/ZapZap · meow-whatsapp · MeowSystem-Icons/scalable/apps/meow-whatsapp.svg · MeowSystem-Icons · SIM (mas a bandeja ignora — D3)\n  FogStripper    · fogstripper   · MeowSystem-Icons/scalable/apps/fogstripper.svg · MeowSystem-Icons · SIM\n  Thunderbird    · thunderbird   · MeowSystem-Icons/scalable/apps/**thunderbird.png** · MeowSystem-Icons · SIM, mas é PNG dentro de `scalable/apps` (`Type=Scalable`) — frágil\n  Boxy SVG       · com.boxy_svg.BoxySVG · Papirus-Dark/<tam>/apps/ → Papirus/<tam>/apps/boxy-svg.svg · Papirus-Dark · NÃO (mas é o desenho certo; conferido lado a lado com o SVG exportado pelo flatpak)\nJOGOS DA STEAM (7 de 8 certos):\n  FAITH, Stray, Sackboy, Mullet Mad Jack, Mad King, Dark Scrolls, ORPHEUS · steam_icon_<appid> · ~/.local/share/icons/hicolor/<tam>/apps/*.png · hicolor · natural, por decisão\n  **Scarlet Deer Inn (1553260) · `Icon=steam` · Papirus-Dark/…/steam.svg · Papirus-Dark · ERRADO** — mostra o logo genérico da Steam. Não existe `steam_icon_1553260` em lugar nenhum; a capa existe, em `~/.local/share/icons/steam-jogos/steam_jogo_1553260.png` (63.592 B).\n\n=== (e) QUAL DAS TRÊS HIPÓTESES EXPLICA CADA DIVERGÊNCIA ===\n(i) diretório não declarado no index.theme → **`~/.local/share/icons/steam-jogos/` não tem `index.theme` NENHUM**: 17 capas (`steam_jogo_*.png`, 1,2 MB) totalmente invisíveis ao resolvedor. É a armadilha do §4d na sua forma extrema. E o `~/.local/share/icons/hicolor/index.theme` regenerado pelo `hicolor.sh` descreve só o que existe NAQUELA base — as bases `.../flatpak/exports/share/icons/hicolor` têm `36x36`, `72x72`, `*@2/apps`, `*/mimetypes` e `symbolic/apps`, que aquela lista não cobre (só não dói hoje porque o flatpak mantém `index.theme` próprio de 55.507 B naquela base).\n(ii) cache do painel → **explica 100% do \"o SVG do Boxy não funciona\"**. Sem trocar a CHAVE (`custom_logo_path`) ou sem relogin, arquivo reescrito não aparece.\n(iii) nome do `.desktop` ≠ nome do arquivo → **`Icon=steam` vs `steam_jogo_1553260.png`**; e o órfão `com.vitoriamaria.HefestoDualsense4Unix.svg` (md5 6d681591… idêntico ao `hefesto-dualsense4unix.svg`), que nenhum `.desktop` da máquina pede.\nE uma quarta, que não estava na lista e é a maior: **o mecanismo errado** — applet `-symbolic` (achatado) e bandeja por `IconPixmap` (D-Bus) não passam por tema de ícones de jeito nenhum.",
          "confianca": "forte",
          "evidencias": [
            {
              "comando": "for b in /usr/bin/cosmic-app-list /usr/bin/cosmic-panel-button /usr/bin/cosmic-panel; do for k in resvg usvg tiny-skia rsvg librsvg cosmic-freedesktop-icons; do echo \"$k: $(strings -a $b | grep -c -- $k)\"; done; done",
              "saida_relevante": "cosmic-app-list -> resvg:18  usvg:36  tiny-skia:56  rsvg:0  librsvg:0  cosmic-freedesktop-icons:5   (idem para cosmic-panel-button e cosmic-panel)",
              "o_que_prova": "Quem renderiza SVG no painel/dock/applets é resvg+usvg+tiny-skia, NÃO o librsvg. A resolução de nome é a cosmic-freedesktop-icons."
            },
            {
              "comando": "strings -a /usr/bin/cosmic-app-list | grep -oE 'vendor/(resvg|usvg|simplecss|svgtypes|fontdb|tiny-skia)[^ ]*' | sort -u   +   grep -inE 'not supported|No fonts with a' ",
              "saida_relevante": "vendor/usvg/src/parser/{filter,mask,clippath,use_node,marker,switch,image,text}.rs ; vendor/resvg/src/filter/{box_blur,composite,color_matrix,turbulence,lighting,convolve_matrix,...}.rs ; \" rule is not supported. Skipped.\" ; \"' is not supported. Selector skipped.\" ; \"- filter input isn't supported and not planed.\" ; \"No fonts with a \" ... \"' font-family.\" ; \"Warning: unsupported blend mode: \"",
              "o_que_prova": "O resvg embutido tem filtro, máscara, clipPath, <use>, <image> e <text>. As limitações reais são: seletores CSS do <style> (simplecss), BackgroundImage/BackgroundAlpha como entrada de filtro, blend modes exóticos e — a que mais dói em ícone — <text> sem fonte no fontdb."
            },
            {
              "comando": "rsvg-convert -w 256 -h 256 -o coquinha-rsvg.png ~/coquinha.svg && rsvg-convert -w 256 -h 256 -o mimir-rsvg.png ~/mimir.svg && identify *.png",
              "saida_relevante": "coquinha-rsvg.png PNG 256x256 8-bit sRGB 14871B ; mimir-rsvg.png PNG 256x256 8-bit sRGB 14377B — os dois desenham o gato inteiro, com o arco Catppuccin",
              "o_que_prova": "Os dois SVG do Boxy são válidos e completos. O sintoma dela NÃO é 'o arquivo está quebrado'. (Comparativo com um CLI de resvg ficou PENDENTE: não há resvg instalado e instalar é proibido.)"
            },
            {
              "comando": "grep -o 'style=\"\"' ~/mimir.svg | wc -l ; grep -c 'width=' ~/coquinha.svg ~/mimir.svg ; grep -oE '<[a-zA-Z:]+' ~/coquinha.svg | sort -u",
              "saida_relevante": "mimir: 4 ocorrências de style=\"\" (coquinha: 0) · width= : 0 nos dois · elementos presentes: <svg, <g, <path e nada mais",
              "o_que_prova": "O Boxy grava style=\"\" quando o preenchimento é removido — e style vazio significa PRETO (default do SVG), não transparente: no render do mimir os olhos/boca saem pretos. E nenhum dos dois declara width/height, só viewBox. São exatamente os dois itens que um saneador tem de corrigir."
            },
            {
              "comando": "grep -rn 'LogoMenu' ~/.config/cosmic/com.system76.CosmicPanel* ; cat ~/.config/cosmic/com.system76.CosmicPanel.Panel/v1/plugins_wings",
              "saida_relevante": "grep: NENHUMA linha. plugins_wings = ([CosmicAppletBattery, CosmicAppletTiling, dev.cappsy.CosmicExtAppletDrives, CosmicAppletWorkspaces],[CosmicAppletStatusArea, clipboard-manager, Bluetooth, Audio, Network, Power]) ; plugins_center = [Eyedropper, Time, Notifications]",
              "o_que_prova": "O applet dev.cappsy.CosmicExtAppletLogoMenu NÃO está em nenhum painel. A chave custom_logo_path=\"…/logos/meow-mocha.svg\" é configuração morta — não existe gato nenhum no painel para 'refletir' o do dock."
            },
            {
              "comando": "busctl --user get-property org.kde.StatusNotifierWatcher /StatusNotifierWatcher org.kde.StatusNotifierWatcher RegisteredStatusNotifierItems ; depois Id/IconName/IconPixmap de cada",
              "saida_relevante": "as 2 \":1.380\" \":1.567\" · :1.380 Id=\"qBittorrent\" IconName=\"\" IconPixmap=51535 bytes · :1.567 Id=\"ZapZap\" IconName=\"\" IconPixmap=58313 bytes",
              "o_que_prova": "A bandeja (CosmicAppletStatusArea) desenha o BITMAP cru que o app manda pelo D-Bus, com IconName vazio. Nem o tema de ícones nem o Icon= do .desktop têm qualquer efeito ali — é por isso que o ZapZap continua verde com o '56' vermelho."
            },
            {
              "comando": "para cada applet de plugins_wings/plugins_center: grep -m1 '^Icon=' no .desktop correspondente",
              "saida_relevante": "11 de 13 devolvem Icon=<id>-symbolic (Battery, Tiling, Drives, Eyedropper, Time, Notifications, Clipboard, Bluetooth, Audio, Network, Power). Só Workspaces e StatusArea não. O botão do dock, com.system76.CosmicPanelAppButton, NÃO é symbolic.",
              "o_que_prova": "Por construção do System76, o applet do painel pede um nome -symbolic, e a libcosmic faz .symbolic(path.contains(\"-symbolic.svg\")) achatando numa cor só. Logo colorida em applet é impossível sem sombrear o .desktop; o botão do dock, que não é symbolic, mantém a cor (e mantém — é o gato roxo da captura)."
            },
            {
              "comando": "python3 resolver.py (implementação da regra do §2) para os 11 nomes -symbolic",
              "saida_relevante": "com.system76.CosmicAppletBattery-symbolic -> /usr/share/icons/hicolor/scalable/apps/…  (candidatos: 1). Idem Power, e todos os outros.",
              "o_que_prova": "MeowSystem-Icons não contém NENHUM arquivo *-symbolic.svg: os applets caem direto no hicolor, o último elo da cadeia. A cobertura do tema sobre o painel é ZERO."
            },
            {
              "comando": "python3 resolver.py com.system76.CosmicPanelAppButton com.system76.CosmicFiles hefesto-dualsense4unix com.system76.CosmicTerm org.qbittorrent.qBittorrent google-chrome com.boxy_svg.BoxySVG thunderbird",
              "saida_relevante": "CADEIA: MeowSystem-Icons -> Papirus-Dark -> breeze-dark -> Cosmic -> Adwaita -> hicolor -> breeze -> Pop -> Pop-Extra -> pop-os-branding. AppButton/Files/Hefesto/Term vencem em MeowSystem-Icons/scalable/apps. qBittorrent, Chrome e Boxy vencem em Papirus-Dark (links para /usr/share/icons/Papirus/…). thunderbird vence em MeowSystem-Icons/scalable/apps/thunderbird.PNG",
              "o_que_prova": "O dock está resolvendo o que o projeto plantou; o que não é Catppuccin é o que o projeto nunca cobriu (qBittorrent, Chrome, Boxy). E há um PNG dentro de uma seção Type=Scalable, que funciona por acidente da ordem de extensão."
            },
            {
              "comando": "grep -E '^(Name|Icon)=' ~/.local/share/applications/steam-*.desktop ; ls ~/.local/share/icons/steam-jogos/ ; find ~/.local/share/icons -name 'steam_icon_1553260*'",
              "saida_relevante": "steam-1553260.desktop -> Name=Scarlet Deer Inn, Icon=steam (os outros 7 pedem steam_icon_<appid>). ~/.local/share/icons/steam-jogos/ tem 17 PNG steam_jogo_*.png e NENHUM index.theme. steam_icon_1553260: nada encontrado.",
              "o_que_prova": "Um dos oito jogos mostra o logo genérico da Steam porque o .desktop pede um nome que não é o dele; e a pasta steam-jogos, com 1,2 MB de capas, é invisível para o resolvedor por não ter index.theme (armadilha do §4d na forma mais grave)."
            },
            {
              "comando": "ps -eo pid,lstart,comm -C cosmic-panel -C cosmic-session ; for f in /proc/34221/fdinfo/*; do grep -l '^inotify' $f; done | wc -l ; find ~/.config/cosmic -newermt '2026-08-05 08:00'",
              "saida_relevante": "cosmic-session iniciou 15:19:00; cosmic-panel (pid 34221) e cosmic-notifications reiniciaram juntos às 15:36:44. O painel mantém exatamente 6 fds de inotify. Nenhum arquivo em ~/.config/cosmic mudou depois das 08:00 de hoje.",
              "o_que_prova": "Ela reiniciou o painel às 15:36 (o reparo do §4f), 6 min depois de salvar o mimir — mas nenhum SVG do Boxy chegou a ser instalado em config nem no tema. E os 6 watches confirmam: o painel não vigia arquivo de ícone; conteúdo reescrito só entra no próximo login."
            },
            {
              "comando": "readlink -f /usr/share/icons/Papirus-Dark/64x64/apps/com.boxy_svg.BoxySVG.svg ; rsvg-convert dos dois e montage lado a lado",
              "saida_relevante": "-> /usr/share/icons/Papirus/64x64/apps/boxy-svg.svg. Os dois renders (Papirus e o exportado pelo flatpak, 4865 B) mostram o MESMO hexágono colorido com o asterisco branco.",
              "o_que_prova": "O ícone do próprio aplicativo Boxy SVG resolve e desenha certo. Se a frase dela era sobre o ícone do app Boxy no dock, não há defeito de renderização — no máximo falta de tematização (ele vem do Papirus, não do MeowSystem-Icons)."
            }
          ],
          "incertezas": [
            "Qual dos três sintomas a frase 'o svg da dock do boxy não funciona em dock' descreve exatamente NÃO foi medido — ela não instalou coquinha.svg/mimir.svg em lugar nenhum (nada em ~/.config/cosmic nem em MeowSystem-Icons mudou depois das 09:40), então não há rastro do que ela tentou. As três leituras possíveis estão todas cobertas acima; a mais provável, pelo cache medido, é 'copiei/reescrevi e não mudou nada'.",
            "O teste comparativo librsvg vs resvg ficou PENDENTE: não há CLI de resvg na máquina e instalar é proibido pela tarefa. A afirmação 'o resvg também renderiza esses dois' é inferida do conjunto de features presentes no binário e da ausência de qualquer construção exótica nos arquivos — não de um render lado a lado.",
            "A ORDEM ENTRE BASES para o mesmo nome de tema foi assumida do §2 (/usr/share antes de ~/.local/share, exports do flatpak por último). Isso é o inverso da ordem do XDG_DATA_DIRS medido nos processos (flatpak-user : flatpak-system : /usr/local/share : /usr/share), o que levanta a suspeita de que a ordem venha de um HashMap e possa não ser determinística entre execuções. Vale um strace novo antes de qualquer decisão que dependa disso.",
            "Meu resolvedor ignora o casamento de TAMANHO: ele devolve a primeira seção de Directories= que contenha o arquivo, e por isso aponta 16x16 no Papirus-Dark. O COSMIC pediria a seção mais próxima do tamanho do dock (size: M). O ARQUIVO VENCEDOR por tema está certo; a subpasta relatada dentro do Papirus pode não ser a real. Nos casos que importam (MeowSystem-Icons só tem scalable/apps) isso não muda nada.",
            "Não foi medido se a libcosmic/iced popula o fontdb com as fontes do sistema antes de chamar o usvg. Se não popular, todo <text> em SVG de ícone some sem aviso. Isso vira uma pré-condição do saneador (converter texto em caminho), não uma aposta.",
            "Não foi medido se a bandeja (CosmicAppletStatusArea) prefere IconName quando ele NÃO é vazio. Hoje os dois itens mandam IconName=\"\", então a questão é acadêmica aqui — mas um app que mandasse IconName poderia, em tese, ser tematizado.",
            "Não foi verificado se o ZapZap tem opção de trocar o ícone da bandeja na própria configuração dele (seria o único caminho para D3)."
          ],
          "solucao": {
            "arquivos_a_mexer": [
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/sanear_svg.py",
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/construir_icones.sh",
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/auditar_icones.sh",
              "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow",
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/hicolor.sh",
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/jogos_steam.sh",
              "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md"
            ],
            "descricao": "Quatro frentes, na ordem em que pagam.\n\n1) `scripts/sanear_svg.py` (NOVO) — o filtro pelo qual passa todo SVG que ela traga de qualquer editor. Só stdlib (`xml.etree`), sem dependência nova, entrada=arquivo, saída=stdout, mais `--conferir` que devolve 1 se o arquivo precisaria mudar. O que ele faz, e cada item veio de uma medição:\n   a. `style=\"\"` (e `style=\"  \"`) é REMOVIDO do elemento — 4 ocorrências em ~/mimir.svg. Deixar é pintar de preto puro; remover devolve a herança do pai, que é o comportamento que ela quis ao apagar a cor no Boxy. Se o elemento também não tiver `fill=`, o saneador RECUSA e diz qual `<path>` ficou sem cor — nunca adivinha uma.\n   b. Garante `viewBox` (deriva de width/height se faltar) E garante `width`/`height` em px (deriva do viewBox se faltarem — o caso dos dois arquivos dela).\n   c. Recusa, com mensagem nomeando o elemento e a linha: `<text>` sem `font-family` embutida (o usvg do COSMIC avisa \"No fonts with a … font-family\" e desenha NADA), `<style>` com seletor que não seja de tipo/classe simples (o simplecss embutido responde \"Selector skipped\"), e `BackgroundImage`/`BackgroundAlpha` como `in=` de filtro (\"filter input isn't supported and not planed\").\n   d. Descarta namespaces de editor (`sodipodi:`, `inkscape:`, `boxy:`, `data-*`) e `<metadata>`.\n   e. Barra o sufixo `-symbolic.svg` no nome de destino quando o arquivo tem mais de uma cor — é a armadilha do §3, e é silenciosa.\n   f. PROVA que o resultado ainda desenha: rasteriza com `rsvg-convert` para PNG num temporário e falha se saírem menos de N pixels não-transparentes (limiar 1% da área). Isso pega o caso 'saneei e apaguei o desenho' sem exigir olho humano.\n\n2) `construir_icones.sh` — passa TODO ícone pelo saneador antes de gravar, e resolve o cache que é a causa real do 'não funciona':\n   - a logo do painel deixa de ser um nome fixo: grava `meow-<flavor>-<hash8-do-conteúdo>.svg` e aponta `custom_logo_path` para ele, apagando as variantes órfãs. Assim vale a regra MEDIDA do §3 (a CHAVE troca ao vivo; o ARQUIVO não) e a arte nova aparece na hora, sem reiniciar nada, sem piscar a tela dela.\n   - o botão do dock (`com.system76.CosmicPanelAppButton` / `com.system76.CosmicAppLibrary`) não tem esse truque — o nome é cravado pelo `.desktop` do System76. Aí o script passa a IMPRIMIR, quando o conteúdo mudou mas o nome não: 'o gato do dock só troca no próximo login (ou `pkill -x cosmic-panel`, que pisca a tela)'. Dizer isso é o conserto; reiniciar sozinho já custou o painel dela duas vezes.\n   - acrescenta `dev.cappsy.CosmicExtAppletLogoMenu` a `plugins_wings` do Panel SE ela quiser o gato no painel — mas atrás de uma chave `LOGO_NO_PAINEL=1` em `meow.conf`, porque o §5 diz que a ordem dos applets é do Ritual da Aurora e é dela.\n\n3) `auditar_icones.sh` — ele já resolve nome→arquivo (764 linhas, e já acerta as duas armadilhas do `find`). Falta ser CONFERÊNCIA, não só relatório: ganha `--conferir` que devolve 0/1 e o `bin/meow` passa a chamá-lo dentro do `doctor` (hoje ele só aparece como linha de `meow_info` em completar_icones.sh:371). As quatro asserções novas, cada uma nascida de uma divergência medida hoje:\n   A. todo `Icon=` de `.desktop` VISÍVEL resolve para algum arquivo — pega o `Icon=steam` do Scarlet Deer Inn (que resolve, mas para o logo genérico: por isso a asserção é mais fina — se o `.desktop` é `steam-<appid>` e o `Icon=` não é `steam_icon_<appid>`, acusa).\n   B. todo diretório de ícone sob `~/.local/share/icons/` que não seja um tema válido é ACUSADO — pega `steam-jogos/` (17 capas, 1,2 MB, sem `index.theme`, invisível).\n   C. todo arquivo em `MeowSystem-Icons/scalable/apps/` é `.svg` — pega `thunderbird.png` numa seção `Type=Scalable`.\n   D. todo arquivo do tema passa no `sanear_svg.py --conferir` — impede que a próxima arte do Boxy entre com `style=\"\"`.\n   E. avisa quando um `.desktop` pede um nome que o tema tem sob OUTRO nome (o órfão `com.vitoriamaria.HefestoDualsense4Unix.svg`, md5 idêntico ao `hefesto-dualsense4unix.svg` e pedido por ninguém).\n\n4) Os dois limites que o projeto tem de PARAR de tentar vencer, e dizer isso em voz alta no README e no doc:\n   - applet do painel é `-symbolic` por decisão do System76 → monocromático por construção. O único caminho seria sombrear 11 `.desktop` de `/usr/share/applications`, que é território do apt e volta a cada upgrade. Não vale.\n   - bandeja é `IconPixmap` cru pelo D-Bus → inalcançável por tema. O doctor passa a dizer isso quando ela perguntar por que o ZapZap continua verde, em vez de deixá-la achar que é bug do projeto.\nE o `docs/COSMIC-THEMING.md` ganha uma §3b com as três medições novas: resvg/usvg vendorizado (não librsvg), `-symbolic` em 11 de 13 applets, e `IconPixmap` na bandeja.",
            "idempotencia": "O saneador é idempotente por construção: rodar em cima da própria saída não muda nada, porque as três operações (remover style vazio, garantir viewBox/width/height, podar namespace de editor) são pontos fixos. A verificação disso vira teste: `sanear_svg.py X | sanear_svg.py - | diff - <(sanear_svg.py X)`. A logo com hash no nome converge: mesmo conteúdo, mesmo hash, mesmo nome, `meow_escrever` não escreve e `custom_logo_path` já aponta certo — nada muda na segunda rodada, e o §5 (ping-pong com o Aurora) não é acordado. O `--conferir` do auditor é leitura pura, como o resto do `doctor` sem `--consertar`. Cuidado explícito, herdado do bug já documentado em construir_icones.sh:103-119: a lista de variantes órfãs da logo tem de ser derivada do que existe no disco, não de uma lista fixa, senão nasce um segundo dono da mesma linha e os dois scripts se desfazem eternamente.",
            "riscos": [
              "Apagar `style=\"\"` MUDA o desenho: o que hoje sai preto passa a herdar a cor do pai (ou nenhuma). É o comportamento certo, mas é uma mudança visível — o saneador precisa listar quais elementos tocou, e o `--conferir` precisa rodar antes de qualquer escrita para ela ver a lista.",
              "Pôr hash no nome da logo cria arquivos novos a cada mudança de arte. Sem a limpeza dos órfãos, `~/.config/cosmic/logos/` vira lixeira. E a limpeza não pode apagar `gato-pop.svg`, que é do Ritual da Aurora (§5) e volta de hora em hora.",
              "Acrescentar o applet da logo a `plugins_wings` é escrever numa chave que o §5 dá ao Aurora ('a ordem dos applets é dela'). Só atrás de opção explícita, e fora do restaurar.",
              "O `rsvg-convert` como prova de que o SVG ainda desenha é uma aproximação: quem desenha no painel é o resvg. Um arquivo pode passar no librsvg e falhar no resvg. Enquanto não houver um renderizador resvg disponível, essa checagem detecta 'ficou vazio', não 'ficou igual ao que o COSMIC vai mostrar'.",
              "Trocar `Icon=steam` por `Icon=steam_icon_1553260` no `.desktop` do Scarlet Deer Inn quebraria o ícone: esse nome NÃO existe. O conserto certo é instalar a capa (`steam-jogos/steam_jogo_1553260.png`) no hicolor do usuário com o nome que o `.desktop` passar a pedir — dois passos, e o `jogos_steam.sh` hoje declara explicitamente que 'não escreve ícone nenhum'. Mudar isso é mudar a promessa do script.",
              "O `~/.local/share/icons/hicolor/index.theme` continua sendo regenerado a partir de UMA base só. Se algum dia o resolvedor usar esse índice para as outras bases de hicolor (exports do flatpak), `36x36`, `72x72`, `*@2/apps` e `symbolic/apps` somem de uma vez. Vale um comentário no hicolor.sh e um teste no doctor, não uma reescrita agora."
            ]
          },
          "titulo": "Três sintomas, três mecanismos — e nenhum deles é o renderizador de SVG: o applet da logo não está no painel, a bandeja desenha bitmap do D-Bus, e o SVG do Boxy esbarra no cache do painel"
        },
        "refutacoes": [
          {
            "refutado": true,
            "motivo": "O esqueleto (\"três sintomas, três mecanismos, e o renderizador não é o culpado\") sobrevive, mas TRÊS afirmações que sustentam o achado caem na medição, e duas delas mandariam o conserto para o lado errado.\n\n(1) O `style=\"\"` NÃO produz \"buraco invisível\" — a evidência está invertida. Rasterizei os dois. Os 4 caminhos com `style=\"\"` no mimir são os olhos/nariz/boca, pintados EM CIMA do corpo opaco do gato (`style=\"fill: rgb(95,103,119)\"` = #5F6777). Preto sobre #5F6777 é o elemento de MAIOR contraste do arquivo e é exatamente o desenho pretendido. Quem tem risco real de contraste é o OUTRO arquivo: a coquinha, cujo corpo é `style=\"fill: rgb(7,13,24)\"` = #070D18 (quase preto) — esse sim some sobre o `crust` #11111b do painel — e a coquinha tem ZERO `style=\"\"`. Mecanismo e exemplo estão anticorrelacionados. Amarrar isso ao commit 29fa004 (\"sumiam sobre papel de parede CLARO\") é não-sequitur: forma preta é MAIS visível sobre fundo claro.\n\n(2) \"(e)(ii) o cache explica 100%\" contradiz a própria evidência 11 do achado. Varri md5 dos dois arquivos em `~/.local/share/icons`, `~/.config` e no repo inteiro: ZERO cópias. Nada em MeowSystem-Icons mudou depois de 09:40:08; `meow-mocha.svg` é de 04/08 19:46. Se nada foi instalado no caminho resolvido, cache nenhum pode ser a causa — cache só morde quando o CONTEÚDO do caminho já resolvido muda. A explicação mais simples é a trivial: a arte só existe em `~/`. O cache é obstáculo FUTURO para o conserto, não causa presente.\n\n(3) D3 (\"a bandeja não passa por tema nem por .desktop... não alcançável\") é refutado empiricamente, na tela dela AGORA. Existem TRÊS StatusNotifierItem, não dois. O terceiro é o app do próprio projeto, com `IconName` PREENCHIDO, `IconPixmap` vazio, e o que aparece na bandeja é o SVG Catppuccin de MeowSystem-Icons, colorido. A bandeja é alcançável por tema para todo app que declare IconName.\n\n(4) O mecanismo de D2 está errado onde importa. Os 13 `/usr/bin/cosmic-applet-*` são symlinks para UM binário (`/usr/bin/cosmic-applets`, md5 c6183c33…), e esse binário tem ZERO ocorrências de `com.system76.CosmicApplet{Battery,Power,Audio,Network,Time}-symbolic`. O `Icon=` do `.desktop` é metadado para a lista do cosmic-settings; o que o painel pinta são nomes de ESTADO cravados no binário, e eles resolvem em Papirus-Dark (`symbolic/devices`, `symbolic/status`), não no hicolor. \"Caem direto no hicolor, o último elo\" e \"cobertura ZERO\" são ambos falsos, e sombrear o `.desktop` (o remédio implícito) não repintaria nada.\n\n(5) A tabela aponta o arquivo errado para o botão do dock: o processo roda `cosmic-panel-button com.system76.CosmicAppLibrary`, e é `com.system76.CosmicAppLibrary.svg` que é pintado. Aqui os dois são byte-idênticos (327acf7d…), então a conclusão está certa por acidente — quem editar só `com.system76.CosmicPanelAppButton.svg` não verá mudança nenhuma.\n\n(6) `steam-jogos` está enquadrado ao contrário: NENHUM `.desktop` da máquina pede `steam_jogo_*`, então `index.theme` ali não mudaria um pixel. Não é a armadilha do §4d (que exige um tema que alguém de fato resolve) — é uma pasta de estoque.\n\n(7) \"os dois SVG renderizam\" não foi medido no motor certo: só com librsvg, que NÃO é o motor do painel. Não há CLI de resvg e instalar é proibido. Isso é inferência, não medição.",
            "evidencia_contraria": "A) `style=\"\"` não é buraco — rasterizado e olhado\n   `rsvg-convert -w 256 -h 256 -o mimir.png ~/mimir.svg` + `Read mimir.png`\n   → gato cinza-azulado com olhos fechados PRETOS, nítidos, e laço verde. Nada invisível.\n   `grep -oE 'style=\"[^\"]*\"|fill=\"[^\"]*\"' ~/mimir.svg | sort | uniq -c`\n   →  4 style=\"\"        1 style=\"fill: rgb(95, 103, 119);\"   1 style=\"fill: rgb(69, 230, 150);\"\n   `grep -oE 'style=\"[^\"]*\"|fill=\"[^\"]*\"' ~/coquinha.svg | sort | uniq -c`\n   →  4 style=\"fill: rgb(122, 124, 140);\"   1 style=\"fill: rgb(7, 13, 24);\"\n   Ou seja: os 4 caminhos de `style=\"\"` do mimir são os 4 que na coquinha valem #7A7C8C — as feições, desenhadas por cima do corpo. Corpo do mimir #5F6777 (claro), corpo da coquinha #070D18 (quase preto). O arquivo sem defeito é o que some no escuro.\n   (Confirmado o resto do item: `grep -c 'width=' ` → 0 nos dois; elementos presentes só `<svg`, `<g`, `<path`.)\n\nB) Os SVG nunca foram instalados → o cache não pode ser a causa\n   C=517035f4652f576f1d183c9f75365a77 (coquinha) · M=9b81f79844543835f4017f92f25edba9 (mimir)\n   `find ~/.local/share/icons ~/.config /mnt/Apate/Desenvolvimento/MeowSystem -name '*.svg' -print0 | xargs -0 md5sum | grep -E \"^($C|$M)\"` → rc=1, ZERO linhas.\n   `find ~/.local/share/icons/MeowSystem-Icons -newermt '2026-08-05 00:00' -type f | wc -l` → 8, todos 09:40:08.\n   md5 de `~/.config/cosmic/logos/meow-mocha.svg` = 327acf7d3f88bf05a01a258cc4bc295c (mtime 04/08 19:46), idêntico a AppButton.svg e AppLibrary.svg.\n\nC) A BANDEJA PASSA POR TEMA — o contraexemplo está viva na tela dela\n   `busctl --user get-property org.kde.StatusNotifierWatcher /StatusNotifierWatcher org.kde.StatusNotifierWatcher RegisteredStatusNotifierItems`\n   → as 3 \":1.380\" \":1.567\" \":1.643/org/ayatana/NotificationItem/hefesto_dualsense4unix\"   ← TRÊS, não dois\n   `busctl --user get-property :1.643 /org/ayatana/NotificationItem/hefesto_dualsense4unix org.kde.StatusNotifierItem Id IconName Title Status`\n   → s \"hefesto-dualsense4unix\" / s \"hefesto-dualsense4unix\" / s \"Hefesto - Dualsense4Unix\" / s \"Active\"\n   IconName NÃO é vazio. IconPixmap dele: 112 bytes de saída do busctl = array vazio (contra 51.535 e 58.313 de qBittorrent/ZapZap).\n   Resolução: `resolver.py hefesto-dualsense4unix` → VENCE em [MeowSystem-Icons] scalable/apps/hefesto-dualsense4unix.svg\n   PROVA VISUAL: `captura-de-tela.sh` → /tmp/Screenshot_2026-08-05_16-07-36.png; recortei 32x32+1660+8 da bandeja e coloquei lado a lado com `rsvg-convert` do SVG do tema: MESMA arte (bigorna+martelo em círculo escuro com anel mauve), colorida, no painel dela agora.\n\nD) O painel não pinta o `Icon=` do `.desktop` do applet\n   `ls -li /usr/bin/cosmic-applet-*` → TODOS symlink -> cosmic-applets\n   `md5sum /usr/bin/cosmic-applet-{power,audio,battery,network,time}` → c6183c3306e77ae20ae5bf6b9e3a4b19 (um só arquivo, 5 vezes)\n   `for n in com.system76.CosmicApplet{Battery,Power,Audio,Network,Time}-symbolic; do strings -a /usr/bin/cosmic-applets | grep -c -- $n; done` → 0 0 0 0 0\n   `strings -a /usr/bin/cosmic-applets | grep -oE '(battery|network|audio|bluetooth|display)[a-z0-9-]*-symbolic'`\n   → battery-symbolic, network-wireless-signal-{excellent,good,ok,weak}-symbolic, network-wired-symbolic,\n     network-wired-disconnected-symbolic, network-wireless-error-symbolic, display-brightness-symbolic,\n     bluetooth-{disabled,active}-symbolic, cosmic-applet-bluetooth-active-symbolic, keyboard-brightness-symbolic\n   resolver.py nesses nomes:\n     battery-symbolic            → [Papirus-Dark] /usr/share/icons/Papirus-Dark/symbolic/devices/battery-symbolic.svg\n     network-wireless-symbolic   → [Papirus-Dark] .../symbolic/devices/network-wireless-symbolic.svg\n     display-brightness-symbolic → [Papirus-Dark] .../symbolic/status/display-brightness-symbolic.svg\n   (contra: com.system76.CosmicAppletBattery-symbolic → [hicolor], que é o nome que NINGUÉM pede em runtime)\n   Cadeia real medida: MeowSystem-Icons → Papirus-Dark → breeze-dark → breeze → hicolor → Cosmic → Pop → Pop-Extra → pop-os-branding → Adwaita (o achado listou outra ordem, com hicolor em 6º).\n\nE) O botão do dock é AppLibrary, não AppButton\n   `tr '\\0' ' ' < /proc/34292/cmdline` → cosmic-panel-button com.system76.CosmicAppLibrary\n   `/usr/share/applications/com.system76.CosmicPanelAppButton.desktop`: Exec=cosmic-panel-button com.system76.CosmicAppLibrary · Icon=com.system76.CosmicPanelAppButton (metadado)\n   `grep -m1 ^Icon= /usr/share/applications/com.system76.CosmicAppLibrary.desktop` → Icon=com.system76.CosmicAppLibrary\n   (salvos pelo acaso: md5 327acf7d… nos dois arquivos)\n\nF) steam-jogos: ninguém pede aqueles nomes\n   `grep -rl 'steam_jogo' ~/.local/share/applications /usr/share/applications | wc -l` → 0\n   `identify steam_jogo_1553260.png` → PNG 256x256 (é ícone quadrado, não capa)\n   `head -8 ~/.local/share/icons/hicolor/index.theme` → Directories= … ,128x128/apps,192x192/apps,256x256/apps,512x512/apps,scalable/apps  ← 256x256/apps JÁ está declarado (o hicolor.sh do §4d já rodou)\n   `find ~/.local/share/icons -name 'steam_icon_*'` → 12 nomes, em 16x16..256x256/apps; steam_icon_1553260 não existe\n   resolver.py: steam → [Papirus-Dark] 128x128/apps/steam.svg · steam_icon_1553260 → NAO RESOLVE · steam_jogo_1553260 → NAO RESOLVE\n\nCONFIRMADOS (não re-litigar):\n   strings resvg/usvg/tiny-skia/rsvg/librsvg/cosmic-freedesktop-icons → app-list 18/36/56/0/0/5 · panel-button 18/36/56/0/0/5 · panel 18/36/34/0/0/5. Módulos vendorizados presentes: usvg/src/parser/{filter,mask,clippath,use_node,marker,switch,image,text}.rs e resvg/src/filter/*.\n   cosmic-panel pid 34221 (15:36:44; sessão 15:19:00), exatamente 6 fds de inotify → inodes resolvidos para CosmicPanel/v1, CosmicPanel.Panel/v1, CosmicPanel.Dock/v1, CosmicTheme.Mode/v1, CosmicTheme.Light/v2, CosmicTheme.Dark/v2. Bate com §4c.\n   LogoMenu: `grep -rn LogoMenu ~/.config/cosmic/com.system76.CosmicPanel*` → vazio; Panel wings/center e Dock wings/center conferidos um a um; nenhum processo de logomenu entre os 13 applets vivos; a captura mostra 4 ícones brancos + \"Meow\"/\"OS\" e NENHUM gato no painel superior. `custom_logo_path` = configuração morta.\n   11 de 13 applets pedem `-symbolic` — número certo, mas 3 deles (Drives, clipboard-manager, Eyedropper) só têm `.desktop` em `~/.local/share/flatpak/exports/share/applications/`, não onde o achado disse procurar.\n   \"achatado numa cor só\": amostrei os ícones da direita do painel na captura → primeiro plano #E9EBF1, #ECEEFA, #E8ECF9, #EDF2FB (quase branco em todos). Confirmado.\n   thunderbird → MeowSystem-Icons/scalable/apps/thunderbird.png (candidatos: 1). PNG dentro de seção Type=Scalable. Confirmado.\n   MeowSystem-Icons não tem NENHUM `*-symbolic*` (find → 0) e declara Directories=scalable/apps,22x22/places,24x24/places,32x32/places,48x48/places,64x64/places.",
            "correcao_sugerida": "Reescreva o achado assim, sintoma por sintoma:\n\n1. \"O SVG do Boxy não funciona no dock\" — causa: (iii) o arquivo nunca saiu de `~/`. Não é cache, não é renderizador, não é `style=\"\"`. Conserto em duas etapas, e a ORDEM importa:\n   a) instalar a arte no caminho que é de fato resolvido — para o botão do dock é `~/.local/share/icons/MeowSystem-Icons/scalable/apps/com.system76.CosmicAppLibrary.svg` (é esse o nome que o `cosmic-panel-button` pede via argv), e por simetria manter `com.system76.CosmicPanelAppButton.svg` igual;\n   b) só então o cache do §3/§4c passa a ser o obstáculo: para a logo do painel, trocar a CHAVE `custom_logo_path` para um nome novo (`meow-mimir.svg`) em vez de reescrever `meow-mocha.svg`; para o tema de ícones, o `construir_icones.sh` continua certo em não reiniciar o painel — mas o `meow doctor` deveria AVISAR \"arte trocada, só entra no próximo login/`pkill -x cosmic-panel`\" em vez de ficar calado.\n\n2. Saneador de SVG do Boxy — mantenha os dois itens, mas com a justificativa certa:\n   - `width`/`height` ausentes: adicionar (derivar do viewBox). Vale sempre, é robustez contra motor que não seja o librsvg.\n   - `style=\"\"`: remover o atributo (não \"pintar de claro\"). Ele NÃO some no painel; ele apenas herda preto do default do SVG. É higiene, não bug de visibilidade.\n   - E acrescente o item que realmente causa o sintoma \"sumiu\": um teste de CONTRASTE do corpo do desenho contra `base`/`crust` (#1e1e2e/#11111b). Foi a coquinha (#070D18) que reprovaria, não o mimir. Reaproveite a lógica de silhueta do commit 29fa004, aplicada ao maior caminho por área, não aos `style=\"\"`.\n   - Não conclua nada sobre resvg sem medir: registre \"não medido — só há librsvg nesta máquina, e instalar é proibido\". Se quiser fechar a lacuna sem instalar nada, o teste honesto é plantar o SVG num nome que o painel realmente resolve e reiniciar o painel uma vez.\n\n3. Bandeja (D3) — reescrever de \"não alcançável\" para \"alcançável por app, via IconName\":\n   O SNI tem dois caminhos exclusivos: `IconName` (passa pelo tema — o hefesto do próprio projeto já ganha assim, em cores) e `IconPixmap` (ARGB cru do app, inalcançável). qBittorrent e ZapZap escolheram pixmap. Logo:\n   - qBittorrent: o conserto é a opção \"usar tema de ícones do sistema\" do próprio app (e o §5 já diz que o tema do qBittorrent é do Aurora — chame o script dele, não escreva no `.conf`).\n   - ZapZap: sem opção equivalente; aí sim é inalcançável, e o `Icon=meow-whatsapp` no `.desktop` continua valendo para o dock/lançador (que é onde ele já funciona), não para a bandeja.\n   - Documente o hefesto como o padrão-ouro: qualquer app do projeto que ponha bandeja deve emitir `IconName`, nunca `IconPixmap`.\n\n4. Applets do painel (D2) — trocar o mecanismo:\n   Os 13 são um binário só (`/usr/bin/cosmic-applets`). O `Icon=<id>-symbolic` do `.desktop` é metadado do cosmic-settings; sombrear o `.desktop` não muda pixel nenhum. Quem pinta são nomes de estado cravados no binário (`battery-symbolic`, `network-wireless-signal-*-symbolic`, `network-wired-symbolic`, `display-brightness-symbolic`, `bluetooth-{active,disabled}-symbolic`, `audio-*`), e eles vencem hoje em **Papirus-Dark/symbolic/{devices,status}**. Consequências:\n   - O tema PODE cobrir o painel — basta declarar `symbolic/devices`, `symbolic/status`, `symbolic/apps` no `Directories=` do MeowSystem-Icons e plantar os nomes de estado. Hoje o `index.theme` não declara nenhuma seção `symbolic/`, então a cobertura é zero por omissão nossa, não por impossibilidade.\n   - Mas isso só muda a FORMA, nunca a cor: `.symbolic(path.contains(\"-symbolic.svg\"))` achata em uma cor só (medido: #E9EBF1..#EDF2FB nos 8 ícones). \"Logo colorida em applet\" segue impossível; \"silhueta Catppuccin no painel\" passa a ser possível. Decida qual das duas o projeto quer e escreva isso no §3.\n   - Nota de escopo do projeto: o cosmic-applets é da System76 e é o mesmo binário para todo mundo — essa rota vale com e sem Aurora, e sem tocar em `/usr/share`.\n\n5. Scarlet Deer Inn — é (iii) puro, e o conserto é uma linha, não um `index.theme`:\n   `cp ~/.local/share/icons/steam-jogos/steam_jogo_1553260.png ~/.local/share/icons/hicolor/256x256/apps/steam_icon_1553260.png`\n   `256x256/apps` já está declarado no `index.theme` do hicolor do usuário (o `hicolor.sh` do §4d já rodou), e os 12 `steam_icon_*` existentes já moram exatamente ali. Alternativa equivalente: corrigir `Icon=` no `.desktop`, mas a Steam reescreve esse arquivo — prefira plantar o ícone. NÃO crie `index.theme` em `steam-jogos`: nenhum `.desktop` pede `steam_jogo_*` e nenhum tema da cadeia herda essa pasta; seria trabalho com efeito zero.\n\n6. Correções de fato na tabela e no texto:\n   - \"AppButton · com.system76.CosmicPanelAppButton\" → o nome resolvido em runtime é `com.system76.CosmicAppLibrary` (argv do `cosmic-panel-button`). Salvo hoje só porque os dois arquivos são idênticos (327acf7d…).\n   - \"Os dois StatusNotifierItem vivos\" → são três; o terceiro é o hefesto e é o contraexemplo.\n   - \"os applets caem direto no hicolor / cobertura ZERO\" → caem em Papirus-Dark, em `symbolic/devices` e `symbolic/status`.\n   - Cadeia de herança: MeowSystem-Icons → Papirus-Dark → breeze-dark → breeze → hicolor → Cosmic → Pop → Pop-Extra → pop-os-branding → Adwaita.\n   - Os 3 applets flatpak (Drives, clipboard-manager, Eyedropper) têm `.desktop` em `~/.local/share/flatpak/exports/share/applications/` — qualquer varredura do projeto precisa incluir essa base, senão some 3 de 13.\n   - Órfão `com.vitoriamaria.HefestoDualsense4Unix.svg`: agora que se sabe que a bandeja usa `IconName=\"hefesto-dualsense4unix\"`, o órfão é seguramente removível — mas confirme antes que o `.desktop` do hefesto também usa `hefesto-dualsense4unix`.\n\n7. O que acrescentar ao `docs/COSMIC-THEMING.md` (é o lugar certo, o §3 e o §4d já quase dizem isso):\n   - §3: \"o `.desktop` do applet é metadado; o applet pinta nomes cravados no binário `cosmic-applets` (um só, 13 symlinks). Para cobrir o painel, declare `symbolic/devices`+`symbolic/status` no nosso `index.theme` — e aceite que sai achatado.\"\n   - Seção nova \"§4g — a bandeja é IconName ou IconPixmap\": com o teste `busctl --user get-property <bus> <path> org.kde.StatusNotifierItem IconName` e o hefesto como prova de que o tema alcança.\n   - §2: nota de que `~/.local/share/icons/steam-jogos` não é tema, é estoque — e que pasta sob `assets/icones/` sem `index.theme` não é armadilha, é apenas ignorada; a armadilha do §4d exige um tema que alguém de fato resolve."
          },
          {
            "refutado": true,
            "motivo": "Quatro dos pontos que sustentam a proposta caem na medição.\n\n1) A REGRA (1a), que é a razão de existir do saneador, parte de uma premissa FALSA. `style=\"\"` não pinta de preto e não bloqueia herança nenhuma: um atributo `style` vazio não declara nada, e o preto que ela viu é o valor INICIAL de `fill` do CSS/SVG. Renderizei os dois casos: idênticos byte a byte. Consequências em cascata: (i) remover `style=\"\"` é no-op visual — o item de \"riscos\" que diz \"o que hoje sai preto passa a herdar a cor do pai\" está errado; (ii) pela própria regra (\"se o elemento também não tiver `fill=`, o saneador RECUSA\"), o `~/mimir.svg` — o arquivo que motivou a frente inteira — é REJEITADO pelo saneador, porque os 4 `<path style=\"\">` dele não têm `fill=`. A ferramenta feita para aceitar a arte dela barra exatamente a arte dela, e barra um arquivo que desenha perfeitamente.\n\n2) A FRENTE 2 paga ZERO nesta máquina: o applet `dev.cappsy.CosmicExtAppletLogoMenu` NÃO ESTÁ INSTALADO (não está no `flatpak list`, não tem `.desktop` em `/usr/share/applications`, não tem binário) e não está em nenhum `plugins_wings`/`plugins_center` do Painel nem do Dock, nem entre os processos. Só sobrou o diretório de config órfão. Ou seja, `custom_logo_path` hoje não é lido por ninguém, e o \"a arte nova aparece na hora, sem piscar a tela\" é um ganho para um consumidor que não existe. O gato que ela vê é o `cosmic-panel-button com.system76.CosmicAppLibrary`, que vem do TEMA. E a opção `LOGO_NO_PAINEL=1` escreveria em `plugins_wings` o id de um applet que não existe na máquina — no melhor caso não faz nada, no pior reproduz o \"painel fantasma\" (applet configurado que nunca vira processo é literalmente o sinal 2 do `aurora-painel-fantasma.sh`), na máquina de UMA tela.\n\n3) AS QUATRO ASSERÇÕES DA FRENTE 3 transformam uma auditoria HOJE LIMPA (51/51, 100%, \"nada a fazer\") em divergência PERMANENTE, e o contrato de código de saída do `meow doctor` converte isso em mentira diária às 5h. As quatro acusam coisas que o projeto faz de PROPÓSITO ou que são de OUTRO DONO: `Icon=steam` do Scarlet Deer Inn foi a Steam que escreveu e o `jogos_steam.sh` copia byte a byte (e declara \"não escreve ícone nenhum\"); `steam-jogos/` é do Ritual da Aurora e não é tema quebrado — os `.desktop` dele usam `Icon=<caminho absoluto>`, que é legal na spec e que o próprio `auditar_icones.sh` já pula; `thunderbird.png` é instalado de propósito pelo `completar_icones.sh` (mapa APELIDO) e RESOLVE; `com.vitoriamaria.HefestoDualsense4Unix.svg` não é órfão — existe `/usr/share/applications/com.vitoriamaria.HefestoDualsense4Unix.desktop` com esse `Icon=`. Nenhuma delas tem conserto legítimo, e no `bin/meow` uma conferência sem `fix_` cai em `\"fix_$nome\"` inexistente → 127 → `falhou=1` → saída 2, que NÃO está no `SuccessExitStatus=1 3` do `meow-doctor.service`: unidade vermelha todo dia. Se ganhar um `fix_` que não conserta nada, é pior: saída 1 → o `ExecStopPost` avisa \"o auto-reparo corrigiu\" TODO DIA sem ter corrigido — exatamente o que o cabeçalho da unidade proíbe.\n\n4) AS DUAS \"MEDIÇÕES NOVAS\" da frente 4 estão erradas como enunciadas. \"-symbolic em 11 de 13 applets\": medido, são 18 `.desktop` com `X-CosmicApplet=true` e 10 com `-symbolic`. \"Bandeja é IconPixmap cru → inalcançável por tema\": dos 3 itens vivos no watcher, qBittorrent e ZapZap têm `IconName=\"\"` (verdade para eles), mas o applet Hefesto — o app DELA — expõe `IconName=\"hefesto-dualsense4unix\"`, que passa pelo tema e que o MeowSystem JÁ está tematizando. Gravar a frase absoluta no doc é gravar uma falsidade, e ela conversa mal com a asserção E, que quer consolidar justamente os ícones do Hefesto.\n\nFora isso: o item 1f faz a construção de ícone depender de `rsvg-convert`, e o `install.sh` só sabe `apt-get` — numa COSMIC não-Debian (regra 6, \"qualquer usuário de COSMIC\") isso precisa ser saída 3 (falta dependência), nunca falha dura, e a proposta não diz isso.\n\nO que NÃO refuto: nada aqui exige sudo, nada reinicia o painel, nada escreve em /usr/share, ~/.config/zsh ou nos atalhos — as travas do `lib/comum.sh` ficam respeitadas, e o `--conferir` é de fato leitura (o auditor só cria `mktemp -d` com trap). A idempotência da logo com hash converge (não há segundo dono de `custom_logo_path`: o `cmd_logo` delega ao `construir_icones.sh`). E o alicerce de renderizador está certo: o COSMIC é resvg/usvg mesmo.",
            "evidencia_contraria": "=== 1. `style=\"\"` NÃO pinta de preto e NÃO bloqueia herança (dois testes) ===\nTeste A/B controlado (scratchpad):\n  a.svg: <g fill=\"#ff0000\"><path style=\"\" d=\"M10 10 H90 V90 H10 Z\"/></g>\n  b.svg: idêntico, SEM o style=\"\"\n  $ rsvg-convert a.svg -o a.png; rsvg-convert b.svg -o b.png; md5sum a.png b.png\n    eeeba7a00226438b7498376eecdf2a09  a.png\n    eeeba7a00226438b7498376eecdf2a09  b.png\n  Pixel (50,50) decodificado dos dois PNG: (255, 0, 0, 255) — VERMELHO herdado do pai NOS DOIS.\n\nNo arquivo que motivou a regra:\n  $ sed 's/ style=\"\"//g' /home/vitoriamaria/mimir.svg > mimir_sem_style.svg\n  $ rsvg-convert -w 512 -h 512 /home/vitoriamaria/mimir.svg -o mimir_orig.png\n  $ rsvg-convert -w 512 -h 512 mimir_sem_style.svg -o mimir_sem.png; md5sum\n    ffc9b7ec16b7bb6235d6f7111b80f9ae  mimir_orig.png\n    ffc9b7ec16b7bb6235d6f7111b80f9ae  mimir_sem.png   <- ZERO diferença\n\nE os 4 elementos são exatamente os que a regra manda RECUSAR:\n  $ grep -c 'style=\"\"' /home/vitoriamaria/mimir.svg  ->  4\n  python3: os 4 são `<path ... style=\"\">` e nenhum tem `fill=` (tem fill=? False, 4x).\n  Nenhum ancestral declara fill (o <svg> tem clip-rule/fill-rule; os <g> só transform).\n  Logo: o preto vem do valor inicial de `fill`, e apagar o atributo não pode mudar isso.\n  Nenhum asset do repo tem o problema: grep -c 'style=\"\"' assets/*.svg -> 0 em todos os 13.\n\n=== 2. O applet da logo NÃO EXISTE nesta máquina ===\n  $ flatpak list --columns=application | grep -i logo        -> (vazio)\n  $ ls /usr/share/applications | grep -i \"logomenu|cappsy\"   -> dev.cappsy.CosmicExtAppletDrives.desktop (só o de Drives)\n  $ command -v cosmic-ext-applet-logomenu cosmic-ext-logomenu-settings -> (nada)\n  $ cat ~/.config/cosmic/com.system76.CosmicPanel.Panel/v1/plugins_wings\n      esquerda: Battery, Tiling, dev.cappsy.CosmicExtAppletDrives, Workspaces\n      direita:  StatusArea, clipboard-manager, Bluetooth, Audio, Network, Power\n    plugins_center (Panel): Eyedropper, Time, Notifications\n    Dock/plugins_wings: Some(([\"com.system76.CosmicPanelAppButton\"], []))\n    Dock/plugins_center: Some([\"com.system76.CosmicAppList\"])\n    -> LogoMenu não aparece em NENHUM dos quatro.\n  $ ps -eo comm,args | grep -i \"logomenu\"  -> nada.  Vivo mesmo está: `cosmic-panel-button com.system76.CosmicAppLibrary`\n  Sobrou só a config órfã: ~/.config/cosmic/dev.cappsy.CosmicExtAppletLogoMenu/v1/{custom_logo_path,custom_logo_active,logo,menu_items}\n    custom_logo_path = \"/home/vitoriamaria/.config/cosmic/logos/meow-mocha.svg\"  (escrito pelo construir_icones.sh, lido por ninguém)\n  Modo de falha conhecido para \"applet configurado que não vira processo\":\n    ~/.config/zsh/scripts/aurora-painel-fantasma.sh:150-162 (sinal 2) — é o vigia que existe porque o painel trava e não cria layer surface.\n\n=== 3. As quatro asserções acusariam o que hoje está CERTO ===\nEstado atual, medido:\n  $ ./scripts/auditar_icones.sh\n    tema selecionado MeowSystem-Icons | 51 visíveis\n    pareados 51 de 51 (100%) — \"Tudo pareado. Nenhum app visível ficou sem ícone — nada a fazer.\"\n  $ ./scripts/auditar_icones.sh --lista | grep -iE \"steam|thunderbird|hefesto\"\n    Scarlet Deer Inn   steam                    usuário   Papirus-Dark (sistema)     <- RESOLVE\n    Thunderbird        thunderbird              sistema   MeowSystem-Icons (usuário) <- o .png RESOLVE\n    Hefesto - Duals…   hefesto-dualsense4unix   usuário   MeowSystem-Icons (usuário)\n  $ ./scripts/auditar_icones.sh --ocultos --lista | grep -i hefesto\n    Hefesto - Dualsense4Unix  com.vitoriamaria.Hefest…  sistema  MeowSystem-Icons (usuário)  <- NÃO é órfão\n\n(A) $ grep -H ^Icon= ~/.local/share/applications/steam-1553260.desktop -> Icon=steam\n    $ grep -H ^Icon= ~/\"Área de trabalho\"/\"Scarlet Deer Inn.desktop\"   -> Icon=steam   (a FONTE, escrita pela Steam)\n    /mnt/Apate/Desenvolvimento/MeowSystem/scripts/jogos_steam.sh:100 -> meow_escrever \"$destino\" \"$(cat \"$arq\")\" 644\n    e o cabeçalho (linha 30): \"Este script não escreve ícone nenhum\". Qualquer conserto na cópia é desfeito na próxima rodada = ping-pong interno.\n    (E `steam_icon_1553260` de fato não existe — a própria proposta reconhece.)\n\n(B) $ grep -rn \"steam-jogos\" ~/.config/zsh/scripts/  -> ~/.config/zsh/scripts/steam-gera-atalhos.sh\n    steam-gera-atalhos.sh:21  ICONS_DIR=\"$HOME/.local/share/icons/steam-jogos\"\n    steam-gera-atalhos.sh:26  rm -f \"$ICONS_DIR\"/steam_jogo_*.png   # \"cache de ícones sempre nasce limpo\"\n    steam-gera-atalhos.sh:61-62  linha_icone=\"Icon=$icone\"  (CAMINHO ABSOLUTO; só cai em Icon=steam sem arte)\n    Instalado pelo Aurora: ~/.config/zsh/scripts/ritual-aurora-self-heal.sh:952 -> /usr/local/bin/steam-gera-atalhos.sh\n    E o próprio auditor já ignora caminho absoluto: auditar_icones.sh:436 e :550 -> `if (i == \"\" || substr(i,1,1) == \"/\" || index(i,\"/\") > 0) next`\n    Ou seja: não é \"invisível\", é ícone por caminho, legal na spec, de OUTRO dono, recriado do zero a cada execução dele.\n\n(C) /mnt/Apate/Desenvolvimento/MeowSystem/scripts/completar_icones.sh:178\n      [thunderbird]=\"/usr/share/icons/hicolor/48x48/apps/thunderbird.png\"   (mapa APELIDO, deliberado)\n    docs/COSMIC-THEMING.md §2: \"Dentro de um tema, a extensão é o laço externo: todos os .svg ... são tentados antes de qualquer .png\" — png no tema resolve.\n\n(E) $ cat /usr/share/applications/com.vitoriamaria.HefestoDualsense4Unix.desktop\n      Icon=com.vitoriamaria.HefestoDualsense4Unix | NoDisplay=true | X-CosmicApplet=true | Exec=hefesto-dualsense4unix-applet\n    completar_icones.sh:118-124 explica que os DOIS nomes são de propósito (lançador dela + applet empacotado).\n\nMecânica do doctor que transforma isso em dano (bin/meow):\n  linha 348  VERIFICAVEIS=(conf gerar tema modo vidro greeter icones hicolor jogos pastas wallpaper apps)\n  linha 349  SEM_CONSERTO=\" conf \"\n  cmd_doctor: `saida=\"$(\"fix_$nome\" 2>&1)\"; rc=$?` ... `case \"$rc\" in 0|1) mexeu=1 ;; *) falhou=1` ; `falhou=1 -> return MEOW_ERRO` (2)\n  ~/.config/systemd/user/meow-doctor.service: `SuccessExitStatus=1 3` (2 = FALHA) e\n    ExecStopPost=... '[ \"$EXIT_STATUS\" = 1 ] || exit 0; ... notify-send ... \"O tema estava divergente e o auto-reparo corrigiu.\"'\n  O cabeçalho da própria unidade: \"sem esse detalhe o auto-reparo avisaria todo dia, mesmo sem ter feito nada, e ela aprenderia a ignorar o aviso.\"\n  Timer ativo agora: `systemctl --user list-timers` -> meow-doctor.timer  Thu 2026-08-06 05:18:49.\n\n=== 4. As duas medições novas do doc estão erradas ===\n  $ grep -l \"X-CosmicApplet=true\" /usr/share/applications/*.desktop | wc -l   -> 18\n  $ grep -h ^Icon= <esses 18> | grep -c -- \"-symbolic\"                        -> 10   (não \"11 de 13\")\n  Bandeja, por D-Bus (leitura pura):\n  $ busctl --user call org.kde.StatusNotifierWatcher /StatusNotifierWatcher org.freedesktop.DBus.Properties Get ss org.kde.StatusNotifierWatcher RegisteredStatusNotifierItems\n      v as 3 \":1.380\" \":1.567\" \":1.643/org/ayatana/NotificationItem/hefesto_dualsense4unix\"\n  $ ...Get ss org.kde.StatusNotifierItem IconName  ->  :1.380 (qBittorrent) = \"\"   ;  :1.567 (ZapZap) = \"\"\n      ;  hefesto_dualsense4unix = \"hefesto-dualsense4unix\"   <- PASSA PELO TEMA, e já é tematizado por nós\n  $ strings -a /usr/bin/cosmic-applet-status-area | grep -oiE \"IconPixmap|IconName|IconThemePath\" | sort | uniq -c\n      5 IconName / 1 IconPixmap / 1 IconThemePath\n\n=== 5. O que a proposta acertou (medido, para não jogar fora) ===\n  $ strings -a /usr/bin/cosmic-panel | grep -ciE \"usvg|resvg\" -> 54 (idem cosmic-panel-button e cosmic-app-list)\n  $ strings -a /usr/bin/cosmic-panel | grep -oiE \"No fonts with a|Selector skipped|filter input isn't supported\"\n      -> as três mensagens existem literalmente. As regras 1c estão ancoradas no renderizador certo.\n  (Mas hoje nenhum SVG do projeto tem <text> ou <style>: grep -l \"<text|<style\" assets/*.svg tema/*.svg /home/vitoriamaria/*.svg -> vazio.)\n  Limiar de 1% do item 1f não daria falso positivo hoje: cobertura não-transparente a 128px dos 16 ícones do tema = 31,6% a 74,85% (menor: meow-symbolic 31,60%).\n  Dependência: $ command -v rsvg-convert -> /usr/bin/rsvg-convert ; install.sh:289 [rsvg-convert]=librsvg2-bin, e install.sh:315-332 só sabe `apt-get`.\n  Travas: lib/comum.sh meow_destino_permitido cobre /usr/share, ~/.config/zsh e *Shortcuts* — nada do plano bate nelas. auditar_icones.sh:186-187 é `mktemp -d` + `trap rm -rf` (leitura). Nenhum trecho do plano chama pkill.\n  Aurora: `grep -rn custom_logo|plugins_wings` em ~/.config/zsh/ -> o self-heal só instala o ARQUIVO ~/.config/cosmic/logos/gato-pop.svg (linha 1503) e /usr/share/icons/hicolor/scalable/apps/com.system76.CosmicAppLibrary.svg (linha ~790); NÃO escreve as chaves. Sem ping-pong de chave — mas atenção: o self-heal reescreve o AppLibrary.svg de /usr/share de hora em hora, e só não nos atrapalha porque o tema selecionado (~/.local/share/icons/MeowSystem-Icons) vence antes do hicolor de sistema.",
            "correcao_sugerida": "FRENTE 1 — saneador: manter, com a regra 1a REESCRITA e o portão invertido.\n  a. `style=\"\"` passa a ser PODA COSMÉTICA, declarada como tal: remove-se porque é lixo do editor, e o saneador diz \"nenhum pixel muda\" (comprovado por md5 de render). PROIBIDO recusar o arquivo por causa dela.\n  b. A conferência que interessa não é \"tem style vazio\", é \"este elemento fica com a cor que ela quis?\". Troque por um RELATÓRIO, nunca um erro: liste os elementos cuja cor efetiva é o preto inicial do CSS (sem fill próprio e sem ancestral com fill) e diga \"estes N caminhos vão sair PRETOS; se não é o que você quis, pinte-os no Boxy\". Isso resolve o caso real do mimir.svg — que hoje desenha, e desenha preto — sem adivinhar cor e sem barrar arte.\n  c. Regra do `-symbolic`: contar cores RESOLVIDAS, ignorando `none` e `currentColor`, senão `assets/meow-symbolic.svg` (fill=\"currentColor\" + fill=\"none\") é acusado sem motivo.\n  d. Item 1f (prova de que ainda desenha): manter, mas `meow_tem rsvg-convert || { meow_pula \"sem rsvg-convert — prova de render pulada\"; }` devolvendo 3 (MEOW_SEM_DEPENDENCIA), NUNCA falha. E dizer no doc que o limiar mede \"ficou vazio\", não \"ficou igual ao resvg\". Medido: 31,6%-74,85% de cobertura hoje, então 1% é folgado.\n  e. Manter 1b (viewBox/width/height), 1d (poda de sodipodi:/inkscape:/boxy:) e 1c (as três recusas do usvg — ancoradas: as mensagens existem no binário).\n\nFRENTE 2 — logo: ANTES de escrever uma linha, decidir com base no fato de que o applet NÃO ESTÁ INSTALADO.\n  - O `construir_icones.sh` deve PULAR o bloco 3 quando `dev.cappsy.CosmicExtAppletLogoMenu` não existe (teste: `flatpak info dev.cappsy.CosmicExtAppletLogoMenu` OU `.desktop` OU o diretório de config com o applet em algum plugins_*), imprimindo \"o applet da logo não está instalado — o gato do painel é só o do dock\". Hoje o projeto escreve uma chave que ninguém lê, e o `meow logo` exibe um caminho que não vira imagem nenhuma: isso é o bug de verdade desta frente.\n  - O nome com hash + limpeza de órfãos é BOM e converge — guarde-o para quando o applet existir, atrás do mesmo teste de presença. A limpeza deve casar só `meow-<flavor>-*.svg` derivado do disco e jamais tocar `gato-pop.svg` (§5).\n  - `LOGO_NO_PAINEL=1` em `plugins_wings`: cortar. Escrever id de applet ausente no painel dela é arriscar o modo de falha que o `aurora-painel-fantasma.sh` existe para consertar, numa máquina de uma tela. Se um dia entrar, só com o applet comprovadamente instalado E fora do `doctor`.\n\nFRENTE 3 — auditor: `--conferir` sim, com asserções que o projeto POSSA satisfazer.\n  Ficam (todas conserta-veis por nós): (D) todo SVG do tema passa no `sanear_svg.py --conferir`; (novo) todo `Icon=` que o `completar_icones.sh` promete entregar está no tema; (novo) `CosmicTk/v1/icon_theme` aponta para o nosso tema e o `index.theme` descreve o disco (§4d); (novo) nenhum arquivo do tema termina em `-symbolic.svg` com mais de uma cor.\n  SAEM do `--conferir` e viram, no máximo, linhas informativas do `meow icones auditar` (comando de mão, sem código de saída venenoso): A (Icon=steam do Scarlet Deer Inn — é da Steam e o jogos_steam.sh copia byte a byte), B (steam-jogos/ é do `steam-gera-atalhos.sh` do Aurora e usa caminho absoluto, que o próprio auditor já pula), C (thunderbird.png é deliberado e RESOLVE), E (com.vitoriamaria.HefestoDualsense4Unix é pedido por `/usr/share/applications/com.vitoriamaria.HefestoDualsense4Unix.desktop`, X-CosmicApplet=true — não é órfão).\n  Regra de ouro a escrever no `bin/meow`: só entra em `VERIFICAVEIS` o que tem `fix_<nome>` que de fato converge; qualquer conferência sem conserto vai para `SEM_CONSERTO` E não pode devolver 1 eternamente. Antes de ligar no `doctor`, rodar duas vezes seguidas e provar `rc=1` depois `rc=0`.\n\nFRENTE 4 — doc: escrever os números medidos, não os lembrados.\n  - \"-symbolic\": 10 dos 18 `.desktop` com `X-CosmicApplet=true` em /usr/share/applications (comando no doc).\n  - Bandeja: trocar a frase absoluta por \"depende do app: quem manda `IconName` passa pelo tema (o applet Hefesto dela manda, e nós já o vestimos); quem manda `IconPixmap` (qBittorrent, ZapZap — medidos com busctl) é inalcançável\". O texto que o doctor mostrar a ela tem de ser esse, senão ele mente sobre o app dela.\n  - §3b pode registrar, com o comando, o que confirmei: resvg/usvg vendorizado no cosmic-panel/panel-button/app-list (54 ocorrências + as três mensagens literais).\n  - Acrescentar um §3c com o achado desta rodada: o applet LogoMenu não está instalado, e por isso `custom_logo_path` é hoje uma chave sem leitor — com o comando `flatpak list | grep -i logo` e o `cat` dos quatro `plugins_*`.\n\nOrdem de pagamento sugerida (invertida em relação à proposta): (1) o pulo do bloco da logo quando o applet não existe — é o único bug que hoje faz o projeto escrever no vazio; (2) o relatório de \"vai sair preto\" do saneador, que resolve o caso real dela; (3) o `--conferir` enxuto do auditor; (4) o doc."
          }
        ]
      },
      {
        "tema": "greeter-bonito",
        "resultado": {
          "titulo": "A tela de login já é dela por baixo — o greeter lê 5 chaves próprias, e um daemon root traz o resto da home dela",
          "causa_ou_achado": "ARQUITETURA REAL (medida, e diferente do que o repo assume)\n\nO cosmic-greeter NÃO é um app que lê a própria configuração e pronto. São DOIS processos, e a maior parte do que aparece na tela de login vem do lado que o scripts/greeter.sh nem toca:\n\n1. cosmic-greeter-daemon (uid 0, Type=dbus, BusName=com.system76.CosmicGreeter, método único GetUserData) varre os usuários entre UID_MIN=1000 e UID_MAX=60000 de /etc/login.defs e, COMO ROOT, entra na home de cada um (faz o baile \"set user uid\" / \"restore root uid\" — strings no binário) e colhe: uid, full_name, icon_opt, theme_opt, theme_builder_opt, bg_path_data, xkb_config_opt, time_applet_config, accessibility_zoom, wallpapers. Entrega por D-Bus (política: só root e o grupo cosmic-greeter).\n2. cosmic-greeter (uid 987, HOME=/var/lib/cosmic-greeter) lê da PRÓPRIA pasta de config APENAS CINCO árvores: com.system76.CosmicGreeter (só last_user e users), com.system76.CosmicTk, CosmicTheme.Light, CosmicTheme.Dark, CosmicTheme.Mode. Nada mais.\n\n(a-i) PAPEL DE PAREDE: não existe chave. O greeter desenha, por saída, a imagem que o cosmic-bg DELA registrou por último em ~/.local/state/cosmic/com.system76.CosmicBackground/v1/wallpapers, cujos BYTES o daemon-root leu e mandou em bg_path_data. Ou seja: a tela de login JÁ usa o carrossel dela — a mesma imagem que estava na tela quando ela deslogou. A armadilha do drwx------ NÃO vale aqui: quem lê é root, não o uid 987. Provado pelo journal, onde a falha é NotFound (arquivo apagado), não PermissionDenied.\n\n(a-ii) AVATAR: /var/lib/AccountsService/icons/<user> (o daemon monta o caminho por nome; não usa users/<user> nem ~/.face). JÁ ESTÁ LÁ E JÁ É O GATO — md5 idêntico a assets/meow-mocha-preto.svg — mas NENHUM script do repo o colocou: foi feito na mão numa sessão anterior. Estado não reproduzível, e é o buraco que o módulo tem que fechar. A rota certa é SetIconFile do AccountsService, que NÃO PEDE SENHA (polkit org.freedesktop.accounts.change-own-user-data tem allow_any=yes).\n\n(a-iii) O QUE ELE NÃO TEM: mensagem/banner, layout, relógio configurável no greeter, papel de parede fixo próprio. O schema com.system76.CosmicGreeter tem duas chaves e as duas são memória de sessão (last_user, users) — escrever ali apaga a lembrança do último login dela. O relógio existe, mas a configuração vem do com.system76.CosmicAppletTime DELA (military_time, show_seconds, first_day_of_week, format_strftime), via daemon. BLUR ELE TEM: o greeter faz bind de ExtBackgroundEffectManagerV1 e OrgKdeKwinBlurManager, e as chaves frosted/frosted_windows/frosted_panel/BlurStrength são do TEMA — o vidro do MeowSystem já vale no login.\n\n(b) ESTADO ATUAL: não consegui medir por dentro. sudo -n falha nesta sessão (\"uma senha é necessária\"). /var/lib/cosmic-greeter é drwxr-x--- 987 e ls devolve \"Permissão negada\" — a armadilha documentada, confirmada de novo. Lista de comandos que preciso que você autorize está em incertezas.\n\n(c) AVATAR — o caminho idempotente: IconFile via D-Bus já responde /var/lib/AccountsService/icons/vitoriamaria, e o arquivo é o gato. Conferir = ler a propriedade IconFile + md5 do arquivo; aplicar = SetIconFile (sem sudo); reverter = guardar o IconFile anterior em ~/.local/state/meowsystem/greeter/avatar-anterior. RISCO REAL: o gato é SVG e o greeter só tem os codecs bmp/hdr/ico/jpeg/png/webp da crate image. Ele tem resvg vendorizado, mas isso é do tema de ícones, não necessariamente do caminho do avatar. Entregar PNG rasterizado resolve nos dois casos (rsvg-convert -w 256 -h 256 já produz 24 KB, testado).\n\n(d) WALLPAPER — nada a copiar. Não existem 239 imagens para duplicar, nem caminho novo, nem /usr/share (que a trava 1 do lib/comum.sh recusa de qualquer jeito). O trabalho do módulo é só manter o PONTEIRO vivo, e o scripts/wallpaper.sh já faz isso (limpar_estado_morto). O que precisa de correção é a ATRIBUIÇÃO do sintoma: o comentário do wallpaper.sh e o do greeter.sh dizem que quem reclama é a tela de BLOQUEIO; medido hoje, quem reclamou em 04/08 23:39:52 foi o cosmic-greeter-daemon[1062] (root, no boot) — ou seja, naquele boot a TELA DE LOGIN ficou sem papel de parede. É a mesma mensagem em dois emissores (13x + 11x do cosmic-greeter dela, 2x do daemon).\n\n(e) FONTE E TAMANHO: o com.system76.CosmicTk do greeter aceita exatamente icon_theme, header_size, show_maximize, show_minimize, interface_font, monospace_font, interface_density, apply_theme_global. A struct de fonte é (family, weight, stretch, style) — NÃO EXISTE tamanho. Os dois botões reais para a TV de 52\" são interface_density (Compact/Standard/Spacious) e header_size. Hoje ela está em Compact nos dois, que é o menor possível. Escala de verdade mora em .local/state/cosmic-comp/outputs.ron (a dela tem scale: 1.05 no DP-1), um mapa RON com chave de tupla connector/make/model — editável, mas frágil. COSMIC_SCALE existe no binário mas o greetd monta o ambiente da sessão do zero e não medi propagação: não medido.\n\n(f) PORTABILIDADE: /etc/systemd/system/display-manager.service é symlink e diz quem manda. Aqui aponta para cosmic-greeter.service. Em gdm/sddm/lightdm aponta para outro, e o módulo pula sem escrever nada. O HOME do greeter sai de getent passwd, nunca cravado.",
          "confianca": "forte",
          "evidencias": [
            {
              "comando": "strings -a /usr/bin/cosmic-greeter-daemon | grep -o 'uidfull_name[A-Za-z_]*'",
              "saida_relevante": "uidfull_nametheme_opttheme_builder_optbg_path_dataxkb_config_opttime_applet_configaccessibility_zoom  (+ 'icon_opt' e 'wallpapers' no mesmo pool de constantes)",
              "o_que_prova": "A struct UserData que o daemon serializa. Prova que tema, papel de parede (bytes), avatar, layout de teclado, config do relogio e zoom de acessibilidade sao TRANSPORTADOS da home do usuario para o greeter — nao lidos pelo uid 987."
            },
            {
              "comando": "sed -n '1244,1249p' <strings do daemon>  # XML de introspeccao embutido",
              "saida_relevante": "<interface name=\"com.system76.CosmicGreeter\">\\n<method name=\"GetUserData\">\\n<arg type=\"...\" direction=\"out\"/>\\n</method>\\n</interface>",
              "o_que_prova": "A superficie D-Bus inteira do daemon e UM metodo de leitura. Nao ha nada a configurar por esse lado; e um cano, nao um painel."
            },
            {
              "comando": "journalctl --since '-7 days' | grep 'failed to read wallpaper'  (+ contagem por emissor)",
              "saida_relevante": "ago 04 23:39:52 cosmic-greeter-daemon[1062]: failed to read wallpaper \"/home/vitoriamaria/.local/share/backgrounds/meowsystem/ativos/_ (2).jpeg\": Os { code: 2, kind: NotFound }   ||   contagem: 13x cosmic-greeter[2859], 11x cosmic-greeter[3112], 2x cosmic-greeter-daemon[1062]",
              "o_que_prova": "DOIS fatos. (1) O erro e NotFound (ENOENT), nao EACCES: o daemon-root ATRAVESSA a home drwxr-x--- dela e le dentro do .local/share drwx------ sem problema — a armadilha do uid 987 nao se aplica ao papel de parede. (2) Quem reclamou naquele boot foi o DAEMON, nao a tela de bloqueio: e a tela de LOGIN que ficou sem papel de parede. Corrige a atribuicao escrita em scripts/wallpaper.sh e scripts/greeter.sh."
            },
            {
              "comando": "cat ~/.local/state/cosmic/com.system76.CosmicBackground/v1/wallpapers",
              "saida_relevante": "[ (\"DP-2\", Path(\"/usr/share/backgrounds/cosmic/orion_nebula_nasa_heic0601a.jpg\")), (\"DP-1\", Path(\"/home/vitoriamaria/.local/share/backgrounds/meowsystem/ativos/cat-gradients-bkg3_bkg5.png\")) ]",
              "o_que_prova": "A fonte exata do papel de parede do login: o ESTADO do cosmic-bg dela, saida por saida. Nao ha carrossel no login — ha a ultima imagem sorteada antes do logout. Nada a copiar, nenhuma das 239 imagens duplicada."
            },
            {
              "comando": "strings -a /usr/bin/cosmic-greeter | grep -B0 -A0 'cosmic-greeter-config/src/lib.rs' (contexto)",
              "saida_relevante": "event cosmic-greeter-config/src/lib.rs:46 cosmic_greeter_config message com.system76.CosmicGreeter event cosmic-greeter-config/src/lib.rs:39 last_user users ... uid last_session",
              "o_que_prova": "O schema proprio do greeter tem SO last_user/users (uid -> last_session). Nao existe chave de mensagem, layout, relogio ou papel de parede. E: escrever ali apagaria a memoria do ultimo login dela."
            },
            {
              "comando": "strings -a /usr/bin/cosmic-greeter | grep -o '.\\{200\\}interface_density.\\{200\\}'",
              "saida_relevante": "...FontConfig family weight stretch style icon_theme header_size show_maximize show_minimize interface_font monospace_font interface_density apply_theme_global com.system76.CosmicTk ... vendor/libcosmic/src/config/mod.rs:31   |   noutro ponto: com.system76.CosmicGreeter com.system76.CosmicTk com.system76.CosmicTheme.Light com.system76.CosmicTheme.Dark com.system76.CosmicTheme.Mode",
              "o_que_prova": "(1) A lista FECHADA de arvores que o greeter le da propria pasta: Greeter, Tk, Light, Dark, Mode — confirma que greeter.sh acertou ao NAO criar as .Builder. (2) A lista fechada de chaves do CosmicTk, onde a fonte e (family,weight,stretch,style): NAO EXISTE tamanho de fonte. Sobram interface_density e header_size como botoes de tamanho."
            },
            {
              "comando": "md5sum /var/lib/AccountsService/icons/vitoriamaria assets/meow-mocha-preto.svg ; ls -l ; grep -rn AccountsService no repo e em ~/.config/zsh",
              "saida_relevante": "3a496a3f5c1e99c231ff741800ac3757  /var/lib/AccountsService/icons/vitoriamaria\\n3a496a3f5c1e99c231ff741800ac3757  ./assets/meow-mocha-preto.svg\\n-rw-r--r-- 1 root root 1971 ago  4 23:54\\n(grep no repo e no Aurora: zero ocorrencias)",
              "o_que_prova": "O gato JA e o avatar de login, byte a byte igual ao asset do repo — mas nem o MeowSystem nem o Ritual da Aurora sabem disso. Foi escrito na mao. Numa maquina limpa (ou depois de um reset) o avatar nao existe. O modulo precisa passar a ser o dono."
            },
            {
              "comando": "grep -A6 'change-own-user-data' /usr/share/polkit-1/actions/org.freedesktop.accounts.policy ; busctl --system get-property org.freedesktop.Accounts /org/freedesktop/Accounts/User1000 org.freedesktop.Accounts.User IconFile",
              "saida_relevante": "<defaults><allow_any>yes</allow_any><allow_inactive>yes</allow_inactive><allow_active>yes</allow_active>   ||   s \"/var/lib/AccountsService/icons/vitoriamaria\"",
              "o_que_prova": "O avatar NAO precisa de sudo: SetIconFile no proprio usuario e liberado pelo polkit sem senha. E a propriedade IconFile e legivel sem privilegio — logo da para CONFERIR idempotencia sem sudo nenhum. Alem disso o keyfile em users/<user> ja esta em sincronia, o que a escrita direta no arquivo nao garantiria."
            },
            {
              "comando": "strings -a /usr/bin/cosmic-greeter | grep -o 'vendor/image/src/codecs/[a-z0-9]*' | sort -u ; e grep de ExtBackgroundEffect/KWinBlur",
              "saida_relevante": "codecs: bmp, hdr, ico, jpeg, png, webp  (nenhum svg)   ||   ExtBackgroundEffectManagerV1, ExtBackgroundEffectSurfaceV1, OrgKdeKwinBlurManager, BlurStrength",
              "o_que_prova": "(1) O decodificador de imagem em memoria do greeter nao sabe SVG — o avatar deve ser PNG para ser seguro (resvg existe no binario, mas serve ao tema de icones). (2) O greeter FAZ bind dos protocolos de blur: o vidro/frosted do tema MeowSystem vale na tela de login, sem chave extra."
            },
            {
              "comando": "ls -l /etc/systemd/system/display-manager.service ; getent passwd cosmic-greeter ; cat /usr/lib/tmpfiles.d/cosmic-greeter.conf ; ls /var/lib/cosmic-greeter/ ; sudo -n true",
              "saida_relevante": "display-manager.service -> /lib/systemd/system/cosmic-greeter.service\\ncosmic-greeter:x:987:987:Cosmic Greeter Account:/var/lib/cosmic-greeter:/usr/sbin/nologin\\nd /var/lib/cosmic-greeter 0750 cosmic-greeter cosmic-greeter\\nd /run/cosmic-greeter 0755 cosmic-greeter cosmic-greeter -\\nls: nao foi possivel abrir o diretorio '/var/lib/cosmic-greeter/': Permissao negada\\nsudo: uma senha e necessaria",
              "o_que_prova": "(1) A deteccao portavel: o symlink display-manager.service diz qual greeter manda, e getent da o HOME sem cravar caminho. (2) O tmpfiles.d reafirma o modo 0750 a cada boot mas NAO apaga conteudo — a config sobrevive. (3) A armadilha de permissao confirmada mais uma vez, e sudo -n nao esta disponivel nesta sessao: o estado interno ficou por medir."
            },
            {
              "comando": "cat ~/.local/state/cosmic-comp/outputs.ron ; cat ~/.config/cosmic/com.system76.CosmicTk/v1/{interface_density,header_size}",
              "saida_relevante": "connector \"DP-1\", make \"PNP(HSI)\", model \"HiTV\" -> mode ((1920,1080),Some(60000)), scale: 1.05   ||   interface_density: Compact ; header_size: Compact",
              "o_que_prova": "Os dois botoes de tamanho para a TV de 52\": o greeter herda densidade Compact (a menor) se copiarmos o CosmicTk dela, e a escala real do compositor mora num outputs.ron com chave de tupla connector/make/model. Copiar o CosmicTk dela sem pensar DEIXA A TELA DE LOGIN MENOR do que o padrao."
            },
            {
              "comando": "cat /usr/bin/cosmic-greeter-start ; strings -a /usr/bin/cosmic-comp | grep -o '.\\{80\\}/run/cosmic-greeter.\\{60\\}'",
              "saida_relevante": "rm -rf /run/cosmic-greeter/cosmic/com.system76.CosmicSettingsDaemon/v1/* ; exec cosmic-comp cosmic-greeter   ||   ...high_contrast invert_colors /run/cosmic-greeter com.system76.CosmicSettingsDaemon...",
              "o_que_prova": "/run/cosmic-greeter e so ESTADO do SettingsDaemon, limpo a cada arranque — nao e o XDG_CONFIG_HOME. Confirma que a pasta de config e mesmo $HOME/.config/cosmic = /var/lib/cosmic-greeter/.config/cosmic (reforcado pelo journal: 'failed to read session directory /var/lib/cosmic-greeter/.local/share/flatpak/...'). A premissa do greeter.sh atual esta correta."
            }
          ],
          "solucao": {
            "descricao": "MODULO 'greeter' EM QUATRO PARTES, EM ORDEM DE CUSTO/BENEFICIO.\n\nPARTE 0 — DETECCAO E DEGRADACAO (vale para toda parte)\n  detectar_greeter(): (i) readlink -f /etc/systemd/system/display-manager.service termina em 'cosmic-greeter.service'? (ii) getent passwd cosmic-greeter existe? -> GREETER_HOME=campo 6, nunca cravado. Se (i) falhar -> meow_pula 'o gerenciador de login desta maquina e <nome> — a tela de login fica como esta', exit 0, ZERO escritas. Se (ii) falhar -> exit 0 igual. Se ambos passarem mas 'sudo -n test -d $GREETER_HOME/.config/cosmic' falhar -> exit 3 imprimindo o comando exato (NUNCA um '[ -d ]' comum: devolve falso por permissao).\n\nPARTE 1 — AVATAR (o unico ganho visual grande que NAO precisa de sudo)\n  Conferir: busctl --system get-property org.freedesktop.Accounts /org/freedesktop/Accounts/User$(id -u) org.freedesktop.Accounts.User IconFile, e md5 do arquivo apontado contra o nosso PNG. Igual -> ok.\n  Aplicar: rasterizar assets/meow-<flavor>-preto.svg -> $MEOW_ESTADO/greeter/avatar.png (256x256, rsvg-convert; a crate 'image' do greeter nao decodifica SVG) e chamar SetIconFile com esse caminho. Polkit libera sem senha (allow_any=yes). O AccountsService copia e mantem users/<user> em sincronia sozinho — por isso NAO escrever em /var/lib/AccountsService/icons na mao.\n  Reverter: gravar o IconFile anterior em $MEOW_ESTADO/greeter/avatar-anterior ANTES da primeira troca; 'meow restaurar' chama SetIconFile com ele (ou com '' se estava vazio).\n\nPARTE 2 — TEMA (o que ja existe, com duas correcoes)\n  Manter a copia de CosmicTheme.Dark/Light/Mode, e continuar sem as .Builder — agora provado: a lista de arvores que o greeter le da propria pasta e exatamente {CosmicGreeter, CosmicTk, Light, Dark, Mode}.\n  Correcao (a): trocar os N 'sudo -n cmp' por UMA leitura — sudo -n find \"$DESTINO\" -type f -printf '%P ' -exec md5sum {} + — e comparar contra os md5 locais. Em ~190 arquivos e a diferenca entre duzias de chamadas e uma.\n  Correcao (b): NUNCA tocar em $DESTINO/com.system76.CosmicGreeter/v1/{last_user,users} — e a memoria de qual usuario/sessao entrou por ultimo. Adicionar isso a lista de exclusao explicita.\n\nPARTE 3 — TAMANHO NA TV (o pedido 'ficou pequeno')\n  Escrever, no CosmicTk do greeter (arvore nova, mas dentro de uma que ele comprovadamente le):\n    interface_density = Spacious   (ela usa Compact no desktop; copiar o dela ENCOLHE o login)\n    header_size       = Standard\n    interface_font    = (family: \"Fira Sans\", weight: Normal, stretch: Normal, style: Normal)\n    monospace_font    = deixar de fora, o login nao mostra monoespacada\n    apply_theme_global= nao escrever (e efeito colateral em GTK, nao e do greeter)\n    icon_theme        = NAO ESCREVER, e isso continua deliberado (§4e: o tema mora em ~/.local/share/icons, ilegivel para o uid 987 -> icone FALTANDO, pior que icone errado)\n  GUARDA OBRIGATORIA na fonte: so aceitar family cujo arquivo, por fc-list, esteja sob /usr/share/fonts ou /usr/local/share/fonts. Uma family que so exista em ~/.local/share/fonts (onde o instalar_fontes.sh poe a JetBrainsMono Nerd) cai em silencio no fallback — e a MESMA armadilha do icon_theme, so que numa chave nova. Fira Sans passa: /usr/share/fonts/opentype/fira, do pacote pop-fonts.\n  FORA DO v1: mexer em .local/state/cosmic-comp/outputs.ron para forcar scale. E um mapa RON com chave de tupla (connector,make,model) que o cosmic-comp reescreve sozinho; um erro ali e uma tela de login em resolucao errada, sem GUI para consertar. Se um dia entrar, entra atras de GREETER_ESCALA no meow.conf, desligado por padrao, e com backup do arquivo.\n\nPARTE 4 — PAPEL DE PAREDE (nao ha o que instalar)\n  Documentar e parar. A imagem do login e, por construcao, a ultima do carrossel dela, lida por root e entregue em bytes. O unico modo de quebra-la e o ponteiro morto — e o scripts/wallpaper.sh ja limpa. O que muda: (i) corrigir o comentario do wallpaper.sh e do greeter.sh, que culpam a tela de BLOQUEIO por um erro que hoje foi medido saindo do cosmic-greeter-daemon (login); (ii) o 'meow doctor' passa a avisar quando o estado tem caminho morto, porque agora sabemos que o preco disso e a tela de login sem fundo no proximo boot.\n\nSUDO, PEDIDO UMA VEZ E EM VOZ ALTA\n  As partes 2 e 3 sao as unicas que precisam. Seguir o padrao ja existente do etapa_pacotes: montar a lista completa de arquivos ANTES, imprimir 'vou rodar como root, e so isto:' com os comandos, e executar num unico bloco (um 'sudo install -D -o cosmic-greeter -g cosmic-greeter -m 644' por arquivo, mas todos dentro da mesma janela de credencial). A parte 1 (avatar) roda ANTES do bloco de sudo, de proposito: e a mudanca mais visivel e nao custa senha nenhuma — se ela nao tiver sudo agora, ainda assim a tela de login ganha o gato.\n\nCHAVES NOVAS NO meow.conf.exemplo (o migrar_conf.py ja sabe acrescentar em conf existente)\n  GREETER=\"sim\"                 # sim|nao — desliga o modulo inteiro\n  GREETER_AVATAR=\"gato\"         # gato|nenhum|<caminho para png/jpg>\n  GREETER_DENSIDADE=\"Spacious\"  # Compact|Standard|Spacious\n  GREETER_FONTE=\"Fira Sans\"     # so families em /usr/share/fonts; vazio = nao escrever\n\nFORA DE ALCANCE, E VALE DIZER NO README\n  mensagem/banner de boas-vindas, formato do relogio pelo lado do greeter (vem do CosmicAppletTime DELA), plano de fundo fixo so do login, layout dos campos, tamanho de fonte em pt. O blur NAO esta fora de alcance: ja vem no tema.",
            "arquivos_a_mexer": [
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/greeter.sh",
              "/mnt/Apate/Desenvolvimento/MeowSystem/meow.conf.exemplo",
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/migrar_conf.py",
              "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow",
              "/mnt/Apate/Desenvolvimento/MeowSystem/install.sh",
              "/mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md",
              "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/wallpaper.sh"
            ],
            "idempotencia": "Cada parte tem um conferir que le e NAO escreve, e todos comparam por conteudo (regra 5).\n\nAVATAR: a propriedade IconFile do D-Bus e legivel sem privilegio; conferir = IconFile aponta para um arquivo cujo md5 bate com o nosso PNG. Bate -> exit 0. Nao bate -> exit 1 no seco, SetIconFile no aplicar. Rodar mil vezes chama SetIconFile zero vez depois da primeira. O AccountsService normaliza o destino (copia para assets/icones/<user>), entao comparar pelo destino e nao pela origem e o que evita o ping-pong.\n\nTEMA E CosmicTk: uma unica chamada 'sudo -n find $DESTINO -type f -exec md5sum {} +' constroi o mapa do lado do greeter; comparar com md5sum local. So os divergentes entram na lista de escrita, e o install -D fixa dono/grupo/modo na mesma chamada (um chown esquecido deixa o greeter sem poder LER a propria config — falha silenciosa, tema de fabrica de novo).\n\nO tmpfiles.d reafirma o modo 0750 de /var/lib/cosmic-greeter a cada boot mas nao apaga conteudo: nao ha erosao entre boots, o conferir devolve 'ja esta' para sempre.\n\nO QUE NAO PODE ENTRAR NO CONFERIR: o estado do papel de parede. Medido em 05/08 e ja escrito no wallpaper.sh — o cosmic-bg reescreve o arquivo inteiro a cada 5 minutos a partir da memoria, entao qualquer 'conserto' volta atras sozinho e o doctor acusaria divergencia eterna. Continua sendo limpeza silenciosa, nao divergencia.\n\nREVERSIVEL: avatar guarda o IconFile anterior; as arvores de tema e o CosmicTk do greeter entram no backup do 'meow restaurar' como copia da arvore inteira sob $MEOW_ESTADO/backup/greeter/, com o mesmo install -o cosmic-greeter na volta.",
            "riscos": [
              "A FONTE QUE SOME EM SILENCIO: escrever interface_font com uma family que so existe em ~/.local/share/fonts (drwx------) faz o greeter cair no fallback sem dizer nada — mesma classe da armadilha do icon_theme do §4e, numa chave nova. Mitigacao obrigatoria: validar por fc-list que o arquivo da family esta sob /usr/share/fonts ou /usr/local/share/fonts antes de escrever.",
              "O AVATAR EM SVG PODE NAO DESENHAR: a crate image do greeter tem bmp/hdr/ico/jpeg/png/webp e nenhum svg. Nao consegui provar qual caminho o avatar percorre (o resvg vendorizado pode ser so do tema de icones) — por isso o modulo entrega PNG rasterizado, que funciona nos dois casos. Se entregassemos o SVG do repo, o sintoma seria 'o gato nao apareceu' sem nenhuma linha no journal.",
              "ESCREVER EM com.system76.CosmicGreeter/v1 APAGA A MEMORIA DELA: as duas unicas chaves do schema proprio do greeter sao last_user e users (uid -> last_session). Um 'restaurar por copia da arvore inteira' que inclua essa pasta reseta qual usuario vem pre-selecionado e qual sessao ele abre. Precisa de exclusao explicita, do mesmo jeito que pinned_workspaces ficou fora do restaurar.",
              "COPIAR O CosmicTk DELA CRU DEIXA O LOGIN MENOR: ela roda interface_density=Compact e header_size=Compact. Um modulo que 'espelhe o desktop' entrega exatamente o oposto do que ela pediu numa TV de 52 polegadas. Os valores do greeter tem que ser proprios, nao herdados.",
              "O outputs.ron DO GREETER E TERRENO MOVEDICO: e estado que o cosmic-comp reescreve, com chave de tupla (connector,make,model) que muda quando ela troca de TV ou de cabo. Escrever ali pode deixar a tela de login numa escala errada sem GUI para desfazer. Fica fora do v1.",
              "SEM sudo NAO DA PARA CONFERIR NADA DO TEMA: /var/lib/cosmic-greeter e drwxr-x--- do uid 987, e um teste comum devolve falso por PERMISSAO. Se o modulo esquecer o sudo -n em QUALQUER teste, ele anuncia 'nao ha cosmic-greeter nesta maquina' numa maquina que tem — o erro ja cometido uma vez neste projeto.",
              "PORTABILIDADE DO CAMINHO: /var/lib/cosmic-greeter e o HOME no Pop!_OS 24.04; em outra distro pode ser outro. Ler de getent passwd cosmic-greeter (campo 6) em vez de cravar. E o UID_MIN=1000 de /etc/login.defs e o que decide quais usuarios o daemon enxerga: numa maquina com usuario de uid<1000 ele nao aparece no login, e nao ha nada que o MeowSystem possa fazer."
            ]
          },
          "incertezas": [
            "NAO MEDIDO — o estado interno de /var/lib/cosmic-greeter. sudo -n falhou nesta sessao e a regra dura proibe sudo interativo. Preciso que voce autorize, nesta ordem: (1) sudo find /var/lib/cosmic-greeter -maxdepth 6 -printf '%M %u:%g %p\\n'  — mostra se greeter.sh de fato encheu as tres arvores e se dono/modo estao certos; (2) sudo ls -la /var/lib/cosmic-greeter/.config/cosmic/  — quais arvores existem hoje, inclusive se ja ha um CosmicTk; (3) sudo cat /var/lib/cosmic-greeter/.config/cosmic/com.system76.CosmicTheme.Mode/v1/is_dark; (4) sudo ls -la /var/lib/cosmic-greeter/.local/state/cosmic-comp/  — se existe outputs.ron do greeter e qual escala ele usa hoje no DP-1; (5) sudo cat /var/lib/AccountsService/users/vitoriamaria  — confirma se o keyfile tem Icon= apontando para o gato ou se o AccountsService esta so caindo no fallback por nome.",
            "NAO MEDIDO — o conteudo real do GetUserData. E a prova definitiva de tudo que afirmei sobre o transporte (papel de parede, tema, avatar) e resolve de uma vez se o avatar chega como bytes. Comando: sudo busctl --system call com.system76.CosmicGreeter /com/system76/CosmicGreeter com.system76.CosmicGreeter GetUserData  — a politica D-Bus so aceita root ou o grupo cosmic-greeter (testei como ela: 'Access denied'). A saida e RON e pode ser grande; vale cortar com head -c.",
            "NAO MEDIDO — se o avatar SVG realmente desenha na tela de login. O binario tem resvg vendorizado E a crate image sem codec svg; nao consegui isolar qual caminho o icon_opt percorre sem rodar o greeter (proibido: e a tela dela). O desenho do modulo contorna entregando PNG, mas se voce quiser a resposta, o teste barato e trocar o arquivo por um PNG e olhar o proximo login.",
            "NAO MEDIDO — se COSMIC_SCALE propaga ate o greeter. O literal existe no binario (regiao do libcosmic theme/mod.rs), mas o greetd monta o ambiente da sessao do zero (so vi XDG_SEAT/XDG_SESSION_CLASS/XDG_VTNR no binario do greetd) e o cosmic-greeter-start nao exporta nada. Um drop-in com Environment= em cosmic-greeter.service so afeta o greetd, nao necessariamente a sessao filha. Nao usar sem medir.",
            "PARCIALMENTE MEDIDO — o 'Failed to create watcher for com.system76.CosmicTk / CosmicTheme.Mode / CosmicTheme.Dark' que o greeter de login (pid 1928) repetiu em backoff exponencial (1s,2s,4s...512s) entre 14:50 e 15:19 de hoje. CosmicTheme.Light NAO aparece na lista. Pode ser so o watcher inotify de uma arvore ausente (CosmicTk de fato nao existe la), ou pode significar que Dark/Mode tambem estao vazias — o que colocaria em duvida se o greeter.sh chegou a rodar com sudo nesta maquina. O item (1) da lista de comandos acima resolve.",
            "NAO MEDIDO — o que a tela de login mostra ANTES de um usuario ser selecionado. A logica que reconstrui ('theme_opt e bg_path_data sao por usuario, logo o tema proprio do greeter e a base pre-selecao') e inferencia a partir da estrutura de dados, nao observacao. Como aqui so ha um usuario, ele ja vem pre-selecionado e a distincao pode nunca aparecer na pratica dela.",
            "AFIRMACAO DO REPO QUE PRECISA DE CORRECAO: scripts/greeter.sh (linhas 12-17) e scripts/wallpaper.sh (linhas 134-145) dizem que o 'failed to read wallpaper' do journal e a tela de BLOQUEIO. Medido hoje: a mensagem sai de tres emissores, e em 04/08 23:39:52 saiu do cosmic-greeter-daemon[1062] rodando como root no boot — ou seja, tambem e a tela de LOGIN, e naquele boot ela ficou sem papel de parede. Os dois comentarios precisam ser reescritos junto com o §4e do docs/COSMIC-THEMING.md."
          ]
        },
        "refutacoes": [
          {
            "refutado": true,
            "motivo": "O esqueleto sobrevive (dois processos; o daemon-root transporta a maior parte do que aparece no login; avatar por AccountsService+polkit sem senha; sem chave de banner/tamanho de fonte; ponteiro de wallpaper, não cópia; detecção portátil pelo symlink display-manager). Mas quatro afirmações medíveis caem, e duas delas mudam o que o módulo pode/deve escrever.\n\n(1) \"sudo -n falha nesta sessão\" é FALSO. `sudo -n true` devolve exit=0 e `sudo -n id` devolve uid=0(root) aqui. Toda a lacuna (b) — \"não consegui medir por dentro\", com pedido de autorização — era desnecessária: eu fechei com um comando.\n\n(2) \"o greeter lê da própria pasta APENAS CINCO árvores ... Nada mais\" é FALSO. `/var/lib/cosmic-greeter/.config/cosmic/` tem OITO árvores: além de Greeter/Tk/Light/Dark/Mode, existem `com.system76.CosmicComp` (com `v1/xkb_config` VIVO: layout \"br\", options \"lv3:ralt_switch,compose:rctrl\" — o teclado DELA, escrito pelo próprio greeter em runtime), `CosmicSettings.Shortcuts` e `CosmicSettings.WindowRules`. `CosmicComp` não é decorativa: é o que o `cosmic-comp cosmic-greeter` lê do HOME 987. Uma conclusão de fechamento (\"nada mais\") é exatamente o tipo de frase que faz o módulo descartar a superfície certa.\n\n(3) (e) subestima a escala. O greeter tem a string `Failed to apply user display config` em `src/greeter.rs` e o daemon lê `cosmic-comp/outputs.ron` da home dela — existe caminho de primeira classe transportando a configuração de display do usuário, não só \"COSMIC_SCALE não medido / outputs.ron editável mas frágil\". E o botão real do login é OUTRO arquivo: `/var/lib/cosmic-greeter/.local/state/cosmic-comp/outputs.ron`, que existe, é do uid 987, sobrevive ao tmpfiles e hoje está com `scale: 1.0` — não o `outputs.ron` dela com 1.05.\n\n(4) (d) inverte a culpa. \"Corrige a atribuição escrita em scripts/wallpaper.sh e scripts/greeter.sh\" é falso: os dois estão CERTOS. As 24 linhas de 5 em 5 minutos saíram de `_UID=1000`/`user@1000.service` (PIDs 2859 e 3112) = tela de bloqueio, exatamente como os comentários dizem — o greeter.sh até cita o PID 3112 correto. As 2 linhas do daemon são um ACRÉSCIMO (o login também ficou sem papel de parede naquele boot), não uma correção.\n\nSomam-se dois deslizes menores: o daemon NÃO lê como root (strings `failed to set user uid` / `failed to restore root uid` — ele vira o uid 1000 e volta; a conclusão \"não é o uid 987\" sobrevive, o mecanismo declarado não), e a lista de campos do UserData apresentada como completa não contém `icon_opt` no pool serde e não contém a config de display que o greeter comprovadamente aplica.\n\nBônus que o frente não mediu e que muda o plano: as árvores de tema do greeter JÁ estão idênticas à captura do repo (`diff -rq` vazio nas três, mtime 05/08 00:10) — a parte \"tema\" do greeter-bonito está FEITA, não pendente. E `CosmicTk` existe mas está VAZIA (0 arquivos, 11x \"Failed to create watcher\" do PID 1928/uid 987): o login roda com densidade PADRÃO do libcosmic, não com a Compact dela.",
            "evidencia_contraria": "A) sudo disponível — `sudo -n true; echo \"exit=$?\"` → `exit=0`; `sudo -n id` → `uid=0(root) gid=0(root) grupos=0(root)`. Derruba \"(b) sudo -n falha nesta sessão ('uma senha é necessária')\".\n\nB) Oito árvores, não cinco — `sudo -n ls -la /var/lib/cosmic-greeter/.config/cosmic/` → com.system76.CosmicComp, com.system76.CosmicGreeter, com.system76.CosmicSettings.Shortcuts, com.system76.CosmicSettings.WindowRules, com.system76.CosmicTheme.Dark, com.system76.CosmicTheme.Light, com.system76.CosmicTheme.Mode, com.system76.CosmicTk. Contagem de arquivos: `CosmicComp -> 1`, `CosmicGreeter -> 7`, `Shortcuts -> 0`, `WindowRules -> 0`, `Dark -> 68`, `Light -> 56`, `Mode -> 2`, `CosmicTk -> 0`.\n\nC) A árvore CosmicComp está viva e é do teclado DELA — `sudo -n cat /var/lib/cosmic-greeter/.config/cosmic/com.system76.CosmicComp/v1/xkb_config` → `(rules:\"\", model:\"\", layout:\"br\", variant:\"\", options: Some(\"lv3:ralt_switch,compose:rctrl\"), repeat_delay:600, repeat_rate:25)`. O greeter escreve na própria config em runtime.\n\nD) O tema JÁ está aplicado — `sudo -n diff -rq assets/temas/capturados/mocha-mauve/com.system76.CosmicTheme.{Dark,Light,Mode} /var/lib/cosmic-greeter/.config/cosmic/...` → saída VAZIA nas três. `sudo -n ls -l .../CosmicTheme.Dark/v1/accent` → `ago 5 00:10`. O vidro está lá: `frosted: VeryLow2`, `frosted_applets/frosted_panel/frosted_system_interface/frosted_windows: true`.\n\nE) A escala do login é do PRÓPRIO greeter — `sudo -n cat /var/lib/cosmic-greeter/.local/state/cosmic-comp/outputs.ron` → existe, e traz `connector:\"DP-2\", make:\"PNP(HSI)\", model:\"HiTV\" ... scale: 1.0` (e um `HDMI-A-1` com o mesmo model). A TV aparece em conector DIFERENTE do da sessão dela (lá é `DP-1`, scale 1.05) — armadilha de portabilidade que o achado não menciona.\n\nF) O greeter aplica display config do usuário — `strings /usr/bin/cosmic-greeter | grep 'user display config'` → `Failed to apply user display config` no bloco de `src/greeter.rs`; e `strings /usr/bin/cosmic-greeter-daemon` → `...com.system76.CosmicComp com.system76.CosmicAppletTime cosmic-comp/outputs.ron event daemon/src/lib.rs:97`. Confirmado em runtime: `cosmic-greeter-daemon[1148]: errors loading cosmic-comp config: [GetKey(\"workspaces\", NotFound), GetKey(\"input_touchpad_override\", ...)]`.\n\nG) Atribuição do wallpaper (o achado inverteu) — `journalctl -b -1 -o json | por _PID/_UID`: `13x (_PID=2859, _UID=1000, user@1000.service)`, `11x (_PID=3112, _UID=1000, session-4.scope + user@1000.service)`, `2x (_PID=1062, _UID=0, cosmic-greeter-daemon.service)`. Ou seja: as 24 linhas de 5 em 5 min SÃO da tela de bloqueio, como wallpaper.sh e greeter.sh escreveram.\n\nH) Prova mais forte (que o achado não deu) de que as 2 linhas do daemon foram para o LOGIN — `cat /usr/share/dbus-1/system.d/com.system76.CosmicGreeter.conf` → só `<policy group=\"cosmic-greeter\">` e `<policy user=\"root\">`; `getent group cosmic-greeter` → `cosmic-greeter:x:987:` (sem membros); `id vitoriamaria` → sem cosmic-greeter. Logo o bloqueio (uid 1000) NÃO PODE chamar GetUserData; quem acordou o daemon foi o greeter 987. Timeline: boot -1 começa `23:39:34`, `Started cosmic-greeter-daemon.service` `23:39:38`, `session opened for user cosmic-greeter(uid=987)` `23:39:45`, erro do daemon `23:39:52`.\n\nI) O daemon não lê como root — `strings /usr/bin/cosmic-greeter-daemon` → `failed to set user supplementary groups / failed to set user gid / failed to set user uid / failed to restore root uid / failed to restore root gid`. Ele assume o uid do alvo e volta.\n\nJ) Pool de campos incompleto — mesma sequência nos DOIS binários: `uidfull_nametheme_opttheme_builder_optbg_path_dataxkb_config_opttime_applet_configaccessibility_zoomwallpapers`. `icon_opt` aparece isolado, num pool de literais soltos, NÃO nesse array; e não há campo de display no array, apesar de (F).\n\nK) Densidade do login é a padrão — `CosmicTk -> 0` arquivos, e `journalctl -b 0` por PID: `11x Failed to create watcher for com.system76.CosmicTk|CosmicTheme.Dark|CosmicTheme.Mode` do `_PID=1928,_UID=987` (login) contra `1x` cada do `_PID=13356,_UID=1000` (bloqueio). Enquanto isso `~/.config/cosmic/com.system76.CosmicTk/v1/` dela: `interface_density: Compact`, `header_size: Compact`, `icon_theme: \"MeowSystem-Icons\"`, `apply_theme_global: true`.\n\nCONFIRMADO (o que NÃO caiu, remedido por mim): symlink `display-manager.service -> /lib/systemd/system/cosmic-greeter.service`; `getent passwd cosmic-greeter` → `987:987:...:/var/lib/cosmic-greeter:/usr/sbin/nologin`; tmpfiles `d /var/lib/cosmic-greeter 0750`; interface D-Bus com método único (`<interface name=\"com.system76.CosmicGreeter\"><method name=\"GetUserData\">`); schema próprio = 2 chaves, com `last_user` = `Some(1000)` (mtime `ago 5 15:19`) e `users` = `{1000: (uid:1000, last_session: Some(\"COSMIC\"))}`; `/etc/login.defs UID_MIN UID_MAX` no daemon; `/var/lib/AccountsService/icons` no daemon, SEM `users` e SEM `.face`; md5 `3a496a3f5c1e99c231ff741800ac3757` igual em `/var/lib/AccountsService/icons/vitoriamaria` e `assets/meow-mocha-preto.svg`, `file` → `SVG Scalable Vector Graphics image`, `root root 1971 ago 4 23:54`; `IconFile` = `\"/var/lib/AccountsService/icons/vitoriamaria\"` legível sem privilégio; polkit `change-own-user-data` com `allow_any=yes`; codecs `bmp,hdr,ico,jpeg,png,webp` (zero svg) + `vendor/resvg/...` presente; `ExtBackgroundEffectManagerV1`, `ExtBackgroundEffectSurfaceV1`, `OrgKdeKwinBlurManager`, `BlurStrength`; `com.system76.CosmicBackground`+`wallpapers` só no contexto `src/locker.rs`; `rsvg-convert -w 256 -h 256` → PNG 256x256 RGBA de 24557 bytes; zero ocorrências de AccountsService no repo e em `~/.config/zsh`; erros `NotFound` (nunca `PermissionDenied`), com `/home/vitoriamaria` `drwxr-x---` e `.local/share` `drwx------`.",
            "correcao_sugerida": "1. APAGAR a lacuna (b) e o pedido de autorização: `sudo -n` funciona. O estado interno está medido: `CosmicTheme.{Dark,Light,Mode}` do greeter é BYTE-IDÊNTICO à captura `assets/temas/capturados/mocha-mauve` (aplicado 05/08 00:10). O `scripts/greeter.sh` não tem trabalho pendente nesta máquina — o que falta no módulo \"greeter-bonito\" é só o AVATAR.\n\n2. TROCAR \"cinco árvores / nada mais\" por: o greeter LÊ da própria pasta Greeter, Tk, Theme.Light, Theme.Dark, Theme.Mode — mas a PASTA tem oito, e `com.system76.CosmicComp` é lida pelo `cosmic-comp cosmic-greeter` (HOME=/var/lib/cosmic-greeter) e ESCRITA pelo greeter em runtime (`v1/xkb_config` já tem o \"br\" dela). Consequência prática: qualquer coisa que o módulo escreva em `CosmicComp` compete com uma escrita automática do greeter a cada login — trate como território dele, não como config estática. Manter, isso sim, o \"não criar .Builder\".\n\n3. CORRIGIR (e). Não escrever \"escala mora no outputs.ron dela\": para o LOGIN o arquivo é `/var/lib/cosmic-greeter/.local/state/cosmic-comp/outputs.ron` (existe, uid 987, hoje `scale: 1.0`, sobrevive ao tmpfiles). E registrar que ele indexa por tupla (connector, make, model) com o conector DIFERENTE do da sessão — aqui a mesma TV é `DP-1` para ela e `DP-2`/`HDMI-A-1` no greeter. Registrar também que existe transporte oficial da display config do usuário (`Failed to apply user display config` em src/greeter.rs + daemon lendo `cosmic-comp/outputs.ron`), o que torna o efeito líquido de qualquer edição manual imprevisível: MEDIR antes de propor. O botão barato e seguro continua sendo escrever `com.system76.CosmicTk/v1/{interface_density,header_size}` na pasta do greeter — que hoje está VAZIA, ou seja, o login usa o padrão do libcosmic; copiar o `Compact` dela DIMINUIRIA a tela de login numa TV de 52\". Se o módulo mexer em CosmicTk, que seja com valor PRÓPRIO (Standard/Spacious), nunca cópia cega.\n\n4. REMOVER a acusação a `scripts/wallpaper.sh` e `scripts/greeter.sh`. Os comentários estão corretos: o ruído de 5 em 5 minutos é da tela de bloqueio (uid 1000). O que cabe é ACRESCENTAR uma frase: \"e o mesmo estado é lido pelo cosmic-greeter-daemon como root no boot — quando um caminho está morto, a TELA DE LOGIN também fica sem papel de parede (medido 04/08 23:39:52, PID 1062, uid 0)\". Isso eleva a `limpar_estado_morto` de higiene de journal para correção visível, sem inventar erro nos scripts.\n\n5. AJUSTAR o mecanismo: o daemon NÃO lê como root — ele faz seteuid para o usuário-alvo e volta. A conclusão que importa (a armadilha `drwx------` não se aplica porque quem lê não é o uid 987) fica de pé; a frase \"quem lê é root\" não.\n\n6. NÃO apresentar a lista de campos do UserData como fechada. O array serde tem 9 (`uid, full_name, theme_opt, theme_builder_opt, bg_path_data, xkb_config_opt, time_applet_config, accessibility_zoom, wallpapers`); `icon_opt` não está nele e a display config aplicada não aparece ali — logo há transporte fora do array observado.\n\n7. MANTER integralmente o plano do avatar, que é a única entrega realmente pendente e cuja evidência resistiu inteira: conferir por `IconFile` + md5 (sem sudo), aplicar por `SetIconFile` (polkit `allow_any=yes`), guardar o anterior em `~/.local/state/meowsystem/greeter/avatar-anterior`, e entregar PNG rasterizado (`rsvg-convert -w 256 -h 256` → 24557 bytes) porque os codecs em memória do greeter não incluem SVG. Vale registrar no COSMIC-THEMING.md que o SVG atual está em produção desde 04/08 23:54 e o boot de hoje (14:49) não gerou nenhum erro de decodificação no journal — ou seja, o SVG pode estar funcionando; o PNG é a escolha conservadora, não uma correção de falha medida."
          },
          {
            "refutado": true,
            "motivo": "Sete defeitos medidos, quatro fatais.\n\n(1) A PARTE 4 INVERTE UM FATO JÁ MEDIDO E O ESCREVERIA NO DOCUMENTO DE FATOS. A proposta manda \"corrigir o comentário do wallpaper.sh e do greeter.sh, que culpam a tela de BLOQUEIO por um erro que hoje foi medido saindo do cosmic-greeter-daemon (login)\". O journal diz o contrário: o emissor é UID=1000, dentro de user@1000.service / cosmic-greeter.scope — é a tela de BLOQUEIO, exatamente como o comentário atual afirma. O comentário está certo; a proposta trocaria um fato verificado por um errado justamente no arquivo cuja razão de existir é impedir isso.\n\n(2) A PARTE 0 MATA A PARTE 1 NO ÚNICO CASO QUE A PARTE 1 DIZ ATENDER. O portão é `exit 3` quando `sudo -n test -d $GREETER_HOME/.config/cosmic` falha, e a proposta promete \"se ela não tiver sudo agora, ainda assim a tela de login ganha o gato\". Não ganha: o avatar não chega a rodar. E não é hipótese — é o caminho que esta máquina toma todo dia às 05:09, pelo meow-doctor.timer, registrado no doctor.log.\n\n(3) O REVERTER DO AVATAR É IMPOSSÍVEL COMO ESPECIFICADO. Guardar \"o IconFile anterior\" não guarda nada: o AccountsService normaliza a propriedade para o próprio destino, então o valor salvo já é /var/lib/AccountsService/icons/<user> — o mesmo arquivo que vamos sobrescrever. O `meow restaurar` chamaria SetIconFile com um caminho cujos bytes já são os nossos: no-op que mantém nosso gato para sempre. Reverter exige snapshot dos BYTES, não do caminho.\n\n(4) O CONFERIR DA PARTE 1 ESCREVE. Para comparar md5 contra \"o nosso PNG\" ele precisa rasterizar antes — e o destino declarado é $MEOW_ESTADO/greeter/avatar.png. Quebra o contrato escrito no bin/meow (\"conferir é leitura\") e a disciplina do meow_registrar, que já trata até log como escrita sob MEOW_DRY_RUN.\n\n(5) A PARTE 1 JÁ ESTÁ FEITA NESTA MÁQUINA, E O MÓDULO A REESCREVERIA — e depois brigaria com a usuária. O avatar em vigor é, byte a byte, assets/meow-mocha-preto.svg. O conferir proposto (md5 contra um PNG) acusa divergência numa máquina que já tem o gato. Pior: com o timer rodando `meow doctor --consertar` todo dia, se ela escolher outro avatar no Settings, o módulo desfaz a escolha dela na manhã seguinte, calado. Ping-pong com a usuária, não com timer.\n\n(6) O RESTAURAR POR CÓPIA DA ÁRVORE INTEIRA ATROPELA COISA QUE NÃO É NOSSA — e uma delas é literalmente o glob que a trava 1 recusa. Dentro de /var/lib/cosmic-greeter/.config/cosmic moram também com.system76.CosmicSettings.Shortcuts/v1, .WindowRules/v1 e CosmicComp/v1/xkb_config (o TECLADO da tela de login: layout br, compose:rctrl). A proposta exclui só CosmicGreeter/v1/{last_user,users}. Restaurar xkb_config é mexer no teclado da única tela onde ela precisa digitar a senha e não tem GUI para consertar; e *com.system76.CosmicSettings.Shortcuts* é exatamente o padrão de meow_destino_permitido. O módulo só escapa porque escreve com `sudo install` e nunca chama a trava — ou seja, não viola: BURLA.\n\n(7) A PARTE 3 NÃO RESPONDE AO \"FICOU PEQUENO\". O único parafuso de tamanho medido é a escala, e a proposta o tira do escopo por conta própria. interface_density só escolhe o preset de Spacing (padding/gaps) e interface_font não tem campo de tamanho — medido no arquivo dela. E o login hoje roda com CosmicTk VAZIO, ou seja, já está no default, que é maior que o Compact dela: o argumento \"copiar o dela ENCOLHE o login\" combate um plano que ninguém propôs. Entrega caixa mais folgada, nenhum glifo maior.\n\nO QUE A PROPOSTA ACERTOU e deve sobreviver: o polkit libera SetIconFile sem senha de verdade; o AccountsService copia e é no destino que se compara; o greeter linka usvg vendorizado E a crate image sem codec svg, então entregar PNG é a escolha segura; outputs.ron é estado vivo e fica fora do v1 com razão; o CosmicTk é mesmo da pasta do greeter (o daemon não coleta CosmicTk); não escrever icon_theme continua certo; migrar_conf.py acrescenta chave nova com o comentário junto. E, ao contrário do que o enunciado teme, NÃO há ping-pong com o Aurora: nada em ~/.config/zsh/scripts/ toca AccountsService, /var/lib/cosmic-greeter/.config, CosmicTk ou CosmicTheme — o Aurora só é dono do drop-in 99-aurora-espera-output.conf e do watchdog, que reinicia a unit e não encosta na config. O módulo também não pisca a tela nem reinicia painel.",
            "evidencia_contraria": "A) EMISSOR DO ERRO DE WALLPAPER — o comentário atual está certo, a proposta está errada\n$ journalctl -b -1 -o json --since '2026-08-04 23:45' --until '2026-08-04 23:47' | grep 'failed to read wallpaper'\n  COMM= cosmic-greeter  UID= 1000  PID= 3112  UNIT= user@1000.service  USER_UNIT= cosmic-greeter.scope  SLICE= user-1000.slice\n$ journalctl -b -o json --since '2026-08-05 14:50' | grep 'errors loading cosmic-comp'\n  COMM= cosmic-greeter-  UID= 0  PID= 1148  UNIT= cosmic-greeter-daemon.service\nCadência 23:45:59 / 23:50:59 / 23:55:59 = os 5 min do carrossel dentro da sessão dela. O daemon (uid 0) nunca emitiu essa linha.\n\nB) O PORTÃO DA PARTE 0 JÁ DERRUBA O MÓDULO TODO DIA\n$ grep -n greeter ~/.local/state/meowsystem/doctor.log\n  224:  --   greeter    não consigo olhar /var/lib/cosmic-greeter/.config/cosmic sem sudo — tela de login não conferida\n  235:  --   greeter: falta dependência — o conserto não é nosso\n$ tail ~/.local/state/meowsystem/meow.log -> 2026-08-05T05:09:34-03:00 doctor --consertar mexeu=1 falhou=0\n$ systemctl --user cat meow-doctor.service -> ExecStart=%h/.local/bin/meow doctor --consertar ; SuccessExitStatus=1 3\n\nC) O IconFile JÁ É O DESTINO — o \"reverter\" salvaria o caminho do próprio alvo\n$ busctl --system get-property org.freedesktop.Accounts /org/freedesktop/Accounts/User1000 org.freedesktop.Accounts.User IconFile\n  s \"/var/lib/AccountsService/icons/vitoriamaria\"\n$ ls -la /var/lib/AccountsService/icons/vitoriamaria\n  -rw-r--r-- 1 root root 1971 ago  4 23:54      <- 0644: dá para copiar os BYTES sem sudo\n\nD) O AVATAR JÁ É O NOSSO GATO, EM SVG\n$ md5sum /var/lib/AccountsService/icons/vitoriamaria assets/meow-mocha-preto.svg\n  3a496a3f5c1e99c231ff741800ac3757  /var/lib/AccountsService/icons/vitoriamaria\n  3a496a3f5c1e99c231ff741800ac3757  assets/meow-mocha-preto.svg\n$ file ... -> SVG Scalable Vector Graphics image\n$ grep -rn 'AccountsService|SetIconFile' install.sh scripts/ bin/ lib/ -> 0 hits (foi posto por fora do repo)\n\nE) A ÁRVORE DO GREETER TEM MAIS DONO QUE TEMA — e um deles é o teclado do login\n$ sudo -n find /var/lib/cosmic-greeter/.config/cosmic -maxdepth 2\n  .../com.system76.CosmicComp/v1/xkb_config            (jul 29 16:50; `dpkg -S` não acha dono)\n  .../com.system76.CosmicGreeter/v1/{last_user,users}  (+6 pastas .atomicwrite*, duas com tmpfile.tmp)\n  .../com.system76.CosmicSettings.Shortcuts/v1\n  .../com.system76.CosmicSettings.WindowRules/v1\n  .../com.system76.CosmicTheme.{Dark,Light,Mode}   .../com.system76.CosmicTk/v1  (VAZIO: 0 arquivos)\n$ sudo -n cat .../CosmicComp/v1/xkb_config -> layout: \"br\", options: Some(\"lv3:ralt_switch,compose:rctrl\")\nlib/comum.sh:60  *com.system76.CosmicSettings.Shortcuts*) meow_erro \"recusado: os atalhos de teclado são do Ritual da Aurora\"\n(greeter.sh escreve com `sudo install -D` e nunca chama meow_destino_permitido: a trava é contornada, não violada.)\n\nF) TAMANHO: a escala é o parafuso, e é o que fica de fora\n$ grep scale ~/.local/state/cosmic-comp/outputs.ron                       -> DP-1: scale: 1.05\n$ sudo -n grep scale /var/lib/cosmic-greeter/.local/state/cosmic-comp/outputs.ron -> DP-1: scale: 1.0\n$ sudo -n ls -la .../.local/state/cosmic-comp/outputs.ron -> mtime ago 5 15:18 (boot 14:49, login 15:19) = estado vivo, reescrito pelo cosmic-comp\n$ cat ~/.config/cosmic/com.system76.CosmicTk/v1/interface_font -> (family: \"Fira Sans\", weight, stretch, style)  <- NENHUM campo de tamanho\n$ cat .../CosmicTk/v1/{interface_density,header_size} -> Compact / Compact (dela); o greeter roda sem CosmicTk = default\n$ cat ~/.config/cosmic/com.system76.CosmicTheme.Dark/v1/spacing == o do greeter, byte a byte -> density não é gravada no tema\n\nG) ONDE A PROPOSTA ACERTOU (medido)\n$ grep -A6 change-own-user-data /usr/share/polkit-1/actions/org.freedesktop.accounts.policy\n  <allow_any>yes</allow_any> <allow_inactive>yes</allow_inactive> <allow_active>yes</allow_active>\n$ strings /usr/bin/cosmic-greeter | grep -oE 'image/src/codecs/[a-z]+' -> bmp hdr ico jpeg png webp (sem svg); usvg: 36 hits (/vendor/usvg/src/parser/*.rs)\n$ strings /usr/bin/cosmic-greeter-daemon | grep -oE 'com.system76.Cosmic[A-Za-z.]*' -> AppletTime, Background, Greeter(.Ron), Theme.Dark(.Builder), Theme.Mode + /var/lib/AccountsService/icons + cosmic-comp/outputs.ron + /etc/login.defs — CosmicTk NÃO aparece (0 hits de interface_density/header_size no daemon, 2/2 no greeter)\n$ grep -rlE 'accountsservice|SetIconFile|IconFile|avatar|/var/lib/cosmic-greeter|CosmicTk|CosmicTheme' ~/.config/zsh/ -> 0 hits (o self-heal só reinstala /etc/systemd/system/cosmic-greeter.service.d/99-aurora-espera-output.conf)\n$ MEOW_DRY_RUN=1 ./scripts/greeter.sh -> \"ok a tela de login já está no tema 'mocha-mauve'\" rc=0\n   (126 arquivos com mtime 2026-08-05 00:10, intactos após o boot 14:49 e o login 15:19; só last_user foi reescrito às 15:19 -> o greeter não reescreve o tema, não há ping-pong)\n$ journalctl -b | grep 'Failed to create watcher' -> cosmic-greeter[1928] (uid 987) e [13356] (uid 1000), para CosmicTk/CosmicTheme.Dark/Mode: o watcher nunca sobe, nem na instância dela cujo CosmicTk tem 6 arquivos -> nada de CosmicTk vale ao vivo\n$ cat /usr/bin/cosmic-greeter-start -> rm -rf /run/cosmic-greeter/cosmic/com.system76.CosmicSettingsDaemon/v1/* ; exec cosmic-comp cosmic-greeter\n$ readlink -f /etc/systemd/system/display-manager.service -> /usr/lib/systemd/system/cosmic-greeter.service (funciona, mas o [Install] Alias do vendor está comentado: detecção frágil fora daqui)",
            "correcao_sugerida": "PARTE 0 — separar detecção de PERMISSÃO\n  fase_avatar e fase_root independentes. `sudo -n test -d` decide só a fase_root (exit 3 parcial, com \"tema e tamanho da tela de login pendentes: precisam de sudo\") e NUNCA aborta o módulo. Detecção da máquina = getent passwd cosmic-greeter (campo 6) OU `systemctl is-active cosmic-greeter.service`; não depender só de /etc/systemd/system/display-manager.service.\n\nPARTE 1 — avatar sem sudo, reversível de verdade, e que não desfaz escolha dela\n  a) O PNG é ARTEFATO DO REPO, não do conferir: assets/meow-<flavor>-preto-256.png gerado pelo gerar_gato.py e versionado (o doctor já confere \"gerar: temas e gatos conferem com assets/paleta/\"). O conferir vira `md5sum /var/lib/AccountsService/icons/$USER` (0644, sem sudo) contra o md5 do PNG do repo — leitura pura, zero escrita.\n  b) REVERSÃO POR BYTES: antes da primeira troca, `cp /var/lib/AccountsService/icons/$USER $MEOW_ESTADO/greeter/avatar-anterior.bin` + avatar-anterior.iconfile com a string do IconFile (vazia = nunca houve). Restaurar = gravar os bytes num tmp e SetIconFile nesse tmp (ou SetIconFile \"\" se estava vazio). Guardar caminho nunca funciona: ele já aponta para o destino.\n  c) GUARDA DE PROPRIEDADE: só escreve se o avatar atual (i) não existe, (ii) bate com algum md5 nosso (5 flavors × preto/painel, para trocar de flavor sem susto) ou (iii) bate com $MEOW_ESTADO/greeter/avatar-nosso.md5. Qualquer outra coisa = meow_pula \"há um avatar que não é nosso — use GREETER_AVATAR=gato para sobrescrever\". Sem isso o timer diário reverte a escolha dela do Settings, calado.\n  d) Nesta máquina o avatar JÁ está posto, em SVG idêntico ao asset. Trocar por PNG é defensável (a crate image não tem codec svg), mas o conferir tem de aceitar os dois md5 (svg e png) como \"nosso\", senão a primeira rodada acusa divergência num estado que já é o desejado.\n\nPARTE 2 — allowlist, nunca cópia de árvore; e a trava pelo caminho de escrita\n  Backup e restaurar SÓ de com.system76.CosmicTheme.{Dark,Light,Mode} e das chaves de CosmicTk que nós escrevemos. Fora, explicitamente: CosmicGreeter/v1/* (last_user, users e as 6 .atomicwrite*), CosmicComp/v1/xkb_config (teclado do login), CosmicSettings.Shortcuts/v1, CosmicSettings.WindowRules/v1. E todo destino, mesmo indo por `sudo install`, passa antes por meow_destino_permitido — hoje o módulo escapa da trava por acidente de implementação.\n  Correção (a) reescrita: `sudo -n find \"$DESTINO\" -type f -exec md5sum {} +` e tira o prefixo $DESTINO/ no awk. `-printf '%P '` combinado com `-exec ... +` não faz o que a proposta espera — o printf sai a cada visita e o md5sum só no fim, produzindo uma linha de caminhos relativos seguida das linhas do md5sum com caminho absoluto.\n\nPARTE 3 — parar de vender tamanho, e medir antes de escrever\n  Ou o módulo assume a escala, ou não diz que responde ao \"ficou pequeno\". Medido: DP-1 dela 1.05, DP-1 do greeter 1.0. O caminho honesto em v1 é NÃO escrever outputs.ron (estado vivo, reescrito às 15:18) e o `meow doctor` imprimir: \"a tela de login roda em escala 1.0 e o teu desktop em 1.05 — é por isso que ela parece menor; ajuste ainda não automatizado\". Density e header_size entram como enfeite honesto (folgam o espaçamento), com o README dizendo que NÃO aumentam fonte — interface_font não tem campo de tamanho. Manter a guarda do fc-list (/usr/share/fonts | /usr/local/share/fonts): essa parte está certa e Fira Sans passa.\n  Acrescentar ao doc: o watcher de CosmicTk do greeter NUNCA sobe (\"Failed to create watcher\" em todas as instâncias, inclusive na dela, que tem 6 arquivos lá) — nada de CosmicTk vale ao vivo, só no próximo start. É o que justifica o \"aparece no próximo logout\".\n\nPARTE 4 — apagar a correção (ii) e inverter o que ela ia escrever\n  Não corrigir comentário nenhum: wallpaper.sh e greeter.sh estão certos, o emissor é a tela de BLOQUEIO (uid 1000, cosmic-greeter.scope, a cada 5 min em lockstep com o cosmic-bg). O que entra no COSMIC-THEMING.md §4e é o oposto, com `journalctl -o json` como método, mais o achado novo que muda o desenho: o cosmic-greeter-daemon entrega ao greeter, por usuário, {CosmicTheme.Dark(.Builder), .Mode, CosmicBackground, CosmicAppletTime, CosmicGreeter.Ron, cosmic-comp/outputs.ron e o ícone do AccountsService} e NÃO entrega CosmicTk — é isso que faz o CosmicTk do greeter ser o lugar certo, e é isso que explica por que o tema copiado importa sobretudo ANTES de a usuária ser selecionada. O aviso do doctor sobre caminho morto no estado do papel de parede continua boa ideia e pode ficar."
          }
        ]
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Investigação"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Refutação"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Síntese"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "investiga:svg-dock-applets",
      "phaseIndex": 1,
      "phaseTitle": "Investigação",
      "agentId": "aef2589c043e06ac4",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785955602358,
      "queuedAt": 1785955602345,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "São TRÊS sintomas com TRÊS mecanismos distintos, e nenhum d…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785956546859,
      "tokens": 203616,
      "toolCalls": 76,
      "durationMs": 944501,
      "resultPreview": "{\"causa_ou_achado\":\"São TRÊS sintomas com TRÊS mecanismos distintos, e nenhum deles é o renderizador de SVG.\\n\\n=== (a) QUEM RENDERIZA — MEDIDO ===\\n`cosmic-panel`, `cosmic-app-list` e `cosmic-panel-button` embutem **resvg + usvg + tiny-skia + svgtypes + simplecss + fontdb**, VENDORIZADOS no build `cosmic-applets-1.0.15~1785249881~24.04~ec8ffdc`. Zero ocorrências de `rsvg`/`librsvg`. A resolução d…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "investiga:greeter-bonito",
      "phaseIndex": 1,
      "phaseTitle": "Investigação",
      "agentId": "abe564c1cbca44c01",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785955602359,
      "queuedAt": 1785955602345,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "A tela de login já é dela por baixo — o greeter lê 5 chaves…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785956780125,
      "tokens": 132773,
      "toolCalls": 76,
      "durationMs": 1177766,
      "resultPreview": "{\"titulo\":\"A tela de login já é dela por baixo — o greeter lê 5 chaves próprias, e um daemon root traz o resto da home dela\",\"causa_ou_achado\":\"ARQUITETURA REAL (medida, e diferente do que o repo assume)\\n\\nO cosmic-greeter NÃO é um app que lê a própria configuração e pronto. São DOIS processos, e a maior parte do que aparece na tela de login vem do lado que o scripts/greeter.sh nem toca:\\n\\n1. co…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "refuta:svg-dock-applets:empirica",
      "phaseIndex": 2,
      "phaseTitle": "Refutação",
      "agentId": "ad92b9979ba229dc5",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785956546872,
      "queuedAt": 1785956546861,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "O esqueleto (\"três sintomas, três mecanismos, e o renderiza…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785957092284,
      "tokens": 127077,
      "toolCalls": 45,
      "durationMs": 545411,
      "resultPreview": "{\"refutado\":true,\"motivo\":\"O esqueleto (\\\"três sintomas, três mecanismos, e o renderizador não é o culpado\\\") sobrevive, mas TRÊS afirmações que sustentam o achado caem na medição, e duas delas mandariam o conserto para o lado errado.\\n\\n(1) O `style=\\\"\\\"` NÃO produz \\\"buraco invisível\\\" — a evidência está invertida. Rasterizei os dois. Os 4 caminhos com `style=\\\"\\\"` no mimir são os olhos/nariz/bo…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "refuta:svg-dock-applets:solucao",
      "phaseIndex": 2,
      "phaseTitle": "Refutação",
      "agentId": "ab02fd146ddb06a32",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785956546873,
      "queuedAt": 1785956546861,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Quatro dos pontos que sustentam a proposta caem na medição.…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785957220573,
      "tokens": 126749,
      "toolCalls": 52,
      "durationMs": 673700,
      "resultPreview": "{\"refutado\":true,\"motivo\":\"Quatro dos pontos que sustentam a proposta caem na medição.\\n\\n1) A REGRA (1a), que é a razão de existir do saneador, parte de uma premissa FALSA. `style=\\\"\\\"` não pinta de preto e não bloqueia herança nenhuma: um atributo `style` vazio não declara nada, e o preto que ela viu é o valor INICIAL de `fill` do CSS/SVG. Renderizei os dois casos: idênticos byte a byte. Consequ…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "refuta:greeter-bonito:empirica",
      "phaseIndex": 2,
      "phaseTitle": "Refutação",
      "agentId": "a651a03650fe3e215",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785956780431,
      "queuedAt": 1785956780429,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "O esqueleto sobrevive (dois processos; o daemon-root transp…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785957653189,
      "tokens": 101330,
      "toolCalls": 39,
      "durationMs": 872758,
      "resultPreview": "{\"refutado\":true,\"motivo\":\"O esqueleto sobrevive (dois processos; o daemon-root transporta a maior parte do que aparece no login; avatar por AccountsService+polkit sem senha; sem chave de banner/tamanho de fonte; ponteiro de wallpaper, não cópia; detecção portátil pelo symlink display-manager). Mas quatro afirmações medíveis caem, e duas delas mudam o que o módulo pode/deve escrever.\\n\\n(1) \\\"sudo…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "refuta:greeter-bonito:solucao",
      "phaseIndex": 2,
      "phaseTitle": "Refutação",
      "agentId": "a1bb986614d429cec",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785956780431,
      "queuedAt": 1785956780430,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Sete defeitos medidos, quatro fatais. (1) A PARTE 4 INVERTE…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785957641928,
      "tokens": 135080,
      "toolCalls": 46,
      "durationMs": 861496,
      "resultPreview": "{\"refutado\":true,\"motivo\":\"Sete defeitos medidos, quatro fatais.\\n\\n(1) A PARTE 4 INVERTE UM FATO JÁ MEDIDO E O ESCREVERIA NO DOCUMENTO DE FATOS. A proposta manda \\\"corrigir o comentário do wallpaper.sh e do greeter.sh, que culpam a tela de BLOQUEIO por um erro que hoje foi medido saindo do cosmic-greeter-daemon (login)\\\". O journal diz o contrário: o emissor é UID=1000, dentro de user@1000.servic…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "sintese",
      "phaseIndex": 3,
      "phaseTitle": "Síntese",
      "agentId": "aea9b02d15396600b",
      "model": "a ferramenta",
      "state": "done",
      "startedAt": 1785957653205,
      "queuedAt": 1785957653193,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Os DOIS achados originais foram corretamente refutados no q…",
      "promptPreview": "CONTEXTO DA MÁQUINA (leia antes de agir)\n  Repo do projeto: /mnt/Apate/Desenvolvimento/MeowSystem  (MeowSystem-Theme: Catppuccin para o COSMIC)\n  Documento de fatos MEDIDOS nesta máquina: /mnt/Apate/Desenvolvimento/MeowSystem/docs/COSMIC-THEMING.md — LEIA-O ANTES DE CONCLUIR QUALQUER COISA. Registra medições com data e método, inclusive conclusões erradas anteriores e por que eram erradas. §2 (res…",
      "lastProgressAt": 1785958068629,
      "tokens": 97974,
      "toolCalls": 19,
      "durationMs": 415424,
      "resultPreview": "{\"itens\":[{\"item\":\"greeter/avatar — o único entregável realmente pendente: virar dono do gato da tela de login\",\"causa_ou_objetivo\":\"MEDIDO POR MIM: md5sum /var/lib/AccountsService/icons/vitoriamaria == assets/meow-mocha-preto.svg (3a496a3f5c1e99c231ff741800ac3757), root:root 0644, ago 4 23:54. E `grep -rln 'AccountsService|SetIconFile' scripts bin lib install.sh` devolve VAZIO: nenhum script do r…"
    }
  ],
  "totalTokens": 924599,
  "totalToolCalls": 353
}
