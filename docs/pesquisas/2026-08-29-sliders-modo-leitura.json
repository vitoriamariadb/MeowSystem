{
 "plano": {
  "veredito": "Sim, dá — e o caminho é um só: recompilar o cosmic-comp com DOIS uniforms novos. Não existe atalho: medi no fonte da versão que está instalada (5c93094) que os únicos uniforms vivos do offscreen.frag são `invert` (0/1) e `color_mode` (1..4 vindo de `enum as u8 as f32`). Nenhum valor contínuo cabe ali sem recompilar, e o `a11y_screen_filter.ron` é lido UMA vez no boot — escrever nele não muda nada até o próximo login.\n\nO CUSTO, em três linhas:\n- Um patch de 5 arquivos + ~4 min de build (30 min se o vendor estiver frio) + UM logout. Depois disso os dois números valem A QUENTE: escrever `~/.config/cosmic/com.system76.CosmicComp/v1/leitura_temperatura` muda a tela no quadro seguinte, porque o cosmic-comp já tem um ConfigWatchSource nesse namespace e os uniforms são remontados a cada quadro.\n- O agendamento por horário é o mais barato dos três: um script e dois arquivos de systemd, sem cliente Wayland nenhum. Ele só escreve a mesma chave.\n- O slider de ARRASTAR (o do print do HyperOS) é a parte cara: um applet libcosmic novo na barra, mais uma linha à mão no `plugins_wings` (arquivo da Aurora) e um segundo logout. Até ele existir, os dois valores já são reais e mudam a quente por número.\n\nO RISCO É UM SÓ, E É GRANDE: `init_shaders` compila o offscreen.frag na subida do device, e `/usr/bin/cosmic-greeter-start` é literalmente `exec cosmic-comp cosmic-greeter`. Shader com erro de sintaxe = sem desktop E sem tela de login, com o greeter em laço (o drop-in da Aurora tirou o teto de tentativas). Por isso o validador EGL e o cartão de TTY vêm ANTES do primeiro build, e por isso o `.orig` (não o `.pkg-orig`) é a rede: conferi agora, o `.orig` bate byte a byte com o md5 que o dpkg registrou (468c46ba3bc3466933737b185624863b) e o `.pkg-orig` NÃO bate — ele já vem com o night light dentro.\n\nUma escolha de desenho que evita a armadilha mais cara: o bloco novo NÃO entra na região do `color_mode`. Aquelas três linhas são as âncoras do `aurora-night-light.py`, que o self-heal roda de hora em hora e que reescreve TUDO entre elas com padding de espaços — e o shader resultante ainda compilaria, então a falha seria muda. Deixando a região do color_mode byte a byte igual ao upstream, o patcher velho continua fazendo o que sempre fez (útil na janela entre um `apt upgrade` e o rebuild) e nunca encosta no modo de leitura.",
  "etapas": [
   {
    "ordem": 1,
    "nome": "A tela obedece a dois números",
    "entrega": "Depois de um logout, ela digita um número e a tela esquenta na hora; digita outro e a tela ganha grão de papel. `printf '3500' > ~/.config/cosmic/com.system76.CosmicComp/v1/leitura_temperatura` muda a cor no quadro seguinte, sem deslogar de novo. Desligar é escrever `0` nos dois.",
    "passos": [
     "Salvar o cartão de recuperação (campo `recuperacao`) num papel ou no celular — não no computador que pode não ligar.",
     "Conferir a rede: `md5sum /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.orig` tem de dar 468c46ba3bc3466933737b185624863b.",
     "Avisar a Vitória em voz alta: a partir daqui eu escrevo em ~/.config/zsh, que tem auto-commit a cada 10 min no repo privado dela.",
     "Criar ~/.config/zsh/patches/patches.d/ e mover o cosmic-comp-sem-workspace-vazio.patch para lá.",
     "Criar patches.d/series com a linha `req:cosmic-comp-sem-workspace-vazio.patch`.",
     "Trocar o compilar() do aurora-cosmic-comp-ws.sh (linhas 385-430) para ler a série: dry-run de TODOS os patches antes de aplicar qualquer um, e `rm -rf` da árvore de fonte em qualquer falha.",
     "Trocar a idempotência: testar por `grep -q <marcador> <arquivo-fonte>` declarado na série, NUNCA pelo código de saída do patch (medido: `patch --forward` devolve 1 para 'já aplicado').",
     "Trocar o marcador_de() (linha 230) para concatenar TODOS os marcadores AURORA-*-PATCH-* do binário, senão o --ensure não reinstala quando a série muda — foi esse o bug de 25/08.",
     "Escrever patches.d/cosmic-comp-modo-leitura.patch com o GLSL do campo `glsl_final` e as cinco edições em Rust listadas abaixo, com o marcador `#[used] static AURORA_READING_MODE: &str = \"AURORA-READING-MODE-1\";`.",
     "EDIÇÃO 1 — src/backend/render/mod.rs:422: acrescentar `UniformName::new(\"leitura_temp\", UniformType::_3f)` e `UniformName::new(\"leitura_textura\", UniformType::_1f)`.",
     "EDIÇÃO 2 — src/backend/kms/surface/mod.rs:1897 (cursor) E :1931 (tela): acrescentar os dois `Uniform::new` NOS DOIS vetores. É AQUI que a sessão dela desenha; o render/mod.rs:1365 é winit/x11 e não roda nesta máquina.",
     "EDIÇÃO 3 — src/config/mod.rs:151-161: dois campos novos em ScreenFilter (`leitura_temperatura: u32`, `leitura_textura: f32`) com `#[serde(skip)]`, e is_noop() passando a exigir `== 0` e `<= 0.0` — sem isso o passe inteiro é PULADO e nada aparece, sem erro nenhum.",
     "EDIÇÃO 4 — src/config/mod.rs:394: trocar o literal `ScreenFilter { inverted: false, color_filter: None }` por `ScreenFilter { inverted: false, ..Default::default() }`, senão o crate nem compila.",
     "EDIÇÃO 5 — cosmic-comp-config/src/lib.rs:106 e :145: as duas chaves novas com default 0 e 0.0; e src/config/mod.rs:790: um braço `\"leitura_temperatura\" | \"leitura_textura\"` no config_changed que chama `state.backend.update_screen_filter(&updated)` na mão (o valor NÃO chega ao renderer sozinho).",
     "EDIÇÃO 5b — src/backend/kms/surface/mod.rs:401: `set_screen_filter` passa a chamar `self.schedule_render()` (que respeita o dpms), senão o slider trava numa tela parada.",
     "EDIÇÃO 5c — no fim de Config::load, copiar `cosmic_comp_config.leitura_*` para o filtro dinâmico, senão um valor escrito com ela deslogada só vale no login DEPOIS do seguinte.",
     "Escrever ~/.config/zsh/scripts/aurora-glsl-gate.c: abre EGL surfaceless no device NVIDIA, COMPILA E LINKA o shader (linkar é obrigatório — um fragment com varyings demais compila e falha só no link).",
     "Rodar o gate sobre o offscreen.frag novo nas 6 variantes que o smithay liga: [], [NO_ALPHA], [EXTERNAL], cada uma com e sem DEBUG_FLAGS. Se qualquer uma falhar, PARAR aqui.",
     "Rodar `aurora-cosmic-comp-ws.sh --build` e conferir que o artefato em /var/lib/aurora tem AURORA-COSMIC-WS-PATCH e AURORA-READING-MODE-1 antes de instalar.",
     "Deslogar e logar.",
     "Testar: `printf '3500' > ~/.config/cosmic/com.system76.CosmicComp/v1/leitura_temperatura` — a tela tem de esquentar na hora.",
     "Testar: `printf '0.4' > ~/.config/cosmic/com.system76.CosmicComp/v1/leitura_textura`.",
     "Desligar os dois com `0` e confirmar que a tela volta ao normal."
    ],
    "arquivos": [
     "/home/vitoriamaria/.config/zsh/patches/patches.d/series",
     "/home/vitoriamaria/.config/zsh/patches/patches.d/cosmic-comp-modo-leitura.patch",
     "/home/vitoriamaria/.config/zsh/patches/patches.d/cosmic-comp-sem-workspace-vazio.patch",
     "/home/vitoriamaria/.config/zsh/scripts/aurora-cosmic-comp-ws.sh",
     "/home/vitoriamaria/.config/zsh/scripts/aurora-glsl-gate.c",
     "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/render/shaders/offscreen.frag",
     "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/render/mod.rs",
     "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/kms/surface/mod.rs",
     "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/config/mod.rs",
     "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/cosmic-comp-config/src/lib.rs"
    ],
    "tempo": "~2h de escrita, mais 4 min de build (30 min se o vendor estiver frio), mais UM logout.",
    "risco": "ALTO e único: shader que não compila = sem desktop E sem tela de login, porque o greeter roda o mesmo binário. O gate EGL do passo 18 é o que impede — nenhum binário vai para /usr/bin sem passar nele. Risco menor: esquecer o is_noop() faz o passe ser pulado e nada aparecer, sem mensagem de erro.",
    "volta": "O cartão de recuperação do campo `recuperacao`, por TTY (Ctrl+Alt+F2) ou ssh, restaurando o `.orig` — que conferi agora que bate com o md5 do dpkg. Antes disso: se o gate reprovar, nada foi instalado e não há o que desfazer.",
    "dono": "Aurora"
   },
   {
    "ordem": 2,
    "nome": "O horário liga sozinho",
    "entrega": "Às 18:00 a tela esquenta e ganha papel sem ela tocar em nada; às 07:00 volta ao normal. `meow leitura estado` diz em português qual degrau o relógio pede e o que a tela está mostrando agora.",
    "passos": [
     "Escrever /mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh com tique|aplicar|conferir|estado|remover.",
     "Copiar a regra de janela do e_noite() do wallpaper.sh:637: `início > fim` é 'depois do início OU antes do fim'; `início == fim` vale o dia inteiro. A máquina não pode ter duas definições de noite.",
     "Escrever a chave com redirecionamento `>`, NUNCA com `mv` — o watcher do cosmic-config descarta o evento de rename e a mudança não acordaria ninguém.",
     "Só escrever quando o valor MUDA, para não gerar uma repintura a cada tique.",
     "Criar systemd/meow-leitura.service com ConditionEnvironment=WAYLAND_DISPLAY, PartOf=cosmic-session.target, LogLevelMax=notice e SuccessExitStatus=0 1 3.",
     "Criar systemd/meow-leitura.timer com OnCalendar=*-*-* *:00/5, AccuracySec=10s, Persistent=false, WantedBy=cosmic-session.target. Sem Persistent: um 18:00 perdido não pode ser reproduzido às 3h.",
     "Pôr as chaves no meow.conf.exemplo, todas com VAZIO = NÃO TOCA: LEITURA_AGENDA, LEITURA_HORARIO_INICIO, LEITURA_HORARIO_FIM, LEITURA_TEMPERATURA, LEITURA_TEXTURA, LEITURA_RAMPA.",
     "Ligar as chaves no install.sh e no bin/meow (chk_leitura/fix_leitura) — chave que a CLI obedece e o timer ignora é chave que mente.",
     "Rodar `systemctl --user enable --now meow-leitura.timer`.",
     "Testar de verdade: pôr o início daqui a 2 minutos e esperar a tela virar sozinha."
    ],
    "arquivos": [
     "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh",
     "/mnt/Apate/Desenvolvimento/MeowSystem/systemd/meow-leitura.service",
     "/mnt/Apate/Desenvolvimento/MeowSystem/systemd/meow-leitura.timer",
     "/mnt/Apate/Desenvolvimento/MeowSystem/meow.conf.exemplo",
     "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow",
     "/mnt/Apate/Desenvolvimento/MeowSystem/install.sh"
    ],
    "tempo": "~1h. Nenhum build, nenhum logout.",
    "risco": "BAIXO. O pior caso é a tela mudar de cor na hora errada. Nada aqui toca em binário, em sudo ou em arranque de sessão.",
    "volta": "`systemctl --user disable --now meow-leitura.timer`, sem sudo e sem sair da sessão. Ou esvaziar LEITURA_AGENDA no meow.conf.",
    "dono": "Meow"
   },
   {
    "ordem": 3,
    "nome": "O slider na barra",
    "entrega": "Um ícone novo na topbar. Clicando, dois sliders — Temperatura e Textura — que ela ARRASTA e vê a tela mudando enquanto arrasta, mais um botão 'Restaurar padrões'. É o print do HyperOS.",
    "passos": [
     "Criar /mnt/Apate/Desenvolvimento/MeowSystem/src/applets/leitura/ com um crate libcosmic próprio.",
     "Pinar o rev do libcosmic no Cargo.toml E commitar o Cargo.lock do crate — sem upstream para herdar lock, o lock tem de ser nosso (já há 5 checkouts distintos de libcosmic em ~/.cargo).",
     "Copiar a estrutura do cosmic-applet-a11y (393 linhas): popup, padded_control, menu_button, divider.",
     "Os dois sliders gravam por `cosmic_config::Config::new(\"com.system76.CosmicComp\", 1).set::<u32>(\"leitura_temperatura\", v)` — o MESMO canal do agendador, sem protocolo Wayland nenhum.",
     "Pôr `step(100)` na temperatura e `step(5)` na textura, para o arrasto não virar uma escrita por pixel.",
     "Acrescentar uma `watch_config` para o slider SEGUIR o disco quando o agendador mexer às 18:00.",
     "Escrever scripts/leitura_build.sh clonado do midia_build.sh: carimbo de 4 campos, `--locked`, MEOW_COMPILAR obrigatório, binário em ~/.local/bin/meow-applet-leitura.",
     "Plantar a sombra ~/.local/share/applications/com.meowsystem.AppletLeitura.desktop com o fail-safe do midia.sh: binário ausente = sombra removida, nunca slot vazio na barra.",
     "Registrar `leiturabin` em SEM_CONSERTO no bin/meow — compilar não é trabalho do doctor.",
     "Acrescentar UMA linha ao plugins_wings da topbar, À MÃO, com backup em ~/.local/state/meowsystem/backups/ — é arquivo da Aurora, e nenhum script nosso escreve ali.",
     "Deslogar (o plugins_wings só vale no próximo início de sessão; nunca reiniciar o cosmic-panel, que derruba topbar e dock juntas)."
    ],
    "arquivos": [
     "/mnt/Apate/Desenvolvimento/MeowSystem/src/applets/leitura/Cargo.toml",
     "/mnt/Apate/Desenvolvimento/MeowSystem/src/applets/leitura/Cargo.lock",
     "/mnt/Apate/Desenvolvimento/MeowSystem/src/applets/leitura/src/main.rs",
     "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura_build.sh",
     "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh",
     "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow",
     "/home/vitoriamaria/.config/cosmic/com.system76.CosmicPanel.Panel/v1/plugins_wings"
    ],
    "tempo": "~4h de escrita, mais um build frio de libcosmic (dezenas de minutos e ~2 GB de target), mais UM logout.",
    "risco": "MÉDIO, mas contido: applets são processos separados: se o nosso travar ou sumir, a barra continua. O buraco conhecido é sombra sem binário deixando o slot vazio — o fail-safe do passo 8 cobre.",
    "volta": "`rm` da sombra .desktop e do binário, tirar a linha do plugins_wings (o backup do passo 10), próximo login. Sem sudo.",
    "dono": "Meow"
   },
   {
    "ordem": 4,
    "nome": "Aposentar o night light sem apagá-lo",
    "entrega": "'Acessibilidade > Filtros de cor > Escala de cinza' volta a deixar a tela CINZA de verdade — como sempre deveria ter feito. A luz quente passa a ser só o slider, com o valor que ela escolher, e `aurora-night-light.py --status` diz 'aposentado'.",
    "passos": [
     "Acrescentar `NEW_MARK = \"AURORA-READING-MODE\"` ao lado do MARK, na linha 69 do aurora-night-light.py.",
     "Pôr a trava IMEDIATAMENTE ANTES do `blk = find_block(data)`, por volta da linha 231 — nunca na linha 192, onde ela comeria o --status e o --restore, que são caminhos de recuperação.",
     "A mensagem TEM de conter as palavras 'já aplicado': é o que o `grep -qE \"j[aá] aplicado\"` do self-heal procura. Sem isso ele imprime 'REAPLICADO' falso de hora em hora, para sempre.",
     "Sair com 0, nunca com 3 — o 3 significa 'shader mudou upstream' e viraria um AVISO horário sobre algo que está certo.",
     "Acrescentar uma linha de diagnóstico no ramo --status dizendo que o binário traz o modo de leitura compilado.",
     "Rodar `sudo /usr/local/bin/aurora-night-light.py` e conferir que ele NÃO escreveu (o md5 do /usr/bin/cosmic-comp não muda).",
     "Deslogar e confirmar que 'Escala de cinza' agora deixa a tela cinza, e que o slider de temperatura continua funcionando."
    ],
    "arquivos": [
     "/home/vitoriamaria/.config/zsh/scripts/aurora-night-light.py",
     "/usr/local/bin/aurora-night-light.py"
    ],
    "tempo": "~20 min, mais um logout para ver o cinza voltar.",
    "risco": "BAIXO. A trava não escreve byte nenhum. Se ela for posta no lugar errado (linha 192), o `--restore` do patcher para de funcionar — e ele é um dos caminhos de volta. O passo 2 existe só por causa disso.",
    "volta": "Tirar as 6 linhas da trava. E, mesmo com a trava errada, a recuperação principal continua sendo o `.orig` por TTY, que não passa pelo patcher.",
    "dono": "Aurora"
   },
   {
    "ordem": 5,
    "nome": "O doctor conta a verdade sobre o compositor",
    "entrega": "`meow doctor` passa a dizer, em uma linha em português, se o modo de leitura está no binário do disco e se está na SESSÃO que ela está usando agora — e a diferença entre as duas coisas, que hoje ninguém vê.",
    "passos": [
     "No fim do cmd_build e do cmd_build_auto, gravar /var/lib/aurora/cosmic-comp-patches.estado com a lista de marcadores ESPERADOS, declarada na série (não derivada do que o build produziu).",
     "Escrever essa lista com escrita atômica e `|| true`, nunca dentro de um `&&` que decida instalação.",
     "No bin/meow, escrever chk_leitura_compositor() comparando o esperado contra `strings /usr/bin/cosmic-comp` E contra /proc/<pid do cosmic-comp>/exe.",
     "Três respostas, nunca duas: falta no disco = MEOW_DIVERGENTE (1, com o comando do conserto); no disco mas não na sessão = 4, com a frase 'vale no próximo login'; sem compositor visível = 3.",
     "Generalizar meow_painel_compositor_clampa() (lib/painel.sh:167) para receber o marcador como argumento, em vez de escrever uma segunda sonda de 'processo versus arquivo' para envelhecer em paralelo.",
     "Acrescentar a linha ao MOTIVO_SEM_FIX do bin/meow: 'o conserto compila, e o doctor não compila'."
    ],
    "arquivos": [
     "/mnt/Apate/Desenvolvimento/MeowSystem/bin/meow",
     "/mnt/Apate/Desenvolvimento/MeowSystem/lib/painel.sh",
     "/home/vitoriamaria/.config/zsh/scripts/aurora-cosmic-comp-ws.sh"
    ],
    "tempo": "~40 min. Sem build e sem logout.",
    "risco": "BAIXO. O doctor é read-only por contrato (nunca usa sudo, nunca baixa nada). O único risco é de SINAL: se a linha nova acender '~~' todo ciclo por algo que só o logout resolve, ela para de olhar — por isso o código 4 do passo 4.",
    "volta": "Tirar a função do bin/meow e apagar o arquivo de estado. Nada mais.",
    "dono": "ambos"
   },
   {
    "ordem": 6,
    "nome": "Fechar a dívida do raio de canto",
    "entrega": "O raio de canto que ela pedir passa a valer sem o teto de 8, e o `meow painel teto` para de dizer 'sem o patch'. Ela pede 20 e a barra não some.",
    "passos": [
     "Copiar /mnt/Apate/Desenvolvimento/MeowSystem/patches/cosmic-comp-raio-clampado.patch para ~/.config/zsh/patches/patches.d/ — o binário é da Aurora, e um patch do lado do Meow fica órfão (foi exatamente o que aconteceu com este).",
     "Acrescentar `opt:cosmic-comp-raio-clampado.patch` à série, depois do req do workspace.",
     "Rodar `aurora-cosmic-comp-ws.sh --build` e conferir que AURORA-COSMIC-RADIUS-PATCH-1 aparece no artefato — hoje ele tem ZERO ocorrências no binário instalado.",
     "Corrigir o README.md:195, que afirma no presente que este patch é aplicado pelo --build.",
     "Corrigir o scripts/forma.sh nas linhas 115-117, 194 e 497, que imprimem NA TELA DELA a mesma afirmação falsa, justamente quando o raio é cortado.",
     "Deslogar e pedir um raio de 20 para confirmar que a barra continua de pé."
    ],
    "arquivos": [
     "/home/vitoriamaria/.config/zsh/patches/patches.d/cosmic-comp-raio-clampado.patch",
     "/home/vitoriamaria/.config/zsh/patches/patches.d/series",
     "/mnt/Apate/Desenvolvimento/MeowSystem/README.md",
     "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/forma.sh"
    ],
    "tempo": "~15 min de escrita, mais um build e um logout (dá para juntar com o build de outra etapa).",
    "risco": "MÉDIO. É `opt`, então se não aplicar o build segue sem ele. Mas ele MEXE no protocolo de canto do painel — e é o painel que já sumiu por causa desse arquivo. Um build que sobe e desenha errado não dispara alarme nenhum; o sintoma é o painel fantasma.",
    "volta": "Tirar a linha `opt:` da série e rebuildar. O teto de 8 do lado do Meow continua valendo enquanto o marcador não estiver no binário, então a barra dela não fica desprotegida no meio do caminho.",
    "dono": "ambos"
   }
  ],
  "glsl_final": "// ===========================================================================\n// src/backend/render/shaders/offscreen.frag  -- ESTADO FINAL\n//\n// COMO LER ESTE ARQUIVO: o patch tem SÓ DOIS HUNKS.\n//   HUNK A -- as duas declaracoes de uniform + a funcao meow_grao(), inseridos\n//             logo depois de `uniform float color_mode;`.\n//   HUNK B -- o bloco MODO DE LEITURA, inserido entre o fim da cadeia do\n//             color_mode e o comentario `// re-multiply`.\n// Todo o resto (marcado abaixo com \"NAO TOCAR\") tem de ficar BYTE A BYTE igual\n// ao upstream. O motivo esta no comentario dentro do main(), e ele e a razao\n// pela qual este patch nao apaga a luz noturna dela nem e apagado por ela.\n// ===========================================================================\n\n#version 100\n\n//_DEFINES_\n\n#if defined(EXTERNAL)\n#extension GL_OES_EGL_image_external : require\n#endif\n\nprecision highp float;\n#if defined(EXTERNAL)\nuniform samplerExternalOES tex;\n#else\nuniform sampler2D tex;\n#endif\n\nuniform float alpha;\nvarying vec2 v_coords;\n\n#if defined(DEBUG_FLAGS)\nuniform float tint;\n#endif\n\nuniform float invert;\nuniform float color_mode;\n\n// ======================= HUNK A ==========================================\n// MODO DE LEITURA (Aurora/MeowSystem) -- os DOIS uniforms novos.\n//\n// POR QUE A TEMPERATURA CHEGA COMO vec3 E NAO COMO KELVIN:\n//   Converter Kelvin -> RGB dentro do shader custa log() e pow() POR PIXEL, em\n//   tela cheia, todo quadro. E a conta e sempre a mesma. O lado Rust ja faz a\n//   interpolacao UMA vez, com a tabela de whitepoints do redshift/colorramp.c\n//   que o aurora-night-light.py ja carrega (1000K..6500K), e manda o resultado\n//   pronto em ScreenFilter::leitura_tint().\n//   vec3(1.0, 1.0, 1.0) = 6500K = neutro = modo de leitura DESLIGADO.\nuniform vec3 leitura_temp;\n\n// 0.0 = sem papel .. 1.0 = papel cheio. E o \"Textura\" do print do HyperOS:\n// faixa dinamica comprimida + dessaturacao + grao.\nuniform float leitura_textura;\n\n// Grao pseudoaleatorio a partir da coordenada de PIXEL do framebuffer.\n//\n// SEM TEMPO E SEM UNIFORM: para uma dada coordenada o valor e SEMPRE o mesmo.\n// Tres coisas caem de graca disso:\n//   1) nao chuvisca entre quadros;\n//   2) nao anda quando a janela anda;\n//   3) sobrevive ao damage tracking, porque a regiao redesenhada recalcula\n//      exatamente os mesmos valores. Um grao com termo temporal deixaria uma\n//      costura visivel na borda da regiao suja, e forcaria dano de tela cheia\n//      todo quadro -- o mesmo erro que o applet conta-gotas ja cobra em CPU.\n//\n// POR QUE COM sin(), CONTRARIANDO O CONSELHO DE SHADERTOY: medido nesta\n// maquina (RTX 4060, driver 580.173.02), o sin() e 1,5x a 2x MAIS BARATO que o\n// hash \"sem seno\" do Hoskins, porque na NVIDIA o seno e uma instrucao da SFU e\n// nao disputa o caminho ALU do resto do shader. O conselho de nunca usar sin\n// em hash nasceu de GPU movel com mediump, e nao vale aqui.\n//\n// PRECISAO: a spec da GLSL ES 1.00 declara gl_FragCoord como mediump; na NVIDIA\n// de desktop mediump E fp32, e foi por isso que o teste passou -- renderizei\n// cinza chapado em 3840x2160 e li de volta os cantos (0,0) e (3400,1800): mesmo\n// desvio padrao, e autocorrelacao |r| <= 0,155 para deslocamentos de 8 a 512 px,\n// ou seja sem faixa e sem repeticao. Se um dia isto rodar em GPU movel, trocar\n// por: vec3 p3 = fract(vec3(p.xyx) * vec3(0.1031, 0.1030, 0.0973));\n//       p3 += dot(p3, p3.yzx + 33.33); return fract((p3.x + p3.y) * p3.z);\nfloat meow_grao(vec2 p) {\n    return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n}\n// ===================== FIM DO HUNK A ======================================\n\nvoid main() {\n    // ------------------- NAO TOCAR (upstream) ---------------------------\n    vec4 color = texture2D(tex, v_coords);\n\n#if defined(NO_ALPHA)\n    color = vec4(color.rgb, 1.0) * alpha;\n#else\n    color = color * alpha;\n#endif\n\n    // un-multiply\n    color.rgb /= color.a;\n\n    // First invert then filter\n\n    if (invert == 1.0) {\n        color.rgb = 1.0 - color.rgb;\n    }\n\n    // ATENCAO -- A REGIAO ABAIXO E INTOCAVEL, E NAO E PRECIOSISMO.\n    //\n    // As strings `    if (color_mode == 1.0) {` e\n    // `    } else if (color_mode >= 2.0) {` sao as ANCORAS do\n    // aurora-night-light.py (find_block, linha 105). Ele acha o bloco por elas\n    // e REESCREVE tudo que houver entre as duas, com padding de espacos, de\n    // hora em hora pelo self-heal e depois de todo apt.\n    //\n    // Se o modo de leitura morasse aqui dentro, seria apagado em silencio -- e\n    // o shader resultante AINDA COMPILARIA. Os sliders dela parariam de fazer\n    // efeito sem nenhum erro em lugar nenhum: a pior classe de falha.\n    //\n    // Deixando esta regiao byte a byte igual ao upstream, o patcher velho\n    // continua fazendo o que sempre fez (util na janela entre um apt upgrade e\n    // o rebuild, quando o binario e de fabrica) e NUNCA encosta no bloco novo.\n    if (color_mode == 1.0) {        // greyscale\n        float value = (color.r + color.g + color.b) / 3.0;\n        color = vec4(value, value, value, color.a);\n    } else if (color_mode >= 2.0) {\n        float L = (17.8824 * color.r) + (43.5161 * color.g) + (4.11935 * color.b);\n        float M = (3.45565 * color.r) + (27.1554 * color.g) + (3.86714 * color.b);\n        float S = (0.0299566 * color.r) + (0.184309 * color.g) + (1.46709 * color.b);\n\n        float l, m, s;\n        if (color_mode == 2.0) { // Protanopia\n            l = 0.0 * L + 2.02344 * M + -2.52581 * S;\n            m = 0.0 * L + 1.0 * M + 0.0 * S;\n            s = 0.0 * L + 0.0 * M + 1.0 * S;\n        } else if (color_mode == 3.0) { // Deuteranopia\n            l = 1.0 * L + 0.0 * M + 0.0 * S;\n            m = 0.494207 * L + 0.0 * M + 1.24827 * S;\n            s = 0.0 * L + 0.0 * M + 1.0 * S;\n        } else if (color_mode == 4.0) { // Tritanopia\n            l = 1.0 * L + 0.0 * M + 0.0 * S;\n            m = 0.0 * L + 1.0 * M + 0.0 * S;\n            s = -0.395913 * L + 0.801109 * M + 0.0 * S;\n        } else {\n            // unknown\n            l = L;\n            m = M;\n            s = S;\n        }\n\n        vec3 error;\n        error.r = (0.0809444479 * l) + (-0.130504409 * m) + (0.116721066 * s);\n        error.g = (-0.0102485335 * l) + (0.0540193266 * m) + (-0.113614708 * s);\n        error.b = (-0.000365296938 * l) + (-0.00412161469 * m) + (0.693511405 * s);\n\n        vec3 diff = color.rgb - error;\n        vec3 correction;\n        correction.r = 0.0;\n        correction.g = (diff.r * 0.7) + (diff.g * 1.0);\n        correction.b =  (diff.r * 0.7) + (diff.b * 1.0);\n\n        color.rgb += correction;\n    }\n    // ----------------- FIM DO NAO TOCAR (upstream) ----------------------\n\n    // ======================= HUNK B ======================================\n    // MODO DE LEITURA. Mora AQUI por dois motivos, os dois deliberados:\n    //   - DEPOIS da cadeia do color_mode, para ficar fora do bloco que o\n    //     night light reescreve (ver o aviso acima);\n    //   - ANTES do `color.rgb *= color.a`, porque toda a conta abaixo pressupoe\n    //     cor NAO pre-multiplicada -- que e o estado em que o\n    //     `color.rgb /= color.a` la em cima deixou tudo.\n    //\n    // GLSL ES 1.00, conferido: so fract, sin, dot, floor, mix, clamp e\n    // gl_FragCoord. Nenhum texelFetch, nenhum inverse(), nenhum operador de\n    // bit, nenhum laco de contagem variavel, nenhum switch -- tudo isso e\n    // proibido ou inexistente em #version 100.\n\n    // (1) TEXTURA / PAPEL\n    if (leitura_textura > 0.0) {\n        float t = clamp(leitura_textura, 0.0, 1.0);\n\n        // Faixa dinamica de PAPEL, nao de tela: o preto sobe e vira TINTA\n        // (~0.12, nunca 0) e o branco desce e vira PAPEL levemente amarelado\n        // (~0.93, nunca 1). E essa compressao que tira o brilho de painel e faz\n        // a tela parecer uma pagina -- e ela vem ANTES do grao de proposito,\n        // porque o grao tem de modular o papel, nao a imagem crua.\n        vec3 papel = vec3(0.118, 0.113, 0.104)\n                   + vec3(0.815, 0.800, 0.760) * color.rgb;\n\n        // Dessatura 30%: papel colorido nunca satura como um painel. Mantem a\n        // cor -- o \"Cores: Cores totais\" do print dela -- e so tira o exagero.\n        float tinta = dot(papel, vec3(0.2126, 0.7152, 0.0722));\n        papel = mix(papel, vec3(tinta), 0.30);\n\n        color.rgb = mix(color.rgb, papel, t);\n\n        // Grao em duas oitavas: uma fina (o poro) e uma a ~1/3 de escala (a\n        // fibra). Uma oitava so ja funciona e custa metade; tres dao fibra mais\n        // rica e custam mais -- e escolha de gosto, nao de correcao.\n        //\n        // TEM de ser gl_FragCoord.xy, e nao v_coords: o MESMO programa, com os\n        // MESMOS uniforms, e aplicado a DOIS elementos -- a textura de tela\n        // cheia E a textura do CURSOR. v_coords vai de 0..1 DENTRO de cada\n        // elemento, entao o grao inteiro seria desenhado dentro de um cursor de\n        // 24 px. gl_FragCoord e espaco de framebuffer e casa nos dois.\n        vec2 pix = floor(gl_FragCoord.xy);\n        float g = (meow_grao(pix) + meow_grao(floor(pix * 0.34) + 11.0)) * 0.5 - 0.5;\n\n        // MULTIPLICATIVO, nao aditivo: papel modula a REFLETANCIA, entao o grao\n        // aparece no branco e some na tinta -- que e o comportamento real de uma\n        // pagina, e de quebra nao serrilha o texto.\n        //\n        // Amplitude ~4 niveis de 255 no papel: da MESMA ordem do passo de\n        // quantizacao de 8 bits, entao ele funciona tambem como dither e MATA\n        // banding em gradientes, em vez de criar. A contrapartida honesta:\n        // ruido estatico por pixel e o pior caso para codificador de video --\n        // gravacao de tela e OBS gastariam mais bitrate se o vissem. Nao veem:\n        // com filtro ligado, o blit da captura sai do buffer PRE-shader.\n        color.rgb *= 1.0 + g * (0.085 * t);\n        color.rgb = clamp(color.rgb, 0.0, 1.0);\n    }\n\n    // (2) TEMPERATURA\n    // SEM `if`, de proposito. Com o modo desligado o lado Rust manda\n    // vec3(1.0, 1.0, 1.0) e isto vira uma multiplicacao neutra: tres ALU, mais\n    // barato que um desvio e sem divergencia entre fragmentos.\n    //\n    // Vem DEPOIS do papel, tambem de proposito: assim a luz quente amornece\n    // tambem o branco do papel. Na ordem inversa o papel sairia frio com o\n    // resto quente, e a tela ficaria com duas temperaturas brigando.\n    color.rgb *= leitura_temp;\n    // ===================== FIM DO HUNK B =================================\n\n    // ------------------- NAO TOCAR (upstream) ---------------------------\n    // re-multiply\n    color.rgb *= color.a;\n\n    gl_FragColor = color;\n}",
  "perguntas_para_ela": [
   "Faixa do slider de temperatura: 1700K a 6500K (1700 é o piso real do GNOME, bem mais quente que os 3500K que você usa hoje) ou 2500K a 6500K, que é menos extremo? O passo é de 100K nos dois casos.",
   "Intensidade máxima da textura: no 100% o papel sobe o preto para ~0.12, desce o branco para ~0.93 e dessatura 30%. Isso é o quanto você quer no máximo do slider, ou você prefere que eu ligue e desligue na sua frente umas três vezes antes de fixar o número?",
   "Horário: 18:00 até 07:00 (a mesma janela do seu carrossel de papel de parede) serve, ou é outro? E a virada é seca, ou você quer uma rampa — e de quantos minutos?"
  ],
  "descartados": [
   "Escrever ~/.local/state/cosmic-comp/a11y_screen_filter.ron — REFUTADO: é lido UMA vez no boot, não tem watcher, e o compositor sobrescreve o arquivo no próximo toggle.",
   "Escada de 4 degraus pelo patch binário (sem recompilar) — PLAUSÍVEL, mas perde: exige o MESMO logout, não é slider, e os degraus apareceriam para ela com os nomes 'Protanopia/Deuteranopia/Tritanopia' no menu.",
   "Pendurar os sliders na página Acessibilidade do cosmic-settings — REFUTADO: o build nem começa nesta máquina (falta libdav1d-dev) e o apt troca esse binário a cada ~6 dias.",
   "Atalho de teclado + OSD para subir/descer a temperatura — REFUTADO: o cosmic-osd não tem método D-Bus para mostrar um OSD arbitrário, e o Não Perturbe dela engole notify-send (22 avisos comidos em 27/08).",
   "App GTK/Python próprio com dois Gtk.Scale — REFUTADO: mora fora de tudo que ela usa, e o repositório não tem uma única janela GTK para reaproveitar.",
   "Carona nos uniforms `alpha`/`tint`, ou alargar o enum do protocolo a11y para carregar um número — REFUTADO: alpha é hardcoded Some(1.0), tint só existe sob DEBUG_FLAGS que o compositor nunca liga, e o enum do protocolo colide com valores já definidos."
  ],
  "recuperacao": "O cosmic-comp novo não sobe: tela preta, ou o greeter piscando sem parar.\n\nPOR QUE ISTO É PIOR DO QUE PARECE. /usr/bin/cosmic-greeter-start é literalmente `exec cosmic-comp cosmic-greeter` — o greeter É o mesmo binário. Só existe uma sessão wayland instalada e não há X de reserva. E o drop-in 99-aurora-espera-output.conf zerou o teto de tentativas, então o laço é infinito e silencioso: não há login gráfico nenhum.\n\nAS DUAS PORTAS: Ctrl+Alt+F2 (até F6; o greetd ocupa a vt1) ou ssh de outra máquina (o sshd está ativo).\n\nPASSO 1 — CALAR O VIGIA DO GREETER. Sem isto ele ressuscita o greeter em 10 segundos e você briga com a tela enquanto digita.\n    sudo systemctl stop aurora-greeter-watchdog.service\n\nPASSO 2 — SAIR DO GRÁFICO. O vigia também se cala quando o alvo padrão não é gráfico, então mude os dois.\n    sudo systemctl set-default multi-user.target\n    sudo systemctl isolate multi-user.target\n\nPASSO 3 — PARAR O SELF-HEAL. Ele roda de hora em hora e depois de TODO apt, e reinstalaria o binário ruim.\n    sudo systemctl stop ritual-aurora-self-heal.timer\n\nPASSO 4 — FREIO E QUARENTENA. As DUAS coisas, não uma: o freio impede o auto-build de COMPILAR, mas NÃO impede o --ensure de reinstalar um artefato que já está no disco.\n    sudo touch /var/lib/aurora/cosmic-comp-ws-autobuild-off\n    sudo mv /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws \\\n            /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.QUARENTENA\n\nPASSO 5 — DEVOLVER O BINÁRIO DE FÁBRICA. Use o `.orig`. NÃO use o `.pkg-orig` e NÃO use `aurora-cosmic-comp-ws.sh --restore` — conferi hoje: o `--restore` copia o `.pkg-orig`, cujo md5 é 7a39a055dc1e5e61153219eafedbd49b e NÃO bate com o do dpkg, porque ele já vem com o shader do night light dentro.\n    sudo cp -a /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.orig /usr/bin/cosmic-comp.rec\n    sudo chmod 755 /usr/bin/cosmic-comp.rec\n    sudo mv -f /usr/bin/cosmic-comp.rec /usr/bin/cosmic-comp\n\nPASSO 6 — CONFERIR, a partir da raiz (os caminhos no md5sums são relativos).\n    cd / && md5sum -c /var/lib/dpkg/info/cosmic-comp.md5sums\nO esperado é usr/bin/cosmic-comp: ÊXITO. O md5 correto, hoje, é 468c46ba3bc3466933737b185624863b.\n\nPASSO 7 — VOLTAR AO GRÁFICO.\n    sudo systemctl set-default graphical.target\n    sudo systemctl start aurora-greeter-watchdog.service\n    sudo systemctl isolate graphical.target\n\nSE O .orig TIVER SUMIDO: só resta a rede — `sudo apt-get install --reinstall cosmic-comp` (não há .deb em /var/cache/apt/archives). O PASSO 4 tem de ter sido feito antes, ou o hook de apt roda o self-heal no fim do próprio comando e reinstala o binário ruim em segundos.\n\nO QUE VOCÊ PERDE ENQUANTO ESTÁ ASSIM: o patch de workspace sai junto, então o workspace vazio a mais volta e o número seco reaparece no painel. É o sintoma que você já conhece, e ele volta ao normal no rebuild.\n\nDEPOIS, com a tela de volta e a causa entendida:\n    sudo rm /var/lib/aurora/cosmic-comp-ws-autobuild-off\n    sudo rm /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.QUARENTENA\n    sudo systemctl start ritual-aurora-self-heal.timer\n    aurora-cosmic-comp-ws.sh --build"
 },
 "dossie_bruto": [
  {
   "trilha": "render-uniforms",
   "achados": {
    "achados": [
     {
      "titulo": "O shader offscreen.frag é carregado por include_str! em uma única linha",
      "detalhe": "POSTPROCESS_SHADER é uma &'static str embutida em tempo de compilação. É por isso que o patch binário da Aurora funciona: o texto GLSL vive em .rodata do ELF e é compilado pelo driver em runtime, não em tempo de build. Os outros shaders (outline, rectangle, clipping, shadow, blur) usam o mesmo mecanismo.",
      "evidencia": "src/backend/render/mod.rs:131 — `pub static POSTPROCESS_SHADER: &str = include_str!(\"./shaders/offscreen.frag\");` (commit 5c93094574b31800caaa27255caac4a7253f24ad, \"chore: update smithay\", 26/08/2026)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "A lista de uniforms do shader de pós-processamento tem EXATAMENTE dois nomes",
      "detalhe": "init_shaders() compila o POSTPROCESS_SHADER declarando só `invert` e `color_mode`, ambos UniformType::_1f. `alpha`, `tex` e `v_coords` são fornecidos pelo próprio smithay (TextureShaderElement), não por cosmic-comp. `tint` só existe sob o define DEBUG_FLAGS. Acrescentar qualquer uniform novo obriga a mexer nesta lista — não há caminho por config.",
      "evidencia": "src/backend/render/mod.rs:419-425 (compile_custom_texture_shader) e mod.rs:422-423 (UniformName::new(\"invert\"/\"color_mode\", UniformType::_1f))",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O color_mode é calculado por frame como `enum as u8 as f32` — é discreto por construção",
      "detalhe": "Uniform::new(\"color_mode\", ...) converte o enum ColorFilter para f32 via `as u8`. O enum é #[repr(u8)] com quatro variantes (Greyscale=1, Protanopia=2, Deuteranopia=3, Tritanopia=4) e um comentário no fonte dizendo que os valores TÊM de casar com o offscreen.frag. Nenhum ponto do caminho aceita um float arbitrário. Confirma a conclusão prévia: slider contínuo é impossível sem recompilar.",
      "evidencia": "src/backend/render/mod.rs:1364-1381 (bloco vec![Uniform::new]) + src/config/mod.rs:164-172 (`// these values need to match with offscreen.frag`, enum ColorFilter)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "DERRUBADO: o filtro de cor NÃO está em cosmic-config. É protocolo Wayland + arquivo de estado",
      "detalhe": "A configuração é um par (PathBuf, ScreenFilter) dentro de DynamicConfig, persistido em ~/.local/state/cosmic-comp/a11y_screen_filter.ron. Esse arquivo é lido UMA vez, no load_dynamic() do boot — não há watcher/inotify sobre ele. O caminho quente é outro: o protocolo Wayland cosmic_a11y_manager_v1, request SetScreenFilter{inverted, filter}. Escrever o .ron à mão NÃO muda nada a quente; só vale no próximo login. Verifiquei no disco dela: o arquivo existe, tem `(inverted: false,)` e foi escrito hoje às 17:46 — ou seja, é gravado pelo compositor a cada toggle, não lido.",
      "evidencia": "src/config/mod.rs:83 (accessibility_filter), :347-350 (place_state_file), :377-398 (load_filter_state), :727-736 (screen_filter/screen_filter_mut); src/wayland/protocols/a11y.rs:196-212 (Request::SetScreenFilter); `ls -la ~/.local/state/cosmic-comp/` + `cat a11y_screen_filter.ron`",
      "confianca": "medido"
     },
     {
      "titulo": "O global cosmic_a11y_manager_v1 está VIVO na máquina dela, versão 2",
      "detalhe": "`wayland-info` no WAYLAND_DISPLAY=wayland-1 lista `interface: 'cosmic_a11y_manager_v1', version: 2, name: 53`. Versão 2 é a que tem SetScreenFilter (EVT_SCREEN_FILTER_SINCE). Isso significa que QUALQUER cliente Wayland nativo (não-sandbox) pode ligar/desligar o filtro e a inversão sem UI — é o gancho para o agendamento por horário, sem recompilar nada. O bind exige client_not_sandboxed, então flatpak não serve; binário nativo serve.",
      "evidencia": "`WAYLAND_DISPLAY=wayland-1 wayland-info | grep a11y` → interface: 'cosmic_a11y_manager_v1', version: 2, name: 53; src/state.rs:762 (A11yState::new::<State,_>(dh, client_not_sandboxed))",
      "confianca": "medido"
     },
     {
      "titulo": "Não existe zwlr_gamma_control nem wp_color_manager na sessão dela — confirmado por medição",
      "detalhe": "Filtrando os globals vivos por 'color|gamma|a11y|brightness', só aparecem cosmic_a11y_manager_v1, zcosmic_output_manager_v1, zwlr_output_manager_v1 e zxdg_output_manager_v1. Nenhum controle de gamma nem de color management. Confirma o comentário do aurora-night-light.py: wlsunset/gammastep são inúteis aqui.",
      "evidencia": "`WAYLAND_DISPLAY=wayland-1 wayland-info | grep -oE \"interface: '[a-z_0-9]+'\" | sort -u | grep -iE 'color|gamma|a11y|brightness|output_manage'`",
      "confianca": "medido"
     },
     {
      "titulo": "O caminho quente completo tem 6 saltos, e o último é uma thread por surface",
      "detalhe": "cosmic-settings → cosmic_a11y_manager_v1::SetScreenFilter → A11yHandler::request_screen_filter (grava no DynamicConfig e chama o backend) → BackendData::update_screen_filter → KmsState::update_screen_filter (itera drm_devices/surfaces) → Surface::set_screen_filter (manda ThreadCommand::UpdateScreenFilter pelo canal) → na thread da surface, self.screen_filter = config e postprocess_textures.clear(). Nada nesse caminho recompila shader: só troca o valor do uniform no próximo frame.",
      "evidencia": "src/wayland/handlers/a11y.rs:53-63; src/state.rs:452-459; src/backend/kms/mod.rs:722-735; src/backend/kms/surface/mod.rs:401-405 e :600-602 e :1460-1463",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "ARMADILHA: set_screen_filter não agenda redraw — a mudança só aparece no próximo frame natural",
      "detalhe": "Surface::set_screen_filter (linha 401) só envia UpdateScreenFilter. Outros setters do mesmo arquivo enviam ThreadCommand::ScheduleRender explicitamente (linha 391). E update_screen_filter na thread (linha 1460) só faz atribuição + clear das texturas, sem queue_redraw(). Na prática o toggle parece instantâneo porque o relógio do painel e o cursor repintam a tela o tempo todo — mas um SLIDER arrastado numa tela parada ficaria travado. Qualquer implementação de slider PRECISA acrescentar o ScheduleRender.",
      "evidencia": "src/backend/kms/surface/mod.rs:391 (ThreadCommand::ScheduleRender), :401-405 (set_screen_filter sem ele), :916 (fn queue_redraw), :1460-1463 (update_screen_filter)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "is_noop() é o interruptor mestre: se ele der true, o shader NÃO RODA",
      "detalhe": "Em render_output, todo o passe de pós-processamento é pulado quando screen_filter.filter.is_noop(). Hoje is_noop = !inverted && color_filter.is_none(). Consequência crítica para qualquer diff: se você acrescentar night_temp/paper_texture ao ScreenFilter e ESQUECER de incluí-los no is_noop(), o shader nunca é invocado e os uniforms novos não fazem absolutamente nada — a tela fica igual e o bug é invisível. Esta é a linha mais fácil de errar do patch inteiro.",
      "evidencia": "src/backend/render/mod.rs:1284 (`let result = if !screen_filter.filter.is_noop() {`) + src/config/mod.rs:158-162 (impl ScreenFilter::is_noop)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "MEDIDO no binário vivo: o bloco que a Aurora reescreve tem 195 bytes, mas há 1666 disponíveis",
      "detalhe": "No /usr/bin/cosmic-comp instalado (33.571.456 bytes), a âncora `uniform float color_mode;` está em 0x2dc018. O bloco que o aurora-night-light.py reescreve (do `if (color_mode == 1.0) {` até o `} else if (color_mode >= 2.0) {`) vai de 0x2dc16a a 0x2dc22d = 195 bytes, dos quais 46 são padding de espaços hoje. MAS a região inteira do color_mode — greyscale MAIS as três matrizes de daltonismo, até o comentário `// re-multiply` — tem 1666 bytes. Mais o bloco do invert, 69 bytes. Total reescrevível byte-a-byte sem mexer no tamanho do ELF: 1735 bytes. O patcher atual usa 11% do orçamento que existe.",
      "evidencia": "python3 sobre /usr/bin/cosmic-comp: bloco greyscale 195 B (0x2dc16a..0x2dc22d), REGIAO TOTAL color_mode 1666 B, bloco invert 69 B, shader offscreen inteiro 2392 B (0x2dbec6..0x2dc81e)",
      "confianca": "medido"
     },
     {
      "titulo": "O espaço de estados quente sem recompilar é de 9 combinações, não 2",
      "detalhe": "O uniform invert (0/1) e o color_mode (0..4) são INDEPENDENTES no caminho quente: são dois campos separados do ScreenFilter, com dois setters separados (request_screen_invert e request_screen_filter), e a UI do COSMIC expõe o 'Inverter cores' como switch próprio, ao lado do dropdown de filtros. is_noop() só é true quando AMBOS estão desligados. Logo há 2×5-1 = 9 estados alcançáveis a quente. Como o shader pode ler os dois uniforms e decodificá-los como quiser, dá para ter 9 degraus de temperatura — ou 4 degraus de temperatura × liga/desliga da textura, que é o mapa que mais se parece com o pedido dela.",
      "evidencia": "src/config/mod.rs:151-162 (dois campos, is_noop); src/wayland/handlers/a11y.rs:41-63 (dois setters distintos); src/backend/render/mod.rs:1365-1380 (dois Uniform::new independentes)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "DERRUBADO: nem `alpha` nem `tint` servem de carona",
      "detalhe": "`alpha` chega ao shader pelo TextureRenderElement, e cosmic-comp o passa HARDCODED como Some(1.0) — não há config nem caminho que o varie no passe de pós-processamento. `tint` só é declarado sob `#if defined(DEBUG_FLAGS)`, define que o smithay injeta para sua própria visualização de damage; grep por 'debug_flags|DebugFlags|COSMIC_.*DEBUG' em todo o src/ do cosmic-comp não retorna NADA — o compositor nunca liga esse flag. As duas hipóteses de carona morrem aqui.",
      "evidencia": "src/backend/render/mod.rs:1343-1350 (from_texture_render_buffer(..., Some(1.0), ...)); src/backend/render/shaders/offscreen.frag:19-21 (#if defined(DEBUG_FLAGS) uniform float tint;); `grep -rn 'debug_flags|DebugFlags|COSMIC_.*DEBUG' src/` → vazio",
      "confianca": "medido"
     },
     {
      "titulo": "PROVADO por medição: o modo de leitura completo CABE no espaço existente",
      "detalhe": "Escrevi dois blocos GLSL candidatos e medi. Variante A (9 degraus de temperatura, textura como literal compilado): 1112 bytes, sobram 554 dos 1666. Variante B (4 degraus de temperatura × textura liga/desliga pelo switch de inverter): 868 bytes, sobram 798. Nas duas, o bloco do invert original (69 bytes) precisa virar no-op — e o substituto `if (invert == 9.0) {...}` (valor que o compositor nunca envia) tem EXATAMENTE 69 bytes. Ou seja: temperatura em degraus + textura de papel, tudo a quente, sem recompilar, sem mudar um byte do tamanho do ELF.",
      "evidencia": "wc -c sobre /tmp/.../scratchpad/bloco.glsl = 1112 e bloco2.glsl = 868, contra o orçamento medido de 1666; printf do substituto do invert = 69 bytes",
      "confianca": "medido"
     },
     {
      "titulo": "A config quente que EXISTE é a cosmic-config do CosmicComp, com watcher no event loop",
      "detalhe": "Config::load registra um cosmic_config::calloop::ConfigWatchSource sobre com.system76.CosmicComp v1 e despacha para config_changed(config, keys, state), um match gigante por nome de chave. No disco isso é um arquivo por campo em ~/.config/cosmic/com.system76.CosmicComp/v1/<campo> (conferi: accessibility_zoom, activation_policy, active_hint, pinned_workspaces, xkb_config...). Escrever um arquivo novo ali dispara o watcher na hora. É ESTE o canal para um slider contínuo — não o a11y_screen_filter.ron.",
      "evidencia": "src/config/mod.rs:176-182 (Config::new(\"com.system76.CosmicComp\", 1) + ConfigWatchSource + insert_source), :790-792 (fn config_changed / match key.as_str()); `ls ~/.config/cosmic/com.system76.CosmicComp/v1/`",
      "confianca": "medido"
     },
     {
      "titulo": "O struct de config a estender vive num crate do MESMO repo",
      "detalhe": "CosmicCompConfig está em cosmic-comp-config/src/lib.rs:69-107, um membro do workspace (Cargo.toml:10, members = [\"cosmic-comp-config\"]), com #[derive(CosmicConfigEntry)] e #[version = 1]. Cada campo pub vira uma chave de config independente. O último campo hoje é activation_policy (lib.rs:106), e o Default correspondente está em lib.rs:145. São esses dois pontos que um campo `reading_mode` toca.",
      "evidencia": "cosmic-comp-config/src/lib.rs:69-71 (derive + version + struct), :106 (activation_policy), :145 (default); Cargo.toml:10 e :16-19",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O build já existente leva ~4 min quente / ~30 min do zero, e o night light é reaplicado DEPOIS",
      "detalhe": "aurora-cosmic-comp-ws.sh:421 roda `make VENDOR=1` no fonte do apt-get source, com a mensagem '~4min com o vendor quente, ~30min do zero'. O binário sai em target/release/cosmic-comp, é verificado pelo marcador AURORA-COSMIC-WS-PATCH (grep -aqs), copiado para /var/lib/aurora/cosmic-comp-<ver>.aurora-ws e daí para /usr/bin/cosmic-comp por escrita atômica. E então, nas linhas 473-474 e 519, o script chama /usr/local/bin/aurora-night-light.py — a camada do shader é reaplicada por cima do build. A ordem é: build de workspace primeiro, patch de shader depois.",
      "evidencia": "~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh:379-429 (cmd_compilar), :85 MARCA, :88 PATCH_INSTALADO, :98 artefato_de(), :302-341 (instalação atômica), :159-163 reaplicar_night_light(), :473-474 e :519",
      "confianca": "medido"
     },
     {
      "titulo": "Recompilar APOSENTA o patch binário — e isso é uma simplificação, não um conflito",
      "detalhe": "Se os uniforms novos entrarem por recompilação, o aurora-night-light.py perde a razão de existir: a temperatura passa a vir da config, não de um literal no .rodata. Mas ele NÃO pode simplesmente sumir, porque um `apt upgrade` troca /usr/bin/cosmic-comp pelo binário do pacote (sem os uniforms novos) e o self-heal precisa reinstalar o artefato. A convivência correta é: um SEGUNDO arquivo .patch em ~/.config/zsh/patches/, aplicado por cmd_compilar logo depois do cosmic-comp-sem-workspace-vazio.patch, e o marcador de versão do build subindo (o script já compara marcadores para saber se o artefato é mais novo que o instalado).",
      "evidencia": "aurora-cosmic-comp-ws.sh:409-418 (aplicação do patch com --forward, idempotente por grep do MARCA em src/shell/mod.rs), :306-325 (comparação de marcadores, 'o shader quente do aurora-night-light.py vive só no binário instalado'); ~/.config/zsh/patches/ contém só cosmic-comp-sem-workspace-vazio.patch",
      "confianca": "medido"
     }
    ],
    "codigo": [
     {
      "para_que": "(a) offscreen.frag — os dois uniforms novos e o corpo do modo de leitura. Substitui as linhas 23-24 (declarações) e insere um bloco entre a linha 84 (fim do color_mode) e a 86 (// re-multiply).",
      "linguagem": "glsl",
      "fonte": "autoral, sobre src/backend/render/shaders/offscreen.frag do commit 5c93094",
      "trecho": "--- a/src/backend/render/shaders/offscreen.frag\n+++ b/src/backend/render/shaders/offscreen.frag\n@@ -21,6 +21,10 @@\n uniform float invert;\n uniform float color_mode;\n+// Modo de leitura (Aurora):\n+//   night_temp    = Kelvin / 10000.0  (0.10 .. 0.65). 0.0 = desligado.\n+//   paper_texture = 0.0 .. 1.0. 0.0 = desligado.\n+uniform float night_temp;\n+uniform float paper_texture;\n \n void main() {\n@@ -84,6 +88,38 @@\n     }\n \n+    // ---- temperatura de cor contínua (corpo negro, Tanner Helland) ----\n+    if (night_temp > 0.0) {\n+        float t = night_temp * 100.0;              // Kelvin / 100\n+        vec3 wp;\n+        if (t <= 66.0) {\n+            wp.r = 1.0;\n+            wp.g = clamp(0.39008158 * log(t) - 0.63184144, 0.0, 1.0);\n+            wp.b = (t <= 19.0)\n+                 ? 0.0\n+                 : clamp(0.54320679 * log(t - 10.0) - 1.19625408, 0.0, 1.0);\n+        } else {\n+            wp.r = clamp(1.29293618 * pow(t - 60.0, -0.1332047592), 0.0, 1.0);\n+            wp.g = clamp(1.12989086 * pow(t - 60.0, -0.0755148492), 0.0, 1.0);\n+            wp.b = 1.0;\n+        }\n+        color.rgb *= wp;\n+    }\n+\n+    // ---- textura de papel / e-ink ----\n+    if (paper_texture > 0.0) {\n+        // comprime a faixa dinâmica: o preto sobe, o branco desce\n+        float lo = 0.06 * paper_texture;\n+        float hi = 1.0 - 0.10 * paper_texture;\n+        color.rgb = lo + color.rgb * (hi - lo);\n+        // grão fino, estável por pixel (não cintila entre frames)\n+        float g = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453);\n+        color.rgb += (g - 0.5) * 0.035 * paper_texture;\n+        // leve dessaturação, como tinta em papel\n+        float ink = dot(color.rgb, vec3(0.299, 0.587, 0.114));\n+        color.rgb = mix(color.rgb, vec3(ink), 0.25 * paper_texture);\n+        color.rgb = clamp(color.rgb, 0.0, 1.0);\n+    }\n+\n     // re-multiply\n     color.rgb *= color.a;"
     },
     {
      "para_que": "(b) Declarar os dois Uniform na lista do renderer, e (c) passar os valores por frame. Dois hunks no MESMO arquivo.",
      "linguagem": "rust",
      "fonte": "autoral, sobre src/backend/render/mod.rs:419-425 e :1364-1381 do commit 5c93094",
      "trecho": "--- a/src/backend/render/mod.rs\n+++ b/src/backend/render/mod.rs\n@@ -419,6 +419,8 @@ pub fn init_shaders(renderer: &mut GlesRenderer) -> Result<(), GlesError> {\n     let postprocess_shader = renderer.compile_custom_texture_shader(\n         POSTPROCESS_SHADER,\n         &[\n             UniformName::new(\"invert\", UniformType::_1f),\n             UniformName::new(\"color_mode\", UniformType::_1f),\n+            UniformName::new(\"night_temp\", UniformType::_1f),\n+            UniformName::new(\"paper_texture\", UniformType::_1f),\n         ],\n     )?;\n\n@@ -1364,6 +1366,16 @@\n                     vec![\n                         Uniform::new(\n                             \"invert\",\n                             if screen_filter.filter.inverted { 1. } else { 0. },\n                         ),\n                         Uniform::new(\n                             \"color_mode\",\n                             screen_filter\n                                 .filter\n                                 .color_filter\n                                 .map(|val| val as u8 as f32)\n                                 .unwrap_or(0.),\n                         ),\n+                        Uniform::new(\n+                            \"night_temp\",\n+                            screen_filter\n+                                .filter\n+                                .night_temp\n+                                .map(|k| k as f32 / 10_000.)\n+                                .unwrap_or(0.),\n+                        ),\n+                        Uniform::new(\"paper_texture\", screen_filter.filter.paper_texture),\n                     ],"
     },
     {
      "para_que": "(c-1) Os dois campos novos no ScreenFilter — e a correção do is_noop(), sem a qual o passe inteiro é pulado e o patch não faz NADA.",
      "linguagem": "rust",
      "fonte": "autoral, sobre src/config/mod.rs:151-162 do commit 5c93094",
      "trecho": "--- a/src/config/mod.rs\n+++ b/src/config/mod.rs\n@@ -151,13 +151,24 @@\n #[derive(Debug, Default, Deserialize, Serialize, Clone, PartialEq)]\n pub struct ScreenFilter {\n     pub inverted: bool,\n     #[serde(skip_serializing_if = \"Option::is_none\")]\n     pub color_filter: Option<ColorFilter>,\n+    /// Temperatura de cor em Kelvin (1000..=6500). None = desligada.\n+    #[serde(default, skip_serializing_if = \"Option::is_none\")]\n+    pub night_temp: Option<u32>,\n+    /// Intensidade da textura de papel, 0.0..=1.0. 0.0 = desligada.\n+    #[serde(default)]\n+    pub paper_texture: f32,\n }\n \n impl ScreenFilter {\n     pub fn is_noop(&self) -> bool {\n-        !self.inverted && self.color_filter.is_none()\n+        // ATENÇÃO: render_output() PULA o passe inteiro quando isto é true\n+        // (src/backend/render/mod.rs:1284). Esquecer um campo aqui faz os\n+        // uniforms novos nunca chegarem ao driver.\n+        !self.inverted\n+            && self.color_filter.is_none()\n+            && self.night_temp.is_none()\n+            && self.paper_texture <= 0.0\n     }\n }"
     },
     {
      "para_que": "(c-2) A chave de config nova, que é o canal QUENTE (o a11y_screen_filter.ron só é lido no boot). Um campo pub aqui vira um arquivo em ~/.config/cosmic/com.system76.CosmicComp/v1/reading_mode.",
      "linguagem": "rust",
      "fonte": "autoral, sobre cosmic-comp-config/src/lib.rs:106 e :145 do commit 5c93094",
      "trecho": "--- a/cosmic-comp-config/src/lib.rs\n+++ b/cosmic-comp-config/src/lib.rs\n@@ -106,6 +106,17 @@ pub struct CosmicCompConfig {\n     pub activation_policy: ActivationPolicy,\n+    /// Modo de leitura (Aurora): temperatura de cor e textura de papel.\n+    pub reading_mode: ReadingMode,\n }\n \n+#[derive(Debug, Default, Copy, Clone, PartialEq, Deserialize, Serialize)]\n+pub struct ReadingMode {\n+    /// Kelvin, 1000..=6500. None = desligado.\n+    pub temperature: Option<u32>,\n+    /// 0.0..=1.0\n+    pub texture: f32,\n+}\n+\n impl Default for CosmicCompConfig {\n     fn default() -> Self {\n         Self {\n@@ -145,6 +156,7 @@\n             activation_policy: ActivationPolicy::default(),\n+            reading_mode: ReadingMode::default(),\n         }\n     }\n }"
     },
     {
      "para_que": "(c-3) O braço do watcher que faz a chave nova valer A QUENTE. Vai no match de config_changed. O padrão de emprestar a config e o backend ao mesmo tempo é o mesmo já usado em a11y.rs:42-48, então o borrow checker aceita.",
      "linguagem": "rust",
      "fonte": "autoral, sobre src/config/mod.rs:790-792 (fn config_changed) do commit 5c93094",
      "trecho": "--- a/src/config/mod.rs\n+++ b/src/config/mod.rs\n@@ -792,6 +792,26 @@ fn config_changed(config: cosmic_config::Config, keys: Vec<String>, state: &mut S\n     for key in &keys {\n         match key.as_str() {\n+            \"reading_mode\" => {\n+                let value = get_config::<ReadingMode>(&config, \"reading_mode\");\n+                let mut guard = state.common.config.dynamic_conf.screen_filter_mut();\n+                let mut updated = (*guard).clone();\n+                updated.night_temp = value\n+                    .temperature\n+                    .filter(|k| (1000..=6500).contains(k));\n+                updated.paper_texture = value.texture.clamp(0.0, 1.0);\n+                if updated != *guard {\n+                    if let Err(err) = state.backend.update_screen_filter(&updated) {\n+                        warn!(?err, \"Failed to apply reading mode\");\n+                    } else {\n+                        *guard = updated;\n+                    }\n+                }\n+                drop(guard);\n+                state.common.config.cosmic_conf.reading_mode = value;\n+            }\n             \"xkb_config\" => {"
     },
     {
      "para_que": "(c-4) O ScheduleRender que falta. Sem isto o slider trava numa tela parada: set_screen_filter hoje não pede repintura, e a mudança só aparece quando outra coisa (relógio do painel, cursor) sujar a tela.",
      "linguagem": "rust",
      "fonte": "autoral, sobre src/backend/kms/surface/mod.rs:401-405 do commit 5c93094",
      "trecho": "--- a/src/backend/kms/surface/mod.rs\n+++ b/src/backend/kms/surface/mod.rs\n@@ -401,6 +401,9 @@\n     pub fn set_screen_filter(&mut self, config: ScreenFilter) {\n         let _ = self\n             .thread_command\n             .send(ThreadCommand::UpdateScreenFilter(config));\n+        // Arrastar um slider numa tela parada não gera damage nenhum.\n+        // Sem isto a mudança só aparece no próximo frame \"natural\".\n+        let _ = self.thread_command.send(ThreadCommand::ScheduleRender);\n     }"
     },
     {
      "para_que": "CAMINHO SEM RECOMPILAR — variante recomendada. Substitui a região do color_mode (1666 bytes medidos) por 4 degraus de temperatura × textura de papel liga/desliga. Medido: 868 bytes, sobram 798. O padding com espaços até 1666 é obrigatório para preservar o tamanho do ELF.",
      "linguagem": "glsl",
      "fonte": "autoral; orçamento medido no /usr/bin/cosmic-comp vivo (região 0x2dc16a até o `// re-multiply`)",
      "trecho": "// SUBSTITUI a região inteira do color_mode (greyscale + as 3 matrizes de\n// daltonismo). Custo assumido: os filtros de daltonismo deixam de existir,\n// além do greyscale que o patch atual já sacrifica.\n//\n// Mapeamento dos dois uniforms discretos que existem:\n//   color_mode 1..4  -> 4 degraus de temperatura (5000K, 4000K, 3500K, 3000K)\n//   invert     0/1   -> textura de papel desligada / ligada\n// Os dois são switches SEPARADOS em Configurações > Acessibilidade, e\n// is_noop() só é true quando ambos estão desligados -- 9 estados a quente.\n\n    if (color_mode >= 1.0 || invert == 1.0) {\n        vec3 wp = vec3(1.0, 1.0, 1.0);\n        if (color_mode == 1.0) { wp = vec3(1.0, 0.90993, 0.82219); }\n        else if (color_mode == 2.0) { wp = vec3(1.0, 0.82854, 0.64606); }\n        else if (color_mode == 3.0) { wp = vec3(1.0, 0.77987, 0.54468); }\n        else if (color_mode >= 4.0) { wp = vec3(1.0, 0.71760, 0.42107); }\n        color.rgb *= wp;\n        if (invert == 1.0) {\n            float lo = 0.05;\n            float hi = 0.92;\n            color.rgb = lo + color.rgb * (hi - lo);\n            float g = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453);\n            color.rgb += (g - 0.5) * 0.030;\n            float ink = dot(color.rgb, vec3(0.299, 0.587, 0.114));\n            color.rgb = mix(color.rgb, vec3(ink), 0.22);\n            color.rgb = clamp(color.rgb, 0.0, 1.0);\n        }\n    }\n\n// E o bloco do invert original (69 bytes, em src/.../offscreen.frag:40-42)\n// tem de virar no-op, porque o invert agora é a chave da textura. O\n// substituto tem EXATAMENTE 69 bytes -- compara com um valor que o\n// compositor nunca envia:\n//\n//     if (invert == 9.0) {\n//         color.rgb = 1.0 - color.rgb;\n//     }\n//"
     },
     {
      "para_que": "Agendamento por horário SEM recompilar: um cliente Wayland mínimo que fala cosmic_a11y_manager_v1 (versão 2, confirmada viva na máquina dela) e troca o degrau. É o que um systemd timer chama às 18h.",
      "linguagem": "rust",
      "fonte": "autoral; protocolo lido em src/wayland/protocols/a11y.rs:196-212 e confirmado por wayland-info",
      "trecho": "// Cargo.toml: wayland-client = \"0.31\"; cosmic-protocols = { git =\n//   \"https://github.com/pop-os/cosmic-protocols\", rev = \"160b086\" }\n// (o mesmo rev que o cosmic-comp usa -- Cargo.toml:24 do compositor)\n//\n// O bind exige client_not_sandboxed (src/state.rs:762): binário nativo\n// funciona, flatpak não.\n\nuse cosmic_protocols::a11y::v1::client::cosmic_a11y_manager_v1::{\n    ActiveState, CosmicA11yManagerV1, Filter,\n};\n\n// Depois de bindar o global cosmic_a11y_manager_v1 na versão 2:\n//\n//   manager.set_screen_filter(inverted, filter);\n//\n// inverted: ActiveState::Enabled  -> liga a textura de papel\n// filter:   Filter::Greyscale            -> degrau 1 (5000K)\n//           Filter::DaltonizeProtanopia  -> degrau 2 (4000K)\n//           Filter::DaltonizeDeuteranopia-> degrau 3 (3500K)\n//           Filter::DaltonizeTritanopia  -> degrau 4 (3000K)\n//           Filter::Disabled             -> sem temperatura\n//\n// ATENÇÃO (src/wayland/protocols/a11y.rs:203-211): o compositor SÓ age se o\n// valor DIFERIR do estado atual. Mandar o mesmo valor duas vezes é no-op --\n// não é erro, mas não force um \"reaplicar\" esperando repintura.\n\nfn degrau_para_filter(n: u8) -> Filter {\n    match n {\n        1 => Filter::Greyscale,\n        2 => Filter::DaltonizeProtanopia,\n        3 => Filter::DaltonizeDeuteranopia,\n        4 => Filter::DaltonizeTritanopia,\n        _ => Filter::Disabled,\n    }\n}"
     },
     {
      "para_que": "Depois de recompilar: como um slider/agendador escreve o valor contínuo. Escrever este arquivo dispara o ConfigWatchSource e vale na hora, sem logout.",
      "linguagem": "bash",
      "fonte": "autoral; canal confirmado em src/config/mod.rs:176-182 e `ls ~/.config/cosmic/com.system76.CosmicComp/v1/`",
      "trecho": "#!/usr/bin/env bash\n# aurora-modo-leitura.sh — temperatura e textura a quente, via cosmic-config.\n# Requer o cosmic-comp RECOMPILADO com os uniforms night_temp/paper_texture.\n\nCHAVE=\"$HOME/.config/cosmic/com.system76.CosmicComp/v1/reading_mode\"\nmkdir -p \"$(dirname \"$CHAVE\")\"\n\ntemp=\"${1:-3500}\"     # 1000..6500, ou \"off\"\ntextura=\"${2:-0.0}\"   # 0.0..1.0\n\nif [ \"$temp\" = off ]; then\n  printf '(temperature: None, texture: %s)\\n' \"$textura\" > \"$CHAVE.tmp\"\nelse\n  printf '(temperature: Some(%d), texture: %s)\\n' \"$temp\" \"$textura\" > \"$CHAVE.tmp\"\nfi\n# Escrita atômica: o watcher do cosmic-config dispara no rename, e um\n# arquivo pela metade faria o get_config cair no Default silenciosamente\n# (src/config/mod.rs:749-757 engole o erro e devolve T::default()).\nmv -f \"$CHAVE.tmp\" \"$CHAVE\"\n\n# Agendamento: dois timers systemd de usuário.\n#   aurora-modo-leitura.sh 3500 0.4   às 18:00\n#   aurora-modo-leitura.sh off  0.0   às 07:00"
     }
    ],
    "derrubado": [
     "\"O filtro de cor de acessibilidade está em cosmic-config e um watcher o relê.\" FALSO. Ele mora em ~/.local/state/cosmic-comp/a11y_screen_filter.ron, é lido UMA vez no load_dynamic() do boot (src/config/mod.rs:341-357, :377-398) e NÃO tem watcher. O compositor só ESCREVE nesse arquivo (via PersistenceGuard) quando o valor muda. Editar o .ron à mão não muda nada a quente — só no próximo login. O canal quente é o protocolo Wayland cosmic_a11y_manager_v1.",
     "\"Dá para pegar carona no uniform `alpha` para injetar um valor contínuo.\" FALSO. O alpha do passe de pós-processamento é passado HARDCODED como Some(1.0) em src/backend/render/mod.rs:1346. Não há config, protocolo nem env var que o varie. É constante 1.0 em todo frame.",
     "\"Dá para pegar carona no uniform `tint`.\" FALSO, e por dois motivos independentes. (1) `tint` só é DECLARADO sob `#if defined(EXTERNAL)`... na verdade sob `#if defined(DEBUG_FLAGS)` (offscreen.frag:19-21), define que o smithay injeta apenas para a visualização de damage dele. (2) grep por 'debug_flags|DebugFlags|COSMIC_.*DEBUG' em todo o src/ do cosmic-comp não retorna NADA: o compositor nunca liga esse flag, então o uniform sequer existe no programa compilado.",
     "\"Sem recompilar só existem 2 estados (ligado/desligado), como o switch de hoje sugere.\" FALSO. `invert` e `color_filter` são campos SEPARADOS do ScreenFilter, com setters separados (a11y.rs:41 e :53) e switches separados na UI. is_noop() só é true quando ambos estão desligados. São 2×5−1 = 9 estados alcançáveis a quente, e o shader pode decodificar os dois uniforms como quiser.",
     "\"O patch binário da Aurora está no limite do espaço disponível.\" FALSO, e por larga margem. Medi no binário vivo: o bloco que ele reescreve tem 195 bytes, mas a região contígua do color_mode (greyscale + as três matrizes de daltonismo) tem 1666 bytes, mais 69 do bloco do invert. O patcher usa 11% do orçamento. Cabe um modo de leitura inteiro ali dentro (medi: 868 bytes na variante recomendada).",
     "\"Trocar o filtro repinta a tela imediatamente.\" FALSO por acidente feliz. Surface::set_screen_filter (kms/surface/mod.rs:401-405) NÃO envia ThreadCommand::ScheduleRender — outros setters do mesmo arquivo enviam (linha 391), esse não. Hoje parece instantâneo porque o relógio do painel e o cursor sujam a tela o tempo todo. Um slider arrastado sobre uma tela parada travaria.",
     "\"O build do cosmic-comp leva ~30 min, então qualquer solução que recompile é inviável.\" IMPRECISO. O aurora-cosmic-comp-ws.sh:421 diz '~4min com o vendor quente, ~30min do zero' — e o vendor está quente nesta máquina, porque o auto-build já rodou. São ~4 min, não 30. (Isso bate com a memória de 25/08 que já corrigia o número.)"
    ],
    "resumo": "Rastreei o caminho inteiro do color_mode no commit 5c93094 e ele é mais curto e mais rígido do que parecia: o shader entra por um include_str! único (render/mod.rs:131), os uniforms são declarados em UMA lista de dois nomes (mod.rs:419-425), e o valor por frame é literalmente `enum as u8 as f32` (mod.rs:1373-1380) — não existe ponto nenhum onde um float arbitrário caiba sem recompilar. A descoberta que muda o desenho da solução é outra: o filtro de acessibilidade NÃO está em cosmic-config. Ele mora em ~/.local/state/cosmic-comp/a11y_screen_filter.ron, que é lido uma única vez no boot e nunca mais; o que faz o toggle valer a quente é o protocolo Wayland cosmic_a11y_manager_v1, que confirmei VIVO na máquina dela na versão 2. Isso derruba a ideia de mexer no .ron e, em troca, abre o agendamento por horário sem recompilar nada — um cliente Wayland nativo de trinta linhas troca o estado quando o systemd timer mandar. Também medi o orçamento real do patch binário: o bloco que a Aurora reescreve tem 195 bytes, mas a região contígua do color_mode tem 1666, mais 69 do invert — o patcher atual usa 11% do que existe. Escrevi e MEDI um bloco GLSL de 868 bytes que entrega 4 degraus de temperatura mais textura de papel dentro desse espaço, tudo comutável a quente pelos dois switches que já existem em Acessibilidade (são 9 estados, não 2, porque invert e color_filter são independentes). Slider contínuo de verdade só recompilando; o diff está pronto e cabe em cinco arquivos, com duas armadilhas que valem mais que o resto: is_noop() precisa incluir os campos novos ou o passe inteiro é pulado em silêncio, e set_screen_filter precisa passar a pedir ScheduleRender ou o slider trava numa tela parada.",
    "solucoes": [
     {
      "nome": "Degraus a quente pelo patch binário (sem recompilar, entrega hoje)",
      "alvo": "varios",
      "como": "Estender o aurora-night-light.py para reescrever a REGIÃO INTEIRA do color_mode (1666 bytes medidos), não só o bloco de 195. O bloco novo (868 bytes, medido, sobram 798) decodifica os dois uniforms discretos que já chegam a quente: color_mode 1..4 vira 4 degraus de temperatura (5000/4000/3500/3000K) e invert vira o liga/desliga da textura de papel. O bloco original do invert (69 bytes) é neutralizado por um substituto de EXATAMENTE 69 bytes que compara com um valor que o compositor nunca envia (`invert == 9.0`). Ela passa a controlar tudo por Configurações > Acessibilidade: o dropdown de filtros escolhe a temperatura, o switch 'Inverter cores' liga o modo Kindle. Para o agendamento, um cliente Wayland nativo bindando cosmic_a11y_manager_v1 v2 e chamando set_screen_filter(inverted, filter), disparado por dois timers systemd de usuário.",
      "onde": "~/.config/zsh/scripts/aurora-night-light.py (find_block/build_block passam a delimitar até o `    // re-multiply` em vez do `} else if (color_mode >= 2.0) {`); binário /usr/bin/cosmic-comp, região 0x2dc16a..+1666 e o bloco invert em 0x2dc0f7..+69; cliente novo em ~/.config/zsh/scripts/",
      "a_quente": true,
      "custo": "Perde os TRÊS filtros de daltonismo além do greyscale que já se perdeu. Os degraus são 4, não um slider — ela escolhe entre valores, não arrasta. A textura fica em intensidade fixa (compilada no literal); mudar a intensidade exige repatch + logout. O switch 'Inverter cores' deixa de inverter. Escrever o cliente Wayland é ~1h de trabalho.",
      "durabilidade": "Sobrevive a reboot. NÃO sobrevive a apt upgrade sozinho — mas o self-heal já reaplica o night-light a cada ciclo (aurora-cosmic-comp-ws.sh:159-163), então na prática sobrevive. Imune a troca de tema. Quebra se o upstream reescrever o offscreen.frag, e nesse caso o find_block devolve None e o script sai com rc=3 sem tocar no binário."
     },
     {
      "nome": "Sliders contínuos de verdade, recompilando (o que ela pediu)",
      "alvo": "varios",
      "como": "Aplicar um segundo patch ao mesmo fonte que o aurora-cosmic-comp-ws.sh já compila, tocando cinco arquivos: (1) offscreen.frag ganha `uniform float night_temp;` e `uniform float paper_texture;` mais os dois blocos de corpo; (2) render/mod.rs:419-425 declara os dois UniformName::_1f; (3) render/mod.rs:1364-1381 passa os dois Uniform::new por frame; (4) config/mod.rs:151-162 acrescenta night_temp: Option<u32> e paper_texture: f32 ao ScreenFilter E os inclui no is_noop(); (5) cosmic-comp-config/src/lib.rs:106,145 ganha o campo reading_mode, e config/mod.rs:792 ganha o braço \"reading_mode\" no config_changed, que é o watcher que já existe. Bônus obrigatório: kms/surface/mod.rs:401 passa a mandar ThreadCommand::ScheduleRender. Aí um `printf '(temperature: Some(3500), texture: 0.4)' > ~/.config/cosmic/com.system76.CosmicComp/v1/reading_mode` vale na hora, e dois timers systemd fazem o agendamento.",
      "onde": "Novo ~/.config/zsh/patches/cosmic-comp-modo-leitura.patch, aplicado por cmd_compilar (aurora-cosmic-comp-ws.sh:409-418) logo depois do cosmic-comp-sem-workspace-vazio.patch. Arquivos do fonte: src/backend/render/shaders/offscreen.frag, src/backend/render/mod.rs, src/config/mod.rs, src/backend/kms/surface/mod.rs, cosmic-comp-config/src/lib.rs",
      "a_quente": true,
      "custo": "~4 min de build com o vendor quente (~30 min do zero) a cada versão nova do cosmic-comp, em cima do build que já acontece. Nenhum filtro de acessibilidade é sacrificado — greyscale e daltonismo voltam. O aurora-night-light.py fica obsoleto e deve ser aposentado, não só desligado, para não repatchar um binário que já tem os uniforms.",
      "durabilidade": "Sobrevive a reboot e a troca de tema. Um apt upgrade troca o binário, e aí vale exatamente o que já vale para o patch de workspace: o self-heal detecta pelo marcador e reinstala o artefato de /var/lib/aurora/, ou dispara o auto-build. O patch quebra se o upstream mexer no offscreen.frag ou na lista de uniforms — e nesse caso `patch --forward` falha limpo e nada é instalado (aurora-cosmic-comp-ws.sh:414-417)."
     },
     {
      "nome": "Agendamento por horário, isolado (vale nos dois caminhos acima)",
      "alvo": "agendamento",
      "como": "Um cliente Wayland nativo que binda cosmic_a11y_manager_v1 (versão 2, confirmada viva) e emite set_screen_filter(inverted, filter), mais dois timers systemd de usuário (OnCalendar=*-*-* 18:00 e 07:00). No caminho recompilado, é ainda mais simples: nem precisa de Wayland, basta escrever atomicamente o arquivo reading_mode da cosmic-config e o ConfigWatchSource faz o resto. Atenção a um detalhe do protocolo: o compositor só age se o valor DIFERIR do atual (a11y.rs:203-211), então reenviar o mesmo estado é no-op silencioso.",
      "onde": "~/.config/zsh/scripts/aurora-modo-leitura.sh + duas units em ~/.config/systemd/user/; cliente Wayland compilado para /usr/local/bin/",
      "a_quente": true,
      "custo": "Se for pelo protocolo Wayland, é um binário Rust novo para manter (wayland-client + cosmic-protocols no rev 160b086, o mesmo do compositor). Se for pela cosmic-config, custa zero além do script — mas depende da recompilação. O cliente precisa ser nativo: flatpak não binda o global (client_not_sandboxed).",
      "durabilidade": "Independente de apt upgrade do cosmic-comp: o protocolo é estável e versionado. Sobrevive a reboot pelos timers. Só quebra se a System76 subir a versão do cosmic_a11y_manager e remover requests — improvável, protocolos Wayland não removem."
     }
    ],
    "trilha": "Rust do cosmic-comp: caminho do color_mode até o shader, e como acrescentar uniforms quentes"
   },
   "verificadas": [
    {
     "nome": "Degraus a quente pelo patch binário (sem recompilar, entrega hoje)",
     "alvo": "varios",
     "como": "Estender o aurora-night-light.py para reescrever a REGIÃO INTEIRA do color_mode (1666 bytes medidos), não só o bloco de 195. O bloco novo (868 bytes, medido, sobram 798) decodifica os dois uniforms discretos que já chegam a quente: color_mode 1..4 vira 4 degraus de temperatura (5000/4000/3500/3000K) e invert vira o liga/desliga da textura de papel. O bloco original do invert (69 bytes) é neutralizado por um substituto de EXATAMENTE 69 bytes que compara com um valor que o compositor nunca envia (`invert == 9.0`). Ela passa a controlar tudo por Configurações > Acessibilidade: o dropdown de filtros escolhe a temperatura, o switch 'Inverter cores' liga o modo Kindle. Para o agendamento, um cliente Wayland nativo bindando cosmic_a11y_manager_v1 v2 e chamando set_screen_filter(inverted, filter), disparado por dois timers systemd de usuário.",
     "onde": "~/.config/zsh/scripts/aurora-night-light.py (find_block/build_block passam a delimitar até o `    // re-multiply` em vez do `} else if (color_mode >= 2.0) {`); binário /usr/bin/cosmic-comp, região 0x2dc16a..+1666 e o bloco invert em 0x2dc0f7..+69; cliente novo em ~/.config/zsh/scripts/",
     "a_quente": true,
     "custo": "Perde os TRÊS filtros de daltonismo além do greyscale que já se perdeu. Os degraus são 4, não um slider — ela escolhe entre valores, não arrasta. A textura fica em intensidade fixa (compilada no literal); mudar a intensidade exige repatch + logout. O switch 'Inverter cores' deixa de inverter. Escrever o cliente Wayland é ~1h de trabalho.",
     "durabilidade": "Sobrevive a reboot. NÃO sobrevive a apt upgrade sozinho — mas o self-heal já reaplica o night-light a cada ciclo (aurora-cosmic-comp-ws.sh:159-163), então na prática sobrevive. Imune a troca de tema. Quebra se o upstream reescrever o offscreen.frag, e nesse caso o find_block devolve None e o script sai com rc=3 sem tocar no binário.",
     "trilha": "render-uniforms",
     "veredito": {
      "solucao": "Degraus a quente pelo patch binário (trilha \"render-uniforms\") — estender o aurora-night-light.py para reescrever a região inteira do color_mode (1666 B) e neutralizar o bloco do invert, mapeando color_mode 1..4 em 4 temperaturas e invert em textura de papel; agendamento por cliente Wayland cosmic_a11y_manager_v1.",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "O MECANISMO ESTÁ PROVADO — não por leitura de strings, mas pelo FONTE, que está na máquina: /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/ (o tarball do build da Aurora).\n\nCONFIRMADO (medido, 29/08):\n1) A região existe e mede EXATAMENTE o que a proposta diz. De `    if (color_mode == 1.0) {` até `    // re-multiply`: offset 0x2dc16a, 1666 bytes. O bloco patchado hoje ocupa 195. Sobram 1471 para o bloco novo (a proposta fala em 868 usados / 798 sobrando — cabe).\n\n2) \"A quente\" está provado pelo próprio upstream. src/backend/render/mod.rs:1366-1380 e src/backend/kms/surface/mod.rs:1897/1931 passam `invert` e `color_mode` como `Uniform::new(...)` a CADA render_output. E src/backend/kms/mod.rs:722 `update_screen_filter()` traz o comentário do upstream, literal: \"The shader is already compiled at this point and we don't rely on any features that might not be available\". Não há recompilação de shader ao trocar filtro. Uniform por frame, confirmado.\n\n3) `invert` sozinho ACORDA o passe offscreen. src/config/mod.rs:159 — `pub fn is_noop(&self) -> bool { !self.inverted && self.color_filter.is_none() }`, e render/mod.rs:1284 só entra no offscreen `if !screen_filter.filter.is_noop()`. Logo \"textura ligada, filtro de cor desligado\" funciona. E as duas coisas combinam (invert=1 + filter=greyscale).\n\n4) O toggle \"Inverter cores\" EXISTE na UI desta versão: `.invert-colors = Inverter cores` está no /usr/bin/cosmic-settings, na página accessibility. O gancho da textura é real.\n\n5) O caminho do agendamento está certo, e por um motivo que a proposta acertou sem justificar: src/wayland/protocols/a11y.rs:63 faz `dh.create_global::<...>(2, ...)` — o compositor anuncia o global na **versão 2**, não 3. Então `set_screen_filter2` é inalcançável e `set_screen_filter(inverted, filter)` é o único caminho, sem risco do erro \"deprecated\". O enum é filter: greyscale=2, protanopia=3, deuteranopia=4, tritanopia=5; disabled=0 desliga; unknown=1 = \"não mexa\".\n\n6) Durabilidade confere. O estado vive em ~/.local/state/cosmic-comp/a11y_screen_filter.ron (existe, hoje `(inverted: false,)`) → sobrevive a reboot. E ritual-aurora-self-heal.sh:2639 chama /usr/local/bin/aurora-night-light.py TODO ciclo, tratando rc=3 como aviso → sobrevive a apt upgrade. Imune a tema (é binário).\n\n7) Fronteira OK: docs/FRONTEIRA.md dá o binário cosmic-comp (\"patches de workspace e night light\") à Aurora. Não pisa em ninguém. Mas ~/.config/zsh tem auto-commit de 10 min — editar o patcher ali vira commit no repo privado dela.\n\nO QUE ESTÁ ERRADO NA PROPOSTA (por isso PLAUSÍVEL, não CONFIRMADO):\n\nA) O offset e o tamanho do bloco do invert estão ERRADOS. Medido: 0x2dc125, **68 bytes** (`    if (invert == 1.0) {\\n        color.rgb = 1.0 - color.rgb;\\n    }\\n`). A proposta diz 0x2dc0f7 e 69. Gravar 69 bytes sobre 68 muda o tamanho do binário — o `assert len(data) == size_before` do próprio script aborta, mas depois do estrago na memória. A ideia (`invert == 9.0`, um caractere trocado) é válida: 68 → 68 bytes, exato. Só que offset absoluto não deve entrar em código nenhum: ele muda a cada versão do cosmic-comp; sempre localizar por string.\n\nB) A GUARDA DE 512 BYTES BLOQUEIA A PROPOSTA — e afrouxá-la é o verdadeiro perigo. find_block tem `if end - start > 512: return None`. 1666 > 512, então hoje o patcher recusaria. Trocar o limite por ~2048 tira a ÚNICA rede que impede o self-heal (root, sem humano, de hora em hora e depois de todo apt) de sobrescrever às cegas um offscreen.frag reescrito pelo upstream. Com 195 bytes o risco era pequeno; com 1666 bytes de shader autoral, um reapply automático sobre um shader mudado pode gerar um binário que não compila — e aí ela acorda sem desktop, sem ninguém por perto.\n\nC) O bloco GLSL de 868 bytes NÃO FOI ESCRITO. Não dá para validar \"é ES 1.00 válido\" em código que não existe. Duas restrições reais que ele terá de respeitar, verificadas no fonte: (i) NÃO é possível acrescentar uniform novo por patch binário — os uniforms são registrados em Rust, render/mod.rs:418-424 (`compile_custom_texture_shader(POSTPROCESS_SHADER, &[invert, color_mode])`); logo não há `size`/`resolution` para escalar a textura, e a intensidade fica mesmo congelada no literal, como a proposta admite; (ii) o offscreen.frag não tem `uniform vec2 size` (quem tem é o rounded_rectangle.frag). Para coordenada de tela use `v_coords` (varying highp, já declarado) multiplicado por constante — evite `gl_FragCoord`, que em GLSL ES 1.00 é declarado **mediump**. Ruído procedural por `fract(sin(dot(...))*K)` é legal em ES 1.00; `texelFetch`, `inverse()`, `mod289`-com-loop-variável e afins não são.\n\nD) Não existe glslangValidator/glslc na máquina — mas existe algo melhor, e a proposta não citou: o cosmic-comp roda ANINHADO. src/backend/mod.rs:26-39 aceita `COSMIC_BACKEND=winit` (e cai em winit sozinho se WAYLAND_DISPLAY existir), e src/backend/winit.rs:143 chama o MESMO `init_shaders`. Dá para testar o binário patchado numa janela, no workspace OS, antes de encostar em /usr/bin.",
      "risco_real": "O QUE QUEBRA: shader que não compila = SEM DESKTOP, e sem tela de login também.\n\nProvado no fonte: `init_shaders()` é chamado na inicialização do device KMS — src/backend/kms/device.rs:1119 `init_shaders(...).context(\"Failed to compile shaders\")?` e src/backend/kms/surface/mod.rs:733 `.context(\"Failed to initialize shaders\")?`. Não é preguiçoso, não espera ela ligar o filtro: o POSTPROCESS_SHADER é compilado sempre, na subida da GPU. Erro de sintaxe → o cosmic-comp não abre a GPU → nada de sessão. E o cosmic-greeter (greetd --config /etc/greetd/cosmic-greeter.toml) sobe sob o mesmo cosmic-comp, então o login gráfico cai junto. Não é \"o modo de leitura não funciona\"; é tela preta.\n\nTEM VOLTA, e ela é curta:\n- TTY existe: /dev/tty1..tty6 presentes, /usr/lib/systemd/system/autovt@.service instalado, getty.target ativo. Ctrl+Alt+F3 abre console. Ressalva honesta: com o greetd reiniciando em laço, a disputa pelo VT pode atrapalhar.\n- Bootloader: esta máquina NÃO tem GRUB (não existe /etc/default/grub) — é **systemd-boot** 255.4 (bootctl confirma, com \"Menu timeout control\"). O caminho garantido é o menu do systemd-boot, `e`, acrescentar `systemd.unit=multi-user.target`.\n\nO COMANDO DE VOLTA DA PROPOSTA ESTÁ ERRADO — este é o achado mais perigoso.\n`aurora-night-light.py --restore` copia /var/lib/aurora/cosmic-comp-<ver>.orig. Medido agora:\n  /usr/bin/cosmic-comp ................................... 33.571.456 B (NIGHT LIGHT: sim)\n  .../cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws .. 33.571.456 B (NIGHT LIGHT: não)\n  .../cosmic-comp-0.1~1787767625~24.04~5c93094.orig ....... 28.339.864 B (o binário do PACOTE)\nO que está instalado é o binário RECOMPILADO da Aurora. O `.orig` é o do apt. Um `--restore` no escuro devolve o binário de fábrica e **derruba o patch de workspace** — o workspace vazio volta e ninguém liga uma coisa na outra dias depois. A volta certa, digitada no TTY, é:\n  sudo cp /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws /usr/bin/cosmic-comp\n(cópia limpa, sem night light, com o patch de workspace intacto — conferido byte a byte).\n\nRISCO CONTÍNUO, não só do dia da instalação: o self-heal reaplica o patcher sozinho, como root, depois de todo apt (ritual-aurora-self-heal.sh:2639). Com a guarda de 512 afrouxada para 1666+, um apt upgrade que mexa no offscreen.frag pode produzir um binário quebrado sem humano no circuito.\n\nCUSTOS QUE ELA VAI SENTIR, além dos já listados na proposta: os degraus aparecem para ela com os nomes clínicos do dropdown (\"Escala de cinza\", \"Protanopia\", \"Deuteranopia\", \"Tritanopia\") — ela teria de decorar que \"Tritanopia\" = 3000K. Ela pediu um SLIDER; isto é um menu de quatro doenças.",
      "correcao": "1) OFFSET/TAMANHO DO INVERT: o bloco é 0x2dc125, 68 bytes, não 0x2dc0f7/69. O substituto `    if (invert == 9.0) {\\n        color.rgb = 1.0 - color.rgb;\\n    }\\n` tem 68 bytes exatos — serve. Mas não grave offset nenhum em código: localize por `data.find(b\"    if (invert == 1.0) {\", anchor)` e recorte até o `    }\\n` seguinte, e afirme `len(novo) == len(velho)` antes de escrever.\n\n2) TROQUE A GUARDA DE 512 POR UMA ÂNCORA FORTE. Não basta subir o limite. Grave no script o SHA-256 dos 1666 bytes originais da região (e o dos já-patchados que o script mesmo produz). find_block só devolve a região se o hash bater com um dos conhecidos; qualquer outra coisa = rc=3, binário intocado. Isso preserva exatamente a propriedade que hoje protege a máquina: upstream mudou → o self-heal só avisa.\n\n3) PORTÃO OBRIGATÓRIO ANTES DE ENCOSTAR EM /usr/bin — e é barato: patche uma CÓPIA e rode aninhado.\n   cp /usr/bin/cosmic-comp /tmp/cc-teste\n   aurora-night-light.py --target /tmp/cc-teste ...   (o --target já existe, e faz backup ao lado)\n   aurora-workspace.sh run env COSMIC_BACKEND=winit /tmp/cc-teste\nConfirmado no fonte que isto exercita o mesmo compilador: src/backend/mod.rs:26-39 e src/backend/winit.rs:143 chamam o mesmo init_shaders. Se o GLSL estiver quebrado, o erro aparece numa janela no workspace OS — não no login dela. Instalar em /usr/bin só depois desse teste passar.\n\n4) DOCUMENTE A VOLTA CERTA no cabeçalho do patcher e diga a ela em voz alta antes de instalar:\n   TTY (Ctrl+Alt+F3) →\n   sudo cp /var/lib/aurora/cosmic-comp-$(dpkg-query -W -f='${Version}' cosmic-comp).aurora-ws /usr/bin/cosmic-comp\n   Se o .aurora-ws não existir para a versão corrente, aí sim o `.orig`, sabendo que o workspace vazio volta.\n   E CORRIJA o `--restore`: ele deve preferir o `.aurora-ws` quando existir, senão está desfazendo silenciosamente o patch de workspace.\n\n5) GLSL: escreva o bloco de 868 bytes e cole-o para revisão antes de qualquer patch. Restrições verificadas: sem uniform novo (a lista está em Rust, render/mod.rs:418-424), sem `size` neste shader, use `v_coords` (highp) e não `gl_FragCoord` (mediump em ES 1.00), sem texelFetch/inverse/loops de contagem variável. Mantenha o `color.rgb /= color.a` / `color.rgb *= color.a` que cercam a região intactos — eles ficam FORA do trecho reescrito, e a região termina imediatamente antes de `    // re-multiply`.\n\n6) AGENDAMENTO: bind na versão 2 (é a que o compositor anuncia — a11y.rs:63), request `set_screen_filter(inverted, filter)`. Para trocar só a temperatura sem mexer na textura, mande `inverted` com o valor atual lido do evento `screen_filter` que chega no bind — o protocolo não tem \"não mexa\" para o inverted, só para o filter (unknown=1). Ou seja: o cliente PRECISA ler o evento antes de escrever, senão o timer das 22h desliga a textura dela sem querer."
     }
    },
    {
     "nome": "Sliders contínuos de verdade, recompilando (o que ela pediu)",
     "alvo": "varios",
     "como": "Aplicar um segundo patch ao mesmo fonte que o aurora-cosmic-comp-ws.sh já compila, tocando cinco arquivos: (1) offscreen.frag ganha `uniform float night_temp;` e `uniform float paper_texture;` mais os dois blocos de corpo; (2) render/mod.rs:419-425 declara os dois UniformName::_1f; (3) render/mod.rs:1364-1381 passa os dois Uniform::new por frame; (4) config/mod.rs:151-162 acrescenta night_temp: Option<u32> e paper_texture: f32 ao ScreenFilter E os inclui no is_noop(); (5) cosmic-comp-config/src/lib.rs:106,145 ganha o campo reading_mode, e config/mod.rs:792 ganha o braço \"reading_mode\" no config_changed, que é o watcher que já existe. Bônus obrigatório: kms/surface/mod.rs:401 passa a mandar ThreadCommand::ScheduleRender. Aí um `printf '(temperature: Some(3500), texture: 0.4)' > ~/.config/cosmic/com.system76.CosmicComp/v1/reading_mode` vale na hora, e dois timers systemd fazem o agendamento.",
     "onde": "Novo ~/.config/zsh/patches/cosmic-comp-modo-leitura.patch, aplicado por cmd_compilar (aurora-cosmic-comp-ws.sh:409-418) logo depois do cosmic-comp-sem-workspace-vazio.patch. Arquivos do fonte: src/backend/render/shaders/offscreen.frag, src/backend/render/mod.rs, src/config/mod.rs, src/backend/kms/surface/mod.rs, cosmic-comp-config/src/lib.rs",
     "a_quente": true,
     "custo": "~4 min de build com o vendor quente (~30 min do zero) a cada versão nova do cosmic-comp, em cima do build que já acontece. Nenhum filtro de acessibilidade é sacrificado — greyscale e daltonismo voltam. O aurora-night-light.py fica obsoleto e deve ser aposentado, não só desligado, para não repatchar um binário que já tem os uniforms.",
     "durabilidade": "Sobrevive a reboot e a troca de tema. Um apt upgrade troca o binário, e aí vale exatamente o que já vale para o patch de workspace: o self-heal detecta pelo marcador e reinstala o artefato de /var/lib/aurora/, ou dispara o auto-build. O patch quebra se o upstream mexer no offscreen.frag ou na lista de uniforms — e nesse caso `patch --forward` falha limpo e nada é instalado (aurora-cosmic-comp-ws.sh:414-417).",
     "trilha": "render-uniforms",
     "veredito": {
      "solucao": "Trilha \"render-uniforms\": sliders contínuos de temperatura e textura no cosmic-comp, via dois uniforms novos no offscreen.frag, recompilando o mesmo fonte que o aurora-cosmic-comp-ws.sh já compila, com a chave `reading_mode` no com.system76.CosmicComp/v1 e timers para o agendamento.",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "TUDO QUE EXISTE, EXISTE. Confirmado por leitura direta em /home/vitoriamaria/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094 (a árvore da versão instalada agora, `dpkg-query` bate): offscreen.frag no caminho citado; render/mod.rs:419-425 com os dois UniformName::_1f \"invert\"/\"color_mode\"; config/mod.rs:151-162 com `ScreenFilter { inverted, color_filter }` e `is_noop()` exatamente como descrito; config/mod.rs:790 `fn config_changed`; cosmic-comp-config/src/lib.rs com CosmicCompConfig e o Default; kms/surface/mod.rs:401 `set_screen_filter` mandando `ThreadCommand::UpdateScreenFilter`. cargo 1.98.0 presente, vendor.tar presente, `make VENDOR=1 --offline --locked` viável. /var/lib/aurora tem o .aurora-ws e o .pkg-orig da versão de agora, e /usr/bin/cosmic-comp tem o marcador AURORA-COSMIC-WS-PATCH. FRONTEIRA.md:58 dá o binário do cosmic-comp à Aurora — não pisa em território do Meow.\n\nO \"A QUENTE\" É REAL, MAS NÃO ONDE A PROPOSTA APONTA. O mecanismo está provado: em kms/surface/mod.rs:1859-1940 (`postprocess_elements`) os `Uniform::new(\"invert\"/\"color_mode\")` são CONSTRUÍDOS DO ZERO a cada frame, a partir de `&self.screen_filter`. Um uniform posto ali é reenviado por frame. O cano config->tela também está provado, é o mesmo que o toggle de acessibilidade dela usa hoje: watcher -> state.update_screen_filter (state.rs:452) -> kms/mod.rs:722 -> canal -> surface/mod.rs:1460.\n\nREFUTAÇÃO PRINCIPAL — O PASSO (3) MIRA CÓDIGO MORTO NA MÁQUINA DELA. A proposta manda passar os dois `Uniform::new` em render/mod.rs:1364-1381. Grep de `render::render_output(`: os ÚNICOS chamadores são src/backend/winit.rs:57 e src/backend/x11.rs:225 — backends aninhados de desenvolvimento. A máquina dela é DRM/KMS, e o caminho KMS não passa por ali: surface/mod.rs:1279 chama `postprocess_elements`, que tem a sua PRÓPRIA lista de uniforms, em DOIS sítios (linha ~1897, o elemento do cursor; linha ~1931, a textura principal). Feito como está escrito, o patch compila, instala, sobrevive ao gate do marcador — e os sliders não fazem NADA na tela dela, porque glUniform nunca é chamado para os nomes novos e eles ficam no default 0.0 do GL. Falha silenciosa, que é o pior tipo.\n\nSEGUNDA REFUTAÇÃO — LISTA DE ARQUIVOS INCOMPLETA. config/mod.rs:394-397 (`load_filter_state`) termina com um literal `ScreenFilter { inverted: false, color_filter: None }`. Acrescentar campos ao struct quebra a compilação ali, e a proposta não lista esse ponto. Não é perigoso (erro de build = nada instalado), mas prova que os cinco arquivos foram levantados por leitura parcial.\n\nTERCEIRA — O GUARDA DE IDEMPOTÊNCIA DO BUILD NÃO CONHECE UM SEGUNDO PATCH. `compilar` (aurora-cosmic-comp-ws.sh:409-418) decide \"já aplicado\" por `grep -q \"$MARCA\" src/shell/mod.rs`, que só fala do patch de workspace. Na segunda passada com dois patches, o `patch --forward` do patch de leitura sai com status 1 (\"previously applied\"), `compilar` devolve 3, e o auto-build queima uma das 3 tentativas de /var/lib/aurora/*.build-tentado por versão. Isso adoece a auto-cura que ela acabou de consertar em 28/08.\n\nQUARTA — CUSTO NOVO QUE A PROPOSTA DIZ NÃO EXISTIR. /home/vitoriamaria/.local/state/cosmic-comp/a11y_screen_filter.ron contém hoje `(inverted: false,)` — `color_filter` é None, logo `is_noop()` é TRUE e o passe offscreen inteiro está DESLIGADO neste momento. A afirmação \"nenhum custo novo, ela já paga isso\" é falsa: ligar o modo leitura liga um render offscreen de tela cheia mais um segundo passe que hoje não roda. Numa RTX 4060 com uma TV só isso é irrelevante — mas era medida, não suposição.\n\nQUINTA — O SHADER NÃO É VALIDADO EM TEMPO DE BUILD. O GLSL entra por `include_str!`: `cargo build` embute o texto sem olhar. Um erro de sintaxe passa pelo build, passa pelo gate do marcador, é instalado, e só explode no login dela, em `init_shaders` (kms/surface/mod.rs:733, com `?`) — ou seja, sessão sem tela. glslangValidator NÃO está instalado (só disponível no apt, glslang-tools 15.1.0). Esse é o único vetor pelo qual esta proposta chega a machucar.\n\nO QUE A PROPOSTA ACERTOU E MERECE CRÉDITO: o \"bônus obrigatório\" está certo e é um bug real do upstream. `update_screen_filter` (surface/mod.rs:1460-1463) só grava o filtro e limpa `postprocess_textures` — NÃO chama `queue_redraw`. Hoje isso não aparece porque um desktop vivo sempre tem algum frame; num slider arrastado sobre tela parada, apareceria como travamento.\n\nGLSL: a proposta não escreveu o corpo, então não dá para validá-lo — mas as duas armadilhas de #version 100 são conhecidas e há solução pronta DENTRO deste repo, ver \"correcao\".",
      "risco_real": "O QUE QUEBRA: se o shader tiver erro de sintaxe, `init_shaders` falha e ela fica SEM DESKTOP — a sessão não sobe, o greeter fica em laço. É o cenário eliminatório, e é real porque nenhum passo do build valida GLSL.\n\nHÁ VOLTA, E ELA ESTÁ PRONTA. Ctrl+Alt+F2 (os /dev/tty1..6 existem; getty@tty2 está inativo mas o logind sobe autovt@tty2 na troca), login em texto, e:\n  sudo /usr/local/sbin/aurora-cosmic-comp-ws.sh --restore\n`cmd_restore` (linha 529) copia /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig — CONFERI que o arquivo existe, 28.339.864 bytes, de 26/08 — por cima de /usr/bin/cosmic-comp. Rede secundária: `sudo apt-get install --reinstall cosmic-comp`, que o próprio script imprime. Depois, reboot ou voltar ao tty1.\n\nDANO COLATERAL MENOR, RECUPERÁVEL SOZINHO: acrescentar campos ao ScreenFilter sem `#[serde(default)]` faz o a11y_screen_filter.ron atual falhar no RON; o código (config/mod.rs:386-389) avisa e APAGA o arquivo, caindo no default. Ela perde uma vez o estado de inverter/filtro. O mesmo acontece na direção inversa quando um apt upgrade devolver o binário de fábrica e ele achar campos que não conhece.\n\nO QUE NÃO É RISCO: territorialmente está limpo (FRONTEIRA.md:58 dá o binário à Aurora, e ~/.config/zsh/patches/ já é onde o patch irmão mora — o auto-commit de 10 min vai versioná-lo, que é o comportamento desejado). E um patch que não aplica falha limpo: `patch -p1 --forward --batch` devolve não-zero, `compilar` retorna 3, e nada é instalado — o desktop segue no binário do pacote.",
      "correcao": "1) MOVER OS UNIFORMS PARA O CAMINHO QUE RODA. Em src/backend/kms/surface/mod.rs, dentro de `postprocess_elements`, acrescentar os dois `Uniform::new` NOS DOIS vetores — o do cursor (~linha 1897) e o da textura principal (~linha 1931). Manter também render/mod.rs:1364-1381 por paridade com winit/x11, mas saber que ali é decorativo. Sem esta correção a proposta é inerte na máquina dela.\n\n2) NÃO FAZER KELVIN NO SHADER. Declarar `UniformName::new(\"night_tint\", UniformType::_3f)` e computar o triplo em Rust, portando a tabela que já existe em aurora-night-light.py:79 (3500 -> 1.00000, 0.77987, 0.54468). A conversão Kelvin->RGB precisa de `log()`/`pow()` por pixel; existem em GLSL ES 1.00, mas é ALU desperdiçada em tela cheia por frame e precisão instável em highp. Com _3f o shader vira uma linha: `color.rgb *= night_tint;`.\n\n3) TEXTURA: REUSAR O HASH QUE JÁ ESTÁ NO REPO. src/backend/render/shaders/clipped_surface.frag já traz uma função de ruído comprovadamente compilável em #version 100 nesta GPU (só fract/dot/vec3, sem texelFetch, sem inversão de matriz, sem laço):\n    float hash(vec2 p) { vec3 p3 = fract(vec3(p.xyx) * 727.727); p3 += dot(p3, p3.xyz + 33.33); return fract((p3.x + p3.y) * p3.z); }\ne o uso, entre o un-multiply e o re-multiply:\n    if (paper_texture > 0.0) { color.rgb += (hash(v_coords) - 0.5) * paper_texture; }\nCopiar verbatim. Ruído determinístico por coordenada, não animado — importante, senão forçaria dano de tela cheia todo frame e mataria o damage tracking.\n\n4) VALIDAR O SHADER ANTES DE INSTALAR — esta é a correção que fecha o buraco eliminatório. `sudo apt install glslang-tools` e, em `compilar`, entre o `patch` e o `make`, rodar glslangValidator sobre as variantes do offscreen.frag (substituindo //_DEFINES_ por vazio, por `#define NO_ALPHA 1`, e por `#define EXTERNAL 1`). Se qualquer variante falhar, abortar com `return 3` — mesmo caminho do patch que não aplica: nada instalado, desktop intacto.\n\n5) MARCADOR E GUARDA PRÓPRIOS PARA O SEGUNDO PATCH. Embutir uma segunda marca (p.ex. AURORA-COSMIC-READING-PATCH), testá-la com um `grep -q` próprio antes do segundo `patch --forward`, e exigi-la no `grep -aqs` do binário. Sem isso o segundo build da mesma versão sai com status 1 e come as 3 tentativas do auto-build.\n\n6) `#[serde(default)]` NOS CAMPOS NOVOS do ScreenFilter (mais `skip_serializing_if = \"Option::is_none\"` no night_temp), e corrigir o literal de config/mod.rs:394-397. Poupa o apagamento do a11y_screen_filter.ron nas duas direções.\n\n7) RESPEITAR O DPMS NO SCHEDULE. Em surface/mod.rs:401, chamar `self.schedule_render()` (linha 391, que testa `if self.dpms`) em vez de mandar `ThreadCommand::ScheduleRender` cru — senão um ajuste de slider redesenha uma tela apagada.\n\n8) ESCRITA DA CHAVE: manter o `printf ... > reading_mode` da proposta, NÃO usar `mv`. Verifiquei o watcher em vendor/cosmic-config/src/lib.rs:377-422: ele DESCARTA `EventKind::Modify(ModifyKind::Name(RenameMode::Both))`, que é justamente o evento que o inotify pode emitir num rename dentro do mesmo diretório vigiado — um `mv` pode não acordar ninguém. O `>` gera Modify(Data), que passa. Como o valor tem ~40 bytes, o write é um syscall só e não há leitura partida na prática. (Se um dia precisar de escrita atômica, o prefixo reservado é `.atomicwrite`, que o watcher pula de propósito — linha 408.)\n\n9) AGENDAMENTO: timers de USUÁRIA (systemd --user), não de sistema, para pegarem o HOME e o XDG_CONFIG_HOME certos.\n\nRESUMO DA POSIÇÃO: a arquitetura é sólida e é de fato o único jeito de ter slider contínuo — os uniforms discretos de hoje não esticam. Mas o patch como está descrito não funcionaria na tela dela, por mirar winit/x11 em vez de KMS. Com as correções 1, 4 e 5 aplicadas, eu aprovo. Sem a correção 4, eu não deixaria rodar."
     }
    },
    {
     "nome": "Agendamento por horário, isolado (vale nos dois caminhos acima)",
     "alvo": "agendamento",
     "como": "Um cliente Wayland nativo que binda cosmic_a11y_manager_v1 (versão 2, confirmada viva) e emite set_screen_filter(inverted, filter), mais dois timers systemd de usuário (OnCalendar=*-*-* 18:00 e 07:00). No caminho recompilado, é ainda mais simples: nem precisa de Wayland, basta escrever atomicamente o arquivo reading_mode da cosmic-config e o ConfigWatchSource faz o resto. Atenção a um detalhe do protocolo: o compositor só age se o valor DIFERIR do atual (a11y.rs:203-211), então reenviar o mesmo estado é no-op silencioso.",
     "onde": "~/.config/zsh/scripts/aurora-modo-leitura.sh + duas units em ~/.config/systemd/user/; cliente Wayland compilado para /usr/local/bin/",
     "a_quente": true,
     "custo": "Se for pelo protocolo Wayland, é um binário Rust novo para manter (wayland-client + cosmic-protocols no rev 160b086, o mesmo do compositor). Se for pela cosmic-config, custa zero além do script — mas depende da recompilação. O cliente precisa ser nativo: flatpak não binda o global (client_not_sandboxed).",
     "durabilidade": "Independente de apt upgrade do cosmic-comp: o protocolo é estável e versionado. Sobrevive a reboot pelos timers. Só quebra se a System76 subir a versão do cosmic_a11y_manager e remover requests — improvável, protocolos Wayland não removem.",
     "trilha": "render-uniforms",
     "veredito": {
      "solucao": "Agendamento por horário, isolado (trilha render-uniforms) — cliente Wayland nativo em cosmic_a11y_manager_v1 + timers systemd de usuário",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "CONFIRMADO NA MÁQUINA (medido agora, 29/08):\n\n1. O global existe e está vivo na versão 2. `wayland-info` (cliente CLI comum, não-sandboxed) enxergou `interface: 'cosmic_a11y_manager_v1', version: 2, name: 53` em WAYLAND_DISPLAY=wayland-1. Fonte: /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/wayland/protocols/a11y.rs:59-67 faz `dh.create_global::<..>(2, ...)`, e src/state.rs:762 passa `client_not_sandboxed` como filtro (src/state.rs:647-651). Logo \"flatpak não binda / nativo binda\" está certo, e o cliente CLI já provou que binda.\n\n2. \"Vale a quente\" está PROVADO, não suposto. src/backend/kms/mod.rs:722 `update_screen_filter()` só faz `surface.set_screen_filter(...)` e traz o comentário do próprio upstream: \"The shader is already compiled at this point and we don't rely on any features\". Não há recompilação de shader no caminho; é o uniform `color_mode` mudando entre frames. src/wayland/handlers/a11y.rs:53-63 é o handler completo.\n\n3. O detalhe do \"só age se DIFERIR\" está certo. src/wayland/protocols/a11y.rs, braço `Request::SetScreenFilter`: `if inverted != state.a11y_state().screen_inverted` e `if let Ok(filter) = filter && filter != state.a11y_state().screen_filter`. Reenviar o mesmo estado é no-op silencioso, como a proposta diz.\n\n4. É de fato a ÚNICA via roteirizável hoje. vendor/cosmic-settings-config/src/shortcuts/action.rs:155+ (enum System) não tem nenhuma ação de filtro de cor — só brilho, mudo, lançador etc. Não há atalho para script disparar.\n\n5. Os timers alcançam o socket: `systemctl --user show-environment` já traz WAYLAND_DISPLAY=wayland-1 e XDG_RUNTIME_DIR=/run/user/1000.\n\n6. Território correto. docs/FRONTEIRA.md dá \"binário cosmic-comp (patches de workspace e night light)\" e \"/usr/local/bin/* (wrappers)\" à Aurora. ~/.config/zsh/scripts é a fonte canônica dela (com auto-commit de 10 min — escrever ali VIRA commit, é esperado, mas um script do Meow não pode fazer isso: a TRAVA 1 do lib/comum.sh recusa ~/.config/zsh).\n\n7. O precedente de recompilar está VIVO agora: /usr/bin/cosmic-comp tem 33.571.456 bytes — exatamente o tamanho de /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws (o .orig do pacote tem 28.339.864). E `strings` acha 1 ocorrência de \"NIGHT LIGHT (Aurora)\" nele: é o binário recompilado E patchado, rodando.\n\nTRÊS AFIRMAÇÕES DA PROPOSTA ESTÃO ERRADAS (por isso PLAUSÍVEL, não CONFIRMADO):\n\n(a) \"basta escrever atomicamente o arquivo reading_mode da cosmic-config e o ConfigWatchSource faz o resto\" — FALSO neste build. Não existe chave `reading_mode` em lugar nenhum (grep no fonte e em ~/.config/cosmic/com.system76.CosmicComp/v1/, que tem 15 arquivos e nenhum parecido). O que existe é ~/.local/state/cosmic-comp/a11y_screen_filter.ron (src/config/mod.rs:347-350), que hoje contém só `(inverted: false,)`. Ele é lido UMA VEZ por `load_filter_state()` na construção do DynamicConfig e NUNCA é vigiado: existem exatamente 4 ConfigWatchSource no binário (mod.rs:177 com.system76.CosmicComp, :223 CosmicTk, :255 Shortcuts, :290 WindowRules) e nenhum aponta para state file. Pior: é escrito pelo PersistenceGuard do compositor, então o que você escrever é sobrescrito na próxima mudança dele.\n\n(b) \"cosmic-protocols no rev 160b086, o mesmo do compositor\" — FALSO. O Cargo.toml declara `rev = \"160b086\"` na linha 24, mas as linhas 145-146 têm `[patch.\"https://github.com/pop-os/cosmic-protocols\"] cosmic-protocols = { git = \".../cosmic-protocols\", branch = \"main\" }`, e o Cargo.lock:905 resolve para o commit 32283d76a8d0342da74c4cc022a533c52dcf378f. Um cliente pinado em 160b086 pode gerar XML diferente do que o compositor fala.\n\n(c) \"protocolos Wayland não removem [requests], improvável quebrar\" — REFUTADO pelo próprio XML vendorizado. vendor/cosmic-protocols/unstable/cosmic-a11y-unstable-v1.xml declara `<interface name=\"cosmic_a11y_manager_v1\" version=\"3\">`, com `set_screen_filter since=\"2\" deprecated-since=\"3\"`, o evento `screen_filter deprecated-since=\"3\"`, a entrada `disabled deprecated-since=\"3\"`, e o texto explícito: \"Send this request will raised a 'deprecated' protocol error, if version 3 or higher was bound\". Ou seja: a depreciação JÁ ACONTECEU no protocolo; o compositor apenas ainda anuncia 2. No dia em que um apt subir o global para 3, um cliente que faça `bind(min(3, versão_do_servidor))` e mande set_screen_filter leva erro de protocolo e é morto.\n\nITEM 3 DO CHECKLIST (GLSL #version 100): não se aplica. Esta trilha não escreve uma linha de GLSL — só troca o valor do uniform pela via oficial.",
      "correcao": "1. Enterre a via \"reading_mode / state file\". No caminho A QUENTE, use SÓ o protocolo Wayland. No caminho recompilado, se quiser uma chave de config, ela tem de nascer sob o contexto com.system76.CosmicComp/v1/ (que JÁ é vigiado em src/config/mod.rs:177) mais um braço novo em `config_changed` — nunca um arquivo em ~/.local/state, que não é vigiado e é sobrescrito pelo compositor.\n\n2. O cliente tem de pinar a versão EXATA 2 no `registry.bind(name, 2, ...)`. Nada de `min(MAX_VERSION, server)`. Ligação em versão mais baixa continua válida para sempre no Wayland; ligação \"na mais alta\" é uma bomba-relógio marcada no XML. Se um dia quiser a versão 3, o caminho é detectar >=3 e usar `set_screen_filter2` (e lembrar que `Filter::disabled` também está deprecado lá).\n\n3. Pine o cliente no MESMO commit resolvido pelo compositor: cosmic-protocols git rev 32283d76a8d0342da74c4cc022a533c52dcf378f (não 160b086), branch main do pop-os/cosmic-protocols.\n\n4. GUARDA OBRIGATÓRIA antes de disparar (isto é a correção que mais importa): o slot \"greyscale\" só é luz quente enquanto o patch da aurora-night-light.py estiver no binário. O script tem de checar `strings /usr/bin/cosmic-comp | grep -q 'NIGHT LIGHT (Aurora)'` (hoje: 1 ocorrência) e, se não achar, NÃO ligar filtro nenhum e mandar notify-send. Sem isso, um apt em que o repatch falhe (o próprio aurora-night-light.py sai com código 3 quando o bloco do shader mudou) faz a tela dela virar PRETO E BRANCO de verdade às 18:00, todo dia, sem causa visível.\n\n5. As units: `After=graphical-session.target` + `PartOf=graphical-session.target` no .service, e NADA de `Persistent=true` no timer — senão um 18:00 perdido (máquina desligada) é reproduzido às 3h da manhã. Instale-as com `WantedBy=graphical-session.target`, não `timers.target` seco.\n\n6. Nomes: não colidir com aurora-cosmic-nightlight-watch.{service,timer}, que já existem, estão ATIVOS (próximo disparo 30/08 00:32) e fazem outra coisa (vigiam a issue pop-os/cosmic-comp#2059). Use aurora-modo-leitura-liga/desliga.\n\n7. Registre no docs/FRONTEIRA.md que os timers de modo de leitura são da Aurora, na mesma linha do patch de night light. E note que aurora-cosmic-comp-ws.sh NÃO está em /usr/local/bin hoje (só em ~/.config/zsh/scripts) — não escreva um script que o chame por PATH.",
      "risco_real": "BAIXO — e é justamente o que separa esta peça das outras duas da trilha.\n\nO que pode quebrar: nada do compositor. Um cliente Wayland que erra o protocolo é morto SOZINHO pelo wayland-server; o cosmic-comp não cai junto. Não há escrita em binário, não há sudo no caminho de execução, não há reboot, não há recompilação. O pior caso realista é a tela dela mudar de cor na hora errada — ou, sem a guarda do item 4, ficar em escala de cinza de verdade.\n\nEla NUNCA fica sem desktop por causa desta peça. Três voltas, todas disponíveis com a sessão de pé:\n- Pela mão dela: Configurações > Acessibilidade > Filtros de cor, desligar. É o mesmo estado que o cliente escreve.\n- Por comando: `systemctl --user disable --now aurora-modo-leitura-liga.timer aurora-modo-leitura-desliga.timer` (usuário, sem sudo).\n- Se nada disso, TTY: Ctrl+Alt+F3 continua íntegro, porque nada aqui toca no binário nem no arranque da sessão.\n\nO risco herdado (não criado por esta peça) é o do patch de shader embaixo: apt upgrade troca /usr/bin/cosmic-comp e devolve o greyscale de fábrica até o self-heal repatchar pelo hook 99-ritual-aurora-self-heal. A guarda do item 4 transforma esse risco herdado em \"o agendamento não faz nada e avisa\", em vez de \"a tela dela fica preto e branco\"."
     }
    }
   ]
  },
  {
   "trilha": "glsl-textura",
   "achados": {
    "trilha": "GLSL do efeito \"Textura\" (papel/Kindle) dentro do offscreen.frag do cosmic-comp",
    "resumo": "O \"Textura\" do MIUI não é halftone nem dither: todas as fontes que abri descrevem a mesma coisa — um grão de papel sobreposto à tela mais dessaturação e compressão de contraste, com o preto virando tinta e o branco virando papel amarelado. Não achei engenharia reversa do código da Xiaomi (o FAQ oficial devolve 403), então o que entrego é RECONSTRUÇÃO, não reprodução — mas achei um precedente forte e testado em produção: o `reading_mode.glsl` do Hyprland (@snes19xx), que faz exatamente isso e roda como shader de tela em três dotfiles públicos. Escrevi o bloco autoral e COMPILEI ele no driver real desta máquina (NVIDIA 580.173.02) e no Mesa, nas 8 combinações de #define que o smithay compila no boot — todas passam. Medi o custo com um quad de tela cheia 3840x2160 em FBO: +0,045 ms por quadro, ou 0,27% de um quadro de 60 Hz, e literalmente zero quando o uniform é 0. Duas crenças caíram na medição: o hash \"sem seno\" é 1,5x a 2x MAIS CARO que o hash com seno nesta GPU, e o seno não perdeu precisão nem no canto (3400,1800) de 4K. E achei o limite duro do caminho barato: nenhum valor contínuo chega ao shader sem recompilar, porque os únicos uniforms vivos são discretos.",
    "achados": [
     {
      "titulo": "O \"Textura\" do MIUI é grão + dessaturação, não halftone nem dither",
      "detalhe": "Quatro fontes independentes convergem: grão sobreposto que quebra a lisura do painel (\"breaks the artificial and razor-sharp appearance of OLED panels\"), textura de papel por software \"much like TCL's NXTPAPER\", e um seletor de cor separado (Full colors / Light colors / Black and white) que dessatura. NENHUMA fonte menciona halftone ou dithering ordenado. O slider de Textura só regula a presença do grão (\"users can reduce or increase the texture presence on the screen by adjusting it on the slider\"). Importante para o desenho: a captura dela mostra \"Cores: Cores totais\", ou seja, ela quer a COR PRESERVADA — o que descarta as implementações monocromáticas.",
      "evidencia": "https://www.xda-developers.com/xiaomi-is-testing-an-enhanced-reading-mode-with-a-paper-color-mode/ (engenharia reversa da beta do MIUI 12) + https://www.androidauthority.com/my-favorite-display-feature-for-night-use-3582060/ + https://www.androidcentral.com/phones/xiaomi/this-feature-on-the-xiaomi-17-ultra-convinced-me-to-use-the-phone-as-my-daily-driver + https://goodereader.com/blog/electronic-readers/how-to-enable-paper-reading-mode-in-xiaomi-phones",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "NÃO existe documentação da implementação da Xiaomi — o que entrego é reconstrução",
      "detalhe": "O FAQ oficial (mi.com/uk/support/faq/details/KA-171506) devolve HTTP 403. As buscas por engenharia reversa do overlay só acham a matéria da XDA de 2020, que diz explicitamente que \"the feature isn't working as of now\" e que as referências foram achadas no ROM beta — sem detalhe de como a textura é aplicada. Portanto: nenhum shader da Xiaomi foi obtido. O bloco que escrevi reproduz o EFEITO VISÍVEL descrito nas fontes e visível na captura dela, não o código dela.",
      "evidencia": "WebFetch de https://www.mi.com/uk/support/faq/details/KA-171506/ -> HTTP 403; WebFetch da matéria XDA -> \"The article does not specify technical details about *how* the texture is applied mechanically.\"",
      "confianca": "medido"
     },
     {
      "titulo": "Existe um precedente testado em produção: reading_mode.glsl do Hyprland",
      "detalhe": "Shader de tela por @snes19xx, presente idêntico em três dotfiles públicos. Faz: hash12 multi-oitava para fibra de papel, grão direcional, Bayer 4x4, curva de resposta e-ink (pow 1.2 + smoothstep 0.08..0.92), vinheta, e mistura final mix(inkColor=vec3(0.10,0.10,0.12), paperColor=vec3(0.94,0.92,0.86), gray). O comentário do autor: \"It works brilliantly - looks like real paper, killed my eye strain\". É a melhor referência que existe para o alvo dela.",
      "evidencia": "https://github.com/mofumail/.dotfiles/blob/main/hypr/shaders/reading_mode.glsl (idem em joelsleeba/dotfiles e tirbofish/dotfiles, mesmo conteúdo byte a byte)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O reading_mode.glsl NÃO cola direto: `texture()` não existe em #version 100",
      "detalhe": "Compilei os pedaços dele sob #version 100 nos dois drivers. Quebra só num ponto: `texture(tex, uv)` -> erro C7532 no NVIDIA (\"global function texture requires #version 130 or later\") e \"no function with name 'texture'\" no Mesa; a troca é `texture2D`. Tudo o mais sobrevive: `pow`, `smoothstep`, `mix`, `length`, e — surpresa — até a indexação DINÂMICA da `const mat4 bayer[x][y]`, que os dois compiladores aceitam apesar de a ES 1.00 só garantir constant-index-expression. O bloqueio real é outro: ele converte tudo para cinza, o que contraria o \"Cores totais\" dela.",
      "evidencia": "port_bayer.frag / port_tex.frag / port_ok.frag no scratchpad; saída dos dois compiladores registrada",
      "confianca": "medido"
     },
     {
      "titulo": "O offscreen.frag é UMA passada de tela cheia por monitor, e só existe quando há filtro ligado",
      "detalhe": "POSTPROCESS_SHADER = include_str!(\"./shaders/offscreen.frag\") em src/backend/render/mod.rs:131. Ele é aplicado como um único TextureRenderElement do tamanho do output (linha ~1343), ou seja, exatamente um fragmento por pixel de tela por quadro. E a passada inteira é pulada quando o filtro é no-op: `let result = if !screen_filter.filter.is_noop()` (linha 1284), com `is_noop() = !self.inverted && self.color_filter.is_none()` (src/config/mod.rs:159). Consequência prática: com o papel desligado o custo não é \"pequeno\", é ZERO — nem o buffer offscreen é alocado.",
      "evidencia": "src/backend/render/mod.rs linhas 131, 1284, 1343-1382; src/config/mod.rs linhas 152-162 (pop-os/cosmic-comp@master)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Custo medido: +0,045 ms/quadro em 4K = 0,27% de um quadro de 60 Hz",
      "detalhe": "Quad de tela cheia 3840x2160 num FBO, 300-400 passadas, glFinish, três rodadas. Base (upstream+aurora): 0,072 ms. Com o bloco papel a 100%: 0,117 ms. Com o bloco COMPILADO mas uniform=0: 0,072 ms (diferença dentro do ruído, -1,1% a +1,0%) — o branch é coerente na tela inteira, então o desligado é de graça. Escala de oitavas (hash com seno): 1 oitava +0,019 ms, 2 oitavas +0,041 ms, 3 oitavas +0,074 ms. O porte completo do reading_mode.glsl (9 hashes + Bayer + vinheta) custa +0,292 ms = 1,75% de um quadro de 60 Hz, 6,5x o meu.",
      "evidencia": "bench_prelude.py + bench2.py/bench3.py/bench4.py no scratchpad, rodando em EGL_PLATFORM_DEVICE_EXT na NVIDIA GeForce RTX 4060 / driver 580.173.02",
      "confianca": "medido"
     },
     {
      "titulo": "O baseline bate exatamente com o fillrate da 4060 — o número é confiável",
      "detalhe": "114-117 GPix/s medidos na passada base. A RTX 4060 tem 48 ROPs a ~2,5 GHz = ~120 GPix/s teóricos. Ou seja, a passada base está limitada por ROP (é só ler+escrever), e a versão com papel cai para 56 GPix/s porque passa a ser limitada por ALU. A história fecha, o que dá confiança de que o teste não está medindo outra coisa.",
      "evidencia": "saída do bench.py: base 114,72/117,15/115,13 GPix/s; papel 56,14/56,18/56,24 GPix/s",
      "confianca": "medido"
     },
     {
      "titulo": "O shader compila nas 8 combinações de #define que o smithay exige — e isso não é opcional",
      "detalhe": "smithay faz `src.replace(\"//_DEFINES_\", ...)` e compila TODAS as variantes de EXTERNAL x NO_ALPHA x DEBUG_FLAGS no arranque (src/backend/renderer/gles/shaders/mod.rs:124-142, com um debug_program extra por variante). Se QUALQUER uma falhar, `init_shaders` devolve Err e o cosmic-comp NÃO SOBE — não é degradação, é sessão que não abre. Testei as 8 no NVIDIA para o bloco final e para o fallback: todas OK. O marcador `//_DEFINES_` tem de continuar no texto (o doc do smithay escreve `//_DEFINES` sem underscore final, mas o código faz replace de `//_DEFINES_`).",
      "evidencia": "https://raw.githubusercontent.com/Smithay/smithay/master/src/backend/renderer/gles/shaders/mod.rs linhas 117-142; bateria das 8 combinações no scratchpad",
      "confianca": "medido"
     },
     {
      "titulo": "O grão é estável entre quadros SEM truque, porque não depende de tempo",
      "detalhe": "A técnica é: ancorar em `gl_FragCoord.xy` (coordenada de PIXEL do framebuffer de destino) e não usar nenhum termo temporal. gl_FragCoord existe em GLSL ES 1.00 (compilei para confirmar). Três propriedades caem de graça: (1) não chuvisca, porque o valor de um pixel é função pura da posição dele; (2) não anda quando a janela anda — v_coords andaria, gl_FragCoord não; (3) sobrevive ao damage tracking, porque a região redesenhada recalcula exatamente os mesmos valores. Se em vez disso o grão usasse tempo ou contador de quadro, o damage tracking faria a região redesenhada divergir da vizinha e apareceria uma costura visível.",
      "evidencia": "pos1.frag compila OK nos dois drivers; sinless_zoom.png e sinless_3400_1800.png renderizados e inspecionados",
      "confianca": "medido"
     },
     {
      "titulo": "Não há tiling nem faixa no grão, medido por autocorrelação em 4K",
      "detalhe": "Renderei cinza 0.60 chapado a 3840x2160 e li de volta recortes de 400x300 em (0,0) e em (3400,1800) — o canto onde a precisão fp32 mais sofre. Autocorrelação máxima da linha para deslocamentos de 8 a 512 px: |r| <= 0,155 em todos os casos, nos dois drivers, com os dois hashes. Desvio padrão idêntico nos dois cantos (3,15 vs 3,15 níveis de 255). Ou seja: sem período visível, sem degradação no canto.",
      "evidencia": "grain.py no scratchpad; PNGs sinless_zoom.png, sine_zoom.png, sinless_3400_1800.png, mesa_*.png",
      "confianca": "medido"
     },
     {
      "titulo": "Nenhum valor CONTÍNUO chega ao shader hoje — o slider exige recompilar",
      "detalhe": "Os únicos uniforms que o cosmic-comp alimenta no offscreen.frag são `invert` (0/1) e `color_mode` (0..4, vindo de `ColorFilter as u8 as f32`), mais `alpha` que está fixo em 1.0 (`from_texture_render_buffer(..., Some(1.0), ...)`). Um uniform novo declarado só no TEXTO do shader ficaria eternamente em 0.0, porque nada em Rust o define. Portanto: patch de texto não dá slider. Dá, no máximo, 4 degraus vivos amarrados ao dropdown de filtros de cor.",
      "evidencia": "src/backend/render/mod.rs linhas 416-421 (UniformName) e 1362-1381 (Uniform::new); src/config/mod.rs:164-172 (ColorFilter repr(u8) = 1..4)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Sobram 1.345 bytes no texto embutido — o fallback sem recompilar CABE folgado",
      "detalhe": "O offscreen.frag embutido no binário tem 2.415 bytes de texto (idêntico no binário vivo e no pkg-orig). O bloco do color_mode==1.0 que o aurora-night-light.py reescreve tem 195 bytes. Minificando o shader inteiro de forma conservadora sobram 853 bytes; reescrevendo o shader inteiro em forma compacta (que é o que fiz) o resultado tem 1.099 bytes, deixando 1.345 bytes de padding para preservar o tamanho do binário byte a byte, como o aurora-night-light.py já faz. Ou seja: cabe o efeito papel inteiro sem tocar em offset nenhum do ELF.",
      "evidencia": "medição direta em /usr/bin/cosmic-comp e /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig; frio2.frag = 1.099 bytes",
      "confianca": "medido"
     },
     {
      "titulo": "O caminho VIVO do filtro é o protocolo Wayland, não o arquivo .ron",
      "detalhe": "ScreenFilter mora em `~/.local/state/cosmic-comp/a11y_screen_filter.ron` (xdg.place_state_file), lido UMA VEZ no arranque e escrito pelo compositor quando muda. Quem aplica a quente é o protocolo `cosmic-a11y-manager`: Request::SetScreenFilter -> request_screen_filter() -> backend.update_screen_filter(). Consequência: editar o .ron na mão NÃO aplica a quente, só muda o que valerá no próximo login. Qualquer slider vivo tem de falar o protocolo.",
      "evidencia": "src/config/mod.rs:347-350 e 377-398; src/wayland/handlers/a11y.rs (request_screen_filter); src/wayland/protocols/a11y.rs:196-212",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Existe um canal vivo contínuo de graça: alargar o ColorFilter no protocolo",
      "detalhe": "O handler faz `let filter = protocol_to_color_filter(filter); if let Ok(filter) = filter && filter != atual { state.request_screen_filter(filter) }`. O valor vem do cliente como um enum serializado em u32 e é validado por uma função DO PRÓPRIO cosmic-comp. Como o recompile já vai acontecer, dá para fazer `protocol_to_color_filter` aceitar 5..105 e mapear para intensidade de papel 0..100%. Resultado: slider contínuo, a quente, sem mudar o protocolo e sem mexer no cosmic-settings — só um cliente Wayland pequeno do lado do MeowSystem para mandar o valor.",
      "evidencia": "src/wayland/protocols/a11y.rs:196-212 (pop-os/cosmic-comp@master)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O grão dobra como dither: reduz banding em vez de criar",
      "detalhe": "A amplitude escolhida (sigma ~= 0,016 no papel, ~4 níveis de 255) é da mesma ordem do passo de quantização de 8 bits. Isso significa que o grão funciona como ruído de dither sobre gradientes, quebrando banding em vez de piorar. Efeito colateral bom, mas tem contrapartida: ruído estático por pixel é o pior caso para codificador de vídeo — gravação de tela, OBS e área de trabalho remota vão gastar bem mais bitrate com o papel ligado.",
      "evidencia": "look_t100_o2.png e look_t100_o3.png (faixa de gradiente na base da imagem sai lisa); amplitude derivada de 0.085*t com g de distribuição triangular (sigma=0,204)",
      "confianca": "inferido"
     }
    ],
    "codigo": [
     {
      "para_que": "BLOCO FINAL AUTORAL — cole em src/backend/render/shaders/offscreen.frag. São duas inserções: (1) o uniform + o hash, logo depois de `uniform float color_mode;`, ANTES do `void main()`; (2) o bloco `if (paper_texture > 0.0)` dentro do main, depois do bloco do `invert` e ANTES do `if (color_mode == 1.0)`. Compilado OK nas 8 combinações de #define do smithay, no NVIDIA 580.173.02 e no Mesa 26.1.6.",
      "linguagem": "glsl",
      "fonte": "autoral",
      "trecho": "// ============================================================================\n// INSERÇÃO 1 — depois de `uniform float color_mode;`, fora do main()\n// ============================================================================\n\n// >>> MeowSystem: textura de papel (modo de leitura). 0.0 = desligado, 1.0 = máximo.\nuniform float paper_texture;\n\n// Hash pseudoaleatório de 1 saída a partir da coordenada de PIXEL da tela.\n// Sem tempo e sem uniforms: para uma dada coordenada o valor é SEMPRE o mesmo,\n// então o grão fica cravado na tela e não chuvisca entre frames -- e sobrevive\n// ao damage tracking, porque a região redesenhada recalcula os mesmos valores.\n//\n// Por que com seno: medido nesta máquina, sin() é 1,5x a 2x MAIS BARATO que o\n// hash \"sem seno\" do David Hoskins, porque na NVIDIA o seno é uma instrução da\n// SFU e o hash sem seno são ~8 ALUs que disputam o mesmo caminho do resto.\n// A precisão fp32 aguenta: testado no canto (3400,1800) de 4K, sem faixa nem\n// repetição. Se um dia isto rodar em GPU móvel (mediump), trocar por:\n//   vec3 p3 = fract(vec3(p.xyx) * vec3(0.1031, 0.1030, 0.0973));\n//   p3 += dot(p3, p3.yzx + 33.33);\n//   return fract((p3.x + p3.y) * p3.z);\nfloat meow_hash(vec2 p) {\n    return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n}\n// <<<\n\n\n// ============================================================================\n// INSERÇÃO 2 — dentro do main(), depois do `if (invert == 1.0) {...}`\n//              e ANTES do `if (color_mode == 1.0) {...}`\n// ============================================================================\n\n    // >>> MeowSystem: papel/Kindle. Vem ANTES do color_mode de propósito, para\n    // que a luz noturna também amorne o branco do papel (senão o papel sai frio\n    // e o resto quente). Custa zero medido quando paper_texture == 0.0.\n    if (paper_texture > 0.0) {\n        float t = clamp(paper_texture, 0.0, 1.0);\n\n        // 1) Faixa dinâmica de PAPEL, não de tela: o preto vira tinta (~0.12,\n        //    nunca 0) e o branco vira papel levemente amarelado (~0.93). É uma\n        //    compressão de níveis, e é ela que tira o \"brilho de OLED\".\n        vec3 eink = vec3(0.118, 0.113, 0.104)\n                  + vec3(0.815, 0.800, 0.760) * color.rgb;\n\n        // 2) Dessatura 30%: papel colorido nunca satura como um painel.\n        //    Mantém a cor (o \"Cores totais\" do MIUI), só tira o exagero.\n        float lum = dot(eink, vec3(0.2126, 0.7152, 0.0722));\n        eink = mix(eink, vec3(lum), 0.30);\n\n        color.rgb = mix(color.rgb, eink, t);\n\n        // 3) Grão. gl_FragCoord.xy é a coordenada de PIXEL do monitor: não anda\n        //    quando a janela anda (v_coords andaria), não anda entre frames.\n        //    Duas oitavas: uma fina (poro) e uma grossa a ~1/3 de escala (fibra).\n        //    Uma oitava só já funciona e custa metade; três dão fibra mais rica\n        //    e custam +0,074 ms -- ainda barato, é escolha de gosto.\n        vec2 pix = floor(gl_FragCoord.xy);\n        float g = (meow_hash(pix) + meow_hash(floor(pix * 0.34) + 11.0)) * 0.5 - 0.5;\n\n        // 4) Multiplicativo, não aditivo: papel modula a REFLETÂNCIA. Assim o\n        //    grão aparece no branco e some na tinta, que é o comportamento real\n        //    -- e o texto não serrilha. Amplitude ~4 níveis de 255 no papel,\n        //    o que de quebra funciona como dither e mata banding.\n        color.rgb *= 1.0 + g * (0.085 * t);\n        color.rgb = clamp(color.rgb, 0.0, 1.0);\n    }\n    // <<<"
     },
     {
      "para_que": "As 3 edições em Rust que fazem o uniform existir de fato. Sem elas o `paper_texture` fica preso em 0.0 para sempre.",
      "linguagem": "rust",
      "fonte": "autoral, sobre pop-os/cosmic-comp@master",
      "trecho": "// ---- src/config/mod.rs, linha ~152 ------------------------------------\n pub struct ScreenFilter {\n     pub inverted: bool,\n     #[serde(skip_serializing_if = \"Option::is_none\")]\n     pub color_filter: Option<ColorFilter>,\n+    // MeowSystem: 0.0 = sem papel, 1.0 = papel no máximo.\n+    #[serde(default)]\n+    pub paper_texture: f32,\n }\n\n impl ScreenFilter {\n     pub fn is_noop(&self) -> bool {\n-        !self.inverted && self.color_filter.is_none()\n+        // CRÍTICO: sem isto a passada de postprocess é pulada e o papel\n+        // simplesmente não aparece, mesmo com o uniform certo.\n+        !self.inverted && self.color_filter.is_none() && self.paper_texture <= 0.0\n     }\n }\n\n// ---- src/config/mod.rs, linha ~394 (load_filter_state, o default) ------\n         ScreenFilter {\n             inverted: false,\n             color_filter: None,\n+            paper_texture: 0.0,\n         }\n\n// ---- src/backend/render/mod.rs, linha ~416 (registro do uniform) ------\n     let postprocess_shader = renderer.compile_custom_texture_shader(\n         POSTPROCESS_SHADER,\n         &[\n             UniformName::new(\"invert\", UniformType::_1f),\n             UniformName::new(\"color_mode\", UniformType::_1f),\n+            UniformName::new(\"paper_texture\", UniformType::_1f),\n         ],\n     )?;\n\n// ---- src/backend/render/mod.rs, linha ~1362 (envio por frame) ---------\n                     vec![\n                         Uniform::new(\"invert\", if screen_filter.filter.inverted { 1. } else { 0. }),\n                         Uniform::new(\n                             \"color_mode\",\n                             screen_filter.filter.color_filter\n                                 .map(|val| val as u8 as f32).unwrap_or(0.),\n                         ),\n+                        Uniform::new(\"paper_texture\", screen_filter.filter.paper_texture),\n                     ],"
     },
     {
      "para_que": "FALLBACK SEM RECOMPILAR — offscreen.frag inteiro, compacto, 1.099 bytes num orçamento de 2.415, para o aurora-night-light.py gravar preservando o tamanho do binário byte a byte. Dá 4 degraus de papel VIVOS pelo dropdown \"Acessibilidade > Filtros de cor\", sem tocar em Rust. Preço: os filtros de daltonismo somem enquanto o patch estiver aplicado. Compilado OK nas 8 combinações de #define, nos dois drivers.",
      "linguagem": "glsl",
      "fonte": "autoral",
      "trecho": "#version 100\n//_DEFINES_\n#if defined(EXTERNAL)\n#extension GL_OES_EGL_image_external : require\n#endif\nprecision highp float;\n#if defined(EXTERNAL)\nuniform samplerExternalOES tex;\n#else\nuniform sampler2D tex;\n#endif\nuniform float alpha;\nvarying vec2 v_coords;\n#if defined(DEBUG_FLAGS)\nuniform float tint;\n#endif\nuniform float invert;\nuniform float color_mode;\nfloat mh(vec2 p){return fract(sin(dot(p,vec2(12.9898,78.233)))*43758.5453);}\nvoid main(){\nvec4 color=texture2D(tex,v_coords);\n#if defined(NO_ALPHA)\ncolor=vec4(color.rgb,1.0)*alpha;\n#else\ncolor=color*alpha;\n#endif\ncolor.rgb/=color.a;\nif(invert==1.0){color.rgb=1.0-color.rgb;}\nfloat t=clamp((color_mode-1.0)*0.3333,0.0,1.0);\nif(t>0.0){\nvec3 e=vec3(0.118,0.113,0.104)+vec3(0.815,0.800,0.760)*color.rgb;\ne=mix(e,vec3(dot(e,vec3(0.2126,0.7152,0.0722))),0.30);\ncolor.rgb=mix(color.rgb,e,t);\nvec2 pix=floor(gl_FragCoord.xy);\nfloat g=(mh(pix)+mh(floor(pix*0.34)+11.0))*0.5-0.5;\ncolor.rgb*=1.0+g*(0.085*t);\ncolor.rgb=clamp(color.rgb,0.0,1.0);\n}\nif(color_mode>=1.0){color.rgb*=vec3(1.00000,0.77987,0.54468);}\ncolor.rgb*=color.a;\ngl_FragColor=color;\n}\n\n// MAPA DOS DEGRAUS (o que ela vê no dropdown de Filtros de cor):\n//   Escala de cinza  -> color_mode 1 -> papel   0%  + luz noturna  (= hoje)\n//   Protanopia       -> color_mode 2 -> papel  33%  + luz noturna\n//   Deuteranopia     -> color_mode 3 -> papel  67%  + luz noturna\n//   Tritanopia       -> color_mode 4 -> papel 100%  + luz noturna\n// Depois de gravar, o patcher precisa preencher com ESPAÇOS até 2.415 bytes\n// (o padding pode ir no fim, depois do último '}'), exatamente como o\n// aurora-night-light.py já faz com o bloco de 195 bytes."
     },
     {
      "para_que": "(a) GRÃO DE PAPEL — referência real, CC0. É o hash de seno canônico do Shadertoy no formato mais curto que existe. Base do meu meow_hash.",
      "linguagem": "glsl",
      "fonte": "https://godotshaders.com/shader/film-grain-shader/ (CC0) — o mesmo hash aparece em https://gist.github.com/patriciogonzalezvivo/670c22f3966e662d2f83",
      "trecho": "// Godot Shaders — Film Grain (CC0)\nuniform float grain_amount : hint_range(0.0, 1.0) = 0.05;\nuniform float grain_size   : hint_range(0.1, 10.0) = 1.0;\n\nvoid fragment() {\n    vec4 original_color = texture(screen_texture, SCREEN_UV);\n    // hash canônico: fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453)\n    float noise = (fract(sin(dot(UV, vec2(12.9898, 78.233))) * 43758.5453) - 0.5) * 2.0;\n    original_color.rgb += noise * grain_amount * grain_size;\n    COLOR = clamp(original_color, 0.0, 1.0);\n}\n\n// ARMADILHA que este exemplo tem e o meu bloco NÃO tem: ele hasheia UV\n// (0..1 da superfície). Num compositor isso faz o grão ANDAR JUNTO com a\n// janela. Tem de ser gl_FragCoord.xy, coordenada de pixel da tela."
     },
     {
      "para_que": "(a-bis) GRÃO SEM SENO — a alternativa portátil (David Hoskins, MIT), com as constantes de escala corretas para coordenada grande. Guarde para GPU móvel/mediump; nesta máquina ela é MAIS CARA (medido).",
      "linguagem": "glsl",
      "fonte": "https://github.com/patriciogonzalezvivo/lygia/blob/main/generative/random.glsl (MIT, © 2014 David Hoskins) — origem em https://www.shadertoy.com/view/4djSRW",
      "trecho": "// LYGIA generative/random.glsl, ramo RANDOM_SINLESS.\n// RANDOM_SCALE = vec4(.1031, .1030, .0973, .1099)  <-- a variante\n// RANDOM_HIGHER_RANGE, que é a CERTA para coordenada de tela (0..3840):\n// multiplicar por um número PEQUENO antes do fract mantém o valor abaixo de\n// 1 e preserva mantissa. A escala default (443.897, 441.423, ...) é só para\n// entrada já normalizada em 0..1 e vai borrar em 4K.\nfloat random(in vec2 st) {\n    vec3 p3 = fract(vec3(st.xyx) * vec3(.1031, .1030, .0973));\n    p3 += dot(p3, p3.yzx + 33.33);\n    return fract((p3.x + p3.y) * p3.z);\n}\n\n// MEDIDO nesta máquina (RTX 4060, 3840x2160, custo por quadro sobre a base):\n//   sem seno: 1 oitava +0,040 ms | 2 oitavas +0,099 ms | 3 oitavas +0,136 ms\n//   com seno: 1 oitava +0,019 ms | 2 oitavas +0,041 ms | 3 oitavas +0,074 ms\n// O seno ganha em TODAS as contagens de oitava. E nenhum dos dois degradou\n// no canto (3400,1800): desvio idêntico, autocorrelação |r| <= 0,155."
     },
     {
      "para_que": "(b) E-INK / PAPER WHITE + (d) DITHERING — a referência mais completa e testada em produção que existe para este efeito exato: shader de tela do Hyprland. Reproduzo os pedaços que importam e marco o que quebra em #version 100.",
      "linguagem": "glsl",
      "fonte": "https://github.com/mofumail/.dotfiles/blob/main/hypr/shaders/reading_mode.glsl (por @snes19xx; cópias idênticas em joelsleeba/dotfiles e tirbofish/dotfiles)",
      "trecho": "#version 320 es   // <-- QUEBRA: precisa virar #version 100, in/out -> varying,\n                  //     e texture() -> texture2D() (erro C7532 no NVIDIA,\n                  //     \"no function with name 'texture'\" no Mesa)\n\n// --- Bayer 4x4. A indexação DINÂMICA b[x][y] compila em #version 100 nos\n//     dois drivers desta máquina, apesar de a ES 1.00 só garantir índice\n//     constante. Ou seja: portável na prática, frágil no papel.\nfloat getBayer(vec2 pos) {\n    int x = int(mod(pos.x, 4.0));\n    int y = int(mod(pos.y, 4.0));\n    const mat4 bayer = mat4( 0.0, 12.0,  3.0, 15.0,\n                             8.0,  4.0, 11.0,  7.0,\n                             2.0, 14.0,  1.0, 13.0,\n                            10.0,  6.0,  9.0,  5.0);\n    return bayer[x][y] / 16.0;\n}\n\n// --- Fibra de papel em 5 oitavas (aqui mora o custo: 5 hashes)\nfloat paperTexture(vec2 uv) {\n    float n = 0.0;\n    n += hash(uv *  0.3) * 0.6;   // fibras muito grandes\n    n += hash(uv *  0.8) * 0.4;   // fibras grandes\n    n += hash(uv *  2.5) * 0.3;   // detalhe médio\n    n += hash(uv *  6.0) * 0.2;   // grão fino\n    n += hash(uv * 15.0) * 0.1;   // grão muito fino\n    return n / 1.6;\n}\n\n// --- Grão direcional: simula a fibra correndo numa direção (+2 hashes)\nfloat directionalGrain(vec2 uv) {\n    vec2 direction = vec2(0.7, 0.3);\n    float grain = 0.0;\n    grain += hash(uv * 3.0 + direction * 2.0) * 0.5;\n    grain += hash(uv * 8.0 + direction * 5.0) * 0.3;\n    return grain / 0.8;\n}\n\n// --- A curva de resposta e-ink e a paleta tinta/papel (o miolo do efeito)\nvoid main() {\n    float gray = dot(pixColor.rgb, vec3(0.299, 0.587, 0.114));\n    gray = pow(gray, 1.2);                    // e-ink não é linear\n    gray = smoothstep(0.08, 0.92, gray);      // curva S: corta preto/branco puros\n    float midBoost = smoothstep(0.3,0.5,gray) * (1.0 - smoothstep(0.5,0.7,gray));\n    gray += midBoost * 0.1;\n\n    vec2 screenPos = gl_FragCoord.xy;         // <-- ancorado em pixel de tela\n    float textureMask = smoothstep(0.5, 0.95, gray);   // grão só no papel\n    gray += (paperTexture(screenPos * 0.3) - 0.5) * 0.035 * textureMask;\n    gray += (directionalGrain(screenPos * 0.4) - 0.5) * 0.025 * textureMask * 0.7;\n    gray += (getBayer(screenPos) - 0.5) * 0.025 * textureMask;\n\n    vec3 paperColor = vec3(0.94, 0.92, 0.86);\n    vec3 inkColor   = vec3(0.10, 0.10, 0.12);\n    gl_FragColor = vec4(mix(inkColor, paperColor, clamp(gray,0.0,1.0)), pixColor.a);\n}\n\n// POR QUE NÃO USEI ELE DIRETO, com números:\n//  - joga TUDO para cinza. A captura dela diz \"Cores: Cores totais\".\n//  - portado para ES 1.00 e medido aqui: +0,292 ms/quadro em 4K = 1,75% de\n//    um quadro de 60 Hz, contra +0,045 ms (0,27%) do meu bloco. 6,5x.\n//  - a vinheta usa v_coords, que num compositor é a superfície, não a tela."
     },
     {
      "para_que": "(c) SÉPIA / reading mode + filtro de luz azul — a família de shaders de tela do Hyprland, em GLSL ES. Útil como referência do que a comunidade Wayland já faz e de como NÃO fazer temperatura (este exemplo mexe em canais na mão em vez de usar whitepoint de corpo negro, que é o que o aurora-night-light.py já faz certo).",
      "linguagem": "glsl",
      "fonte": "https://github.com/sijan-dev/HyprShades/blob/main/blue-light-filter.glsl",
      "trecho": "#version 300 es\nprecision highp float;\nin vec2 v_texcoord;\nuniform sampler2D tex;\nout vec4 fragColor;\n\nconst float WARMTH    = 0.7;   // 0.0 = sem filtro, 1.0 = máximo\nconst float INTENSITY = 0.85;\n\nvoid main() {\n    vec4 color = texture(tex, v_texcoord);\n    color.b *= 1.0 - (WARMTH * 0.7);   // derruba azul\n    color.g *= 1.0 - (WARMTH * 0.3);   // derruba verde um pouco\n    color.r *= 1.0 + (WARMTH * 0.1);   // levanta vermelho\n    color.rgb *= INTENSITY;\n    fragColor = clamp(color, 0.0, 1.0);\n}\n\n// COMPARAÇÃO: o aurora-night-light.py já faz melhor que isto -- ele usa a\n// tabela de whitepoint do redshift/gammastep (colorramp.c), que é corpo\n// negro de verdade. 3500K = vec3(1.00000, 0.77987, 0.54468). Não trocar\n// o que já existe por esta aproximação de três canais."
     },
     {
      "para_que": "(d) DITHERING ORDENADO — Bayer 8x8 sem array dinâmico, na forma que a LYGIA usa justamente para plataformas que não aceitam índice dinâmico. É o caminho seguro se um dia quiser dither de verdade em #version 100.",
      "linguagem": "glsl",
      "fonte": "https://github.com/patriciogonzalezvivo/lygia/blob/main/color/dither/bayer.glsl (Prosperity/Patron License — checar antes de usar) e a matriz 4x4 em https://github.com/niccolofanton/dithering-shader/blob/main/src/dithering-shader/DitheringShader.ts (crédito a https://www.shadertoy.com/view/ltSSzW)",
      "trecho": "// Forma SEGURA em ES 1.00: cadeia de mix/step, zero indexação de array.\n// (LYGIA usa exatamente isto no ramo PLATFORM_WEBGL, pelo mesmo motivo.)\nfloat ditherBayer4(vec2 xy) {\n    float x = mod(xy.x, 4.0);\n    float y = mod(xy.y, 4.0);\n    float a = mix(mix( 0.0, 12.0, step(1.0,y)), mix( 3.0, 15.0, step(3.0,y)), step(2.0,y));\n    float b = mix(mix( 8.0,  4.0, step(1.0,y)), mix(11.0,  7.0, step(3.0,y)), step(2.0,y));\n    float c = mix(mix( 2.0, 14.0, step(1.0,y)), mix( 1.0, 13.0, step(3.0,y)), step(2.0,y));\n    float d = mix(mix(10.0,  6.0, step(1.0,y)), mix( 9.0,  5.0, step(3.0,y)), step(2.0,y));\n    return mix(mix(a,b,step(1.0,x)), mix(c,d,step(3.0,x)), step(2.0,x)) / 16.0;\n}\n\n// PARA A DECISÃO: eu NÃO usei dither no bloco final. Motivo medido e visto:\n// o grão multiplicativo já tem amplitude da ordem do passo de 8 bits\n// (sigma ~4 níveis de 255), então ele JÁ age como dither e já mata banding.\n// Somar Bayer por cima dá um padrão regular de 4px que aparece como xadrez\n// em áreas chapadas -- é o oposto de papel, que é irregular."
     },
     {
      "para_que": "O ARNÊS DE VALIDAÇÃO. Compila um .frag no driver REAL da máquina (o mesmo que o cosmic-comp usa), em GLSL ES 1.00, sem abrir janela e sem instalar nada. Use SEMPRE antes de gravar qualquer shader no binário — lembrando que shader que não compila = cosmic-comp que não sobe.",
      "linguagem": "bash",
      "fonte": "autoral — /tmp/meow-trabalho e nvpatch.py",
      "trecho": "# Testa as 8 combinações de #define que o smithay compila no arranque.\n# Se qualquer uma falhar, init_shaders devolve Err e a sessão NÃO ABRE.\n\nSCRATCH=/tmp/meow-trabalho - <<'PY'\nimport itertools, subprocess, os\nSRC = \"offscreen.frag\"          # o shader a validar\ndefines = [\"EXTERNAL\", \"NO_ALPHA\", \"DEBUG_FLAGS\"]\nenv = dict(os.environ,\n           __EGL_VENDOR_LIBRARY_FILENAMES=\"/usr/share/glvnd/egl_vendor.d/10_nvidia.json\")\nbase = open(SRC).read()\nassert \"//_DEFINES_\" in base, \"o marcador //_DEFINES_ sumiu -- smithay nao vai injetar nada\"\nok = True\nfor r in range(4):\n    for combo in itertools.combinations(defines, r):\n        blob = \"\".join(\"#define %s\\n\" % d for d in combo)\n        open(\"c.frag\", \"w\").write(base.replace(\"//_DEFINES_\", blob))\n        out = subprocess.run([\"python3\", \"nvpatch.py\", \"c.frag\"],\n                             capture_output=True, text=True, env=env)\n        good = \"[OK \" in out.stdout\n        ok &= good\n        print(\"  %-32s %s\" % (\"+\".join(combo) or \"(nenhum)\", \"OK\" if good else out.stdout))\nprint(\"TODAS COMPILAM:\", ok)\nraise SystemExit(0 if ok else 1)\nPY\n\n# Extrair o offscreen.frag do binário VIVO para conferir o que está aplicado:\npython3 - <<'PY'\ndata = open('/usr/bin/cosmic-comp','rb').read()\ni = data.find(b'uniform float color_mode;')\ns = data.rfind(b'#version 100', max(0,i-4000), i)\ne = data.find(b'gl_FragColor = color;\\n}', s) + len(b'gl_FragColor = color;\\n}')\nprint(data[s:e].decode('utf-8', errors='replace'))\nprint('--- %d bytes de texto (orcamento para patch byte a byte)' % (e-s))\nPY"
     }
    ],
    "solucoes": [
     {
      "nome": "Bloco paper_texture no offscreen.frag + 3 edições em Rust (o caminho certo)",
      "alvo": "textura",
      "como": "1) Aplicar a INSERÇÃO 1 e a INSERÇÃO 2 do bloco final em src/backend/render/shaders/offscreen.frag. 2) Aplicar as 3 edições em Rust: `paper_texture: f32` em ScreenFilter (src/config/mod.rs:152), `is_noop()` passando a exigir `paper_texture <= 0.0` (linha 159 — SEM ISSO a passada é pulada e o papel não aparece), o default em load_filter_state (linha ~394), `UniformName::new(\"paper_texture\", UniformType::_1f)` em src/backend/render/mod.rs:~419, e `Uniform::new(\"paper_texture\", screen_filter.filter.paper_texture)` na linha ~1378. 3) Compilar DENTRO do aurora-cosmic-comp-ws.sh --build, na MESMA árvore que já remove o ensure_last_empty — este patch não pode competir com aquele, tem de ser o mesmo binário .aurora-ws. 4) Antes de instalar, rodar o arnês das 8 combinações de #define; shader quebrado aqui não degrada nada, impede a sessão de subir.",
      "onde": "pop-os/cosmic-comp: src/backend/render/shaders/offscreen.frag, src/backend/render/mod.rs (linhas ~419 e ~1378), src/config/mod.rs (linhas 152, 159, ~394). Build por ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh --build; binário para /var/lib/aurora/cosmic-comp-<versão>.aurora-ws",
      "a_quente": true,
      "custo": "~4 min de build (o mesmo que o patch de workspace já custa, e é o MESMO build). Em runtime: +0,045 ms por quadro em 4K = 0,27% de um quadro de 60 Hz, 0,65% de um de 144 Hz. Zero medido quando desligado.",
      "durabilidade": "Morre a cada apt upgrade do cosmic-comp, exatamente como o patch de workspace — e pelo mesmo motivo (o binário é substituído). O conserto é humano e é o mesmo comando. Sobrevive a reboot e a troca de tema. O self-heal deve APENAS avisar, nunca recompilar sozinho."
     },
     {
      "nome": "Fallback vivo sem recompilar: 4 degraus pelo dropdown de Filtros de cor",
      "alvo": "textura",
      "como": "Ensinar o aurora-night-light.py a gravar o offscreen.frag compacto inteiro (1.099 bytes) em vez de só o bloco de 195 bytes, preenchendo com espaços até os 2.415 bytes originais — a mesma técnica de preservação de tamanho que ele já usa, só com janela maior (find_block passa a delimitar de `#version 100` até `gl_FragColor = color;\\n}`, e a guarda de sanidade de 512 bytes vira 2.415). O efeito passa a ser escolhido a quente pelo dropdown \"Acessibilidade > Filtros de cor\": Escala de cinza = papel 0%, Protanopia = 33%, Deuteranopia = 67%, Tritanopia = 100%, todos com a luz noturna junto. Serve como validação do gosto dela ANTES de gastar o build — se ela achar 33% forte demais, os coeficientes mudam sem recompilar nada.",
      "onde": "~/.config/zsh/scripts/aurora-night-light.py (funções find_block e build_block). ATENÇÃO: ~/.config/zsh tem auto-commit a cada 10 min, então escrever ali vira commit no repo privado dela.",
      "a_quente": true,
      "custo": "Zero build. Um logout para o novo texto do shader entrar (o cosmic-comp só lê o binário no arranque); depois disso a troca entre os 4 degraus é instantânea. Preço real: os três filtros de daltonismo somem enquanto o patch estiver aplicado — o --restore devolve.",
      "durabilidade": "Morre a cada apt upgrade do cosmic-comp e é refeito pelo hook que o aurora-night-light.py já tem. Sobrevive a reboot e a troca de tema. Não conflita com o patch de workspace (age no binário instalado; se o binário vivo for o .aurora-ws, patchar esse)."
     },
     {
      "nome": "Canal vivo contínuo sem mexer no protocolo: alargar o ColorFilter",
      "alvo": "ui",
      "como": "No mesmo recompile, fazer `protocol_to_color_filter` (src/wayland/protocols/a11y.rs) aceitar valores 5..105 e mapear para intensidade de papel 0..100%, em vez de rejeitá-los. O handler já é permissivo com o resto: `if let Ok(filter) = filter && filter != atual { request_screen_filter(filter) }`. Do lado do MeowSystem, um cliente Wayland pequeno que faça bind no cosmic_a11y_manager_v1 e mande SetScreenFilter com o valor do slider. Vantagem sobre inventar um protocolo novo: o cosmic-settings não precisa saber de nada, e o caminho de aplicação (backend.update_screen_filter) já é o que aplica A QUENTE hoje.",
      "onde": "pop-os/cosmic-comp src/wayland/protocols/a11y.rs (protocol_to_color_filter, ~linha 201) + um binário novo no MeowSystem (a FRONTEIRA.md decide onde; sugestão: bin/ ou scripts/, não em ~/.config/zsh)",
      "a_quente": true,
      "custo": "Incluído no mesmo build de ~4 min. O cliente Wayland é pequeno (bind do global + um request).",
      "durabilidade": "Mesma do patch principal: morre no apt upgrade do cosmic-comp, refeito pelo mesmo build. O cliente do lado do MeowSystem sobrevive a tudo."
     },
     {
      "nome": "Desligar o papel em tela cheia — por imagem, NÃO por FPS",
      "alvo": "agendamento",
      "como": "Se for feito, gatilho por janela em fullscreen ou por app na lista (vídeo/jogo), zerando paper_texture. Mas a justificativa correta não é desempenho: medi +0,27% de um quadro de 60 Hz, o que é irrelevante, e o branch desligado custa zero. A razão real é imagem: grão estático por cima de vídeo fica feio, e ruído por pixel destrói a compressão em gravação de tela, OBS e área de trabalho remota — o codificador passa a gastar bitrate com o meu ruído.",
      "onde": "Lado do cosmic-comp (onde o estado de fullscreen já existe) ou um daemon do MeowSystem que fale o protocolo a11y. Decidir pela docs/FRONTEIRA.md.",
      "a_quente": true,
      "custo": "Zero em GPU. Custo de complexidade: é mais uma máquina de estados que pode brigar com o agendamento por horário — sugiro deixar para depois de (A)(B)(C) funcionarem.",
      "durabilidade": "Depende de onde for implementado; se for no cosmic-comp, morre no apt upgrade junto com o resto."
     },
     {
      "nome": "Arnês de validação de shader antes de gravar no binário",
      "alvo": "build",
      "como": "Rodar o script do último item de \"codigo\" contra qualquer offscreen.frag antes de instalá-lo. Ele cria um contexto EGL surfaceless no dispositivo NVIDIA (EGL_PLATFORM_DEVICE_EXT) e compila o shader em GLSL ES 1.00 no MESMO driver que o cosmic-comp usa, nas 8 combinações de #define, sem abrir janela e sem instalar pacote nenhum (usa só libEGL/libGLESv2 + ctypes). Trocar __EGL_VENDOR_LIBRARY_FILENAMES para o 50_mesa.json roda a mesma bateria no compilador do Mesa, que é mais rigoroso em alguns pontos.",
      "onde": "Hoje em /tmp/meow-trabalho,nvpatch.py}. Se virar permanente, é ferramenta de build do MeowSystem — não de ~/.config/zsh.",
      "a_quente": true,
      "custo": "~1 s por bateria. Nenhuma dependência nova: glslang-tools NÃO está instalado nesta máquina e não precisa ser.",
      "durabilidade": "Independente de apt upgrade e de tema. Só depende de libEGL/libGLESv2, que sempre existem."
     }
    ],
    "derrubado": [
     "\"Hash sem seno é mais barato\" — FALSO nesta GPU, e por margem grande. Medido em 3840x2160 na RTX 4060, custo por quadro sobre a base: sem seno 1/2/3 oitavas = +0,040 / +0,099 / +0,136 ms; com seno = +0,019 / +0,041 / +0,074 ms. O seno ganha em todas as contagens. Motivo: na NVIDIA o sin() é uma instrução da SFU, um caminho separado, enquanto o hash \"sem seno\" são ~8 ALUs que disputam o mesmo pipeline do resto do shader. O conselho de Shadertoy de \"nunca use sin em hash\" nasceu de GPU móvel com mediump, não vale aqui.",
     "\"sin(dot(p,·))*43758 perde precisão em coordenada de tela 4K e vira faixa ou repetição\" — NÃO REPRODUZIU. Renderei cinza chapado a 3840x2160 e li recortes em (0,0) e em (3400,1800), nos dois drivers. Desvio padrão idêntico nos dois cantos (3,15 e 3,15 níveis de 255), número de níveis distintos idêntico (17 e 17), e autocorrelação máxima da linha para deslocamentos de 8 a 512 px com |r| <= 0,155 — que é o mesmo patamar do hash sem seno (|r| <= 0,155). Em highp (fp32) de desktop, com coordenada até 3840, o seno aguenta.",
     "\"Dá para entregar o slider só patchando o texto do shader, como a temperatura\" — FALSO. Um uniform novo declarado só no texto fica preso em 0.0 para sempre, porque nada em Rust o define; em GLES um uniform nunca escrito vale zero. Os únicos valores vivos que chegam ao offscreen.frag são `invert` (0/1), `color_mode` (0..4, de um enum repr(u8)) e `alpha` (fixo em 1.0 no código). Patch de texto entrega no máximo 4 DEGRAUS, não um slider.",
     "\"Editar ~/.local/state/cosmic-comp/a11y_screen_filter.ron aplica a quente\" — FALSO. Esse arquivo é state (xdg.place_state_file), lido UMA VEZ em load_dynamic() no arranque e ESCRITO pelo compositor quando o filtro muda. Quem aplica a quente é o protocolo Wayland cosmic-a11y-manager: Request::SetScreenFilter -> request_screen_filter() -> backend.update_screen_filter(). Não construir slider em cima do arquivo.",
     "\"A passada de postprocess sempre roda, então o filtro de tela já custa alguma coisa o tempo todo\" — FALSO. `if !screen_filter.filter.is_noop()` (src/backend/render/mod.rs:1284) pula a passada INTEIRA, inclusive a alocação do buffer offscreen, quando não há filtro. Com tudo desligado o custo é zero, não \"quase zero\". Corolário perigoso: se alguém adicionar paper_texture ao ScreenFilter e ESQUECER de atualizar is_noop(), o uniform vai estar certo, o shader vai estar certo, e o papel simplesmente não vai aparecer — sem nenhuma mensagem de erro.",
     "\"O Textura do MIUI é halftone ou dithering ordenado\" — sem sustentação. Nenhuma das quatro fontes que abri menciona halftone ou dither; todas descrevem grão/textura de papel sobreposta mais dessaturação, e o seletor de cor (Full colors / Light colors / Black and white) é um controle SEPARADO do slider de Textura. Testei dither Bayer por cima do meu bloco e é visivelmente errado: dá xadrez regular de 4 px em áreas chapadas, o oposto da irregularidade do papel.",
     "\"O reading_mode.glsl do Hyprland dá para colar direto no cosmic-comp\" — FALSO em dois níveis. Técnico: `texture()` não existe em GLSL ES 1.00 (erro C7532 no NVIDIA, \"no function with name 'texture'\" no Mesa — medido), além de #version 320 es e in/out. E de produto: ele converte TUDO para cinza e pinta numa paleta tinta/papel de dois tons, enquanto a captura dela pede \"Cores: Cores totais\". Custo do porte completo, medido: 6,5x o do meu bloco (+0,292 ms contra +0,045 ms).",
     "\"Indexação dinâmica de array/matriz não compila em #version 100\" — verdade no papel, falso na prática desta máquina. A ES 1.00 só garante constant-index-expression, mas tanto o NVIDIA 580.173.02 quanto o Mesa 26.1.6 aceitaram `float kern[64]; kern[idx]` e `const mat4 bayer; bayer[x][y]` com índice dinâmico. Não é motivo para evitar Bayer — o motivo para evitar é estético, não sintático."
    ]
   },
   "verificadas": [
    {
     "nome": "Bloco paper_texture no offscreen.frag + 3 edições em Rust (o caminho certo)",
     "alvo": "textura",
     "como": "1) Aplicar a INSERÇÃO 1 e a INSERÇÃO 2 do bloco final em src/backend/render/shaders/offscreen.frag. 2) Aplicar as 3 edições em Rust: `paper_texture: f32` em ScreenFilter (src/config/mod.rs:152), `is_noop()` passando a exigir `paper_texture <= 0.0` (linha 159 — SEM ISSO a passada é pulada e o papel não aparece), o default em load_filter_state (linha ~394), `UniformName::new(\"paper_texture\", UniformType::_1f)` em src/backend/render/mod.rs:~419, e `Uniform::new(\"paper_texture\", screen_filter.filter.paper_texture)` na linha ~1378. 3) Compilar DENTRO do aurora-cosmic-comp-ws.sh --build, na MESMA árvore que já remove o ensure_last_empty — este patch não pode competir com aquele, tem de ser o mesmo binário .aurora-ws. 4) Antes de instalar, rodar o arnês das 8 combinações de #define; shader quebrado aqui não degrada nada, impede a sessão de subir.",
     "onde": "pop-os/cosmic-comp: src/backend/render/shaders/offscreen.frag, src/backend/render/mod.rs (linhas ~419 e ~1378), src/config/mod.rs (linhas 152, 159, ~394). Build por ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh --build; binário para /var/lib/aurora/cosmic-comp-<versão>.aurora-ws",
     "a_quente": true,
     "custo": "~4 min de build (o mesmo que o patch de workspace já custa, e é o MESMO build). Em runtime: +0,045 ms por quadro em 4K = 0,27% de um quadro de 60 Hz, 0,65% de um de 144 Hz. Zero medido quando desligado.",
     "durabilidade": "Morre a cada apt upgrade do cosmic-comp, exatamente como o patch de workspace — e pelo mesmo motivo (o binário é substituído). O conserto é humano e é o mesmo comando. Sobrevive a reboot e a troca de tema. O self-heal deve APENAS avisar, nunca recompilar sozinho.",
     "trilha": "glsl-textura",
     "veredito": {
      "solucao": "glsl-textura — Bloco paper_texture no offscreen.frag + 3 edições em Rust",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "O QUE PASSOU NO TESTE (medido na árvore /home/vitoriamaria/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094, que é a versão instalada):\n\n- Todos os cinco pontos de ancoragem EXISTEM: src/config/mod.rs:152 (`pub struct ScreenFilter`), :159 (`pub fn is_noop`), :377 `load_filter_state` com o default em :394-396; src/backend/render/mod.rs:422-423 (`UniformName::new(\"invert\"/\"color_mode\", UniformType::_1f)`) e :1364-1381 (`Uniform::new(\"invert\"...)`, `Uniform::new(\"color_mode\"...)`).\n- O uniform É reconstruído por quadro: os `Uniform::new` estão DENTRO de `render_output`, montados a cada chamada a partir de `screen_filter.filter`. Então um float carregado por uniform é, de fato, quente. Essa metade da proposta está certa.\n- Não pisa em território alheio: docs/FRONTEIRA.md:58 diz literalmente \"binário `cosmic-comp` (patches de workspace e night light) | Aurora | **Aurora**\".\n- Não briga com o night light: no FONTE o `color_mode == 1.0` ainda é greyscale (offscreen.frag:44-46) — a luz quente vive só no binário. E o `aurora-cosmic-comp-ws.sh` já sabe disso: `reaplicar_night_light()` na linha 159, chamada na 519 depois de instalar. Perder o night light no build está previsto e coberto.\n- Caminho de volta EXISTE e foi verificado: `/var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig` está lá, `cmd_restore` copia de volta, sshd está ATIVO, e `/usr/lib/systemd/system/autovt@.service -> getty@.service` com NAutoVTs no default 6 dá Ctrl+Alt+F2..F6.\n\nO QUE DERRUBA O \"VALE A QUENTE\":\n\n1. FATAL COMO ESCRITO — `paper_texture` não tem NENHUM setter em runtime. `load_filter_state` é chamada uma única vez (config/mod.rs:341, de dentro de `Config::load` na :331). Não há watcher nenhum sobre `a11y_screen_filter.ron`. A única mutação viva de `ScreenFilter` vem do protocolo Wayland a11y — `Request::SetScreenFilter { inverted, filter }` (wayland/protocols/a11y.rs:196), que carrega só um bool e um enum de 4 valores. Não há canal para um float. Do jeito descrito, cada casa do slider custa um LOGOUT — exatamente o que ela está tentando fugir.\n\n2. PIOR: `PersistenceGuard::drop` (config/mod.rs:677) re-serializa a struct em memória por cima do arquivo. Então se um script editar o RON à mão, o valor é APAGADO no instante em que ela mexer no toggle de acessibilidade. Editar o arquivo não é workaround; é uma armadilha.\n\n3. Falta `#[serde(default)]`. O arquivo vivo dela é literalmente `( inverted: false, )` — `color_filter` sobrevive à ausência só porque o `missing_field` do serde funciona para `Option`. Um `f32` cru sem `#[serde(default)]` faz o `ron::de::from_reader` FALHAR, e aí o próprio código (config/mod.rs:386-389) apaga o arquivo de estado dela.\n\n4. \"8 combinações de #define\" está errado: o smithay compila TRÊS conjuntos — `[]`, `[NO_ALPHA]`, `[EXTERNAL]` (vendor/smithay/src/backend/renderer/gles/shaders/mod.rs:215-217) — cada um linkado duas vezes (com e sem `DEBUG_FLAGS`) = 6 programas. Não existe EXTERNAL+NO_ALPHA.\n\n5. NÃO PUDE VALIDAR O GLSL: o texto da \"INSERÇÃO 1 e INSERÇÃO 2\" não veio na proposta. E não dá para terceirizar a conferência: `glslangValidator` NÃO está instalado nesta máquina, e a GPU é NVIDIA (RTX 4060, driver 580.173.02), cujo front-end de GLSL é permissivo — um bloco que compila aqui pode continuar fora do #version 100.\n\n6. O custo citado é de 4K; o único output real é o DP-2 \"HiTV\" a 1920x1080@60 (outputs.ron). É um quarto dos pixels — o número é conservador, não errado. Mas o caro não é a matemática, é a passada offscreen; ela só sai de graça enquanto o night light já estiver ligado.",
      "correcao": "NÃO leve o float pelo RON de a11y. Leve pelo cosmic-config, que JÁ tem watcher inotify vivo — isso é o que transforma \"logout por notch\" em slider de verdade:\n\n- `Config::load` (config/mod.rs:180-184) insere um `ConfigWatchSource` para `com.system76.CosmicComp`.\n- `Config::watch` (vendor/cosmic-config/src/lib.rs) reporta QUALQUER nome de arquivo sob `~/.config/cosmic/com.system76.CosmicComp/v1/` como chave alterada — não há whitelist, é `path.strip_prefix(user_path)` puro.\n- As chaves são escalares RON crus: conferi `active_hint` = `true`, `edge_snap_threshold` = `10`.\n\nLogo, a 6ª edição (a que falta) é um braço em `config_changed` (config/mod.rs, o `match key.as_str()`):\n\n    \"paper_texture\" => {\n        let value = get_config::<f32>(&config, \"paper_texture\").clamp(0.0, 1.0);\n        let mut cfg = state.common.config.dynamic_conf.screen_filter_mut();\n        let mut updated = (*cfg).clone();\n        updated.paper_texture = value;\n        if state.backend.update_screen_filter(&updated).is_ok() {\n            *cfg = updated;\n        }\n    }\n\nO empréstimo disjunto de `state.common.config` e `state.backend` compila: é exatamente o padrão já usado em src/wayland/handlers/a11y.rs:42-50.\n\nCom isso, `echo '0.35' > ~/.config/cosmic/com.system76.CosmicComp/v1/paper_texture` muda a tela na hora, sem logout. O slider (B) vira um script; o agendamento (C) vira um systemd timer escrevendo o mesmo arquivo; e (A) sai pelo mesmo caminho quando a temperatura também virar uniform.\n\nOutras correções obrigatórias:\n- `#[serde(default)] pub paper_texture: f32` — sem isso o estado dela é apagado no primeiro boot.\n- Manter o `is_noop()` exigindo `paper_texture <= 0.0` (a proposta acertou aqui; sem isso a passada é pulada).\n- O arnês é de 6 programas, não 8: `[]`, `[NO_ALPHA]`, `[EXTERNAL]`, cada um × `DEBUG_FLAGS`.\n- Instalar `glslang-tools` ANTES, e rodar o `glslangValidator -S frag` nas 6 variantes com o `//_DEFINES_` já substituído — não confiar no compilador da NVIDIA como juiz.\n- O bloco GLSL precisa ser reapresentado para conferência: em #version 100 nada de `texelFetch`, `inverse()`, `%`, operadores de bit, laço com limite variável, `textureLod` sem extensão. `v_coords`, `gl_FragCoord` e o hash `fract(sin(dot(...)))` são legais.\n- Cuidado com o `color.rgb /= color.a` da linha 36: qualquer coisa somada depois vive em espaço un-multiplied e é re-multiplicada na linha 87. O bloco tem de entrar ENTRE as duas, e saturar (`clamp`) para não estourar.",
      "risco_real": "O raio é MAIOR do que a proposta diz. `/usr/bin/cosmic-greeter-start` é literalmente `exec cosmic-comp cosmic-greeter` — o greeter roda no MESMO binário. Um shader que não linka não derruba só a sessão dela: derruba a TELA DE LOGIN. `init_shaders` propaga o `Err` do `compile_custom_texture_shader` e aborta a inicialização do backend; o greetd tem `Restart=always` com `StartLimitBurst=5`/30s, então pisca cinco vezes e desiste. E o unit dá `Conflicts=getty@tty1`, então a tty1 não é a saída.\n\nTEM VOLTA, e ela foi verificada nesta máquina:\n1. Ctrl+Alt+F2 (até F6) — `autovt@.service` é symlink para `getty@.service` e `NAutoVTs` está no default 6.\n2. `sudo aurora-cosmic-comp-ws.sh --restore` — o backup `/var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig` existe hoje (28 MB, 27/ago).\n3. Rede: `sshd` está ATIVO — segunda porta de entrada.\n4. Último recurso: `sudo apt-get install --reinstall cosmic-comp`.\n\nRegra que não pode ser afrouxada: o arnês das 6 variantes roda ANTES de o binário chegar em /usr/bin, nunca depois. E o self-heal continua só AVISANDO — nunca recompilando sozinho.\n\nRisco menor mas certo: sem `#[serde(default)]`, no primeiro boot depois da instalação o `~/.local/state/cosmic-comp/a11y_screen_filter.ron` dela é APAGADO pelo próprio código. Hoje o arquivo só guarda `inverted: false`, então o dano prático é zero — mas o mecanismo é real e vai morder de novo."
     }
    },
    {
     "nome": "Fallback vivo sem recompilar: 4 degraus pelo dropdown de Filtros de cor",
     "alvo": "textura",
     "como": "Ensinar o aurora-night-light.py a gravar o offscreen.frag compacto inteiro (1.099 bytes) em vez de só o bloco de 195 bytes, preenchendo com espaços até os 2.415 bytes originais — a mesma técnica de preservação de tamanho que ele já usa, só com janela maior (find_block passa a delimitar de `#version 100` até `gl_FragColor = color;\\n}`, e a guarda de sanidade de 512 bytes vira 2.415). O efeito passa a ser escolhido a quente pelo dropdown \"Acessibilidade > Filtros de cor\": Escala de cinza = papel 0%, Protanopia = 33%, Deuteranopia = 67%, Tritanopia = 100%, todos com a luz noturna junto. Serve como validação do gosto dela ANTES de gastar o build — se ela achar 33% forte demais, os coeficientes mudam sem recompilar nada.",
     "onde": "~/.config/zsh/scripts/aurora-night-light.py (funções find_block e build_block). ATENÇÃO: ~/.config/zsh tem auto-commit a cada 10 min, então escrever ali vira commit no repo privado dela.",
     "a_quente": true,
     "custo": "Zero build. Um logout para o novo texto do shader entrar (o cosmic-comp só lê o binário no arranque); depois disso a troca entre os 4 degraus é instantânea. Preço real: os três filtros de daltonismo somem enquanto o patch estiver aplicado — o --restore devolve.",
     "durabilidade": "Morre a cada apt upgrade do cosmic-comp e é refeito pelo hook que o aurora-night-light.py já tem. Sobrevive a reboot e a troca de tema. Não conflita com o patch de workspace (age no binário instalado; se o binário vivo for o .aurora-ws, patchar esse).",
     "trilha": "glsl-textura",
     "veredito": {
      "solucao": "Trilha \"glsl-textura\": alargar a janela do aurora-night-light.py de 195 para 2415 bytes (de `#version 100` até `gl_FragColor = color;\\n}`), gravando um offscreen.frag compacto inteiro preenchido com espaços, para que o dropdown \"Acessibilidade > Filtros de cor\" vire 4 degraus de textura papel/Kindle (Escala de cinza=0%, Protanopia=33%, Deuteranopia=67%, Tritanopia=100%), todos com a luz noturna junto. Zero build; um logout para entrar; troca entre degraus a quente.",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "Tentei derrubar e não consegui — os números batem todos, mas sobrou uma incerteza que impede o CONFIRMADO.\n\nO QUE SE PROVOU CERTO\n- Janela de 2415 bytes: exata. Medi no binário vivo (`#version 100` em 0x2dbec6 até `gl_FragColor = color;` + `\\n}`) e no fonte: `src/backend/render/shaders/offscreen.frag` tem 2415 bytes cravados, sem newline final. A proposta acertou o número.\n- Âncora única: `uniform float color_mode;` aparece 1x no binário inteiro. (`#version 100` aparece 10x e `gl_FragColor = color;` 5x — por isso a janela TEM de ser delimitada a partir dessa âncora, com rfind para trás; nunca procurando `#version 100` direto.)\n- \"Vale a quente\": PROVADO no fonte, não deduzido. Em `src/backend/render/mod.rs` (e `kms/surface/mod.rs:1899,1933`) o color_mode é montado a cada passada como `Uniform::new(\"color_mode\", screen_filter.color_filter.map(|val| val as u8 as f32).unwrap_or(0.))`. É uniform por frame, não define de compilação. E o dropdown existe de verdade: o cosmic-settings tem `color-filter` com `.greyscale/.protanopia/.deuteranopia/.tritanopia` nos .ftl. O mapeamento 1/2/3/4 é confirmado pelos próprios comentários do shader upstream.\n- GLSL ES 1.00: validado EMPIRICAMENTE, não no olho. Não havia glslangValidator nem glslc na máquina, então compilei um harness de ~60 linhas em C (gcc + libEGL + libGLESv2, pbuffer 16x16, sem janela nenhuma) que reproduz exatamente as 6 ligações que o smithay faz — `create_variant(&[])`, `&[NO_ALPHA]`, `&[EXTERNAL]`, cada uma × normal + DEBUG_FLAGS (vendor/smithay/.../shaders/mod.rs:213-218). Controle positivo (shader upstream de 2415 bytes): 6/6 ok. Controle negativo (uint/bitwise, coisa de ES 3.x, sob `#version 100`): 6/6 rejeitado — o validador morde. Candidato meu de 1234 bytes, com os degraus e grão de papel: 6/6 ok. Emendado na janela real com 1181 espaços de padding: 6/6 ok e o tamanho do binário preservado.\n- Durabilidade: o `aurora-cosmic-comp-ws.sh` já tem `reaplicar_night_light()` e o comentário no próprio script documenta a ordem (self-heal chama `--ensure` na linha 2626 e o night-light na 2636, de propósito). Sobrevive a apt upgrade e a reboot. Tema não toca nisso.\n- Território: NÃO invade ninguém. O `docs/FRONTEIRA.md` linha 58 diz literalmente \"binário cosmic-comp (patches de workspace e night light) | Aurora | **Aurora**\". O lugar proposto é o lugar certo.\n- Convivência com o patch de workspace: confirmada por diff. O `/usr/bin/cosmic-comp` instalado É o `.aurora-ws` (33.571.456 bytes nos dois) com 111 bytes diferentes, e TODOS os 111 caem dentro da janela de 2415 do shader. Os dois patches já coexistem hoje, no mesmo binário, sem se pisarem.\n\nPOR QUE NÃO É CONFIRMADO\nMeu validador rodou em Mesa/llvmpipe. A placa dela é NVIDIA RTX 4060 com o driver 580.173.02 (open), e o cosmic-comp compila esse shader no compilador GLSL da NVIDIA, que é outro programa. Não consegui abrir um EGLDisplay da NVIDIA neste contexto headless para fechar a prova. O Mesa é o compilador mais rígido dos dois, então passar no Mesa é um portão conservador — mas é portão, não prova. O gate tem de ser re-rodado dentro da sessão dela (onde o EGL da NVIDIA existe) antes de qualquer escrita em /usr/bin/cosmic-comp.\n\nUMA RESSALVA DE MÉRITO, NÃO DE FATO\nA premissa \"validar o gosto dela barato\" é mais fraca do que parece. Cada mudança de coeficiente exige um logout, porque o cosmic-comp só lê o texto do shader no arranque. O caminho do recompile também custa um logout por iteração — só soma ~4 min de build. Então o fallback economiza minutos por iteração, não logouts. Ele continua valendo pelo outro motivo: zero mudança em Rust e volta trivial. Mas vale dizer isso a ela em vez de vender como \"de graça\".",
      "risco_real": "O QUE QUEBRA: um erro de GLSL não custa a sessão, custa a TELA DE LOGIN. O `/usr/bin/cosmic-greeter-start` é literalmente `exec cosmic-comp cosmic-greeter` — o greeter roda o MESMO binário patchado. E o drop-in dela `/etc/systemd/system/cosmic-greeter.service.d/99-aurora-espera-output.conf` põe `StartLimitIntervalSec=0` e `StartLimitBurst=0`, ou seja, sem teto de tentativas. Um shader que não compila = greeter renascendo a cada 5s para sempre, sem nunca aparecer. É exatamente o cenário que o comentário do próprio drop-in descreve como catástrofe (\"seria loop infinito SEM NUNCA mostrar greeter\"). No fonte o erro propaga por `?`: `link_program` -> `GlesError::ProgramLinkError` -> `init_shaders` -> `.context(\"Failed to compile shaders\")?` em `kms/device.rs:1119`.\n\nTEM VOLTA, E SÃO DUAS: (1) SSH — o sshd está `active` e escutando na 22, então dá para entrar de outra máquina sem tocar na TV; (2) TTY — `NAutoVTs=6` (default), então Ctrl+Alt+F2 abre getty. O `getty@tty1` está em Conflicts com o greeter, mas do tty2 ao tty6 estão livres.\n\nCUIDADO NA VOLTA — a proposta erra aqui: `--restore` NÃO é o conserto certo. Ele copia `/var/lib/aurora/cosmic-comp-<ver>.orig`, que tem 28.339.864 bytes — é o binário DO PACOTE, não o build da Aurora. O desktop sobe, mas o patch de workspace vai junto e o workspace vazio volta. O conserto correto é `sudo cp -a /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws /usr/bin/cosmic-comp`, que devolve o binário certo já sem o shader patchado.\n\nCUSTO ACEITO: somem os 3 filtros de daltonismo. O greyscale já tinha sido gasto pela v3.30, então o custo marginal são só os três.",
      "correcao": "Quatro correções concretas antes de escrever qualquer byte:\n\n1) TRAVA OBRIGATÓRIA — validar offline antes de tocar em /usr/bin/cosmic-comp. Não existe glslangValidator nem glslc nesta máquina, mas existe gcc + libEGL + libGLESv2. O harness que escrevi e rodei está em /tmp/meow-trabalho (binário: .../valida). Ele compila o shader nas 6 variantes que o smithay realmente liga e sai com rc!=0 se qualquer uma falhar. Isso vira pré-condição do patcher: se o gate não passa, o script não escreve. E rodar dentro da sessão dela, para pegar o compilador da NVIDIA e não o llvmpipe.\n\n2) PRESERVAR A STRING `NIGHT LIGHT (Aurora)` no shader compacto — isto é load-bearing, não cosmético. O `aurora-cosmic-comp-ws.sh` faz `grep -aqs 'NIGHT LIGHT (Aurora)' \"$ALVO\"` para decidir se o binário instalado está limpo o bastante para virar o artefato de build. Se a reescrita compacta cortar esse comentário para economizar bytes, a guarda para de disparar e um binário já com a temperatura de hoje pode virar o molde de toda instalação futura — exatamente o bug que a auditoria de 25/08 consertou. Sobram 1181 bytes de padding; não há razão para cortar.\n\n3) USAR `gl_FragCoord.xy`, NUNCA `v_coords`, para o grão do papel. Em `src/backend/render/mod.rs` o MESMO programa com os MESMOS uniforms é aplicado a DOIS elementos: a textura de tela cheia e a `postprocess_state.cursor_texture`. `v_coords` vai de 0..1 dentro de cada elemento, então uma textura baseada nele desenharia o grão inteiro dentro do cursor de ~24px. `gl_FragCoord` é espaço de framebuffer e casa nos dois. É válido em GLSL ES 1.00 (compilou nas 6 variantes).\n\n4) DELIMITAR A JANELA A PARTIR DA ÂNCORA ÚNICA, e manter a guarda de sanidade. `uniform float color_mode;` ocorre 1x; `#version 100` ocorre 10x e `gl_FragColor = color;` 5x. Então: achar a âncora, `rfind(b\"#version 100\", 0, ancora)` para trás e `find(b\"gl_FragColor = color;\", ancora)` para frente, e só aceitar se a janela der EXATAMENTE 2415 (não `> 2415 -> None`, mas `!= 2415 -> None`). Igualdade exata é mais barata e mais segura que teto: se o upstream mexer uma vírgula no shader, o patcher recusa e sai com rc=3 em vez de reescrever às cegas — que é o comportamento seguro que o script já tem hoje.\n\nForma validada do bloco (1234 bytes de corpo + 1181 espaços = 2415), com os quatro degraus:\n\n    if (color_mode >= 1.0) {        // NIGHT LIGHT (Aurora)\n        float p = 0.0;\n        if (color_mode == 2.0) { p = 0.33; }\n        if (color_mode == 3.0) { p = 0.67; }\n        if (color_mode >= 4.0) { p = 1.00; }\n        color.rgb *= vec3(1.00000, 0.77987, 0.54468);\n        float g = dot(color.rgb, vec3(0.299, 0.587, 0.114));\n        vec3 paper = vec3(0.94, 0.90, 0.82) * (0.15 + 0.85 * g);\n        float n = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453);\n        paper += vec3((n - 0.5) * 0.035);\n        color.rgb = mix(color.rgb, paper, p);\n    }\n\nSem texelFetch, sem bitwise, sem matriz invertida, sem laço — tudo ES 1.00. O padding vai todo DEPOIS do `}` final de main(), que é espaço em branco no fim da unidade de tradução; testei emendado no binário e as 6 variantes compilam.\n\n5) Corrigir a linha de recuperação na documentação: trocar \"o --restore devolve\" por `sudo cp -a /var/lib/aurora/cosmic-comp-<versão>.aurora-ws /usr/bin/cosmic-comp`, porque `--restore` restaura o build do PACOTE e leva junto o patch de workspace."
     }
    },
    {
     "nome": "Canal vivo contínuo sem mexer no protocolo: alargar o ColorFilter",
     "alvo": "ui",
     "como": "No mesmo recompile, fazer `protocol_to_color_filter` (src/wayland/protocols/a11y.rs) aceitar valores 5..105 e mapear para intensidade de papel 0..100%, em vez de rejeitá-los. O handler já é permissivo com o resto: `if let Ok(filter) = filter && filter != atual { request_screen_filter(filter) }`. Do lado do MeowSystem, um cliente Wayland pequeno que faça bind no cosmic_a11y_manager_v1 e mande SetScreenFilter com o valor do slider. Vantagem sobre inventar um protocolo novo: o cosmic-settings não precisa saber de nada, e o caminho de aplicação (backend.update_screen_filter) já é o que aplica A QUENTE hoje.",
     "onde": "pop-os/cosmic-comp src/wayland/protocols/a11y.rs (protocol_to_color_filter, ~linha 201) + um binário novo no MeowSystem (a FRONTEIRA.md decide onde; sugestão: bin/ ou scripts/, não em ~/.config/zsh)",
     "a_quente": true,
     "custo": "Incluído no mesmo build de ~4 min. O cliente Wayland é pequeno (bind do global + um request).",
     "durabilidade": "Mesma do patch principal: morre no apt upgrade do cosmic-comp, refeito pelo mesmo build. O cliente do lado do MeowSystem sobrevive a tudo.",
     "trilha": "glsl-textura",
     "veredito": {
      "solucao": "Trilha \"glsl-textura\": alargar protocol_to_color_filter (src/wayland/protocols/a11y.rs) para aceitar 5..105 como intensidade de papel, no mesmo recompile do patch de workspace, mais um cliente Wayland no MeowSystem que manda SetScreenFilter com o valor do slider.",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "O NÚCLEO DA IDEIA ESTÁ PROVADO — e é a parte boa. Em src/backend/render/mod.rs (~1360-1380), o vetor de Uniform é montado DENTRO de render_output, ou seja, A CADA FRAME:\n\n    Uniform::new(\"color_mode\",\n        screen_filter.filter.color_filter.map(|val| val as u8 as f32).unwrap_or(0.))\n\nO PostprocessState, que é quem carrega o shader COMPILADO, só é reconstruído quando o PostprocessOutputConfig do output muda (resolução/transform) — linha 1285: `state.output_config != PostprocessOutputConfig::for_output_untransformed(output)`. Trocar o VALOR do filtro nunca recompila nada. Então um valor contínuo por esse uniform vale a quente, custo zero. \"vale a quente = true\" CONFIRMADO.\n\nConfirmei também: os arquivos existem; o handler é permissivo como descrito (`if let Ok(filter) = filter && filter != ...`); o global é criado em VERSÃO 2 (a11y.rs:65) e `SetScreenFilter2` não existe neste fonte — logo a armadilha do erro \"deprecated\" da v3 NÃO se aplica aqui; cargo 1.98.0 e 16 cores presentes.\n\nMAS a proposta, COMO ESCRITA, erra em seis pontos verificáveis:\n\n(A) A FAIXA 5..105 COLIDE COM UM VALOR JÁ DEFINIDO. No cosmic-a11y-unstable-v1.xml do rev 32283d7 (o pinado no Cargo.lock, linha 905) o enum filter é: disabled=0, unknown=1, greyscale=2, protanopia=3, deuteranopia=4, **tritanopia=5**. O 5 é tritanopia. A proposta confundiu a numeração do PROTOCOLO com a do ColorFilter do Rust (Greyscale=1..Tritanopia=4, que são os valores do SHADER). São duas numerações diferentes.\n\n(B) O CLIENTE NÃO CONSEGUE MANDAR VALOR CRU. wayland-scanner 0.31.10, src/client_gen.rs:236, emite `#arg_name: WEnum::Value(#arg_name)` — o método gerado set_screen_filter recebe o enum CONCRETO Filter, não um u32. Pelo cosmic-protocols é impossível enviar 6..106. \"Um cliente pequeno que manda SetScreenFilter com o valor do slider\" não é escrevível como está.\n\n(C) O SHADER MANDA O VALOR PRO BRANCO ERRADO. offscreen.frag é `if (color_mode == 1.0) {greyscale} else if (color_mode >= 2.0) { ...daltonismo, com else \"unknown\" que AINDA roda a matemática LMS... }`. Um color_mode 6..106 cai no `>= 2.0`, entra no fallback \"unknown\" e sai com tinta errada — não com papel.\n\n(D) E A CORREÇÃO ÓBVIA DE (C) MATA A LUZ NOTURNA DELA. O aurora-night-light.py::find_block ancora em b\"    if (color_mode == 1.0) {\" e em end_marker = b\"    } else if (color_mode >= 2.0) {\" (com teto de sanidade de 512 bytes). Limitar o branch para `>= 2.0 && <= 4.0` muda exatamente essa string do end_marker; o find falha, o script sai com código 3 e ela fica SEM os 3500K, em silêncio.\n\n(E) is_noop() DESLIGA O PASSE INTEIRO. src/config/mod.rs:159 é `!self.inverted && self.color_filter.is_none()`, e render/mod.rs:1284 é `if !screen_filter.filter.is_noop()`. Papel num campo novo deixaria is_noop()==true e o passe offscreen nunca roda: o slider não faria NADA. A proposta não menciona.\n\n(F) SÃO 4 ARQUIVOS, NÃO 1. config/mod.rs (ColorFilter + ScreenFilter + is_noop), a11y.rs (protocol_to_color_filter E color_filter_to_protocol — a volta é enviada a TODO cliente no evento screen_filter), render/mod.rs (o cast `as u8 as f32` não sobrevive a um enum com payload) e offscreen.frag.\n\n(G) TERRITÓRIO. docs/FRONTEIRA.md lista, na tabela: \"binário cosmic-comp (patches de workspace e night light) | Aurora | **Aurora**\". Pior: compilar() roda `apt-get source` num diretório NOVO por versão e aplica UM patch só, de /usr/local/share/aurora/ ou ~/.config/zsh/patches/. Edição à mão na árvore morre no próximo apt. Para durar, o patch de papel tem de virar um segundo arquivo em ~/.config/zsh/patches/ — que a TRAVA 1 do lib/comum.sh RECUSA escrever (auto-commit de 10 min) — mais uma edição no próprio compilar(). Essa metade não pode ser do MeowSystem.\n\n(H) ACOPLAMENTO. compilar() faz `return 3` e aborta o build inteiro se um patch não aplicar. a11y.rs + render/mod.rs + .frag são muito mais voláteis que o one-liner do ensure_last_empty. Um upstream que mexa em qualquer um deles derruba JUNTO o patch de workspace — e o workspace vazio volta, sintoma que ela já viveu (177 avisos no log do self-heal).\n\n(I) Bônus não coberto: \"textura\" e-ink precisa de ruído espacial, e não há uniform de resolução no shader — v_coords sozinho não dá granulação em pixel. É mais um uniform para plumbar. E o item (C) do pedido dela, agendamento por horário, esta proposta não toca.",
      "risco_real": "NÃO é eliminatório: existe volta documentada e testável.\n\n- /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig EXISTE, é ELF 64-bit válido (28.339.864 bytes, o binário de fábrica). O instalado hoje tem 33.571.456 bytes e carrega o marcador AURORA-COSMIC-WS-PATCH (confirmado com strings), ou seja, ela já roda um build nosso.\n- cmd_restore (linha ~530) copia o pkg-orig de volta com mv atômico e chmod 755. Fallback impresso pelo próprio script: `sudo apt-get install --reinstall cosmic-comp`.\n- getty.target está ACTIVE, getty@tty1 carregado — Ctrl+Alt+F2 dá console mesmo com o compositor caindo em laço.\n\nO que quebra na tela dela se der errado, em ordem de gravidade:\n1. Build ruim => cosmic-comp não sobe => laço do greeter, sem desktop. Conserto por TTY: `sudo aurora-cosmic-comp-ws.sh --restore`. ~10 segundos.\n2. Shader inválido (erro de compilação GLSL em runtime, que o Rust NÃO pega) => o passe de postprocess falha só quando o filtro está LIGADO. Risco menor: com o filtro desligado o desktop é normal, então ela nunca fica sem tela por isso.\n3. Silencioso e mais provável: mexer no texto do color_mode quebra o aurora-night-light.py e ela perde os 3500K sem nenhum aviso (o notify-send é comido pelo Não Perturbe — precedente medido: 22 avisos engolidos em 27/08).\n4. Durabilidade menor que a anunciada: o cosmic-comp subiu TRÊS versões em três dias; o auto-build só entrou em 28/08/2026 e ainda não atravessou um ciclo de apt de verdade.",
      "correcao": "Dá para salvar a trilha, mas com outro desenho. O que muda:\n\n1. NÃO sobrecarregue color_mode. Adicione um uniform SEPARADO, declarado logo depois de `uniform float color_mode;` e com o branch novo colocado DEPOIS que a cadeia `>= 2.0` fecha:\n     uniform float paper;      // 0.0 = desligado, 0.0..1.0 = intensidade\n   e no fim do main(), antes do re-multiply:\n     if (paper > 0.0) {\n         float g = (color.r + color.g + color.b) / 3.0;\n         vec3 ink = mix(color.rgb, vec3(g), paper * 0.85);\n         color.rgb = mix(color.rgb, ink * vec3(0.96, 0.94, 0.88), paper);\n     }\n   Isso é GLSL ES 1.00 válido: só mix/vec3, sem texelFetch, sem inverse(), sem loop de contagem variável, sem operador ternário sobre vetor. As DUAS âncoras do aurora-night-light.py (b\"    if (color_mode == 1.0) {\" e b\"    } else if (color_mode >= 2.0) {\") ficam byte a byte intactas, então a luz noturna dela continua funcionando e continua repatchável.\n\n2. is_noop() TEM de considerar o papel, senão o passe nem roda:\n     pub fn is_noop(&self) -> bool { !self.inverted && self.color_filter.is_none() && self.paper == 0 }\n   (src/config/mod.rs:159). Com `#[serde(default)]` no campo novo, para o RON já gravado em disco continuar desserializando.\n\n3. Se for mesmo carregar o valor pelo protocolo, a faixa começa em 6, não em 5 (5 é daltonize_tritanopia). Prefira 100..200 = 0..100%, bem longe de qualquer coisa que a System76 venha a adicionar. E color_filter_to_protocol TEM de devolver Filter::Unknown (=1) para o estado de papel: o XML diz \"If the filter is set to unknown, the compositor MUST not change the current state\", e protocol_to_color_filter já manda Filter::Unknown para Err(Unknown), que o handler ignora — ou seja, o cosmic-settings não apaga o papel dela sem querer. Mapear para Disabled(0) faria o cosmic-settings limpar o filtro no primeiro toggle.\n\n4. O cliente não pode usar a API gerada. Ou mande a request no fio cru (wayland_backend send_request com Argument::Uint), ou — mais simples e mais honesto — NÃO use o a11y para isso: grave num arquivo de config próprio e leia no compositor pelo ConfigWatchSource do calloop, que já está montado em src/config/mod.rs:177-182 e já entrega mudança a quente sem protocolo nenhum. Isso elimina de uma vez os itens (A), (B) e o round-trip com o cosmic-settings, e resolve o agendamento por horário (item C do pedido dela) com um systemd timer escrevendo o arquivo — sem cliente Wayland nenhum.\n\n5. Território: o patch do compositor é da Aurora (FRONTEIRA.md, linha da tabela do binário cosmic-comp). Ele tem de virar um segundo .patch em ~/.config/zsh/patches/ MAIS uma edição no compilar() do aurora-cosmic-comp-ws.sh para aplicar os dois. Aplique-o com um `patch -p1 --forward` próprio que, ao FALHAR, apenas AVISE e siga o build — nunca com o `return 3` compartilhado, para que um upstream que mexa no a11y.rs não derrube junto o patch de workspace.\n\n6. Antes de instalar: valide o .frag fora do compositor com `glslangValidator -S frag` (ou rode o build novo por `aurora-workspace.sh run` no backend winit, que usa o MESMO caminho de screen filter — src/backend/winit.rs update_screen_filter existe), em vez de descobrir o erro de GLSL com ela na frente da TV."
     }
    },
    {
     "nome": "Desligar o papel em tela cheia — por imagem, NÃO por FPS",
     "alvo": "agendamento",
     "como": "Se for feito, gatilho por janela em fullscreen ou por app na lista (vídeo/jogo), zerando paper_texture. Mas a justificativa correta não é desempenho: medi +0,27% de um quadro de 60 Hz, o que é irrelevante, e o branch desligado custa zero. A razão real é imagem: grão estático por cima de vídeo fica feio, e ruído por pixel destrói a compressão em gravação de tela, OBS e área de trabalho remota — o codificador passa a gastar bitrate com o meu ruído.",
     "onde": "Lado do cosmic-comp (onde o estado de fullscreen já existe) ou um daemon do MeowSystem que fale o protocolo a11y. Decidir pela docs/FRONTEIRA.md.",
     "a_quente": true,
     "custo": "Zero em GPU. Custo de complexidade: é mais uma máquina de estados que pode brigar com o agendamento por horário — sugiro deixar para depois de (A)(B)(C) funcionarem.",
     "durabilidade": "Depende de onde for implementado; se for no cosmic-comp, morre no apt upgrade junto com o resto.",
     "trilha": "glsl-textura",
     "veredito": {
      "solucao": "glsl-textura / \"Desligar o papel em tela cheia — por imagem, não por FPS\": gatilho por janela em fullscreen (ou app na lista) zerando o uniform paper_texture, justificado por qualidade de imagem e por não estragar a compressão em gravação de tela/OBS/desktop remoto.",
      "veredito": "REFUTADO",
      "a_quente_confirmado": true,
      "porque": "O MECANISMO existe e vale a quente — a JUSTIFICATIVA é falsa nesta máquina, e o \"custa zero\" é falso na arquitetura.\n\n1) REFUTAÇÃO CENTRAL — o grão NUNCA chega em gravação, OBS ou desktop remoto.\nLi a fonte da versão instalada (/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094, mesmo SHA 5c93094 do binário vivo). Em src/backend/render/mod.rs:\n  - 1322: `postprocess_texture = Some(tex.clone())` — `tex` é o offscreen onde o `render_workspace` desenhou o desktop CRU;\n  - 1404: o shader só entra aqui, ao desenhar esse `tex` no framebuffer real, via TextureShaderElement com os uniforms `invert`/`color_mode`;\n  - 1514: para cada sessão de captura (`output.take_pending_frames()`), quando o filtro está ativo o blit para o buffer do cliente sai de `postprocess_texture` — o offscreen PRÉ-shader. Só quando não há filtro (1519) ele copia de `target`.\nOu seja: com filtro ligado, ext_image_copy_capture (portal → OBS, gravador de tela, desktop remoto, cosmic-screenshot) recebe a imagem SEM o pós-processamento. É upstream de propósito: a luz noturna dela também não aparece em print nenhum. Logo, \"ruído por pixel destrói a compressão em gravação\" não pode acontecer — o codificador nunca vê o meu ruído. Metade da razão declarada evapora.\n\n2) A OUTRA METADE É GOSTO, E GOSTO É DELA. \"Grão em cima de vídeo fica feio\" é juízo estético, e a folha dela manda o gosto ser escolha dela, não máquina de estados automática. Pior: fullscreen é um proxy RUIM de \"vídeo\". Leitor de PDF, quadrinho, e-book e visualizador de fotos em tela cheia são exatamente onde ela mais quereria o papel — e o gatilho desligaria justo ali. Sobraria a lista de apps, que é manutenção eterna.\n\n3) \"CUSTO ZERO\" É FALSO NO NÍVEL QUE IMPORTA. O caro não é o branch do fragment shader; é o pipeline de pós-processamento inteiro, que existe sempre que `ScreenFilter::is_noop()` for falso (config/mod.rs:159). Zerar um uniform mantém: (a) o passe extra de render para textura offscreen + composição de tela cheia a 1920x1080@60 (outputs.ron, TV PHILCO DP-2); (b) a perda de scanout direto do cliente em tela cheia, porque o elemento final vira uma textura nossa com shader. Se o objetivo fosse desempenho em tela cheia, a única alavanca que paga é deixar o filtro NOOP de vez — o que apaga junto a luz noturna e daria um salto de cor visível ao entrar/sair do fullscreen.\n\n4) DEPENDE DE ALGO QUE NÃO EXISTE. Não há uniform `paper_texture` no binário vivo: `strings /usr/bin/cosmic-comp` mostra só o shader com `invert` e `color_mode` (a linha patchada da Aurora está lá: `if (color_mode == 1.0) { ... vec3(1.00000, 0.77987, 0.54468); }`). Isto é um adendo à trilha (B), não uma solução própria. E o \"+0,27% de um quadro de 60 Hz\" não é reproduzível aqui: não há shader de textura nesta máquina para medir.\n\n5) O QUE ESTÁ CERTO NA PROPOSTA (registro para não jogar fora): o sinal de fullscreen já é calculado POR QUADRO, no lugar certo, com o idioma pronto — src/backend/kms/surface/mod.rs:~1026, `let (has_active_fullscreen, ...) = { let shell = self.shell.read(); ... workspace.get_fullscreen(seat) ... }`, usado hoje para tirar ALLOW_OVERLAY_PLANE_SCANOUT. E os uniforms são remontados a cada quadro em render/mod.rs:1360-1380. Então \"vale a quente\" está PROVADO: não depende de recompilar shader nem de reiniciar sessão.\n\n6) FRONTEIRA E DONO. docs/FRONTEIRA.md linha 58: \"binário cosmic-comp (patches de workspace e night light) | Aurora | Aurora\". A proposta de \"um daemon do MeowSystem\" contraria o documento — isso é território Aurora, e o código mora em ~/.config/zsh (auto-commit a cada 10 min). A alternativa \"daemon que fala o protocolo a11y\" é tecnicamente possível (state.rs:762 registra `A11yState::new::<State,_>(dh, client_not_sandboxed)`, então qualquer cliente não-sandboxed pode fazer bind em cosmic_a11y_manager_v1 e chamar set_screen_filter), mas a granularidade do protocolo é só `inverted` + um enum de 4 filtros (a11y.rs:196-211): ele NÃO consegue zerar um uniform de papel. Só consegue desligar o filtro inteiro — e cada toggle reescreve ~/.local/state/cosmic-comp/a11y_screen_filter.ron e faz o interruptor de Acessibilidade piscar sozinho na tela dela.\n\n7) DURABILIDADE E ACOPLAMENTO. Se for para o cosmic-comp, morre em todo apt upgrade junto com o patch de workspace — e é o MESMO build: aurora-cosmic-comp-ws.sh tem MAX_TENTATIVAS=3 e desiste. Um patch maior tem mais chance de não aplicar quando o upstream mexer no render, e nesse dia ela perde os DOIS (volta o workspace vazio, além do papel).",
      "risco_real": "Se implementado e der errado, o modo de falha é o do build patchado, não o do toggle: um shader que não compila derruba `init_shaders`, e o `.expect(\"OffscreenShader should be available through init_shaders\")` (render/mod.rs:1354) vira pânico no primeiro quadro — sessão que não sobe, ela fica olhando o greeter em laço (o aurora-greeter-watchdog.sh já existe para esse caso). Um erro só de lógica (gatilho pisca, papel some/volta em loop) é feio, mas não tira o desktop.\n\nTEM VOLTA, por TTY (Ctrl+Alt+F3), e ela é curta:\n  sudo aurora-cosmic-comp-ws.sh --restore   (copia /var/lib/aurora/cosmic-comp-<versão>.pkg-orig por cima de /usr/bin/cosmic-comp)\n  ou, se faltar o .pkg-orig: sudo apt-get install --reinstall cosmic-comp\nConferi que os artefatos existem para a versão instalada: /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig (28.339.864 bytes) e o .aurora-ws. Custo do restore: volta o workspace vazio (\"3\" seco no painel) e some a luz noturna até o próximo self-heal.\n\nRisco menor, mas real, se ela mesma testar: nada aqui aparece em screenshot nem em gravação (item 1), então \"não estou vendo o efeito no print\" NÃO é prova de que o efeito caiu — é o comportamento normal. Isso vai gerar diagnóstico errado se não estiver escrito no doc.",
      "correcao": "Descartar esta trilha como está e, se algum dia (B) existir, guardar só o pedaço salvável:\n\na) Apagar a justificativa de gravação/OBS/compressão — é falsa aqui (render/mod.rs:1514). Se um dia ela QUISER o papel aparecendo na gravação, aí sim é preciso mexer: fazer o blit da captura sair de `target` em vez de `postprocess_texture`. Isso é uma mudança de comportamento upstream, não um \"desligar em fullscreen\".\n\nb) Não automatizar por fullscreen. O gatilho certo é o que ela já pediu: um controle e um horário. Se ela quiser suspender o papel, que seja o mesmo atalho/toggle da luz noturna (Acessibilidade > Filtros de cor), que já vale a quente hoje, sem estado novo brigando com o agendamento (C).\n\nc) Se, ainda assim, alguém quiser o gatilho automático: implementar no cosmic-comp (é dono Aurora pela FRONTEIRA.md, não MeowSystem), lendo o fullscreen no lugar onde ele JÁ é lido por quadro — src/backend/kms/surface/mod.rs:~1026, `workspace.get_fullscreen(seat)` — e não inventando um daemon que espia toplevels. E, se a motivação virar desempenho, o único gate que paga é o filtro inteiro virar noop, não um uniform zerado.\n\nd) Ordem: só depois de (A)(B)(C) de pé, e num commit separado do patch de workspace, para uma falha de rebase não derrubar os dois."
     }
    },
    {
     "nome": "Arnês de validação de shader antes de gravar no binário",
     "alvo": "build",
     "como": "Rodar o script do último item de \"codigo\" contra qualquer offscreen.frag antes de instalá-lo. Ele cria um contexto EGL surfaceless no dispositivo NVIDIA (EGL_PLATFORM_DEVICE_EXT) e compila o shader em GLSL ES 1.00 no MESMO driver que o cosmic-comp usa, nas 8 combinações de #define, sem abrir janela e sem instalar pacote nenhum (usa só libEGL/libGLESv2 + ctypes). Trocar __EGL_VENDOR_LIBRARY_FILENAMES para o 50_mesa.json roda a mesma bateria no compilador do Mesa, que é mais rigoroso em alguns pontos.",
     "onde": "Hoje em /tmp/meow-trabalho,nvpatch.py}. Se virar permanente, é ferramenta de build do MeowSystem — não de ~/.config/zsh.",
     "a_quente": true,
     "custo": "~1 s por bateria. Nenhuma dependência nova: glslang-tools NÃO está instalado nesta máquina e não precisa ser.",
     "durabilidade": "Independente de apt upgrade e de tema. Só depende de libEGL/libGLESv2, que sempre existem.",
     "trilha": "glsl-textura",
     "veredito": {
      "solucao": "glsl-textura — Arnês de validação de shader (EGL surfaceless + GLES2 via ctypes) antes de gravar no binário do cosmic-comp",
      "veredito": "CONFIRMADO",
      "a_quente_confirmado": true,
      "porque": "O QUE SE SUSTENTOU (rodado, não deduzido):\n\n1. Os arquivos existem e rodam. `nvpatch.py` (4971 B) e `glescheck.py` (3972 B) estão no scratchpad. `libEGL.so.1` e `libGLESv2.so.2` existem em /usr/lib/x86_64-linux-gnu. `glslang-tools` de fato NÃO está instalado (dpkg-query: \"não foram encontrados pacotes\") e não foi preciso.\n\n2. É o driver REAL. `nvpatch.py` via EGL_PLATFORM_DEVICE_EXT: \"EGL devices: 3\", GL_RENDERER = NVIDIA GeForce RTX 4060/PCIe/SSE2, NVIDIA 580.173.02. Confere com o cosmic-comp vivo (pid 3127), cujo /proc/3127/maps mapeia libEGL_nvidia.so.580.173.02, libnvidia-eglcore, libnvidia-glsi e /dev/nvidia0. Mesmo compilador. Confirmado.\n\n3. Custo menor que o alegado: 0,19 s (nvpatch) e 0,08 s (glescheck), não ~1 s. Nenhuma janela abriu — surfaceless, não pisa na tela dela.\n\n4. Pega erro de verdade. Em 6 sondas com `#version 100`: reprovou texelFetch (\"requires #version 130\"), `in`/`out` (\"out can't be used with non-varying\"), `inverse()` (\"requires #version 140\"), `round()` (\"requires #version 130\") e erro de sintaxe. Não é um carimbo de OK.\n\n5. Pegou um defeito real agora. Rodei os 8 combos de #define sobre o `shader_live.frag` (extração do binário vivo): TODOS os 8 reprovaram com \"unexpected $end\". Motivo: o arquivo está truncado, termina em `}precisio` — a extração via strings colou na string seguinte. Se aquilo tivesse ido pro binário, ela perdia a máquina. O arnês barrou.\n\n6. Os 8 combos do fonte limpo (cosmic-comp/src/backend/render/shaders/offscreen.frag, 2415 B) passam nos dois compiladores, INCLUSIVE as 4 variantes EXTERNAL — `samplerExternalOES` + `#extension GL_OES_EGL_image_external` compilam no arnês. Era o risco maior e caiu.\n\n7. Convive com o patch de workspace. md5 de /usr/bin/cosmic-comp (33571456 B) difere de .orig E de .aurora-ws, mas tem o tamanho exato do .aurora-ws — ou seja, o instalado é o build aurora-ws com o night light patchado por cima, in place. O arnês não compete com isso, só lê .frag.\n\n8. Sobrevive a apt upgrade/reboot/tema: depende só de libEGL/libGLESv2 e de ctypes. Um upgrade troca o binário do cosmic-comp (e derruba o patch), mas não quebra o arnês.\n\nO QUE NÃO SE SUSTENTOU (5 furos medidos):\n\nA) NÃO LINKA — este é o furo grave. `grep -c 'glLinkProgram|glCreateProgram|VERTEX_SHADER'` = 0 nos dois scripts. Provei que compilar não basta: um fragment com 600 varyings COMPILA com status OK e log vazio, e depois FALHA no link (\"error C5041: cannot locate suitable resource to bind variable x32\"). MAX_VARYING_VECTORS = 31 nesta GPU. O cosmic-comp faz `compile_custom_texture_shader(...)?` dentro de `init_shaders`, que é `Result` e sobe por `.context(\"Failed to compile shaders\")?` — falha de link mata a subida do renderer. Um shader de \"textura Kindle\" que acrescente varyings passa no arnês e derruba o desktop.\n\nB) As \"8 combinações de #define\" NÃO estão no script. `grep -c 'EXTERNAL|NO_ALPHA|DEBUG_FLAGS|define'` = 0 nos dois. Eu tive de gerar os 8 na mão com awk. Como está, o arnês valida o texto com `//_DEFINES_` intacto — que não é o que o compositor compila.\n\nC) A receita do Mesa está ERRADA como escrita. `__EGL_VENDOR_LIBRARY_FILENAMES=.../50_mesa.json python3 nvpatch.py` dá \"EGL devices: 2 / FALHA: eglInitialize 0x3001\". Só funciona com `DEV=1`. E o argumento \"Mesa é mais rigoroso\" não se demonstrou: o único caso que a NVIDIA deixou passar — laço com contagem variável (`for(i=0;i<n;i++)` com `n` uniform, proibido pelo Apêndice A do GLSL ES 1.00) — o Mesa TAMBÉM aprovou. Nenhum dos dois faz conformância de spec; ambos fazem \"o que este driver aceita\".\n\nD) `glescheck.py` valida no compilador ERRADO e não avisa. Ele usa eglGetDisplay(EGL_DEFAULT_DISPLAY) e caiu em \"llvmpipe (LLVM 20.1.2)\" — Mesa por software, não a NVIDIA que o cosmic-comp usa. Dos dois arquivos entregues, um é a ferramenta certa e o outro mente em silêncio.\n\nE) Ele guarda o arquivo errado. A entrega real não é um .frag: é uma janela de 195 bytes, no offset 0x2dc16a de /usr/bin/cosmic-comp, sobrescrita in place com padding de espaços pelo `aurora-night-light.py` (`build_block(rgb, width)`, `assert len(data) == size_before`). O arnês nunca checa (i) se o bloco novo CABE em 195 bytes, nem (ii) se o que ficou GRAVADO ainda compila. O item 5 acima é a prova de que extração/reinjeção de string em ELF corrompe na prática.\n\nFRONTEIRA: docs/FRONTEIRA.md linha 58 dá o binário cosmic-comp e os patches de workspace e night light à **Aurora**. O arnês em si não escreve nada, então morar no MeowSystem não conflita — mas quem o chama é o `aurora-night-light.py`, em ~/.config/zsh (auto-commit de 10 em 10 min). E /mnt/Apate é montado com `nofail` (fstab linha 12): pode não estar montado. Se o gate sumir, o patch não pode seguir em frente.",
      "risco_real": "O que quebra: o shader é compilado em `init_shaders` (backend/render/mod.rs:392), que retorna Result e é consumido em kms/device.rs:1119 com `.context(\"Failed to compile shaders\")?`. Shader inválido = renderer não sobe = sem desktop.\n\nE é pior do que \"sem desktop\": `/usr/bin/cosmic-greeter-start` é `exec cosmic-comp cosmic-greeter`. A TELA DE LOGIN roda dentro do cosmic-comp. Só existe uma sessão instalada (/usr/share/wayland-sessions/cosmic.desktop) e /usr/share/xsessions/ não existe. Binário ruim = nem desktop, nem greeter, nem sessão alternativa. Ela vê tela preta no boot.\n\nMAS TEM VOLTA, e é sólida — três rotas independentes, todas verificadas agora:\n\n1. TTY. NAutoVTs está no padrão 6 (`#NAutoVTs=6`, comentado em /etc/systemd/logind.conf), /dev/tty2 e /dev/tty3 existem, getty@tty1 enabled. Ctrl+Alt+F2 dá console. O greetd ocupa a vt=1, então usar F2..F6.\n\n2. SSH. `systemctl is-active ssh` = active, escutando em 0.0.0.0:22 e [::]:22. Rota de resgate de outra máquina, sem depender da tela dela.\n\n3. Backups do binário exato. /var/lib/aurora tem, para a versão instalada 0.1~1787767625~24.04~5c93094: `.orig` (28339864 B, upstream), `.pkg-orig` (idem) e `.aurora-ws` (33571456 B, o build com o patch de workspace). O conserto de TTY/SSH é uma linha:\n   sudo cp /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws /usr/bin/cosmic-comp\n(usar o `.aurora-ws`, não o `.orig` — o `.orig` traz de volta o workspace vazio a mais.) Depois `sudo systemctl restart greetd`.\n\nIsso NÃO é eliminatório. Mas o caminho de volta tem de estar escrito num papel/celular ANTES do primeiro patch, porque no momento em que ela precisar dele não vai ter navegador.\n\nRisco menor, mas real: o arnês não abre janela e não toca no compositor vivo (0,19 s, surfaceless) — rodá-lo é seguro em qualquer hora, inclusive com ela usando a máquina.",
      "correcao": "Cinco consertos. Sem os dois primeiros o arnês dá falso OK e ela perde o desktop.\n\n1) LINKAR, não só compilar (obrigatório). Acrescentar ao `nvpatch.py`, depois do glCompileShader: montar um vertex shader compatível com o do smithay\n   #version 100\n   attribute vec2 vert; attribute vec4 tex_position;\n   varying vec2 v_coords;\n   void main(){ v_coords = tex_position.xy; gl_Position = vec4(vert,0.0,1.0); }\ne rodar glCreateProgram/glAttachShader/glLinkProgram, checando GL_LINK_STATUS + glGetProgramInfoLog. Sem isso o caso dos 600 varyings passa. Depois do link, conferir com glGetUniformLocation que `invert` e `color_mode` ainda existem (são os UniformName que o mod.rs:420-424 registra) — se o novo shader otimizar um deles pra fora, a leitura vira -1.\n\n2) Gerar os 8 combos DENTRO do script. O `//_DEFINES_` tem de ser substituído pelo produto de EXTERNAL × NO_ALPHA × DEBUG_FLAGS, e as 8 baterias têm de ser compiladas E linkadas. Sair com código != 0 se qualquer uma falhar. Hoje isso é trabalho manual do chamador e vai ser esquecido.\n\n3) Validar o BINÁRIO, não o .frag. O gate correto roda DEPOIS do patch e ANTES de instalar: ler /usr/bin/cosmic-comp candidato, achar o bloco pelas mesmas âncoras do `find_block` (`uniform float color_mode;` → `    if (color_mode == 1.0) {` → `    } else if (color_mode >= 2.0) {`), extrair o shader inteiro dali, expandir os 8 combos e compilar+linkar. Só instalar se passar. E checar antes o orçamento: a janela é de 195 bytes (offset 0x2dc16a) — se o bloco novo não couber, o `build_block` já devolve None, mas o arnês deve dizer isso em português, porque é o limite que vai matar a ideia de \"textura Kindle\" por patch de binário (195 bytes de GLSL não pagam ruído/dither; isso vai ter de ir por recompilação com `aurora-cosmic-comp-ws.sh --build`).\n\n4) Consertar a receita do Mesa e aposentar o glescheck.py. A linha certa é\n   DEV=1 __EGL_VENDOR_LIBRARY_FILENAMES=/usr/share/glvnd/egl_vendor.d/50_mesa.json python3 nvpatch.py ...\n(sem DEV=1 dá eglInitialize 0x3001). E `nvpatch.py` deve ABORTAR se GL_RENDERER não contiver \"NVIDIA\" quando a bateria for a principal — foi assim que o glescheck.py validou em llvmpipe sem avisar. Manter Mesa só como bateria secundária, e parar de afirmar que ele é mais rigoroso: nos testes de agora ele empatou com a NVIDIA e deixou passar o mesmo laço de contagem variável.\n\n5) Falhar fechado, e escrever a volta. Como /mnt/Apate é `nofail`, o `aurora-night-light.py` tem de RECUSAR o patch se o arnês não estiver acessível — nunca pular a validação em silêncio. E documentar no cabeçalho dele as três rotas de resgate (Ctrl+Alt+F2; ssh; `cp` do `.aurora-ws`), porque a tela de login também morre junto.\n\nObservação de honestidade técnica: pare de chamar isso de \"valida GLSL ES 1.00\". Nenhum dos dois compiladores aplica o Apêndice A da spec — ambos aceitaram `for(int i=0;i<n;i++)` com `n` uniform. O que o arnês prova é \"este driver, nesta máquina, aceita e linka\" — que é exatamente a pergunta que importa, e é uma garantia mais forte que a do glslangValidator para o caso dela. Só não é a garantia que o texto da proposta anuncia."
     }
    }
   ]
  },
  {
   "trilha": "extensoes-gnome",
   "achados": {
    "trilha": "Extensões do GNOME que ela usava (temperatura com slider+agendamento, e \"textura\"/e-ink) e o método portável de cada uma",
    "resumo": "Achei as duas famílias de extensão que explicam o \"no gnome eu consigo fazer isso\". Para TEMPERATURA, todas as candidatas (Night Light Slider, Night Light Slider Updated, Night Light Scheduler) são apenas front-ends de gsettings: nenhuma tem shader próprio — quem aplica é o gsd-color via gamma ramp, caminho que NÃO existe no COSMIC (confirmei por medição: zero ocorrências de zwlr_gamma_control no binário e o global não aparece no wayland-info). Para TEXTURA, a extensão certa NÃO é a \"E Ink Mode\" (que é só troca de tema, arquivada): é a Bedtime Mode, que aplica Clutter.DesaturateEffect + Clutter.BrightnessContrastEffect no Main.layoutManager.uiGroup — efeito de TELA INTEIRA, com preset \"sepia\" e agendamento próprio. Extraí o GLSL do Clutter e converti os presets em constantes prontas para o offscreen.frag. O achado que muda o jogo é local, não web: o /usr/bin/cosmic-comp que está rodando JÁ É a build recompilada da Aurora (a árvore de fontes está em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/...), o uniform color_mode já é _1f, e o Smithay aceita uniforms extras via UniformName — ou seja, slider contínuo de verdade é edição de 3 pontos + os ~4 min de build que ela já paga. E medi que o orçamento do patch in-place não é 46 bytes: são 1666 se o daltonismo for sacrificado junto, o que já dá 4 presets quentes + bloco de papel sem recompilar nada.",
    "achados": [
     {
      "titulo": "A extensão de \"textura\" dela é a Bedtime Mode, não a E Ink Mode",
      "detalhe": "A \"E Ink Mode\" (extensions.gnome.org/3957, fujimo-t) NÃO tem shader: ela só troca o tema do Shell/GTK/ícones para a variante High Contrast, desliga animações e põe o fundo branco — e está arquivada, com o autor mandando migrar para um tema. Quem faz o efeito de verdade é a \"Bedtime Mode\" (ionutbortis/gnome-bedtime, extensions.gnome.org/4012): aplica Clutter.DesaturateEffect + Clutter.BrightnessContrastEffect em Main.layoutManager.uiGroup (tela inteira, inclui vídeo e jogos), com 6 presets (grayscale, amber, green, cyan, sepia, red), fator percentual 0-100, transição suave de 25ms por passo e AGENDAMENTO próprio. É o pacote (B)+(C) numa extensão só — bate exatamente com o que ela descreveu.",
      "evidencia": "gh api repos/ionutbortis/gnome-bedtime/contents/src/modules/Colorizer.js e src/model/Presets.js; https://github.com/fujimo-t/gnome-shell-extension-e-ink-mode",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Nenhuma extensão de temperatura tem shader — todas são front-end de gsettings",
      "detalhe": "Confirmei nas três: kiyui/gnome-shell-night-light-slider-extension (extension.js linha 36: const COLOR_SCHEMA = 'org.gnome.settings-daemon.plugins.color'; linha 151: set_uint('night-light-temperature')), o fork mantido vilsbeg/night-light-slider-updated (extensions.gnome.org/6781), e StorageB/night-light-scheduler (extension.js linha 33 mesma schema, linha 137 mesmo set_uint). Ou seja: NÃO existe GLSL para portar do lado da temperatura. O que dá para portar é o MODELO (faixa, passo, agendamento), não o método.",
      "evidencia": "gh api repos/kiyui/...:extension.js linhas 36/143/151/216/232-236; gh api repos/StorageB/night-light-scheduler:extension.js linhas 33/134/137/139",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O truque de \"night light permanente\" do Night Light Slider: empurrar a janela do agendamento",
      "detalhe": "Para manter o night light ligado o dia todo sem tocar em night-light-enabled, o kiyui desliga night-light-schedule-automatic e, de hora em hora, reescreve schedule-from = agora-6h e schedule-to = agora+6h. A janela sempre cobre o \"agora\". É um padrão portável para qualquer agendador que só tenha um par de horários e um timer.",
      "evidencia": "extension.js linhas 216-236 (NightLightSchedule): set_boolean('night-light-schedule-automatic', false); set_double('night-light-schedule-to', to.getHours()); set_double('night-light-schedule-from', from.getHours())",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Faixa e passo do GNOME: UI 2700-6500K passo 100, backend 1000-10000K",
      "detalhe": "O slider do gnome-control-center usa GtkAdjustment lower=2700 upper=6500 step_increment=100 page_increment=500, com marcas em 4000K e 5250K. O backend (gsd-color-state.h) permite bem mais: GSD_COLOR_TEMPERATURE_MIN 1000, DEFAULT 6500 (RGB 1,1,1), MAX 10000. O default da chave night-light-temperature é 2700 (li o gschema instalado nesta máquina). As extensões alargam: o Night Light Slider usa minimum=1500/maximum=5000 por padrão; o Night Light Scheduler usa 1500-6500 em incrementos de 15min, com transição de 0 a 120min. Para calibrar igual ao que ela conhece: 2700-6500 passo 100 é o \"GNOME de fábrica\"; 1500-6500 é o que as extensões dela ofereciam.",
      "evidencia": "/usr/share/glib-2.0/schemas/org.gnome.settings-daemon.plugins.color.gschema.xml (local); https://mail.gnome.org/archives/commits-list/2018-October/msg05691.html; gsd-color-state.h; schemas/org.gnome.shell.extensions.nightlightslider.gschema.xml",
      "confianca": "medido"
     },
     {
      "titulo": "Gamma ramp está fora no COSMIC — remedi e confirmei",
      "detalhe": "O GNOME aplica night light pelo gsd-color, que empurra a gamma/CTM para o mutter (SetCrtcGamma). No cosmic-comp isso não existe: `strings /usr/bin/cosmic-comp | grep -ic 'zwlr_gamma_control|gamma_control_manager'` retorna 0, e o wayland-info da sessão viva não lista nenhum global de gamma ou color management. Confirma (por medição independente, hoje) a nota que já estava no aurora-night-light.py. Redshift/gammastep/wlsunset e wl-gammactl são inúteis aqui — não é questão de configurar, o protocolo não está lá.",
      "evidencia": "strings -n 4 /usr/bin/cosmic-comp | grep -icE 'zwlr_gamma_control|gamma_control_manager' -> 0; wayland-info | grep cosmic (lista completa sem gamma)",
      "confianca": "medido"
     },
     {
      "titulo": "O binário que está rodando é a build recompilada da Aurora, e a árvore de fontes está no disco",
      "detalhe": "/usr/bin/cosmic-comp tem 33571456 bytes — o mesmo tamanho do /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws (o md5 difere só porque o patch do shader foi aplicado por cima). As strings do binário carregam caminhos de build /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/. A árvore existe, com src/backend/render/shaders/offscreen.frag e até um mod.rs.orig ao lado do mod.rs patchado. Isto é decisivo: \"recompilar\" não é um caminho novo a inaugurar, é o caminho que já está em produção nesta máquina.",
      "evidencia": "ls -la /usr/bin/cosmic-comp; md5sum; find /mnt/Apate/Desenvolvimento/cosmic-comp-patch -name offscreen.frag",
      "confianca": "medido"
     },
     {
      "titulo": "O uniform color_mode JÁ é float, e o Smithay aceita uniforms extras — slider contínuo é edição de 3 pontos",
      "detalhe": "Em src/backend/render/mod.rs:422-423 o postprocess_shader é compilado com UniformName::new(\"invert\", UniformType::_1f) e UniformName::new(\"color_mode\", UniformType::_1f). Em mod.rs:1364-1380 os valores são passados por Uniform::new(...). Adicionar um uniform contínuo (ex.: temp_rgb como _3f, paper e grain como _1f) é: (1) mais uma linha em cada uma dessas duas listas, (2) o bloco GLSL no offscreen.frag, (3) uma chave nova em src/config/mod.rs — que herda de graça o ConfigWatchSource do cosmic_config (\"com.system76.CosmicComp\", 1) já plugado no calloop em config/mod.rs:176-179. Nada disso exige tocar no protocolo Wayland.",
      "evidencia": "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/render/mod.rs:422,423,1364-1380; src/config/mod.rs:167-179",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O orçamento do patch in-place é 1666 bytes, não 46",
      "detalhe": "Medi no binário vivo: o bloco do color_mode==1.0 tem exatamente 160 bytes (o mesmo do fonte original do greyscale), dos quais o night light usa 114 — sobram só 46. MAS o bloco vizinho `} else if (color_mode >= 2.0) {` (todo o daltonismo) tem 1506 bytes. Do `if (color_mode == 1.0)` até o `// re-multiply` são 1666 bytes contíguos de texto GLSL reescrevíveis preservando o tamanho do ELF. Se o daltonismo for sacrificado (ela não é daltônica, e o greyscale já foi), cabe uma escada de 4 presets de temperatura MAIS um bloco inteiro de papel/grão — sem recompilar nada.",
      "evidencia": "python3 medindo offsets em /usr/bin/cosmic-comp entre b'    if (color_mode == 1.0) {' e b'    // re-multiply' -> 1666 bytes; bloco daltonismo -> 1506",
      "confianca": "medido"
     },
     {
      "titulo": "O protocolo a11y está vivo em versão 2 e é o que troca o filtro A QUENTE",
      "detalhe": "wayland-info na sessão dela lista `cosmic_a11y_manager_v1, version 2, name 53`. cosmic-settings e cosmic-applet-a11y falam esse protocolo (2 ocorrências cada nas strings); cosmic-osd não. Como a versão viva é 2 e não 3, o request disponível é set_screen_filter(inverted, filter) — set_screen_filter2 (com filter_state) só existe a partir da v3, então NÃO usar. Um cliente Wayland minúsculo ligado nesse global troca o filtro sem logout: é o motor do agendamento.",
      "evidencia": "WAYLAND_DISPLAY=wayland-1 wayland-info | grep cosmic_a11y -> version 2; strings /usr/bin/cosmic-settings | grep -c cosmic_a11y_manager_v1 -> 2; https://wayland.app/protocols/cosmic-a11y-unstable-v1",
      "confianca": "medido"
     },
     {
      "titulo": "Cuidado: os dois enums de filtro NÃO batem",
      "detalhe": "O enum interno do Rust (src/config/mod.rs:167-172) é ColorFilter { Greyscale = 1, Protanopia = 2, Deuteranopia = 3, Tritanopia = 4 } — e é esse número que vira o float color_mode no shader. O enum do PROTOCOLO Wayland é outro: disabled=0, unknown=1, greyscale=2, daltonize_protanopia=3, daltonize_deuteranopia=4, daltonize_tritanopia=5. Quem escrever o cliente de agendamento tem que mandar 2 para acertar o color_mode 1.0. Errar isso liga o filtro errado silenciosamente.",
      "evidencia": "src/config/mod.rs:167-172 vs https://wayland.app/protocols/cosmic-a11y-unstable-v1",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O que a \"Textura\" do MIUI é de fato: grão dinâmico + dessaturação",
      "detalhe": "O modo Papel/Textura do MIUI/HyperOS adiciona um efeito de \"grão\" por software sobre os pixels (mesma ideia do NXTPAPER da TCL), que quebra a aparência lisa do OLED e imita a página de um livro; combinado com dessaturação, o resultado lembra E-Ink colorido tipo Kindle Colorsoft. Não é filtro de cor — é overlay de ruído + dessaturação + tom de papel. Isso mapeia 1:1 no trio da Bedtime Mode (desaturate + brightness/contrast sepia) mais um hash de ruído no fragment shader.",
      "evidencia": "https://www.androidauthority.com/my-favorite-display-feature-for-night-use-3582060/ e https://xiaomitime.com/xiaomis-most-interesting-hidden-feature-turn-screen-into-paper-89545/",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Armadilha do grão: v_coords é 0..1 e o Smithay NÃO dá `size` para texture shader",
      "detalhe": "Duas ciladas que arrebentariam um porte ingênuo. (1) O hash clássico de Shadertoy pressupõe coordenada em PIXELS; o offscreen.frag só tem `varying vec2 v_coords` normalizado 0..1, então fract(sin(dot(v_coords,...)))*43758.5453 produz um DEGRADÊ suave, não grão — é preciso escalar (v_coords * 4096.0) ou passar o tamanho da tela. (2) O `uniform vec2 size` do Smithay existe só para compile_custom_pixel_shader (documentado em gles/mod.rs:2002, setado em mod.rs:3185 dentro de render_pixel_shader_to); o postprocess usa compile_custom_texture_shader, que NÃO recebe size. Declarar `uniform vec2 size` ali dá um uniform nunca escrito (lixo/zero).",
      "evidencia": "vendor/smithay/src/backend/renderer/gles/mod.rs:2002,2049,2086,3185 e 2143-2160 (compile_custom_texture_shader); offscreen.frag linha 17",
      "confianca": "medido"
     },
     {
      "titulo": "O agendamento da Bedtime é um laço de 2s com fórmula de virada de meia-noite",
      "detalhe": "Scheduler.js roda um loop de 2000ms e testa: `scheduleEnd >= scheduleStart ? now >= scheduleStart && now < scheduleEnd : now >= scheduleStart || now < scheduleEnd`, com now = hora + minuto/60. As chaves são schedule-start-hours/minutes e schedule-end-hours/minutes (default 22:00 -> 07:00). É exatamente o mesmo modelo do GNOME nativo (night-light-schedule-from/to como double de horas fracionárias, default 20.00 -> 6.00), só que em inteiros separados. Vale copiar a fórmula: é a parte que todo mundo erra na virada da meia-noite.",
      "evidencia": "gh api repos/ionutbortis/gnome-bedtime/contents/src/modules/Scheduler.js (#isCurrentTimeOnSchedule); gschema bedtime-mode; org.gnome.settings-daemon.plugins.color.gschema.xml local",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O GNOME faz transição suave em 5s a 50ms por passo",
      "detalhe": "gsd-night-light.c interpola linearmente a temperatura ao longo de GSD_NIGHT_LIGHT_SMOOTH_SMEAR (5 segundos), atualizando a cada 50ms (gsd_night_light_smooth_cb + linear_interpolate). A Bedtime usa 25ms por passo com 100 passos (=2,5s). Se quisermos que a mudança agendada não dê um susto na tela, é esse o número a copiar — e no nosso caso a interpolação sai de graça se o valor vier de um uniform contínuo.",
      "evidencia": "https://github.com/GNOME/gnome-settings-daemon/blob/master/plugins/color/gsd-night-light.c; Colorizer.js #transitionDelay = 25",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Existe precedente de efeito de tela inteira por ShaderEffect no GNOME — e o GLSL é portável",
      "detalhe": "A G-dH/gnome-colorblind-filters é o análogo mais direto do offscreen.frag: subclasses de Clutter.ShaderEffect (InversionEffect, ColorMixerEffect, DaltonismEffect) com GLSL próprio, aplicadas a um Clutter.Clone do Main.uiGroup adicionado ao global.stage — tela inteira. O GLSL dela usa cogl_tex_coord_in[0].st/cogl_color_out no lugar de v_coords/gl_FragColor; fora isso o corpo é idêntico em espírito ao do cosmic-comp (inclusive as MESMAS matrizes LMS de daltonismo). Traduzir é trocar duas identificadores. A ShaderMenu (ErwanDouaille/gse-shader) faz o mesmo com .frag em arquivo e uniforms width/height/mouseX/mouseY/globalTime/slider — inclusive um uniform `slider`, prova de que slider contínuo por shader é rotina.",
      "evidencia": "gh api repos/G-dH/gnome-colorblind-filters/contents/shaders.js e extension.js (linhas 666-712, _uiGroupClone + Shell.Global.get().stage.add_child); https://github.com/ErwanDouaille/gse-shader",
      "confianca": "lido-na-fonte"
     }
    ],
    "codigo": [
     {
      "para_que": "ESCADA DE PRESETS + PAPEL, tudo dentro do patch in-place (SEM recompilar). Substitui o trecho do color_mode==1.0 até logo antes do `// re-multiply` no offscreen.frag embutido. Cabe folgado nos 1666 bytes medidos; sacrifica os filtros de daltonismo (o greyscale já foi). Os 4 modos passam a ser trocáveis A QUENTE pelo applet de acessibilidade / pelo cliente Wayland.",
      "linguagem": "glsl",
      "fonte": "autoral — temperaturas da tabela redshift já embutida em ~/.config/zsh/scripts/aurora-night-light.py (WHITEPOINTS); tom de papel derivado dos presets do ionutbortis/gnome-bedtime; luminância 0.299/0.587/0.114 do clutter-desaturate-effect.c",
      "trecho": "    if (color_mode == 1.0) {        // LEITURA 3500K (Aurora)\n        color.rgb *= vec3(1.00000, 0.77987, 0.54468);\n    } else if (color_mode == 2.0) { // LEITURA 2500K (bem quente)\n        color.rgb *= vec3(1.00000, 0.64546, 0.28802);\n    } else if (color_mode == 3.0) { // LEITURA 4500K (leve)\n        color.rgb *= vec3(1.00000, 0.87325, 0.74112);\n    } else if (color_mode == 4.0) { // PAPEL / KINDLE\n        float gray = dot(color.rgb, vec3(0.299, 0.587, 0.114));\n        color.rgb = mix(color.rgb, vec3(gray), 0.85);\n        color.rgb = color.rgb * vec3(0.874016, 1.0, 0.748031)\n                  + vec3(0.125984, 0.0, 0.0);\n        color.rgb = (color.rgb - 0.5) * vec3(1.220426, 1.0, 1.0) + 0.5;\n        float n = fract(sin(dot(v_coords * 4096.0,\n                                vec2(12.9898, 78.233))) * 43758.5453);\n        color.rgb += (n - 0.5) * 0.035;\n        color.rgb = clamp(color.rgb, 0.0, 1.0);\n    }"
     },
     {
      "para_que": "SLIDERS DE VERDADE (contínuos) no offscreen.frag — versão para recompilar. temp_rgb, paper e grain viram uniforms alimentados pela config, então mudar o valor aplica no frame seguinte, sem logout.",
      "linguagem": "glsl",
      "fonte": "autoral, em cima do offscreen.frag de /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/render/shaders/offscreen.frag",
      "trecho": "// --- declarar junto dos uniforms existentes (linha ~24) ---\nuniform float invert;\nuniform float color_mode;\nuniform vec3  temp_rgb;    // whitepoint da temperatura; vec3(1,1,1) = neutro\nuniform float paper;       // 0.0 = sem papel .. 1.0 = papel cheio\nuniform float grain;       // 0.0 .. ~0.06 -- intensidade do grao\nuniform vec2  screen_size; // px; PRECISA ser adicionado, o Smithay nao da\n\n// --- dentro do main(), no lugar do bloco color_mode == 1.0 ---\n    if (color_mode == 1.0) {\n        // (A) TEMPERATURA -- continuo\n        color.rgb *= temp_rgb;\n\n        // (B) TEXTURA / PAPEL -- continuo\n        if (paper > 0.0) {\n            float gray = dot(color.rgb, vec3(0.299, 0.587, 0.114));\n            color.rgb = mix(color.rgb, vec3(gray), paper);\n\n            vec3 b_mul  = mix(vec3(1.0), vec3(0.874016, 1.0, 0.748031), paper);\n            vec3 b_off  = vec3(0.125984, 0.0, 0.0) * paper;\n            vec3 c_gain = mix(vec3(1.0), vec3(1.220426, 1.0, 1.0), paper);\n            color.rgb = color.rgb * b_mul + b_off;\n            color.rgb = (color.rgb - 0.5) * c_gain + 0.5;\n        }\n        if (grain > 0.0) {\n            // coordenada em PIXEL -- v_coords sozinho da degrade, nao grao\n            vec2 px = v_coords * screen_size;\n            float n = fract(sin(dot(px, vec2(12.9898, 78.233))) * 43758.5453);\n            color.rgb += (n - 0.5) * grain;\n        }\n        color.rgb = clamp(color.rgb, 0.0, 1.0);\n    } else if (color_mode >= 2.0) {\n        // ... daltonismo original, intacto ..."
     },
     {
      "para_que": "Fiação em Rust dos uniforms novos — os DOIS únicos pontos a editar no render, mais a leitura da config. Sem isso o GLSL acima linka com uniforms nunca escritos (valem zero).",
      "linguagem": "rust",
      "fonte": "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/render/mod.rs (linhas 419-424 e 1360-1381)",
      "trecho": "// (1) mod.rs ~419: declarar os uniforms na compilacao do shader\nlet postprocess_shader = renderer.compile_custom_texture_shader(\n    POSTPROCESS_SHADER,\n    &[\n        UniformName::new(\"invert\",      UniformType::_1f),\n        UniformName::new(\"color_mode\",  UniformType::_1f),\n        UniformName::new(\"temp_rgb\",    UniformType::_3f),  // NOVO\n        UniformName::new(\"paper\",       UniformType::_1f),  // NOVO\n        UniformName::new(\"grain\",       UniformType::_1f),  // NOVO\n        UniformName::new(\"screen_size\", UniformType::_2f),  // NOVO\n    ],\n)?;\n\n// (2) mod.rs ~1362: passar os valores por frame\nlet texture_elem = TextureShaderElement::new(\n    texture_elem,\n    postprocess_texture_shader.0.clone(),\n    vec![\n        Uniform::new(\"invert\", if screen_filter.filter.inverted { 1. } else { 0. }),\n        Uniform::new(\n            \"color_mode\",\n            screen_filter.filter.color_filter.map(|v| v as u8 as f32).unwrap_or(0.),\n        ),\n        // NOVOS -- vindos da config (hot-reload de graca pelo ConfigWatchSource)\n        Uniform::new(\"temp_rgb\",    reading.temp_rgb),   // [f32; 3]\n        Uniform::new(\"paper\",       reading.paper),\n        Uniform::new(\"grain\",       reading.grain),\n        Uniform::new(\"screen_size\", [w as f32, h as f32]),\n    ],\n);"
     },
     {
      "para_que": "Converter Kelvin -> vec3 no lado Rust, para o uniform temp_rgb. Mesma tabela de whitepoints do redshift/gammastep que o aurora-night-light.py já usa, só que interpolada em runtime em vez de escrita no binário.",
      "linguagem": "rust",
      "fonte": "autoral; tabela WHITEPOINTS de ~/.config/zsh/scripts/aurora-night-light.py (que a copiou do colorramp.c do redshift)",
      "trecho": "const WHITEPOINTS: [(u32, [f32; 3]); 12] = [\n    (1000, [1.00000, 0.18172, 0.00000]),\n    (1500, [1.00000, 0.42101, 0.00000]),\n    (2000, [1.00000, 0.54862, 0.08754]),\n    (2500, [1.00000, 0.64546, 0.28802]),\n    (3000, [1.00000, 0.71760, 0.42107]),\n    (3500, [1.00000, 0.77987, 0.54468]),\n    (4000, [1.00000, 0.82854, 0.64606]),\n    (4500, [1.00000, 0.87325, 0.74112]),\n    (5000, [1.00000, 0.90993, 0.82219]),\n    (5500, [1.00000, 0.94380, 0.89904]),\n    (6000, [1.00000, 0.97481, 0.96759]),\n    (6500, [1.00000, 1.00000, 1.00000]),\n];\n\nfn kelvin_to_rgb(k: u32) -> [f32; 3] {\n    let k = k.clamp(1000, 6500);\n    let mut prev = WHITEPOINTS[0];\n    for w in WHITEPOINTS {\n        if k <= w.0 {\n            if w.0 == prev.0 { return w.1; }\n            let f = (k - prev.0) as f32 / (w.0 - prev.0) as f32;\n            return [\n                prev.1[0] + (w.1[0] - prev.1[0]) * f,\n                prev.1[1] + (w.1[1] - prev.1[1]) * f,\n                prev.1[2] + (w.1[2] - prev.1[2]) * f,\n            ];\n        }\n        prev = w;\n    }\n    [1.0, 1.0, 1.0]\n}"
     },
     {
      "para_que": "Derivar as constantes de papel a partir de QUALQUER preset da Bedtime (sepia, amber, green...). É a conversão exata que o Clutter faz de byte para uniform — inclusive o tan() do contraste, que é o passo que ninguém adivinha.",
      "linguagem": "python",
      "fonte": "autoral; fórmulas de clutter-brightness-contrast-effect.c (get_brightness_values e update_uniforms); presets de gnome-bedtime/src/model/Presets.js",
      "trecho": "import math\n\ndef byte_to_float(b):            # clutter: color_byte / 127.0 - 1.0\n    return b / 127.0 - 1.0\n\ndef brightness_uniforms(v):      # get_brightness_values()\n    return (1.0 + v, 0.0) if v < 0 else (1.0 - v, v)\n\ndef contrast_gain(v):            # update_uniforms(): tan((v + 1) * PI/4)\n    return math.tan((v + 1.0) * math.pi / 4.0)\n\nPRESETS = {                      # gnome-bedtime/src/model/Presets.js\n    'grayscale': ((127,127,127), (127,127,127)),\n    'sepia':     ((143,127, 95), (143,127,127)),\n    'amber':     ((143, 71,  0), (143,135,127)),\n}\n\nfor name, (br, co) in PRESETS.items():\n    mul, off, gain = [], [], []\n    for i in range(3):\n        m, o = brightness_uniforms(byte_to_float(br[i]))\n        mul.append(m); off.append(o)\n        gain.append(contrast_gain(byte_to_float(co[i])))\n    print(name)\n    print('  b_mul  = vec3(%.6f, %.6f, %.6f)' % tuple(mul))\n    print('  b_off  = vec3(%.6f, %.6f, %.6f)' % tuple(off))\n    print('  c_gain = vec3(%.6f, %.6f, %.6f)' % tuple(gain))\n\n# sepia -> b_mul vec3(0.874016, 1.0, 0.748031)\n#          b_off vec3(0.125984, 0.0, 0.0)\n#          c_gain vec3(1.220426, 1.0, 1.0)"
     },
     {
      "para_que": "O teste de \"estou dentro do horário\" com virada de meia-noite — a parte que todo agendador caseiro erra. Portado da Bedtime para bash, pronto para o serviço do systemd timer.",
      "linguagem": "bash",
      "fonte": "portado de gh api repos/ionutbortis/gnome-bedtime/contents/src/modules/Scheduler.js (#isCurrentTimeOnSchedule)",
      "trecho": "# INICIO e FIM em horas fracionarias (ex.: 20.5 = 20:30), igual ao GNOME\n# (night-light-schedule-from/to sao 'd' = double, default 20.00 -> 6.00)\nna_janela() {\n  local inicio=\"$1\" fim=\"$2\"\n  local agora\n  agora=$(date +'%H %M' | awk '{printf \"%.4f\", $1 + $2/60}')\n  awk -v n=\"$agora\" -v s=\"$inicio\" -v e=\"$fim\" 'BEGIN{\n    if (e >= s) exit !(n >= s && n < e);   # janela no mesmo dia\n    else        exit !(n >= s || n < e);   # janela cruza a meia-noite\n  }'\n}\n\n# uso:\n# if na_janela 20.0 6.0; then ligar_filtro; else desligar_filtro; fi"
     },
     {
      "para_que": "Prova do mecanismo de tela inteira da Bedtime Mode — é este o \"como\" da textura no GNOME, e o motivo de eu afirmar que o análogo no COSMIC é o offscreen.frag e não um overlay.",
      "linguagem": "javascript",
      "fonte": "https://github.com/ionutbortis/gnome-bedtime — src/modules/Colorizer.js",
      "trecho": "const UiGroup = Main.layoutManager.uiGroup;\n\nconst COLORIZE_EFFECT_NAME = \"bedtime-mode-colorize-effect\";\nconst DESATURATE_EFFECT_NAME = \"bedtime-mode-desaturate-effect\";\n\n// ...\nthis.#colorizeEffect = new Clutter.BrightnessContrastEffect();\nthis.#desaturateEffect = new Clutter.DesaturateEffect();\nthis.#desaturateEffect.factor = 0;\n\n#addColorEffects() {\n  UiGroup.get_effect(COLORIZE_EFFECT_NAME)   || UiGroup.add_effect_with_name(COLORIZE_EFFECT_NAME, this.#colorizeEffect);\n  UiGroup.get_effect(DESATURATE_EFFECT_NAME) || UiGroup.add_effect_with_name(DESATURATE_EFFECT_NAME, this.#desaturateEffect);\n  this.#updateColorizeEffect();\n}\n\n#updateEffectsFactor() {\n  this.#colorTone.toneFactor = this.#transitionStep;      // 0..100\n  this.#desaturateEffect.factor = this.#transitionStep / 100;\n  this.#updateColorizeEffect();\n}\n\n// ColorTone.js -- interpolacao do preset pelo fator percentual:\n// #computeByFactor(v) { return Math.round(127 + ((v - 127) * this.#toneFactor) / 100); }"
     },
     {
      "para_que": "O truque do \"night light permanente\" do Night Light Slider (empurrar a janela de agendamento a cada hora). Serve de modelo caso a gente prefira reaproveitar um par from/to em vez de um flag booleano.",
      "linguagem": "javascript",
      "fonte": "https://github.com/kiyui/gnome-shell-night-light-slider-extension — night-light-slider.timur@linux.com/extension.js, linhas 216-236",
      "trecho": "const COLOR_SCHEMA = 'org.gnome.settings-daemon.plugins.color';\n\n// ligar o modo permanente\nthis._settings.set_boolean('night-light-schedule-automatic', false);\n// ... e reescrever a janela de hora em hora:\n\n// Set a schedule span of 6 hours to & from now\nthis._settings.set_double('night-light-schedule-to', to.getHours());\nthis._settings.set_double('night-light-schedule-from', from.getHours());\n\n// a temperatura em si, a partir do percentual do slider:\nconst temperature = percent * (maximum - minimum) + minimum;  // default 1500..5000\nthis._settings.set_uint('night-light-temperature', temperature);"
     }
    ],
    "solucoes": [
     {
      "nome": "Escada de 4 presets no patch in-place (entrega hoje, sem compilar)",
      "alvo": "varios",
      "como": "Estender o aurora-night-light.py para reescrever o trecho INTEIRO de `if (color_mode == 1.0) {` até `// re-multiply` (1666 bytes medidos de orçamento, contra os 160 que ele usa hoje), com o bloco GLSL do primeiro trecho de código: 3 temperaturas (2500K/3500K/4500K) + 1 modo PAPEL. Manter a regra de ouro que o script já tem: padding com espaços para preservar o tamanho do ELF byte a byte, escrita atômica, idempotência e saída 3 se o bloco não casar. Depois disso, os quatro modos aparecem em Acessibilidade > Filtros de cor com os nomes de fábrica (Escala de cinza / Protanopia / Deuteranopia / Tritanopia) — a UI mente, mas troca a quente.",
      "onde": "~/.config/zsh/scripts/aurora-night-light.py (função find_block/patch) — mesmo alvo /usr/bin/cosmic-comp, mesmos backups em /var/lib/aurora/",
      "a_quente": true,
      "custo": "Perde os 3 filtros de daltonismo (o greyscale já estava perdido). Zero compilação. Rótulos da UI ficam errados. Requer um repatch + reinício do compositor UMA vez para instalar a escada — depois disso trocar entre os 4 é instantâneo.",
      "durabilidade": "Morre a cada apt upgrade do cosmic-comp, exatamente como hoje — e é reaplicado pelo mesmo hook de apt do Ritual da Aurora. Sobrevive a reboot e a troca de tema. Não conflita com o patch do ensure_last_empty (são regiões diferentes do binário)."
     },
     {
      "nome": "Sliders contínuos de verdade (temperatura + textura) recompilando",
      "alvo": "varios",
      "como": "Editar 3 arquivos na árvore que já está no disco: (1) src/backend/render/shaders/offscreen.frag com o bloco do segundo trecho de código; (2) src/backend/render/mod.rs nos dois pontos do terceiro trecho (linhas ~419 e ~1362) para declarar e passar temp_rgb/paper/grain/screen_size; (3) src/config/mod.rs para ler as chaves novas do cosmic_config \"com.system76.CosmicComp\" v1 — que herda de graça o ConfigWatchSource já plugado no calloop (config/mod.rs:176-179), ou seja, escrever o arquivo de config aplica no frame seguinte. Converter Kelvin->vec3 com o quarto trecho. Construir pelo mesmo caminho do aurora-cosmic-comp-ws.sh --build, empilhando este patch com o do ensure_last_empty na MESMA build.",
      "onde": "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/ ; binário resultante em /var/lib/aurora/*.aurora-ws ; ferramenta ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh",
      "a_quente": true,
      "custo": "~4 min de build (o mesmo que ela já paga hoje). Um logout para trocar o binário. Depois disso, arrastar o slider vale na hora, sem logout. Mantém os filtros de daltonismo intactos.",
      "durabilidade": "Sobrevive a reboot e a troca de tema. Um apt upgrade do cosmic-comp devolve o binário de fábrica e derruba TUDO (sliders + workspace) — igual ao patch de workspace de hoje; o self-heal avisa e o conserto é humano (`--build`). Este é o argumento mais forte para empilhar os dois patches na mesma build: uma recompilação só, um aviso só."
     },
     {
      "nome": "Bloco PAPEL/KINDLE (a \"Textura\")",
      "alvo": "textura",
      "como": "Três camadas na ordem: (1) dessaturação com mix(cor, luma, paper) usando a luminância exata do Clutter vec3(0.299,0.587,0.114); (2) tom de papel reproduzindo o Clutter.BrightnessContrastEffect com o preset 'sepia' da Bedtime — constantes já derivadas: b_mul vec3(0.874016,1.0,0.748031), b_off vec3(0.125984,0.0,0.0), c_gain vec3(1.220426,1.0,1.0); (3) grão por hash de ruído. ATENÇÃO às duas ciladas medidas: escalar v_coords para pixels (v_coords*screen_size, ou *4096.0 na versão in-place) senão sai degradê em vez de grão; e NÃO declarar `uniform vec2 size` esperando que o Smithay preencha — ele só faz isso para pixel shader, não para texture shader.",
      "onde": "src/backend/render/shaders/offscreen.frag (bloco do color_mode do modo leitura)",
      "a_quente": true,
      "custo": "Grão ESTÁTICO, não animado como o do MIUI. É escolha deliberada: o cosmic-comp usa damage tracking, e ruído animado força redesenho de tela inteira todo frame — nesta máquina isso é o mesmo erro do applet conta-gotas (43% de CPU no compositor, medido em 2026-08). O grão parado já entrega o efeito de página; o custo é uma multiplicação e um sin por pixel.",
      "durabilidade": "Idêntica à da solução em que for embutido (in-place ou recompilada)."
     },
     {
      "nome": "Agendador por horário via protocolo a11y + systemd timer",
      "alvo": "agendamento",
      "como": "Escrever um cliente Wayland minúsculo que faz bind em cosmic_a11y_manager_v1 (VERSÃO 2 — é o que a sessão dela anuncia, então set_screen_filter e NÃO set_screen_filter2) e chama set_screen_filter(inverted, filter). Mandar filter=2 (greyscale no enum DO PROTOCOLO) para acionar o color_mode 1.0 do shader — os dois enums não batem, essa é a pegadinha. Rodar por um serviço + timer do systemd (OnCalendar de 15 em 15 min, ou OnUnitActiveSec=2min), usando a função na_janela() do sexto trecho de código para o teste com virada de meia-noite. Guardar início/fim em horas fracionárias, como o GNOME faz (night-light-schedule-from/to, default 20.00 -> 6.00). Precisa de WAYLAND_DISPLAY e XDG_RUNTIME_DIR no ambiente do serviço — serviço de usuário, não de sistema.",
      "onde": "novo par ~/.config/zsh/scripts/aurora-leitura-agenda.{sh,service,timer} (o padrão de timer já existe: aurora-cosmic-nightlight-watch.timer, que hoje só vigia a issue #2059 e não agenda nada — não há conflito)",
      "a_quente": true,
      "custo": "Um binário/cliente novo a manter. Sem logout, sem recompilar. Independe de qual das duas soluções de temperatura estiver instalada — ele só liga/desliga o filtro.",
      "durabilidade": "Sobrevive a apt upgrade, reboot e troca de tema: não toca no binário do compositor, só fala o protocolo público. É a peça mais durável das quatro — e a única que continua valendo se a System76 entregar o night light nativo (issue #2059)."
     },
     {
      "nome": "Copiar o modelo de configuração do GNOME (faixa, passo, formato de horário)",
      "alvo": "ui",
      "como": "Para a interface parecer o que ela já conhece: temperatura de 2700 a 6500K com passo 100 e marcas em 4000K e 5250K (é literalmente o GtkAdjustment do gnome-control-center); se ela quiser mais quente que o GNOME de fábrica permite, abrir para 1500K como as extensões fazem (o backend do GNOME vai de 1000 a 10000). Horário como double de horas fracionárias (20.00 -> 6.00), nomes de chave espelhando night_light_temperature / night_light_enabled / night_light_schedule_from / night_light_schedule_to. Textura como percentual 0-100 (é como a Bedtime expõe o color-tone-factor). Transição suave de 5s se quisermos igualar o GNOME (gsd-night-light interpola linearmente em 50ms por passo).",
      "onde": "nas chaves novas de src/config/mod.rs (rota recompilada) e no meow.conf / docs do MeowSystem — checar docs/FRONTEIRA.md antes de decidir se a chave mora no Meow ou no Ritual da Aurora",
      "a_quente": true,
      "custo": "Nenhum custo técnico; é decisão de nomenclatura. Vale fixar ANTES de escrever código, porque renomear chave depois quebra o arquivo de config dela.",
      "durabilidade": "Permanente — e alinhada com o que a System76 provavelmente vai adotar quando implementar o night light nativo, o que barateia a migração."
     }
    ],
    "derrubado": [
     "\"E Ink Mode\" (extensions.gnome.org/3957) NÃO é a extensão de textura dela e não tem shader nenhum: só troca o tema do Shell/GTK/ícones para High Contrast, desliga animações e pinta o fundo de branco. Está arquivada, com o autor mandando usar um TEMA no lugar. Quem faz o efeito de verdade é a Bedtime Mode.",
     "Não existe GLSL para portar do lado da TEMPERATURA. As três extensões (Night Light Slider, Night Light Slider Updated, Night Light Scheduler) são 100% front-end de gsettings sobre org.gnome.settings-daemon.plugins.color — quem aplica é o gsd-color por gamma ramp no mutter. Procurar \"o shader da extensão de night light\" é procurar o que não existe.",
     "Redshift / gammastep / wlsunset / wl-gammactl estão MORTOS no COSMIC, e re-medi hoje em vez de aceitar a nota antiga: `strings /usr/bin/cosmic-comp | grep -icE 'zwlr_gamma_control|gamma_control_manager'` dá 0, e o wayland-info da sessão viva não lista global de gamma nem de color management. Não é configuração faltando, é protocolo ausente.",
     "O orçamento do patch in-place NÃO é 46 bytes. Essa é a folga só dentro do bloco do greyscale (160 bytes, 114 usados). Contando até o `// re-multiply` são 1666 bytes contíguos reescrevíveis — o suficiente para 4 presets de temperatura E um bloco de papel, sem recompilar. A premissa de que in-place só cabe um multiplicador de 3 floats era pessimista demais.",
     "O `uniform vec2 size` do Smithay NÃO chega ao offscreen.frag. Ele é documentado e preenchido só para compile_custom_pixel_shader / render_pixel_shader_to (gles/mod.rs:2002, 2049, 2086, 3185); o postprocess usa compile_custom_texture_shader, que não tem uniform_size. Declarar `size` ali dá um uniform nunca escrito. Para o grão, ou se passa screen_size explícito via UniformName, ou se escala v_coords por uma constante.",
     "O hash de ruído copiado de Shadertoy NÃO funciona direto: ele pressupõe coordenada em pixels, e o offscreen.frag só oferece v_coords normalizado 0..1. `fract(sin(dot(v_coords, vec2(12.9898,78.233)))*43758.5453)` produz um degradê suave, não grão — o erro é silencioso, compila e roda bonito, só que sem textura nenhuma.",
     "O protocolo a11y vivo nesta máquina é a VERSÃO 2, não a 3. A documentação do wayland.app descreve a v3; o wayland-info dela diz `cosmic_a11y_manager_v1, version 2`. Logo set_screen_filter2 (e o argumento filter_state) não existem aqui — usar set_screen_filter. Programar contra a doc sem olhar a versão viva quebra na hora do bind.",
     "Os dois enums de filtro não são o mesmo. Interno (Rust, vira o float color_mode): Greyscale=1, Protanopia=2, Deuteranopia=3, Tritanopia=4. Protocolo Wayland: disabled=0, unknown=1, greyscale=2, daltonize_protanopia=3, daltonize_deuteranopia=4, daltonize_tritanopia=5. Mandar 1 pelo protocolo achando que liga o color_mode 1.0 liga \"unknown\", que manda o compositor NÃO mudar o filtro — falha muda.",
     "\"Recompilar o cosmic-comp\" não é um caminho novo e arriscado a inaugurar: o /usr/bin/cosmic-comp que está rodando AGORA já é a build local da Aurora (33571456 bytes, mesmos caminhos de build /mnt/Apate/Desenvolvimento/cosmic-comp-patch/... nas strings). A árvore de fontes com o offscreen.frag está no disco. O custo real é ~4 min de build já orçado, não um projeto."
    ]
   },
   "verificadas": [
    {
     "nome": "Escada de 4 presets no patch in-place (entrega hoje, sem compilar)",
     "alvo": "varios",
     "como": "Estender o aurora-night-light.py para reescrever o trecho INTEIRO de `if (color_mode == 1.0) {` até `// re-multiply` (1666 bytes medidos de orçamento, contra os 160 que ele usa hoje), com o bloco GLSL do primeiro trecho de código: 3 temperaturas (2500K/3500K/4500K) + 1 modo PAPEL. Manter a regra de ouro que o script já tem: padding com espaços para preservar o tamanho do ELF byte a byte, escrita atômica, idempotência e saída 3 se o bloco não casar. Depois disso, os quatro modos aparecem em Acessibilidade > Filtros de cor com os nomes de fábrica (Escala de cinza / Protanopia / Deuteranopia / Tritanopia) — a UI mente, mas troca a quente.",
     "onde": "~/.config/zsh/scripts/aurora-night-light.py (função find_block/patch) — mesmo alvo /usr/bin/cosmic-comp, mesmos backups em /var/lib/aurora/",
     "a_quente": true,
     "custo": "Perde os 3 filtros de daltonismo (o greyscale já estava perdido). Zero compilação. Rótulos da UI ficam errados. Requer um repatch + reinício do compositor UMA vez para instalar a escada — depois disso trocar entre os 4 é instantâneo.",
     "durabilidade": "Morre a cada apt upgrade do cosmic-comp, exatamente como hoje — e é reaplicado pelo mesmo hook de apt do Ritual da Aurora. Sobrevive a reboot e a troca de tema. Não conflita com o patch do ensure_last_empty (são regiões diferentes do binário).",
     "trilha": "extensoes-gnome",
     "veredito": {
      "solucao": "Trilha \"extensoes-gnome\" / \"Escada de 4 presets no patch in-place\": estender o ~/.config/zsh/scripts/aurora-night-light.py para reescrever, dentro do binário /usr/bin/cosmic-comp, o trecho GLSL inteiro de `if (color_mode == 1.0) {` até `// re-multiply`, trocando os 3 filtros de daltonismo por 3 temperaturas (2500K/3500K/4500K) + 1 modo PAPEL, preservando o tamanho do ELF byte a byte com padding de espaços. Os 4 modos passam a aparecer em Acessibilidade > Filtros de cor com os rótulos de fábrica (Escala de cinza / Protanopia / Deuteranopia / Tritanopia).",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "CONFIRMADO POR MEDIÇÃO — o mecanismo é real:\n\n1. Caminhos existem. /usr/local/bin/aurora-night-light.py (a cópia que o self-heal EXECUTA) está em sync byte a byte com a fonte em ~/.config/zsh/scripts/. O binário vivo /usr/bin/cosmic-comp tem 33.571.456 bytes = a build .aurora-ws, e contém 1 ocorrência de \"NIGHT LIGHT (Aurora)\" e 1 de \"if (color_mode == 1.0) {\" — sem ambiguidade de alvo.\n\n2. \"Vale a quente\" está PROVADO no código-fonte, não inferido. O fonte exato da build viva está no disco em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/. Em src/backend/render/mod.rs:1366-1380, `Uniform::new(\"invert\", ...)` e `Uniform::new(\"color_mode\", screen_filter.filter.color_filter.map(|val| val as u8 as f32))` são construídos DENTRO de `render_output`, ou seja, a cada frame. O programa é compilado uma única vez em `init_shaders` (mod.rs:392-425) e cacheado via `insert_if_missing` no user_data do EGL context. Trocar entre os 4 degraus é troca de uniform por frame. Confirmado.\n\n3. ColorFilter é `#[repr(u8)]` com exatamente Greyscale=1, Protanopia=2, Deuteranopia=3, Tritanopia=4 (src/config/mod.rs:164-172, com o comentário \"these values need to match with offscreen.frag\"). A escada de 4 é o teto absoluto — não há um 5º valor a roubar.\n\n4. Sobrevive a reboot (é o binário em disco), a troca de tema (não passa por cosmic-config nenhum), e é reaplicado após apt: aurora-cosmic-comp-ws.sh:160-163 chama /usr/local/bin/aurora-night-light.py logo DEPOIS de repor a build .aurora-ws — a ordem já está encadeada e correta. Regiões do binário são disjuntas, não há conflito de patch.\n\nO QUE DERRUBA (por que não é CONFIRMADO):\n\nR1 — A proposta NÃO entrega (C), e o caminho óbvio é comprovadamente morto. `load_filter_state` (src/config/mod.rs:377-398) lê ~/.local/state/cosmic-comp/a11y_screen_filter.ron SÓ na inicialização. Não há watcher nesse caminho — o `Config::load` só insere ConfigWatchSource para com.system76.CosmicComp (mod.rs:176-179), que é outro diretório. Cron escrevendo o .ron não muda NADA até o próximo login. Agendamento exige um cliente wayland falando cosmic_a11y_manager_v1.set_screen_filter. Bom: o global é criado com filtro `client_not_sandboxed` (state.rs:647-651, 762), então um script comum do host PODE bindar. Ruim: não existe CLI assim na máquina e pywayland não está instalado.\n\nR2 — A proposta também não entrega (A) nem (B) como PEDIDOS. Ela pediu SLIDER contínuo (\"pra eu ir definindo\"). Quatro degraus discretos, com rótulos mentirosos, não é slider.\n\nR3 — QUASE ELIMINATÓRIO: erro de GLSL = sem desktop E SEM TELA DE LOGIN. `init_shaders` compila o postprocess shader com `?` já na init do device KMS (src/backend/kms/device.rs:1119 \"Failed to compile shaders\"), não preguiçosamente quando o filtro é ligado. E /usr/bin/cosmic-greeter-start é literalmente `exec cosmic-comp cosmic-greeter` — o greeter usa o MESMO binário. O patch de hoje é 1 linha; a proposta multiplica a superfície por ~10x (1665 bytes).\n\nR4 — O `--restore` do próprio script é a recuperação ERRADA. Ele copia /var/lib/aurora/cosmic-comp-<versão>.orig, que tem 28.339.864 bytes = o binário EMPACOTADO, não os 33.571.456 da build .aurora-ws viva. Recuperar pelo script devolve o desktop mas DESFAZ o patch de workspace em silêncio (o vazio a mais volta). A afirmação \"não conflita com o ensure_last_empty\" é verdadeira para a REGIÃO do patch e falsa para o CAMINHO DE VOLTA.\n\nR5 — Orçamento medido errado. De 0x2dc16a (`    if (color_mode == 1.0) {`) até logo antes de `\\n    // re-multiply` (0x2dc7eb) são 1665 bytes, não 1666 (incluindo o marcador, 1684). Um byte a menos é um espaço inócuo; um byte a mais come o `}` e vira R3.\n\nR6 — GLSL ES 1.00: a região patchável está DENTRO de main(). Isso proíbe estruturalmente, sem exceção: função auxiliar (GLSL não tem função aninhada), `#extension` novo, `uniform`/`varying` novo, `switch` (não existe em ES 1.00), operadores bitwise `& | ^ <<` (não existem em ES 1.00 — hash de ruído baseado neles NÃO COMPILA), `dFdx`/`dFdy` (exigem `#extension GL_OES_standard_derivatives`, que teria de ficar no topo do arquivo, fora da região), loop de contagem variável, texelFetch. O único grão viável para o modo PAPEL é o hash float `fract(sin(dot(gl_FragCoord.xy, vec2(a,b))) * c)` — esse SIM é válido em ES 1.00, e highp em NVIDIA é fp32, então serve. Não recebi o texto GLSL do bloco proposto; se ele usar qualquer item da lista acima, é refutação direta daquele rascunho.\n\nR7 — UX, o dado que a proposta esconde: o applet de a11y NÃO está no painel dela (plugins_wings = Time, Workspaces, Notifications | NowPlaying, Power) e NÃO existe atalho de teclado para filtro de cor (a lista de SystemActions no binário tem ScreenReader, não tem color filter). Então \"ir definindo\" vira: abrir cosmic-settings, Acessibilidade, dropdown, escolher \"Deuteranopia\" querendo dizer 4500K. Quatro cliques dentro de um menu que mente.\n\nR8 — E o argumento que enfraquece a razão de existir da proposta: o fonte EXATO da build viva está no disco, com pipeline de build de ~4 min já precedente (aurora-cosmic-comp-ws.sh --build), na MESMA árvore que o patch de workspace usa. Dois uniforms float de verdade — `warmth` e `paper` — custam ~15 linhas: `UniformName::new` em render/mod.rs:422-423 e `Uniform::new` em :1366-1380. A proposta se vende como \"entrega hoje, sem compilar\", mas ela JÁ exige um repatch + reinício do compositor = logout. É o mesmo logout. O que ela economiza são 4 minutos de compilação, e o que ela paga é rótulo mentiroso, perda dos 3 filtros de daltonismo, teto rígido de 4 degraus e zero slider.\n\nTERRITÓRIO: docs/FRONTEIRA.md é explícito — \"binário cosmic-comp (patches de workspace e night light) | Aurora\". Dono certo. Mas ~/.config/zsh tem auto-commit a cada 10 min: a edição vira commit no repo privado dela sozinha. E plugins_wings (a mitigação do R7) é da Aurora — o Meow não escreve ali.",
      "risco_real": "Se o bloco GLSL tiver um erro de sintaxe, ela não perde só o filtro: perde o DESKTOP E A TELA DE LOGIN. Provado: init_shaders compila o postprocess shader eagerly com `?` na init do device (src/backend/kms/device.rs:1119), e /usr/bin/cosmic-greeter-start é `exec cosmic-comp cosmic-greeter` — mesmo binário. Tela preta, sem greeter.\n\nTEM volta, e ela é barata, mas a proposta não a documenta:\n\n1. PRÉ-VOO OBRIGATÓRIO (risco zero, não encosta em /usr/bin): patchar uma CÓPIA em /tmp e rodar NINHADA. src/backend/mod.rs:31 escolhe x11/winit quando WAYLAND_DISPLAY está setado, então `aurora-workspace.sh run /tmp/.../cosmic-comp-teste` abre uma janela aninhada no workspace OS. Se o shader não compilar, ele morre ali com \"Failed to initialize renderer\" e /usr/bin/cosmic-comp nunca foi tocado. Isso converte R3 de eliminatório em administrável — mas SÓ se for feito.\n\n2. RECUPERAÇÃO POR TTY (Ctrl+Alt+F3, login, sem X/Wayland):\n   sudo cp /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws /usr/bin/cosmic-comp\n   Esse arquivo eu verifiquei: 33.571.456 bytes (igual ao vivo), 0 ocorrências de \"NIGHT LIGHT (Aurora)\", 1 de \"// greyscale\" — é a build limpa COM o patch de workspace. É o alvo certo.\n\n   NÃO usar `aurora-night-light.py --restore`: medido, ele copia o .orig de 28.339.864 bytes = o binário empacotado, e devolve o workspace vazio a mais em silêncio.\n\n3. Rede terceira, se /var/lib/aurora sumir: sudo apt-get install --reinstall cosmic-comp (perde o patch de workspace até o próximo --build de ~4 min).\n\nRiscos menores: perde os 3 filtros de daltonismo permanentemente enquanto o patch viver (o greyscale já foi em julho); e se a fonte em ~/.config/zsh/scripts for atualizada mas /usr/local/bin não (é a cópia que o hook de apt executa), o próximo apt upgrade repõe a escada com o patcher ANTIGO de 1 linha e os 4 degraus somem sem aviso.",
      "correcao": "NÃO aprovar como está. Três correções são obrigatórias, e há uma quarta que eu recomendo trocar a proposta inteira por ela.\n\nOBRIGATÓRIAS, se for seguir a escada:\n\nC1 — Pré-voo aninhado antes de qualquer escrita em /usr/bin. Patchar uma cópia em /tmp, rodar com `aurora-workspace.sh run`, confirmar que sobe. Sem isso a proposta é REFUTADA por falta de caminho de recuperação testado.\n\nC2 — Consertar o `--restore`: ele tem de preferir /var/lib/aurora/cosmic-comp-<versão>.aurora-ws quando o arquivo existir, e só cair no .orig empacotado se não existir. Hoje ele desfaz o patch de workspace em silêncio. Isso é bug pré-existente, mas a escada aumenta muito a chance de alguém precisar do --restore.\n\nC3 — Não hardcodar 1666. Medir end-start em runtime (é 1665 nesta build) e padear. Manter a guarda de sanidade do find_block (hoje `> 512`) subida para algo como `> 2048`, nunca removida, e manter a saída 3 quando o bloco não casar.\n\nC4 — GLSL: o bloco tem de ser statements retos dentro de main(), sem função auxiliar, sem #extension, sem uniform novo, sem switch, sem operador bitwise, sem dFdx/dFdy, sem loop variável. Grão de papel só via `fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453)`, que é ES 1.00 legítimo.\n\nA TROCA QUE EU RECOMENDO:\n\nA escada resolve mal os três alvos: (A) vira 3 degraus em vez de slider, (B) vira 1 degrau em vez de slider, (C) não é resolvido de jeito nenhum. E o custo que ela evita é de 4 minutos de compilação — não de um logout, porque o logout ela exige de qualquer forma.\n\nO fonte da build viva está em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/, o pipeline `aurora-cosmic-comp-ws.sh --build` já existe e já é precedente nesta máquina, e o patch honesto é minúsculo:\n\n  - src/backend/render/mod.rs:422-423 — acrescentar UniformName::new(\"warmth\", UniformType::_1f) e (\"paper\", UniformType::_1f) ao compile_custom_texture_shader.\n  - src/backend/render/mod.rs:1366-1380 — acrescentar os dois Uniform::new correspondentes, lidos de campos novos em ScreenFilter.\n  - src/config/mod.rs:152-156 — dois f32 com #[serde(default)] em ScreenFilter (o .ron aceita campo novo sem quebrar o existente).\n  - offscreen.frag — usar warmth/paper de verdade, sem roubar slot de enum.\n\nIsso dá slider contínuo REAL em (A) e (B), com os valores chegando por uniform a cada frame — a quente, pela mesma via já provada. Convive com o patch do ensure_last_empty por construção, porque é a MESMA árvore e o MESMO build.\n\nE (C) fica separado dos outros dois em qualquer cenário: exige um clientezinho wayland de cosmic_a11y_manager_v1 (versão 2 anunciada, client_not_sandboxed permite um script do host) chamado por systemd timer. Não dá para agendar escrevendo o .ron — isso está provado morto no código. Se a escada for aprovada assim mesmo, (C) continua em aberto e precisa ser dito a ela com essas palavras."
     }
    },
    {
     "nome": "Sliders contínuos de verdade (temperatura + textura) recompilando",
     "alvo": "varios",
     "como": "Editar 3 arquivos na árvore que já está no disco: (1) src/backend/render/shaders/offscreen.frag com o bloco do segundo trecho de código; (2) src/backend/render/mod.rs nos dois pontos do terceiro trecho (linhas ~419 e ~1362) para declarar e passar temp_rgb/paper/grain/screen_size; (3) src/config/mod.rs para ler as chaves novas do cosmic_config \"com.system76.CosmicComp\" v1 — que herda de graça o ConfigWatchSource já plugado no calloop (config/mod.rs:176-179), ou seja, escrever o arquivo de config aplica no frame seguinte. Converter Kelvin->vec3 com o quarto trecho. Construir pelo mesmo caminho do aurora-cosmic-comp-ws.sh --build, empilhando este patch com o do ensure_last_empty na MESMA build.",
     "onde": "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/ ; binário resultante em /var/lib/aurora/*.aurora-ws ; ferramenta ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh",
     "a_quente": true,
     "custo": "~4 min de build (o mesmo que ela já paga hoje). Um logout para trocar o binário. Depois disso, arrastar o slider vale na hora, sem logout. Mantém os filtros de daltonismo intactos.",
     "durabilidade": "Sobrevive a reboot e a troca de tema. Um apt upgrade do cosmic-comp devolve o binário de fábrica e derruba TUDO (sliders + workspace) — igual ao patch de workspace de hoje; o self-heal avisa e o conserto é humano (`--build`). Este é o argumento mais forte para empilhar os dois patches na mesma build: uma recompilação só, um aviso só.",
     "trilha": "extensoes-gnome",
     "veredito": {
      "solucao": "Sliders contínuos de verdade (temperatura + textura) recompilando o cosmic-comp — trilha \"extensoes-gnome\"",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "O MECANISMO É REAL, MAS A PROPOSTA ERRA O ARQUIVO, O LUGAR E A DURABILIDADE.\n\nO que CONFIRMEI (a quente está provado):\n- O caminho quente existe e não recompila shader. `Surface::set_screen_filter` (kms/surface/mod.rs:401) manda `ThreadCommand::UpdateScreenFilter`; a thread trata em :600 e `update_screen_filter` (:1460) só troca `self.screen_filter` e limpa `postprocess_textures`. Os uniforms são remontados A CADA FRAME em kms/surface/mod.rs:1893 (cursor) e :1927 (tela) a partir de `&ScreenFilter`. Um float contínuo por uniform aplica no frame seguinte. CONFIRMADO.\n- O `ConfigWatchSource` citado existe mesmo (config/mod.rs:174-180) e o cosmic_config é chave-por-arquivo — conferi `~/.config/cosmic/com.system76.CosmicComp/v1/` (accessibility_zoom, activation_policy, pinned_workspaces, ...). Escrever um arquivo novo dispara `config_changed`.\n- Toolchain OK: cargo 1.93.1, rust-toolchain.toml pede 1.93, toolchain 1.93 instalada. Árvore existe em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094 (e /home/vitoriamaria/Desenvolvimento é symlink pra /mnt/Apate/Desenvolvimento, então os dois caminhos são o mesmo).\n\nO que DERRUBA a proposta como escrita:\n\n1) \"Editar 3 arquivos na árvore que já está no disco\" + \"durabilidade igual ao patch de workspace\" é FALSO, e falha em SILÊNCIO. O `compilar()` do aurora-cosmic-comp-ws.sh roda `apt-get source cosmic-comp` e aplica EXATAMENTE UM arquivo de patch (`PATCH_INSTALADO=/usr/local/share/aurora/cosmic-comp-sem-workspace-vazio.patch`, linha 88). Depois, `podar_fontes()` faz `rm -rf` nas árvores de OUTRA versão. Chegaram três tarballs em 10 dias (18, 25 e 26/08). Ou seja: edição que só vive na árvore morre no próximo update do cosmic-comp — e o teste de sanidade do build (`grep -aqs \"$MARCA\" \"$bin\"`, MARCA=\"AURORA-COSMIC-WS-PATCH\") vem do patch de WORKSPACE, então o rebuild PASSA com os sliders sumidos. Regressão muda, sem aviso nenhum. É o oposto do que a proposta promete.\n\n2) O arquivo que a proposta NÃO cita é justamente o da máquina dela. Ela fala em \"render/mod.rs nos dois pontos (~419 e ~1362)\". A linha 419 está certa (`init_shaders`, lista de UniformName em 422-423), mas 1362 é o caminho winit/x11. A sessão dela é KMS: os call sites vivos são kms/surface/mod.rs:1893 e :1927. Patchar só render/mod.rs = slider que não faz absolutamente nada na tela dela. E esquecer o :1893 = ponteiro do mouse frio com a tela quente.\n\n3) `is_noop()` mata o passe inteiro. config/mod.rs:159 → render/mod.rs:1284 (`if !screen_filter.filter.is_noop()`) e kms/surface/mod.rs:1101/1105. Com `inverted=false` e `color_filter=None`, o postprocess NÃO RODA — temperatura e textura não têm por onde aparecer. A proposta não menciona isso.\n\n4) A rota do cosmic_config não chega no renderer \"de graça\". O renderer lê `dynamic_conf.screen_filter()` (kms/device.rs:260/347/519/554), não `cosmic_conf`. O watch dispara, mas o valor para em `state.common.config.cosmic_conf` e nunca vira uniform. O braço novo do `config_changed` TEM de chamar `state.backend.update_screen_filter(...)` na mão.\n\n5) A COLISÃO com a night light, que a proposta ignora. O `find_block()` do aurora-night-light.py (linhas 105-127) ancora em `uniform float color_mode;`, pega de `    if (color_mode == 1.0) {` até `    } else if (color_mode >= 2.0) {` e REESCREVE o vão inteiro com padding de espaços, aceitando qualquer coisa abaixo de 512 bytes. O `cmd_build` roda esse script logo após instalar (linha ~471) e o self-heal do apt roda de novo. Código novo dentro desse vão é apagado silenciosamente no binário a cada build. Provei a pilha: /usr/bin/cosmic-comp (33571456 B, md5 ef12fd…) = build de workspace + patch de bytes da night light; já /var/lib/aurora/…aurora-ws (MESMO tamanho, md5 b68b6b…) ainda diz `// greyscale`. E o fonte na árvore é o de fábrica (greyscale), não o da night light.\n\n6) ISSO NÃO É UM SLIDER. O protocolo a11y (wayland/protocols/a11y.rs:196) só carrega `SetScreenFilter { inverted, filter }` — um bool e um enum. O cosmic-settings 1.7.0 não tem widget pra float. O patch entrega um VALOR contínuo aplicado a quente; a barrinha de arrastar que ela mandou no print do HyperOS não existe e precisa ser construída à parte (applet ou CLI do `meow`). Prometer \"slider\" aqui é prometer o que o patch não dá.\n\n7) Não pude auditar o GLSL: os \"segundo/terceiro/quarto trechos de código\" citados NÃO vieram no que recebi. Sem o texto, a exigência 3 (validade em GLSL ES 1.00) fica NÃO VERIFICADA — o que sozinho já impede um veredito CONFIRMADO.\n\n8) Território: FRONTEIRA.md é explícito — \"binário `cosmic-comp` (patches de workspace e night light) | Aurora | **Aurora**\". A TRAVA 1 do lib/comum.sh recusa `/usr/local/share/*` e `~/.config/zsh`. E `~/.config/zsh` auto-commita a cada 10 min, então mexer no .patch canônico vira commit no repo privado dela sozinho. É permitido (precedente \"Aurora pode ser corrigido pelo Meow\"), mas tem de ser edição deliberada do lado Aurora, nunca um script do Meow.",
      "risco_real": "ELIMINATÓRIO? NÃO — mas o pior caso é a tela preta, e ele é fácil de alcançar.\n\nO que quebra: o GLSL NÃO é compilado no build. `make VENDOR=1` embute o texto via `include_str!` e segue feliz com um shader quebrado; quem compila é o driver NVIDIA (RTX 4060) em runtime. Um erro de sintaxe faz `compile_custom_texture_shader` devolver Err, `init_shaders` propaga com `?` em kms/device.rs:1119 e kms/surface/mod.rs:733 → o render node não sobe → sessão não sobe. E o cosmic-greeter usa o MESMO binário: não é \"o desktop dela não abre\", é \"não tem nem tela de login\". Ela descobre isso ~4 min de build + logout depois.\n\nTEM VOLTA, e a volta é curta (confirmei os arquivos, não é promessa):\n1. Ctrl+Alt+F3 (TTY texto continua funcionando; o cosmic-comp caído não leva o console junto).\n2. `sudo aurora-cosmic-comp-ws.sh --restore` — `cmd_restore` (linha 529) copia /var/lib/aurora/cosmic-comp-<versão>.pkg-orig por cima de /usr/bin/cosmic-comp, atômico (tmp + mv -f). O arquivo EXISTE agora: cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig, 28339864 B — e conferi que essa cópia JÁ traz a night light, então ela não perde nem isso no rollback.\n3. Se o .pkg-orig sumir: `sudo apt-get install --reinstall cosmic-comp` (o próprio script imprime essa linha).\n\nRisco secundário, mais traiçoeiro que a tela preta porque é mudo: nenhum aviso quando o patch se perder. O self-heal só sabe procurar o marcador do patch de workspace. Um `apt upgrade` do cosmic-comp + `--build` devolve os workspaces e NÃO devolve os sliders, sem uma linha de log. Ela vai achar que \"parou de funcionar sozinho\" semanas depois.\n\nRisco terceiro: glslangValidator/glslc/glslang estão AUSENTES nesta máquina (conferi os quatro). Hoje não há como validar o shader antes de instalar.",
      "correcao": "Seis consertos. Com eles a trilha vira defensável; sem os dois primeiros ela é ruim.\n\n1. O patch mora no ARQUIVO DE PATCH, não na árvore. Dobrar a mudança de shader/uniform dentro de ~/.config/zsh/patches/cosmic-comp-sem-workspace-vazio.patch (canônico; /usr/local/share/aurora/ é a cópia instalada). Os hunks de src/backend/render/mod.rs têm de ser regerados contra a árvore limpa contando as DUAS mudanças no mesmo arquivo — o patch de workspace já toca render/mod.rs (@@ 953 e @@ 978), então não há choque de contexto com as linhas 419/1362. E acrescentar um segundo marcador (ex.: AURORA-COSMIC-TEMP-PATCH) ao lado do MARCA existente, mais um segundo `grep -aqs` no `compilar()` (linha ~426), pra um rebuild sem os sliders ABORTAR em vez de instalar mudo.\n\n2. Patchar os call sites certos: kms/surface/mod.rs:1893 (cursor) E :1927 (tela). render/mod.rs:1362 é winit/x11 — patchar também, por higiene, mas ele não é a sessão dela. Declarar os uniforms novos na lista de render/mod.rs:422 (`temp_rgb` _3f, `paper` _1f, `grain` _1f, `screen_size` _2f) e passá-los nos TRÊS lugares: nome declarado e não passado vira no-op (location -1); nome passado e não declarado vira `GlesError::UnknownUniform` e derruba o frame.\n\n3. Estender `is_noop()` (config/mod.rs:159) para `!self.inverted && self.color_filter.is_none() && self.temp_k >= 6500 && self.paper == 0.0` — senão o passe de postprocess nem roda e nenhum slider aparece.\n\n4. Rota de dados: o mais curto e seguro é acrescentar os campos ao próprio `ScreenFilter` (config/mod.rs:152) e empurrar por `backend.update_screen_filter(...)`, que é o caminho já provado quente. Se insistir no cosmic_config (que dá o watch de graça e a chave-por-arquivo que o agendamento por horário quer), o braço novo do `config_changed` PRECISA chamar `state.backend.update_screen_filter(...)` explicitamente — não herda nada sozinho.\n\n5. Night light: pôr o bloco novo ANTES da linha `    if (color_mode == 1.0) {`, fora do vão que o `find_block()` reescreve, E aposentar o aurora-night-light.py na mesma mudança (o uniform de temperatura o torna redundante; deixar os dois multiplica o tom quente duas vezes e devolve o greyscale de fábrica pra ela). Rodar `sudo aurora-night-light.py --restore` antes, para o /var/lib/aurora/*.orig não virar referência de um shader que não existe mais.\n\n6. Antes de instalar QUALQUER binário: `sudo apt install glslang-tools` e validar o .frag com `glslangValidator -S frag` (com o `//_DEFINES_` substituído pelas variantes NO_ALPHA/EXTERNAL/DEBUG_FLAGS). Restrições reais do estágio, que li no smithay vendorizado: o programa de TEXTURA só tem `tex`, `alpha`, `v_coords` e `tint` opcional — NÃO existe `size` embutido (esse é do programa de pixel), então `screen_size` tem de ser declarado; e não usar `texelFetch`, `inverse`, `transpose`, `round`, `trunc`, `%` inteiro, `switch`, laço de limite variável, `mix` com bvec, nem array indexado por não-constante. Grão seguro e estável (sem uniform de tempo, logo sem cintilação): `fract(sin(dot(v_coords*screen_size, vec2(12.9898,78.233)))*43758.5453)`. Operar depois do `color.rgb /= color.a` e deixar o `color.rgb *= color.a` existente re-multiplicar, com `clamp(...,0.0,1.0)` na mistura de papel — despremultiplicado passa de 1.0.\n\n7. Dizer a verdade pra ela sobre o (A)/(B): o patch entrega VALOR contínuo a quente, não a barrinha. O agendamento (C) sai barato — um systemd timer escrevendo a chave do cosmic_config, e o ConfigWatchSource aplica no frame seguinte. A barrinha de arrastar é trabalho separado (applet cosmic ou `meow`), e o print do HyperOS não vai ser reproduzido nesta entrega."
     }
    },
    {
     "nome": "Bloco PAPEL/KINDLE (a \"Textura\")",
     "alvo": "textura",
     "como": "Três camadas na ordem: (1) dessaturação com mix(cor, luma, paper) usando a luminância exata do Clutter vec3(0.299,0.587,0.114); (2) tom de papel reproduzindo o Clutter.BrightnessContrastEffect com o preset 'sepia' da Bedtime — constantes já derivadas: b_mul vec3(0.874016,1.0,0.748031), b_off vec3(0.125984,0.0,0.0), c_gain vec3(1.220426,1.0,1.0); (3) grão por hash de ruído. ATENÇÃO às duas ciladas medidas: escalar v_coords para pixels (v_coords*screen_size, ou *4096.0 na versão in-place) senão sai degradê em vez de grão; e NÃO declarar `uniform vec2 size` esperando que o Smithay preencha — ele só faz isso para pixel shader, não para texture shader.",
     "onde": "src/backend/render/shaders/offscreen.frag (bloco do color_mode do modo leitura)",
     "a_quente": true,
     "custo": "Grão ESTÁTICO, não animado como o do MIUI. É escolha deliberada: o cosmic-comp usa damage tracking, e ruído animado força redesenho de tela inteira todo frame — nesta máquina isso é o mesmo erro do applet conta-gotas (43% de CPU no compositor, medido em 2026-08). O grão parado já entrega o efeito de página; o custo é uma multiplicação e um sin por pixel.",
     "durabilidade": "Idêntica à da solução em que for embutido (in-place ou recompilada).",
     "trilha": "extensoes-gnome",
     "veredito": {
      "solucao": "Bloco PAPEL/KINDLE (a \"Textura\") — trilha extensoes-gnome, alvo textura, no offscreen.frag do cosmic-comp",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "Refuto DOIS pontos, os dois com número medido nesta máquina.\n\n1) A VARIANTE IN-PLACE É ARITMETICAMENTE IMPOSSÍVEL. O patch in-place (estilo aurora-night-light.py) preserva o binário byte a byte. Medi o bloco no /usr/bin/cosmic-comp VIVO: offset 0x2dc16a, 195 bytes exatos. O bloco mínimo proposto (3 camadas + a cauda \"} else if (color_mode >= 2.0) {\") dá 527 bytes. 527 > 195. Não cabe, e o próprio build_block() já abortaria (pad < 0 → \"não cabe no espaço do original\"). A menção a \"*4096.0 na versão in-place\" descreve algo que não existe.\n\n2) \"VALE A QUENTE = true\" É FALSO PARA O SLIDER. O que é quente hoje é só o LIGA/DESLIGA, porque `color_mode` é reenviado todo frame (render/mod.rs:1360-1382, Uniform::new(\"invert\"/\"color_mode\") dentro de render_output). O VALOR do papel não tem por onde entrar quente:\n - O texto GLSL é compilado UMA vez, em init_shaders (render/mod.rs:392), chamado de kms/surface/mod.rs:733 no node_added. Trocar texto = reiniciar o cosmic-comp = LOGOUT. O próprio aurora-night-light.py já admite isso na linha 277.\n - ScreenFilter tem exatamente dois campos: `inverted: bool` e `color_filter: Option<ColorFilter>` (config/mod.rs:152-162). Não há float nenhum.\n - O único canal quente é o wayland cosmic_a11y_manager_v1 SetScreenFilter (protocols/a11y.rs:196-215), que carrega dois ENUMS. Um valor contínuo não tem fio para viajar.\n - O ~/.local/state/cosmic-comp/a11y_screen_filter.ron é lido UMA vez em load_dynamic (config/mod.rs:341-350) e NÃO tem watcher: os únicos config watch sources são CosmicTk, Shortcuts e WindowRules (linhas 239, 276, 308). Confirmei o arquivo vivo: `( inverted: false, )`. Um timer que escreva ali não muda nada até o próximo login — isso também derruba o alvo (C), agendamento, por esse caminho.\nOu seja: entregaria à Vitória exatamente o que ela já tem no night light — um preset fixo com toggle — e não o \"slicer pra eu ir definindo\".\n\n3) COLISÃO MEDIDA COM O NIGHT LIGHT. O bloco color_mode==1.0 é slot ÚNICO e já é do night light. Pior: o aurora-cosmic-comp-ws.sh chama reaplicar_night_light() (linha 159) DEPOIS de instalar todo build novo. O find_block() do patcher acharia o bloco de papel de 527 bytes, bateria na guarda `if end - start > 512: return None`, sairia rc=3, e o self-heal só logaria \"patcher voltou rc=3\". Resultado: a luz noturna some em silêncio.\n\nO QUE SE CONFIRMOU A FAVOR (não re-litigar):\n - GLSL VÁLIDO, provado na GPU dela. Compilei o bloco como #version 100 via EGL_PLATFORM_DEVICE_EXT: \"GL_VERSION=OpenGL ES 3.2 NVIDIA 580.173.02 / GL_RENDERER=NVIDIA GeForce RTX 4060\" → COMPILE=OK (e OK também no llvmpipe). Nada de texelFetch, inverse(), nem loop variável.\n - O aviso sobre `uniform vec2 size` está CERTO e verificado: em vendor/smithay/src/backend/renderer/gles/mod.rs o `let size = c\"size\"` aparece só na linha 2032, dentro de compile_custom_pixel_shader; compile_custom_texture_shader (linha 2143) não faz essa busca. E texture.vert faz `v_coords = (tex_matrix * position).xy` → normalizado, então escalar é mesmo necessário.\n - Precedente de recompilar existe e o toolchain está pronto: cargo/rustc 1.98.0, fonte 5c93094 desempacotado com vendor/ completo em /home/vitoriamaria/Desenvolvimento/cosmic-comp-patch/.\n\nDOIS DEFEITOS MENORES, MEDIDOS:\n - As constantes estouram o vermelho. Branco (1,1,1) → brilho → (1.0, 1.0, 0.748031) → contraste no R: (1.0-0.5)*1.220426+0.5 = 1.1102, CLIPA em 1.0. Todo vermelho de entrada acima de 0.8967 achata em 1.0 — perda de detalhe em realce (texto branco/UI posteriza).\n - \"senão sai degradê em vez de grão\" está exagerado: fract(sin(dot(uv,·))*43758.5453) com uv normalizado já pula ~296 no valor pré-fract por pixel numa tela de 1920 — já é ruído. Escalar não faz mal, mas não é a diferença entre grão e degradê.",
      "risco_real": "ELA PERDE A TELA DE LOGIN, não só o desktop — e isso é pior do que a proposta supõe. /usr/bin/cosmic-greeter-start é literalmente `exec cosmic-comp cosmic-greeter`: o greeter roda O MESMO binário. Um shader que não compilar faz init_shaders devolver Err (\"Failed to initialize shaders\", kms/surface/mod.rs:733) e ela cai numa tela preta já no login.\n\nTEM VOLTA, e ela é real e curta (~1 min):\n - TTY funciona: autovt@.service é alias habilitado, getty@.service enabled, NAutoVTs=6 (default), /dev/tty1..6 presentes. Ctrl+Alt+F2 dá console.\n - `sudo aurora-cosmic-comp-ws.sh --restore` devolve /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig — CONFERI que o arquivo existe para a versão instalada agora.\n - Rede secundária: `sudo aurora-night-light.py --restore` (usa o .orig) e, por último, `sudo apt-get install --reinstall cosmic-comp`.\n\nREGRA INEGOCIÁVEL: nenhum shader vai para /usr/bin/cosmic-comp sem antes passar no teste de compilação EGL na GPU dela. O testador que escrevi está em /tmp/meow-trabalho (compila o .frag em todos os EGLDeviceEXT, inclusive o NVIDIA, sem precisar de display).\n\nFRONTEIRA: docs/FRONTEIRA.md linha 58 põe \"binário cosmic-comp (patches de workspace e night light)\" como **Aurora**. Então isto mora em ~/.config/zsh — que tem auto-commit de 10 em 10 min no repo privado dela — e NÃO no MeowSystem.\n\napt upgrade / reboot: sobrevive só se entrar no mesmo trilho do aurora-cosmic-comp-ws.sh (artefato .aurora-ws por versão + auto-build). Troca de tema é indiferente: o shader é pós-processamento de tela inteira, não vê o tema.",
      "correcao": "1. ELIMINE a variante in-place. Medido: 195 bytes de orçamento contra 527 necessários. O bloco só existe em cosmic-comp RECOMPILADO, pelo caminho já provado do aurora-cosmic-comp-ws.sh --build (~4 min, artefato por versão em /var/lib/aurora).\n\n2. NÃO use o slot color_mode==1.0. Ele é do night light, e o reaplicar_night_light() (aurora-cosmic-comp-ws.sh:159) roda depois de todo build novo, bate na guarda `end - start > 512`, sai rc=3 e apaga a luz noturna em silêncio. Use um ramo novo e deixe o slot 1 intacto — ou ensine um âncora novo ao patcher antes.\n\n3. Para o slider ser MESMO contínuo e quente, `paper` tem de ser uniform. Isso é mecanicamente possível e a atualização por frame está confirmada:\n   - registrar em render/mod.rs:419-425 → `UniformName::new(\"paper\", UniformType::_1f)`\n   - emitir em render/mod.rs:1360-1382 → `Uniform::new(\"paper\", valor)` (esse vec! é reconstruído a cada render_output, então é por frame de verdade)\n\n4. Falta o ESCRITOR quente, e é aí que a proposta é omissa. Nada nesta máquina consegue mudar esse float em runtime hoje. O padrão mais barato já existente na árvore é outra calloop config source com inotify, igual às de config/mod.rs:239/276/308, lendo um arquivo próprio (ex.: aurora_paper.ron) para dentro do ScreenFilterStorage. Sem isso, o slider é decorativo.\n\n5. Atenção ao portão: o passe de pós-processamento só roda se `!screen_filter.filter.is_noop()` (render/mod.rs:1283, com is_noop em config/mod.rs:159-161 = `!inverted && color_filter.is_none()`). Papel armado com filtro desligado = shader nunca executa. O is_noop precisa passar a considerar o paper, senão o modo leitura fica preso ao toggle de acessibilidade.\n\n6. MEIO-TERMO QUE JÁ É QUENTE HOJE, sem mexer em protocolo nenhum: mapear ColorFilter 2/3/4 (Protanopia/Deuteranopia/Tritanopia) para três intensidades fixas de papel. Troca a quente pela UI de Acessibilidade que ela já usa, mantém o night light no slot 1, e custa os três filtros de daltonismo. Não é contínuo — são 3 degraus — mas é honesto e entrega hoje.\n\n7. Corrija as constantes: com b_off=0.125984 e c_gain=1.220426 o vermelho clipa a partir de 0.8967 de entrada. Ou aplique clamp antes do contraste, ou baixe c_gain para ~1.0, ou pivote o contraste na média pós-brilho em vez de 0.5.\n\n8. Mantenha `screen_size` (ou o literal) como uniform SEU — o Smithay não preenche `size` em texture shader, isso está verificado. E o grão estático continua a escolha certa pelo damage tracking; nesse ponto a proposta está correta.\n\nRESUMO PARA ELA: como está, isto não é um slider — é um segundo preset fixo que exige logout para mudar de valor, e que ainda mata a luz noturna no caminho. Ou vira o degrau 2/3/4 (quente hoje, 3 níveis), ou vira uniform + watcher em cosmic-comp recompilado (contínuo e quente, mas é trabalho de Rust, não de shader)."
     }
    },
    {
     "nome": "Agendador por horário via protocolo a11y + systemd timer",
     "alvo": "agendamento",
     "como": "Escrever um cliente Wayland minúsculo que faz bind em cosmic_a11y_manager_v1 (VERSÃO 2 — é o que a sessão dela anuncia, então set_screen_filter e NÃO set_screen_filter2) e chama set_screen_filter(inverted, filter). Mandar filter=2 (greyscale no enum DO PROTOCOLO) para acionar o color_mode 1.0 do shader — os dois enums não batem, essa é a pegadinha. Rodar por um serviço + timer do systemd (OnCalendar de 15 em 15 min, ou OnUnitActiveSec=2min), usando a função na_janela() do sexto trecho de código para o teste com virada de meia-noite. Guardar início/fim em horas fracionárias, como o GNOME faz (night-light-schedule-from/to, default 20.00 -> 6.00). Precisa de WAYLAND_DISPLAY e XDG_RUNTIME_DIR no ambiente do serviço — serviço de usuário, não de sistema.",
     "onde": "novo par ~/.config/zsh/scripts/aurora-leitura-agenda.{sh,service,timer} (o padrão de timer já existe: aurora-cosmic-nightlight-watch.timer, que hoje só vigia a issue #2059 e não agenda nada — não há conflito)",
     "a_quente": true,
     "custo": "Um binário/cliente novo a manter. Sem logout, sem recompilar. Independe de qual das duas soluções de temperatura estiver instalada — ele só liga/desliga o filtro.",
     "durabilidade": "Sobrevive a apt upgrade, reboot e troca de tema: não toca no binário do compositor, só fala o protocolo público. É a peça mais durável das quatro — e a única que continua valendo se a System76 entregar o night light nativo (issue #2059).",
     "trilha": "extensoes-gnome",
     "veredito": {
      "solucao": "Agendador por horário via protocolo a11y (cosmic_a11y_manager_v1 v2) + systemd timer de usuário",
      "veredito": "CONFIRMADO",
      "a_quente_confirmado": true,
      "porque": "Tudo que a proposta afirma como fato eu consegui provar nesta máquina, e a pegadinha central dela está certa.\n\nVERSÃO 2 — PROVADO. Rodei o `a11y_ler.py` (somente leitura) contra a sessão viva: `global: cosmic_a11y_manager_v1 name=53 version_anunciada=2`, seguido de `evento screen_filter (v2): inverted=0 filter=0 (disabled)`. O XML vendorizado em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/vendor/cosmic-protocols/unstable/cosmic-a11y-unstable-v1.xml declara `version=\"3\"` com `set_screen_filter2`, MAS o compositor faz `dh.create_global::<...>(2, ...)` (src/wayland/protocols/a11y.rs:64). Ou seja: o XML promete 3, o binário entrega 2. A proposta acertou ao mandar usar `set_screen_filter` e não `set_screen_filter2` — quem lesse só o XML erraria.\n\nA PEGADINHA DOS ENUMS — PROVADA. Em src/config/mod.rs:167 o enum tem o comentário do próprio upstream `// these values need to match with offscreen.frag` e `Greyscale = 1`. O XML do protocolo diz `greyscale value=\"2\"`. A tradução acontece em `protocol_to_color_filter()`. Então protocolo filter=2 -> ColorFilter::Greyscale=1 -> `color_mode` 1.0 -> exatamente o bloco que o aurora-night-light.py reescreveu. Confirmei que o patch está vivo: `strings /usr/bin/cosmic-comp | grep -c 0.77987` devolve 1.\n\nA QUENTE — PROVADO POR CÓDIGO, não por fé. O `Uniform::new(\"color_mode\", ...)` está DENTRO da função de render por frame (backend/render/mod.rs:1374), montado a cada quadro a partir de `screen_filter.filter.color_filter`. O caminho do request é síncrono: Dispatch -> `request_screen_filter` -> `backend.update_screen_filter()` -> ThreadCommand::UpdateScreenFilter -> `self.screen_filter = filter_config; self.postprocess_textures.clear();`. Nada recompila shader. O comentário do próprio upstream em kms/mod.rs:730 é explícito: \"The shader is already compiled at this point\". Não há recompilação, não há logout.\n\nCLIENTE PODE BINDAR — PROVADO. O filtro de cliente é `client_not_sandboxed` (state.rs:762), não uma allowlist de shell components. E a prova empírica é melhor que a leitura: meu script python de socket cru bindou o global agora, sem ser painel nem settings. Um serviço systemd de usuário não é sandboxed.\n\nAMBIENTE — PROVADO. `systemctl --user show-environment` já traz `WAYLAND_DISPLAY=wayland-1` e `XDG_RUNTIME_DIR=/run/user/1000`. Não precisa de gambiarra de importação de ambiente.\n\nSEM CONFLITO — PROVADO. Li o aurora-cosmic-nightlight-watch.sh inteiro: ele só faz `curl` na API do GitHub para ver se a issue #2059 fechou e manda notify-send. Timer `OnCalendar=daily`. Não agenda nada, não toca no protocolo. A proposta está certa: não há colisão.\n\nSEM AMPLIFICAÇÃO DE ESCRITA — PROVADO. Eu suspeitei que um timer de 2 em 2 min fosse martelar o arquivo de estado. Não martela: o Dispatch checa `if filter != state.a11y_state().screen_filter` ANTES de chamar `request_screen_filter`, então o PersistenceGuard que escreve ~/.local/state/cosmic-comp/a11y_screen_filter.ron (hoje contendo `(inverted: false,)`) só é criado em mudança real. A cadência de 2 min é segura.\n\nGLSL — NÃO SE APLICA, e é aí que está a força desta trilha. Ela não escreve uma linha de shader, não recompila o cosmic-comp, não encosta no binário. Por isso é a única das quatro que não compete com o patch `aurora-cosmic-comp-ws.sh --build` nem com o aurora-night-light.py: ela só fala o protocolo público por cima do que já estiver instalado.\n\nTERRITÓRIO — OK. docs/FRONTEIRA.md lista \"binário cosmic-comp (patches de workspace e night light) | Aurora\" e a TRAVA 1 do lib/comum.sh proíbe o MeowSystem de escrever em ~/.config/zsh. Como isto é script Aurora escrito à mão (padrão de toda a família aurora-*), o endereço proposto está certo. Não pisa em gtk.css, não pisa em cosmic-config do Meow.",
      "risco_real": "NÃO É ELIMINATÓRIO, e por larga margem. O pior caso desta trilha é a tela dela ficar com uma tonalidade errada — quente na hora errada, ou preto-e-branco. Ela NUNCA fica sem desktop: nada aqui toca no binário do compositor, no ELF, na sessão ou em config de boot. O compositor não reinicia, não há janela de tela preta, não há risco de não subir no reboot.\n\nVolta, em três níveis, do mais barato ao mais caro, e nenhum deles exige TTY:\n1. Ela mesma, com o mouse: Acessibilidade > Filtros de cor, o mesmo toggle que já usa hoje. Um clique desfaz.\n2. `systemctl --user disable --now aurora-leitura-agenda.timer` — mata o agendador de vez, sem sudo, sem sair da sessão.\n3. `rm ~/.local/state/cosmic-comp/a11y_screen_filter.ron` e relogar, se o estado persistido ficar estranho. Verifiquei que esse arquivo hoje tem 24 bytes e só guarda `(inverted: false,)`.\n\nNão precisa de TTY em nenhum cenário, porque em nenhum cenário o desktop cai.\n\nO RISCO REAL, o único que me preocupa de verdade, é OUTRO e a proposta não o menciona: o dia em que o patch do shader NÃO estiver aplicado. O aurora-night-light.py é repatchado pelo self-heal a cada apt (o cabeçalho dele confirma: \"O patch é refeito pelo self-heal a cada apt\"), MAS o próprio script documenta a saída de falha: se um update upstream mudar o bloco do shader, ele \"NÃO toca no binário e sai com código 3 — o self-heal apenas avisa\". Nesse dia, `filter=greyscale` volta a significar greyscale de verdade. Resultado: às 20:00, sozinha, a tela dela fica PRETO E BRANCO, sem ninguém tocar em nada, e o aviso do self-heal está num log que ela não lê. É um susto noturno silencioso, não um dano — mas é exatamente o tipo de coisa que aparece dias depois sem culpado, que é o que o FRONTEIRA.md diz para evitar. Tem conserto barato (ver correção 1) e por isso não derruba a solução.\n\nRisco menor, cosmético: se ela desligar o filtro na mão dentro da janela agendada, o timer religa em até 2 min e ela briga com o próprio computador.",
      "correcao": "Três correções. A 1 e a 2 são obrigatórias; a 3 é ajuste de expectativa.\n\n1. GUARDA ANTES DE MANDAR (obrigatória — é o que impede o susto preto-e-branco).\nO agendador NÃO pode mandar filter=2 às cegas. Antes de ligar, ele tem que confirmar que o binário vivo está patchado, com uma linha barata:\n    grep -qa '0.77987' /usr/bin/cosmic-comp || { notify-send \"Modo leitura pausado\" \"O patch do shader caiu (update do cosmic-comp?). Rode: sudo aurora-night-light.py\"; exit 0; }\nSem essa guarda, o dia em que o aurora-night-light.py sair com código 3 é o dia em que a tela dela fica em escala de cinza às 20:00 sozinha. Alternativa mais limpa e mais estável a mudanças de temperatura: chamar `aurora-night-light.py --status` (que já existe e não escreve nada) e só prosseguir se ele reportar patch aplicado.\n\n2. ECOAR O `inverted` ATUAL, NUNCA CHUTAR ZERO (obrigatória — bug de correção).\nA assinatura do v2 é `set_screen_filter(inverted, filter)`: os DOIS argumentos vão juntos, não dá para mexer em um só. Se o agendador mandar `inverted=0` fixo, ele DESLIGA a inversão de tela caso ela tenha ligado. A ordem certa é: bindar, ler o evento `screen_filter` que o compositor manda no bind (é ele que informa o `inverted` corrente — o próprio a11y_ler.py já imprime isso), e devolver esse mesmo valor no request.\nDetalhe fino que vale ouro aqui: o protocolo tem `filter=1 (unknown)` justamente para o caso inverso — o `protocol_to_color_filter()` devolve `Err(Unknown)` e o compositor NÃO mexe no filtro. Serve para alterar só o `inverted`. E para DESLIGAR o modo leitura, `filter=0 (disabled)` é válido nesta versão (só foi deprecado a partir da v3, que esta máquina não anuncia).\n\n3. \"ÀS 20:00 EM PONTO\" NÃO É GARANTIDO (ajuste de expectativa, sem conserto necessário).\nMedi: `update_screen_filter` na thread da surface faz só `self.screen_filter = filter_config; self.postprocess_textures.clear();` (kms/surface/mod.rs:1460-1463). Ele NÃO chama schedule_render. A cor muda no PRÓXIMO quadro que o compositor desenhar. Com a tela parada, isso pode esperar o relógio do painel virar o minuto ou ela mexer o mouse. Na prática: atraso de segundos, nunca falha. Não vale complicar o script por isso — vale só não prometer precisão de relógio.\n\n4. HIGIENE DE INSTALAÇÃO (não é risco, é durabilidade).\nO install.sh do Ritual da Aurora registra unidade por unidade à mão (`cp` do .timer para ~/.config/systemd/user/ + `systemctl --user enable --now`, ver linhas 618-620 e 1379-1381). Sem editar o install.sh, o timer novo some numa reinstalação. E lembrar: ~/.config/zsh tem auto-commit a cada 10 min — os três arquivos novos viram commit no repo privado dela sozinhos, sem ninguém pedir.\n\nFeitas 1 e 2, esta é de fato a peça mais durável do conjunto e a única que continua fazendo sentido se a System76 entregar o night light nativo."
     }
    },
    {
     "nome": "Copiar o modelo de configuração do GNOME (faixa, passo, formato de horário)",
     "alvo": "ui",
     "como": "Para a interface parecer o que ela já conhece: temperatura de 2700 a 6500K com passo 100 e marcas em 4000K e 5250K (é literalmente o GtkAdjustment do gnome-control-center); se ela quiser mais quente que o GNOME de fábrica permite, abrir para 1500K como as extensões fazem (o backend do GNOME vai de 1000 a 10000). Horário como double de horas fracionárias (20.00 -> 6.00), nomes de chave espelhando night_light_temperature / night_light_enabled / night_light_schedule_from / night_light_schedule_to. Textura como percentual 0-100 (é como a Bedtime expõe o color-tone-factor). Transição suave de 5s se quisermos igualar o GNOME (gsd-night-light interpola linearmente em 50ms por passo).",
     "onde": "nas chaves novas de src/config/mod.rs (rota recompilada) e no meow.conf / docs do MeowSystem — checar docs/FRONTEIRA.md antes de decidir se a chave mora no Meow ou no Ritual da Aurora",
     "a_quente": true,
     "custo": "Nenhum custo técnico; é decisão de nomenclatura. Vale fixar ANTES de escrever código, porque renomear chave depois quebra o arquivo de config dela.",
     "durabilidade": "Permanente — e alinhada com o que a System76 provavelmente vai adotar quando implementar o night light nativo, o que barateia a migração.",
     "trilha": "extensoes-gnome",
     "veredito": {
      "solucao": "Trilha \"extensoes-gnome\": copiar o modelo de configuracao do GNOME (faixa 2700-6500K passo 100 com marcas em 4000/5250, horario como double de horas fracionarias, chaves night_light_*, textura 0-100, transicao de 5s) para as chaves novas de src/config/mod.rs e para o meow.conf.",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "O nucleo factual da trilha esta errado, e da para provar.\n\n1) \"2700 a 6500K com passo 100 e marcas em 4000K e 5250K (e literalmente o GtkAdjustment do gnome-control-center)\" — FALSO. O adjustment real (panels/display/cc-night-light-page.blp) e lower=1700, upper=4700, step-increment=100, page-increment=500, marcas em 1700, 2700, 3700 e 4700, com a escala invertida. Passo 100 e a unica parte certa. 2700 e o DEFAULT/uma marca, nunca o piso; 6500 nao aparece na UI (e o GSD_COLOR_TEMPERATURE_DEFAULT do backend, o neutro RGB 1,1,1). 4000 e 5250 nao existem em lugar nenhum do night light do GNOME. gnome-control-center nem esta instalado nesta maquina (dpkg -l so tem gnome-settings-daemon-common 46.0), entao a frase \"e literalmente o GtkAdjustment\" nao era verificavel localmente — e quando fui a fonte, nao bate.\n\n2) \"se ela quiser mais quente que o GNOME de fabrica permite, abrir para 1500K\" — a premissa cai junto: o GNOME de fabrica ja vai a 1700K, mais quente que o piso de 2700 que a propria trilha propoe. A proposta clipava a faixa MAIS do que o GNOME, achando que estava copiando.\n\n3) \"o backend do GNOME vai de 1000 a 10000\" — verdadeiro no GNOME (GSD_COLOR_TEMPERATURE_MIN 1000 / MAX 10000 em plugins/color/gsd-color-state.h), mas INUTILIZAVEL aqui: /home/vitoriamaria/.config/zsh/scripts/aurora-night-light.py so conhece a tabela de whitepoints do redshift, que termina em 6500 = (1,1,1), e clampa com `return t if 1000 <= t <= 6500`. Acima de 6500 nao ha valor a interpolar. Adotar 1000-10000 como faixa nominal escreveria na doc dela um numero que o codigo nao sabe honrar.\n\n4) Formato de horario colide com o que ela JA tem. O meow.conf.exemplo (75 chaves, todas UPPER_SNAKE de shell) ja resolveu esse problema uma vez: WALLPAPER_NOITE_INICIO=\"18:00\" / WALLPAPER_NOITE_FIM=\"07:00\", HH:MM, com a janela atravessando a meia-noite documentada. Trazer double de horas fracionarias (20.00 -> 6.00) poe DOIS formatos de hora no mesmo arquivo, e o double e uma armadilha em arquivo editado a mao: quem quer 20:30 escreve 20.30 e recebe 20:18. No GNOME esse double e artefato interno — a UI coleta horas+minutos em dois GtkAdjustment e converte (dialog_adjustments_get_frac_hours). Ninguem digita. De quebra, a propria Bedtime que a trilha cita como precedente NAO usa double: usa schedule-start-hours/-minutes e schedule-end-hours/-minutes, inteiros separados. A trilha mistura dois modelos incompativeis e chama de \"o modelo do GNOME\".\n\n5) Nomes de chave night_light_temperature / night_light_enabled em snake_case ingles nao cabem no meow.conf: as 75 chaves de la sao variaveis de shell em UPPER_SNAKE. E \"as chaves novas de src/config/mod.rs E no meow.conf\" sao dois arquivos com duas sintaxes (RON x shell) — nao da para ter \"a mesma chave\" nos dois; da para ter duas, e ai a pergunta de dono fica em aberto, nao fechada.\n\n6) O \"onde\" aponta para o arquivo errado quando o assunto e a quente — ver o campo a_quente.\n\nO que sobrevive e vale guardar: passo 100 (certo), horas fracionarias como formato do GNOME (certo, mas nao para o arquivo dela), color-tone-factor inteiro percentual da Bedtime (certo, confirmado no gschema da extensao), e a transicao suave — GSD_NIGHT_LIGHT_SMOOTH_SMEAR 5.f segundos com g_timeout_add(50, gsd_night_light_smooth_cb) confirmados em plugins/color/gsd-night-light.c. Esses quatro eu confirmo.",
      "correcao": "Numeros corretos, se a trilha for reescrita:\n\nTEMPERATURA — faixa util aqui e 1700-6500K, passo 100, marcas em 1700 / 2700 / 3500 / 4700 / 6500. 1700 e o piso real do GNOME (nao 2700); 6500 e o neutro e o teto que /home/vitoriamaria/.config/zsh/scripts/aurora-night-light.py sabe representar (WHITEPOINTS termina em 6500). O 3500 entra como marca porque e o valor que ela roda hoje (/var/lib/aurora/night-light-temp = 3500). Se quiser espelhar o GNOME ao pe da letra, a UI e 1700-4700 e o backend 1000-6500 (limite da tabela, nao 10000).\n\nHORARIO — manter HH:MM string, igual ao WALLPAPER_NOITE_INICIO/_FIM que ja esta no meow.conf.exemplo (linhas 401-402), com a mesma regra de janela que atravessa a meia-noite. Nada de double. Se em algum momento a chave precisar existir do lado do compositor (RON), ai sim converter para horas fracionarias na fronteira do codigo, nunca no arquivo que ela edita.\n\nNOMES — UPPER_SNAKE em portugues, no padrao do arquivo: LEITURA_TEMPERATURA, LEITURA_TEXTURA, LEITURA_HORARIO_INICIO, LEITURA_HORARIO_FIM, LEITURA_TRANSICAO. Nada de night_light_*.\n\nTEXTURA — 0-100 inteiro esta certo (precedente color-tone-factor confirmado), mas o nome nao: color-tone-factor e \"intensidade de um preset de tom\", nao textura. Se a chave for LEITURA_TEXTURA, o precedente serve so para a escala, nao para a semantica.\n\nTRANSICAO — 5s / passo de 50ms esta certo e e barato aqui: o uniform e remontado a cada frame (src/backend/render/mod.rs:1360-1380), entao interpolar nao custa nada. Mas so existe na rota recompilada; o patch de texto no binario nao anima.\n\nONDE — a chave nao vai para o meow.conf. Ver risco_real.",
      "risco_real": "Nomear chave nao desenha pixel nenhum, entao a trilha isolada nao tem como apagar a tela dela. O risco e outro, e e o que a FRONTEIRA.md foi escrita para evitar.\n\nDONO ERRADO. /mnt/Apate/Desenvolvimento/MeowSystem/docs/FRONTEIRA.md ja responde a pergunta que a trilha manda \"checar depois\": a linha \"binario cosmic-comp (patches de workspace e night light) | Aurora | **Aurora**\". E a temperatura JA TEM chave: /var/lib/aurora/night-light-temp (contem 3500), lida por read_pref_temp() e gravada por `sudo aurora-night-light.py --temp N`. Criar uma segunda chave em meow.conf da dois donos ao mesmo valor — e o self-heal da Aurora roda de hora em hora e a cada apt reaplicando o dele. Sintoma: ela mexe no slider, funciona, e horas depois volta sozinho ao 3500, sem culpado. E exatamente o modo de falha que a FRONTEIRA descreve (\"o sintoma aparece dias depois, sem culpado\"). Volta: trivial (apagar a chave duplicada), mas o diagnostico e caro.\n\nTRAVA 1. lib/comum.sh recusa escrita em ~/.config/zsh — entao o Meow nem consegue mandar na configuracao do patcher; ele so pode conferir e dizer. Uma chave em meow.conf que \"manda na temperatura\" seria uma chave que mente.\n\nRENOMEAR DEPOIS. A propria trilha admite: renomear chave quebra o arquivo dela. Com o formato errado fixado agora (double de hora), o custo de desfazer e reescrever o meow.conf dela + a doc + o doctor. Barato hoje, caro depois — por isso vale refutar antes de virar codigo.\n\nDESKTOP. Nao fica sem. E arquivo de texto, versionado em git, sem sudo e sem rede. Mesmo a rota recompilada que a trilha pressupoe tem volta por TTY: /var/lib/aurora/ guarda o .orig de TODA versao (conferi: 7 arquivos .orig/.pkg-orig, incluindo o 5c93094 atual), e `sudo cp` do .orig por Ctrl+Alt+F3 restaura. Detalhe medido que ajuda: /proc/<pid>/exe do cosmic-comp vivo aponta para \"/usr/bin/cosmic-comp (deleted)\" — trocar o binario NAO derruba a sessao em curso, o efeito so aparece no proximo login. Isso da uma janela de conserto, mas tambem significa que um binario ruim so morde no greeter, quando ela ja nao tem desktop para consertar de dentro. O TTY continua sendo o caminho, e ele existe."
     }
    }
   ]
  },
  {
   "trilha": "agendamento",
   "achados": {
    "trilha": "Agendamento (\"liga sozinho a partir do horário X\") — desenho, units systemd, e o achado que muda a trilha inteira",
    "resumo": "O agendamento é a trilha mais barata das três, e o caminho mudou no meio da medição. A premissa herdada era que o único jeito de ligar a luz quente é a Vitória clicar em \"Acessibilidade → Filtros de cor\"; medi que é falso: o global `cosmic_a11y_manager_v1` é criado com `client_not_sandboxed` (qualquer cliente não-flatpak binda) e eu bindei ele agora, ao vivo, com um script Python de 60 linhas sem dependência nenhuma — ele leu `inverted=0 filter=0 (disabled)` da máquina dela. Ou seja: um timer de usuário consegue ligar e desligar a luz quente A QUENTE, hoje, sem recompilar nada e sem logout. O que NÃO funciona é o caminho óbvio de arquivo: `~/.local/state/cosmic-comp/a11y_screen_filter.ron` é lido uma única vez no `insert_idle` da subida da sessão e nunca mais — escrever nele não faz nada até o próximo login. Geoclue está instalado mas morto (`AvailableAccuracyLevel: 0`, e a URL padrão dele é a Mozilla Location Service, desligada); o próprio COSMIC não usa geoclue — o `cosmic-settings-daemon` deriva latitude/longitude do FUSO, por uma tabela geonames embutida no binário. Então pôr do sol sai de graça de `America/Sao_Paulo`, sem rede e sem serviço. E a rampa gradual, que eu esperava exigir recompilação, tem uma versão de hoje: os blocos de daltonismo do shader (protanopia/deuteranopia/tritanopia) são ~1500 bytes de texto que ninguém nesta máquina usa, e cabem quatro temperaturas diferentes — quatro degraus comutáveis a quente pelo mesmo protocolo.",
    "achados": [
     {
      "titulo": "O filtro de cor É comandável por script, a quente, hoje — provado ao vivo",
      "detalhe": "O `cosmic-comp` cria o global de acessibilidade com `A11yState::new::<State, _>(dh, client_not_sandboxed)` — o filtro de cliente é \"não é flatpak\", NÃO é \"é componente do shell\". Escrevi um cliente Wayland cru em Python (socket + struct, zero dependência), bindei o `cosmic_a11y_manager_v1` na sessão viva dela e recebi os eventos de estado. Saída literal: `global: cosmic_a11y_manager_v1 name=53 version_anunciada=2` / `evento magnifier: active=0` / `evento screen_filter (v2): inverted=0 filter=0 (disabled)`. NÃO enviei nenhum request de escrita (a tarefa é só leitura), mas o request existe no mesmo objeto já bindado: opcode 1, `set_screen_filter(inverted:uint, filter:uint)`. Isso derruba a premissa de que só o clique na GUI liga a luz quente, e é o que torna o agendamento possível sem tocar no compositor.",
      "evidencia": "/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/src/state.rs:762 ; execução de /tmp/meow-trabalho em 29/08/2026 17:5x",
      "confianca": "medido"
     },
     {
      "titulo": "O protocolo instalado é VERSÃO 2, não 3 — e mandar o request errado derruba o cliente",
      "detalhe": "O XML vendorizado declara `version=\"3\"` e tem `set_screen_filter2`, mas o compositor cria o global com `dh.create_global::<...>(2, ...)`. O registry me anunciou `version_anunciada=2`, medido. Consequência dura: na v3 o `set_screen_filter` (opcode 1) levanta o erro de protocolo `deprecated` e MATA a conexão do cliente. Um agendador que crave o opcode 1 quebra calado no dia em que um `apt upgrade` subir o global para 3. O cliente tem de ramificar por versão: v2 → opcode 1 `(inverted, filter)`; v3+ → opcode 2 `(inverted, filter, filter_state)`, com `filter=unknown(1)` para desligar sem perder qual filtro está selecionado.",
      "evidencia": "src/wayland/protocols/a11y.rs:64 (`create_global(2, ...)`) + vendor/cosmic-protocols/unstable/cosmic-a11y-unstable-v1.xml (enum error: deprecated) + saída do a11y_ler.py",
      "confianca": "medido"
     },
     {
      "titulo": "Escrever o a11y_screen_filter.ron NÃO liga nada — ele é lido uma vez, na subida da sessão",
      "detalhe": "Eu ia propor o caminho barato (o agendador escreve um arquivo, o compositor relê por inotify, como o relógio faz). É falso. O arquivo existe — `~/.local/state/cosmic-comp/a11y_screen_filter.ron`, conteúdo atual `( inverted: false, )` — mas o único ponto de leitura é `Config::load_dynamic` → `load_filter_state`, chamado no construtor, e o estado é empurrado para o compositor num `loop_handle.insert_idle(...)` que roda UMA vez. A escrita de volta é o `Drop` do `PersistenceGuard`. Não há watch, não há inotify, não há releitura. Escrever ali é agendar para o próximo login, que é exatamente o que ela não pediu.",
      "evidencia": "src/config/mod.rs:318-329 (insert_idle), :341-356 (load_dynamic), :377-400 (load_filter_state), :662-690 (PersistenceGuard) ; `cat ~/.local/state/cosmic-comp/a11y_screen_filter.ron`",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Geoclue está instalado, a frente está rodando, e mesmo assim não sabe onde ela está",
      "detalhe": "`geoclue-2.0 2.7.0-3ubuntu7` instalado; `geoclue.service` é `static` e está `inactive (dead)`; a frente demo está vivo (PID 3275). Mas a propriedade `AvailableAccuracyLevel` do Manager responde `uint32 0` — zero é NONE, ou seja nenhuma fonte de localização disponível. A causa está no `/etc/geoclue/geoclue.conf`: a fonte WiFi usa, por padrão, a Mozilla Location Service (\"If not set, defaults to Mozilla's Location Service with a hardcoded key\"), que foi desligada; não há `/etc/geolocation` para a static-source; não há modem 3G/GPS. E o `whitelist` de frentes do conf lista `geoclue-demo-agent;gnome-shell;io.elementary...;sm.puri.Phosh;lipstick` — nenhum componente COSMIC. Conclusão prática: qualquer desenho que dependa de geoclue nesta máquina nasce quebrado.",
      "evidencia": "`dpkg -l | grep geoclue`; `systemctl status geoclue`; `gdbus call --system --dest org.freedesktop.GeoClue2 ... GetAll` → `({'InUse': <false>, 'AvailableAccuracyLevel': <uint32 0>},)`; /etc/geoclue/geoclue.conf",
      "confianca": "medido"
     },
     {
      "titulo": "O próprio COSMIC não usa geoclue: ele tira lat/lon do FUSO HORÁRIO",
      "detalhe": "O `cosmic-settings-daemon` calcula nascer/pôr do sol (é o que move o `auto_switch` do modo claro/escuro) com a crate `sunrise` (`vendor/sunrise/src/solar_equation/mod.rs` aparece nas strings do binário). A localização vem de `src/location.rs`, que faz `static GEODATA: &[u8] = include_bytes!(\"../data/timezone-geodata.bitcode-v0-6\")` e decodifica um `BTreeMap<String, GeoPosition>` — timezone → coordenada da maior cidade próxima. Nenhuma chamada de rede, nenhum geoclue. As strings do binário confirmam: `failed to decode timezone geodata:`, `cosmic_settings_daemon::location`, `/etc/localtime`, `/usr/share/zoneinfo`. Isso é PRECEDENTE dentro da própria casa: coordenada fixa derivada do fuso é o que o sistema dela já faz.",
      "evidencia": "https://raw.githubusercontent.com/pop-os/cosmic-settings-daemon/master/src/location.rs ; `strings /usr/bin/cosmic-settings-daemon | grep -i sunrise` → vendor/sunrise/src/solar_equation/mod.rs",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Relógio e fuso estão certos, e o pôr do sol de São Paulo balança 83 minutos no ano",
      "detalhe": "`timedatectl`: `Time zone: America/Sao_Paulo (-03, -0300)`, `System clock synchronized: yes`, `NTP service: active`, `/etc/localtime -> /usr/share/zoneinfo/America/Sao_Paulo`. Não há DST no Brasil desde 2019, então o cálculo não tem o degrau que estraga agendadores. Calculei (NOAA, lat -23.5505 lon -46.6333, TZ -3): hoje 29/08/2026 nascer 06:20 / pôr 17:55; solstício de junho 06:48 / 17:29; solstício de dezembro 05:17 / 18:52. O pôr do sol varia 1h23 ao longo do ano — é grande o bastante para justificar oferecer \"por-do-sol\" como valor da chave, e pequeno o bastante para que um HH:MM fixo continue sendo uma resposta honesta se ela preferir.",
      "evidencia": "`timedatectl`; cálculo NOAA rodado em python3 no scratchpad",
      "confianca": "medido"
     },
     {
      "titulo": "Não há pista de cidade em lugar nenhum dos repos — o fuso é a única fonte",
      "detalhe": "Grep por `latitude|longitude|timezone|fuso|America/Sao|geoclue|sunset|sunrise|nascer|por do sol` em todo o MeowSystem (`*.sh`, `*.py`, `*.conf*`, `*.md`) devolve zero coordenadas. As duas únicas menções relevantes são `docs/COSMIC-THEMING.md:321` e `scripts/som.sh:12` (que falam da lista de fusos por outro motivo — confundir código de som com código de fuso) e `scripts/aplicar_tema.sh:64`, que descreve o daemon calculando nascer/pôr do sol. Portanto a proposta é: coordenada VAZIA por padrão, derivada de `/etc/localtime` como o COSMIC faz, e uma chave `LEITURA_LOCAL=\"lat,lon\"` para ela corrigir se São Paulo estiver longe demais.",
      "evidencia": "grep -rniE em /mnt/Apate/Desenvolvimento/MeowSystem (30 resultados, nenhum com coordenada)",
      "confianca": "medido"
     },
     {
      "titulo": "Um timer de 1 minuto custa quase nada de CPU e MUITO de journal — e o journal tem cura",
      "detalhe": "Medi o trabalho do tique (ler conf, calcular a janela, comparar, gravar só se mudou): 100 execuções em 0,86 s de relógio, 0,54 s de CPU (user+sys) — 8,6 ms de relógio e 5,4 ms de CPU por tique, 3,8 MB de RSS. A invocação via systemd de usuário custa 20-30 ms (cinco medições: 0,02 0,03 0,03 0,02 0,03). O custo real é outro: o `xstick-vigia.timer`, que já roda de minuto em minuto nesta máquina, escreveu 2884 linhas de journal nas últimas 24 h — exatamente 2 por invocação (\"Starting…\"/\"Finished…\") × 1440. O `meow-wallpaper` (15 min) escreveu 181. Um agendador minutely triplicaria o ruído do journal dela por uma feature que muda de estado 8 vezes por dia. A cura está no manual: `LogLevelMax=` \"é aplicado a qualquer mensagem escrita por qualquer processo desta unidade, ASSIM COMO a qualquer mensagem escrita pelo gerenciador (PID 1) em referência a esta unidade\" — `LogLevelMax=notice` corta o Starting/Finished, que são `info`.",
      "evidencia": "/usr/bin/time em 100 execuções; systemd-run --user --wait ×5; `journalctl --user -u xstick-vigia.service --since '24 hours ago' | wc -l` → 2884; `man systemd.exec`, verbete LogLevelMax=",
      "confianca": "medido"
     },
     {
      "titulo": "O OnCalendar sabe fazer faixa de minutos — e sabe fazer passo de 5",
      "detalhe": "Testei quatro formas com `systemd-analyze calendar`. `*-*-* 18:00..19:00` PARSEIA (hora 18, minuto 00..19, segundo 00) e dispara 18:00, 18:01, 18:02… — é uma rampa de 20 tiques restrita à janela, sem tocar o resto do dia. `*-*-* *:00/5` normaliza para `*-*-* *:00/5:00` e dispara de 5 em 5 minutos o dia todo. `*-*-* 18:00/5:00` dispara de 5 em 5 dentro da hora 18 só. O que NÃO parseia é `*-*-* 18:00..18:19:00` (\"Argumento inválido\") — o `..` vale dentro de UM campo, não atravessando dois pontos. Isso mata a ideia de daemon dormindo: dá para fazer a rampa inteira com timer sem estado, e um tique perdido por suspensão se conserta sozinho no seguinte, porque cada tique recalcula do relógio.",
      "evidencia": "`systemd-analyze calendar --iterations=5` para as quatro expressões, em 29/08/2026",
      "confianca": "medido"
     },
     {
      "titulo": "A rampa gradual tem uma versão de HOJE: quatro degraus escondidos no shader",
      "detalhe": "Extraí de novo o texto do shader do binário vivo. O bloco `else if (color_mode >= 2.0)` — protanopia, deuteranopia, tritanopia — tem cerca de 1500 bytes de GLSL (matrizes LMS, vetor de erro, correção). Ninguém nesta máquina usa esses três filtros (o estado lido pelo protocolo é `filter=0 disabled`, e o `a11y_screen_filter.ron` diz `inverted: false` sem `color_filter`). Cabem, com folga e preservando o tamanho byte a byte por padding de espaços — que é exatamente o que o `aurora-night-light.py` já sabe fazer —, três multiplicações de temperatura extras. Resultado: `color_mode` 1/2/3/4 vira uma escada de 4 temperaturas, comutável A QUENTE pelo mesmo `set_screen_filter`. Não é o slider contínuo (isso é da outra trilha, e exige recompilar), mas transforma o \"tapa de 3500K às 18:00\" numa rampa de 4 passos.",
      "evidencia": "extração do shader de /usr/bin/cosmic-comp (bloco color_mode==1.0 já patchado pela Aurora, seguido de ~1500 B de daltonismo); ~/.config/zsh/scripts/aurora-night-light.py (padding preservando tamanho)",
      "confianca": "medido"
     },
     {
      "titulo": "O ambiente Wayland ESTÁ no gerenciador de usuário — a unit consegue falar com o compositor",
      "detalhe": "`systemctl --user show-environment` traz `WAYLAND_DISPLAY=wayland-1`, `XDG_RUNTIME_DIR=/run/user/1000`, `XDG_CURRENT_DESKTOP=COSMIC`, `XDG_SESSION_TYPE=wayland`. O socket `/run/user/1000/wayland-1` existe e é do usuário dela. Sem isso o desenho inteiro cairia (uma unit de SISTEMA não teria como). E `cosmic-session.target` existe e está `active` — é o alvo certo para pendurar o timer, porque fora da sessão gráfica o agendador não tem com quem falar e ficaria falhando 288 vezes por dia.",
      "evidencia": "`systemctl --user show-environment`; `ls -la /run/user/1000/wayland-*`; `systemctl --user list-units --all | grep cosmic-session.target`",
      "confianca": "medido"
     },
     {
      "titulo": "Comparação dos cinco agendadores, com os números de verdade",
      "detalhe": "GNOME Night Light — horário manual (`night-light-schedule-from`=20.00, `-to`=6.00, em horas decimais) OU pôr do sol calculado (`night-light-schedule-automatic`=true, o padrão). Localização por geoclue (`gclue_simple_new`, precisão de cidade), com fallback na gsettings `night-light-last-coordinates` (padrão `(91,181)`, coordenada inválida de propósito para forçar atualização). Temperatura padrão 2700K. Transição: DUAS escalas — `GSD_NIGHT_LIGHT_SMOOTH_SMEAR 5.f` segundos para uma mudança pedida pelo usuário (callback a cada 50 ms, interpolação linear), e `GSD_NIGHT_LIGHT_POLL_SMEAR 1` HORA para a rampa em torno do nascer/pôr do sol. Polling: `GSD_NIGHT_LIGHT_POLL_TIMEOUT 60` segundos; `GSD_NIGHT_LIGHT_SCHEDULE_TIMEOUT 5` segundos após mudança de config. || KDE Night Light — `DayTemperature` 6500, `NightTemperature` 4500, `Active` false, `Mode` DarkLight (nas versões atuais os modos são: automático por localização, coordenada fixa, horários manuais, constante). A rampa não é um tempo fixo: ele anda em degraus de `TEMPERATURE_STEP = 50` K e calcula o intervalo do timer dividindo o tempo que falta até a próxima transição pelo número de degraus (`interval = dateTime.msecsTo(m_prev.second) * TEMPERATURE_STEP / abs(target - current)`); só o ajuste de PREVIEW tem duração cravada, `QUICK_ADJUST_DURATION = 2000` ms. O nascer/pôr do sol vem de um `KDarkLightScheduleProvider` externo. || redshift/gammastep — não é horário nem \"pôr do sol\": é ELEVAÇÃO SOLAR. Interpola entre noite e dia com `get_transition_progress_from_elevation()` entre `transition->low` e `transition->high` (padrões 3° e -6°), o que dá uma rampa naturalmente longa perto do crepúsculo. `SLEEP_DURATION 5000` ms entre reavaliações, `SLEEP_DURATION_SHORT 100` ms durante fade, `FADE_LENGTH 40` iterações → ~4 s de fade. Localização: geoclue2 ou manual (lat/lon na mão). || wlsunset — `config.high_temp = 6500`, `config.low_temp = 4000`, `config.gamma = 1.0`. Com `-l`/`-L` calcula o sol; com `-S`/`-s` (HH:MM) entra em `config.manual_time = true` e ignora a coordenada. Passo de animação `anim_kelvin_step = 10` K, com o intervalo derivado da largura do crepúsculo (`dawn_step_time = max(1, (sunrise - dawn) * anim_kelvin_step / temp_diff)`) — ou seja, ~1 s por degrau de 10K. Inútil aqui: exige `zwlr_gamma_control_manager_v1`, que o cosmic-comp não expõe (e nenhum dos três está instalado nesta máquina — `which redshift gammastep wlsunset` não acha nada). || MIUI/HyperOS Modo de leitura — três modos de agenda MUTUAMENTE EXCLUSIVOS: \"Boa leitura noturna\" (horário de dormir padrão), \"Personalizado\" (início e fim na mão) e \"Do pôr ao nascer do sol\". O modo pôr do sol depende do fuso do aparelho estar certo. A textura (o que deixa parecido com Kindle) tem slider próprio de intensidade e é ortogonal à agenda.",
      "evidencia": "gsettings local (org.gnome.settings-daemon.plugins.color) + /usr/share/glib-2.0/schemas/*.color.gschema.xml ; gsd-night-light.c ; kwin nightlightsettings.kcfg + nightlightmanager.cpp ; redshift.c ; wlsunset main.c ; documentação Xiaomi/TechBone",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "A janela que atravessa a meia-noite já tem dono neste repo — e o desenho dele é o certo",
      "detalhe": "O `scripts/wallpaper.sh` já resolve exatamente este problema para o carrossel noturno, com `WALLPAPER_NOITE_INICIO=\"18:00\"` / `WALLPAPER_NOITE_FIM=\"07:00\"` e a função `e_noite()` (linha 637): quando `início > fim` a noite é \"depois do início OU antes do fim\", e `início == fim` vale NOITE O DIA INTEIRO (a leitura alternativa — janela vazia — seria uma chave que não faz nada e não avisa, o defeito que o projeto mais documenta ter cometido). O agendador de leitura tem de herdar essa semântica em vez de inventar outra, senão a mesma máquina passa a ter duas definições de \"noite\". Herdei também a precedência de leitura de chave: AMBIENTE > meow.conf > padrão (`valor_da_chave`, linha ~208), que existe porque uma chave nova lida só do ambiente é obedecida pela CLI e ignorada pelo timer.",
      "evidencia": "scripts/wallpaper.sh:616-652 (minutos_de_hora, e_noite), :205-250 (conf_bruto/valor_da_chave/sim_ou_nao), meow.conf.exemplo:387-402",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "A função de degrau, escrita e testada — inclusive na virada da meia-noite",
      "detalhe": "Rodei a implementação em bash puro. Janela 18:00→07:00, rampa 20 min, 4 degraus: 17:55→0, 18:00→1, 18:04→1, 18:05→2, 18:10→3, 18:15→4, 18:20→4, 23:00→4, 06:39→4, 06:45→3, 06:50→2, 06:55→1, 07:00→0. Mesma janela com rampa 0 e 1 degrau (o mundo de hoje, sem o patch de 4 degraus): 17:59→0, 18:00→1, 03:00→1, 06:59→1, 07:00→0. O cálculo é derivado SÓ do relógio, sem contador acumulado — que é a razão de a suspensão/hibernação não desalinhar a rampa (é o mesmo princípio do redshift, que reavalia a elevação em vez de contar passos).",
      "evidencia": "execução de /tmp/meow-trabalho em 29/08/2026",
      "confianca": "medido"
     }
    ],
    "codigo": [
     {
      "para_que": "O cliente Wayland cru que LÊ e ESCREVE o filtro de tela do COSMIC, a quente, sem nenhuma dependência (nem pywayland, nem cargo). É a peça que torna o agendamento possível hoje. O modo `ler` foi executado ao vivo na máquina dela em 29/08/2026 e devolveu `inverted=0 filter=0 (disabled)`; os modos de escrita NÃO foram executados (a tarefa era só leitura). Ramifica por versão do global: v2 usa `set_screen_filter` (opcode 1), v3+ usa `set_screen_filter2` (opcode 2) — mandar o opcode errado numa v3 levanta o erro de protocolo `deprecated` e mata a conexão.",
      "linguagem": "python",
      "fonte": "autoral, sobre o wire protocol do Wayland e o vendor/cosmic-protocols/unstable/cosmic-a11y-unstable-v1.xml da árvore em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/",
      "trecho": "#!/usr/bin/env python3\n# scripts/leitura_filtro.py — fala com o filtro de tela do cosmic-comp.\n#\n# POR QUE ISTO EXISTE, E POR QUE NÃO É UM ARQUIVO DE CONFIG\n#   O caminho barato seria escrever uma chave e deixar o compositor reler. Medido\n#   em 29/08/2026: NÃO EXISTE esse caminho. O `~/.local/state/cosmic-comp/\n#   a11y_screen_filter.ron` é lido UMA vez, no `insert_idle` da subida da sessão\n#   (src/config/mod.rs:318-329), e só é reescrito pelo `Drop` do PersistenceGuard.\n#   Sem watch, sem inotify. Escrever nele agenda para o próximo login.\n#\n#   O único caminho a quente é o protocolo `cosmic_a11y_manager_v1`. E ele é\n#   ABERTO: o global nasce com `client_not_sandboxed` (src/state.rs:762) — o filtro\n#   é \"não é flatpak\", não \"é componente do shell\". Qualquer script binda.\n#\n# POR QUE PYTHON CRU E NÃO RUST\n#   O wire protocol do Wayland é: id do objeto (u32), (tamanho<<16 | opcode) (u32),\n#   argumentos, tudo little-endian e alinhado em 4 bytes. São 60 linhas. Um cliente\n#   em Rust custaria um `cargo build` no caminho de um timer, e este projeto já tem\n#   Python em scripts/ (appid_sonda.py, converter_icone.py).\n#\n# USO\n#   leitura_filtro.py ler            imprime \"<inverted> <filter>\" e sai 0\n#   leitura_filtro.py degrau <0..4>  0 = desliga; 1..4 = color_mode 1..4\nimport os, socket, struct, sys\n\nIFACE = \"cosmic_a11y_manager_v1\"\n# O enum `filter` do XML. O degrau N do agendador vira o color_mode N do shader,\n# e color_mode N é o filtro N+1 do protocolo (greyscale=2 é color_mode 1.0).\nDEGRAU_PARA_FILTRO = {1: 2, 2: 3, 3: 4, 4: 5}\nNOMES = {0: \"disabled\", 1: \"unknown\", 2: \"greyscale\",\n         3: \"protanopia\", 4: \"deuteranopia\", 5: \"tritanopia\"}\n\n\ndef _msg(obj, op, payload=b\"\"):\n    return struct.pack(\"<II\", obj, ((8 + len(payload)) << 16) | op) + payload\n\n\ndef _wstr(s):\n    b = s.encode() + b\"\\0\"\n    return struct.pack(\"<I\", len(b)) + b + b\"\\0\" * ((-len(b)) % 4)\n\n\nclass Sessao:\n    DISPLAY, REG, A11Y = 1, 2, 4\n\n    def __init__(self):\n        rt = os.environ.get(\"XDG_RUNTIME_DIR\")\n        wd = os.environ.get(\"WAYLAND_DISPLAY\")\n        if not rt or not wd:\n            raise SystemExit(3)   # 3 = sem dependência: não há sessão gráfica\n        self.s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)\n        self.s.settimeout(3)\n        try:\n            self.s.connect(wd if wd.startswith(\"/\") else os.path.join(rt, wd))\n        except OSError:\n            raise SystemExit(3)\n        self.versao = 0\n        self.estado = None      # (inverted, filter) ou (inverted, filter, ativo)\n        self._bind()\n\n    # Um roundtrip: manda wl_display.sync e drena até a callback voltar.\n    def _drenar(self, sync_id):\n        self.s.sendall(_msg(self.DISPLAY, 0, struct.pack(\"<I\", sync_id)))\n        buf = b\"\"\n        while True:\n            try:\n                pedaco = self.s.recv(65536)\n            except socket.timeout:\n                return\n            if not pedaco:\n                return\n            buf += pedaco\n            while len(buf) >= 8:\n                obj, cab = struct.unpack(\"<II\", buf[:8])\n                tam, op = cab >> 16, cab & 0xFFFF\n                if len(buf) < tam:\n                    break\n                corpo, buf = buf[8:tam], buf[tam:]\n                if obj == sync_id and op == 0:\n                    return\n                self._evento(obj, op, corpo)\n\n    def _evento(self, obj, op, corpo):\n        if obj == self.REG and op == 0:                      # wl_registry.global\n            nome, = struct.unpack(\"<I\", corpo[:4])\n            n, = struct.unpack(\"<I\", corpo[4:8])\n            if corpo[8:8 + n - 1].decode() == IFACE:\n                pad = (n + 3) & ~3\n                self._alvo = (nome, struct.unpack(\"<I\", corpo[8 + pad:12 + pad])[0])\n        elif obj == self.A11Y and op == 1:                    # screen_filter (v2)\n            self.estado = struct.unpack(\"<II\", corpo[:8])\n        elif obj == self.A11Y and op == 2:                    # screen_filter2 (v3)\n            self.estado = struct.unpack(\"<III\", corpo[:12])\n        elif obj == self.DISPLAY and op == 0:                 # wl_display.error\n            _, cod, n = struct.unpack(\"<III\", corpo[:12])\n            sys.stderr.write(\"erro de protocolo %d: %s\\n\"\n                             % (cod, corpo[12:12 + n - 1].decode()))\n            raise SystemExit(2)\n\n    def _bind(self):\n        self._alvo = None\n        self.s.sendall(_msg(self.DISPLAY, 1, struct.pack(\"<I\", self.REG)))\n        self._drenar(3)\n        if not self._alvo:\n            raise SystemExit(3)   # compositor sem o global: nada a fazer\n        nome, self.versao = self._alvo\n        usar = min(self.versao, 3)\n        self.s.sendall(_msg(self.REG, 0, struct.pack(\"<I\", nome)\n                            + _wstr(IFACE) + struct.pack(\"<II\", usar, self.A11Y)))\n        self._drenar(5)\n\n    # NA v2 o request é (inverted, filter) e `disabled`(0) desliga.\n    # NA v3 é (inverted, filter, filter_state), `disabled` no filtro é ERRO de\n    # protocolo, e desligar se faz com filter=unknown(1) + estado=0 — que preserva\n    # qual filtro ela tinha escolhido na GUI.\n    def degrau(self, n):\n        inv = self.estado[0] if self.estado else 0\n        if self.versao >= 3:\n            if n == 0:\n                arg = (inv, 1, 0)\n            else:\n                arg = (inv, DEGRAU_PARA_FILTRO[n], 1)\n            self.s.sendall(_msg(self.A11Y, 2, struct.pack(\"<III\", *arg)))\n        else:\n            flt = 0 if n == 0 else DEGRAU_PARA_FILTRO[n]\n            self.s.sendall(_msg(self.A11Y, 1, struct.pack(\"<II\", inv, flt)))\n        self._drenar(7)   # roundtrip: não sair antes de o compositor processar\n\n    def como_degrau(self):\n        if not self.estado:\n            return 0\n        if len(self.estado) == 3 and self.estado[2] == 0:\n            return 0\n        flt = self.estado[1]\n        for d, f in DEGRAU_PARA_FILTRO.items():\n            if f == flt:\n                return d\n        return 0\n\n\nif __name__ == \"__main__\":\n    cmd = sys.argv[1] if len(sys.argv) > 1 else \"ler\"\n    ses = Sessao()\n    if cmd == \"ler\":\n        e = ses.estado or (0, 0)\n        print(\"versao=%d degrau=%d inverted=%d filter=%d (%s)\"\n              % (ses.versao, ses.como_degrau(), e[0], e[1], NOMES.get(e[1], \"?\")))\n    elif cmd == \"degrau\":\n        n = int(sys.argv[2])\n        if ses.como_degrau() != n:\n            ses.degrau(n)\n        print(ses.como_degrau())\n    else:\n        sys.stderr.write(\"uso: leitura_filtro.py {ler|degrau <0..4>}\\n\")\n        raise SystemExit(2)\n"
     },
     {
      "para_que": "O agendador propriamente dito, no formato do MeowSystem (cabeçalho longo, aplicar/conferir/estado/remover, meow_escrever, MEOW_OK/MEOW_DIVERGENTE/MEOW_ERRO/MEOW_SEM_DEPENDENCIA). A função `_leitura_degrau` foi executada e conferida contra 24 horários, inclusive na virada da meia-noite.",
      "linguagem": "bash",
      "fonte": "autoral, modelado em scripts/relogio.sh e scripts/wallpaper.sh do MeowSystem",
      "trecho": "#!/usr/bin/env bash\n# leitura.sh — o MODO DE LEITURA liga e desliga sozinho, no horário dela.\n#\n# O QUE ELA PEDIU\n#   Em 29/08/2026, com um print do \"Modo de leitura > Clássico\" do HyperOS:\n#   \"não conseguimos um slicer pra ambos? pra eu ir definindo? e eu conseguir\n#   setar uma mudança automática a partir de horário x?\". Este arquivo é o\n#   TERCEIRO pedido — o horário. Os dois sliders são de outros arquivos.\n#\n# ============================================================================\n# O ACHADO QUE MUDOU O DESENHO (medido em 29/08/2026, nesta máquina)\n# ============================================================================\n#\n# 1. NÃO EXISTE ARQUIVO PARA ESCREVER. E EU IA ESCREVER UM.\n#    O caminho natural aqui seria o do `relogio.sh`: gravar uma chave e deixar o\n#    COSMIC reler. O arquivo até existe —\n#    `~/.local/state/cosmic-comp/a11y_screen_filter.ron`, hoje `( inverted: false, )` —\n#    mas ele é lido UMA vez, no `insert_idle` da subida da sessão\n#    (src/config/mod.rs:318-329 da árvore em /mnt/Apate/Desenvolvimento/\n#    cosmic-comp-patch/), e só é reescrito pelo `Drop` do `PersistenceGuard`\n#    (:662-690). Não há watch. Escrever ali agenda para o PRÓXIMO LOGIN, que é\n#    o oposto do que ela pediu.\n#\n# 2. O PROTOCOLO ESTÁ ABERTO, E ISSO SALVA A SPRINT.\n#    `A11yState::new::<State, _>(dh, client_not_sandboxed)` (src/state.rs:762): o\n#    filtro de cliente é \"não é flatpak\", NÃO \"é componente do shell\". Bindei o\n#    global com um cliente Python cru na sessão viva dela e recebi o estado:\n#        global: cosmic_a11y_manager_v1 name=53 version_anunciada=2\n#        evento screen_filter (v2): inverted=0 filter=0 (disabled)\n#    Ou seja: dá para ligar e desligar a luz quente A QUENTE, hoje, de dentro de\n#    um timer, sem recompilar nada e sem logout. É o `leitura_filtro.py` ao lado.\n#\n# 3. A VERSÃO É 2, E ISSO É UMA ARMADILHA COM DATA DE VALIDADE.\n#    O XML vendorizado declara version=3, mas o compositor cria o global com\n#    `create_global(2, ...)`. Na v3 o request antigo levanta o erro `deprecated` e\n#    MATA a conexão. O cliente ramifica por versão de propósito: no dia em que um\n#    `apt upgrade` subir para 3, isto continua funcionando em vez de morrer calado.\n#\n# 4. GEOCLUE NÃO SERVE, E O PRÓPRIO COSMIC NÃO USA GEOCLUE.\n#    `geoclue-2.0` está instalado e a frente demo está vivo, mas o Manager\n#    responde `AvailableAccuracyLevel: uint32 0` — nenhuma fonte. A fonte WiFi\n#    aponta, por padrão, para a Mozilla Location Service, que foi desligada.\n#    E o `cosmic-settings-daemon`, que já calcula nascer/pôr do sol para o\n#    `auto_switch` do tema, NÃO usa geoclue: ele decodifica uma tabela geonames\n#    embutida no binário e olha o FUSO (`src/location.rs`, `include_bytes!(\n#    \"../data/timezone-geodata.bitcode-v0-6\")`). Aqui o fuso é America/Sao_Paulo,\n#    e a coordenada sai dele. `LEITURA_LOCAL` existe para ela corrigir se São\n#    Paulo estiver longe demais — o pôr do sol balança 1h23 no ano (17:29 em\n#    junho, 18:52 em dezembro), então errar de cidade custa minutos, não horas.\n#\n# ============================================================================\n# POR QUE UM TIMER SEM ESTADO, E NÃO UM DAEMON DORMINDO\n# ============================================================================\n#   A rampa gradual tenta puxar para o desenho errado: um processo que dorme e\n#   conta passos. Ele erra em duas situações que esta máquina tem: suspensão\n#   (acorda contando de onde parou, com a tela na cor de duas horas atrás) e\n#   morte do processo (a rampa para no meio e ninguém percebe).\n#\n#   Aqui cada tique RECALCULA O DEGRAU A PARTIR DO RELÓGIO e não guarda contador\n#   nenhum. É o princípio do redshift, que reavalia a elevação do sol em vez de\n#   acumular passos. Um tique perdido se conserta sozinho no seguinte.\n#\n#   O CUSTO, MEDIDO E NÃO ESTIMADO: o trabalho de um tique é 8,6 ms de relógio e\n#   5,4 ms de CPU (100 execuções em 0,86 s); a invocação via systemd custa\n#   20-30 ms. A 5 minutos são 288 tiques/dia — 7 segundos de relógio por dia. O\n#   custo REAL é o journal: o `xstick-vigia.timer`, que já roda de minuto em\n#   minuto aqui, escreveu 2884 linhas em 24 h (2 por invocação). Por isso o\n#   `.timer` é de 5 minutos e não de 1, e por isso o `.service` leva\n#   `LogLevelMax=notice` — o manual do systemd.exec diz que ele filtra também as\n#   mensagens que o GERENCIADOR escreve sobre a unidade, que é onde mora o\n#   Starting/Finished.\n#\n# A TRÉGUA: SE ELA DESLIGAR NA MÃO, ISTO CALA A BOCA ATÉ AMANHÃ\n#   O filtro tem controle na GUI (Acessibilidade → Filtros de cor). Sem trégua,\n#   ela desligaria às 20h e o timer religaria em 5 minutos, para sempre — a\n#   versão de 288 vezes por dia do erro que custou a opacidade do painel. Então:\n#   quando o `tique` vê o compositor DESLIGADO e o registro diz que fomos NÓS que\n#   ligamos, isso é ela dizendo não. Grava uma trégua e não toca em mais nada até\n#   o fim da janela. A trégua morre sozinha no fim da noite; não há o que limpar.\n#\n# USO\n#   leitura.sh tique     o que o timer chama; aplica o degrau da hora atual\n#   leitura.sh aplicar   instala/liga o timer conforme o conf, e dá um tique\n#   leitura.sh conferir  0 = conforme · 1 = divergente · 2 = erro · 3 = sem sessão\n#   leitura.sh estado    o que o compositor está desenhando AGORA, e por quê\n#   leitura.sh remover   desliga o filtro, para e desarma o timer\nset -uo pipefail\n\nMEOW_RAIZ=\"${MEOW_RAIZ:-$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)}\"\n# shellcheck source=../lib/comum.sh\n. \"$MEOW_RAIZ/lib/comum.sh\"\n\n# VAZIO = NÃO TOCA, igual a RELOGIO_SEGUNDOS, JANELAS_TILING e VIDRO_OPACIDADE_*.\nLEITURA_AGENDA=\"${LEITURA_AGENDA:-}\"\nLEITURA_HORARIO_INICIO=\"${LEITURA_HORARIO_INICIO:-}\"\nLEITURA_HORARIO_FIM=\"${LEITURA_HORARIO_FIM:-}\"\nLEITURA_RAMPA=\"${LEITURA_RAMPA:-}\"\nLEITURA_DEGRAUS=\"${LEITURA_DEGRAUS:-}\"\nLEITURA_LOCAL=\"${LEITURA_LOCAL:-}\"\n\nLEITURA_CLIENTE=\"$MEOW_RAIZ/scripts/leitura_filtro.py\"\nLEITURA_TREGUA=\"$MEOW_ESTADO/leitura/tregua\"\nLEITURA_NOSSO=\"$MEOW_ESTADO/leitura/ultimo-degrau\"\nLEITURA_UNIDADE=\"meow-leitura.timer\"\n\n# --- as horas -----------------------------------------------------------------\n# Mesmo parser do wallpaper.sh:616. Não é preguiça: é para a máquina inteira ter\n# UMA definição de \"18:00\", em vez de duas que divergem no dia do bug.\n_leitura_minutos() {\n  local v=\"$1\" h m\n  case \"$v\" in [0-9][0-9]:[0-9][0-9]|[0-9]:[0-9][0-9]) ;; *) return 1 ;; esac\n  h=\"${v%%:*}\"; m=\"${v##*:}\"\n  [ \"$((10#$h))\" -le 23 ] && [ \"$((10#$m))\" -le 59 ] || return 1\n  echo $(( 10#$h * 60 + 10#$m ))\n}\n\n# \"por-do-sol\" e \"nascer-do-sol\" resolvem pela coordenada; qualquer outra coisa\n# tem de ser HH:MM. Delega ao helper para não ter duas trigonometrias no repo.\n_leitura_hora_da_chave() {\n  local v=\"$1\" padrao=\"$2\" qual\n  case \"$v\" in\n    por-do-sol|por|sunset)        qual=por ;;\n    nascer-do-sol|nascer|sunrise) qual=nascer ;;\n    *) _leitura_minutos \"$v\" && return 0\n       meow_aviso \"$3=\\\"$v\\\" não é HH:MM nem por-do-sol; usando $padrao\"\n       _leitura_minutos \"$padrao\"; return 0 ;;\n  esac\n  \"$MEOW_RAIZ/scripts/leitura_sol.py\" \"$qual\" \"${LEITURA_LOCAL:-}\" 2>/dev/null \\\n    || { meow_aviso \"não consegui calcular o $qual do sol; usando $padrao\"\n         _leitura_minutos \"$padrao\"; }\n}\n\n# O CORAÇÃO. Devolve 0 (desligado) até N (mais quente), só do relógio.\n#\n# CONFERIDO EM 24 HORÁRIOS, incluindo a virada da meia-noite. Com 18:00→07:00,\n# rampa 20, N=4: 17:55→0 · 18:00→1 · 18:05→2 · 18:10→3 · 18:15→4 · 23:00→4 ·\n# 06:45→3 · 06:55→1 · 07:00→0. Com rampa 0 e N=1 (o mundo de hoje, sem o patch\n# de quatro degraus): 17:59→0 · 18:00→1 · 06:59→1 · 07:00→0.\n#\n# A JANELA ATRAVESSA A MEIA-NOITE e `início == fim` vale o DIA INTEIRO — as duas\n# regras são as do `e_noite()` do wallpaper.sh:637, herdadas de propósito.\n# A rampa é aparada em metade da janela: sem isso, uma janela de 30 min com\n# rampa de 20 teria subida e descida se sobrepondo, e o degrau oscilaria.\n_leitura_degrau() {\n  local ini=\"$1\" fim=\"$2\" rampa=\"$3\" n=\"$4\" agora=\"$5\"\n  local dur desde meio\n  dur=$(( (fim - ini + 1440) % 1440 )); [ \"$dur\" -eq 0 ] && dur=1440\n  desde=$(( (agora - ini + 1440) % 1440 ))\n  [ \"$desde\" -ge \"$dur\" ] && { echo 0; return; }\n  meio=$(( dur / 2 )); [ \"$rampa\" -gt \"$meio\" ] && rampa=\"$meio\"\n  if [ \"$rampa\" -gt 0 ] && [ \"$desde\" -lt \"$rampa\" ]; then\n    echo $(( (n * (desde + 1) + rampa - 1) / rampa )); return\n  fi\n  if [ \"$rampa\" -gt 0 ] && [ $((dur - desde)) -le \"$rampa\" ]; then\n    echo $(( (n * (dur - desde) + rampa - 1) / rampa )); return\n  fi\n  echo \"$n\"\n}\n\n_leitura_dependencia() {\n  if [ ! -x \"$LEITURA_CLIENTE\" ]; then\n    meow_pula \"leitura_filtro.py ausente — nada a agendar\"; return \"$MEOW_SEM_DEPENDENCIA\"\n  fi\n  if [ -z \"${WAYLAND_DISPLAY:-}\" ] || [ -z \"${XDG_RUNTIME_DIR:-}\" ]; then\n    meow_pula \"sem sessão Wayland — o filtro de tela só existe dentro da sessão\"\n    return \"$MEOW_SEM_DEPENDENCIA\"\n  fi\n  return \"$MEOW_OK\"\n}\n\n_leitura_no_compositor() { \"$LEITURA_CLIENTE\" ler 2>/dev/null | sed -n 's/.*degrau=\\([0-9]*\\).*/\\1/p'; }\n\n_leitura_alvo() {\n  local ini fim rampa n\n  ini=\"$(_leitura_hora_da_chave \"${LEITURA_HORARIO_INICIO:-18:00}\" 18:00 LEITURA_HORARIO_INICIO)\"\n  fim=\"$(_leitura_hora_da_chave \"${LEITURA_HORARIO_FIM:-07:00}\" 07:00 LEITURA_HORARIO_FIM)\"\n  rampa=\"${LEITURA_RAMPA:-0}\"; case \"$rampa\" in ''|*[!0-9]*) rampa=0 ;; esac\n  n=\"${LEITURA_DEGRAUS:-1}\";  case \"$n\" in ''|*[!0-9]*|0) n=1 ;; esac\n  [ \"$n\" -gt 4 ] && n=4\n  _leitura_degrau \"$ini\" \"$fim\" \"$rampa\" \"$n\" \"$(_leitura_minutos \"$(date +%H:%M)\")\"\n}\n\ncmd_tique() {\n  [ \"$LEITURA_AGENDA\" = \"sim\" ] || { meow_pula \"LEITURA_AGENDA não é sim — o horário é seu\"; return \"$MEOW_OK\"; }\n  _leitura_dependencia || return $?\n\n  local alvo agora nosso\n  alvo=\"$(_leitura_alvo)\"\n  agora=\"$(_leitura_no_compositor)\"; agora=\"${agora:-0}\"\n  nosso=\"$(cat \"$LEITURA_NOSSO\" 2>/dev/null || echo 0)\"\n\n  # FORA DA JANELA: a trégua morre junto com a noite, e nós desligamos o que\n  # nós ligamos — nunca o que ela ligou (por isso o teste do `nosso`).\n  if [ \"$alvo\" = \"0\" ]; then\n    meow_seco || rm -f \"$LEITURA_TREGUA\" 2>/dev/null\n    if [ \"$agora\" != \"0\" ] && [ \"$nosso\" != \"0\" ]; then\n      meow_seco && meow_muda \"desligaria o modo de leitura (fim da janela)\" \\\n                || { \"$LEITURA_CLIENTE\" degrau 0 >/dev/null; meow_muda \"modo de leitura desligado\"; }\n      meow_seco || printf '0' > \"$LEITURA_NOSSO\"\n    else\n      meow_ok \"fora da janela; nada a fazer\"\n    fi\n    return \"$MEOW_OK\"\n  fi\n\n  # A TRÉGUA. Compositor desligado + registro dizendo que fomos nós = ela disse\n  # não. Um religamento aqui seria discutir com quem está na frente da tela.\n  if [ -f \"$LEITURA_TREGUA\" ]; then\n    meow_pula \"trégua: você desligou na mão; volto no início da próxima janela\"\n    return \"$MEOW_OK\"\n  fi\n  if [ \"$agora\" = \"0\" ] && [ \"$nosso\" != \"0\" ]; then\n    meow_seco || { mkdir -p \"$(dirname \"$LEITURA_TREGUA\")\" 2>/dev/null\n                   date +%s > \"$LEITURA_TREGUA\"; printf '0' > \"$LEITURA_NOSSO\"; }\n    meow_aviso \"você desligou o filtro na mão — trégua até o fim desta janela\"\n    meow_registrar \"leitura.sh tregua alvo=$alvo\"\n    return \"$MEOW_OK\"\n  fi\n\n  if [ \"$agora\" = \"$alvo\" ]; then\n    meow_ok \"modo de leitura já no degrau $alvo\"\n    return \"$MEOW_OK\"\n  fi\n  if meow_seco; then\n    meow_muda \"poria o modo de leitura no degrau $alvo (está em $agora)\"\n    return \"$MEOW_DIVERGENTE\"\n  fi\n  \"$LEITURA_CLIENTE\" degrau \"$alvo\" >/dev/null || {\n    meow_erro \"o compositor recusou o degrau $alvo\"; return \"$MEOW_ERRO\"; }\n  mkdir -p \"$(dirname \"$LEITURA_NOSSO\")\" 2>/dev/null; printf '%s' \"$alvo\" > \"$LEITURA_NOSSO\"\n  meow_muda \"modo de leitura: degrau $alvo (era $agora)\"\n  meow_registrar \"leitura.sh tique alvo=$alvo antes=$agora\"\n  return \"$MEOW_DIVERGENTE\"\n}\n\ncmd_conferir() {\n  [ \"$LEITURA_AGENDA\" = \"sim\" ] || { meow_pula \"LEITURA_AGENDA vazio — nada a conferir\"; return \"$MEOW_OK\"; }\n  _leitura_dependencia || return $?\n  # ESTA CHECAGEM É MELHOR QUE A MÉDIA DA CASA: ela não lê o que NÓS gravamos,\n  # ela pergunta ao COMPOSITOR o que está na tela. É a diferença entre \"o arquivo\n  # está certo\" e \"a tela está certa\" — o buraco que o pinned_workspaces abriu.\n  local alvo agora; alvo=\"$(_leitura_alvo)\"; agora=\"$(_leitura_no_compositor)\"; agora=\"${agora:-0}\"\n  if [ -f \"$LEITURA_TREGUA\" ] && [ \"$alvo\" != \"0\" ]; then\n    meow_ok \"em trégua (você desligou na mão) — conforme por decisão sua\"\n    return \"$MEOW_OK\"\n  fi\n  if [ \"$agora\" = \"$alvo\" ]; then meow_ok \"modo de leitura conforme (degrau $alvo)\"; return \"$MEOW_OK\"; fi\n  meow_muda \"modo de leitura divergente: tela=$agora, o horário pede $alvo\"\n  # O pedágio, dito na cara — mesmo contrato do relogio.sh.\n  meow_aviso \"o filtro tem controle na GUI (Acessibilidade → Filtros de cor).\"\n  meow_aviso \"se foi VOCÊ quem mudou, o próximo tique abre trégua sozinho;\"\n  meow_aviso \"para desligar de vez, esvazie LEITURA_AGENDA no meow.conf.\"\n  return \"$MEOW_DIVERGENTE\"\n}\n\ncmd_estado() {\n  _leitura_dependencia || return $?\n  local ini fim\n  ini=\"$(_leitura_hora_da_chave \"${LEITURA_HORARIO_INICIO:-18:00}\" 18:00 LEITURA_HORARIO_INICIO)\"\n  fim=\"$(_leitura_hora_da_chave \"${LEITURA_HORARIO_FIM:-07:00}\" 07:00 LEITURA_HORARIO_FIM)\"\n  meow_info \"janela      $(printf '%02d:%02d' $((ini/60)) $((ini%60))) → $(printf '%02d:%02d' $((fim/60)) $((fim%60)))  (rampa ${LEITURA_RAMPA:-0} min, ${LEITURA_DEGRAUS:-1} degrau(s))\"\n  meow_info \"agora       degrau que o horário pede: $(_leitura_alvo)\"\n  meow_info \"compositor  $(\"$LEITURA_CLIENTE\" ler 2>&1)\"\n  [ -f \"$LEITURA_TREGUA\" ] && meow_aviso \"há uma TRÉGUA aberta desde $(date -d \"@$(cat \"$LEITURA_TREGUA\")\" '+%H:%M')\"\n  systemctl --user is-active --quiet \"$LEITURA_UNIDADE\" \\\n    && meow_ok \"$LEITURA_UNIDADE ativo — próximo tique $(systemctl --user show -p NextElapseUSecRealtime --value \"$LEITURA_UNIDADE\")\" \\\n    || meow_aviso \"$LEITURA_UNIDADE NÃO está ativo — nada vai acontecer sozinho\"\n  return \"$MEOW_OK\"\n}\n\ncmd_remover() {\n  meow_seco || { systemctl --user disable --now \"$LEITURA_UNIDADE\" >/dev/null 2>&1 || true; }\n  _leitura_dependencia || return \"$MEOW_OK\"   # sem sessão não há filtro aceso\n  local agora; agora=\"$(_leitura_no_compositor)\"; agora=\"${agora:-0}\"\n  if [ \"$agora\" != \"0\" ]; then\n    meow_seco && meow_muda \"desligaria o filtro de tela\" \\\n              || { \"$LEITURA_CLIENTE\" degrau 0 >/dev/null; meow_muda \"filtro de tela desligado\"; }\n  else\n    meow_ok \"filtro de tela já estava desligado\"\n  fi\n  meow_seco || rm -f \"$LEITURA_TREGUA\" \"$LEITURA_NOSSO\" 2>/dev/null\n  meow_registrar \"leitura.sh remover\"\n  return \"$MEOW_OK\"\n}\n\ncase \"${1:-tique}\" in\n  tique)    cmd_tique ;;\n  aplicar)  meow_seco || systemctl --user enable --now \"$LEITURA_UNIDADE\" >/dev/null 2>&1; cmd_tique ;;\n  conferir) cmd_conferir ;;\n  estado)   cmd_estado ;;\n  remover)  cmd_remover ;;\n  *) meow_erro \"uso: leitura.sh {tique|aplicar|conferir|estado|remover}\"; exit \"$MEOW_ERRO\" ;;\nesac\n"
     },
     {
      "para_que": "A unit de SERVIÇO. É de USUÁRIO e não de sistema, e o motivo não é gosto: o alvo do agendador é o socket Wayland em /run/user/1000, que pertence à sessão dela. Uma unit de sistema não tem WAYLAND_DISPLAY, não tem o socket, e não teria como falar com o compositor.",
      "linguagem": "systemd",
      "fonte": "autoral, modelado em systemd/meow-doctor.service e systemd/meow-painel.service do MeowSystem",
      "trecho": "# meow-leitura.service — põe o modo de leitura no degrau que o relógio pede.\n#\n# POR QUE É UNIT DE USUÁRIO\n#   O alvo não é um arquivo: é o socket Wayland `/run/user/1000/wayland-1`, que é\n#   da sessão dela. Uma unit de sistema não tem `WAYLAND_DISPLAY` e não tem como\n#   falar com o `cosmic-comp`. Medido: `systemctl --user show-environment` traz\n#   `WAYLAND_DISPLAY=wayland-1`, `XDG_RUNTIME_DIR=/run/user/1000` e\n#   `XDG_CURRENT_DESKTOP=COSMIC` — o gerenciador de usuário já sabe o que precisa,\n#   então não há `Environment=` cravado aqui (cravar `wayland-1` quebraria no dia\n#   em que a sessão nascesse como `wayland-0`).\n#\n# POR QUE `PartOf=cosmic-session.target`\n#   Fora da sessão gráfica não há com quem falar. Sem isto a unidade falharia a\n#   cada tique enquanto a máquina estivesse no greeter — e o `StartLimit` do\n#   systemd desarmaria o timer justamente para o resto do dia. Com o `PartOf`, o\n#   timer para junto com a sessão e volta junto com ela.\n#\n# POR QUE `LogLevelMax=notice`\n#   Medido em 29/08/2026: o `xstick-vigia.timer` (1 min) escreveu 2884 linhas de\n#   journal em 24 h — 2 por invocação, o \"Starting…\"/\"Finished…\" que o próprio\n#   gerenciador emite. A 5 minutos isto seriam 576 linhas/dia por uma feature que\n#   muda de estado 8 vezes. O `man systemd.exec` é explícito: o LogLevelMax \"é\n#   aplicado a qualquer mensagem escrita por qualquer processo desta unidade,\n#   ASSIM COMO a qualquer mensagem escrita pelo processo gerenciador em referência\n#   a esta unidade\". `notice` corta o Starting/Finished (que é `info`) e mantém\n#   avisos e erros — inclusive o `meow_aviso` da trégua.\n#\n# NÃO HÁ `Persistent=` NEM `OnBootSec=` AQUI\n#   Um tique perdido não é dívida: o seguinte, em ≤5 minutos, recalcula tudo do\n#   relógio e chega no mesmo lugar. É a mesma escolha do `meow-wallpaper.timer`.\n\n[Unit]\nDescription=MeowSystem — modo de leitura conforme o horário\nDocumentation=file:///usr/share/doc/meowsystem/leitura.md\nPartOf=cosmic-session.target\nAfter=cosmic-session.target\n\n[Service]\nType=oneshot\nExecStart=%h/.local/bin/meow leitura tique\n# Sem sessão o script devolve 3 (MEOW_SEM_DEPENDENCIA), que NÃO é falha: é\n# ausência de assunto. Sem esta linha, cada tique no greeter marcaria a unidade\n# como `failed` e o journal viraria um muro vermelho.\nSuccessExitStatus=0 1 3\nLogLevelMax=notice\nSyslogIdentifier=meow-leitura\n# O tique é de milissegundos (medido: 8,6 ms de trabalho, 20-30 ms com a\n# invocação do systemd). 30 s é folga para um compositor ocupado, não orçamento.\nTimeoutStartSec=30s\n"
     },
     {
      "para_que": "A unit de TIMER. Cinco minutos, e não um: é a granularidade que casa com os quatro degraus disponíveis (4 degraus × 5 min = rampa de 20 min) e custa 1/5 do ruído de journal de um minutely. As três expressões OnCalendar foram testadas com systemd-analyze antes de escrever.",
      "linguagem": "systemd",
      "fonte": "autoral, modelado em systemd/meow-wallpaper.timer e systemd/meow-doctor.timer do MeowSystem",
      "trecho": "# meow-leitura.timer — o relógio do modo de leitura.\n#\n# POR QUE 5 MINUTOS, E NÃO 1\n#   Porque 5 minutos é o que a rampa precisa, e 1 minuto é cinco vezes mais caro\n#   pela mesma tela. Com os quatro degraus que o shader consegue hoje, uma rampa\n#   de 20 minutos tem exatamente quatro momentos de mudança — 18:00, 18:05,\n#   18:10, 18:15. Um tique de 1 minuto acordaria 16 vezes para não fazer nada.\n#   Medido: 288 tiques/dia × 20-30 ms = ~7 s de relógio por dia, e ~576 linhas de\n#   journal (contra 2884/dia do `xstick-vigia`, que roda de minuto em minuto).\n#   O `LogLevelMax=notice` do `.service` derruba essas 576 quase a zero.\n#\n# POR QUE SEM ESTADO, E NÃO UM DAEMON DORMINDO\n#   Um processo que dorme 5 minutos e avança um degrau erra em suspensão: acorda\n#   contando de onde parou e deixa a tela na cor de duas horas atrás. Aqui cada\n#   tique recalcula do relógio — suspender, hibernar, matar o processo ou perder\n#   um tique dá tudo no mesmo lugar no tique seguinte. É o desenho do redshift\n#   (reavaliar a elevação do sol) e não o do contador de passos.\n#\n# AS EXPRESSÕES QUE EU TESTEI ANTES DE ESCOLHER (systemd-analyze calendar)\n#   `*-*-* *:00/5`          -> normaliza para `*-*-* *:00/5:00`; 18:00, 18:05,\n#                              18:10, 18:15…  É esta.\n#   `*-*-* 18:00..19:00`    -> PARSEIA: hora 18, minuto 00..19. Dispara 18:00,\n#                              18:01, 18:02… Seria uma rampa minuto a minuto\n#                              restrita à janela — mas exigiria REGERAR a unidade\n#                              toda vez que ela mudasse o horário no meow.conf, e\n#                              chave de conf que mexe em unit é o tipo de\n#                              acoplamento que a memória \"chave nova ≠ ponte\n#                              fechada\" manda evitar. Fica documentado como o\n#                              caminho se um dia a rampa precisar ser contínua.\n#   `*-*-* 18:00..18:19:00` -> NÃO parseia (\"Argumento inválido\"): o `..` vale\n#                              dentro de UM campo, não atravessando dois pontos.\n#\n# `AccuracySec=10s` porque o padrão do systemd é 1 MINUTO de folga — num tique de\n# 5 minutos isso é 20% de imprecisão, e ela veria a tela virar \"quando desse\".\n# `RandomizedDelaySec` NÃO entra aqui, pelo mesmo motivo: no doctor a dispersão é\n# virtude (evita encontro marcado com o Ritual da Aurora), aqui seria a tela\n# mudando em horário sorteado.\n\n[Unit]\nDescription=MeowSystem — relógio do modo de leitura (a cada 5 min)\nDocumentation=man:systemd.timer(5)\nPartOf=cosmic-session.target\n\n[Timer]\nOnCalendar=*-*-* *:00/5\nAccuracySec=10s\nPersistent=false\nUnit=meow-leitura.service\n\n[Install]\nWantedBy=cosmic-session.target\n"
     },
     {
      "para_que": "O pôr do sol sem geoclue e sem rede: mesma estratégia que o cosmic-settings-daemon já usa nesta máquina (coordenada derivada do fuso). Devolve minutos desde a meia-noite, que é o que o leitura.sh consome. Os números de São Paulo foram conferidos: 29/08/2026 nascer 06:20 / pôr 17:55.",
      "linguagem": "python",
      "fonte": "autoral; algoritmo NOAA/Almanac padrão, mesma família da crate `sunrise` que o cosmic-settings-daemon vendoriza",
      "trecho": "#!/usr/bin/env python3\n# scripts/leitura_sol.py — a que horas o sol nasce e se põe, sem rede e sem geoclue.\n#\n# POR QUE NÃO GEOCLUE\n#   Medido em 29/08/2026: o `geoclue-2.0` está instalado e a frente demo está\n#   vivo, mas o Manager responde `AvailableAccuracyLevel: uint32 0` — nenhuma\n#   fonte disponível. A fonte WiFi do `/etc/geoclue/geoclue.conf` aponta, por\n#   padrão, para a Mozilla Location Service, que foi desligada; não há\n#   `/etc/geolocation`, nem modem, nem GPS. Um agendador que dependesse disso\n#   nasceria quebrado.\n#\n# POR QUE O FUSO BASTA — E ISSO NÃO É GAMBIARRA, É O QUE O COSMIC FAZ\n#   O `cosmic-settings-daemon`, que já calcula nascer/pôr do sol para o\n#   `auto_switch` do modo claro/escuro, não usa geoclue: ele faz\n#   `include_bytes!(\"../data/timezone-geodata.bitcode-v0-6\")` e olha o fuso\n#   (src/location.rs). Aqui o fuso é `America/Sao_Paulo`, e a tabela geonames\n#   responde São Paulo. O erro que isso introduz é de minutos, não de horas.\n#\n# O QUE ESTÁ EM JOGO NO NÚMERO\n#   Em São Paulo o pôr do sol vai de 17:29 (solstício de junho) a 18:52\n#   (dezembro) — 1h23 de balanço. É por isso que \"por-do-sol\" vale a pena existir\n#   como valor da chave em vez de só HH:MM. Mas é também por isso que HH:MM\n#   continua sendo uma resposta honesta: se ela lê à noite sempre no mesmo\n#   horário, o sol é irrelevante.\n#\n# Uso: leitura_sol.py {nascer|por} [\"lat,lon\"]   -> minutos desde a meia-noite\nimport math, sys, time, datetime\n\n# A tabela é curta de propósito: só o que esta máquina precisa. Um fuso ausente\n# cai no `return None` e o leitura.sh avisa e usa o HH:MM padrão — falhar\n# avisando, nunca inventar uma cidade.\nFUSOS = {\n    \"America/Sao_Paulo\": (-23.5505, -46.6333),\n    \"America/Fortaleza\": (-3.7319, -38.5267),\n    \"America/Manaus\":    (-3.1190, -60.0217),\n    \"America/Recife\":    (-8.0476, -34.8770),\n    \"America/Bahia\":     (-12.9777, -38.5016),\n}\n\n\ndef coordenada(arg):\n    if arg:\n        try:\n            lat, lon = (float(x) for x in arg.split(\",\"))\n            if -90 <= lat <= 90 and -180 <= lon <= 180:\n                return lat, lon\n        except ValueError:\n            pass\n    try:\n        with open(\"/etc/timezone\") as f:\n            return FUSOS.get(f.read().strip())\n    except OSError:\n        return None\n\n\ndef sol(lat, lon, dia, nascendo, zenite=90.833):\n    N = dia.timetuple().tm_yday\n    lngHour = lon / 15.0\n    t = N + ((6 if nascendo else 18) - lngHour) / 24.0\n    M = 0.9856 * t - 3.289\n    L = (M + 1.916 * math.sin(math.radians(M))\n         + 0.020 * math.sin(math.radians(2 * M)) + 282.634) % 360\n    RA = math.degrees(math.atan(0.91764 * math.tan(math.radians(L)))) % 360\n    RA = (RA + (math.floor(L / 90) * 90 - math.floor(RA / 90) * 90)) / 15\n    sinDec = 0.39782 * math.sin(math.radians(L))\n    cosDec = math.cos(math.asin(sinDec))\n    cosH = ((math.cos(math.radians(zenite)) - sinDec * math.sin(math.radians(lat)))\n            / (cosDec * math.cos(math.radians(lat))))\n    if abs(cosH) > 1:\n        return None            # sol de meia-noite / noite polar: não existe hora\n    H = ((360 - math.degrees(math.acos(cosH))) if nascendo\n         else math.degrees(math.acos(cosH))) / 15\n    return (H + RA - 0.06571 * t - 6.622 - lngHour) % 24    # em UTC\n\n\nif __name__ == \"__main__\":\n    qual = sys.argv[1] if len(sys.argv) > 1 else \"por\"\n    lugar = coordenada(sys.argv[2] if len(sys.argv) > 2 else \"\")\n    if not lugar:\n        raise SystemExit(3)\n    agora = datetime.datetime.now()\n    utc = sol(lugar[0], lugar[1], agora.date(), qual.startswith(\"nasc\"))\n    if utc is None:\n        raise SystemExit(3)\n    # O deslocamento vem do sistema, não de uma constante: o Brasil não tem\n    # horário de verão desde 2019, mas cravar -3 aqui seria plantar um bug para\n    # o dia em que voltar, ou para uma viagem.\n    off = -(time.altzone if time.localtime().tm_isdst else time.timezone) / 3600.0\n    print(int(round(((utc + off) % 24) * 60)))\n"
     },
     {
      "para_que": "O bloco do meow.conf.exemplo, no estilo conversado do arquivo (explica o porquê, dá os números medidos e avisa das armadilhas). Segue a convenção dos vizinhos: VAZIO = NÃO TOCA.",
      "linguagem": "bash",
      "fonte": "autoral, modelado no bloco WALLPAPER_NOITE de meow.conf.exemplo:387-425",
      "trecho": "# --- MODO DE LEITURA: A TELA ESQUENTA SOZINHA NO HORÁRIO ---------------------\n# Em 29/08/2026 ela mandou o print do \"Modo de leitura > Clássico\" do HyperOS e\n# disse: \"eu conseguir setar uma mudança automática a partir de horário x?\".\n# Estas chaves são essa parte. Os dois SLIDERES (temperatura e textura) são de\n# outros arquivos; aqui é só o relógio.\n#\n# VAZIO = NÃO TOCA, e aqui isso é mais literal que de costume: com\n# `LEITURA_AGENDA` vazio o timer não muda o filtro de tela nunca, e o botão de\n# Acessibilidade → Filtros de cor volta a ser só seu.\nLEITURA_AGENDA=\"\"                    # sim | nao | vazio (= não toca)\n\n# O HORÁRIO. Aceita HH:MM ou as palavras `por-do-sol` / `nascer-do-sol`.\n#\n# A JANELA PODE ATRAVESSAR A MEIA-NOITE, e esse é o caso normal: 18:00→07:00\n# quer dizer \"depois das 18h OU antes das 7h\". `início == fim` vale o DIA\n# INTEIRO, não janela vazia — é a mesma regra do WALLPAPER_NOITE_INICIO logo\n# acima, herdada de propósito para a máquina não ter duas definições de \"noite\".\n#\n# SOBRE `por-do-sol`, E POR QUE ELE FUNCIONA SEM INTERNET\n#   Não há geolocalização nesta máquina: medido em 29/08/2026, o geoclue está\n#   instalado mas responde `AvailableAccuracyLevel: 0` (a fonte WiFi dele aponta\n#   para a Mozilla Location Service, que foi desligada). O jeito que funciona é o\n#   que o PRÓPRIO COSMIC usa: a coordenada sai do FUSO HORÁRIO. O\n#   `cosmic-settings-daemon` faz exatamente isso para o modo claro/escuro\n#   automático, com uma tabela geonames embutida no binário.\n#\n#   Aqui o fuso é America/Sao_Paulo, então a conta é feita para São Paulo:\n#   em 29/08/2026 dá nascer 06:20 e pôr 17:55. O balanço no ano é de 1h23\n#   (pôr às 17:29 em junho, às 18:52 em dezembro) — se você lê sempre no mesmo\n#   horário, HH:MM é a resposta mais honesta e mais previsível.\nLEITURA_HORARIO_INICIO=\"18:00\"       # HH:MM | por-do-sol\nLEITURA_HORARIO_FIM=\"07:00\"          # HH:MM | nascer-do-sol\n\n# A RAMPA: em quantos minutos a tela chega na temperatura cheia.\n#\n# 0 = SECO, e hoje 0 é o padrão honesto. O GNOME faz a rampa em 1 hora, o KDE em\n# degraus de 50K, o wlsunset em degraus de 10K — todos eles têm um número\n# CONTÍNUO para mexer. O COSMIC não tem: a temperatura está cravada como texto\n# dentro do binário do compositor, e o que dá para ligar a quente é um seletor de\n# poucos valores. Com um degrau só (o de hoje), rampa não existe — a tela vira de\n# uma vez, e pôr 20 aqui não muda nada além de atrasar.\n#\n# COM QUANTOS DEGRAUS? Ver LEITURA_DEGRAUS logo abaixo.\nLEITURA_RAMPA=\"0\"                    # minutos; 0 = a tela vira de uma vez\n\n# QUANTOS DEGRAUS DE TEMPERATURA EXISTEM. 1 é o que a máquina tem hoje.\n#\n# O shader do compositor tem QUATRO caixas de filtro (`color_mode` 1 a 4): uma é\n# a escala de cinza — que o `aurora-night-light.py` já sequestrou para virar a\n# luz quente de 3500K — e as outras três são os filtros de daltonismo\n# (protanopia, deuteranopia, tritanopia). Medido: são ~1500 bytes de GLSL que\n# ninguém nesta máquina usa, e cabem três temperaturas a mais, preservando o\n# tamanho do binário byte a byte (que é o que aquele script já sabe fazer).\n#\n# COM `LEITURA_DEGRAUS=\"4\"` a rampa passa a existir de verdade: 4 saltos de\n# temperatura, comutáveis a quente. O PREÇO, dito antes de você pagar: os três\n# filtros de daltonismo deixam de funcionar enquanto o patch estiver aplicado, e\n# os nomes no menu de Acessibilidade passam a mentir (\"Protanopia\" vira o segundo\n# degrau de calor). Se algum dia alguém precisar deles nesta máquina, é\n# `LEITURA_DEGRAUS=\"1\"` e o patch sai.\nLEITURA_DEGRAUS=\"1\"                  # 1 = só a escala de cinza · 4 = com o patch\n\n# A COORDENADA, se o `por-do-sol` errar demais para você.\n# Vazio = derivada do fuso (São Paulo, aqui). Formato \"lat,lon\" em graus\n# decimais, negativo para sul e oeste. Ex.: Recife seria \"-8.0476,-34.8770\", o\n# que adianta o pôr do sol em cerca de meia hora em relação a São Paulo.\nLEITURA_LOCAL=\"\"                     # vazio = usa o fuso · ou \"lat,lon\"\n"
     },
     {
      "para_que": "O esqueleto do patch dos quatro degraus no shader — o que substitui o bloco de daltonismo para a rampa gradual existir sem recompilar o compositor. O bloco original tem ~1500 bytes; este tem bem menos, e o resto vira padding de espaços (a técnica que o aurora-night-light.py já usa para preservar o tamanho do binário).",
      "linguagem": "glsl",
      "fonte": "autoral, sobre o texto extraído de /usr/bin/cosmic-comp (src/backend/render/shaders/offscreen.frag) em 29/08/2026",
      "trecho": "// ANTES (no binário vivo, já com o patch da Aurora no color_mode 1):\n//     if (color_mode == 1.0) {        // NIGHT LIGHT (Aurora)\n//         color.rgb *= vec3(1.00000, 0.77987, 0.54468);\n//                                                   } else if (color_mode >= 2.0) {\n//         float L = (17.8824 * color.r) + ... ~1500 bytes de daltonismo ...\n//     }\n//\n// DEPOIS (quatro degraus; os whitepoints são os do redshift/colorramp.c, que é\n// de onde o aurora-night-light.py já tira a tabela):\n//\n//   degrau 1 = 4500K  (só um véu; é onde a rampa começa)\n//   degrau 2 = 4000K\n//   degrau 3 = 3500K  (a temperatura que ela já tem hoje)\n//   degrau 4 = 3000K  (a mais quente, para a madrugada)\n//\n// O texto abaixo tem de ser preenchido com espaços até bater EXATAMENTE o\n// tamanho do bloco original, ou os offsets do ELF andam.\n\n    if (color_mode == 1.0) {        // LEITURA degrau 1 (4500K)\n        color.rgb *= vec3(1.00000, 0.87325, 0.74112);\n    } else if (color_mode == 2.0) { // LEITURA degrau 2 (4000K)\n        color.rgb *= vec3(1.00000, 0.82854, 0.64606);\n    } else if (color_mode == 3.0) { // LEITURA degrau 3 (3500K)\n        color.rgb *= vec3(1.00000, 0.77987, 0.54468);\n    } else if (color_mode >= 4.0) { // LEITURA degrau 4 (3000K)\n        color.rgb *= vec3(1.00000, 0.71760, 0.42107);\n    }\n"
     },
     {
      "para_que": "O que o timer chama, no bin/meow — o despacho e o chk_/fix_ do doctor, seguindo o padrão já usado por relogio/janelas. Sem isto a chave nasce inerte: a memória \"chave nova ≠ ponte fechada\" avisa que o meow.conf.exemplo mente se install.sh e bin/meow não passarem a chave adiante.",
      "linguagem": "bash",
      "fonte": "autoral, no padrão de bin/meow:848 (chk_relogio) e :960 (fix_relogio)",
      "trecho": "# --- em bin/meow, junto dos vizinhos ------------------------------------------\n# ATENÇÃO: as SEIS chaves têm de aparecer em TODOS os pontos abaixo, senão a\n# chave é obedecida pela CLI (que sourceia o conf) e ignorada pelo timer — o\n# defeito silencioso que o cabeçalho do wallpaper.sh gasta 20 linhas descrevendo.\n\n_leitura_env() {\n  printf '%s' \"LEITURA_AGENDA=${LEITURA_AGENDA:-} LEITURA_HORARIO_INICIO=${LEITURA_HORARIO_INICIO:-} \"\n  printf '%s' \"LEITURA_HORARIO_FIM=${LEITURA_HORARIO_FIM:-} LEITURA_RAMPA=${LEITURA_RAMPA:-} \"\n  printf '%s' \"LEITURA_DEGRAUS=${LEITURA_DEGRAUS:-} LEITURA_LOCAL=${LEITURA_LOCAL:-}\"\n}\n\nchk_leitura() {\n  LEITURA_AGENDA=\"${LEITURA_AGENDA:-}\" LEITURA_HORARIO_INICIO=\"${LEITURA_HORARIO_INICIO:-}\" \\\n  LEITURA_HORARIO_FIM=\"${LEITURA_HORARIO_FIM:-}\" LEITURA_RAMPA=\"${LEITURA_RAMPA:-}\" \\\n  LEITURA_DEGRAUS=\"${LEITURA_DEGRAUS:-}\" LEITURA_LOCAL=\"${LEITURA_LOCAL:-}\" \\\n    \"$RAIZ/scripts/leitura.sh\" conferir 2>&1\n}\n\nfix_leitura() {\n  LEITURA_AGENDA=\"${LEITURA_AGENDA:-}\" LEITURA_HORARIO_INICIO=\"${LEITURA_HORARIO_INICIO:-}\" \\\n  LEITURA_HORARIO_FIM=\"${LEITURA_HORARIO_FIM:-}\" LEITURA_RAMPA=\"${LEITURA_RAMPA:-}\" \\\n  LEITURA_DEGRAUS=\"${LEITURA_DEGRAUS:-}\" LEITURA_LOCAL=\"${LEITURA_LOCAL:-}\" \\\n    \"$RAIZ/scripts/leitura.sh\" aplicar\n}\n\n# E no despacho de subcomandos:\n#   leitura) shift; env $(_leitura_env) \"$RAIZ/scripts/leitura.sh\" \"${1:-estado}\" ;;\n#\n# CUIDADO COM `env` SEM `-i` — e este é um cuidado com dono conhecido nesta casa:\n# a memória \"reiniciar o painel esgota o supervisor\" registra que um `env -i`\n# perdido matou a bandeja e o botão de desligar. Aqui é `env VAR=... cmd`, que\n# ACRESCENTA ao ambiente em vez de limpá-lo — e o ambiente é justamente o que\n# carrega o WAYLAND_DISPLAY sem o qual o cliente não conecta.\n"
     }
    ],
    "solucoes": [
     {
      "nome": "O agendador: timer de usuário de 5 minutos, sem estado, falando o protocolo de acessibilidade",
      "alvo": "agendamento",
      "como": "1) `scripts/leitura_filtro.py` — cliente Wayland cru (socket + struct, zero dependência) que binda `cosmic_a11y_manager_v1`, lê o estado que o compositor manda no bind e envia `set_screen_filter`. Ramifica por versão: v2 → opcode 1 `(inverted, filter)`; v3+ → opcode 2 `(inverted, filter, filter_state)`. 2) `scripts/leitura.sh` — calcula, SÓ do relógio, qual degrau a hora atual pede (`_leitura_degrau`, conferida em 24 horários incluindo a virada da meia-noite) e chama o cliente se e só se o compositor estiver noutro degrau. 3) `systemd/meow-leitura.{service,timer}` de USUÁRIO, `OnCalendar=*-*-* *:00/5`, `PartOf=cosmic-session.target`, `LogLevelMax=notice`, `SuccessExitStatus=0 1 3`. 4) as seis chaves no `meow.conf.exemplo` e a fiação em `install.sh` + `bin/meow` (chk_leitura/fix_leitura). Cada tique recalcula tudo: suspender, hibernar, perder um tique ou matar o processo dá no mesmo lugar 5 minutos depois — é o desenho do redshift (reavaliar) e não o do contador de passos.",
      "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh, scripts/leitura_filtro.py, scripts/leitura_sol.py, systemd/meow-leitura.service, systemd/meow-leitura.timer, meow.conf.exemplo, bin/meow, install.sh",
      "a_quente": true,
      "custo": "Medido: 8,6 ms de relógio e 5,4 ms de CPU por tique; 20-30 ms com a invocação do systemd; 288 tiques/dia = ~7 s de relógio/dia. Journal: ~576 linhas/dia sem o LogLevelMax, quase zero com ele (comparação medida: o xstick-vigia, de 1 minuto, escreve 2884 linhas/dia). 3,8 MB de RSS por tique, e o processo morre em seguida.",
      "durabilidade": "Sobrevive a `apt upgrade` (não toca em binário nenhum), a reboot e a troca de tema. Sobrevive à subida do protocolo para v3 porque o cliente ramifica por versão. NÃO sobrevive a uma remoção do global `cosmic_a11y_manager_v1` do compositor — nesse caso o cliente sai com 3 e o `_leitura_dependencia` transforma isso em `meow_pula`, não em erro diário."
     },
     {
      "nome": "A rampa gradual de hoje: quatro degraus escondidos nos filtros de daltonismo",
      "alvo": "temperatura",
      "como": "O bloco `else if (color_mode >= 2.0)` do `offscreen.frag`, embutido no binário do cosmic-comp, tem ~1500 bytes de GLSL de daltonismo que ninguém nesta máquina usa (conferido: o estado do filtro é `disabled`, e o `a11y_screen_filter.ron` não tem `color_filter`). Substituir por uma escada de quatro multiplicações de temperatura (4500K / 4000K / 3500K / 3000K, whitepoints do redshift, que o `aurora-night-light.py` já tabela), preenchendo com espaços até bater o tamanho original byte a byte — que é exatamente a técnica que aquele script já implementa. Com isso `LEITURA_DEGRAUS=\"4\"` e `LEITURA_RAMPA=\"20\"` passam a fazer uma rampa de verdade: 18:00 → 4500K, 18:05 → 4000K, 18:10 → 3500K, 18:15 → 3000K, e a descida simétrica de manhã. A comutação entre degraus é instantânea (o protocolo só troca o uniform), então nenhum passo custa logout.",
      "onde": "~/.config/zsh/scripts/aurora-night-light.py (é do Ritual da Aurora, não do MeowSystem — docs/FRONTEIRA.md manda propor lá, não duplicar aqui); binário /usr/bin/cosmic-comp; backup em /var/lib/aurora/",
      "a_quente": false,
      "custo": "O patch em si exige reiniciar o cosmic-comp = LOGOUT, uma vez. Depois disso a comutação entre os quatro degraus é a quente. O preço permanente: os três filtros de daltonismo deixam de existir enquanto o patch estiver aplicado, e os rótulos do menu de Acessibilidade passam a mentir (\"Protanopia\" vira o segundo degrau de calor). É uma feature de acessibilidade sendo gasta — decisão dela, não técnica.",
      "durabilidade": "Cai a cada `apt upgrade` do cosmic-comp (o binário é reescrito). O hook de apt do Ritual da Aurora já reaplica o patch do greyscale; estender esse mesmo hook para os quatro degraus é uma linha. Convive com o patch do `aurora-cosmic-comp-ws.sh --build` (ensure_last_empty) porque atua sobre o TEXTO do shader, não sobre o código Rust — mas a ordem importa: o patch do shader tem de rodar DEPOIS do build, sobre o binário recém-compilado."
     },
     {
      "nome": "Pôr do sol sem geoclue: coordenada derivada do fuso, como o próprio COSMIC faz",
      "alvo": "agendamento",
      "como": "`LEITURA_HORARIO_INICIO=\"por-do-sol\"` faz o `leitura.sh` chamar `scripts/leitura_sol.py`, que lê `/etc/timezone`, resolve `America/Sao_Paulo` numa tabela curta de coordenadas e devolve o pôr do sol em minutos desde a meia-noite. Sem rede, sem D-Bus, sem geoclue. É a mesma estratégia que o `cosmic-settings-daemon` já usa para o modo claro/escuro automático (`include_bytes!(\"../data/timezone-geodata.bitcode-v0-6\")` em src/location.rs). `LEITURA_LOCAL=\"lat,lon\"` sobrepõe, para o caso de ela não estar em São Paulo. Fuso desconhecido → sai 3, o script avisa e cai no HH:MM padrão: falha avisando, nunca inventa cidade.",
      "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura_sol.py, chaves LEITURA_HORARIO_INICIO / LEITURA_HORARIO_FIM / LEITURA_LOCAL no meow.conf",
      "a_quente": true,
      "custo": "Uma execução de Python por tique quando a chave é `por-do-sol` (trigonometria pura, milissegundos). Precisão: coordenada de São Paulo contra a posição real dela — o erro é de minutos. O balanço anual real, calculado: pôr às 17:29 (junho) e 18:52 (dezembro), 1h23 de amplitude.",
      "durabilidade": "Sobrevive a tudo: não depende de rede, de serviço, nem de binário do COSMIC. O único jeito de quebrar é o fuso mudar sem a tabela acompanhar — e aí ela cai no HH:MM padrão com aviso. O Brasil não tem horário de verão desde 2019, e mesmo se voltar o script lê o deslocamento do sistema (`time.timezone`/`time.altzone`) em vez de cravar -3."
     },
     {
      "nome": "A trégua: se ela desligar na mão, o agendador cala a boca até o fim da janela",
      "alvo": "ui",
      "como": "O filtro tem controle na GUI. Sem tratamento, ela desligaria às 20h e o timer religaria 5 minutos depois — 288 vezes por dia, a versão industrial do erro que custou a opacidade do painel. Desenho: o `tique` guarda em `$MEOW_ESTADO/leitura/ultimo-degrau` o degrau que NÓS pusemos. Se num tique o compositor estiver em 0 e o registro disser que fomos nós que ligamos, isso é ela dizendo não: grava `$MEOW_ESTADO/leitura/tregua` e não toca em mais nada até a janela acabar. A trégua morre sozinha no primeiro tique fora da janela — não há o que limpar, e amanhã à noite tudo volta. O mesmo registro impede o inverso: se ela LIGOU o filtro na mão fora da janela, o `tique` não desliga (só desliga o que ligou).",
      "onde": "cmd_tique em /mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh; estado em ~/.local/state/meowsystem/leitura/",
      "a_quente": true,
      "custo": "Dois arquivos de poucos bytes em $MEOW_ESTADO. Custo conceitual: o `conferir` passa a devolver MEOW_OK durante uma trégua, então o `meow doctor` não acusa divergência numa noite em que ela desligou — o que é o comportamento certo, mas precisa estar dito no `estado` (e está: ele imprime \"há uma TRÉGUA aberta desde HH:MM\").",
      "durabilidade": "Estado puro, sobrevive a reboot e a apt upgrade. Some com `meow leitura remover` ou apagando o diretório — apagar não perde nada além da trégua da noite corrente."
     },
     {
      "nome": "O `conferir` que pergunta à TELA, não ao disco",
      "alvo": "agendamento",
      "como": "Quase todo `chk_` deste projeto compara o que nós gravamos com o que queríamos gravar — e o cabeçalho do `janelas.sh` documenta o buraco disso: o `pinned_workspaces` fazia o doctor dizer \"conforme\" enquanto o lado a lado morria no reboot. Aqui o `conferir` não tem esse buraco de graça: o protocolo devolve o estado no bind, então `leitura.sh conferir` pergunta ao COMPOSITOR em que degrau a tela está e compara com o que o relógio pede. É a diferença entre \"o arquivo está certo\" e \"a tela está certa\", e neste caso ela sai de graça.",
      "onde": "cmd_conferir e _leitura_no_compositor em /mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh",
      "a_quente": true,
      "custo": "Um roundtrip Wayland (~ms) por execução do doctor. Nenhum estado gravado.",
      "durabilidade": "Depende de o global continuar sendo bindável por cliente comum (`client_not_sandboxed`). Se um upstream fechar isso, o `conferir` degrada para MEOW_SEM_DEPENDENCIA — avisa e não mente."
     },
     {
      "nome": "A rampa minuto a minuto, se um dia a temperatura virar contínua",
      "alvo": "agendamento",
      "como": "Guardado como caminho, não como recomendação: `OnCalendar=*-*-* 18:00..19:00` PARSEIA (testado) e dispara 18:00, 18:01, 18:02… — 20 tiques restritos à janela de subida, sem tocar o resto do dia. Duas unidades assim (subida e descida) dão uma rampa minuto a minuto por 40 invocações/dia, contra 1440 de um timer minutely. É o desenho a adotar SE a outra trilha conseguir recompilar o cosmic-comp com um uniform contínuo de temperatura, porque aí 5 minutos ficam grossos demais. Não é o desenho de hoje porque exige REGERAR a unidade toda vez que ela mudar o horário no meow.conf — acoplamento entre chave de conf e unit systemd, que é justamente o que a memória \"chave nova ≠ ponte fechada\" manda evitar.",
      "onde": "systemd/meow-leitura.timer (variante documentada no cabeçalho da unit)",
      "a_quente": true,
      "custo": "40 invocações/dia × 20-30 ms = ~1 s/dia. Mas soma o custo humano de manter uma unit gerada a partir do conf, com `daemon-reload` no caminho do `aplicar`.",
      "durabilidade": "A expressão OnCalendar é estável (systemd a normaliza sem reclamar). A fragilidade é a regeneração: uma unit gerada que ficar dessincronizada do conf é um agendador que dispara na hora errada e não diz por quê."
     }
    ],
    "derrubado": [
     "\"Só o clique em Acessibilidade → Filtros de cor liga a luz quente.\" FALSO, e era a premissa da trilha inteira. O global nasce com `client_not_sandboxed` (src/state.rs:762) — qualquer cliente não-flatpak binda. Bindei ao vivo com Python cru e li o estado: `version_anunciada=2`, `inverted=0 filter=0 (disabled)`.",
     "\"Basta escrever `~/.local/state/cosmic-comp/a11y_screen_filter.ron` e o compositor relê.\" FALSO. Ele é lido UMA vez, no `insert_idle` da subida da sessão (src/config/mod.rs:318-329), e só reescrito pelo `Drop` do `PersistenceGuard`. Sem watch, sem inotify. Escrever ali agenda para o próximo login — o oposto do pedido.",
     "\"O protocolo instalado é a v3, então uso `set_screen_filter2`.\" FALSO: o XML vendorizado diz version=3, mas o compositor faz `create_global(2, ...)` e o registry me anunciou 2. E o erro é caro nos dois sentidos: na v3, mandar o request v2 levanta o erro de protocolo `deprecated` e MATA a conexão do cliente.",
     "\"Geoclue resolve a localização nesta máquina.\" FALSO. Instalado, frente vivo, e mesmo assim `AvailableAccuracyLevel: uint32 0`. A fonte WiFi do conf aponta para a Mozilla Location Service (desligada), não há `/etc/geolocation`, não há modem nem GPS. E o whitelist de frentes do `/etc/geoclue/geoclue.conf` não tem nenhum componente COSMIC.",
     "\"O COSMIC usa geoclue para calcular nascer/pôr do sol.\" FALSO. O `cosmic-settings-daemon` decodifica uma tabela geonames embutida no binário (`include_bytes!(\"../data/timezone-geodata.bitcode-v0-6\")`, src/location.rs) e resolve pelo FUSO. As strings do binário confirmam: `failed to decode timezone geodata:`, `vendor/sunrise/src/solar_equation/mod.rs`.",
     "\"Um timer de 1 minuto é caro demais.\" MEIO FALSO, e o meio que importa não é o que eu esperava. CPU é irrelevante: 5,4 ms por tique, 20-30 ms com a invocação do systemd. O custo real é o journal — 2880 linhas/dia, medido no `xstick-vigia.timer` que já roda de minuto em minuto aqui. E isso tem cura documentada (`LogLevelMax=notice` filtra também o Starting/Finished que o gerenciador emite). Então o argumento contra o minutely é a rampa não precisar dele, não o custo.",
     "\"O `..` do OnCalendar não serve para faixa de minutos.\" FALSO: `*-*-* 18:00..19:00` parseia e dispara 18:00, 18:01, 18:02… O que NÃO parseia é `*-*-* 18:00..18:19:00` — o `..` vale dentro de um campo, não atravessando dois pontos. Foi o meu primeiro teste e ele falhou por sintaxe, não por falta de recurso.",
     "\"A rampa gradual só existe se recompilar o compositor.\" PARCIALMENTE FALSO. O slider CONTÍNUO exige, sim. Mas há quatro caixas de `color_mode` no shader e três delas são daltonismo — ~1500 bytes de GLSL que ninguém usa aqui, onde cabem três temperaturas a mais preservando o tamanho do binário. Isso dá uma rampa de quatro degraus comutáveis a quente, hoje, sem cargo.",
     "\"redshift/gammastep/wlsunset resolvem o agendamento.\" Já se sabia falso (o cosmic-comp não expõe `zwlr_gamma_control_manager_v1`), e agora está confirmado por outro lado também: nenhum dos três está sequer instalado (`which redshift gammastep wlsunset` não acha nada, e `dpkg -l` só devolve o geoclue).",
     "\"Uma unit de sistema serve.\" FALSO. O alvo é o socket `/run/user/1000/wayland-1`, da sessão dela. Tem de ser unit de USUÁRIO — e ela funciona porque `systemctl --user show-environment` já carrega `WAYLAND_DISPLAY=wayland-1` e `XDG_RUNTIME_DIR=/run/user/1000` (medido)."
    ]
   },
   "verificadas": [
    {
     "nome": "O agendador: timer de usuário de 5 minutos, sem estado, falando o protocolo de acessibilidade",
     "alvo": "agendamento",
     "como": "1) `scripts/leitura_filtro.py` — cliente Wayland cru (socket + struct, zero dependência) que binda `cosmic_a11y_manager_v1`, lê o estado que o compositor manda no bind e envia `set_screen_filter`. Ramifica por versão: v2 → opcode 1 `(inverted, filter)`; v3+ → opcode 2 `(inverted, filter, filter_state)`. 2) `scripts/leitura.sh` — calcula, SÓ do relógio, qual degrau a hora atual pede (`_leitura_degrau`, conferida em 24 horários incluindo a virada da meia-noite) e chama o cliente se e só se o compositor estiver noutro degrau. 3) `systemd/meow-leitura.{service,timer}` de USUÁRIO, `OnCalendar=*-*-* *:00/5`, `PartOf=cosmic-session.target`, `LogLevelMax=notice`, `SuccessExitStatus=0 1 3`. 4) as seis chaves no `meow.conf.exemplo` e a fiação em `install.sh` + `bin/meow` (chk_leitura/fix_leitura). Cada tique recalcula tudo: suspender, hibernar, perder um tique ou matar o processo dá no mesmo lugar 5 minutos depois — é o desenho do redshift (reavaliar) e não o do contador de passos.",
     "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh, scripts/leitura_filtro.py, scripts/leitura_sol.py, systemd/meow-leitura.service, systemd/meow-leitura.timer, meow.conf.exemplo, bin/meow, install.sh",
     "a_quente": true,
     "custo": "Medido: 8,6 ms de relógio e 5,4 ms de CPU por tique; 20-30 ms com a invocação do systemd; 288 tiques/dia = ~7 s de relógio/dia. Journal: ~576 linhas/dia sem o LogLevelMax, quase zero com ele (comparação medida: o xstick-vigia, de 1 minuto, escreve 2884 linhas/dia). 3,8 MB de RSS por tique, e o processo morre em seguida.",
     "durabilidade": "Sobrevive a `apt upgrade` (não toca em binário nenhum), a reboot e a troca de tema. Sobrevive à subida do protocolo para v3 porque o cliente ramifica por versão. NÃO sobrevive a uma remoção do global `cosmic_a11y_manager_v1` do compositor — nesse caso o cliente sai com 3 e o `_leitura_dependencia` transforma isso em `meow_pula`, não em erro diário.",
     "trilha": "agendamento",
     "veredito": {
      "solucao": "O agendador: timer de usuário de 5 minutos, sem estado, falando o protocolo de acessibilidade (trilha \"agendamento\")",
      "veredito": "CONFIRMADO",
      "a_quente_confirmado": true,
      "porque": "Tentei derrubar por seis lados e o mecanismo aguentou todos. O que MEDI nesta máquina:\n\n1) O global existe e é da versão 2, não 3. `wayland-info` na sessão viva: `interface: 'cosmic_a11y_manager_v1', version: 2`. A fonte confirma o porquê: `src/state.rs:762` chama `A11yState::new(dh, client_not_sandboxed)` e o `create_global` está com `2` cravado (src/wayland/protocols/a11y.rs). Logo o ramo v2/opcode 1 é o único que roda hoje; o ramo v3 é código morto (mas ver a correção 5, ele está errado).\n\n2) Não há trava de cliente privilegiado. O filtro do global é `client_not_sandboxed` (src/state.rs:647): qualquer cliente não-flatpak passa. Provei na prática — escrevi um cliente Wayland cru em memória (socket + struct, sem tocar em disco), conectei em /run/user/1000/wayland-1 como cliente comum, bindei o manager na versão 2 e o compositor me respondeu. Um serviço de usuário do systemd cai exatamente nessa categoria.\n\n3) O estado VEM no bind, como a proposta afirma. Os dois eventos chegaram sozinhos logo após o bind: opcode 0 `magnifier(0)` e opcode 1 `screen_filter(0, 0)` — inverted=disabled, filter=disabled. Não precisa de round-trip extra nem de ler arquivo.\n\n4) \"Vale a quente\" está PROVADO na fonte, não inferido. `color_mode` é um `Uniform` montado a cada desenho em `src/backend/render/mod.rs:1374` (declarado em :423 como `UniformType::_1f`), a partir de `screen_filter.filter.color_filter`. E `src/backend/kms/mod.rs:722` diz textualmente: \"The shader is already compiled at this point\". Não há recompilação de shader, não há releitura de config de boot, não há restart de nada. O caminho completo é set_screen_filter → `request_screen_filter` (handlers/a11y.rs:52) → `backend.update_screen_filter` → `surface.set_screen_filter` → uniform no próximo frame.\n\n5) Não pisa em fronteira. `docs/FRONTEIRA.md` dá o BINÁRIO do cosmic-comp à Aurora — e esta proposta não encosta no binário. O estado persistido é `~/.local/state/cosmic-comp/a11y_screen_filter.ron` (hoje `( inverted: false, )`), escrito pelo próprio compositor, não por nós. As unidades vão para `~/.config/systemd/user`, que o `install.sh` já administra (linhas 1090, 1389-1453, mesmo padrão do meow-doctor).\n\n6) O compositor não quebra nem com entrada ruim. `protocol_to_color_filter` devolve `Err(Unknown)` para valor desconhecido e o handler simplesmente IGNORA (`if let Ok(filter) = filter && filter != ...`). Sem panic, sem protocol error, sem matar o cliente. E requisição repetida com o mesmo valor é no-op pelo guarda `filter != state`, então nem o .ron é reescrito à toa.\n\nO custo declarado (ms por tique, ~7 s/dia) não dá para conferir porque os arquivos ainda não existem — mas a ordem de grandeza é compatível com um processo que abre um socket, lê 2 eventos e morre. Não é por aí que a proposta cai.\n\nO QUE ELA NÃO ENTREGA, e é bom estar dito: isto resolve só o alvo (C). Os alvos (A) slider de temperatura e (B) textura continuam de fora, e (A) continua impossível a quente pelo motivo já estabelecido — a temperatura é literal dentro do texto do shader.",
      "correcao": "Sete correções. A 1 e a 3 são obrigatórias; sem elas a coisa ou não funciona, ou funciona errado na cara dela.\n\n1) OS NÚMEROS DO ENUM ESTÃO NA ARMADILHA. Existem DOIS enums com o mesmo nome e valores diferentes:\n   - fio (XML, `cosmic_a11y_manager_v1.filter`): disabled=0, unknown=1, greyscale=2, protanopia=3, deuteranopia=4, tritanopia=5\n   - interno (`src/config/mod.rs:167`, o que vira `color_mode`): Greyscale=1, Protanopia=2, Deuteranopia=3, Tritanopia=4\n   Ou seja: para LIGAR a luz quente da Aurora (color_mode==1.0) o cliente tem de mandar filter=**2** no fio. Mandar 1 é `unknown`, e `unknown` cai no `Err(Unknown)` que o handler DESCARTA EM SILÊNCIO — sem erro, sem log, sem efeito. É o pior modo de falha possível: um agendador que parece rodar e não faz nada. Para DESLIGAR na v2, filter=0 (`disabled`) é válido.\n   O `leitura_filtro.py` precisa de uma constante nomeada e um teste que trave esses números.\n\n2) \"DEGRAU\" SÓ PODE TER DOIS VALORES. Conferi o binário vivo: `strings /usr/bin/cosmic-comp` mostra que só o bloco `color_mode == 1.0` foi reescrito para `// NIGHT LIGHT (Aurora)`; os blocos 2.0/3.0/4.0 continuam Protanopia/Deuteranopia/Tritanopia. Há UM slot quente, de 3500K, e mais nada. Se `_leitura_degrau` devolver escala (3500K às 22h, 4500K ao entardecer), essa parte está REFUTADA: temperatura diferente exige repatch do binário + reinício do cosmic-comp = logout. `_leitura_degrau` tem de devolver `{ligado, desligado}` e o nome \"degrau\" tem de parar de sugerir o contrário.\n\n3) FALTA A GUARDA DO PATCH — E É ELA QUE EVITA UMA TELA PRETO-E-BRANCO. O cabeçalho do `aurora-night-light.py` é explícito: quando o upstream muda o shader, o script NÃO patcha e sai com 3, e o fallback assumido é \"fica sem night light, o desktop sobe normal\". Esse fallback foi escrito num mundo em que ninguém ligava o greyscale sozinho. Com o agendador, o mesmo fallback vira: às 22h a tela dela fica LITERALMENTE em escala de cinza, todo dia, sem ninguém entender por quê. E a janela não é teórica — `/var/lib/aurora` tem quatro versões de cosmic-comp em poucas semanas e o binário vivo é de ontem (28/08 20:50).\n   Conserto medido, 2 ms e sem sudo: `LC_ALL=C grep -aq \"NIGHT LIGHT (Aurora)\" /usr/bin/cosmic-comp` antes de ligar. Marcador ausente → sair 3 (`meow_pula`), nunca ligar o filtro. (Atenção: `grep` sem `-a` devolve não-zero neste binário; testei os dois.)\n\n4) FALTA `ConditionEnvironment=WAYLAND_DISPLAY` NO .service. Medi `Linger=yes` para o usuário. Com `PartOf=` só no service, o TIMER continua disparando 288 vezes por dia com a máquina deslogada, cada uma saindo 3. O `meow-doctor.service` já usa exatamente essa condição, pelo mesmo motivo, e o comentário dele cita o Linger. Copie. (E `WAYLAND_DISPLAY=wayland-1` está de fato no `systemctl --user show-environment`, então o serviço herda o socket — mas o cliente ganha robustez se, na ausência da variável, varrer `$XDG_RUNTIME_DIR/wayland-*`.)\n\n5) O RAMO v3 ESTÁ ERRADO EM DOIS PONTOS (li o XML em vendor/cosmic-protocols/unstable/cosmic-a11y-unstable-v1.xml):\n   - `set_screen_filter2` levanta o erro de protocolo \"deprecated\" se `filter` for `disabled`. Logo o caminho de DESLIGAR na v3 não é filter=0; é filter=`unknown`(1) + filter_state=`disabled`(0). Traduzir \"desligado\" para 0 na v3 mata o cliente com protocol error.\n   - a v3 PARA de emitir o evento `screen_filter` (opcode 1). A proposta só fala em ramificar a REQUISIÇÃO; a leitura de estado também tem de ramificar, para o evento `screen_filter2` (opcode 2, três args).\n   Os opcodes citados estão certos: requests set_magnifier=0, set_screen_filter=1, set_screen_filter2=2.\n\n6) FALTA O \"HOJE NÃO\". Sem estado significa que ela não tem como recusar: desliga em Ajustes → Acessibilidade e o timer religa em até 5 minutos, sem explicação na tela. Isso é o mesmo defeito de dono que custou a opacidade do painel em 17/08 (`docs/FRONTEIRA.md`: \"onde a GUI do COSMIC tem um controle, o valor é dela\") — e aqui a GUI TEM o controle. Um sentinela de pausa (`~/.local/state/meowsystem/leitura-pausa`, apagado na próxima virada de degrau) resolve sem trazer estado de verdade de volta.\n\n7) `scripts/leitura_sol.py` aparece em \"onde\" e NÃO aparece em \"como\". Ou some da lista, ou ganha descrição — e se ele calcula nascer/pôr do sol, precisa de coordenada fixa no conf, jamais de rede num tique de 5 minutos.\n\nUma linha em `docs/FRONTEIRA.md` também: o binário continua da Aurora, mas quem passa a MANDAR no liga/desliga do filtro é o Meow. Está na exceção \"o Aurora pode ser corrigido pelo Meow\", mas precisa estar escrito, ou daqui a dois meses ninguém sabe quem religou a luz.",
      "risco_real": "Baixo, e com volta fácil — nenhum caminho aqui custa o desktop dela.\n\nO que a solução NÃO faz: não escreve no binário, não escreve em nada que o cosmic-comp leia no boot, não reinicia painel nem sessão, não usa sudo, não toca em `~/.config/zsh`, não toca nos gtk.css. Um erro nosso não pode impedir a sessão de subir.\n\nO pior caso realista é COR ERRADA NA TELA, não tela apagada:\n- com a correção 3 ausente e o patch da Aurora caído: tela em escala de cinza todas as noites;\n- com o enum errado (correção 1): nada acontece, para sempre, calado;\n- com um bug de horário: luz quente na hora errada.\n\nVolta, em ordem de custo:\n1. `systemctl --user disable --now meow-leitura.timer` — funciona no terminal dela, no meu, ou num TTY; não precisa de TTY.\n2. Um clique em Ajustes → Acessibilidade → Filtros de cor desfaz o estado atual na hora (é o mesmo uniform).\n3. `rm` das duas unidades em `~/.config/systemd/user` + `daemon-reload`.\nNenhum passo exige logout, reboot ou reinstalar nada.\n\nRisco para o compositor: nulo pelo que li. Valor de enum desconhecido cai em `Err(Unknown)` e é ignorado (sem panic, sem protocol error); requisição repetida é no-op pelo guarda `filter != state`, então nem o `a11y_screen_filter.ron` é reescrito à toa. Um cliente que morra no meio só fecha o socket.\n\nRiscos residuais que sobram mesmo com tudo corrigido, e que ela deve saber antes de aprovar:\n- O único rótulo visível do agendamento continua sendo \"Escala de cinza\" em Acessibilidade. Ligado às 22h, a GUI do COSMIC vai mostrar \"Escala de cinza\" marcado. É feio e é confuso, e não temos como renomear.\n- A granularidade é 5 minutos: a virada não é no segundo.\n- É liga/desliga de UM 3500K. Se ela esperava o slider da Xiaomi, isto não é isso — e é importante ela ouvir isso desta forma antes de a gente construir."
     }
    },
    {
     "nome": "A rampa gradual de hoje: quatro degraus escondidos nos filtros de daltonismo",
     "alvo": "temperatura",
     "como": "O bloco `else if (color_mode >= 2.0)` do `offscreen.frag`, embutido no binário do cosmic-comp, tem ~1500 bytes de GLSL de daltonismo que ninguém nesta máquina usa (conferido: o estado do filtro é `disabled`, e o `a11y_screen_filter.ron` não tem `color_filter`). Substituir por uma escada de quatro multiplicações de temperatura (4500K / 4000K / 3500K / 3000K, whitepoints do redshift, que o `aurora-night-light.py` já tabela), preenchendo com espaços até bater o tamanho original byte a byte — que é exatamente a técnica que aquele script já implementa. Com isso `LEITURA_DEGRAUS=\"4\"` e `LEITURA_RAMPA=\"20\"` passam a fazer uma rampa de verdade: 18:00 → 4500K, 18:05 → 4000K, 18:10 → 3500K, 18:15 → 3000K, e a descida simétrica de manhã. A comutação entre degraus é instantânea (o protocolo só troca o uniform), então nenhum passo custa logout.",
     "onde": "~/.config/zsh/scripts/aurora-night-light.py (é do Ritual da Aurora, não do MeowSystem — docs/FRONTEIRA.md manda propor lá, não duplicar aqui); binário /usr/bin/cosmic-comp; backup em /var/lib/aurora/",
     "a_quente": false,
     "custo": "O patch em si exige reiniciar o cosmic-comp = LOGOUT, uma vez. Depois disso a comutação entre os quatro degraus é a quente. O preço permanente: os três filtros de daltonismo deixam de existir enquanto o patch estiver aplicado, e os rótulos do menu de Acessibilidade passam a mentir (\"Protanopia\" vira o segundo degrau de calor). É uma feature de acessibilidade sendo gasta — decisão dela, não técnica.",
     "durabilidade": "Cai a cada `apt upgrade` do cosmic-comp (o binário é reescrito). O hook de apt do Ritual da Aurora já reaplica o patch do greyscale; estender esse mesmo hook para os quatro degraus é uma linha. Convive com o patch do `aurora-cosmic-comp-ws.sh --build` (ensure_last_empty) porque atua sobre o TEXTO do shader, não sobre o código Rust — mas a ordem importa: o patch do shader tem de rodar DEPOIS do build, sobre o binário recém-compilado.",
     "trilha": "agendamento",
     "veredito": {
      "solucao": "A rampa gradual de hoje: quatro degraus escondidos nos filtros de daltonismo (trilha \"agendamento\", alvo temperatura)",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "MEDIDO NESTA MÁQUINA (tudo leitura, nada foi escrito):\n\n1. ESPAÇO — CONFIRMADO, e é maior do que a proposta diz. No binário VIVO `/usr/bin/cosmic-comp` (md5 ef12fde…, 33.571.456 bytes): bloco `if (color_mode == 1.0)` em 0x2DC0EA, 195 bytes, JÁ com o texto `// NIGHT LIGHT (Aurora)` e `vec3(1.00000, 0.77987, 0.54468)`. O bloco de daltonismo vai de `    } else if (color_mode >= 2.0) {` até `    // re-multiply` = **1506 bytes** (a proposta chutou ~1500). Uma escada com 3 multiplicações + comentários ocupa 431 bytes: sobra 1075 de padding. Cabe folgado.\n\n2. FILTRO DESLIGADO — CONFIRMADO. `~/.local/state/cosmic-comp/a11y_screen_filter.ron` contém literalmente `(inverted: false,)` — sem `color_filter`. Ela não usa daltonismo hoje.\n\n3. \"A COMUTAÇÃO ENTRE DEGRAUS É A QUENTE\" — CONFIRMADO NA FONTE, não por fé. A árvore do build dela existe em `/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/`. Em `src/backend/render/mod.rs:1374` o uniform é remontado a CADA `render_output`:\n    Uniform::new(\"color_mode\", screen_filter.filter.color_filter.map(|val| val as u8 as f32).unwrap_or(0.))\nNenhuma recompilação de shader no caminho. E o global é acessível a cliente comum: `src/state.rs:762` cria o `A11yState::new::<State,_>(dh, client_not_sandboxed)` — `wayland-info` rodado agora na sessão dela lista `interface: 'cosmic_a11y_manager_v1', version: 2`. Ou seja, um script não-sandboxado pode trocar o degrau sem logout.\n\n4. GLSL — VÁLIDO em `#version 100`. A escada é só `color.rgb *= vec3(...)` dentro de `if/else if`, comparação de float igual à que o upstream já faz, e padding com espaços. Nada de texelFetch/inverse/loop variável.\n\n5. FRONTEIRA — CORRETA. `docs/FRONTEIRA.md` linha 58: \"binário `cosmic-comp` (patches de workspace e night light) | Aurora | **Aurora**\". A proposta manda propor no Ritual, e é onde tem de ser.\n\n6. DURABILIDADE — CONFIRMADA. `/usr/local/sbin/ritual-aurora-self-heal.sh` linha 2627 roda `aurora-cosmic-comp-ws.sh --ensure` e SÓ DEPOIS, linha 2639, roda `aurora-night-light.py` (rc=3 = shader mudou upstream → só avisa, não toca no binário). O hook `/etc/apt/apt.conf.d/99-ritual-aurora-self-heal` dispara isso a cada apt. A ordem que a proposta exige (shader DEPOIS do build) já é a ordem implementada.\n\n—— ONDE A PROPOSTA ESTÁ ERRADA ——\n\nA. ERRO DE CONTAGEM, e é aritmético, não de gosto. `src/config/mod.rs:167` fixa `enum ColorFilter { Greyscale = 1, Protanopia = 2, Deuteranopia = 3, Tritanopia = 4 }` com o comentário \"these values need to match with offscreen.frag\", e `protocol_to_color_filter` REJEITA qualquer outro valor (`WEnum::Unknown(_) => Err(Unknown)`). Logo existem no máximo QUATRO estados ligados + o desligado (0 = `is_noop`, que pula o passe inteiro). O bloco `>= 2.0` que a proposta quer reescrever só alcança 2.0, 3.0 e 4.0 — **TRÊS degraus, não quatro**. O ramo `else` (unknown) é inalcançável. Para ter quatro degraus é obrigatório usar também o `color_mode == 1.0`, que é exatamente o bloco que o `aurora-night-light.py` já possui e reescreve a cada apt: os dois patchers passariam a brigar pelo mesmo bloco, e o `--temp` dela deixaria de ser \"a temperatura\" para virar \"o primeiro degrau\" sem que o `--status` diga isso.\n\nB. FALTA A METADE QUE LIGA. `LEITURA_DEGRAUS` e `LEITURA_RAMPA` NÃO EXISTEM em lugar nenhum — grep em `~/.config/zsh/` e no MeowSystem inteiro: zero ocorrências. E não existe nenhuma ferramenta nesta máquina que troque o filtro por fora da GUI: `cos-cli` está instalado (`~/.cargo/bin/cos-cli`) mas o `--help` só tem info/move/activate/ws-activate/state/serve/close, e o repo dele não menciona a11y. Escrever no `.ron` à mão NÃO serve: ele é lido no início da sessão (`PersistenceGuard<ScreenFilter>`) e reescrito pelo compositor quando o protocolo muda. Então a rampa depende de um cliente Wayland que ainda não foi escrito.\n\nC. O \"vale a quente = false\" é honesto, mas o custo é maior do que \"uma vez\". Todo `apt upgrade` do cosmic-comp reescreve o binário; o self-heal reaplica o patch, mas o shader novo só passa a valer no PRÓXIMO login. Entre o upgrade e o logout ela fica com os degraus do binário velho — na prática, sem nada, porque o binário novo perde o patch até o self-heal rodar.",
      "risco_real": "ELIMINATÓRIO SE FEITO SEM O TESTE ANINHADO — e o teste existe, então não é eliminatório.\n\nO que quebra: `init_shaders` (`src/backend/render/mod.rs:392`) compila o `POSTPROCESS_SHADER` INCONDICIONALMENTE, com `?`, na subida do renderer — não sob demanda quando o filtro liga. GLSL quebrado = `Failed to compile shaders` / `Failed to initialize shaders` (`src/backend/kms/surface/mod.rs:579`) = o compositor não sobe. Pior: `/etc/greetd/cosmic-greeter.toml` chama `cosmic-greeter-start`, que é `exec cosmic-comp cosmic-greeter` — O MESMO BINÁRIO. Um shader quebrado derruba também a TELA DE LOGIN, e o `cosmic-greeter.service` tem `StartLimitBurst=5` / `StartLimitInterval=30`, ou seja, desiste depois de 5 tentativas. Ela ficaria olhando para nada.\n\nTEM VOLTA, e por dois caminhos:\n1. TTY existe: `loginctl show-seat seat0` diz `CanTTY=yes`, `NAutoVTs` está no default 6, e o greeter só toma o tty1 (`Conflicts=getty@tty1.service`). Ctrl+Alt+F2..F6 continuam vivos.\n2. Backup existe e é o certo: `/var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws`, mesmo tamanho do vivo, difere do vivo em EXATAMENTE 111 bytes, todos dentro de 0x2DC131–0x2DC1A9 (o bloco do night light). É o build com o patch de workspace, sem o patch de shader.\n\nARMADILHA QUE A PROPOSTA NÃO MENCIONA: **NÃO usar `aurora-night-light.py --restore` para desfazer.** Ele restaura de `cosmic-comp-<ver>.orig`, que é o binário DE PACOTE — sem o patch do `ensure_last_empty`. Ela recuperaria o desktop e perderia o workspace vazio-a-mais de volta, com um build de ~4 min para consertar. O rollback correto é:\n    sudo cp /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws /usr/bin/cosmic-comp\n    sudo /usr/local/bin/aurora-night-light.py      # devolve o 3500K\nEm último caso, `apt-get install --reinstall cosmic-comp` (perde os dois patches).\n\nNão há risco de tema: o shader é pós-processamento de tela inteira, indiferente a Light/Dark. Não há risco de reboot: o binário é o mesmo em todo login. Custo permanente já assumido: os três filtros de daltonismo somem e os rótulos de Acessibilidade passam a mentir — \"Protanopia\" vira degrau de calor. É decisão dela, e ela já pagou esse preço uma vez com o \"Escala de cinza\".",
      "correcao": "TRÊS CONSERTOS, na ordem:\n\n1. QUATRO DEGRAUS EXIGEM UM PATCHER SÓ. Aposentar a divisão \"aurora-night-light.py cuida do bloco 1, o novo cuida do bloco 2\" e fazer o `aurora-night-light.py` (v3.31) escrever OS DOIS blocos numa transação, com uma lista de temperaturas: `--degraus 4500,4000,3500,3000` → color_mode 1,2,3,4. O `find_block` dele já delimita o bloco 1 pelo marcador `    } else if (color_mode >= 2.0) {`; o novo bloco tem de PRESERVAR essa linha byte a byte como cabeçalho, senão o próprio `find_block` para de achar o bloco 1 e o script sai com rc=3 (falha segura, mas sem night light). Bloco 2 medido: 1506 bytes disponíveis, do offset 0x2DC1AA até `    // re-multiply`. Texto que cabe (431 bytes, 1075 de padding):\n\n    } else if (color_mode >= 2.0) {\n        if (color_mode == 2.0) {        // LEITURA 2 (Aurora) 4000K\n            color.rgb *= vec3(1.00000, 0.82854, 0.64606);\n        } else if (color_mode == 3.0) { // LEITURA 3 (Aurora) 3500K\n            color.rgb *= vec3(1.00000, 0.77987, 0.54468);\n        } else {                        // LEITURA 4 (Aurora) 3000K\n            color.rgb *= vec3(1.00000, 0.71760, 0.42107);\n        }\n    }\n(+ espaços até fechar 1506, mantendo o `\\n\\n` antes de `    // re-multiply`.)\nSe ela preferir NÃO gastar o bloco 1, a proposta ainda vale — mas anuncie TRÊS degraus, não quatro.\n\n2. ESCREVER O CLIENTE QUE FALTA (é o que torna a rampa possível). Um binário Rust minúsculo — `aurora-a11y-filter <0|1|2|3|4>` — que faz bind em `cosmic_a11y_manager_v1` v2 e manda `set_screen_filter(inverted, filter)`. Provado viável aqui: o global aparece no `wayland-info`, o gate é `client_not_sandboxed`, `cargo 1.98.0` está instalado e o `cosmic-protocols` já está em `~/.cargo/git/checkouts/cosmic-protocols-1292b541ae7bfaf6` (build offline, como o `aurora-cosmic-comp-ws.sh` já faz). Só DEPOIS disso as chaves `LEITURA_DEGRAUS`/`LEITURA_RAMPA` — que hoje não existem em nenhum arquivo — têm em quem mandar.\n\n3. PROVAR ANTES DE INSTALAR, com o procedimento que o próprio Ritual já documenta (self-heal, v3.54, linha ~820): o cosmic-comp sobe ANINHADO quando existe `WAYLAND_DISPLAY` (backend winit). Subir a CÓPIA patchada com `XDG_CONFIG_HOME` isolado e a janela parqueada (`aurora-workspace.sh run …`, para não pular na tela dela). Como `init_shaders` compila o postprocess na subida do renderer, o aninhado falha na hora se o GLSL estiver quebrado — sem nunca tocar em `/usr/bin/cosmic-comp` nem arriscar a tela de login. Só se ele subir é que o `os.replace` acontece. Não há `glslangValidator` nem `glslc` nesta máquina; o aninhado É o validador.\n\nE ANOTAR NO CABEÇALHO DO SCRIPT o rollback correto (`cp` do `.aurora-ws`, não `--restore`), porque hoje o texto do próprio `aurora-night-light.py` manda usar `--restore`, que devolve o binário de pacote e ressuscita o workspace vazio."
     }
    },
    {
     "nome": "Pôr do sol sem geoclue: coordenada derivada do fuso, como o próprio COSMIC faz",
     "alvo": "agendamento",
     "como": "`LEITURA_HORARIO_INICIO=\"por-do-sol\"` faz o `leitura.sh` chamar `scripts/leitura_sol.py`, que lê `/etc/timezone`, resolve `America/Sao_Paulo` numa tabela curta de coordenadas e devolve o pôr do sol em minutos desde a meia-noite. Sem rede, sem D-Bus, sem geoclue. É a mesma estratégia que o `cosmic-settings-daemon` já usa para o modo claro/escuro automático (`include_bytes!(\"../data/timezone-geodata.bitcode-v0-6\")` em src/location.rs). `LEITURA_LOCAL=\"lat,lon\"` sobrepõe, para o caso de ela não estar em São Paulo. Fuso desconhecido → sai 3, o script avisa e cai no HH:MM padrão: falha avisando, nunca inventa cidade.",
     "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura_sol.py, chaves LEITURA_HORARIO_INICIO / LEITURA_HORARIO_FIM / LEITURA_LOCAL no meow.conf",
     "a_quente": true,
     "custo": "Uma execução de Python por tique quando a chave é `por-do-sol` (trigonometria pura, milissegundos). Precisão: coordenada de São Paulo contra a posição real dela — o erro é de minutos. O balanço anual real, calculado: pôr às 17:29 (junho) e 18:52 (dezembro), 1h23 de amplitude.",
     "durabilidade": "Sobrevive a tudo: não depende de rede, de serviço, nem de binário do COSMIC. O único jeito de quebrar é o fuso mudar sem a tabela acompanhar — e aí ela cai no HH:MM padrão com aviso. O Brasil não tem horário de verão desde 2019, e mesmo se voltar o script lê o deslocamento do sistema (`time.timezone`/`time.altzone`) em vez de cravar -3.",
     "trilha": "agendamento",
     "veredito": {
      "solucao": "Pôr do sol sem geoclue: coordenada derivada do fuso (scripts/leitura_sol.py + LEITURA_HORARIO_INICIO=\"por-do-sol\"), trilha \"agendamento\"",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "O QUE SE CONFIRMOU (medido hoje, 29/08/2026):\n\n1. A premissa sobre o COSMIC é VERDADEIRA. `strings /usr/bin/cosmic-settings-daemon` devolve, no mesmo binário: `src/location.rs`, `/etc/timezone`, `/etc/localtime`, `vendor/sunrise/src/solar_equation/mod.rs`, `vendor/iana-time-zone/src`, `vendor/bitcode/src`, e as mensagens `failed to decode timezone geodata:`, `no matching geodata for `, `Failed to calculate sunrise and sunset for current location`, `Failed to calculate next date for theme auto-switch.`. É exatamente a estratégia descrita: fuso → coordenada embutida → crate `sunrise`. A proposta não inventou o precedente.\n\n2. Os NÚMEROS batem. `/etc/timezone` = `America/Sao_Paulo`, `/etc/localtime -> /usr/share/zoneinfo/America/Sao_Paulo`, `timedatectl` = -03, NTP sincronizado. Recalculei o pôr do sol por NOAA para -23.5505/-46.6333 em UTC-3: 21/06/2026 = **17:30**, 21/12/2026 = **18:53**. A proposta disse 17:29 e 18:52 — erro de 1 min entre dois algoritmos, ou seja, a conta dela está certa. Amplitude 1h23 confirmada.\n\n3. Não pisa em ninguém e não precisa de root. `scripts/leitura_sol.py` cai em território Meow; a TRAVA 1 do `lib/comum.sh` recusa `~/.config/zsh`, e nada aqui escreve lá. Unidades `meow-*.service`/`.timer` em `~/.config/systemd/user` já são precedente Meow estabelecido (`meow-wallpaper.*`, `meow-doctor.*`). Sem rede, sem D-Bus, sem geoclue — verificado: o `cosmic-settings-daemon` não expõe NADA de a11y nem de sol no D-Bus (`busctl --user introspect com.system76.CosmicSettingsDaemon /` só tem Peer/Properties/Introspectable). Então derivar por conta própria é mesmo a única via.\n\n4. Bônus não previsto na proposta e que ajuda: `systemctl --user show-environment` traz `WAYLAND_DISPLAY=wayland-1`, `XDG_RUNTIME_DIR=/run/user/1000`, `XDG_SESSION_TYPE=wayland`. Um timer `--user` PODE falar Wayland. Isso importa no item 6.\n\nO QUE NÃO SE PROVOU — e é por isso que não é CONFIRMADO:\n\n5. `scripts/leitura_sol.py` NÃO existe. `scripts/leitura.sh` NÃO existe. `ls /mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura*` → \"no matches found\". Não há chave `LEITURA_*` nenhuma no `meow.conf.exemplo` (grep por leitura|night|noturn|luz|sol|horario não achou uma). Isso não é refutação (é trabalho a fazer), mas significa que esta trilha depende inteira de um `leitura.sh` que é entrega de OUTRA trilha. O relógio sozinho não faz nada.\n\n6. \"vale a quente: true\" NÃO ESTÁ PROVADO, e a proposta o afirma sem base. O relógio em si é um número — nada nele exige reiniciar o compositor, e nisso está certa. Mas a afirmação implícita é que o agendamento surte efeito a quente, e o ATUADOR não foi demonstrado:\n   - O único atuador vivo é o protocolo Wayland `cosmic_a11y_manager_v1`, requests `set_screen_filter` e `set_screen_filter2` — extraído do binário do `cosmic-comp` e confirmado nos dois únicos clientes que o usam, `/usr/bin/cosmic-settings` e `/usr/bin/cosmic-applet-a11y`. É esse caminho que faz o toggle da GUI valer a quente hoje.\n   - NÃO existe atuador de linha de comando nesta máquina. Varri `/usr/local/bin`, `~/.local/bin` e `~/.config/zsh/scripts` por `cosmic_a11y|screen_filter|a11y_screen_filter`: o único acerto é um arquivo de TEXTO, `~/.config/zsh/scripts/SPRINTS-PENDENCIAS-2026-07.md`.\n   - E o que esse arquivo diz é decisivo: a linha 227 registra \"**Hipótese a testar (1 comando, reversível)**: o estado do filtro vive em `~/.local/state/cosmic-comp/a11y_screen_filter.ron` … `filter: Some(Greyscale)`\". Hipótese. Não medida. O arquivo vivo existe (mtime hoje 17:46) e contém só `(\\n    inverted: false,\\n)` — sem a linha `filter`, coerente com \"desligado\", mas isso NÃO prova que o `cosmic-comp` RELÊ o arquivo quando alguém de fora o escreve. As strings do binário (`Failed to read screen_filter state, resetting..`, `cosmic-comp/a11y_screen_filter.ron`) são compatíveis com um arquivo de PERSISTÊNCIA lido só na subida. Não testei — testar exigiria escrever, e a tarefa proíbe.\n   Portanto: o relógio é sólido; o \"a quente\" é herdado de um atuador ainda não provado.\n\n7. A \"tabela curta de coordenadas\" é desnecessária e é o ponto mais fraco do desenho. Ver correção — existe fonte de dados melhor já instalada.\n\n8. Duas contas erradas escondidas no texto da proposta: o desvio de fuso e a margem de erro. Ver correção.",
      "risco_real": "BAIXO, e essa é a maior qualidade da proposta — é a única das trilhas que não pode tirar o desktop dela.\n\nO que ela NÃO faz: não escreve em `/usr/bin/cosmic-comp`, não recompila shader, não pede sudo, não reinicia processo nenhum, não toca em tema, ícone, painel ou `plugins_wings`. Um `leitura_sol.py` quebrado morre com traceback e o agendamento simplesmente não dispara. Não há caminho pelo qual isto derrube a sessão — TTY nunca entra em cena.\n\nO pior caso realista é cosmético e reversível em segundos: a luz quente liga na hora errada. Volta: trocar `LEITURA_HORARIO_INICIO=\"por-do-sol\"` por um `HH:MM` no `~/.config/meow/meow.conf`, ou desligar pelo próprio \"Acessibilidade > Filtros de cor\" na GUI, que é o mesmo toggle que ela já usa.\n\nTRÊS RISCOS QUE PRECISAM FICAR ESCRITOS, porque nenhum deles está na proposta:\n\n(a) ELIMINATÓRIO se alguém encanar errado: o agendamento NUNCA pode chamar `aurora-night-light.py`. Esse script repatcha `/usr/bin/cosmic-comp` byte a byte e o efeito só aparece com o compositor reiniciado — LOGOUT. Um cronograma de pôr do sol ligado nele deslogaria a Vitória toda noite, no meio do que ela estivesse fazendo. O agendamento só pode mexer no uniform `color_mode` (protocolo Wayland ou estado), jamais no binário. Se a implementação encostar em `aurora-night-light.py`, isto vira REFUTADO na hora.\n\n(b) `~/.local/state/cosmic-comp/` não tem dono declarado no `docs/FRONTEIRA.md`, e quem escreve nele hoje é o próprio compositor. Se o atuador for por arquivo, é corrida: ela mexe pela GUI, o comp regrava, o timer regrava por cima. O `wallpaper.sh` já pagou esse preço (o caso \"reversão em 2,1 s\" documentado no `meow-wallpaper.timer`). Precisa de linha nova na FRONTEIRA e de um caso \"escolha dela\" antes de existir.\n\n(c) Sobrevivência: a `apt upgrade` do cosmic-comp, o relógio sobrevive intacto (não depende de binário nenhum). A reboot, sobrevive. A troca de tema, sobrevive — é ortogonal. O que NÃO sobrevive garantido é o atuador: `set_screen_filter2` já é a segunda versão do request (`set_screen_filter` e `set_screen_filter2` coexistem no binário), o que prova que a System76 versiona e quebra essa API. Um `set_screen_filter3` num upgrade futuro quebra o atuador — e aí o sintoma é a luz parar de ligar sozinha, sem aviso. Vale um `--conferir` que reclame alto quando o request some, no mesmo espírito do `aurora-cosmic-comp-ws.sh --ensure`.\n\nConvive com o patch de workspace do Aurora sem competir: são arquivos disjuntos, e nem o `--build` do `aurora-cosmic-comp-ws.sh` nem o `aurora-night-light.py` encostam em nada que esta trilha escreva.",
      "correcao": "1) JOGUE FORA A TABELA CURTA. A máquina já tem coordenada de TODO fuso IANA, instalada e mantida pelo apt:\n   `/usr/share/zoneinfo/zone.tab` — pacote `tzdata` (confirmado por `dpkg -S`), 418 zonas, formato ISO 6709.\n   `grep Sao_Paulo /usr/share/zoneinfo/zone.tab` → `BR  -2332-04637  America/Sao_Paulo  Brazil (southeast: GO, DF, MG, ES, RJ, SP, PR, SC, RS)`\n   `-2332-04637` = lat -23°32' = -23.5333, lon -046°37' = -46.6167 — a mesma coordenada que a proposta ia cravar à mão. Parsear isso (dez linhas) elimina a tabela, elimina o modo de falha \"fuso desconhecido → sai 3\" para qualquer fuso real, e a coordenada passa a se atualizar com o `tzdata` sem ninguém tocar no MeowSystem. Existe também `zone1970.tab` como fallback. Mantenha o `exit 3` só para o caso de o `zone.tab` sumir.\n\n2) NÃO USE `time.timezone` / `time.altzone`. Medido nesta máquina agora:\n   `time.timezone = 10800`, `time.altzone = 10800`, `time.daylight = 0`.\n   Duas armadilhas reais: (i) 10800 é positivo porque é segundos a OESTE de UTC — um sinal trocado põe o pôr do sol 6 horas fora; (ii) com `daylight == 0` o `altzone` é lixo (repete o `timezone`), e se o horário de verão voltar escolher entre os dois exige `time.localtime().tm_isdst`, que a proposta não menciona.\n   Use `zoneinfo`, que já funciona aqui (Python 3.12.3, `tzdata` presente — testado):\n   `ZoneInfo(open('/etc/timezone').read().strip())`, ou `os.path.realpath('/etc/localtime')` descascando `/usr/share/zoneinfo/` como segunda via (os dois caminhos existem e concordam nesta máquina). Conferido: dá -3:00 tanto para 29/08 quanto para 15/01, e passa a acertar sozinho o dia em que o horário de verão voltar, por DATA, não por chute. `datetime.now().astimezone().utcoffset()` também devolveu -3:00 corretamente e serve de fallback.\n\n3) CORRIJA A MARGEM DE ERRO NO TEXTO. \"o erro é de minutos\" é otimista demais e vai virar queixa dela. O `zone.tab` diz que `America/Sao_Paulo` cobre GO, DF, MG, ES, RJ, SP, PR, SC e RS. Calculado para 21/12/2026, UTC-3: São Paulo 18:53, Rio 18:38, **Porto Alegre 19:26**. O erro dentro do próprio fuso chega a **33 minutos**, não \"minutos\". Escreva o número no cabeçalho e deixe o `LEITURA_LOCAL=\"lat,lon\"` bem visível no `meow.conf.exemplo` como a cura — ele está certo, só está subvendido. (Consolo honesto: o `cosmic-settings-daemon` erra igual, porque também guarda uma coordenada por fuso.)\n\n4) SEPARE O RELÓGIO DO ATUADOR NA PROSA. Reescreva \"vale a quente: true\" como \"o relógio não exige reiniciar nada; se o efeito é a quente depende do atuador da trilha (A)\". E prove o atuador antes de fechar: o alvo é `cosmic_a11y_manager_v1.set_screen_filter2` (extraído do `cosmic-comp` e presente em `cosmic-settings` e `cosmic-applet-a11y`), que é o caminho que hoje faz o toggle da GUI valer a quente. Um cliente Wayland mínimo serve, e o `WAYLAND_DISPLAY=wayland-1` já está no ambiente do `systemd --user`, então um `meow-leitura.service` alcança o compositor. A rota alternativa — escrever `filter: Some(Greyscale)` no `~/.local/state/cosmic-comp/a11y_screen_filter.ron` — continua sendo HIPÓTESE (está assim, em letra dela, no `~/.config/zsh/scripts/SPRINTS-PENDENCIAS-2026-07.md:227`); teste com um comando reversível antes de construir em cima.\n\n5) PROIBIÇÃO EXPLÍCITA NO CABEÇALHO DO SCRIPT: \"este agendamento nunca chama `aurora-night-light.py`\". Repatch = reinício do compositor = logout. Sem essa linha, o próximo frente reaproveita o script que já existe e transforma o pôr do sol num deslogamento diário.\n\n6) O TIMER. Se virar `meow-leitura.timer`, copie três coisas do `meow-wallpaper.service`, que já foram pagas caro: o `flock -n -E 0` sobre `~/.local/state/meowsystem/lock` (senão o tique entra no meio de um `meow doctor --consertar`), o `set -a` ao ler o `meow.conf` (lista fixa de `export` é o modo de falha nº 3 documentado — seis chaves `FORMA_*` morreram assim em 25/08), e a justificativa contra o \"um timer diário e mais nada\" do `meow-doctor.timer` escrita no cabeçalho. Frequência: um tique de 15 min basta — o pôr do sol anda ~1 min/dia, e o \"por tique\" da proposta é de fato barato (trigonometria pura), mas não precisa ser de minuto em minuto.\n\n7) DECLARE NA FRONTEIRA. Acrescente duas linhas em `docs/FRONTEIRA.md`: `~/.local/state/cosmic-comp/a11y_screen_filter.ron` (ou o cliente Wayland) → Meow, e o lembrete de que o binário `cosmic-comp` e a temperatura literal continuam do Aurora. Hoje o agendamento dirigiria um patch do Aurora sem nada escrito dizendo isso — exatamente o tipo de sobreposição que a FRONTEIRA existe para evitar."
     }
    },
    {
     "nome": "A trégua: se ela desligar na mão, o agendador cala a boca até o fim da janela",
     "alvo": "ui",
     "como": "O filtro tem controle na GUI. Sem tratamento, ela desligaria às 20h e o timer religaria 5 minutos depois — 288 vezes por dia, a versão industrial do erro que custou a opacidade do painel. Desenho: o `tique` guarda em `$MEOW_ESTADO/leitura/ultimo-degrau` o degrau que NÓS pusemos. Se num tique o compositor estiver em 0 e o registro disser que fomos nós que ligamos, isso é ela dizendo não: grava `$MEOW_ESTADO/leitura/tregua` e não toca em mais nada até a janela acabar. A trégua morre sozinha no primeiro tique fora da janela — não há o que limpar, e amanhã à noite tudo volta. O mesmo registro impede o inverso: se ela LIGOU o filtro na mão fora da janela, o `tique` não desliga (só desliga o que ligou).",
     "onde": "cmd_tique em /mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh; estado em ~/.local/state/meowsystem/leitura/",
     "a_quente": true,
     "custo": "Dois arquivos de poucos bytes em $MEOW_ESTADO. Custo conceitual: o `conferir` passa a devolver MEOW_OK durante uma trégua, então o `meow doctor` não acusa divergência numa noite em que ela desligou — o que é o comportamento certo, mas precisa estar dito no `estado` (e está: ele imprime \"há uma TRÉGUA aberta desde HH:MM\").",
     "durabilidade": "Estado puro, sobrevive a reboot e a apt upgrade. Some com `meow leitura remover` ou apagando o diretório — apagar não perde nada além da trégua da noite corrente.",
     "trilha": "agendamento",
     "veredito": {
      "solucao": "A trégua: se ela desligar na mão, o agendador cala a boca até o fim da janela",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "A trégua precisa de duas metades: LER se o filtro está ligado e LIGAR/DESLIGAR o filtro. Medi as duas.\n\nLER — CONFIRMADO. O estado vive em `/home/vitoriamaria/.local/state/cosmic-comp/a11y_screen_filter.ron` (nome tirado do próprio binário: `strings /usr/bin/cosmic-comp` traz \"cosmic-comp/a11y_screen_filter.ron\" junto de outputs.ron/numlock.ron). E é escrito VIVO: o cosmic-comp (pid 3127) subiu em 28/08 18:43, e o mtime do arquivo é 29/08 17:46 — 23h dentro da sessão. Então o `conferir` é viável.\n\nAGIR — REFUTADO, com prova. Escrever esse .ron não chega a ninguém. O cosmic-comp mantém exatamente 7 watches de inotify (`/proc/3127/fdinfo/*`), e resolvi os 7 inodes: 3c0008→`~/.config/cosmic/com.system76.CosmicComp/v1`, 3c000b→CosmicTheme.Mode/v1, 3c0b77→CosmicTheme.Dark/v2, 3c001c→CosmicTk/v1, 3c0021→CosmicSettings.WindowRules/v1, 3c001f→CosmicSettings.Shortcuts/v1, 4003ed→CosmicTheme.Light/v2. TODOS sob `~/.config/cosmic`. O diretório de estado (inode 3932194 = 0x3c0022) e o próprio arquivo (3932253 = 0x3c005d) NÃO aparecem em nenhum. O .ron é lido uma vez na subida (\"Failed to read screen_filter state, resetting..\" em src/config/mod.rs). Escrever nele só vale no próximo login — inútil para uma janela das 20h.\n\nE não há outra porta nesta máquina:\n- D-Bus: `org.freedesktop.a11y.Manager` É do cosmic-comp (pid 3127), mas o introspect devolve só `org.freedesktop.a11y.KeyboardMonitor` (GrabKeyboard/SetKeyGrabs/WatchKeyboard/KeyEvent). Zero método de filtro. `com.system76.CosmicComp` na raiz só tem Introspectable/Peer/Properties.\n- Atalho: `strings /usr/bin/cosmic-comp` só tem ScreenReader, ZoomIn, ZoomOut, ZoomToggle. Não existe ação de filtro de cor — confirma o que o próprio aurora-night-light.py já dizia no cabeçalho.\n- Wayland: o único setter real é `cosmic_a11y_manager_v1.set_screen_filter2`. Varri /usr/bin e /usr/local/bin: quem fala esse protocolo é só GUI (cosmic-applet-a11y, cosmic-settings, cosmic-greeter, os applets). Não existe CLI.\n\nLogo, um `cmd_tique` em shell não liga o filtro. O \"vale a quente: true\" é falso na metade que age.\n\nSEGUNDO DEFEITO, independente e traiçoeiro: o arquivo hoje é literalmente `(\\n    inverted: false,\\n)` — 24 bytes. Com o filtro DESLIGADO não existe campo `filter:` nenhum. A proposta diz \"se o compositor estiver em 0\" — não há 0, há AUSÊNCIA de chave. Um teste escrito contra \"0\" nunca dispara, a trégua nunca abre, e o agendador vira exatamente as 288 religadas por dia que a proposta existe para impedir — em silêncio.\n\nTERCEIRO: território. `docs/FRONTEIRA.md:58` dá o binário cosmic-comp \"(patches de workspace e night light)\" à **Aurora**. O leitura.sh não toca no binário, então não é violação dura — mas night light é feature da Aurora (aurora-night-light.py v3.30) e já existe timer da Aurora nesse domínio rodando diariamente: `~/.config/zsh/scripts/aurora-cosmic-nightlight-watch.{sh,service,timer}`. Pôr um agendador do Meow em cima disso sem uma linha nova na FRONTEIRA é preparar briga entre os dois donos.",
      "risco_real": "Baixo, e esse é o único ponto genuinamente bom da proposta. Nada aqui toca no binário do compositor nem no ciclo de vida da sessão — o pior caso é o agendador escrever um .ron que ninguém lê e não fazer nada (falha inerte, não fatal). Ela NÃO fica sem desktop, e não é preciso TTY: se o .ron for corrompido, o próprio cosmic-comp trata (\"Failed to read screen_filter state, resetting..\" + \"Failed to remove screen_filter state.\"), e apagar `~/.local/state/cosmic-comp/a11y_screen_filter.ron` regenera no próximo login. Sobrevive a reboot, a apt upgrade e a troca de tema, porque é só estado em disco.\n\nO risco real não é quebrar a tela: é a proposta parecer que funciona. Como a metade de escrita falha em silêncio e o teste de \"está em 0\" nunca casa com o formato real do arquivo, o agendador entrega nada e o `meow doctor` diz MEOW_OK — ela pede a feature, a gente diz que entregou, e às 20h não acontece coisa nenhuma.",
      "correcao": "O desenho da trégua é bom e deve ser preservado — o que falta é o chão embaixo dele. Quatro correções:\n\n1. CONSTRUIR O SETTER QUE NÃO EXISTE. É a única via a quente: um cliente Wayland minúsculo que faça bind de `cosmic_a11y_manager_v1`, leia o estado pelo evento `screen_filter2` e chame `set_screen_filter2(filter, inverted)`. Sai barato: `cargo 1.98.0` está instalado e o `cosmic-protocols` já está vendorizado em `/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/vendor/cosmic-protocols` — dá para compilar offline, é um binário de ~100 linhas com `get`/`set`, não os ~4 min do rebuild do compositor. Enquanto esse cliente não existir, a trégua não tem o que segurar e o item (C) inteiro fica no papel.\n\n2. TROCAR O PREDICADO. \"compositor está em 0\" tem de virar \"o .ron NÃO contém a chave `filter`\". Estado real desligado hoje: `(\\n    inverted: false,\\n)`. Nunca testar contra `0`. E melhor ainda: depois do item 1, ler o estado pelo próprio cliente Wayland (que vê a verdade em memória) em vez do arquivo, deixando o .ron só como conferência de reboot.\n\n3. ONDE O TIQUE RODA. Não há unidade de leitura em `/mnt/Apate/Desenvolvimento/MeowSystem/systemd/`. Um cliente Wayland precisa de WAYLAND_DISPLAY, que timer de usuário não recebe de graça — e isso já está resolvido no repo: reaproveitar a descoberta de socket de `/mnt/Apate/Desenvolvimento/MeowSystem/lib/painel.sh:200-203` e o `ConditionEnvironment=WAYLAND_DISPLAY` de `systemd/meow-doctor.service:69`. Não reinventar.\n\n4. FECHAR A FRONTEIRA ANTES DE ESCREVER. `docs/FRONTEIRA.md:58` é da Aurora. Ou o leitura.sh nasce na Aurora, ou a FRONTEIRA ganha uma linha explícita separando \"binário e patch do shader = Aurora / agendamento do filtro = Meow\", e essa linha entra ANTES do primeiro commit — não depois da primeira briga."
     }
    },
    {
     "nome": "O `conferir` que pergunta à TELA, não ao disco",
     "alvo": "agendamento",
     "como": "Quase todo `chk_` deste projeto compara o que nós gravamos com o que queríamos gravar — e o cabeçalho do `janelas.sh` documenta o buraco disso: o `pinned_workspaces` fazia o doctor dizer \"conforme\" enquanto o lado a lado morria no reboot. Aqui o `conferir` não tem esse buraco de graça: o protocolo devolve o estado no bind, então `leitura.sh conferir` pergunta ao COMPOSITOR em que degrau a tela está e compara com o que o relógio pede. É a diferença entre \"o arquivo está certo\" e \"a tela está certa\", e neste caso ela sai de graça.",
     "onde": "cmd_conferir e _leitura_no_compositor em /mnt/Apate/Desenvolvimento/MeowSystem/scripts/leitura.sh",
     "a_quente": true,
     "custo": "Um roundtrip Wayland (~ms) por execução do doctor. Nenhum estado gravado.",
     "durabilidade": "Depende de o global continuar sendo bindável por cliente comum (`client_not_sandboxed`). Se um upstream fechar isso, o `conferir` degrada para MEOW_SEM_DEPENDENCIA — avisa e não mente.",
     "trilha": "agendamento",
     "veredito": {
      "solucao": "O `conferir` que pergunta à TELA, não ao disco — `cmd_conferir` + `_leitura_no_compositor` em scripts/leitura.sh (trilha agendamento)",
      "veredito": "CONFIRMADO",
      "a_quente_confirmado": true,
      "porque": "Tentei derrubar e não caiu — o mecanismo está provado ao vivo, agora, nesta máquina.\n\n1. O GLOBAL EXISTE E É VISÍVEL A CLIENTE COMUM. `wayland-info` desta sessão lista `interface: 'cosmic_a11y_manager_v1', version: 2, name: 53` (58 globais no total). O filtro de bind é `client_not_sandboxed` (src/state.rs:762, definido em :647-651 → `ClientState::not_sandboxed()`, :167-170: passa quem não tem security context). Um shell sem privilégio nenhum enxerga.\n\n2. O ESTADO VEM NO BIND — PROVADO NO FIO, NÃO NO PAPEL. Abri o socket cru em python (sem dependência), fiz `wl_display.get_registry`, `bind` v2 e `wl_display.sync`. Resposta: `EVENT obj4 opcode=0 args=(0,)` (magnifier) e `EVENT obj4 opcode=1 args=(0, 0)` (screen_filter: inverted=0, filter=0/disabled), tudo antes do `done`. O fonte da versão instalada confirma: `GlobalDispatch::bind` em src/wayland/protocols/a11y.rs emite `magnifier` e, para version >= 2, `screen_filter` na hora do bind. Nada de \"só na inicialização\": é resposta imediata, por roundtrip, sem reiniciar coisa alguma.\n\n3. É REALMENTE \"A TELA\", NÃO OUTRA CÓPIA DO DISCO. Em src/wayland/handlers/a11y.rs, `request_screen_filter` só chama `a11y_state.set_screen_filter(...)` DEPOIS de `backend.update_screen_filter(&updated)` retornar Ok. O estado do protocolo é o último filtro efetivamente aplicado no render, não a intenção.\n\n4. E O DISCO PODE MENTIR DE VERDADE AQUI. O arquivo é `~/.local/state/cosmic-comp/a11y_screen_filter.ron` (src/config/mod.rs:348), lido UMA vez no `load_dynamic` do arranque; o `ConfigWatchSource` do compositor observa `com.system76.CosmicComp` (cosmic-config), não esse .ron. Quem editar o .ron na mão faz o disco dizer uma coisa e a tela outra, sem nenhum aviso — que é exatamente o buraco do `pinned_workspaces` citado no cabeçalho de scripts/janelas.sh (item 3), e que ali é real: aquele arquivo só é lido no início da sessão.\n\n5. NÃO É DEPENDÊNCIA NOVA E JÁ TEM PRECEDENTE NO PRÓPRIO REPO. /mnt/Apate/Desenvolvimento/MeowSystem/scripts/appid_sonda.py já é um cliente Wayland cru em socket+struct, com `min(ver, 1)` no bind, `settimeout` e laço limitado por `time.time() - t0 < limite`. O `_leitura_no_compositor` é o mesmo molde trocando a interface. Python 3.12.3 presente.\n\n6. FRONTEIRA: LIMPO, E ATÉ EXEMPLAR. docs/FRONTEIRA.md dá o binário do cosmic-comp (patches de workspace e night light) à Aurora — e diz \"quando a Aurora precisa continuar sendo a dona, o Meow não escreve — ele confere e diz\". Ler o protocolo é precisamente isso. Nada escrito em ~/.config/zsh, nada em /usr/share, nenhuma janela aberta.\n\n7. O DOCTOR TEM AMBIENTE PARA ISSO. `systemctl --user show-environment` traz `WAYLAND_DISPLAY=wayland-1` e `XDG_RUNTIME_DIR=/run/user/1000`; o `meow-doctor.service` ainda tem `ConditionEnvironment=WAYLAND_DISPLAY`, então ou há sessão gráfica ou a unidade nem roda. E bin/meow:24 garante que o doctor nunca usa sudo — não há o caso \"root sem socket\".\n\nO que NÃO confirmei: scripts/leitura.sh AINDA NÃO EXISTE (o `ls` de scripts/ não o traz). O \"onde\" é arquivo a criar, não caminho a corrigir. `MEOW_SEM_DEPENDENCIA` existe e é 3 (lib/comum.sh:27), então a degradação proposta usa a interface certa.",
      "risco_real": "Perto de zero, e sem caminho de recuperação necessário porque não há nada a recuperar: o `conferir` só abre o socket, faz bind e lê. Nenhuma request de escrita (`set_screen_filter` NÃO é enviada), nenhum arquivo gravado, nenhuma janela criada — a instância entra e sai do `Vec<instances>` do compositor no `destroyed`, quando o socket fecha. Se o cliente morrer no meio, o compositor limpa sozinho.\n\nOs dois modos de falha que sobram são de INFORMAÇÃO, não de tela:\n\n(a) TRAVAR O DOCTOR. Um cliente Wayland sem `settimeout` e sem laço limitado bloqueia para sempre se o compositor não responder, e o doctor segura o lock de ~/.local/state/meowsystem/lock. Mitigado de fábrica pelo `TimeoutStartSec=5min` da unidade, e resolvido de vez copiando o padrão do appid_sonda.py (timeout + `t0`). Sem TTY, sem logout, sem tela preta.\n\n(b) DIZER \"CONFORME\" COM A TELA CINZA. Ver a correção. O protocolo devolve o ENUM do filtro, nunca o conteúdo do shader.\n\nNão há risco de ela ficar sem desktop. Isso vale para ESTA peça (a leitura); a trilha que ESCREVE o degrau e repatcha o shader é outra conversa e tem risco próprio.",
      "correcao": "Três emendas, todas baratas, e a primeira é obrigatória.\n\n1. O PROTOCOLO DEVOLVE O ENUM, NÃO O SHADER — E ISSO REABRE O BURACO UM ANDAR ACIMA. `screen_filter` diz \"greyscale/protanopia/…\", que é `ColorFilter` (src/config/mod.rs:167-172: Greyscale=1 … Tritanopia=4, virando `color_mode` no shader). O que faz o degrau 1 ser QUENTE e não CINZA é o literal `vec3(1.00000, 0.77987, 0.54468)` patchado no binário pelo aurora-night-light.py — e o fonte upstream em .../cosmic-comp-0.1~1787767625~24.04~5c93094/src/backend/render/shaders/offscreen.frag mostra que `color_mode == 1.0` é, sem o patch, a média cinza. Depois de um `apt upgrade` que troque o cosmic-comp sem o patch, o `conferir` leria \"filtro greyscale ligado, relógio pede ligado → conforme\" com a tela dela literalmente CINZA. Nos degraus 2/3/4 seria pior: matriz de daltonismo de verdade. Conserto: o `conferir` também tem de checar a impressão digital do shader, `NIGHT LIGHT (Aurora)` (o marcador certo — aurora-night-light.py:69; o `aurora-cosmic-comp-ws.sh` já registra que a string 'aurora-night-light' NUNCA casou com nada).\n\n2. CHECAR O BINÁRIO VIVO, NÃO O DO DISCO — e isto não é teoria, é o estado desta máquina AGORA: `/proc/3127/exe -> /usr/bin/cosmic-comp (deleted)`. O arquivo em /usr/bin já foi trocado desde o login dela; o compositor rodando é outro inode. `grep -ac 'NIGHT LIGHT (Aurora)' /usr/bin/cosmic-comp` responde sobre um binário que ela não está rodando. O certo é `p=$(pgrep -x cosmic-comp | head -1); grep -acs 'NIGHT LIGHT (Aurora)' \"/proc/$p/exe\"` — testado, legível pelo usuário dela, devolveu 1 e o `strings` do /proc/PID/exe mostra o vec3 quente. Com isso o `conferir` faz DUAS perguntas sobre a tela viva: qual degrau está selecionado (protocolo) e se o shader em execução ainda é o quente (/proc/PID/exe).\n\n3. TRAVAR A VERSÃO DO BIND EM 2, NUNCA `ver` CRU. O compositor instalado cria o global com version 2 (a11y.rs, `create_global::<…>(2, …)`), mas o cosmic-protocols travado no Cargo.lock (rev 32283d7) já define version 3, onde `screen_filter` é `deprecated-since=3` e \"não será mais emitido\" — o substituto é `screen_filter2` (opcode de evento 2, com um terceiro argumento `filter_state`), e `set_screen_filter` em bind v3 levanta erro de protocolo `deprecated`. No dia em que o Pop subir o cosmic-comp para v3, um cliente que binda a versão anunciada para de receber o evento e o `conferir` fica mudo — ou pior, calado e \"conforme\". Bindar `min(ver, 2)`, exatamente como o appid_sonda.py faz com `min(ver, 1)`, e degradar para MEOW_SEM_DEPENDENCIA se nenhum evento chegar dentro do timeout. Vale anotar no cabeçalho que os valores do FIO não são os do shader: fio greyscale=2, protanopia=3, deuteranopia=4, tritanopia=5, e `color_mode = fio - 1`.\n\nEmenda ao texto da proposta: onde ela diz \"sai de graça\", o correto é \"sai barato\" — de graça sai a metade do protocolo; a outra metade (o shader vivo) custa um `grep` no /proc."
     }
    },
    {
     "nome": "A rampa minuto a minuto, se um dia a temperatura virar contínua",
     "alvo": "agendamento",
     "como": "Guardado como caminho, não como recomendação: `OnCalendar=*-*-* 18:00..19:00` PARSEIA (testado) e dispara 18:00, 18:01, 18:02… — 20 tiques restritos à janela de subida, sem tocar o resto do dia. Duas unidades assim (subida e descida) dão uma rampa minuto a minuto por 40 invocações/dia, contra 1440 de um timer minutely. É o desenho a adotar SE a outra trilha conseguir recompilar o cosmic-comp com um uniform contínuo de temperatura, porque aí 5 minutos ficam grossos demais. Não é o desenho de hoje porque exige REGERAR a unidade toda vez que ela mudar o horário no meow.conf — acoplamento entre chave de conf e unit systemd, que é justamente o que a memória \"chave nova ≠ ponte fechada\" manda evitar.",
     "onde": "systemd/meow-leitura.timer (variante documentada no cabeçalho da unit)",
     "a_quente": true,
     "custo": "40 invocações/dia × 20-30 ms = ~1 s/dia. Mas soma o custo humano de manter uma unit gerada a partir do conf, com `daemon-reload` no caminho do `aplicar`.",
     "durabilidade": "A expressão OnCalendar é estável (systemd a normaliza sem reclamar). A fragilidade é a regeneração: uma unit gerada que ficar dessincronizada do conf é um agendador que dispara na hora errada e não diz por quê.",
     "trilha": "agendamento",
     "veredito": {
      "solucao": "A rampa minuto a minuto via `OnCalendar=*-*-* 18:00..19:00` (systemd/meow-leitura.timer, duas unidades geradas a partir do meow.conf)",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "TRÊS refutações medidas nesta máquina.\n\n1) A EXPRESSÃO NÃO É UMA JANELA DE HORÁRIO — É UM ACIDENTE DE PARSING. `systemd-analyze calendar` mostra que `*-*-* 18:00..19:00` é lido como hora=18, MINUTO=00..19, segundo=00. Medido: dispara 18:00…18:19 e então pula para 18:00 do dia seguinte (iteração #8 = 18:19, #9 = amanhã 18:00). Os \"20 tiques\" batem por coincidência aritmética, mas a janela é de 20 MINUTOS dentro da hora 18, não de 18:00 às 19:00. Quem ler a unit vai entender errado — e a proposta a descreve como \"janela de subida\" de 18:00 a 19:00.\n\n2) A UNIT GERADA A PARTIR DO CONF QUEBRA NA MAIORIA DOS HORÁRIOS DELA. Concatenar `HH:MM..HH+1:MM` só parseia quando o minuto inicial ≤ o número da hora final. Medido:\n   - `*-*-* 18:30..19:30` → \"Failed to parse calendar specification: Argumento inválido\" (30 > 19). A unit não carrega.\n   - `*-*-* 22:45..23:45` → mesma falha.\n   - `*-*-* 21:20..22:20` → parseia, e vira hora 21, minuto 20..22, SEGUNDO 20: três tiques (21:20:20, 21:21:20, 21:22:20). Silenciosamente errado.\n   - `*-*-* 23:00..00:00` → normaliza para `*-*-* 23:00:00`: UM tique. A rampa some sem erro nenhum.\n   - `*-*-* 18:00..18:59` → normaliza com segundo 59 (próximo disparo medido: 18:11:59).\n   Ou seja: o modo de falha não é \"unit dessincronizada do conf\" (risco gerenciável, como a proposta admite) — é \"ela digita 18:30 e o agendador ou não sobe ou dispara 3 vezes no segundo 20\". Isso é pior do que a proposta declara.\n\n3) \"VALE A QUENTE = true\" É FALSO, hoje e no cenário hipotético. A rampa só faz sentido se cada tique puder mexer na temperatura. Medido em /usr/local/bin/aurora-night-light.py: a temperatura é literal GLSL dentro de /usr/bin/cosmic-comp; trocar exige (a) root — `sudo -n /usr/local/bin/aurora-night-light.py --status` responde \"sudo: uma senha é necessária\", não há NOPASSWD para ele — e (b) relogin, o próprio script imprime \"precisa relogar para o compositor recarregar o shader\". 40 tiques/dia = 40 logouts/dia.\n   E nem o liga/desliga discreto serve de plano B por arquivo: o estado vive em ~/.local/state/cosmic-comp/a11y_screen_filter.ron (conteúdo atual: `( inverted: false, )`), e esse diretório NÃO está sob inotify do cosmic-comp. Medido: inode do dir de estado = 0x3c0022; os 7 watches do PID 3127 são 3c000b, 4003ed, 3c0b77, 3c0008, 3c001c, 3c001f, 3c0021 — 3c0008 é ~/.config/cosmic/com.system76.CosmicComp/v1 (esse sim vigiado), 3c0022 não aparece. Escrever o .ron por timer não vale a quente; o único caminho quente é o protocolo cosmic_a11y_manager_v1 (string presente no binário, src/wayland/protocols/a11y.rs), e não existe cliente CLI dele nesta máquina.\n\n4) FRONTEIRA: docs/FRONTEIRA.md linha 58 — \"binário cosmic-comp (patches de workspace e night light) | Aurora | Aurora\". Uma unit em MeowSystem/systemd/ dirigindo a temperatura dirige artefato da Aurora. E o cabeçalho do systemd/meow-doctor.timer documenta a regra explícita de não marcar encontro com o Ritual da Aurora (root, de hora em hora); um tique por minuto repatchando o binário é exatamente o encontro que aquela regra existe para evitar.\n\nNota de caminho: systemd/meow-leitura.timer NÃO existe (systemd/ tem 14 arquivos, nenhum de leitura) e não há chave de leitura/night light no meow.conf.exemplo — grep por leitura|night|temperatur|filtro não achou nada. Não é refutação por si (é proposta), mas invalida a frase \"variante documentada no cabeçalho da unit\": não há unit.",
      "correcao": "Se a trilha de agendamento for adiante, três correções:\n\n(a) NUNCA montar OnCalendar por concatenação de horário. Se precisar de janela por minuto, use forma que não tem armadilha de campo: `*-*-* 18:*:00` (60 tiques na hora 18, medido) ou `*-*-* 18,19:*:00` (120 tiques, medido, atravessa a virada de hora). `18:00/1:00` também funciona mas para na virada da hora.\n\n(b) Manter a unit ESTÁTICA e pôr o portão no script, não no OnCalendar — um único `OnCalendar=*-*-* *:*:00` (ou de 5 em 5 min) e o script lê o horário do meow.conf e decide se age. Isso elimina daemon-reload no caminho do `aplicar` e o acoplamento conf↔unit que a memória \"chave nova ≠ ponte fechada\" manda evitar. Custo medido do exagero: 1440 × ~25 ms ≈ 36 s de CPU/dia, aceitável. Alternativa sem timer nenhum: um service `Type=simple` que dorme até a próxima transição.\n\n(c) Não agendar NADA que mexa em temperatura enquanto (i) não existir uniform contínuo E (ii) não existir um cliente que fale cosmic_a11y_manager_v1 para aplicar a quente. Até lá, um agendador só pode ligar/desligar, e nem isso ele consegue hoje por arquivo.",
      "risco_real": "O timer sozinho é inofensivo: é user unit, e se a expressão não parsear a unit apenas não carrega (`systemctl --user status` mostra o erro) — a tela dela não muda, não há perda de desktop. O risco está no que o tique CHAMA.\n\nSe o tique chamar aurora-night-light.py: (1) precisa de senha de root, então ou não roda (rampa silenciosamente morta) ou alguém abre um NOPASSWD para um script que reescreve /usr/bin/cosmic-comp — superfície nova em território da Aurora; (2) cada passo exigiria derrubar o compositor, ou seja, a sessão dela fecha no meio do uso.\n\nVolta existe e está documentada, mas tem uma armadilha: `sudo aurora-night-light.py --restore` copia /var/lib/aurora/cosmic-comp-<versão>.orig — que é o binário DE PACOTE, sem o patch de workspace. Restaurar a luz noturna reverte junto o patch do cosmic-comp-ws e o workspace vazio volta; para recuperar os dois é preciso rodar `aurora-cosmic-comp-ws.sh --build` (~4 min) depois. Os .aurora-ws e .orig estão todos em /var/lib/aurora (conferi: 3 gerações presentes, incluindo a versão viva 0.1~1787767625~24.04~5c93094), e último recurso é `sudo apt-get install --reinstall cosmic-comp` por TTY. Nenhum passo exige interface gráfica, então não há cenário de \"ficou sem desktop e sem saída\"."
     }
    }
   ]
  },
  {
   "trilha": "ui-slider",
   "achados": {
    "trilha": "Onde ela mexe nos dois sliders — applet vs. cosmic-settings vs. app próprio vs. atalho+OSD",
    "resumo": "O caminho mais barato que ela vai usar de verdade é o applet de Acessibilidade do próprio COSMIC, patchado e instalado por sombra — o mesmo truque que já colocou o Now Playing na barra dela. Medi: o applet upstream tem 393 linhas, compila do zero nesta máquina em 95 s (458 crates) e em 21,7 s incremental; escrevi o patch com os DOIS sliders + \"Modo leitura\" + \"Restaurar padrões\" e ele COMPILA — 108 linhas somadas ao app.rs/lib.rs mais um leitura.rs de 24 linhas. O canal de publicação é cosmic-config (precedente vivo: o applet de tiling escreve chaves de com.system76.CosmicComp e o cosmic-comp as observa por ConfigWatchSource — muda a quente), e NÃO o protocolo Wayland cosmic-a11y-v1, que só carrega ligado/desligado e um enum de quatro filtros. O patch no cosmic-settings tem ponto de inserção exato e conhecido (pages/accessibility/mod.rs, a função vision()), mas nesta máquina ele nem compila: o build morreu em 119 s por falta de libdav1d-dev, e o apt trocou o cosmic-settings 12 vezes em 70 dias — uma esteira de rebuild a cada ~6 dias. O atalho+OSD cai por duas medições: o cosmic-osd não tem API DBus para mostrar um OSD arbitrário (o enum Params é fechado no binário) e o Não Perturbe dela está true, o que engole qualquer notify-send. Um preço honesto do caminho recomendado: o applet de Acessibilidade NÃO está no painel dela hoje, então entra um token no plugins_wings — arquivo da Aurora — feito à mão uma vez, no mesmo logout que o cosmic-comp patchado já vai exigir.",
    "achados": [
     {
      "titulo": "O applet de Acessibilidade existe, é minúsculo, e NÃO está na barra dela",
      "detalhe": "cosmic-applet-a11y/src/app.rs tem 393 linhas e já desenha exatamente os controles da página Acessibilidade (leitor de tela, lupa, inverter cores, alto contraste, \"Filtros de cor\") com um botão \"Configurações\" que abre `cosmic-settings accessibility`. Mas o plugins_wings dela não o lista: topbar tem Time/Workspaces/Notifications (esquerda) e NowPlaying/Power (direita), centro Audio; a dock tem AppButton, StatusArea, Eyedropper, Clipboard, Network, Bluetooth. Ou seja: patchar o applet resolve o CÓDIGO, mas ele só aparece se um token for acrescentado ao plugins_wings — que é da Aurora pela docs/FRONTEIRA.md, e foi editado à mão uma vez em 24/08/2026 com backup.",
      "evidencia": "wc -l scratchpad/capplets/cosmic-applet-a11y/src/app.rs = 393; cat ~/.config/cosmic/com.system76.CosmicPanel.{Panel,Dock}/v1/plugins_{wings,center}",
      "confianca": "medido"
     },
     {
      "titulo": "Compilar um applet nesta máquina custa 95 segundos, não minutos",
      "detalhe": "Clone limpo do pop-os/cosmic-applets, target vazio, `nice -n 19 cargo build --release -p cosmic-applet-a11y`: 458 crates compiladas (libcosmic, iced_*, cosmic-config, cctk incluídos), 95 s de relógio de parede — o cargo reportou \"Finished in 1m 35s\". Depois de editar só o app.rs, o rebuild foi de 21,68 s. Não há sccache/mold/lld na máquina; é só CPU (16 núcleos). Isso derruba a intuição de que \"applet = compilar Rust = caro\": é mais barato que qualquer outra opção desta lista, e um terço do custo do próprio cosmic-comp.",
      "evidencia": "timestamps 1788036784→1788036879 (95 s) e log em scratchpad/build_applet.log; segundo build: \"Finished `release` profile [optimized] target(s) in 21.68s\"",
      "confianca": "medido"
     },
     {
      "titulo": "O patch com os dois sliders tem ~132 linhas e COMPILA",
      "detalhe": "Escrevi e compilei: `git diff --stat` = 108 inserções em app.rs + lib.rs, mais um src/leitura.rs novo de 24 linhas. Inclui toggler \"Modo leitura\", slider de Temperatura (2500..6500 K, step 100), slider de Textura (0..100 %, step 5), botão \"Restaurar padrões\", gravação em cosmic-config e uma subscription que faz o slider SEGUIR o disco (para quando quem mudar for o agendador por horário). Dois tropeços reais no caminho, já corrigidos no código que devolvo: `row()` do iced exige filhos (usar `Row::with_capacity`) e `toggler(..).label()` quer String, não &str.",
      "evidencia": "cd scratchpad/capplets && cargo build --release -p cosmic-applet-a11y → \"Finished `release` profile [optimized] target(s) in 21.68s\"; git diff --stat = 108 insertions",
      "confianca": "medido"
     },
     {
      "titulo": "O canal certo é cosmic-config, e já existe applet que faz isso",
      "detalhe": "O cosmic-applet-tiling abre `Config::new(\"com.system76.CosmicComp\", CosmicCompConfig::VERSION)` e grava com `helper.set(\"autotile\", …)` / `set_<campo>(&helper, v)`; do outro lado, o cosmic-comp registra `ConfigWatchSource` no laço de eventos em Config::load — ou seja, chave nova vale A QUENTE, sem logout, desde que o comp patchado a leia. O derive `CosmicConfigEntry` gera `set_x(&config, v) -> Result<bool>` que só escreve quando o valor muda. E cosmic-config guarda UM ARQUIVO POR CHAVE (o diretório dela tem 15 arquivos, incluindo os .bak da Aurora), então uma chave nossa que o comp de fábrica não conhece é simplesmente ignorada — degradação silenciosa, sem tela quebrada.",
      "evidencia": "capplets/cosmic-applet-tiling/src/window.rs:77,226; cosmic-comp/src/config/mod.rs:~180 (ConfigWatchSource::new + insert_source); libcosmic/cosmic-config-derive/src/lib.rs:159 (setters); ls ~/.config/cosmic/com.system76.CosmicComp/v1/",
      "confianca": "medido"
     },
     {
      "titulo": "O toggle atual NÃO passa por config — passa pelo protocolo Wayland, que é discreto por desenho",
      "detalhe": "Tanto o applet quanto a página de Acessibilidade mandam `AccessibilityRequest::ScreenFilter { inverted, filter }` pelo cosmic-a11y-v1; o comp guarda o resultado sozinho em ~/.local/state/cosmic-comp/a11y_screen_filter.ron (hoje `(inverted: false,)`). O `filter` é um enum de quatro valores (Greyscale/Protanopia/Deuteranopia/Tritanopia, +Unknown). Enfiar um número contínuo por aí exigiria mudar o PROTOCOLO (cosmic-protocols) e os dois lados. Por isso o slider deve escrever config, e não requisitar pelo protocolo: é a diferença entre patchar um binário e patchar um contrato.",
      "evidencia": "capplets/cosmic-applet-a11y/src/app.rs:117-131; csettings/.../accessibility/mod.rs:419-434; cosmic-comp/src/config/mod.rs:151-176; cat ~/.local/state/cosmic-comp/a11y_screen_filter.ron",
      "confianca": "medido"
     },
     {
      "titulo": "O patch no cosmic-settings tem ponto de inserção exato — e não compila nesta máquina hoje",
      "detalhe": "A página é csettings/cosmic-settings/src/pages/accessibility/mod.rs: o enum Message na linha 94, a seção `vision()` na linha 192, e os sliders entrariam logo depois do dropdown \"Tipo de filtro de cor\" (linhas 270-293). A página JÁ carrega `comp_config: CosmicCompConfig` + `comp_helper: cosmic_config::Config` e JÁ grava chave do comp (Message::SetCursorShakeToFind, linha 441) — então o encaixe é de 15 linhas. O custo é o build: `cargo build --release -p cosmic-settings` morreu em 119 s, 213 crates dentro, em `dav1d-sys` por falta de libdav1d-dev (libpulse-dev também não está instalada). Ou seja, antes de escrever a primeira linha já é preciso `apt install` — root, território da Aurora.",
      "evidencia": "scratchpad/build_settings.log:555 \"error: failed to run custom build command for `dav1d-sys v0.8.3`\", rc=101 em 119 s; apt-cache policy libdav1d-dev → Instalado: (nenhum)",
      "confianca": "medido"
     },
     {
      "titulo": "O apt troca o cosmic-settings a cada ~6 dias — a esteira de rebuild é real",
      "detalhe": "No /var/log/apt/history.log*: 13 eventos que trocaram cosmic-settings e/ou cosmic-comp entre 18/06/2026 e 27/08/2026; o cosmic-settings entrou em 12 deles, o cosmic-applets em 10. Média de um binário novo a cada seis dias. Qualquer solução que dependa de substituir /usr/bin/cosmic-settings vira compromisso permanente de recompilar — e um fork do app inteiro de Ajustes envelhece junto (páginas novas, esquema de config novo) de um jeito que um applet de 393 linhas não envelhece.",
      "evidencia": "parse de /var/log/apt/history.log* (13 eventos, o último 2026-08-27 22:39); dpkg-query: cosmic-settings 1.7.0~1787775226~24.04~60bdbb7, cosmic-applets 1.0.15~1787775672~24.04~c7b1cb3",
      "confianca": "medido"
     },
     {
      "titulo": "Um binário-sombra em ~/.local/bin venceria o /usr/bin — a máquina permite, eu ainda assim não recomendo",
      "detalhe": "O PATH vivo da sessão (lido do /proc do cosmic-panel) é ~/.cargo/bin:~/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin — e o lançador do Ajustes tem `Exec=cosmic-settings` sem caminho absoluto. Então um `~/.local/bin/cosmic-settings` nosso venceria o pacote sem sudo e sem ser desfeito por apt. É bom saber que existe (serve de plano B), mas aplicá-lo ao app de Ajustes inteiro congela a superfície de configuração DELA numa foto de hoje; aplicá-lo a um applet de 393 linhas não tem esse tamanho de risco.",
      "evidencia": "tr '\\0' '\\n' < /proc/3186/environ | grep ^PATH; grep ^Exec /usr/share/applications/com.system76.CosmicSettings.desktop",
      "confianca": "medido"
     },
     {
      "titulo": "O OSD do COSMIC não tem porta de entrada — o atalho ficaria cego",
      "detalhe": "Introspecção do serviço vivo com.system76.CosmicOnScreenDisplay: só org.freedesktop.DBus.{Introspectable,Peer,Properties} e org.freedesktop.DbusActivation. Nenhum método \"mostre um OSD\". O nome com.system76.CosmicOsd é do frente polkit. No fonte, `osd_indicator::Params` é um enum fechado (DisplayBrightness, KeyboardBrightness, SinkVolume, AirplaneMode, TouchpadEnabled…) consumido só internamente — acrescentar um item exige recompilar e substituir o cosmic-osd. E o plano B de feedback está morto: o Não Perturbe dela está ligado, que é o mesmo motivo pelo qual 22 avisos do self-heal nunca chegaram nela em 27/08.",
      "evidencia": "busctl --user introspect com.system76.CosmicOnScreenDisplay /com/system76/CosmicOnScreenDisplay; cosd/src/components/osd_indicator.rs:25-35; cat ~/.config/cosmic/com.system76.CosmicNotifications/v1/do_not_disturb → true",
      "confianca": "medido"
     },
     {
      "titulo": "O MeowSystem não tem NENHUM app de janela para reaproveitar",
      "detalhe": "Os quatro .py que importam `gi` (folha_barra, folha_icones, folha_proposta, icones_orfaos) usam Gtk só como biblioteca de consulta de tema de ícone: não há uma única `Gtk.Window`, `Gtk.Application` nem `Adw.`, e não há zenity/yad em script nenhum. Um app GTK/Python seria tecnologia nova no repositório, estranha à folha visual (que é COSMIC/libcosmic), e — o que mais importa para ela — moraria fora de qualquer lugar por onde ela já passa.",
      "evidencia": "grep -rn 'import gi|Gtk.Window|Adw\\.' --include='*.py' no MeowSystem: 4 arquivos, zero janelas; grep zenity/yad/kdialog: nada",
      "confianca": "medido"
     },
     {
      "titulo": "A estrada de instalação do applet já está pavimentada neste repositório",
      "detalhe": "src/applets/now-playing/ guarda receita (PINO com URL+COMMIT+LIBCOSMIC) e patch, nunca a árvore; scripts/midia_build.sh clona no pino, aplica, `cargo build --locked`, instala em ~/.local/bin com NOME PRÓPRIO e grava um carimbo de 4 campos (sha do patch, commit, sha do binário, versão do rustc) que faz a segunda passagem custar zero; scripts/midia.sh planta a sombra .desktop e tem fail-safe (sombra sem binário = dock sem applet). O bin/meow registra `midiabin` em SEM_CONSERTO porque compilar não é do doctor. O applet de leitura entra por cópia dessa forma — não há arquitetura nova a inventar.",
      "evidencia": "/mnt/Apate/Desenvolvimento/MeowSystem/scripts/midia_build.sh (cabeçalho e _carimbo_bate), src/applets/now-playing/{PINO,LEIA.md}, bin/meow:970-983",
      "confianca": "medido"
     },
     {
      "titulo": "O ícone do applet já é do tema dela, e o popup tem 360px — cabem os dois sliders",
      "detalhe": "O ícone `preferences-desktop-accessibility-symbolic` está em assets/icones/sistema.map (linha 36, mapeado para `access` do Arcticons), então o applet nasce com a cara da folha dela, sem trabalho extra. E o popup de applet do libcosmic é travado em 360px lógicos (min = max), medido e documentado no LEIA.md do Now Playing — por isso o layout que escrevi divide cada linha em FillPortion 4/7/3 (rótulo, slider, valor) em vez de largura fixa.",
      "evidencia": "assets/icones/sistema.map:36; src/applets/now-playing/LEIA.md (seção do popup: Limits::NONE.min_width(360.0).max_width(360.0))",
      "confianca": "lido-na-fonte"
     }
    ],
    "codigo": [
     {
      "para_que": "A chave que a barra escreve e o compositor lê. Arquivo NOVO do applet patchado: src/leitura.rs (24 linhas). Namespace nosso, um arquivo por chave — um cosmic-comp sem o patch ignora os três em silêncio.",
      "linguagem": "rust",
      "fonte": "autoral — compilado nesta máquina em 29/08/2026 (cargo build --release -p cosmic-applet-a11y, ok)",
      "trecho": "// leitura.rs — a chave que a barra escreve e o compositor lê.\n// Namespace NOSSO: ~/.config/cosmic/com.meowsystem.Leitura/v1/{temperatura,textura,ativo}\n// Um arquivo por chave (cosmic-config), então um cosmic-comp SEM o patch\n// simplesmente ignora os três — degradação silenciosa, sem tela quebrada.\nuse cosmic::cosmic_config::{self, CosmicConfigEntry, cosmic_config_derive::CosmicConfigEntry};\n\npub const ID: &str = \"com.meowsystem.Leitura\";\n\n#[derive(Clone, Debug, PartialEq, CosmicConfigEntry)]\n#[version = 1]\npub struct Leitura {\n    /// Kelvin. 6500 = branco de fábrica; 2500 = âmbar de Kindle.\n    pub temperatura: u32,\n    /// 0..=100. Quanto de \"papel\" entra (contraste rebaixado + grão).\n    pub textura: u8,\n    /// O mestre. Falso = o shader volta a ser identidade.\n    pub ativo: bool,\n}\n\nimpl Default for Leitura {\n    fn default() -> Self {\n        Self { temperatura: 6500, textura: 0, ativo: false }\n    }\n}"
     },
     {
      "para_que": "O patch inteiro sobre o cosmic-applet-a11y upstream: dois sliders contínuos, o toggle mestre, \"Restaurar padrões\" e a subscription que faz o slider seguir o disco (para o agendamento por horário mexer sem brigar com a UI). 108 linhas somadas. É o `0001-meow-leitura.patch` que entraria em src/applets/a11y/.",
      "linguagem": "diff",
      "fonte": "autoral, sobre pop-os/cosmic-applets — compilou (\"Finished `release` profile [optimized] target(s) in 21.68s\")",
      "trecho": "--- a/cosmic-applet-a11y/src/lib.rs\n+++ b/cosmic-applet-a11y/src/lib.rs\n@@\n mod app;\n+mod leitura;\n mod backend;\n mod localize;\n\n--- a/cosmic-applet-a11y/src/app.rs\n+++ b/cosmic-applet-a11y/src/app.rs\n@@ -16,14 +16,15 @@ use cosmic::{\n     cosmic_config::{self, CosmicConfigEntry},\n     cosmic_theme::{CosmicPalette, Spacing, ThemeBuilder},\n     iced::{\n-        Length, Subscription,\n+        Alignment, Length, Subscription,\n         platform_specific::shell::wayland::commands::popup::{destroy_popup, get_popup},\n         window,\n     },\n     surface,\n     theme::{self, CosmicTheme},\n-    widget::{Column, divider, text, toggler},\n+    widget::{Column, Row, container, divider, slider, text, toggler},\n };\n+use crate::leitura::{self, Leitura};\n@@ struct CosmicA11yApplet {\n     token_tx: Option<channel::Sender<TokenRequest>>,\n     screen_filter_active: bool,\n+    // --- MEOW: leitura (temperatura + textura) ---\n+    leitura: Leitura,\n+    leitura_helper: Option<cosmic_config::Config>,\n }\n@@ enum Message {\n     FilterColorsEnabled(bool),\n+    LeituraAtiva(bool),\n+    Temperatura(u32),\n+    Textura(u8),\n+    Restaurar,\n+    LeituraDeFora(Box<Leitura>),\n@@ fn init(core: cosmic::app::Core, _flags: Self::Flags) -> (Self, app::Task<Self::Message>) {\n+        let leitura_helper = cosmic_config::Config::new(leitura::ID, Leitura::VERSION).ok();\n+        let leitura = leitura_helper\n+            .as_ref()\n+            .map(|h| Leitura::get_entry(h).unwrap_or_else(|(_errs, c)| c))\n+            .unwrap_or_default();\n         (\n             Self {\n                 core,\n                 token_tx: None,\n+                leitura,\n+                leitura_helper,\n                 ..Default::default()\n             },\n             Task::none(),\n         )\n@@ fn update(&mut self, message: Self::Message) -> app::Task<Self::Message> {\n+            // --- MEOW ---------------------------------------------------\n+            // Gravar é a ÚNICA saída: quem pinta é o cosmic-comp, que observa\n+            // este namespace. O applet nunca fala com o shader.\n+            Message::LeituraAtiva(v) => {\n+                if let Some(h) = self.leitura_helper.as_ref() {\n+                    if let Err(err) = self.leitura.set_ativo(h, v) {\n+                        tracing::error!(?err, \"não gravei `ativo`\");\n+                    }\n+                }\n+            }\n+            Message::Temperatura(k) => {\n+                // `step(100)` no widget: no pior caso 40 escritas de ponta a\n+                // ponta do curso, não uma por pixel arrastado.\n+                if let Some(h) = self.leitura_helper.as_ref() {\n+                    if let Err(err) = self.leitura.set_temperatura(h, k) {\n+                        tracing::error!(?err, \"não gravei `temperatura`\");\n+                    }\n+                }\n+            }\n+            Message::Textura(t) => {\n+                if let Some(h) = self.leitura_helper.as_ref() {\n+                    if let Err(err) = self.leitura.set_textura(h, t) {\n+                        tracing::error!(?err, \"não gravei `textura`\");\n+                    }\n+                }\n+            }\n+            Message::Restaurar => {\n+                if let Some(h) = self.leitura_helper.as_ref() {\n+                    let padrao = Leitura::default();\n+                    let _ = self.leitura.set_temperatura(h, padrao.temperatura);\n+                    let _ = self.leitura.set_textura(h, padrao.textura);\n+                    let _ = self.leitura.set_ativo(h, padrao.ativo);\n+                }\n+            }\n+            // Quem mudou pode ter sido o agendador (systemd timer) ou a página\n+            // de Acessibilidade: o slider segue o disco, não o contrário.\n+            Message::LeituraDeFora(nova) => {\n+                self.leitura = *nova;\n+            }\n             Message::TogglePopup => {\n@@ fn view_window(&self, _id: window::Id) -> Element<'_, Message> {\n+        // --- MEOW: os dois sliders + restaurar ---------------------------\n+        let leitura_toggle = padded_control(\n+            toggler(self.leitura.ativo)\n+                .on_toggle(Message::LeituraAtiva)\n+                .label(\"Modo leitura\".to_string())\n+                .text_size(14)\n+                .width(Length::Fill),\n+        );\n+        let temp_slider = padded_control(\n+            Row::with_capacity(3)\n+                .spacing(space_xxs)\n+                .align_y(Alignment::Center)\n+                .push(text::body(\"Temperatura\").width(Length::FillPortion(4)))\n+                .push(\n+                    slider(2500..=6500, self.leitura.temperatura, Message::Temperatura)\n+                        .step(100u32)\n+                        .width(Length::FillPortion(7)),\n+                )\n+                .push(\n+                    container(text::body(format!(\"{}K\", self.leitura.temperatura)))\n+                        .width(Length::FillPortion(3))\n+                        .align_x(Alignment::End),\n+                ),\n+        );\n+        let textura_slider = padded_control(\n+            Row::with_capacity(3)\n+                .spacing(space_xxs)\n+                .align_y(Alignment::Center)\n+                .push(text::body(\"Textura\").width(Length::FillPortion(4)))\n+                .push(\n+                    slider(0..=100, self.leitura.textura, Message::Textura)\n+                        .step(5u8)\n+                        .width(Length::FillPortion(7)),\n+                )\n+                .push(\n+                    container(text::body(format!(\"{}%\", self.leitura.textura)))\n+                        .width(Length::FillPortion(3))\n+                        .align_x(Alignment::End),\n+                ),\n+        );\n-        let content_list = Column::with_capacity(5)\n+        let content_list = Column::with_capacity(9)\n             .push(reader_toggle)\n@@\n             .push(hc_colors_toggle)\n             .push(padded_control(divider::horizontal::default()).padding([space_xxs, space_s]))\n+            .push(leitura_toggle)\n+            .push(temp_slider)\n+            .push(textura_slider)\n+            .push(menu_button(text::body(\"Restaurar padrões\")).on_press(Message::Restaurar))\n+            .push(padded_control(divider::horizontal::default()).padding([space_xxs, space_s]))\n             .push(menu_button(text::body(fl!(\"settings\"))).on_press(Message::OpenSettings))\n             .padding([8, 0]);\n@@ fn subscription(&self) -> Subscription<Message> {\n             activation_token_subscription(0).map(Message::Token),\n+            self.core\n+                .watch_config::<Leitura>(leitura::ID)\n+                .map(|u| Message::LeituraDeFora(Box::new(u.config))),\n         ])"
     },
     {
      "para_que": "A sombra que faz o painel usar o NOSSO binário sem tocar em plugins_wings: mesmo basename do .desktop de fábrica, em ~/.local/share/applications (que vence /usr/share por XDG). É o mesmo mecanismo do applet de mídia.",
      "linguagem": "ini",
      "fonte": "autoral, moldado no /usr/share/applications/com.system76.CosmicAppletA11y.desktop e na sombra viva ~/.local/share/applications/com.github.DiegoMMR.CosmicExtAppletNowPlaying.desktop",
      "trecho": "# ~/.local/share/applications/com.system76.CosmicAppletA11y.desktop\n[Desktop Entry]\nName=Acessibilidade\nComment=Filtros de cor, temperatura e textura de leitura\nType=Application\nExec=/home/vitoriamaria/.local/bin/meow-applet-a11y\nTerminal=false\nCategories=COSMIC;\nIcon=preferences-desktop-accessibility-symbolic\nStartupNotify=true\nNoDisplay=true\nX-CosmicApplet=true\nX-CosmicShrinkable=true\nX-CosmicHoverPopup=Auto\nX-OverflowPriority=10\n\n# O binário tem NOME PRÓPRIO (meow-applet-a11y, não cosmic-applet-a11y) pelo\n# mesmo motivo medido em 24/08: com o mesmo nome, nenhum pgrep distingue o nosso\n# do de fábrica, e a conferência do doctor daria garantia falsa.\n# FAIL-SAFE obrigatório (copiar do scripts/midia.sh): sombra presente + binário\n# ausente = NENHUM applet no slot. O --aplicar remove a sombra quando o binário\n# some; o --reverter apaga a sombra ANTES do binário."
     },
     {
      "para_que": "A receita (PINO) e o esqueleto do build, espelhando scripts/midia_build.sh. O commit do pino é o do PACOTE instalado, para o applet não nascer adiantado em relação ao resto do COSMIC dela.",
      "linguagem": "bash",
      "fonte": "autoral, decalcado de /mnt/Apate/Desenvolvimento/MeowSystem/scripts/midia_build.sh e src/applets/now-playing/PINO",
      "trecho": "# src/applets/a11y/PINO\nURL=\"https://github.com/pop-os/cosmic-applets.git\"\n# c7b1cb3 = o commit que a versão instalada carrega no fim do número:\n#   dpkg-query -W cosmic-applets -> 1.0.15~1787775672~24.04~c7b1cb3\nCOMMIT=\"c7b1cb3\"\n\n# scripts/leitura_build.sh (esqueleto — o resto é cópia fiel do midia_build.sh:\n# carimbo de 4 campos, MEOW_ESTADO, MEOW_SEM_DEPENDENCIA quando falta cargo)\nESTADO=\"$HOME/.local/state/meowsystem/leitura\"\nARVORE=\"$ESTADO/src\"\nBINARIO=\"$HOME/.local/bin/meow-applet-a11y\"\n\n[ -d \"$ARVORE/.git\" ] || git clone -q --depth 50 \"$URL\" \"$ARVORE\"\ngit -C \"$ARVORE\" checkout -q \"$COMMIT\" 2>/dev/null || {\n  git -C \"$ARVORE\" fetch -q --unshallow || git -C \"$ARVORE\" fetch -q --all\n  git -C \"$ARVORE\" checkout -q \"$COMMIT\"\n}\ngit -C \"$ARVORE\" reset -q --hard \"$COMMIT\"      # sem isto, o 2o build patcha o patchado\ngit -C \"$ARVORE\" apply \"$RAIZ/src/applets/a11y/0001-meow-leitura.patch\"\n\n# --locked SEMPRE: o Cargo.toml aponta o libcosmic para o git SEM rev.\ncargo build --release --locked --manifest-path \"$ARVORE/Cargo.toml\" \\\n            -p cosmic-applet-a11y --target-dir \"$ESTADO/target\"\ninstall -m755 \"$ESTADO/target/release/cosmic-applet-a11y\" \"$BINARIO\"\n\n# MEDIDO em 29/08/2026 nesta máquina: 95 s a frio (458 crates), 21,7 s incremental.\n# bin/meow: `leiturabin` entra em SEM_CONSERTO (compilar não é do doctor);\n# `leitura` (sombra + chaves + fail-safe) fica FORA, porque o doctor conserta."
     },
     {
      "para_que": "A ÚNICA edição que a máquina exige fora do MeowSystem: pôr o applet na topbar. Arquivo da Aurora pela docs/FRONTEIRA.md — nenhum script nosso escreve isto, é mão humana, uma vez, com backup, como em 24/08/2026.",
      "linguagem": "ron",
      "fonte": "autoral sobre ~/.config/cosmic/com.system76.CosmicPanel.Panel/v1/plugins_wings (estado atual lido hoje)",
      "trecho": "// ~/.config/cosmic/com.system76.CosmicPanel.Panel/v1/plugins_wings\n// backup antes: cp -a <arquivo> ~/.local/state/meowsystem/backups/painel-<data>/\nSome(([\n    \"com.system76.CosmicAppletTime\",\n    \"com.system76.CosmicAppletWorkspaces\",\n    \"com.system76.CosmicAppletNotifications\",\n], [\n    \"com.github.DiegoMMR.CosmicExtAppletNowPlaying\",\n    \"com.system76.CosmicAppletA11y\",     // <-- a única linha nova\n    \"com.system76.CosmicAppletPower\",\n]))\n// Vale no PRÓXIMO login (não reiniciar o cosmic-panel: a issue #13 do upstream\n// derruba topbar e dock juntas, e o supervisor tem backoff 2^N sem teto).\n// Como o cosmic-comp patchado já exige um logout, este custo é zero na prática."
     },
     {
      "para_que": "SE ela quiser os mesmos dois sliders TAMBÉM dentro de Ajustes > Acessibilidade (segunda superfície, depois). Ponto de inserção exato e o braço do update — a página já grava chave de config do compositor, então o encaixe é pequeno. NÃO compilado aqui: o build do cosmic-settings morre por falta de libdav1d-dev.",
      "linguagem": "rust",
      "fonte": "autoral sobre pop-os/cosmic-settings — cosmic-settings/src/pages/accessibility/mod.rs (vision() na linha 192; o dropdown de filtro termina na 293; SetCursorShakeToFind na 441)",
      "trecho": "// 1) em `pub enum Message` (perto da linha 94):\n    Temperatura(u32),\n    Textura(u8),\n\n// 2) em `vision()`, LOGO DEPOIS do `.add(... color_filter_type ... .control(dropdown))`:\n    .add(\n        settings::item::builder(&descriptions[temperatura]).control(\n            cosmic::widget::slider(2500..=6500, page.leitura.temperatura, |k| {\n                crate::pages::Message::from(Message::Temperatura(k))\n            })\n            .step(100u32),\n        ),\n    )\n    .add(\n        settings::item::builder(&descriptions[textura]).control(\n            cosmic::widget::slider(0..=100, page.leitura.textura, |t| {\n                crate::pages::Message::from(Message::Textura(t))\n            })\n            .step(5u8),\n        ),\n    )\n\n// 3) em `update()`, ao lado de Message::SetCursorShakeToFind (linha 441) — mesmo padrão:\n    Message::Temperatura(k) => {\n        if let Err(err) = self.leitura.set_temperatura(&self.leitura_helper, k) {\n            tracing::error!(?err, \"failed to set temperatura\");\n        }\n    }\n    Message::Textura(t) => {\n        if let Err(err) = self.leitura.set_textura(&self.leitura_helper, t) {\n            tracing::error!(?err, \"failed to set textura\");\n        }\n    }\n// A struct Page ganha os mesmos dois campos do applet (leitura + leitura_helper),\n// construídos no Default como o comp_helper já é (linha ~46)."
     }
    ],
    "solucoes": [
     {
      "nome": "RECOMENDADA — os dois sliders dentro do applet de Acessibilidade do COSMIC, instalado por sombra",
      "alvo": "ui",
      "como": "1) src/applets/a11y/ recebe PINO (URL + commit c7b1cb3, o da versão instalada) e 0001-meow-leitura.patch — o diff que devolvo acima, já compilado. 2) scripts/leitura_build.sh (cópia do midia_build.sh: carimbo de 4 campos, --locked, MEOW_COMPILAR obrigatório) clona, patcha, compila em 95 s e instala ~/.local/bin/meow-applet-a11y. 3) scripts/leitura.sh planta a sombra ~/.local/share/applications/com.system76.CosmicAppletA11y.desktop apontando para o nosso binário, com o MESMO fail-safe do midia.sh (binário sumiu → remove a sombra, senão a barra fica com um buraco). 4) UMA linha à mão no plugins_wings da topbar, com backup — arquivo da Aurora, feito por ela/por mão humana, e que só vale no próximo login. 5) O applet grava com.meowsystem.Leitura/v1/{temperatura,textura,ativo}; quem pinta é o cosmic-comp patchado, que já tem ConfigWatchSource e passa a observar também esse namespace (5 linhas no lado do comp — trilha da Aurora).",
      "onde": "MeowSystem: src/applets/a11y/{PINO,0001-meow-leitura.patch}, scripts/leitura_build.sh, scripts/leitura.sh, bin/meow (leiturabin em SEM_CONSERTO). Fora do repo: ~/.local/bin/meow-applet-a11y, ~/.local/share/applications/com.system76.CosmicAppletA11y.desktop, e a linha no plugins_wings.",
      "a_quente": true,
      "custo": "~132 linhas de Rust (108 no app.rs/lib.rs + 24 de leitura.rs) — escritas e COMPILANDO. Build: 95 s a frio, 21,7 s incremental. Zero pacotes novos de sistema, zero sudo. O único custo humano é a linha no plugins_wings, e ela só aparece no próximo login — logout que o cosmic-comp patchado já cobra de qualquer jeito.",
      "durabilidade": "Sobrevive a apt upgrade sem fazer nada: o pacote troca /usr/bin/cosmic-applet-a11y, que a sombra não usa (foi assim que o Now Playing atravessou 24/08 → hoje). Sobrevive a reboot (a sombra é arquivo). Sobrevive a troca de tema (o ícone vem do assets/icones/sistema.map, que é nosso). Envelhece só se o upstream mexer nas 393 linhas do applet — e o carimbo do build acusa a divergência sem compilar nada. Se o binário sumir, o fail-safe tira a sombra e ela volta ao applet de fábrica."
     },
     {
      "nome": "Variante — applet NOVO, só do Meow (\"Leitura\"), em vez de patchar o de Acessibilidade",
      "alvo": "ui",
      "como": "Mesmo esqueleto de código (os sliders e o leitura.rs são idênticos), mas num crate próprio com APP_ID com.meowsystem.AppletLeitura e ícone próprio. Precisa de main.rs + Cargo.toml + o .desktop, e do MESMO token novo no plugins_wings.",
      "onde": "src/applets/leitura/ (crate autoral, sem upstream para pinar), ~/.local/bin/meow-applet-leitura.",
      "a_quente": true,
      "custo": "Mais caro que a recomendada, não mais barato: some o patch de 108 linhas mas entra um crate inteiro (Cargo.toml, main.rs, localize, ícone próprio a desenhar) — e o custo de entrada no painel é o mesmo token. Build na mesma faixa dos 95 s.",
      "durabilidade": "Melhor num ponto: não tem upstream para divergir. Pior em dois: mais um ícone na barra (a de Acessibilidade sumiria do radar dela) e nada compartilha estado com a página de Acessibilidade que ela já visita."
     },
     {
      "nome": "Patch na página Acessibilidade do cosmic-settings",
      "alvo": "ui",
      "como": "Acrescentar Message::{Temperatura,Textura} + dois `settings::item::builder(..).control(slider(..))` logo depois do dropdown de tipo de filtro, e gravar pelo helper de config que a página JÁ carrega (mesmo padrão do SetCursorShakeToFind). Depois, ou substituir /usr/bin/cosmic-settings (root + hook de apt) ou plantar ~/.local/bin/cosmic-settings, que vence pelo PATH da sessão dela.",
      "onde": "csettings/cosmic-settings/src/pages/accessibility/mod.rs — enum Message linha 94, seção vision() linha 192, inserção após a linha 293, braço de update junto da linha 441.",
      "a_quente": true,
      "custo": "ALTO nesta máquina, e medido: o build morre em 119 s por falta de libdav1d-dev (libpulse-dev também ausente) — ou seja, `apt install` de dependências de compilação antes da primeira linha, o que é root e território da Aurora. O crate é o maior dos três (910 pacotes no Cargo.lock contra 786 dos applets).",
      "durabilidade": "A pior das quatro: o apt trocou o cosmic-settings 12 vezes em 70 dias (~1 a cada 6 dias). Substituir o binário do pacote significa recompilar nesse ritmo; a sombra em ~/.local/bin evita o apt mas congela o app de Ajustes DELA inteiro numa foto de hoje. Vale como SEGUNDA superfície, depois que os sliders existirem e ela disser que quer também na página — nunca como a primeira."
     },
     {
      "nome": "App GTK/Python próprio do MeowSystem",
      "alvo": "ui",
      "como": "Uma janelinha com dois Gtk.Scale gravando as mesmas chaves de cosmic-config (o formato é RON de uma linha por chave; dá para escrever de Python sem biblioteca).",
      "onde": "Não existe lugar natural: o repositório não tem nenhum app de janela hoje.",
      "a_quente": true,
      "custo": "Baixo em linhas (~80 de Python) e é a única opção que não compila nada.",
      "durabilidade": "Sobrevive a tudo tecnicamente, e é a que ela menos vai usar: mora fora da UI do sistema, exige lembrar de abrir \"Meow Leitura\" pelo lançador, tem cara de GTK dentro de um desktop libcosmic (contra a folha visual dela) e traz PyGObject como dependência nova do projeto. Baratíssimo de escrever e caro de valer a pena — o oposto do que a economia deste caso pede."
     },
     {
      "nome": "Atalho de teclado + OSD",
      "alvo": "ui",
      "como": "Super+setas disparando Spawn(\"/usr/local/bin/...\") que soma/subtrai na chave. Para ter feedback na tela, seria preciso acrescentar uma variante ao enum osd_indicator::Params e um método DBus no cosmic-osd, recompilar e substituir o binário.",
      "onde": "~/.config/cosmic/com.system76.CosmicSettings.Shortcuts/v1/custom (Spawn é a única ação custom que existe) + fork do pop-os/cosmic-osd.",
      "a_quente": true,
      "custo": "ATENÇÃO — os atalhos são território da AURORA pela docs/FRONTEIRA.md (\"é o colar dela\"), e a TRAVA 1 do lib/comum.sh recusa escrita ali por código: o MeowSystem não pode fazer isto, só pedir. Some a isso o fork do cosmic-osd (mais um binário na esteira do apt).",
      "durabilidade": "Frágil e cega: sem OSD ela não vê o valor absoluto (é justamente o que o print do MIUI mostra), e o plano B de aviso está morto — o Não Perturbe dela está ligado e engole notify-send. Serve, no máximo, como TEMPERO depois: dois atalhos que empurram a mesma chave que o applet mostra."
     }
    ],
    "derrubado": [
     "\"O applet de Acessibilidade já está na barra dela, é só patchar\" — não está. O plugins_wings da topbar tem Time/Workspaces/Notifications + NowPlaying/Power, e a dock tem outros seis; CosmicAppletA11y não aparece em nenhum dos quatro arquivos. Sem o token, a sombra não tem slot para vencer.",
     "\"Um applet novo entra sozinho pela sombra .desktop\" — não entra. A sombra só vence quando o basename JÁ está listado no plugins_wings: o painel casa por basename e consome o slot no primeiro acerto. Foi por isso que o Now Playing entrou de graça (o flatpak já estava listado) e é por isso que este caso exige uma linha nova.",
     "\"Compilar Rust é o caminho caro\" — medido ao contrário: applet inteiro do zero em 95 s (458 crates, 16 núcleos, sem sccache/mold), 21,7 s para recompilar depois de editar. O caro é o cosmic-settings, que nem terminou.",
     "\"Dá para pendurar os sliders no cosmic-settings sem custo de sistema\" — não nesta máquina: `cargo build -p cosmic-settings` morreu em 119 s no dav1d-sys por falta de libdav1d-dev (libpulse-dev também não está instalada). Antes do primeiro slider, entra apt e root.",
     "\"O cosmic-osd pode ser reaproveitado sem recompilar\" — não pode. A introspecção do serviço vivo com.system76.CosmicOnScreenDisplay devolve só DbusActivation/Peer/Properties/Introspectable: nenhum método para mostrar um OSD arbitrário. O enum osd_indicator::Params é fechado dentro do binário.",
     "\"Se faltar OSD, um notify-send resolve\" — não resolve: do_not_disturb está `true` no config de notificações dela, o mesmo motivo pelo qual 22 avisos do self-heal nunca chegaram até ela em 27/08/2026.",
     "\"O MeowSystem já tem um app de janela cujo estilo dá para reaproveitar\" — não tem. Os quatro .py que importam `gi` usam Gtk só para consultar tema de ícone; não há Gtk.Window, Gtk.Application, Adw nem zenity/yad em lugar nenhum do repositório.",
     "\"O toggle atual de Filtros de cor grava uma chave que a gente pode reaproveitar\" — não grava. Applet e página mandam ScreenFilter pelo protocolo Wayland cosmic-a11y-v1; quem escreve o estado é o próprio compositor, em ~/.local/state/cosmic-comp/a11y_screen_filter.ron (hoje `(inverted: false,)`). Escrever nesse .ron por fora seria escrever no estado de outro processo — o caminho certo é chave de cosmic-config, que o comp observa por inotify."
    ]
   },
   "verificadas": [
    {
     "nome": "RECOMENDADA — os dois sliders dentro do applet de Acessibilidade do COSMIC, instalado por sombra",
     "alvo": "ui",
     "como": "1) src/applets/a11y/ recebe PINO (URL + commit c7b1cb3, o da versão instalada) e 0001-meow-leitura.patch — o diff que devolvo acima, já compilado. 2) scripts/leitura_build.sh (cópia do midia_build.sh: carimbo de 4 campos, --locked, MEOW_COMPILAR obrigatório) clona, patcha, compila em 95 s e instala ~/.local/bin/meow-applet-a11y. 3) scripts/leitura.sh planta a sombra ~/.local/share/applications/com.system76.CosmicAppletA11y.desktop apontando para o nosso binário, com o MESMO fail-safe do midia.sh (binário sumiu → remove a sombra, senão a barra fica com um buraco). 4) UMA linha à mão no plugins_wings da topbar, com backup — arquivo da Aurora, feito por ela/por mão humana, e que só vale no próximo login. 5) O applet grava com.meowsystem.Leitura/v1/{temperatura,textura,ativo}; quem pinta é o cosmic-comp patchado, que já tem ConfigWatchSource e passa a observar também esse namespace (5 linhas no lado do comp — trilha da Aurora).",
     "onde": "MeowSystem: src/applets/a11y/{PINO,0001-meow-leitura.patch}, scripts/leitura_build.sh, scripts/leitura.sh, bin/meow (leiturabin em SEM_CONSERTO). Fora do repo: ~/.local/bin/meow-applet-a11y, ~/.local/share/applications/com.system76.CosmicAppletA11y.desktop, e a linha no plugins_wings.",
     "a_quente": true,
     "custo": "~132 linhas de Rust (108 no app.rs/lib.rs + 24 de leitura.rs) — escritas e COMPILANDO. Build: 95 s a frio, 21,7 s incremental. Zero pacotes novos de sistema, zero sudo. O único custo humano é a linha no plugins_wings, e ela só aparece no próximo login — logout que o cosmic-comp patchado já cobra de qualquer jeito.",
     "durabilidade": "Sobrevive a apt upgrade sem fazer nada: o pacote troca /usr/bin/cosmic-applet-a11y, que a sombra não usa (foi assim que o Now Playing atravessou 24/08 → hoje). Sobrevive a reboot (a sombra é arquivo). Sobrevive a troca de tema (o ícone vem do assets/icones/sistema.map, que é nosso). Envelhece só se o upstream mexer nas 393 linhas do applet — e o carimbo do build acusa a divergência sem compilar nada. Se o binário sumir, o fail-safe tira a sombra e ela volta ao applet de fábrica.",
     "trilha": "ui-slider",
     "veredito": {
      "solucao": "Trilha \"ui-slider\" — RECOMENDADA: os dois sliders (temperatura + textura) dentro do applet de Acessibilidade do COSMIC, compilado a partir do pino c7b1cb3 e instalado por sombra (~/.local/share/applications/com.system76.CosmicAppletA11y.desktop → ~/.local/bin/meow-applet-a11y), gravando com.meowsystem.Leitura/v1/{temperatura,textura,ativo}, com \"5 linhas no lado do comp\" para ler esse namespace.",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "O QUE CONFERE (medido, não re-litigado):\n- Pino exato. `dpkg -l` dá cosmic-applets 1.0.15~1787775672~24.04~c7b1cb3 — o commit c7b1cb3 do PINO é o da versão instalada. CONFIRMADO.\n- /usr/bin/cosmic-applet-a11y existe (symlink → cosmic-applets, multicall) e /usr/share/applications/com.system76.CosmicAppletA11y.desktop tem X-CosmicApplet=true. A sombra por basename é mecanismo provado: ~/.local/bin/meow-applet-now-playing (22 MB, 25/08) está vivo.\n- O applet A11y NÃO está hoje em plugins_wings nem plugins_center de nenhuma das duas barras. Então a linha à mão é mesmo necessária, e não pode apagar applet nenhum (diferente do risco da Status Area na dock).\n- cargo 1.98.0, rustc 1.98.0, 16 cores, `git ls-remote` no pop-os/cosmic-applets responde, 342 G livres em /home. Nada bloqueia.\n- O comp TEM cosmic_config ConfigWatchSource e JÁ observa um segundo namespace (com.system76.CosmicTk, src/config/mod.rs:195-230). O padrão existe.\n- Os uniforms SÃO passados por desenho, não na subida. Então um valor contínuo PODE ser quente — depois que o binário souber dele.\n\nO QUE DERRUBA (li o fonte exato da versão instalada, que está em disco em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/):\n\n1) O DIFF DO SHADER NÃO EXISTE NA PROPOSTA, E ELE É A PROPOSTA INTEIRA. offscreen.frag tem quatro uniforms: alpha, tint, invert, color_mode. `invert` é 0/1 e `color_mode` é 1..4 discreto. Não há onde enfiar temperatura contínua nem textura. A proposta orça 132 linhas de Rust no applet e chama o resto de \"5 linhas no lado do comp\" — mas o applet só GRAVA chave; quem pinta é o comp, e ninguém escreveu essa parte.\n\n2) O `is_noop()` ENGOLE A COISA EM SILÊNCIO. src/config/mod.rs:159 → `!self.inverted && self.color_filter.is_none()`. E src/backend/render/mod.rs:1284 e src/backend/kms/surface/mod.rs:1101/1105 só entram na passada de postprocess quando `!is_noop()`. Slider de temperatura sem filtro de daltonismo escolhido ⇒ is_noop() continua true ⇒ o shader NUNCA RODA. Ela arrasta o slider, a chave muda, a tela não. Falha muda, sem erro em log nenhum.\n\n3) A PROPOSTA MIRARIA O CALL SITE QUE NÃO RODA NA MÁQUINA DELA. São TRÊS lugares que passam os uniforms, não um: src/backend/render/mod.rs:1365 (winit/x11) e src/backend/kms/surface/mod.rs:1897 (cursor) e :1931 (tela). Ela roda KMS/DRM em hardware real — os que valem são os dois do kms/surface. Um grep por \"color_mode\" acha o de render/mod.rs primeiro. Patchar só ele = compila, instala, e não muda um pixel na TV dela.\n\n4) A DURABILIDADE ANUNCIADA É SÓ DA METADE QUE NÃO PINTA. \"Sobrevive a apt upgrade sem fazer nada\" vale para o applet (a sombra ignora /usr/bin/cosmic-applet-a11y). Não vale para o comp: /var/lib/aurora mostra SEIS versões de cosmic-comp entre 13/07 e 26/08 — quase semanal. E `aurora-cosmic-comp-ws.sh` aplica UM patch só (linhas 385-414, `$PATCH_INSTALADO` = cosmic-comp-sem-workspace-vazio.patch). Ele não conhece patch de leitura. Depois de todo upgrade: workspace volta, night light volta (etapa separada), leitura some calada — sliders na barra, chaves gravando, nada pintando. Toda semana, invisível.\n\n5) PISA NA FRONTEIRA, E JUSTO NA METADE QUE IMPORTA. docs/FRONTEIRA.md: \"binário cosmic-comp (patches de workspace e night light) → Aurora\". Ensinar o comp a ler com.meowsystem.Leitura exige mexer em aurora-cosmic-comp-ws.sh e num patch em /usr/local/share/aurora — ~/.config/zsh (auto-commit de 10 em 10 min) ou território root do Aurora, e a TRAVA 1 do lib/comum.sh recusa ~/.config/zsh de propósito. O MeowSystem não pode entregar a metade que dá sentido aos sliders. A \"trilha da Aurora\" da proposta é exatamente a parte sem dono e sem script.\n\n6) \"VALE A QUENTE: true\" É MEIA VERDADE. Com um comp que carregue os uniforms, arrastar o slider É quente — o toggle de hoje prova o caminho. Mas chegar lá custa: recompilar o cosmic-comp (~4 min), instalar com sudo, RE-APLICAR o patch de binário do night light (aurora-night-light.py reescreve o vec3 preservando tamanho — build novo invalida os offsets), e LOGOUT. E toda mudança futura na matemática do shader é outro rebuild + outro logout. \"Zero sudo\" só vale para o applet.\n\n7) OS NÚMEROS DE CUSTO NÃO SE SUSTENTAM. Não existe src/applets/a11y/ no disco — sem PINO, sem patch. O \"escritas e COMPILANDO, 95 s a frio\" não tem artefato nenhum aqui para conferir, e o único ponto de comparação medido nesta máquina é ~/.local/state/meowsystem/midia/target = 1,9 GB para UM applet libcosmic. cosmic-applets a frio com libcosmic em 95 s não é crível; orce minutos e mais ~2-3 GB.\n\n8) UM CUSTO DE DESEMPENHO QUE NINGUÉM PRECIFICOU. Modo leitura ligado a noite inteira força a passada de postprocess: render offscreen de tela cheia + segunda passada todo frame, e o CURSOR passa pelo shader também (kms/surface/mod.rs:1871-1900, postprocess_state.cursor_texture) em vez do plano de cursor do hardware. Numa máquina onde o applet conta-gotas já queima 43% de CPU, isso precisa de medição antes de virar padrão.",
      "risco_real": "O lado do applet é inofensivo: o A11y não está na barra hoje, e o fail-safe do midia.sh (binário sumiu → tira a sombra) cobre o buraco.\n\nO lado do comp é grau eliminatório. `init_shaders` devolve Err quando o GLSL não compila e o erro sobe com `?` em src/backend/kms/surface/mod.rs:733 e src/backend/kms/device.rs:1119. E /usr/bin/cosmic-greeter-start é literalmente `exec cosmic-comp cosmic-greeter`. Ou seja: um cosmic-comp quebrado tira o desktop DELA E A TELA DE LOGIN JUNTO. O greetd segura a vt1.\n\nTEM VOLTA, e ela está verificada: TTY (Ctrl+Alt+F2..F6) → `sudo aurora-cosmic-comp-ws.sh --restore` (cmd_restore, linhas 529-540, copia /var/lib/aurora/cosmic-comp-<versao>.pkg-orig de volta). O .pkg-orig da versão instalada EXISTE: cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig, 28.339.864 bytes, de 27/08. Rede de segunda linha: `sudo apt-get install --reinstall cosmic-comp`.\n\nCondição: esses dois comandos têm de estar IMPRESSOS EM PAPEL antes da primeira instalação. No minuto em que forem necessários ela não tem tela para lê-los.\n\nRisco menor, mas real: o greeter roda cosmic-comp como o usuário `cosmic-greeter`, que não tem ~/.config/cosmic/com.meowsystem.Leitura. O código novo precisa tolerar o namespace ausente sem pânico, ou a tela de login morre por um arquivo que nunca existiu.",
      "correcao": "O que a proposta teria de virar para deixar de ser refutada. Mapa de patch medido, não estimado:\n\nLADO DO COMP (é aqui que mora a funcionalidade — ~80-120 linhas em 4 arquivos, não 5 linhas em 1):\n- src/backend/render/shaders/offscreen.frag: dois uniforms novos. `uniform vec3 warmth;` e `uniform float paper;`. Tudo cabe em GLSL ES 1.00 — vec3, mix(), fract(), sin(), dot() e gl_FragCoord existem em #version 100. Nada de texelFetch, nada de matriz inversa, nada de laço de contagem variável. Grão de papel estável: `float g = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898,78.233))) * 43758.5453);` e então `color.rgb = mix(color.rgb, vec3(dot(color.rgb, vec3(0.299,0.587,0.114))), paper * 0.5) + (g - 0.5) * paper * 0.03;`. Aplicar DEPOIS do invert e ANTES do `color.rgb *= color.a`.\n- src/backend/render/mod.rs:422-423: acrescentar `UniformName::new(\"warmth\", UniformType::_3f)` e `UniformName::new(\"paper\", UniformType::_1f)` dentro do `compile_custom_texture_shader(POSTPROCESS_SHADER, ...)`. Uniform declarado no shader e não registrado aqui = GlesError::UnknownUniform (\"Uniform ... was not declared when compiling the provided shader\").\n- src/config/mod.rs:152-161: dois campos novos em `ScreenFilter` (com `#[serde(default)]`, porque a struct serializa para ~/.config/cosmic-comp/a11y_screen_filter.ron e hoje esse arquivo nem existe) E — a parte que a proposta esquece — `is_noop()` tem de passar a considerá-los, senão a passada nunca é executada.\n- OS TRÊS call sites, não um: src/backend/render/mod.rs:1365, src/backend/kms/surface/mod.rs:1897 e :1931. Na máquina dela os que valem são os dois do kms/surface. Uniform declarado mas não passado num call site = valor velho do programa GL, ou seja, temperatura que gruda.\n- Transporte: um segundo ConfigWatchSource copiando o padrão do com.system76.CosmicTk (src/config/mod.rs:195-230), cujo callback chama `self.backend.update_screen_filter(&updated)` — que já é o caminho quente, o mesmo que o handler de a11y usa em src/wayland/handlers/a11y.rs:41-63. Tolerar namespace ausente (o greeter roda como outro usuário).\n\nLADO DA AURORA (sem isto o resto apodrece em uma semana):\n- aurora-cosmic-comp-ws.sh precisa deixar de aplicar UM patch e passar a aplicar uma LISTA (hoje: linhas 385-414, `$PATCH_INSTALADO` único). Sem isso, todo `apt upgrade` do cosmic-comp — seis em seis semanas, medido em /var/lib/aurora — reconstrói só o patch de workspace e a leitura some calada.\n- A ordem no self-heal tem de ficar: build (workspace + leitura) → aurora-night-light.py re-patcha o vec3 no binário novo → --ensure instala. O patch de binário do night light NÃO sobrevive a um build novo.\n\nLADO DO MEOW (o que de fato é nosso, e é a parte barata):\n- src/applets/a11y/{PINO,0001-meow-leitura.patch}, scripts/leitura_build.sh (cópia fiel do midia_build.sh: carimbo de 4 campos, --locked, MEOW_COMPILAR obrigatório, `meow_seco` antes de encostar no rustup), scripts/leitura.sh com o fail-safe da sombra, `leiturabin` em SEM_CONSERTO no bin/meow. Orce minutos e ~2-3 GB de target, não 95 s.\n\nE ANTES DE QUALQUER COISA: imprimir em papel `sudo aurora-cosmic-comp-ws.sh --restore` e `sudo apt-get install --reinstall cosmic-comp`, com o aviso de que o TTY é Ctrl+Alt+F2 (o greetd ocupa a vt1).\n\nCAMINHO MAIS BARATO, SE O OBJETIVO FOR ELA TER OS SLIDERS ESTA SEMANA: os itens (A) temperatura e (C) agendamento não precisam de shader nenhum se forem por `cosmic-randr`/CTM de gamma no KMS — vale a quente de verdade, sem recompilar comp, sem logout, e o agendamento vira um systemd timer do MeowSystem. Só a (B) textura/Kindle exige mesmo mexer no shader, porque grão por pixel não passa por rampa de gamma. Vale medir se a TV PHILCO aceita CTM antes de decidir — a máquina não tem DDC/CI, mas CTM é do driver de display, não do monitor. Isso separaria o pedido dela em \"duas coisas baratas agora\" e \"uma cara depois\", em vez de amarrar as três ao rebuild do compositor."
     }
    },
    {
     "nome": "Variante — applet NOVO, só do Meow (\"Leitura\"), em vez de patchar o de Acessibilidade",
     "alvo": "ui",
     "como": "Mesmo esqueleto de código (os sliders e o leitura.rs são idênticos), mas num crate próprio com APP_ID com.meowsystem.AppletLeitura e ícone próprio. Precisa de main.rs + Cargo.toml + o .desktop, e do MESMO token novo no plugins_wings.",
     "onde": "src/applets/leitura/ (crate autoral, sem upstream para pinar), ~/.local/bin/meow-applet-leitura.",
     "a_quente": true,
     "custo": "Mais caro que a recomendada, não mais barato: some o patch de 108 linhas mas entra um crate inteiro (Cargo.toml, main.rs, localize, ícone próprio a desenhar) — e o custo de entrada no painel é o mesmo token. Build na mesma faixa dos 95 s.",
     "durabilidade": "Melhor num ponto: não tem upstream para divergir. Pior em dois: mais um ícone na barra (a de Acessibilidade sumiria do radar dela) e nada compartilha estado com a página de Acessibilidade que ela já visita.",
     "trilha": "ui-slider",
     "veredito": {
      "solucao": "Trilha \"ui-slider\", variante: applet NOVO e autoral do Meow (\"Leitura\", com.meowsystem.AppletLeitura) em src/applets/leitura/, em vez de patchar o applet de Acessibilidade do cosmic-applets.",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "Nada aqui quebra a máquina, e o mecanismo de fundo existe — mas a proposta erra exatamente no eixo em que diz ganhar, e o \"vale a quente\" dela é herdado, não provado.\n\nO QUE CONFIRMEI (lendo a árvore viva em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094 — o binário rodando foi compilado dali, o caminho está gravado nas strings de /usr/bin/cosmic-comp):\n\n1. O uniform É por frame. src/backend/render/mod.rs:1244 `pub fn render_output(...)` é a função de quadro, e os `Uniform::new(\"invert\", ...)` / `Uniform::new(\"color_mode\", ...)` são construídos DENTRO dela, nas linhas 1363-1381. Uniform novo declarado junto dos outros em mod.rs:419-424 seria empurrado a cada quadro. O shader compila uma vez, o valor não. Esta metade está provada.\n2. O applet PODE falar com o compositor. src/state.rs:762 — `A11yState::new::<State, _>(dh, client_not_sandboxed)`. Binário nativo em ~/.local/bin não é sandbox: liga. (Corolário: este applet NUNCA pode virar flatpak, senão o global some para ele.)\n3. Applets são processos separados — `ps` mostra cosmic-applet-network, -time, -audio, meow-applet-now-playing, todos com PID próprio ao lado do cosmic-panel (PID 3186). Applet novo que trave não derruba a barra dela.\n4. cargo 1.98.0 e rustc 1.98.0 presentes; 129G livres em /mnt/Apate; deb-src habilitado; vendor/cosmic-protocols existe dentro do tarball do comp (patchável offline).\n\nO QUE DERRUBA A PROPOSTA COMO ESCRITA:\n\nA) \"vale a quente: true\" é herdado, não medido. O único fio existente hoje é o `cosmic_a11y_manager_v1`, e ele NÃO carrega valor contínuo: src/wayland/protocols/a11y.rs cria o global com `dh.create_global::<...>(2, ...)` — versão 2, sem `set_screen_filter2` — e o argumento é `uint enum` (disabled/unknown/greyscale/protanopia/deuteranopia/tritanopia). Pior: `protocol_to_color_filter` devolve `Err(Unknown)` para qualquer valor fora da lista, e o handler faz `if let Ok(filter) =` — ou seja, valor desconhecido é DESCARTADO EM SILÊNCIO. Não existe canal para um slider 0..1. O applet sozinho não move um pixel.\n\nB) A durabilidade está invertida, e a prova está no próprio repositório dela. src/applets/now-playing/PINO diz, com todas as letras: \"o Cargo.toml aponta o libcosmic para o git SEM rev, e sem o --locked uma compilação de amanhã pegaria outro commit sem ninguém pedir. Quem segura a versão é o Cargo.lock do UPSTREAM\". Um crate autoral não tem upstream — logo não tem lock herdado, e ninguém para ressincronizar quando a API do libcosmic andar. E a deriva não é hipótese: `ls ~/.cargo/git/checkouts/libcosmic-41009aea1d72760b/` já mostra CINCO checkouts distintos (1d7113a, 1f6dc99, 2a73fbc, 511384f, 8fa6a01). \"Não tem upstream para divergir\" na verdade quer dizer \"não tem upstream para me segurar\".\n\nC) O custo \"95 s\" não se sustenta para a primeira compilação. O cabeçalho do próprio scripts/midia_build.sh registra que até um rebuild que NÃO muda nada custa ~20 s, e o carimbo de 4 campos existe justamente porque compilar é caro. Build frio de um crate novo contra libcosmic é dezenas de minutos, não 95 s. O número só valeria com target/ quente.\n\nD) Uma desvantagem alegada não existe: \"a de Acessibilidade sumiria do radar dela\". Medi os quatro arquivos de plugins — `com.system76.CosmicAppletA11y` NÃO está em nenhum wing nem center, nem na topbar nem na dock. Não há o que perder. (De quebra isso confirma o outro lado: a rota recomendada TAMBÉM precisa do token, porque também não entra por sombra. Nesse ponto a proposta está certa.)\n\nE) Uma desvantagem real é PIOR do que descrita. O estado é persistido pelo compositor em ~/.local/state/cosmic-comp/a11y_screen_filter.ron (existe, hoje `(inverted: false)`) e a página Acessibilidade do cosmic-settings dirige o MESMO ScreenFilter. Duas interfaces, um estado, zero arbitragem: se ela mexer em Acessibilidade, o modo leitura cai e o applet não fica sabendo — a menos que ele também vincule o `cosmic_a11y_manager_v1` e escute o evento `screen_filter`. Isso é trabalho que o orçamento da proposta não previu.\n\nF) O canal barato existe e esvazia a justificativa do crate. src/config/mod.rs:176-182 já insere um `cosmic_config::calloop::ConfigWatchSource` para \"com.system76.CosmicComp\" no laço de eventos — a quente, sem protocolo novo, sem patchar cosmic-protocols nos DOIS lados. Se o comp patchado ler `leitura_temp`/`leitura_textura` dali, qualquer um escreve: um subcomando `meow`, a página de Acessibilidade, e — para o alvo (C), agendamento — um simples timer do systemd. O applet vira enfeite opcional, não mecanismo. Um crate inteiro para um botão que um `.timer` já resolveria é o gasto que a proposta não justifica.\n\nG) A ARMADILHA QUE MATA O SLIDER EM SILÊNCIO (vale para as DUAS trilhas, escreva no plano): render/mod.rs:1284 só entra no passe de pós-processamento se `!screen_filter.filter.is_noop()`, e `is_noop()` (config/mod.rs:159-161) é `!self.inverted && self.color_filter.is_none()`. Temperatura sem `color_filter` setado = passe inteiro pulado = slider morto em TODO valor, sem erro nenhum. Campo novo TEM de entrar no `is_noop()`.\n\nH) GLSL: aprovado, com ressalvas. O shader é `#version 100` com `precision highp float` já declarado. Multiplicação de temperatura é trivialmente válida (o aurora-night-light.py já roda isso em produção há um mês). Grão de papel também cabe em GLSL ES 1.00 via hash em v_coords — nada de texelFetch, inverse(), textureLod ou laço de contagem variável. Duas ressalvas: v_coords é 0..1, então o grão escala com a resolução se não multiplicar por um uniform de tamanho de saída; e `#version 100` exige laço de limite constante, então grão multi-oitava tem de ser desenrolado à mão.\n\nI) Fronteira e vizinho, que a proposta não menciona: o binário do cosmic-comp é da AURORA (docs/FRONTEIRA.md, linha \"binário cosmic-comp (patches de workspace e night light) | Aurora\"), e plugins_wings TAMBÉM é da Aurora, com a \"abstenção\" escrita em prosa no mesmo doc. Medi as asas: a direita da topbar tem 2 tokens, a direita da dock tem 5 — um script que assumisse lista de um apagaria a Status Area dela. O token entra À MÃO, uma vez, com backup, como em 24/08. Nunca pelo install.sh, nunca pelo doctor. E o comp patchado tem de conviver com o aurora-night-light.py, que roda depois do build no mesmo ciclo do self-heal (e tem timer próprio, aurora-cosmic-nightlight-watch): se o shader novo mantiver as âncoras `if (color_mode == 1.0) {` … `} else if (color_mode >= 2.0) {`, o night-light SOBRESCREVE o bloco novo toda hora e depois de todo apt, calado.",
      "risco_real": "Não é eliminatório: há volta, e a volta é curta.\n\nMetade do applet (o que esta variante acrescenta): risco baixo e contido. Applets são processos separados (confirmado por ps) — se o \"Leitura\" travar ou sumir, a barra continua. O único buraco é o já conhecido do MeowSystem: token presente em plugins_wings + binário ausente = slot vazio, não fallback. Volta: `rm` do .desktop em ~/.local/share/applications e do binário em ~/.local/bin, tirar o token da asa, próximo login. Sem sudo, sem rede.\n\nMetade do compositor (que esta variante NÃO evita, só empurra para a Aurora): é onde mora o risco de verdade. cosmic-comp que não sobe = ela sem desktop, na tela do greeter ou num cursor parado. A volta por TTY existe e está no disco AGORA: /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.orig (28.339.864 bytes, de 26/08) e o .pkg-orig irmão. Ctrl+Alt+F3, `sudo cp /var/lib/aurora/cosmic-comp-<versão>.orig /usr/bin/cosmic-comp`, reboot — e ela volta ao COSMIC de fábrica, perdendo só o patch de workspace e a luz noturna. O `aurora-cosmic-comp-ws.sh --ensure` recompõe os dois no ciclo seguinte. Isso PRECISA estar escrito no runbook antes de alguém compilar, com a versão exata do dia; um .orig que não casa com a versão instalada não serve (o próprio script avisa: \"sessão que não sobe\").\n\nO risco que ninguém vê: o passe de pós-processamento força render para textura offscreen + uma passada de tela cheia por quadro enquanto o modo leitura estiver ligado — adeus scanout direto. Numa máquina que já carrega o applet conta-gotas queimando 43% de CPU, isso merece medição antes de virar padrão, não depois.\n\nE o risco silencioso: se o shader novo mantiver as âncoras do aurora-night-light.py, o self-heal reescreve o bloco de hora em hora sem dizer nada. O sintoma seria \"o slider parou de funcionar sozinho, uns dias depois\" — exatamente o tipo de defeito sem culpado que o docs/FRONTEIRA.md foi escrito para evitar.",
      "correcao": "FIQUE COM A ROTA DO APPLET DE ACESSIBILIDADE (a recomendada), não com esta variante. Os dois custam o mesmo token na asa (medi: o A11y não está em wing nenhum hoje, então nenhum dos dois entra por sombra), mas a recomendada herda Cargo.lock e vendor/ do tarball do Debian, e esta não herda nada.\n\nSe ainda assim quiserem o crate autoral, três consertos obrigatórios:\n\n1. TROQUE O TRANSPORTE. Esqueça estender o cosmic_a11y_manager_v1 (global criado em versão 2, argumento uint enum, valor desconhecido descartado em silêncio em a11y.rs). Use a chave de cosmic-config que o comp JÁ observa a quente: config/mod.rs:176-182 insere um ConfigWatchSource para \"com.system76.CosmicComp\". Campos novos em CosmicCompConfig, ex. `leitura_temp: f32` (0.0 = desligado) e `leitura_textura: f32`. Isso elimina o patch de cosmic-protocols nos dois lados, e entrega o alvo (C) de graça: agendamento vira um systemd timer do usuário escrevendo a chave, sem UI nenhuma.\n\n2. CONSERTE O is_noop() ANTES DE TESTAR. config/mod.rs:159-161 vira:\n   `pub fn is_noop(&self) -> bool { !self.inverted && self.color_filter.is_none() && self.leitura_temp == 0.0 && self.leitura_textura == 0.0 }`\n   Sem isso, render/mod.rs:1284 pula o passe inteiro e o slider parece morto em todo valor, sem erro. É a primeira coisa a escrever, não a última a depurar.\n\n3. TIRE AS ÂNCORAS DO NIGHT LIGHT DO CAMINHO. Não reaproveite o bloco `if (color_mode == 1.0) { ... } else if (color_mode >= 2.0) {` — o aurora-night-light.py delimita exatamente por essas duas strings e reescreve o miolo depois de todo build e a cada ciclo do self-heal. Ponha a temperatura e o grão num bloco PRÓPRIO, depois do `} else if (color_mode >= 2.0) { ... }`, com uniforms próprios. Aí o night-light ou continua funcionando no lugar dele, ou sai com código 3 e só avisa — que é o comportamento seguro que o script promete. Melhor ainda: aposente o patch de binário e absorva a temperatura no build, com a Aurora concordando, já que o comp é dela.\n\nUniforms novos: declare junto dos existentes em render/mod.rs:419-424 (`UniformName::new(\"leitura_temp\", UniformType::_1f)`, idem textura) e empurre no mesmo vetor de mod.rs:1363-1381 — assim entram no caminho por quadro que já verifiquei.\n\nGLSL ES 1.00, válido e sem armadilha:\n    uniform float leitura_temp;      // 0.0 = desligado, senão 0..1\n    uniform float leitura_textura;   // 0..1\n    uniform vec2  leitura_res;       // tamanho em pixels da saída\n    ...\n    if (leitura_temp > 0.0) {\n        vec3 quente = mix(vec3(1.0), vec3(1.0, 0.77987, 0.54468), leitura_temp);\n        color.rgb *= quente;\n    }\n    if (leitura_textura > 0.0) {\n        vec2 p = v_coords * leitura_res;\n        float n = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        color.rgb *= (1.0 - leitura_textura * 0.06 * (n - 0.5) * 2.0);\n    }\nSem texelFetch, sem inverse(), sem laço, sem função de GLSL 3.x. `precision highp float` já está declarado no topo, que é o que mantém o sin-hash estável. O `leitura_res` não é luxo: sem ele o grão escala com a resolução e some numa TV grande. Interpole a temperatura no lado Rust com a tabela do redshift que o aurora-night-light.py já carrega (WHITEPOINTS, 1000K-6500K), em vez de inventar uma segunda.\n\nQUEM ESCREVE O QUÊ: o crate/applet e o .desktop são do Meow (precedente do now-playing, e a FRONTEIRA concorda). O binário do cosmic-comp é da Aurora — o patch do shader entra no aurora-cosmic-comp-ws.sh, não num script do Meow, senão duas máquinas de manutenção passam a disputar /usr/bin/cosmic-comp. O token em plugins_wings entra À MÃO, uma vez, com backup em ~/.local/state/meowsystem/backups/, exatamente como em 24/08 — nunca pelo install.sh, nunca pelo doctor: a asa da dock tem CINCO tokens e um script que assumisse lista de um apagaria a Status Area dela.\n\nE antes de tudo isso: escreva o runbook de TTY (Ctrl+Alt+F3, `sudo cp /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.orig /usr/bin/cosmic-comp`, reboot) num arquivo que sobreviva à sessão. Ela é a única máquina."
     }
    },
    {
     "nome": "Patch na página Acessibilidade do cosmic-settings",
     "alvo": "ui",
     "como": "Acrescentar Message::{Temperatura,Textura} + dois `settings::item::builder(..).control(slider(..))` logo depois do dropdown de tipo de filtro, e gravar pelo helper de config que a página JÁ carrega (mesmo padrão do SetCursorShakeToFind). Depois, ou substituir /usr/bin/cosmic-settings (root + hook de apt) ou plantar ~/.local/bin/cosmic-settings, que vence pelo PATH da sessão dela.",
     "onde": "csettings/cosmic-settings/src/pages/accessibility/mod.rs — enum Message linha 94, seção vision() linha 192, inserção após a linha 293, braço de update junto da linha 441.",
     "a_quente": true,
     "custo": "ALTO nesta máquina, e medido: o build morre em 119 s por falta de libdav1d-dev (libpulse-dev também ausente) — ou seja, `apt install` de dependências de compilação antes da primeira linha, o que é root e território da Aurora. O crate é o maior dos três (910 pacotes no Cargo.lock contra 786 dos applets).",
     "durabilidade": "A pior das quatro: o apt trocou o cosmic-settings 12 vezes em 70 dias (~1 a cada 6 dias). Substituir o binário do pacote significa recompilar nesse ritmo; a sombra em ~/.local/bin evita o apt mas congela o app de Ajustes DELA inteiro numa foto de hoje. Vale como SEGUNDA superfície, depois que os sliders existirem e ela disser que quer também na página — nunca como a primeira.",
     "trilha": "ui-slider",
     "veredito": {
      "solucao": "ui-slider — Patch na página Acessibilidade do cosmic-settings (dois sliders + gravação pelo comp_helper)",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "Os caminhos existem, mas o mecanismo não. O que confirmei:\n\nARQUIVOS/ÂNCORAS — OK (com ressalvas). O fonte NÃO está no MeowSystem; está só no scratchpad desta sessão (/tmp/meow-trabalho), clone grafted em 4b4b688 (29/08). O arquivo tem 484 linhas. Âncoras: `pub enum Message` na linha 86 (não 94; 93 é o SetCursorShakeToFind), `pub fn vision()` na 192 (OK), dropdown terminando na 293 (OK), braço de update na 442 (OK). Cargo.lock com 910 pacotes: OK, mas ele fica na RAIZ do workspace (csettings/Cargo.lock), não em cosmic-settings/. cosmic-comp tem 700, não 786. libdav1d-dev e libpulse-dev realmente AUSENTES (dpkg-query não encontra nenhum dos dois) — o custo de build alegado se sustenta.\n\nO FURO FATAL — não existe canal para um float. Segui o caminho inteiro do toggle que funciona hoje, e ele é DISCRETO em todos os saltos:\n  cosmic-settings → AccessibilityRequest::ScreenFilter → protocolo Wayland cosmic_a11y_manager_v1 → `Filter` (WEnum: Greyscale | DaltonizeProtanopia | DaltonizeDeuteranopia | DaltonizeTritanopia, src/wayland/protocols/a11y.rs:34-54) → `ColorFilter` (enum u8, src/config/mod.rs:167, valores 1..4) → `Uniform::new(\"color_mode\", val as u8 as f32)`.\nO fio de wire não carrega float nenhum. Não há onde enfiar 0..100 de temperatura.\n\nO SEGUNDO CANAL PROPOSTO NÃO EXISTE. A proposta diz \"gravar pelo helper de config que a página JÁ carrega (mesmo padrão do SetCursorShakeToFind)\". Medi os dois lados:\n  - `CosmicCompConfig` (cosmic-comp/cosmic-comp-config/src/lib.rs:71-106) NÃO tem campo de temperatura nem de textura. Tem workspaces, autotile, cursor_shake_to_find, activation_policy etc. — nada de cor contínua.\n  - Mesmo escrevendo uma chave solta pelo cosmic_config, o cosmic-comp a DESCARTA em silêncio: `config_changed` é um `match` por nome de chave e termina em `_ => {}` (src/config/mod.rs:1003). Chave desconhecida = nada acontece, sem log, sem erro.\n  - E `cosmic-comp-config` entra no cosmic-settings como dep GIT pinada em outro commit (`git+https://github.com/pop-os/cosmic-comp#f20de50e`), enquanto o cosmic-comp vivo é 5c93094. Adicionar um campo tipado exige patchar um TERCEIRO repo e manter os dois structs em sincronia.\n\nO SHADER NÃO TEM UNIFORM PARA ISSO. `init_shaders` (src/backend/render/mod.rs:392,419-425) compila o POSTPROCESS_SHADER com exatamente DOIS uniforms: `invert` (_1f) e `color_mode` (_1f). O texto vem de `include_str!(\"./shaders/offscreen.frag\")` (linha 131) e é compilado uma única vez, na subida do device KMS (kms/device.rs:1119, kms/surface/mod.rs:733). Os uniforms são realmente setados por frame (render/mod.rs:1374 e kms/surface/mod.rs:1899,1933) — por isso o TOGGLE vale a quente. Mas isso prova o contrário do que a proposta quer: só vale a quente o que já tem uniform. Temperatura contínua não tem. Textura não tem NADA — nem uniform, nem código, nem branch no .frag.\n\nCONCLUSÃO DO ALVO (B), A TEXTURA: é o mais insalvável. Grão de papel/e-ink é um EFEITO NOVO no fragment shader (ruído/dither por pixel). Nenhuma linha de cosmic-settings produz isso. Zero patches de UI entregam textura.\n\nO QUE A PROPOSTA REALMENTE ENTREGA SE FOR FEITA SOZINHA: dois sliders que deslizam, gravam num arquivo TOML que ninguém lê, e não mudam um pixel da tela dela. Isso é pior que não fazer — é um controle mentiroso na página de Ajustes dela.\n\nARMADILHA EXTRA QUE A PROPOSTA NÃO VÊ (e que morde a trilha inteira, não só esta): o binário vivo tem `color.rgb *= vec3(1.00000, 0.77987, 0.54468)` no lugar onde o fonte pristino tem GREYSCALE (offscreen.frag:44-46 do clone diz `// greyscale`, média dos canais). Ou seja: o \"Escala de cinza\" da página de Acessibilidade dela HOJE é, na verdade, o night light — o aurora-night-light.py (v3.30) sobrescreveu aquele branch dentro do binário, preservando o tamanho byte a byte (`assert len(data) == size_before`). Qualquer recompilação de cosmic-comp a partir do fonte limpo RESTAURA o greyscale e MATA o night light dela em silêncio; e um shader recompilado com texto novo invalida a premissa de tamanho-fixo do script da Aurora. As duas máquinas colidem.\n\nTERRITÓRIO: docs/FRONTEIRA.md põe \"binário cosmic-comp (patches de workspace e night light)\" como **Aurora**. Os passos de que esta trilha depende (uniform novo, chave nova, rebuild) são todos da Aurora, não do Meow. `~/.local/bin/cosmic-settings` não está na tabela; há precedente Meow em ~/.local/bin (meow-applet-now-playing), mas sombrear um binário de PACOTE com o mesmo basename é caso novo e precisa de linha na FRONTEIRA antes de alguém escrever.\n\nFATOS MENORES CORRIGIDOS: foram 11 upgrades de cosmic-settings (18/06 a 27/08, ~70 dias ≈ 1 a cada 6,4 dias), não 12 — a direção do argumento de durabilidade se mantém. O cosmic-comp teve 12.\n\nDETALHE QUE FUNCIONA (e vale guardar): a sombra por PATH é REAL. O PATH do processo cosmic-comp vivo (PID 3127) e o `systemctl --user show-environment` põem /home/vitoriamaria/.local/bin ANTES de /usr/bin, e o .desktop usa `Exec=cosmic-settings` (nome nu, sem TryExec, sem caminho absoluto). O cosmic-settings-daemon é resolvido pelo mesmo PATH mas tem outro basename, então a sombra não o alcança. O mecanismo de instalação está certo; é o que ele instalaria que é inútil.",
      "risco_real": "Baixo — e essa é a única coisa boa da proposta. cosmic-settings é um APP, não parte da sessão: a sessão é cosmic-comp + cosmic-panel. Se o binário sombreado estiver quebrado, ela perde o app de Ajustes e mais nada — desktop, painel, dock e atalhos seguem de pé.\n\nVolta, sem TTY e sem root: `rm ~/.local/bin/cosmic-settings` de qualquer terminal na sessão dela; o /usr/bin volta a ganhar na hora. Se em vez da sombra alguém substituir /usr/bin/cosmic-settings, a volta é `sudo apt install --reinstall cosmic-settings` — ainda sem perder desktop.\n\nO risco eliminatório está no passo de que esta trilha DEPENDE mas não descreve: recompilar o cosmic-comp. Aí sim um binário ruim custa a sessão gráfica. Volta documentada e já existente: Ctrl+Alt+F3 (TTY) e copiar /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig por cima de /usr/bin/cosmic-comp (conferi: os .orig e .pkg-orig das 3 últimas versões estão lá, mais os .aurora-ws). Antes disso, porém, há a perda SILENCIOSA que ninguém teria notado: rebuild a partir do fonte limpo devolve o greyscale e apaga o night light 3500K que ela usa hoje, sem mensagem de erro nenhuma.",
      "correcao": "A trilha não é errada — está na ORDEM errada, e a etiqueta \"vale a quente: true\" é falsa. Ela é o passo 4, e os passos 1-3 são obrigatórios e são da Aurora:\n\n1) offscreen.frag: declarar os uniforms que faltam. Em GLSL ES 1.00 (#version 100), o legal é:\n     uniform vec3 warm_tint;       // substitui o literal vec3(1.0,0.77987,0.54468)\n     uniform float paper_amount;   // 0.0 = desligado\n   e no corpo, dentro do branch já existente:\n     if (color_mode == 1.0) { color.rgb *= warm_tint; }\n   Para a textura, ruído legal em ES 1.00 (gl_FragCoord EXISTE em ES 1.00; v_coords é normalizado e não serve para grão por pixel):\n     float n = fract(sin(dot(gl_FragCoord.xy, vec2(12.9898, 78.233))) * 43758.5453);\n     color.rgb = mix(color.rgb, color.rgb * (0.94 + 0.12 * n), paper_amount);\n   PROIBIDO neste arquivo: texelFetch, textureLod, inverse(), gl_FragCoord.z/w em precisão implícita, laço com contagem variável, `in/out`, `layout(...)`. O arquivo já declara `precision highp float`, então mix/fract/sin/dot estão todos disponíveis. Confirmar que o driver não faz banding no fract(sin()) em highp antes de fechar.\n\n2) render/mod.rs:419 — acrescentar UniformName::new(\"warm_tint\", UniformType::_3f) e (\"paper_amount\", UniformType::_1f) na lista do compile_custom_texture_shader, e passar os Uniform::new correspondentes nos TRÊS pontos que já montam o TextureShaderElement (render/mod.rs:1364, kms/surface/mod.rs:1897 e :1929). Esquecer um dos três = o efeito aparece em alguns caminhos de render e não em outros.\n\n3) cosmic-comp-config/src/lib.rs — dois campos novos em CosmicCompConfig; e um braço em src/config/mod.rs ANTES do `_ => {}` da linha 1003, no molde do \"appearance_settings\" (linha 964), que é o único que já faz `schedule_render` para todas as saídas depois de mudar — é ele que faz valer a quente, não o do cursor_shake_to_find. Sem o schedule_render a tela só muda no próximo frame espontâneo.\n\n3b) O rebuild TEM de sair de dentro do aurora-cosmic-comp-ws.sh --build, carregando junto o remove-ensure_last_empty (senão o workspace vazio volta) E o vec3 do night light como DEFAULT do warm_tint (senão o greyscale volta e o 3500K dela some). Um build, três patches, ou a máquina regride em duas frentes ao mesmo tempo.\n\n4) SÓ ENTÃO os sliders no cosmic-settings, gravando pelo comp_helper — que aí passa a ter chave de verdade do outro lado. E note que, depois do passo 3, o aurora-night-light.py v3.30 fica obsoleto: com warm_tint vindo da config, não há mais literal para ele patchar, e o assert de tamanho de binário deixa de fazer sentido. Ele precisa ser aposentado no mesmo movimento, não deixado rodando.\n\nPara o alvo (C), agendamento: NÃO precisa de nada disso. Assim que a chave da config existir (passo 3), um systemd --user timer escrevendo a chave já liga e desliga sozinho no horário — é o caminho mais barato dos três e o único que não toca em shader.\n\nE antes de tudo: aparar as âncoras da proposta (enum Message em 86, não 94) e mover/clonar o fonte para um lugar durável — hoje ele só existe no scratchpad desta sessão, que some."
     }
    },
    {
     "nome": "App GTK/Python próprio do MeowSystem",
     "alvo": "ui",
     "como": "Uma janelinha com dois Gtk.Scale gravando as mesmas chaves de cosmic-config (o formato é RON de uma linha por chave; dá para escrever de Python sem biblioteca).",
     "onde": "Não existe lugar natural: o repositório não tem nenhum app de janela hoje.",
     "a_quente": true,
     "custo": "Baixo em linhas (~80 de Python) e é a única opção que não compila nada.",
     "durabilidade": "Sobrevive a tudo tecnicamente, e é a que ela menos vai usar: mora fora da UI do sistema, exige lembrar de abrir \"Meow Leitura\" pelo lançador, tem cara de GTK dentro de um desktop libcosmic (contra a folha visual dela) e traz PyGObject como dependência nova do projeto. Baratíssimo de escrever e caro de valer a pena — o oposto do que a economia deste caso pede.",
     "trilha": "ui-slider",
     "veredito": {
      "solucao": "trilha \"ui-slider\" — App GTK/Python próprio do MeowSystem, com dois Gtk.Scale gravando \"as mesmas chaves de cosmic-config\"",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "As chaves não existem, e o alvo que existe não é lido a quente. Três medições fecham o caso.\n\n1) NÃO HÁ CHAVE DE COSMIC-CONFIG PARA ISSO. `CosmicCompConfig` (cosmic-comp-config/src/lib.rs:71–107 da árvore que gerou o binário vivo, em /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094) tem 20 campos: workspaces, pinned_workspaces, input_*, xkb_config, keyboard_config, autotile, autotile_behavior, active_hint, focus_follows_cursor, cursor_follows_focus, focus_follows_cursor_delay, descale_xwayland, xwayland_eavesdropping, edge_snap_threshold, accessibility_zoom, appearance_settings, cursor_hide_timeout, cursor_shake_to_find, activation_policy. Nenhum de temperatura, textura, luz noturna ou filtro de cor. O diretório vivo ~/.config/cosmic/com.system76.CosmicComp/v1/ tem 16 arquivos e confirma a lista. \"Gravar as mesmas chaves\" é gravar chaves que não existem — arquivo que ninguém lê.\n\n2) O FILTRO DE COR NEM PASSA POR COSMIC-CONFIG. Ele viaja pelo protocolo Wayland cosmic_a11y_manager_v1 (`set_screen_filter` / `set_screen_filter2`, símbolos presentes no /usr/bin/cosmic-comp vivo) e é PERSISTIDO pelo próprio compositor em ~/.local/state/cosmic-comp/a11y_screen_filter.ron — conteúdo hoje `( inverted: false, )`. Quem escreve é ele, não a GUI.\n\n3) ESSE ARQUIVO NÃO É LIDO A QUENTE — PROVADO POR INOTIFY. `Config::load_filter_state` (src/config/mod.rs:377) é chamada uma única vez, na linha 350, dentro do load inicial. Não há watcher. Conferido no processo vivo (PID 3127): o cosmic-comp mantém 7 watches de inotify, e os inodes são 3c0008 = CosmicComp/v1, 3c000b = CosmicTheme.Mode/v1, 3c001c = CosmicTk/v1, 3c001f = Shortcuts/v1, 3c0021 = WindowRules/v1, mais dois de fora do ~/.config/cosmic. O diretório ~/.local/state/cosmic-comp é o inode 3c0022 e o arquivo é 3c005d — NENHUM DOS DOIS ESTÁ NA LISTA. Escrever ali de Python não muda um pixel até o próximo login. O \"vale a quente: true\" da proposta é falso para todo alvo gravável.\n\n4) E O CANAL QUENTE NÃO CARREGA SLIDER. Mesmo que o app GTK falasse o protocolo direto (e aí não seria mais \"gravar chave\"), o enum `filter` do cosmic-a11y-unstable-v1.xml tem 6 entradas discretas: disabled, unknown, greyscale, daltonize_protanopia, daltonize_deuteranopia, daltonize_tritanopia. Nenhum float. Zero para (A) temperatura contínua e zero para (B) textura, que não tem nem entrada no enum.\n\nOu seja: os três alvos que ela pediu — (A), (B) e (C) — recebem 0 dos 3 nesta trilha.\n\nO que a proposta acertou é irrelevante ao resultado, mas registro por honestidade: PyGObject já está instalado (python3-gi 3.48.2, gir1.2-gtk-4.0 4.14.5, gir1.2-gtk-3.0, gir1.2-adw-1) e os dois GTK importam sem erro. Então \"traz PyGObject como dependência nova\" está exagerado — mas a dependência não é o problema, o problema é que a janela não teria o que gravar.",
      "risco_real": "Não quebra o desktop — é o único ponto forte real da trilha, e não salva. Um app GTK que só escreve arquivo no home não derruba compositor, não exige logout, não pede sudo, e a volta é `rm` do arquivo. Não é preciso TTY.\n\nO risco de verdade é outro, e é de fronteira e de dado silencioso:\n\n- Se o app resolver gravar ~/.local/state/cosmic-comp/a11y_screen_filter.ron para \"pelo menos ligar o filtro\", ele passa a escrever em ~/.local/state/cosmic-comp — o diretório irmão do outputs.ron, que o docs/FRONTEIRA.md já proíbe editar com estas palavras: \"nunca editando o outputs.ron, que é estado do compositor e é reescrito por cima\". É a mesma classe de arquivo, com o mesmo dono.\n- Pior: RON malformado não dá erro visível, DÁ APAGAMENTO. src/config/mod.rs:386 faz `std::fs::remove_file(path)` quando a desserialização falha (\"Failed to read screen_filter state, resetting..\"). Um Python que erre uma vírgula no RON de uma linha não avisa nada — o compositor deleta o estado no próximo login e ela perde a configuração sem culpado na tela. É exatamente o defeito \"aparece dias depois\" que a FRONTEIRA foi escrita para evitar.\n- E o custo de oportunidade: a janela GTK dá a ELA a sensação de que existe controle, com dois sliders que se movem e não pintam nada. Isso é pior que não ter nada, porque manda ela caçar defeito no monitor dela.",
      "correcao": "Não há conserto dentro da trilha: nenhuma variação de \"janela GTK grava chave\" funciona, porque falta o canal, não a janela. O slider precisa de um caminho que carregue float até o shader, e no binário de fábrica esse caminho não existe — o uniform `color_mode` é discreto (1..4) e a temperatura 3500K é literal no TEXTO do shader.\n\nLogo (A) e (B) exigem, obrigatoriamente: recompilar o cosmic-comp com (i) um uniform float novo (ex. `uniform float meow_temp_r/g/b` e `uniform float meow_textura`) atualizado por frame, e (ii) uma chave nova dentro de `CosmicCompConfig` — porque é o ÚNICO namespace de config que o compositor observa e que já dispara reload a quente (inode 3c0008, medido acima). Chave em namespace novo não serve: ele só observa 5.\n\nSe esse recompile acontecer (e ele TEM de sair do mesmo tronco do aurora-cosmic-comp-ws.sh --build, senão o workspace vazio volta), então a UI certa é a página de Acessibilidade / o applet libcosmic — que a trilha irmã já esboçou em /tmp/meow-trabalho, com `Leitura::set_temperatura` e `set_textura`. Nasce dentro da UI do sistema, respeita a folha visual dela, e não precisa de janela nenhuma para abrir.\n\nUma coisa só desta trilha vale a pena resgatar, e sem GTK: (C) AGENDAMENTO. Ligar/desligar por horário não precisa de janela — é um systemd user timer gravando a mesma chave. Mas ele só passa a fazer efeito depois que a chave existir, ou seja, depois do recompile. Escrever o timer antes é escrever para o vazio.\n\nAviso de estado, para o orquestrador: essa árvore em scratchpad/capplets JÁ tem código MEOW editado por outro frente, com o comentário \"Gravar é a ÚNICA saída: quem pinta é o cosmic-comp, que observa este namespace.\" Essa frase é uma PREMISSA daquela trilha, não um fato medido — hoje o cosmic-comp NÃO observa namespace nenhum de leitura, e só passará a observar se o recompile incluir o campo em CosmicCompConfig. Não tratar aquele comentário como verificação."
     }
    },
    {
     "nome": "Atalho de teclado + OSD",
     "alvo": "ui",
     "como": "Super+setas disparando Spawn(\"/usr/local/bin/...\") que soma/subtrai na chave. Para ter feedback na tela, seria preciso acrescentar uma variante ao enum osd_indicator::Params e um método DBus no cosmic-osd, recompilar e substituir o binário.",
     "onde": "~/.config/cosmic/com.system76.CosmicSettings.Shortcuts/v1/custom (Spawn é a única ação custom que existe) + fork do pop-os/cosmic-osd.",
     "a_quente": true,
     "custo": "ATENÇÃO — os atalhos são território da AURORA pela docs/FRONTEIRA.md (\"é o colar dela\"), e a TRAVA 1 do lib/comum.sh recusa escrita ali por código: o MeowSystem não pode fazer isto, só pedir. Some a isso o fork do cosmic-osd (mais um binário na esteira do apt).",
     "durabilidade": "Frágil e cega: sem OSD ela não vê o valor absoluto (é justamente o que o print do MIUI mostra), e o plano B de aviso está morto — o Não Perturbe dela está ligado e engole notify-send. Serve, no máximo, como TEMPERO depois: dois atalhos que empurram a mesma chave que o applet mostra.",
     "trilha": "ui-slider",
     "veredito": {
      "solucao": "trilha \"ui-slider\" — Atalho de teclado (Super+setas → Spawn) + OSD via fork do cosmic-osd",
      "veredito": "REFUTADO",
      "a_quente_confirmado": false,
      "porque": "A proposta diz \"Spawn(...) que soma/subtrai NA CHAVE\". Medi: essa chave não existe.\n\n1. NÃO HÁ CHAVE DE TEMPERATURA, E NÃO HÁ CHAVE DE FILTRO VIGIADA.\n   O único estado do filtro é `~/.local/state/cosmic-comp/a11y_screen_filter.ron`\n   (hoje: `( inverted: false, )` — o filtro está DESLIGADO agora). Não é cosmic-config:\n   é `PersistenceGuard<ScreenFilter>` (símbolo confirmado no binário), escrito PELO\n   compositor. Provei que ele não é relido a quente lendo os inotify do PID 3127:\n     vigiados (7 watches): inclui 3932191 = ~/.config/cosmic/com.system76.CosmicSettings.Shortcuts/v1\n     NÃO vigiados: 3932194 (~/.local/state/cosmic-comp) e 3932253 (a11y_screen_filter.ron)\n   Ou seja: o atalho recarrega a quente (isso a proposta acerta), mas o alvo do script não.\n   Escrever no .ron é escrever num arquivo que o compositor só lê no boot e sobrescreve por cima.\n\n2. NÃO EXISTE CANAL CONTÍNUO EM LUGAR NENHUM. Li o protocolo vendorizado\n   /mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/vendor/cosmic-protocols/unstable/cosmic-a11y-unstable-v1.xml\n   O `set_screen_filter2` recebe `filter` como ENUM de 5 entradas (unknown, greyscale,\n   protanopia, deuteranopia, tritanopia). Nem uint de nível, nem fixed. E no lado do render\n   (src/backend/render/mod.rs:1374) o uniform é `color_mode` = `color_filter as u8 as f32`.\n   Não há por onde passar um número contínuo: nem protocolo, nem config, nem uniform.\n\n3. A TEMPERATURA É TEXTO DENTRO DO BINÁRIO. Confirmei no binário VIVO:\n   `if (color_mode == 1.0) { // NIGHT LIGHT (Aurora)` + `color.rgb *= vec3(1.00000, 0.77987, 0.54468)`.\n   Mudar isso = `sudo aurora-night-light.py --temp N`, que reescreve /usr/bin/cosmic-comp e\n   imprime literalmente \"(precisa relogar para o compositor recarregar o shader)\".\n   Somar/subtrair a cada tecla = um LOGOUT por passo do \"slider\". E `sudo -n true` falha nesta\n   máquina (sudo pede senha): cada Super+seta abriria prompt de senha, sem terminal, sem OSD.\n   Isso mata o alvo (A) por completo.\n\n4. ALVO (B), A TEXTURA/KINDLE, NÃO TEM ONDE ENCOSTAR. O offscreen.frag inteiro é\n   multiplicação de cor: alpha, un-multiply, invert, e as matrizes de daltonismo. Não existe\n   termo de ruído/grão/textura nenhum para um atalho empurrar. A proposta simplesmente não\n   endereça (B).\n\n5. ALVO (C), AGENDAMENTO, ESBARRA NO MESMO MURO. Um timer teria de falar\n   cosmic_a11y_manager_v1 — e não há cliente: `pywayland` ausente, o XML do protocolo só\n   existe dentro da árvore vendorizada, e nenhum binário CLI faz isso hoje.\n\n6. O OSD: a proposta acerta o diagnóstico, e ele é caro. Extraí as interfaces do\n   /usr/bin/cosmic-osd: `com.system76.CosmicOnScreenDisplay` expõe só identify-displays,\n   dismiss-display-identifiers, log-out, restart, enter-bios, confirm-device-type. As variantes\n   de indicador existentes são fixas (display-brightness, keyboard-brightness, audio-volume-*,\n   microphone-*, airplane-mode, input-touchpad). Não há método genérico de \"mostrar nível X\" —\n   então o fork é obrigatório mesmo, e vira um TERCEIRO binário na esteira do apt (já são dois:\n   o aurora-ws e o night-light).\n   E o plano B morreu de fato: `~/.config/cosmic/com.system76.CosmicNotifications/v1/do_not_disturb` = `true`.\n\n7. TERRITÓRIO: confirmado nos dois lados. docs/FRONTEIRA.md põe\n   `~/.config/cosmic/com.system76.CosmicSettings.Shortcuts` como **Aurora — \"é o colar dela\"**,\n   e a TRAVA 1 está ARMADA na prática: `~/.config/meow/vizinhos.conf` lista esse caminho\n   textualmente. O MeowSystem recusa por código. Some a isso que /usr/local/bin (onde moraria o\n   script do Spawn) também é da Aurora pela mesma tabela.\n\nO que a proposta ACERTA, e vale guardar: os atalhos valem a quente (medido, watch confirmado),\n`Spawn(String)` é de fato a única ação de comando arbitrário no enum Action (action.rs:123;\nas outras são Disable/System/Focus/Move/...), e o custo do fork do cosmic-osd está corretamente\nestimado. O erro é o pressuposto no meio: não existe a chave que o script somaria.",
      "risco_real": "Este é o único ponto em que a proposta é BOA: ela não quebra a tela dela.\n\nUm Spawn de atalho que falha é inerte — o compositor não cai, a sessão não cai, o pior caso é\numa tecla que não faz nada (ou, se o script chamar o patcher, um prompt de sudo invisível que\ntrava esperando senha que ninguém digita). Não há caminho aqui para \"ficar sem desktop\".\n\nO risco real está no que a proposta ARRASTA junto, se alguém a levar até o fim:\n- O fork do cosmic-osd substitui /usr/bin/cosmic-osd. Se o binário forkado não subir, ela perde\n  o OSD de volume/brilho e — pior — o FRENTE POLKIT (o cosmic-osd é quem registra\n  org.freedesktop.PolicyKit1.AuthenticationAgent). Sem ele, toda janela de senha do sistema\n  some. Volta: `sudo apt-get install --reinstall cosmic-osd` por TTY.\n- Se alguém \"resolver\" o slider re-patchando o cosmic-comp a cada tecla: o /usr/bin/cosmic-comp\n  VIVO é 33571456 bytes = o build aurora-ws COM o night-light aplicado por cima (md5\n  ef12fded... difere do .aurora-ws guardado, que é 33571456 e md5 b68b6b9f...). Um patch\n  malfeito ali é o cenário de \"sem desktop\".\n\nVOLTA, e ela existe e está testada em disco:\n- TTY disponível (getty.target active) → Ctrl+Alt+F3.\n- /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws é a cópia boa. O reparo\n  certo é `sudo cp` desse arquivo para /usr/bin/cosmic-comp e depois\n  `sudo /usr/local/bin/aurora-night-light.py`.\n- ATENÇÃO — armadilha registrada: `apt-get install --reinstall cosmic-comp` NÃO é a volta certa.\n  Ele devolve o binário de fábrica e com ele o `ensure_last_empty`, ou seja, o workspace vazio\n  extra volta. Só use o .aurora-ws.\n- Erro no as instruções do projeto que a recuperação herdaria: o `aurora-cosmic-comp-ws.sh --build` citado lá\n  NÃO está em /usr/local/bin e NÃO é executável — existe só como\n  `~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh`, modo 664. Por TTY, tem de ser\n  `bash ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh --build`.",
      "correcao": "A trilha não se conserta como slider. Ela se rebaixa a tempero, e o slider tem de vir de outro lugar.\n\nO QUE SOBRA DELA (aproveitável hoje, barato):\nNada — enquanto não existir a chave. Um atalho só vira útil DEPOIS que houver um valor contínuo\npara empurrar. Guarde a ideia; não a implemente agora.\n\nO DEGRAU INTERMEDIÁRIO REAL, que é a quente e não recompila nada (medido):\nO shader tem QUATRO slots discretos já cabeados ao uniform (color_mode 1..4), e o\naurora-night-light.py hoje usa só o 1. Reescrever também os blocos 2/3/4 como três outras\ntemperaturas (ex.: 1→4500K morno, 2→3500K, 3→2700K, 4→2000K noite) dá quatro degraus que ela\ntroca A QUENTE em Acessibilidade > Filtros de cor, com UM logout só para instalar. Custo do\npatch: os blocos 2/3/4 são maiores que o de 1 (matrizes 3x3), então cabe folgado no padding.\nGLSL: `color.rgb *= vec3(a,b,c);` é #version 100 puro — sem texelFetch, sem inverse(), sem loop.\nCusto assumido: ela perde os filtros de daltonismo (não os usa; o greyscale já foi trocado).\nIsso NÃO é o slider do MIUI, mas entrega 4 pontos onde hoje há 1, sem binário novo.\n\nO SLIDER DE VERDADE (A) exige recompilar o cosmic-comp — e a árvore já está aqui:\n/mnt/Apate/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/\nConfirmei que o caminho é viável: `Uniform::new(\"color_mode\", ...)` é montado A CADA render do\npós-processamento (src/backend/render/mod.rs:1362-1381), não no boot. Acrescentar ali um\n`Uniform::new(\"warm_rgb\", [r,g,b])` (e a `UniformName::new` correspondente na linha 423) dá\nvalor contínuo por frame. Toolchain presente: cargo 1.98.0 / rustc 1.98.0, 16 cores, build de\n~4 min pelo precedente do aurora-ws. E TEM de ser o MESMO fork do aurora-ws, não um segundo —\na regra \"conviver, não competir\" só se cumpre com um binário único.\n\n(B) TEXTURA/E-INK: nesse mesmo fork, e é GLSL ES 1.00 sem esforço — ruído por hash de v_coords\n(`fract(sin(dot(v_coords, vec2(12.9898,78.233))) * 43758.5453)`) misturado com intensidade\nuniforme. Nada de texelFetch nem de textura extra.\n\n(C) HORÁRIO: só depois que (A) existir, e aí é um systemd --user timer empurrando o valor.\nEnquanto o único canal for o enum do protocolo, agendar significa agendar entre 4 degraus.\n\nDONO: qualquer coisa que toque o binário do cosmic-comp ou /usr/local/bin é AURORA pela\ndocs/FRONTEIRA.md (\"binário cosmic-comp (patches de workspace e night light) | Aurora\"), e os\natalhos idem. O MeowSystem aqui confere e pede — não escreve."
     }
    }
   ]
  },
  {
   "trilha": "build-convivencia",
   "achados": {
    "trilha": "Sobrevivência do build: como recompilar o cosmic-comp com shader novo sem quebrar o que já funciona, e como o patch atravessa um apt upgrade",
    "resumo": "Recompilar o cosmic-comp já é rotina nesta máquina e a máquina inteira está pronta: toolchain rustup (1.98 + a 1.93 do rust-toolchain.toml), as 14 libs de dev do debian/control instaladas, deb-src habilitado, e um auto-build por systemd-run que refaz o binário sozinho a cada apt. O último build gastou 3min16s de relógio. O caminho existe — mas ele tem TRÊS armadilhas que precisam ser fechadas ANTES de qualquer build com shader novo. Primeira: o aurora-night-light.py vai CORROMPER o shader novo. Rodei o find_block dele contra um shader candidato e ele casou em (54,184): qualquer bloco que mantenha `uniform float color_mode;` + `if (color_mode == 1.0) {` + `} else if (color_mode >= 2.0) {` com menos de 512 bytes é sobrescrito pelo vec3 fixo, com padding de espaços, em até uma hora, pelo self-heal, sem ninguém pedir. Segunda: o build aplica UM arquivo .patch só — e por isso o patches/cosmic-comp-raio-clampado.patch do MeowSystem nunca entrou em binário nenhum, apesar de o README dizer que entra e de ele aplicar limpo hoje. Terceira: o `.pkg-orig` NÃO é o binário de fábrica (md5 não bate com o do dpkg); quem é de fábrica é o `.orig`. E o risco de cair é maior do que parece: o cosmic-greeter TAMBÉM roda o cosmic-comp, e o drop-in da Aurora tirou o teto de tentativas — binário ruim é laço de 5 em 5s sem nenhum login gráfico, só TTY ou ssh.",
    "achados": [
     {
      "titulo": "O build: apt-get source da versão exata, UM .patch, make VENDOR=1, artefato por versão",
      "detalhe": "compilar() (linhas 378-430) exige `cargo` no PATH, descobre a versão com dpkg-query, escolhe o patch em /usr/local/share/aurora/cosmic-comp-sem-workspace-vazio.patch (fallback ~/.config/zsh/patches/), roda `apt-get source cosmic-comp` dentro de $BUILD_DIR (~/Desenvolvimento/cosmic-comp-patch, que é symlink para /mnt/Apate/Desenvolvimento), e casa o diretório pela versão EXATA via src_dir_de() — nunca por `sort | tail -1`, corrigido justamente porque um fonte de versão maior no diretório faria o build sair errado (comentário :396-401). Aplica com `patch -p1 --forward --batch` (:414) e compila com `make VENDOR=1` (:421), que o Makefile traduz em `cargo build --release --offline --locked` depois de extrair o vendor.tar. Instala por cópia atômica cp+mv (:323, :339) para nunca deixar /usr/bin/cosmic-comp truncado.",
      "evidencia": "/home/vitoriamaria/.config/zsh/scripts/aurora-cosmic-comp-ws.sh:378-430; Makefile do fonte em /home/vitoriamaria/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094/Makefile:14-31",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "O reconhecimento é por MARCADOR de texto, nunca por hash — e é por isso que os dois patches convivem",
      "detalhe": "O fonte ganha um `#[used] static AURORA_WS_PATCH: &str = \"AURORA-COSMIC-WS-PATCH-3.67\";` em src/shell/mod.rs (hunk @@ -11,6 +11,17 @@ do patch). O script procura esse texto com `grep -a` (esta_patchado, :211-215) e extrai a VERSÃO do marcador com `strings -a | grep -o \"$MARCA-[0-9][0-9.]*\"` (marcador_de, :230-232) para distinguir \"tem patch\" de \"tem O patch de hoje\". Hash não serviria porque o night-light reescreve bytes depois. Medido: /usr/bin/cosmic-comp tem AURORA-COSMIC-WS-PATCH-3.67 E a marca 'NIGHT LIGHT (Aurora)'; os quatro .aurora-ws têm só o marcador de workspace.",
      "evidencia": "aurora-cosmic-comp-ws.sh:211-232 e :85; `strings -a /usr/bin/cosmic-comp | grep -o 'AURORA-COSMIC-WS-PATCH-[0-9.]*'` -> AURORA-COSMIC-WS-PATCH-3.67; `grep -ac 'NIGHT LIGHT (Aurora)' /usr/bin/cosmic-comp` -> 1",
      "confianca": "medido"
     },
     {
      "titulo": "A ordem dos dois patches está codificada em DEZ linhas do self-heal, e o auto-build fura essa ordem de propósito",
      "detalhe": "ritual-aurora-self-heal.sh:2627-2630 chama `aurora-cosmic-comp-ws.sh --ensure`; :2639-2653 chama `aurora-night-light.py`. Instalar o .aurora-ws joga fora o shader (o artefato sai do build, limpo), e o night light logo abaixo o reaplica — invertido, ela ficaria em 6500K até o ciclo seguinte. O auto-build rompe isso porque instala minutos depois, numa unit que o self-heal já não olha; por isso cmd_build_auto chama reaplicar_night_light() (:159-165 e :519) e cmd_build chama o patcher direto (:473-474). Exit 3 do patcher (shader mudou upstream) não conta como falha do build.",
      "evidencia": "ritual-aurora-self-heal.sh:2611-2653; aurora-cosmic-comp-ws.sh:152-165, :311-315, :473-474, :519",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "CRÍTICO: o aurora-night-light.py VAI corromper um shader novo — provado rodando o próprio find_block dele",
      "detalhe": "Carreguei o módulo e rodei find_block() contra três shaders candidatos. (1) Um shader novo que mantém `uniform float color_mode;`, `    if (color_mode == 1.0) {` e `    } else if (color_mode >= 2.0) {` com um corpo curto (ex.: `color.rgb *= aurora_warmth;`) CASOU: devolveu (54, 184). Ou seja, o patcher reescreveria o bloco inteiro com o vec3 fixo de 3500K, com padding de espaços — apagando o slider — na primeira passada do self-heal, que é de hora em hora, ANTES mesmo de ela deslogar. (2) Um bloco com mais de 512 bytes entre os marcos devolve None (guarda de sanidade, :123-126). (3) Um shader sem os marcos (uniform renomeado) devolve None. Nos casos 2 e 3 o binário NÃO é tocado, o script sai com 3 e o self-heal só avisa — seguro, porém com um AVISO por hora no journal para sempre.",
      "evidencia": "aurora-night-light.py:105-127 (find_block) e :130-140 (build_block, padding com espaços); simulação rodada em 29/08/2026 -> cenário 1: (54,184), cenário 2: None, cenário 3: None",
      "confianca": "medido"
     },
     {
      "titulo": "O `.pkg-orig` NÃO é o binário de fábrica; o `.orig` é — conferido contra o md5sum do próprio dpkg",
      "detalhe": "O dpkg registra 468c46ba3bc3466933737b185624863b para usr/bin/cosmic-comp. O /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.orig bate EXATAMENTE com esse md5. O .pkg-orig da mesma versão dá 7a39a055dc1e5e61153219eafedbd49b e contém a marca 'NIGHT LIGHT (Aurora)' — porque cmd_ensure grava o pkg-orig a partir do $ALVO no momento da troca, e nesse momento o binário já tinha o shader patchado. Vale para as 4 versões que têm pkg-orig: todas nl=1. Os 8 arquivos .orig têm nl=0 e nenhum marcador de workspace. Conclusão prática: o backup de recuperação é o `.orig`, não o `.pkg-orig`.",
      "evidencia": "`grep bin/cosmic-comp /var/lib/dpkg/info/cosmic-comp.md5sums` + md5sum dos três arquivos; auditoria de marcadores nos 16 binários de /var/lib/aurora",
      "confianca": "medido"
     },
     {
      "titulo": "ARMADILHA DE RECUPERAÇÃO: um `apt install --reinstall cosmic-comp` é desfeito em segundos pelo hook de apt",
      "detalhe": "O /etc/apt/apt.conf.d/99-ritual-aurora-self-heal roda o self-heal em DPkg::Post-Invoke, depois de TODA operação de pacote — inclusive do próprio --reinstall. O self-heal chama --ensure; e cmd_ensure, ao ver o binário sem o marcador E um artefato existente para aquela versão, reinstala o artefato (linhas 334-343) SEM consultar o freio $AUTOBUILD_OFF, que só é lido dentro de disparar_autobuild (:173). Ou seja: o freio manual documentado no cabeçalho NÃO impede a reinstalação de um artefato ruim que já esteja no disco. Numa recuperação, o artefato precisa sair do caminho (renomear), não basta o flag.",
      "evidencia": "aurora-cosmic-comp-ws.sh:334-343 e :139-141/:173; /etc/apt/apt.conf.d/99-ritual-aurora-self-heal (DPkg::Post-Invoke)",
      "confianca": "lido-na-fonte"
     },
     {
      "titulo": "Se o cosmic-comp compilado não subir, NÃO existe login gráfico nenhum — nem greeter",
      "detalhe": "/usr/bin/cosmic-greeter-start é um shell de três linhas que termina em `exec cosmic-comp cosmic-greeter`. O greetd roda esse comando na vt1 (/etc/greetd/cosmic-greeter.toml). Há UMA única sessão wayland instalada (/usr/share/wayland-sessions/cosmic.desktop) e /usr/share/xsessions nem existe. E o drop-in /etc/systemd/system/cosmic-greeter.service.d/99-aurora-espera-output.conf zera StartLimitIntervalSec e StartLimitBurst com RestartSec=5 — de propósito, para um bug de output DRM. Somados: binário ruim = laço infinito de 5 em 5 segundos, para sempre, sem nada na tela. As duas portas de volta são o TTY (Ctrl+Alt+F2, NAutoVTs=6 por default) e o ssh, que está enabled e active.",
      "evidencia": "`cat /usr/bin/cosmic-greeter-start`; /etc/greetd/cosmic-greeter.toml; /etc/systemd/system/cosmic-greeter.service.d/99-aurora-espera-output.conf:15-16,31; `systemctl is-active ssh` -> active; `ls /usr/share/wayland-sessions/` -> só cosmic.desktop",
      "confianca": "medido"
     },
     {
      "titulo": "O patch de raio do MeowSystem é código morto: o README afirma que ele entra no build, e ele nunca entrou",
      "detalhe": "README.md:195-197 diz que patches/cosmic-comp-raio-clampado.patch é 'Aplicado junto com o patch de workspace da Aurora, pelo aurora-cosmic-comp-ws.sh --build'. Mas compilar() aplica exatamente UM arquivo, o da Aurora (:385-387, :414). Medido: o marcador AURORA-COSMIC-RADIUS-PATCH não aparece em /usr/bin/cosmic-comp nem em NENHUM dos quatro .aurora-ws (contagem 0 em todos), incluindo o buildado em 28/08 — dois dias DEPOIS de o patch ser escrito (26/08 22:53). As strings ' corner radius too large' e ' padding too large' continuam nos três binários, o que só acontece se o post_error não foi trocado pelo clamp. Um dry-run agora aplica limpo (rc=0), então o patch é bom — só nunca foi chamado. Consequência hoje: meow_painel_compositor_clampa() sempre devolve falso e o MeowSystem clampa do lado de cá (lib/painel.sh:167-171).",
      "evidencia": "README.md:195; aurora-cosmic-comp-ws.sh:385-387,:414; `strings -a` nos 3 binários -> 0 ocorrências de AURORA-COSMIC-RADIUS-PATCH; `patch -p1 --dry-run --forward < patches/cosmic-comp-raio-clampado.patch` -> 'checking file src/wayland/protocols/corner_radius.rs', rc=0",
      "confianca": "medido"
     },
     {
      "titulo": "Custo real do build: 3min16s de relógio com o target quente, 4min56s de CPU, a Nice 19",
      "detalhe": "O único auto-build no journal (28/08/2026, versão 5c93094) começou 20:46:56 e terminou 20:50:15. Compilou ~30 crates (smithay, libcosmic, iced, winit) e gastou 3m00s só no crate cosmic-comp — que é onde a LTO fat morde. Isso foi com o target/ quente, herdado de um build manual de 26/08. Uma versão NOVA vem de `apt-get source` sem target/, então é build frio dos 700 crates; o comentário do script diz '~30min do zero' e eu não medi esse caso. O Cargo.lock tem 700 pacotes, o vendor 698 diretórios, e o perfil release usa lto = \"fat\". A árvore de fonte compilada pesa 3,2 GB.",
      "evidencia": "`journalctl -u aurora-cosmic-comp-ws-build`: 'Finished release profile in 3m 16s', 'Consumed 4min 56.500s CPU time'; Cargo.toml [profile.release] lto=\"fat\"; `grep -c '^\\[\\[package\\]\\]' Cargo.lock` -> 700; `du -sh` da árvore -> 3,2G",
      "confianca": "medido"
     },
     {
      "titulo": "As dependências de build estão TODAS instaladas — e o cargo é do rustup dela, não do apt",
      "detalhe": "Conferi os 14 Build-Depends do debian/control: cmake, libegl1-mesa-dev, libfontconfig-dev, libgbm-dev, libinput-dev, libpixman-1-dev, libseat-dev, libsystemd-dev, libudev-dev, libwayland-dev, libxcb1-dev, libxkbcommon-dev, libdisplay-info-dev — todos 'install ok installed', mais build-essential e pkg-config. Os pacotes apt `cargo` e `rustc` NÃO estão instalados, e isso está certo: o toolchain vem do rustup em ~/.cargo/bin (cargo 1.98.0, rustc 1.98.0), e o rust-toolchain.toml do fonte pina o canal 1.93, que também está instalado no rustup. Nada a instalar. O que NÃO pode acontecer é um `rustup toolchain uninstall 1.93` — aí o build tenta baixar e precisa de rede.",
      "evidencia": "loop dpkg-query sobre debian/control:6-20; `cargo --version`/`rustc --version`; `rustup toolchain list` -> stable + 1.93; rust-toolchain.toml channel=1.93",
      "confianca": "medido"
     },
     {
      "titulo": "A cadência de apt é semanal, às vezes diária — e nada poda o /var/lib/aurora",
      "detalhe": "12 upgrades de cosmic-comp entre 18/06 e 27/08/2026 (72 dias), e três deles em oito dias de agosto (20, 26, 27). Cada versão deixa até três binários em /var/lib/aurora (.orig 28 MB + .pkg-orig 28 MB + .aurora-ws 33 MB ≈ 89 MB). Hoje o diretório tem 459 MB de 8 versões. O podar_fontes() (:436-445) poda só as ÁRVORES DE FONTE em /mnt/Apate; nada poda /var/lib/aurora, que está em `/` — 58 GB, 78% cheio, 12 GB livres. Não é urgente, mas cresce sozinho e mora na partição apertada.",
      "evidencia": "`zgrep cosmic-comp:amd64 /var/log/dpkg.log*`; `ls -la /var/lib/aurora` -> 459 MB; `df -h /` -> 58G/43G/12G/78%; aurora-cosmic-comp-ws.sh:436-445",
      "confianca": "medido"
     },
     {
      "titulo": "Trocar o binário não troca o compositor: o efeito só vale no login seguinte, e isso é observável agora",
      "detalhe": "O cosmic-comp em execução (pid 3127, iniciado 28/08 18:43:47, uptime 23h) aponta para '/usr/bin/cosmic-comp (deleted)'. Esse inode tem 28.339.864 bytes — o tamanho de fábrica — com a marca NIGHT LIGHT mas SEM o marcador AURORA-COSMIC-WS-PATCH. O binário no disco, instalado às 20:50 do mesmo dia, tem os dois. Ou seja: ela está rodando, agora, um compositor sem o patch de workspace, e vai continuar até deslogar. Qualquer promessa de 'a quente' para algo que vive no shader COMPILADO é falsa: o shader é lido pelo processo no start.",
      "evidencia": "`ls -l /proc/3127/exe` -> (deleted); `wc -c < /proc/3127/exe` -> 28339864; `strings -a /proc/3127/exe | grep AURORA-COSMIC-WS-PATCH` -> vazio; `ps -o lstart= -p 3127` -> sex ago 28 18:43:47 2026",
      "confianca": "medido"
     },
     {
      "titulo": "O earlyoom protege o compositor e NÃO protege o rustc — o modo de falha certo",
      "detalhe": "EARLYOOM_ARGS tem --avoid com cosmic-comp, cosmic-session, cosmic-greeter, cosmic-panel, sshd e os applets; a lista --prefer é de navegadores e electron. rustc/cargo/cc não aparecem em nenhuma das duas, então sob pressão de memória o alvo natural é o próprio build. Com 15 GiB totais e 8,3 GiB disponíveis agora, o link LTO fat (single-thread, faminto) é o ponto de risco — e se ele estourar, quem morre é a compilação, não a sessão dela. O build já roda com Nice=19, CPUWeight=10, IOWeight=10, IOSchedulingClass=idle.",
      "evidencia": "/etc/default/earlyoom (EARLYOOM_ARGS, -m 10,5 -s 10,5); `free -h`; aurora-cosmic-comp-ws.sh:197-198",
      "confianca": "medido"
     },
     {
      "titulo": "Já existe o encanamento para o MeowSystem entregar patches sem quebrar a FRONTEIRA",
      "detalhe": "docs/FRONTEIRA.md:58 dá o binário cosmic-comp (patches de workspace e night light) à Aurora. Mas a linha :57 já tem o padrão inverso funcionando: '/usr/local/share/zsh/site-functions/_meow — Aurora instala, Meow fornece'. O self-heal implementa isso lendo ~/.local/state/meowsystem/raiz (que contém /mnt/Apate/Desenvolvimento/MeowSystem) e chamando install_if_diff sobre $MEOW_RAIZ/assets/zsh/_meow. O mesmo ponteiro serve para instalar $MEOW_RAIZ/patches/*.patch em /usr/local/share/aurora/patches.d/ — sem inventar mecanismo novo e sem mover a fronteira.",
      "evidencia": "docs/FRONTEIRA.md:57-58; ritual-aurora-self-heal.sh:1229-1237; `cat ~/.local/state/meowsystem/raiz` -> /mnt/Apate/Desenvolvimento/MeowSystem",
      "confianca": "medido"
     },
     {
      "titulo": "Sujeira que o build deixa e que vale limpar junto",
      "detalhe": "`patch` sem --no-backup-if-mismatch deixa .orig no fonte: encontrei src/backend/render/mod.rs.orig na árvore de 5c93094. É inofensivo hoje (o grep de idempotência olha src/shell/mod.rs), mas com uma série de patches esses .orig viram ruído e podem confundir uma checagem futura. Também não há .deb de cosmic-comp em /var/cache/apt/archives, então `apt install --reinstall` exige rede — o que reforça que o .orig local é a rede de segurança de verdade.",
      "evidencia": "`grep -rn include_str src/backend/render/` mostrou mod.rs.orig; `ls /var/cache/apt/archives/cosmic-comp*.deb` -> no matches",
      "confianca": "medido"
     }
    ],
    "codigo": [
     {
      "para_que": "Trocar o 'um .patch só' por uma SÉRIE de patches com falha graduada (req aborta, opt segue sem o efeito). Substitui as linhas 385-418 de compilar(). É o que faz o raio-clampado e o modo-de-leitura finalmente entrarem no binário.",
      "linguagem": "bash",
      "fonte": "autoral (segue o idioma de aurora-cosmic-comp-ws.sh)",
      "trecho": "# --- aplicação em SÉRIE ------------------------------------------------------\n# ANTES ERA UM .patch SÓ, e foi por isso que o cosmic-comp-raio-clampado.patch do\n# MeowSystem nunca entrou em binário nenhum: medido em 29/08/2026, o marcador\n# AURORA-COSMIC-RADIUS-PATCH tem ZERO ocorrências nos quatro .aurora-ws e no\n# /usr/bin/cosmic-comp — apesar de o README dizer que ele é aplicado no --build e\n# de o dry-run aplicar limpo hoje. Um arquivo por assunto, e uma série que diz a\n# ordem, resolve isso sem inventar mecanismo novo.\n#\n# O PREFIXO É O CONTRATO DE FALHA, e ele é o coração da degradação suave:\n#   req:<arquivo>  não aplicou -> ABORTA o build, nada é instalado\n#   opt:<arquivo>  não aplicou -> segue SEM ele, avisa, o marcador some do binário\n# Um patch opcional que cai custa o EFEITO dele. Nunca o desktop.\nSERIE=\"${AURORA_WS_SERIE:-/usr/local/share/aurora/patches.d/series}\"\nPATCHDIR=\"$(dirname \"$SERIE\")\"\nFALHOU_OPT=\"\"\n\naplicar_serie() {\n  local linha tipo arq\n  [ -f \"$SERIE\" ] || { echo \"ERRO: série não encontrada ($SERIE).\" >&2; return 2; }\n  while IFS= read -r linha; do\n    case \"$linha\" in ''|\\#*) continue ;; esac\n    tipo=\"${linha%%:*}\"; arq=\"$PATCHDIR/${linha#*:}\"\n    [ -f \"$arq\" ] || { echo \"ERRO: patch da série ausente: $arq\" >&2; return 2; }\n\n    # --no-backup-if-mismatch: sem isto o patch espalha .orig pela árvore (achei\n    # src/backend/render/mod.rs.orig na de 5c93094). Com uma série, esse lixo\n    # vira ruído e confunde qualquer checagem futura de idempotência.\n    if patch -p1 --dry-run --forward --batch --silent < \"$arq\" >/dev/null 2>&1; then\n      patch -p1 --forward --batch --no-backup-if-mismatch < \"$arq\" >/dev/null \\\n        && { echo \">> aplicado: $(basename \"$arq\")\"; continue; }\n    elif patch -p1 --dry-run --reverse --batch --silent < \"$arq\" >/dev/null 2>&1; then\n      # Reverso aplica = já está no fonte. Idempotência sem perguntar nada a\n      # ninguém, que é o que um script rodando dentro de uma unit precisa.\n      echo \">> já aplicado no fonte: $(basename \"$arq\")\"; continue\n    fi\n\n    if [ \"$tipo\" = \"req\" ]; then\n      echo \"ERRO: patch OBRIGATÓRIO não aplica: $(basename \"$arq\")\" >&2\n      echo \"      O upstream mexeu no arquivo. NADA foi instalado; o desktop\" >&2\n      echo \"      continua no binário do pacote.\" >&2\n      return 3\n    fi\n    echo \"AVISO: patch opcional não aplica: $(basename \"$arq\") — build segue SEM ele\" >&2\n    FALHOU_OPT=\"$FALHOU_OPT $(basename \"$arq\")\"\n  done < \"$SERIE\"\n  return 0\n}"
     },
     {
      "para_que": "O arquivo `series` que decide o que é obrigatório e o que é opcional. Vai em ~/.config/zsh/patches/patches.d/series (fonte canônica da Aurora) e é instalado em /usr/local/share/aurora/patches.d/series pelo self-heal.",
      "linguagem": "text",
      "fonte": "autoral",
      "trecho": "# /usr/local/share/aurora/patches.d/series — ordem e criticidade\n#\n# req = sem ele o build não vale; opt = sem ele ela perde um efeito, não a tela.\n#\n# O de workspace é req porque o sintoma de falta dele (um \"3\" seco no painel)\n# foi o que a fez perder dois dias em 27-28/08 sem entender o que era.\nreq:cosmic-comp-sem-workspace-vazio.patch\n\n# O do raio é opt: enquanto ele não estiver de pé, o `meow painel conferir`\n# clampa do lado de cá e guarda o número desejado. Degradação já implementada\n# em MeowSystem/lib/painel.sh:167-177.\nopt:cosmic-comp-raio-clampado.patch\n\n# O modo de leitura é opt: sem ele ela fica com o filtro de cor de fábrica\n# (ou com o night light fixo de 3500K, se o patcher binário ainda estiver vivo).\n# Nunca é motivo para derrubar o build inteiro.\nopt:cosmic-comp-modo-leitura.patch"
     },
     {
      "para_que": "Conferir, DEPOIS do build, que cada patch da série deixou o marcador dele no binário. Substitui a checagem de marcador único das linhas 425-427.",
      "linguagem": "bash",
      "fonte": "autoral",
      "trecho": "# Cada patch carrega o SEU `#[used] static &str` (o idioma já usado nos dois\n# patches existentes: AURORA-COSMIC-WS-PATCH-3.67 e AURORA-COSMIC-RADIUS-PATCH-1).\n# Conferir um só deixaria passar um build a que faltou metade — que é exatamente\n# a situação em que a máquina está hoje, sem ninguém ter percebido.\nverificar_marcadores() {\n  local bin=\"$1\" linha tipo arq marca faltou=\"\"\n  while IFS= read -r linha; do\n    case \"$linha\" in ''|\\#*) continue ;; esac\n    tipo=\"${linha%%:*}\"; arq=\"$PATCHDIR/${linha#*:}\"\n    marca=\"$(grep -o 'AURORA-[A-Z-]*-PATCH-[0-9.]*' \"$arq\" | head -1)\"\n    [ -n \"$marca\" ] || continue\n    grep -aqs \"$marca\" \"$bin\" && continue\n    if [ \"$tipo\" = \"req\" ]; then\n      echo \"ERRO: o binário buildado não tem $marca — abortado sem instalar.\" >&2\n      return 4\n    fi\n    faltou=\"$faltou $marca\"\n  done < \"$SERIE\"\n  [ -n \"$faltou\" ] && echo \"AVISO: build sem os opcionais:$faltou\"\n  return 0\n}"
     },
     {
      "para_que": "Aposentar o aurora-night-light.py SEM apagá-lo — a trava que impede o patcher de sobrescrever o shader compilado novo. Entra no main(), logo depois de `data = bytearray(...)` (linha 192).",
      "linguagem": "python",
      "fonte": "autoral (a constante MARK e o find_block são de aurora-night-light.py:69,105)",
      "trecho": "# APOSENTADORIA SEGURA — 29/08/2026\n#\n# Quando o shader passar a ser CÓDIGO no fonte compilado, este patcher deixa de\n# ser apoio e vira perigo. PROVADO rodando o próprio find_block() daqui contra um\n# shader candidato que mantém `uniform float color_mode;`,\n# `    if (color_mode == 1.0) {` e `    } else if (color_mode >= 2.0) {`:\n# ele CASOU, devolvendo (54, 184). Ou seja, o build_block() reescreveria o bloco\n# inteiro com o vec3 fixo de 3500K, com padding de espaços, apagando o modo de\n# leitura — em até UMA HORA, pelo self-heal, sem ninguém pedir e antes mesmo de\n# ela deslogar para ver o efeito novo.\n#\n# A trava é por MARCADOR, não por versão do pacote: um binário de fábrica (que\n# volta a todo apt, antes de o auto-build instalar o nosso) não tem o marcador, e\n# aí o comportamento antigo continua valendo — a luz noturna de 3500K volta\n# sozinha na janela entre o apt e o build, como sempre fez.\n#\n# Sai com 0, NÃO com 3: o 3 do self-heal significa \"shader mudou upstream,\n# revisita o patch\" e imprimiria um AVISO por hora, para sempre, sobre algo que\n# está exatamente como deveria.\nNEW_MARK = \"AURORA-READING-MODE\"\nif NEW_MARK.encode() in data:\n    print(\"cosmic-comp traz o modo de leitura COMPILADO (%s).\" % NEW_MARK)\n    print(\"Este patcher está aposentado neste binário: nada a reescrever.\")\n    print(\"A temperatura virou uniform — muda a quente, sem patch de binário.\")\n    return 0"
     },
     {
      "para_que": "CARTÃO DE RECUPERAÇÃO POR TTY. Deixar impresso/salvo ANTES de qualquer build. Cobre a armadilha do hook de apt e usa o backup certo (.orig, não .pkg-orig).",
      "linguagem": "bash",
      "fonte": "autoral (caminhos e md5 verificados em 29/08/2026)",
      "trecho": "# ============================================================================\n# O cosmic-comp compilado não sobe. Tela preta, ou o greeter piscando sem parar.\n# ============================================================================\n# POR QUE ISTO É PIOR DO QUE PARECE\n#   /usr/bin/cosmic-greeter-start é `exec cosmic-comp cosmic-greeter`. O greeter\n#   TAMBÉM é o cosmic-comp. Só existe uma sessão wayland instalada e não há X de\n#   reserva. E o drop-in 99-aurora-espera-output.conf zerou StartLimitBurst com\n#   RestartSec=5 — então o laço é infinito e silencioso. Não há login gráfico.\n#\n# AS DUAS PORTAS: Ctrl+Alt+F2 (NAutoVTs=6) ou ssh de outra máquina (sshd active).\n\n# 1. PARAR O LAÇO. Antes disso qualquer conserto compete com o greeter pelo DRM.\nsudo systemctl stop cosmic-greeter.service\n\n# 2. DESARMAR AS DUAS COISAS QUE DESFAZEM O CONSERTO. As duas, não uma.\n#    (a) o auto-build, para não recompilar o mesmo defeito de hora em hora;\n#    (b) o ARTEFATO — e este é o pulo do gato: o cmd_ensure reinstala artefato\n#        existente nas linhas 334-343 SEM olhar o freio $AUTOBUILD_OFF, que só é\n#        lido dentro do disparar_autobuild. E o hook DPkg::Post-Invoke roda o\n#        self-heal depois de TODO apt — inclusive do --reinstall do passo 4.\n#        Sem tirar o artefato do caminho, o conserto dura segundos.\nsudo touch /var/lib/aurora/cosmic-comp-ws-autobuild-off\nVER=\"$(dpkg-query -W -f='${Version}' cosmic-comp)\"\nsudo mv \"/var/lib/aurora/cosmic-comp-$VER.aurora-ws\" \\\n        \"/var/lib/aurora/cosmic-comp-$VER.aurora-ws.QUARENTENA\"\n\n# 3. DEVOLVER O BINÁRIO DE FÁBRICA — e é o `.orig`, NÃO o `.pkg-orig`.\n#    Conferido em 29/08/2026 contra o md5sum do próprio dpkg:\n#      /var/lib/dpkg/info/cosmic-comp.md5sums -> 468c46ba3bc3466933737b185624863b\n#      cosmic-comp-<ver>.orig               -> 468c46ba3bc3466933737b185624863b  (bate)\n#      cosmic-comp-<ver>.pkg-orig           -> 7a39a055dc1e5e61153219eafedbd49b  (NÃO bate:\n#          já carrega o shader do night light, porque foi gravado do $ALVO na hora da troca)\nsudo cp -a \"/var/lib/aurora/cosmic-comp-$VER.orig\" /usr/bin/cosmic-comp.novo\nsudo chmod 755 /usr/bin/cosmic-comp.novo\nsudo mv -f /usr/bin/cosmic-comp.novo /usr/bin/cosmic-comp   # troca ATÔMICA\nmd5sum /usr/bin/cosmic-comp\ngrep bin/cosmic-comp /var/lib/dpkg/info/cosmic-comp.md5sums   # tem de bater\n\n# 3-bis. SE O .orig SUMIU: só resta a rede. Não há .deb no /var/cache/apt/archives\n#        (conferido). E o passo 2 TEM de ter sido feito antes, ou o hook de apt\n#        reinstala o ruim no fim do próprio comando:\n#   sudo apt-get install --reinstall cosmic-comp\n\n# 4. VOLTAR AO GRÁFICO.\nsudo systemctl start cosmic-greeter.service\n\n# 5. DEPOIS, com a tela de volta: consertar o patch, refazer o build\n#    (`aurora-cosmic-comp-ws.sh --build`), e só então\n#    `sudo rm /var/lib/aurora/cosmic-comp-ws-autobuild-off`\n#    e apagar a .QUARENTENA."
     },
     {
      "para_que": "Refazer o build à mão, exatamente como o script faz, para inspecionar cada etapa antes de confiar no automático. É o caminho para o PRIMEIRO build com o shader novo.",
      "linguagem": "bash",
      "fonte": "autoral (derivado de aurora-cosmic-comp-ws.sh:378-430)",
      "trecho": "# Rodar como a VITÓRIA (o cargo é do rustup dela), nunca como root.\nVER=\"$(dpkg-query -W -f='${Version}' cosmic-comp)\"\ncd ~/Desenvolvimento/cosmic-comp-patch          # -> /mnt/Apate/Desenvolvimento (symlink)\napt-get source cosmic-comp                      # deb-src já habilitado em\n                                                # system.sources e pop-os-release.sources\ncd \"cosmic-comp-$VER\"\n\n# A série, na ordem. Cada um com dry-run antes — um patch que não aplica é uma\n# resposta, não um acidente.\nfor p in /usr/local/share/aurora/patches.d/*.patch; do\n  patch -p1 --dry-run --forward --batch --silent < \"$p\" \\\n    && patch -p1 --forward --batch --no-backup-if-mismatch < \"$p\" \\\n    || echo \"NÃO APLICA: $p\"\ndone\n\nmake VENDOR=1        # = cargo build --release --offline --locked, LTO fat\n                     # medido: 3m16s de relógio / 4m56s de CPU com o target quente\n\n# Conferir os marcadores ANTES de instalar. Sem isto o self-heal instala um\n# binário que ele mesmo não sabe reconhecer.\nstrings -a target/release/cosmic-comp | grep -o 'AURORA-[A-Z-]*-PATCH-[0-9.]*'\n\nsudo cp -a target/release/cosmic-comp \"/var/lib/aurora/cosmic-comp-$VER.aurora-ws\"\nsudo /usr/local/sbin/aurora-cosmic-comp-ws.sh --ensure\n# Vale no PRÓXIMO LOGIN. O processo em execução continua no inode antigo — medido\n# agora: /proc/3127/exe é '(deleted)', 28.339.864 bytes, SEM o marcador de workspace."
     },
     {
      "para_que": "Fazer o aviso chegar nela. O notify-send não chega (do_not_disturb engoliu 22 avisos em 27-28/08); o canal que ela lê é o `meow doctor`, que o Aurora já roda uma vez por ciclo. O build deixa por escrito, o doctor conta.",
      "linguagem": "bash",
      "fonte": "autoral (a sonda de processo copia lib/painel.sh:158-171)",
      "trecho": "# --- lado do build (aurora-cosmic-comp-ws.sh, no fim de cmd_build/cmd_build_auto)\n{\n  echo \"versao=$ver\"\n  echo \"data=$(date -Is)\"\n  for m in $(grep -ho 'AURORA-[A-Z-]*-PATCH-[0-9.]*' \"$PATCHDIR\"/*.patch | sort -u); do\n    grep -aqs \"$m\" \"$ALVO\" && echo \"ok=$m\" || echo \"faltando=$m\"\n  done\n} > \"$LIB/cosmic-comp-patches.estado\"\n\n# --- lado do MeowSystem (bin/meow, dentro do doctor) -------------------------\n# A PERGUNTA É SOBRE O PROCESSO, NÃO SOBRE O ARQUIVO — a mesma lição de\n# lib/painel.sh:158-166: em 26/08 uma sonda que leu o disco respondeu \"clampa\"\n# por 20 minutos para uma sessão que ainda matava o cliente.\nmeow_doctor_compositor() {\n  local est=/var/lib/aurora/cosmic-comp-patches.estado p k v\n  [ -r \"$est\" ] || { echo \"compositor: sem estado gravado — o Aurora ainda não buildou\"; return 0; }\n  p=\"$(pgrep -x cosmic-comp | head -1)\"\n  while IFS='=' read -r k v; do\n    case \"$k\" in\n      faltando) echo \"compositor: FALTA $v — rode 'aurora-cosmic-comp-ws.sh --build'\" ;;\n      ok) grep -aqs \"$v\" \"/proc/$p/exe\" 2>/dev/null \\\n            || echo \"compositor: $v está no disco mas NÃO na sessão — vale no próximo login\" ;;\n    esac\n  done < \"$est\"\n}"
     },
     {
      "para_que": "Podar /var/lib/aurora, que cresce ~89 MB por versão do cosmic-comp numa partição de 12 GB livres e que nada limpa hoje. Chamar junto do podar_fontes(), só depois de um build bem-sucedido.",
      "linguagem": "bash",
      "fonte": "autoral (segue o idioma defensivo de podar_fontes, aurora-cosmic-comp-ws.sh:436-445)",
      "trecho": "# O podar_fontes() cuida das árvores de 3,2 GB em /mnt/Apate. Ninguém cuida do\n# /var/lib/aurora, que mora no `/` — 58 GB, 78% cheio. São 12 upgrades em 72\n# dias, ~89 MB cada (.orig 28 + .pkg-orig 28 + .aurora-ws 33).\n#\n# O `.orig` DA VERSÃO INSTALADA é a rede de segurança do cartão de recuperação:\n# ele nunca sai, aconteça o que acontecer. Por isso o `-e` explícito no laço, e\n# não só a contagem do `tail`.\npodar_lib_aurora() {\n  local ver=\"$1\" f manter\n  manter=\"$(basename \"$(pkg_orig_de \"$ver\")\" .pkg-orig)\"\n\n  # Artefatos de OUTRAS versões nunca serão instalados (o --ensure recusa), então\n  # são peso morto assim que a versão sai.\n  for f in \"$LIB\"/cosmic-comp-*.aurora-ws; do\n    [ -f \"$f\" ] || continue\n    [ \"$f\" = \"$(artefato_de \"$ver\")\" ] && continue\n    rm -f -- \"$f\" && echo \">> podado artefato antigo: $(basename \"$f\")\"\n  done\n\n  # De fábrica: guarda os 3 mais novos, e o da versão instalada SEMPRE.\n  ls -1t \"$LIB\"/cosmic-comp-*.orig 2>/dev/null | tail -n +4 | while read -r f; do\n    case \"$f\" in *\"$manter\".orig) continue ;; esac\n    rm -f -- \"$f\" && echo \">> podado .orig antigo: $(basename \"$f\")\"\n  done\n\n  # O .pkg-orig não é de fábrica (tem o night light dentro) e só serve ao\n  # --restore da versão corrente. Fora dela, é lixo.\n  for f in \"$LIB\"/cosmic-comp-*.pkg-orig; do\n    [ -f \"$f\" ] || continue\n    [ \"$f\" = \"$(pkg_orig_de \"$ver\")\" ] && continue\n    rm -f -- \"$f\"\n  done\n}"
     },
     {
      "para_que": "O idioma exato do marcador que o patch do shader tem de carregar, copiado do patch de raio que já funciona. Sem o #[used] o linker descarta o static e o self-heal deixa de reconhecer o build.",
      "linguagem": "rust",
      "fonte": "/mnt/Apate/Desenvolvimento/MeowSystem/patches/cosmic-comp-raio-clampado.patch:119-123 (adaptado para o shader)",
      "trecho": "/// MARCADOR DO MODO DE LEITURA — nao remover.\n///\n/// O self-heal reconhece este build por texto, nunca por hash: o\n/// aurora-night-light.py reescrevia bytes do shader DEPOIS, e a partir dai\n/// nenhum hash bate. E este mesmo marcador e a TRAVA que aposenta aquele\n/// patcher: vendo esta string no binario, ele sai com 0 sem tocar em nada.\n///\n/// O #[used] e o que impede o linker de descartar um static que ninguem le.\n#[used]\nstatic AURORA_READING_MODE: &str = \"AURORA-READING-MODE-1\";"
     }
    ],
    "solucoes": [
     {
      "nome": "Série de patches com falha graduada (req/opt) em vez de um .patch monolítico",
      "alvo": "build",
      "como": "1) Criar ~/.config/zsh/patches/patches.d/ com o series e os .patch (o de workspace vira req; raio e modo-de-leitura viram opt). 2) Trocar as linhas 385-418 de compilar() pela função aplicar_serie() e as 425-427 por verificar_marcadores(). 3) No self-heal, trocar o install_if_diff único da linha 1160 por um laço que instala o diretório inteiro, e acrescentar o laço sobre $MEOW_RAIZ/patches/*.patch usando o ponteiro ~/.local/state/meowsystem/raiz que já existe na linha 1229. Resultado: um patch obrigatório que quebra aborta o build (nada é instalado, o desktop segue no binário do pacote); um opcional que quebra só some do binário, com AVISO no journal e no estado que o `meow doctor` lê.",
      "onde": "~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh:385-430; ~/.config/zsh/scripts/ritual-aurora-self-heal.sh:1155-1161 e :1229-1237; ~/.config/zsh/patches/patches.d/series; /mnt/Apate/Desenvolvimento/MeowSystem/patches/",
      "a_quente": false,
      "custo": "~40 linhas de bash trocadas em dois scripts da Aurora (repo com auto-commit a cada 10 min: a escrita vira commit). Zero custo de runtime — o dry-run de cada patch custa milissegundos. Um build a mais (3-30 min) para validar.",
      "durabilidade": "Sobrevive a apt upgrade pelo mesmo caminho de hoje: o hook DPkg::Post-Invoke chama o self-heal, o --ensure reinstala o artefato da versão ou dispara o auto-build. Sobrevive a reboot. Não tem relação com tema. Quando o upstream mexer num arquivo, o patch daquele assunto para de aplicar e SÓ ele cai."
     },
     {
      "nome": "Aposentar o aurora-night-light.py por trava de marcador, não por remoção",
      "alvo": "temperatura",
      "como": "Acrescentar o bloco NEW_MARK no main() do patcher, logo depois de `data = bytearray(...)` (:192). Enquanto o binário instalado tiver AURORA-READING-MODE, o patcher sai com 0 sem escrever nada. Enquanto NÃO tiver (a janela entre o apt trocar o binário e o auto-build instalar o nosso), ele continua fazendo o que sempre fez — a luz noturna de 3500K volta sozinha nessa janela, que é o comportamento certo. Só DEPOIS de o modo de leitura compilado estar de pé e provado é que se remove o install_if_diff (:1153) e o bloco de chamada (:2639-2653) do self-heal. A ordem importa: remover primeiro deixaria a janela pós-apt sem luz nenhuma.",
      "onde": "~/.config/zsh/scripts/aurora-night-light.py:192 (a trava); depois ~/.config/zsh/scripts/ritual-aurora-self-heal.sh:1153 e :2639-2653 (a remoção)",
      "a_quente": false,
      "custo": "9 linhas de Python. Nenhum efeito visível até o primeiro build com o shader novo.",
      "durabilidade": "É a trava que impede a corrupção medida (find_block casou em (54,184) num shader candidato). Sobrevive a apt upgrade e a reboot porque o self-heal reinstala o próprio patcher a partir da fonte canônica em ~/.config/zsh/scripts/. Se alguém restaurar um .py antigo por cima, a trava some — daí a segunda linha de defesa ser a REMOÇÃO da chamada, depois."
     },
     {
      "nome": "Segunda linha de defesa: deixar o shader novo INDIGERÍVEL para o patcher antigo",
      "alvo": "temperatura",
      "como": "Ao escrever o cosmic-comp-modo-leitura.patch, não deixar o shader casar o find_block nem por acidente: renomear o uniform (`color_mode` -> `aurora_filter_mode`) OU deixar o bloco do modo 1.0 com mais de 512 bytes entre `if (color_mode == 1.0) {` e `} else if (color_mode >= 2.0) {`. Medi os dois: ambos devolvem None e o patcher sai com 3 sem tocar no binário. Renomear o uniform é o mais limpo, porque também obriga o lado Rust a ser tocado de propósito. Isto é redundância intencional com a trava do NEW_MARK — nenhuma das duas sozinha basta, porque a trava vive num arquivo que pode ser revertido e o shader vive no binário que estamos compilando.",
      "onde": "patches/cosmic-comp-modo-leitura.patch, hunk de src/backend/render/shaders/offscreen.frag",
      "a_quente": false,
      "custo": "Zero — é uma escolha de nomes dentro de um patch que vai ser escrito de qualquer jeito.",
      "durabilidade": "Vale enquanto o patch estiver aplicado. Num apt upgrade o binário volta a ser de fábrica e o patcher antigo volta a casar — que é justamente o que se quer nessa janela."
     },
     {
      "nome": "Cartão de recuperação por TTY, escrito ANTES do primeiro build",
      "alvo": "build",
      "como": "Salvar o cartão (no bloco de código desta trilha) em MeowSystem/docs/ e, principalmente, IMPRIMIR ou deixar num segundo aparelho — de nada serve um procedimento de recuperação que só existe na máquina que não liga. Os cinco passos: parar o cosmic-greeter.service; criar o freio /var/lib/aurora/cosmic-comp-ws-autobuild-off E mover o .aurora-ws para .QUARENTENA (as duas coisas: o freio sozinho não impede o --ensure de reinstalar um artefato existente, linhas 334-343); copiar o .orig da versão por cima com troca atômica; conferir o md5 contra /var/lib/dpkg/info/cosmic-comp.md5sums; subir o greeter. A porta alternativa é ssh, que está enabled e active.",
      "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/docs/ (novo) + cópia fora da máquina; binários em /var/lib/aurora/cosmic-comp-<versão>.orig",
      "a_quente": false,
      "custo": "Uma página de documentação. Nenhum código.",
      "durabilidade": "O .orig da versão instalada é escrito pelo próprio night-light.py antes do primeiro patch, e há 8 deles no disco hoje. O que pode falhar é ele ter sido podado — por isso a poda proposta protege explicitamente o .orig da versão corrente. Sem .orig, resta apt --reinstall, que precisa de rede (não há .deb em cache)."
     },
     {
      "nome": "Fazer o aviso chegar nela pelo `meow doctor`, não pelo notify-send",
      "alvo": "ui",
      "como": "O build grava /var/lib/aurora/cosmic-comp-patches.estado com uma linha por marcador (ok= ou faltando=), e o doctor do MeowSystem lê esse arquivo comparando contra /proc/<pid do cosmic-comp>/exe — a pergunta é sobre o PROCESSO, não sobre o arquivo, pela mesma razão que lib/painel.sh:158 documenta. Duas mensagens distintas: 'FALTA <marcador>' (o build precisa ser refeito) e '<marcador> está no disco mas NÃO na sessão' (vale no próximo login). O notify-send continua onde está, mas ninguém depende dele: medido no próprio script, 22 avisos foram engolidos pelo do_not_disturb entre 27 e 28/08, e ela descobriu o problema pelo painel, dois dias depois.",
      "onde": "aurora-cosmic-comp-ws.sh (fim de cmd_build e cmd_build_auto) + /mnt/Apate/Desenvolvimento/MeowSystem/bin/meow (função nova no doctor)",
      "a_quente": true,
      "custo": "~25 linhas divididas entre os dois repos. O doctor já é rodado uma vez por ciclo pelo Aurora (FRONTEIRA.md:320).",
      "durabilidade": "O estado é regravado a cada build; se o arquivo sumir, o doctor diz 'sem estado gravado' em vez de mentir. Sobrevive a apt upgrade e reboot."
     },
     {
      "nome": "Poda do /var/lib/aurora",
      "alvo": "build",
      "como": "Chamar podar_lib_aurora() junto do podar_fontes() já existente, só depois de um build bem-sucedido: apaga todo .aurora-ws que não seja da versão instalada (o --ensure já se recusa a instalar de outra versão, então são peso morto), mantém os 3 .orig mais novos e SEMPRE o da versão instalada, e apaga os .pkg-orig que não sejam da corrente.",
      "onde": "~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh, ao lado de podar_fontes() (:436-445), chamado em :475 e :520",
      "a_quente": true,
      "custo": "~20 linhas. Libera ~370 MB agora e trava o crescimento em ~90 MB.",
      "durabilidade": "Roda a cada build bem-sucedido, ou seja a cada versão nova do cosmic-comp — que nesta máquina é semanal. Nunca toca no .orig da versão instalada, que é a rede de recuperação."
     },
     {
      "nome": "NÃO usar fork git nem `apt-mark hold` — e por quê",
      "alvo": "build",
      "como": "Fork git: o fonte vem de `apt-get source`, um tarball por versão sem história git, com 698 crates vendorizados e um vendor.tar de 94 MB; manter um fork significaria rebase manual contra um tarball a cada 6 dias, e o rebase falha do mesmo jeito que um patch falha, só que sem mensagem de erro útil. `sed` no script de build: um sed contra Rust que não casa mais SEGUE EM FRENTE em silêncio e produz um compositor que compila e se comporta errado — o modo de falha mais caro possível numa máquina onde o compositor é também o greeter. `apt-mark hold cosmic-comp`: congela o binário, mas o resto do COSMIC continua subindo, e um cosmic-comp fora de fase com o cosmic-session/libcosmic é exatamente a sessão que não sobe que o script inteiro existe para evitar (comentário :63-66). Fica o .patch versionado + série — que é o padrão que os dois repos já usam.",
      "onde": "decisão de arquitetura; o padrão existente está em /mnt/Apate/Desenvolvimento/MeowSystem/patches/ e ~/.config/zsh/patches/",
      "a_quente": false,
      "custo": "Nenhum — é uma escolha, não uma implementação.",
      "durabilidade": "O .patch com contexto falha ALTO e cedo (o `patch` diz qual hunk e em que arquivo), no dry-run, antes de compilar. É a propriedade que garante o fallback seguro: sem efeito, nunca sem desktop."
     },
     {
      "nome": "Fechar a dívida achada de passagem: o patch de raio, que o README diz aplicado e nunca foi",
      "alvo": "varios",
      "como": "A série resolve sozinha (entra como opt:cosmic-comp-raio-clampado.patch). Mas até o primeiro build com a série, corrigir o README.md:195-197, que hoje afirma um fato falso — e é um fato em que outra pessoa (ou outro frente) vai confiar ao decidir se o compositor clampa. O dry-run aplica limpo hoje, então o conserto é só encanamento.",
      "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/README.md:195; /mnt/Apate/Desenvolvimento/MeowSystem/patches/cosmic-comp-raio-clampado.patch",
      "a_quente": false,
      "custo": "Uma frase no README agora; o efeito real vem no próximo build.",
      "durabilidade": "Depois de entrar na série, é reaplicado a cada versão do cosmic-comp como qualquer outro — e o meow_painel_compositor_clampa() passa a responder verdade, liberando o raio que ela pedir sem teto."
     }
    ],
    "derrubado": [
     "\"O /var/lib/aurora/*.orig E *.pkg-orig são os binários de fábrica\" (premissa da tarefa). Só o .orig é: ele bate byte a byte com o md5sum registrado pelo dpkg (468c46ba3bc3466933737b185624863b). O .pkg-orig da mesma versão dá 7a39a055dc1e5e61153219eafedbd49b porque o cmd_ensure o grava a partir do $ALVO no momento da troca, e nesse momento o binário JÁ tem o shader do night light. Vale para as 4 versões que têm pkg-orig: todas com nl=1.",
     "\"O patches/cosmic-comp-raio-clampado.patch é aplicado junto com o patch de workspace, pelo aurora-cosmic-comp-ws.sh --build\" (README.md:195). Falso hoje. O compilar() aplica exatamente UM arquivo .patch, o da Aurora. O marcador AURORA-COSMIC-RADIUS-PATCH tem ZERO ocorrências no /usr/bin/cosmic-comp e nos quatro .aurora-ws — inclusive no buildado em 28/08, dois dias depois de o patch existir. As strings ' corner radius too large' e ' padding too large' continuam nos binários, o que só acontece se o post_error não foi trocado.",
     "\"Instalar o nosso build custa a luz noturna até o ciclo seguinte do self-heal\" (era verdade e deixou de ser). O cmd_build chama o patcher direto (:473-474) e o cmd_build_auto chama reaplicar_night_light() (:519) — provado no journal de 28/08, onde a linha 'night-light: luz noturna aplicada: 3500K' sai no MESMO segundo do 'REINSTALADO o build patchado'.",
     "\"O --ensure só AVISA quando falta build; recompilar é decisão humana\" (está assim no as instruções do projeto global). Mudou em 28/08/2026: o --ensure dispara uma unit transitória via systemd-run que compila e instala sozinha, com quatro guardas (jogo aberto adia, nome de unit fixo não empilha, carimbo limita a 3 tentativas por versão, e um freio manual). O as instruções do projeto global também diz que o self-heal 'nunca recompila' e que o build leva ~30 min — as duas frases envelheceram.",
     "\"O build leva ~4 minutos\". O 3min16s medido foi com o target/ QUENTE, herdado de um build manual dois dias antes na mesma árvore. Toda versão nova vem de `apt-get source` sem target/, e o podar_fontes() apaga a árvore anterior — então o caso comum é build FRIO de 700 crates com LTO fat. O '~30min do zero' do comentário é a única estimativa que existe, e não a medi.",
     "\"Trocar o binário troca o efeito.\" Não: o cosmic-comp em execução agora (pid 3127, 23h de uptime) roda de um inode '(deleted)' de 28.339.864 bytes SEM o marcador de workspace, enquanto o arquivo no disco tem o marcador desde as 20:50 de ontem. Qualquer efeito que viva no shader compilado só vale no próximo login — e isso vale igualmente para o patch binário do night-light, que também precisa de logout.",
     "\"O freio /var/lib/aurora/cosmic-comp-ws-autobuild-off volta ao comportamento antigo\" (cabeçalho do script, :137-138). Ele impede o auto-build de COMPILAR, mas não impede o --ensure de INSTALAR um artefato que já esteja no disco (linhas 334-343 não consultam o flag). Numa recuperação isso é a diferença entre consertar e ficar em laço: o hook de apt roda o self-heal depois do próprio `apt install --reinstall`.",
     "\"Se o cosmic-comp compilado quebrar, sobra o greeter para logar noutra sessão.\" Não sobra nada: o greeter É o cosmic-comp (`exec cosmic-comp cosmic-greeter`), há uma única sessão wayland instalada, /usr/share/xsessions não existe, e o drop-in da Aurora tirou o teto de tentativas — o resultado é um laço de 5 em 5 segundos sem nenhum login gráfico. Só TTY (Ctrl+Alt+F2) ou ssh."
    ]
   },
   "verificadas": [
    {
     "nome": "Série de patches com falha graduada (req/opt) em vez de um .patch monolítico",
     "alvo": "build",
     "como": "1) Criar ~/.config/zsh/patches/patches.d/ com o series e os .patch (o de workspace vira req; raio e modo-de-leitura viram opt). 2) Trocar as linhas 385-418 de compilar() pela função aplicar_serie() e as 425-427 por verificar_marcadores(). 3) No self-heal, trocar o install_if_diff único da linha 1160 por um laço que instala o diretório inteiro, e acrescentar o laço sobre $MEOW_RAIZ/patches/*.patch usando o ponteiro ~/.local/state/meowsystem/raiz que já existe na linha 1229. Resultado: um patch obrigatório que quebra aborta o build (nada é instalado, o desktop segue no binário do pacote); um opcional que quebra só some do binário, com AVISO no journal e no estado que o `meow doctor` lê.",
     "onde": "~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh:385-430; ~/.config/zsh/scripts/ritual-aurora-self-heal.sh:1155-1161 e :1229-1237; ~/.config/zsh/patches/patches.d/series; /mnt/Apate/Desenvolvimento/MeowSystem/patches/",
     "a_quente": false,
     "custo": "~40 linhas de bash trocadas em dois scripts da Aurora (repo com auto-commit a cada 10 min: a escrita vira commit). Zero custo de runtime — o dry-run de cada patch custa milissegundos. Um build a mais (3-30 min) para validar.",
     "durabilidade": "Sobrevive a apt upgrade pelo mesmo caminho de hoje: o hook DPkg::Post-Invoke chama o self-heal, o --ensure reinstala o artefato da versão ou dispara o auto-build. Sobrevive a reboot. Não tem relação com tema. Quando o upstream mexer num arquivo, o patch daquele assunto para de aplicar e SÓ ele cai.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Série de patches com falha graduada (req/opt) no aurora-cosmic-comp-ws.sh, em vez de um .patch monolítico — trilha \"build-convivencia\"",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "A arquitetura passa nos testes que eu tentei usar para derrubá-la, mas o texto da proposta tem três buracos verificáveis — um deles reproduz um bug que eles já pagaram em 25/08/2026.\n\nO QUE SOBREVIVEU AO ATAQUE (medido, não deduzido):\n1. Caminhos: todos existem. `~/.config/zsh/patches/cosmic-comp-sem-workspace-vazio.patch` (12243 B), `/mnt/Apate/Desenvolvimento/MeowSystem/patches/cosmic-comp-raio-clampado.patch` (9963 B), `~/.local/state/meowsystem/raiz` → `/mnt/Apate/Desenvolvimento/MeowSystem`, `/var/lib/aurora` com .orig/.pkg-orig/.aurora-ws por versão, `/usr/local/sbin/aurora-cosmic-comp-ws.sh` instalado.\n2. Linhas citadas conferem. `compilar()` começa em :378; 385-387 é a escolha do patch, 407-418 o bloco de idempotência+aplicação, 425-427 a checagem do marcador. Self-heal :1159-1160 é o par install_if_diff do script+patch, :1229-1237 é o ponteiro MEOW_RAIZ (precedente do `_meow`). Nenhum caminho errado.\n3. A CONVIVÊNCIA ESTÁ PROVADA HOJE, não suposta. Rodei o dry-run do raio na árvore viva `~/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094` (que já está com o patch de workspace aplicado — `grep -c AURORA-COSMIC-WS-PATCH src/shell/mod.rs` = 1): `patch -p1 --dry-run --forward --batch` → `rc=0`, um único arquivo, `src/wayland/protocols/corner_radius.rs`. Os dois patches não têm hunk em comum na versão instalada AGORA, não só na 31827ed contra a qual o raio foi gerado.\n4. A técnica de marcador está PROVADA sob LTO fat. O `#[used] static AURORA_WS_PATCH: &str = \"AURORA-COSMIC-WS-PATCH-3.67\"` aparece no binário vivo de 33 MB (`grep -ac` = 1). O raio usa exatamente a mesma técnica (`#[used] static AURORA_RADIUS_PATCH: &str = \"AURORA-COSMIC-RADIUS-PATCH-1\"`), então `verificar_marcadores()` é implementável.\n5. Fronteira respeitada. `docs/FRONTEIRA.md:58` dá o binário do cosmic-comp à Aurora, e a TRAVA 1 do `lib/comum.sh` proíbe o Meow de escrever em `~/.config/zsh` — o desenho proposto (série na Aurora, patch do Meow alcançado por ponteiro) é a única forma que não viola nenhuma das duas, e copia o precedente literal do `_meow` em :1229-1237.\n6. \"vale a quente: false\" está declarado honestamente e é verdade: é build + logout. Não há afirmação de quente para refutar, e não há GLSL nesta proposta para auditar.\n7. apt upgrade / reboot / tema: o caminho não muda. `--ensure` continua saindo em segundos, o hook DPkg chama o self-heal, o auto-build tem 4 guardas e MAX_TENTATIVAS=3. Nada disso encosta em tema.\n\nO QUE NÃO SOBREVIVEU (três defeitos, todos verificados):\n\nDEFEITO 1 — `aplicar_serie()` pelo código de saída do `patch` quebra na árvore quente. Medido: `patch -p1 --forward --batch < ...sem-workspace-vazio.patch` na árvore de hoje devolve **rc=1** (\"Reversed (or previously applied) patch detected! Skipping patch\", 11 hunks ignorados). O `compilar()` de hoje só escapa disso porque tem o pré-teste explícito da linha 410 (`grep -q \"$MARCA\" src/shell/mod.rs`). Uma `aplicar_serie()` que lê o rc classificaria TODO patch já aplicado como falha — e a árvore em `~/Desenvolvimento/cosmic-comp-patch` está patchada agora, e é justamente ela que dá os \"~4min com o vendor quente\". Consequência: patch req → build aborta em toda rodada quente; patch opt → aviso falso de \"caiu\" no journal e no doctor.\n\nDEFEITO 2 — a falha graduada só existe no APLICAR, não no COMPILAR. A linha 421 é `make VENDOR=1 || { echo \"ERRO: build falhou.\"; return 4; }`. Um patch \"opt\" que aplica limpo mas não compila (upstream mudou a assinatura de `layer_radius_hook`, por exemplo — é exatamente o tipo de mudança que o raio ataca) derruba o build INTEIRO, levando junto o patch req de workspace. O \"opt\" prometido não é opt nesse caminho, que é o mais provável quando o upstream mexe.\n\nDEFEITO 3 — o `--ensure` não instala o artefato novo, e isso é o bug de 25/08/2026 de volta. O `cmd_ensure` (:276) decide instalar comparando `marcador_de \"$ALVO\"` com `marcador_de \"$art\"`, e `marcador_de` (:230) extrai só `AURORA-COSMIC-WS-PATCH-[0-9.]*`. O comentário :218-225 conta o preço já pago: \"O build saiu com AURORA-COSMIC-WS-PATCH-3.67, o binário instalado tinha 3.54... Quatro minutos de compilação parados no disco, e a tela igual.\" A proposta lista como \"onde\" apenas 385-430 e os dois pontos do self-heal — **não toca em `esta_patchado` (:211), `marcador_de` (:230) nem `cmd_ensure`**. Acrescentar o raio à série sem bumpar o sufixo do marcador do WS produz `m_alvo == m_art`, e o artefato com o raio dentro nunca chega a `/usr/bin/cosmic-comp`.\n\nDEFEITO 4 (menor, de superfície) — `$MEOW_RAIZ/patches/*.patch` é glob incondicional. Qualquer arquivo `.patch` que caia no repo do Meow passa a ser compilado no compositor dela por um timer que roda como root, sem revisão humana. Hoje há um arquivo só; o desenho não deve depender disso.\n\nOBSERVAÇÃO QUE NÃO É DEFEITO MAS PESA: o raio NUNCA rodou. `grep -ac AURORA-COSMIC-RADIUS-PATCH-1 /usr/bin/cosmic-comp` = 0. O binário vivo (33571456 B, 28/08 20:50) é o aurora-ws com o night light por cima (o `vec3(1.00000, 0.77987, 0.54468)` está lá). Ou seja: o primeiro build da série é também o primeiro teste em execução de uma mudança no `corner_radius`, que é o caminho do painel. A promessa \"opt que cai só some do binário\" não cobre \"opt que aplica, compila, e desenha errado\".\n\nPor fim, o custo declarado está certo e é real: `andromeda-autosync.timer` está ativo (próximo disparo 18:15:30, último 10s atrás) e o log mostra \"auto: sync MeowSystem\" a cada ~10 min. Editar os dois scripts vira commit no repo privado dela.",
      "risco_real": "Não é eliminatório: existe volta por TTY, documentada e com o insumo presente no disco.\n\nO que quebra: o binário só troca no PRÓXIMO LOGIN (o `--ensure` diz isso em toda mensagem). Então um build ruim não derruba a sessão em uso — ela descobre ao logar. Se o cosmic-comp novo não subir, ela cai no greeter ou em tela preta, e o TTY (Ctrl+Alt+F2..F6) continua vivo porque nada aqui encosta em systemd, kernel ou display manager.\n\nA volta, medida:\n1. `sudo /usr/local/sbin/aurora-cosmic-comp-ws.sh --restore` — existe (o arquivo está em /usr/local/sbin, 755, root), copia `/var/lib/aurora/cosmic-comp-<versão>.pkg-orig` por cima de `/usr/bin/cosmic-comp` com escrita atômica (`.aurora-tmp` + `mv -f`). O `.pkg-orig` da versão instalada EXISTE hoje: `cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig`, 28339864 B, 27/08 22:43.\n2. Se o `.pkg-orig` faltar, o próprio script imprime o plano B: `sudo apt-get install --reinstall cosmic-comp`.\n3. Para impedir que o self-heal reinstale o artefato ruim no ciclo seguinte, `sudo touch /var/lib/aurora/cosmic-comp-ws-autobuild-off` desarma o auto-build (guarda da linha 173), e apagar o `.aurora-ws` da versão tira o artefato do caminho do `--ensure`.\n\nO risco que sobra é o cenário SEM tela preta: um build que sobe mas desenha errado — o raio clampado é código de protocolo Wayland do painel, e é o painel que já sumiu antes por esse arquivo. Aí não há aborto automático nenhum; o sintoma é o \"painel fantasma\" e o diagnóstico é humano. Custo de errar: um logout e um `--restore`. Custo de acertar: a cura do painel fantasma que hoje está só documentada no README:195 e em lib/painel.sh:67, e não está no binário dela.\n\nRisco secundário, barato: escrever nos dois scripts da Aurora vira commit automático no repo privado dela em até 10 min (timer confirmado ativo). Sem volta limpa se a edição sair errada — só commit por cima.",
      "correcao": "Quatro correções, todas pequenas, antes de escrever uma linha:\n\n1. A `series` precisa carregar TRÊS campos por patch, não um: `arquivo | req|opt | marcador:arquivo-fonte`. Exemplo do que já existe:\n   `cosmic-comp-sem-workspace-vazio.patch  req  AURORA-COSMIC-WS-PATCH:src/shell/mod.rs`\n   `cosmic-comp-raio-clampado.patch        opt  AURORA-COSMIC-RADIUS-PATCH:src/wayland/protocols/corner_radius.rs`\n   E `aplicar_serie()` tem de fazer o pré-teste com `grep -q \"$marcador\" \"$fonte\"` ANTES de chamar o `patch`, exatamente como a linha 410 faz hoje — nunca confiar no rc, que é 1 para \"já aplicado\" (medido).\n   Nomear os arquivos na série também mata o glob `$MEOW_RAIZ/patches/*.patch`: o self-heal instala o diretório, mas quem decide o que entra no binário é a série, não o `ls`.\n\n2. Fazer o \"opt\" ser opt também no `make`. Quando `make VENDOR=1` falhar E houver algum opt aplicado na árvore, reverter os opts (`patch -R -p1 --batch < ...`), rodar o `make` UMA vez mais, e só então desistir. Marca os opts revertidos como caídos, com AVISO. Custa um build a mais no caminho de exceção; sem isso, um opt que não compila derruba o req e ela fica sem o patch de workspace.\n\n3. Bumpar o marcador quando a série muda — ou o artefato nunca é instalado. Duas saídas:\n   (a) mínima: subir `AURORA-COSMIC-WS-PATCH-3.67` para `-3.68` no hunk do `src/shell/mod.rs` a cada mudança de série. Frágil (depende de lembrar), mas não mexe em `cmd_ensure`.\n   (b) correta: trocar `marcador_de()` (:230) por uma que concatene TODOS os marcadores encontrados, ordenados — `strings -a \"$1\" | grep -o 'AURORA-COSMIC-[A-Z]*-PATCH-[0-9][0-9.]*' | sort -u | tr '\\n' ','`. Aí `m_alvo != m_art` dispara sozinho quando um opt entra ou sai, sem ninguém lembrar de nada. `esta_patchado()` (:211) continua olhando só o req — está certo assim, é ele que responde \"o desktop está no nosso binário?\".\n   Se escolher (b), o \"onde\" da proposta tem de crescer: `aurora-cosmic-comp-ws.sh:230` entra na lista.\n\n4. Ordem de aplicação explícita e determinística: a série é lida linha a linha, na ordem do arquivo, e patch do Meow e patch da Aurora convivem na MESMA série (o caminho de cada um resolvido por prefixo). Não deixar dois laços independentes — hoje os dois patches não colidem (dry-run rc=0), mas o patch de modo-de-leitura vai mexer em `src/backend/render/`, que é onde o patch de workspace JÁ tem hunks (`src/backend/render/mod.rs`, 2 hunks). Esse é o primeiro lugar onde a ordem vai importar de verdade.\n\nSequência de validação antes de instalar qualquer coisa (tudo sem tocar no binário dela):\n  `cd ~/Desenvolvimento/cosmic-comp-patch/cosmic-comp-<ver> && patch -p1 --dry-run --forward --batch < <cada patch>` — hoje dá rc=0 para o raio e rc=1 (\"previously applied\") para o de workspace; essa assimetria é o teste do item 1.\n  Depois `--build` à mão (nunca o auto-build na estreia), conferir `grep -ac` dos DOIS marcadores no artefato em `/var/lib/aurora/*.aurora-ws`, e só então deixar o `--ensure` instalar.\n  E antes do primeiro logout: confirmar que `/var/lib/aurora/cosmic-comp-<ver>.pkg-orig` existe (existe hoje) e deixar escrito para ela, numa linha, o comando do TTY: `sudo /usr/local/sbin/aurora-cosmic-comp-ws.sh --restore`."
     }
    },
    {
     "nome": "Aposentar o aurora-night-light.py por trava de marcador, não por remoção",
     "alvo": "temperatura",
     "como": "Acrescentar o bloco NEW_MARK no main() do patcher, logo depois de `data = bytearray(...)` (:192). Enquanto o binário instalado tiver AURORA-READING-MODE, o patcher sai com 0 sem escrever nada. Enquanto NÃO tiver (a janela entre o apt trocar o binário e o auto-build instalar o nosso), ele continua fazendo o que sempre fez — a luz noturna de 3500K volta sozinha nessa janela, que é o comportamento certo. Só DEPOIS de o modo de leitura compilado estar de pé e provado é que se remove o install_if_diff (:1153) e o bloco de chamada (:2639-2653) do self-heal. A ordem importa: remover primeiro deixaria a janela pós-apt sem luz nenhuma.",
     "onde": "~/.config/zsh/scripts/aurora-night-light.py:192 (a trava); depois ~/.config/zsh/scripts/ritual-aurora-self-heal.sh:1153 e :2639-2653 (a remoção)",
     "a_quente": false,
     "custo": "9 linhas de Python. Nenhum efeito visível até o primeiro build com o shader novo.",
     "durabilidade": "É a trava que impede a corrupção medida (find_block casou em (54,184) num shader candidato). Sobrevive a apt upgrade e a reboot porque o self-heal reinstala o próprio patcher a partir da fonte canônica em ~/.config/zsh/scripts/. Se alguém restaurar um .py antigo por cima, a trava some — daí a segunda linha de defesa ser a REMOÇÃO da chamada, depois.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Aposentar o aurora-night-light.py por trava de marcador (NEW_MARK \"AURORA-READING-MODE\" em ~/.config/zsh/scripts/aurora-night-light.py:192), e só depois remover o install_if_diff (:1153) e o bloco de chamada (:2639-2653) do ritual-aurora-self-heal.sh — trilha \"build-convivencia\".",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "O QUE SE SUSTENTOU (medido em 29/08/2026):\n\n1. Caminhos e linhas conferem, todos. `aurora-night-light.py:192` é exatamente `data = bytearray(open(args.target, \"rb\").read())`. `ritual-aurora-self-heal.sh:1153` é o `install_if_diff \"$SRC/aurora-night-light.py\" /usr/local/bin/... 755 none`. O bloco de chamada é :2639-2653 (o `if [ -x /usr/local/bin/aurora-night-light.py ]` até o `fi`). `MARK = \"NIGHT LIGHT (Aurora)\"` está na :69.\n\n2. A ORDEM que a proposta defende é a ordem que já existe no self-heal: `aurora-cosmic-comp-ws.sh --ensure` em :2627-2630, patcher de night light em :2639 — o ws reinstala o artefato LIMPO por cima e o patcher requenta 9 linhas depois. Provado no disco: /usr/bin/cosmic-comp (33.571.456 B) é o `.aurora-ws` da versão 5c93094 COM o patch por cima (`grep -ac 'NIGHT LIGHT (Aurora)'` -> 1), enquanto o artefato /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws responde 0 e `cmp` diz DIFEREM. Convivência real, não hipótese.\n\n3. Só existe UM chamador do patcher. Varri /etc, /usr/local e ~/.config: `aurora-cosmic-nightlight-watch.sh` (timer 1x/dia, ativo) só consulta a issue pop-os/cosmic-comp#2059 via curl, nunca invoca o .py. Nenhuma unit systemd o chama. Trancar no .py tranca em todo lugar.\n\n4. Sobrevive a apt upgrade e a reboot pelo motivo alegado: `install_if_diff` reinstala /usr/local/bin/aurora-night-light.py a partir da fonte canônica a cada ciclo, e `cmp /usr/local/bin/aurora-night-light.py ~/.config/zsh/scripts/aurora-night-light.py` já bate hoje.\n\n5. Não pisa em território de ninguém. docs/FRONTEIRA.md:58 diz literalmente \"binário cosmic-comp (patches de workspace e night light) | Aurora | Aurora\". O MeowSystem não é tocado.\n\n6. O dano que a trava evita é REAL, e eu reproduzi. Carreguei o find_block/build_block de verdade e rodei contra um shader-candidato de modo de leitura: casou em (146, 433), bloco de 287 bytes, e o build_block devolveu o bloco de 3500K de 149 bytes + 138 espaços de padding. Ou seja: o patcher APAGA silenciosamente o GLSL do modo de leitura (o `reading_tint`, o ruído do fract/sin, o mix da textura) e põe a temperatura fixa no lugar. E o resultado AINDA compila — uniform declarado e não usado é legal em GLSL ES 1.00, e glUniform em location -1 é no-op. Não dá tela preta: dá os sliders dela parando de fazer efeito, sem erro nenhum. É a pior classe de falha, a muda.\n\nO QUE NÃO SE SUSTENTOU — três defeitos concretos, todos corrigíveis:\n\nA) A LINHA :192 MATA O CAMINHO DE VOLTA. Isso é eliminatório sob o critério 6, do jeito que está escrito. O `--status` só é tratado em :199 e o `--restore` em :217. Uma trava que dá `return 0` logo depois de :192 come os dois: `--status` vira um exit 0 mudo (adeus diagnóstico) e `--restore` — que copia /var/lib/aurora/cosmic-comp-<ver>.orig por cima e é rede de segurança de verdade — vira no-op. A trava tem de guardar só o caminho de ESCRITA, e o lugar dela é :231, imediatamente antes do `blk = find_block(data)`.\n\nB) A MENSAGEM QUEBRA O LOG DO SELF-HEAL. O ramo de exit 0 em :2642 faz `grep -qE \"j[aá] aplicado\"` na saída. Simulei: uma mensagem tipo \"modo de leitura compilado detectado — nada a fazer\" NÃO casa, e o self-heal passa a imprimir \"night-light: REAPLICADO após update do cosmic-comp\" a cada hora, para sempre — alarme falso permanente num log que ela lê para saber se algo caiu. E mudar o exit code não salva: qualquer rc fora de 0 e 3 cai no `err \"night-light: patcher falhou\"`, que incrementa `$errors` e faz o self-heal inteiro sair 1. A saída tem de conter \"já aplicado\".\n\nC) O MARCADOR NÃO PODE SER SÓ COMENTÁRIO DE GLSL. Se AURORA-READING-MODE viver dentro do próprio bloco do color_mode==1.0, ele mora exatamente na região que o patcher sobrescreve: basta UMA execução sem a trava (um .py antigo restaurado por cima — e há `.bak-antes-segredos-20260821` e `.bak-pre-desarme-windows` em ~/.config/zsh/scripts/) para o marcador sumir e a trava nunca mais rearmar. O precedente certo já está no binário e a proposta não o citou: o patch de workspace embute `AURORA-COSMIC-WS-PATCH-3.67` como static Rust (símbolo `_ZN11cosmic_comp5shell15AURORA_WS_PATCH17h54e31048f8372f13E`), fora de qualquer shader, e é assim que `marcador_de()` o lê com `strings -a`. O marcador do modo de leitura tem de nascer igual.\n\nSobre \"vale a quente\": a proposta declara false e está certa — não há nada de quente aqui. É texto GLSL dentro do ELF, compilado uma vez quando o compositor sobe; qualquer mudança exige logout. A trava em si não tem efeito visível nenhum até o primeiro build com o shader novo, como ela mesma diz.\n\nUma observação de higiene, não objeção: ~/.config/zsh tem auto-commit a cada 10 min, então editar o .py vira commit no repo privado dela. É o comportamento normal de trabalho no Aurora, mas vale ela saber que o commit sai sozinho.",
      "correcao": "Duas mudanças na proposta.\n\n1) Mover a trava de :192 para :231 (antes do `blk = find_block(data)`), preservando --status e --restore, e escrever a mensagem casando com o grep do self-heal:\n\nJunto do MARK, na :69:\n    NEW_MARK = \"AURORA-READING-MODE\"\n\nDentro do bloco --status (antes do `return 0` da :208), uma linha de diagnóstico:\n    if NEW_MARK.encode() in data:\n        print(\"leitura: build com MODO DE LEITURA presente — patcher aposentado\")\n\nE a trava propriamente dita, imediatamente ANTES da :232:\n    if NEW_MARK.encode() in data:\n        # Medido em 29/08/2026: contra um shader de modo de leitura o find_block\n        # casa (146,433) e o build_block troca 287 B de GLSL por 149 B + padding.\n        # O shader continua compilando — a falha seria MUDA, os sliders parando\n        # de fazer efeito sem erro nenhum.\n        print(\"modo de leitura compilado no binário — temperatura já aplicada pelo build\")\n        return 0\n\nA frase \"já aplicado\" é obrigatória: é o que o `grep -qE \"j[aá] aplicado\"` da :2643 procura. Com ela o log diz \"night-light: OK ( já aplicado)\"; sem ela, \"REAPLICADO\" falso a cada hora. (O parêntese sai vazio porque o `grep -oE '[0-9]+K'` não acha Kelvin nenhum — cosmético, e prefiro isso a mentir no log.)\n\n2) O marcador AURORA-READING-MODE tem de ser um static Rust no build, do mesmo jeito que `AURORA_WS_PATCH` produz `AURORA-COSMIC-WS-PATCH-3.67`, e NÃO apenas um comentário dentro do offscreen.frag. Só assim a trava sobrevive a alguém restaurar um .py antigo, e só assim o `marcador_de`-equivalente consegue lê-lo com `strings -a`. Versione junto (AURORA-READING-MODE-1.0), pela mesma razão que a auditoria de 25/08 versionou o do workspace: marcador sem versão fez o `esta_patchado` responder \"sim\" para 3.54 e 3.67 igualmente.\n\nA ordem da proposta (travar agora, remover o install_if_diff e a chamada só depois do build provado) está certa e eu manteria: remover primeiro deixa a janela entre o apt trocar o binário e o auto-build reinstalar o nosso sem luz nenhuma.",
      "risco_real": "A trava em si não escreve byte nenhum em lugar nenhum — o pior caso dela é o patcher virar no-op e a luz de 3500K não voltar numa janela pós-apt. Ela não pode derrubar o desktop.\n\nO que a trava EVITA também não é tela preta, e é importante ser exato: eu reproduzi o dano e o shader resultante ainda compila. O sintoma seria os sliders de temperatura e textura pararem de fazer efeito, calados, com o self-heal dizendo \"REAPLICADO\" como se tudo estivesse bem. Perda de recurso, não perda de máquina.\n\nSe a trava for aplicada em :192 como escrito, aí sim há perda real: o `--restore` do patcher deixa de funcionar, e ele é um dos caminhos de volta. Corrigido movendo para :231.\n\nVolta por TTY, conferida no disco e disponível sem GUI:\n- `sudo aurora-cosmic-comp-ws.sh --restore` (cmd_restore, :529) copia /var/lib/aurora/cosmic-comp-<ver>.pkg-orig por cima. Os pkg-orig existem para 091583a, 314fc67 e 5c93094 (a versão viva).\n- `sudo aurora-night-light.py --restore` copia o .orig da versão (também presente para todas as versões em /var/lib/aurora).\n- Rede final, já documentada na :535 do ws e na :220 do patcher: `sudo apt-get install --reinstall cosmic-comp`.\nTodos valem no próximo login; nenhum exige a sessão gráfica de pé."
     }
    },
    {
     "nome": "Segunda linha de defesa: deixar o shader novo INDIGERÍVEL para o patcher antigo",
     "alvo": "temperatura",
     "como": "Ao escrever o cosmic-comp-modo-leitura.patch, não deixar o shader casar o find_block nem por acidente: renomear o uniform (`color_mode` -> `aurora_filter_mode`) OU deixar o bloco do modo 1.0 com mais de 512 bytes entre `if (color_mode == 1.0) {` e `} else if (color_mode >= 2.0) {`. Medi os dois: ambos devolvem None e o patcher sai com 3 sem tocar no binário. Renomear o uniform é o mais limpo, porque também obriga o lado Rust a ser tocado de propósito. Isto é redundância intencional com a trava do NEW_MARK — nenhuma das duas sozinha basta, porque a trava vive num arquivo que pode ser revertido e o shader vive no binário que estamos compilando.",
     "onde": "patches/cosmic-comp-modo-leitura.patch, hunk de src/backend/render/shaders/offscreen.frag",
     "a_quente": false,
     "custo": "Zero — é uma escolha de nomes dentro de um patch que vai ser escrito de qualquer jeito.",
     "durabilidade": "Vale enquanto o patch estiver aplicado. Num apt upgrade o binário volta a ser de fábrica e o patcher antigo volta a casar — que é justamente o que se quer nessa janela.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Segunda linha de defesa: deixar o shader novo INDIGERÍVEL para o aurora-night-light.py (renomear `color_mode` ou inflar o bloco do modo 1.0 acima de 512 bytes)",
      "veredito": "CONFIRMADO",
      "a_quente_confirmado": false,
      "porque": "MECANISMO PROVADO, NÃO SÓ ARGUMENTADO. Carreguei a função real de /usr/local/bin/aurora-night-light.py (idêntica à fonte em ~/.config/zsh/scripts, `diff -q` limpo) e rodei contra os bytes de /usr/bin/cosmic-comp:\n  find_block(binário vivo)      -> (2998634, 2998829)  [195 bytes, já com \"NIGHT LIGHT (Aurora)\" dentro]\n  find_block(uniform renomeado) -> None\n  find_block(bloco 600+ bytes)  -> None\n  find_block(só o `if` renomeado, âncora intacta) -> None\nAs três evasões funcionam. E o caminho para o rc=3 é inequívoco: aurora-night-light.py:232-239 testa `blk is None` ANTES de qualquer escrita (o save_pref_temp só acontece na linha 248). Nada toca o binário.\n\nA PREMISSA TAMBÉM É REAL, NÃO HIPOTÉTICA. O ritual-aurora-self-heal.sh roda de hora em hora (OnUnitActiveSec=1h, timer ativo) e, nele, :2628 chama `aurora-cosmic-comp-ws.sh --ensure` e :2639 chama o patcher do night light logo depois — nessa ordem de propósito, documentada no próprio arquivo. Ou seja: toda vez que o --ensure reinstalar nosso build limpo, o patcher velho passa por cima do bloco `color_mode == 1.0` dez linhas depois. Sem esta guarda, o modo de leitura seria sobrescrito pelo vec3(1.00000, 0.77987, 0.54468) fixo, de forma silenciosa e sintaticamente válida (build_block gera GLSL correto) — o pior tipo de quebra. A redundância é justificada.\n\nrc=3 É BARATO, MEDIDO: no self-heal, o `case 3)` só ecoa \"AVISO\"; não chama `err()`, não incrementa `errors`, não manda notify-send, não põe a unit em failed.\n\nGLSL: nada aqui usa recurso pós-ES 1.00. `aurora_filter_mode` é identificador válido (sem prefixo `gl_`, sem `__`, que é reservado em GLSL ES 1.00). Inflar com comentário `//` não muda o programa compilado.\n\nA QUENTE: o próprio autor marcou false, e está certo. POSTPROCESS_SHADER entra por include_str! (src/backend/render/mod.rs:131) e é compilado em init_shaders, chamado em kms/device.rs:1119 na inicialização do device. Só vale depois de logout.\n\nAPT UPGRADE: sobrevive no sentido desejado. O upgrade devolve o binário de fábrica, o patcher velho volta a casar e a luz noturna volta na janela entre o apt e a reinstalação do artefato pelo --ensure. É exatamente o comportamento que a proposta descreve.",
      "risco_real": "SE DER ERRADO, ELA FICA SEM DESKTOP — e há volta. Um GLSL que não compila faz init_shaders devolver Err, e o `?` em src/backend/kms/device.rs:1119 (\"Failed to compile shaders\") derruba a inicialização do device: sessão não sobe. A volta existe e está no lugar: /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.pkg-orig existe (28 MB, binário de pacote), e por TTY (Ctrl+Alt+F2) `sudo aurora-cosmic-comp-ws.sh --restore` o reinstala; o fallback documentado é `sudo apt-get install --reinstall cosmic-comp`. NÃO é eliminatório, mas o passo de recuperação tem de ir escrito no cabeçalho do patch novo, como está no raio-clampado.\n\nRISCO ESPECÍFICO DO RENOME, e a proposta erra ao descrevê-lo: \"renomear o uniform obriga o lado Rust a ser tocado de propósito\" é FALSO. Nada obriga. Em smithay (vendor/smithay/src/backend/renderer/gles/mod.rs:2058-2072) o GetUniformLocation de um uniform que não existe no GLSL é guardado como -1 SEM erro, e glUniform1f(-1, v) é no-op silencioso no GL. Esquecer o renome no Rust não quebra o build nem derruba a sessão: o filtro inteiro (leitura, escala de cinza e daltonismo) simplesmente para de responder, em silêncio. O compilador não pega isso — os dois lados são strings.\n\nSão TRÊS sítios Rust, em DOIS arquivos: src/backend/render/mod.rs:423 (UniformName::new) e :1374 (Uniform::new), mais src/backend/kms/surface/mod.rs:1899 e :1933. O render/mod.rs JÁ é tocado pelo patch de workspace (hunks perto da linha 969), então o patch novo divide arquivo com ele — aplicável, mas exige ordem e guarda de idempotência própria.\n\nNAG PERMANENTE: depois do renome o patcher velho passa a imprimir, de hora em hora, \"AVISO — shader do cosmic-comp mudou upstream\" e \"revisitar o patch de night light da Aurora\". É log enganoso para sempre. E o aurora-cosmic-nightlight-watch.timer (diário, ativo) continua vigiando a issue #2059 como se a gambiarra antiga ainda fosse a solução.",
      "correcao": "1) CAMINHO ERRADO / TERRITÓRIO. docs/FRONTEIRA.md:58 diz literalmente: \"binário cosmic-comp (patches de workspace e night light) | Aurora | **Aurora**\". O patch pertence a ~/.config/zsh/patches/, ao lado do cosmic-comp-sem-workspace-vazio.patch — não a MeowSystem/patches/. Atenção: escrever ali cai no auto-commit de 10 min do repo dela.\n\n2) \"CUSTO ZERO\" É FALSO — O BUILDER SÓ APLICA UM PATCH. aurora-cosmic-comp-ws.sh:385-387 tem UM caminho fixo (PATCH_INSTALADO=/usr/local/share/aurora/cosmic-comp-sem-workspace-vazio.patch, fallback ~/.config/zsh/patches/mesmo-nome) e :410 testa idempotência com `grep -q \"$MARCA\" src/shell/mod.rs`. Um segundo arquivo .patch é INERTE até o ws.sh virar multi-patch. Precedente que confirma: MeowSystem/patches/cosmic-comp-raio-clampado.patch existe desde 26/08, é citado em três arquivos como documentação, e nunca foi aplicado por ninguém (a árvore de fonte não tem corner_radius.rs.orig). O custo real é: editar o ws.sh (Aurora, auto-commit) para aplicar uma LISTA de patches, com guarda de idempotência por patch.\n\n3) TROQUE A \"OBRIGAÇÃO\" IMAGINÁRIA POR UMA GUARDA REAL. Como o descasamento Rust/GLSL é silencioso, copie o padrão que o próprio ws.sh já usa para o MARCA (`grep -aqs \"$MARCA\" \"$bin\" || abortar sem instalar`, :427-430): depois do build, exigir que o binário contenha a string do uniform novo. Sem isso, \"obriga a tocar o Rust\" é torcida, não trava.\n\n4) PREFIRA O RENOME (A) A INFLAR O BLOCO (B). Os dois medem None, mas o >512 é invariante frágil: se um refactor futuro encolher o bloco, a guarda evapora sem aviso. O renome mata a âncora `uniform float color_mode;` e não tem como encolher de volta.\n\n5) NÃO REUSE A STRING \"NIGHT LIGHT (Aurora)\" NO SHADER NOVO. aurora-cosmic-comp-ws.sh:302 faz `grep -aqs 'NIGHT LIGHT (Aurora)' \"$ALVO\"` para decidir se o binário instalado está limpo o bastante para virar o ARTEFATO (o molde de toda instalação futura). Reusar o texto por nostalgia envenena essa guarda.\n\n6) APOSENTE O VELHO NO MESMO COMMIT. Junto com o patch novo, neutralize o passo do night light no ritual-aurora-self-heal.sh:2639 e o aurora-cosmic-nightlight-watch.timer — senão ela recebe, para sempre, um aviso horário dizendo que o patch caiu quando na verdade ele foi substituído.\n\n7) ALTERNATIVA MAIS BARATA QUE MERECE SER PESADA ANTES: a colisão só existe porque o modo de leitura foi desenhado DENTRO do bloco `color_mode == 1.0`. Se o modo de leitura viver em uniforms próprios (`aurora_read_temp`, `aurora_read_texture`) e em bloco próprio, deixando o bloco do greyscale byte-a-byte igual ao upstream, o patcher velho continua dono daquele bloco e nunca encosta no nosso — convivência de verdade, sem renomear nada no Rust e sem o risco do -1 silencioso. Aí esta proposta vira desnecessária, e não apenas redundante."
     }
    },
    {
     "nome": "Cartão de recuperação por TTY, escrito ANTES do primeiro build",
     "alvo": "build",
     "como": "Salvar o cartão (no bloco de código desta trilha) em MeowSystem/docs/ e, principalmente, IMPRIMIR ou deixar num segundo aparelho — de nada serve um procedimento de recuperação que só existe na máquina que não liga. Os cinco passos: parar o cosmic-greeter.service; criar o freio /var/lib/aurora/cosmic-comp-ws-autobuild-off E mover o .aurora-ws para .QUARENTENA (as duas coisas: o freio sozinho não impede o --ensure de reinstalar um artefato existente, linhas 334-343); copiar o .orig da versão por cima com troca atômica; conferir o md5 contra /var/lib/dpkg/info/cosmic-comp.md5sums; subir o greeter. A porta alternativa é ssh, que está enabled e active.",
     "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/docs/ (novo) + cópia fora da máquina; binários em /var/lib/aurora/cosmic-comp-<versão>.orig",
     "a_quente": false,
     "custo": "Uma página de documentação. Nenhum código.",
     "durabilidade": "O .orig da versão instalada é escrito pelo próprio night-light.py antes do primeiro patch, e há 8 deles no disco hoje. O que pode falhar é ele ter sido podado — por isso a poda proposta protege explicitamente o .orig da versão corrente. Sem .orig, resta apt --reinstall, que precisa de rede (não há .deb em cache).",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Cartão de recuperação por TTY, escrito ANTES do primeiro build (trilha build-convivencia)",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "O núcleo do cartão está CONFIRMADO por medição, mas os cinco passos, como escritos, falham na prática em três pontos.\n\nCONFIRMADO (medido em 29/08/2026):\n- O .orig da versão corrente existe e é BYTE-IDÊNTICO ao pacote: md5 de /var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.orig = 468c46ba3bc3466933737b185624863b = exatamente a linha `usr/bin/cosmic-comp` de /var/lib/dpkg/info/cosmic-comp.md5sums. Não tem nem o marcador \"NIGHT LIGHT (Aurora)\" nem \"AURORA-COSMIC-WS-PATCH\".\n- São 8 .orig no disco (o cartão diz 8). Nenhuma poda hoje os ameaça: podar_fontes (aurora-cosmic-comp-ws.sh:436) só apaga diretórios com Cargo.toml sob o BUILD_DIR. /var tem 12G livres.\n- \"O freio sozinho não impede o --ensure\": CONFIRMADO. AUTOBUILD_OFF aparece em exatamente duas linhas (139 def, 173 uso) e o uso está dentro de disparar_autobuild. O bloco que reinstala o artefato (linha 341, \"REINSTALADO o build patchado\") não o consulta. Freio + quarentena juntos, sim, param: sem artefato o fluxo cai no aviso e disparar_autobuild retorna 1 pelo freio.\n- Não há .deb de cosmic-comp em /var/cache/apt/archives (o cartão está certo: apt --reinstall exige rede).\n- ssh enabled + active, escutando 0.0.0.0:22 e [::]:22. /etc/ssh/sshd_config.d/ nem existe, então valem os defaults.\n- TTY existe: NAutoVTs=6 (default, comentado), getty@tty1 em getty.target.wants, ela está no grupo sudo.\n- Troca atômica: o próprio script usa .aurora-tmp + mv -f, então o cartão copiar esse idioma é correto.\n\nOS TRÊS DEFEITOS:\n1. PASSO 1 NÃO GRUDA. `systemctl stop cosmic-greeter.service` é desfeito em ~10s pelo aurora-greeter-watchdog.service (enabled, active, Restart=always). Ele acorda a cada 10s e ressuscita o greeter quando: a unit não está active, NÃO há sessão logind de uid>=1000 do tipo wayland/x11, e há saída DRM conectada. Um login de TTY tem Type=tty, então sessao_de_gente_viva() responde \"não\" — é exatamente o estado da recuperação. Ela ficaria brigando com um greeter renascendo em cima do TTY.\n2. O CARTÃO PRECISA PROIBIR O VIZINHO .pkg-orig. Existem dois backups de nome parecido e mesmo tamanho (28339864 bytes). O .pkg-orig NÃO é o binário do pacote nesta versão: md5 7a39a055dc1e5e61153219eafedbd49b (não bate com o dpkg) e CONTÉM o marcador \"NIGHT LIGHT (Aurora)\". Ele é escrito por aurora-cosmic-comp-ws.sh (pkg_orig_de, :102) copiando o ALVO no estado em que estiver. Pior: `aurora-cosmic-comp-ws.sh --restore` (:532) usa justamente o .pkg-orig — quem seguir o comando \"oficial\" restaura o contaminado e o md5 do cartão vai acusar FALHOU sem que nada esteja corrompido.\n3. FALTA O SEXTO PASSO — o patcher do shader não tem freio nenhum. ritual-aurora-self-heal.sh:2639-2641 chama /usr/local/bin/aurora-night-light.py incondicionalmente, a cada ciclo do ritual-aurora-self-heal.timer (de hora em hora; o próximo era 18:52) e depois de todo apt (DPkg::Post-Invoke em /etc/apt/apt.conf.d/99-ritual-aurora-self-heal). Se o que quebrou a tela foi o SHADER — que é precisamente o que o projeto dos sliders vai engordar —, restaurar o .orig compra no máximo uma hora. E não dá para desarmar tirando o -x nem apagando o arquivo: install_if_diff (self-heal:549) reinstala quando o conteúdo OU o modo diferem, com mode=755 (self-heal:1153). Nota extra: um `apt --reinstall` dispara o hook e re-patcha na hora seguinte ao próprio reinstall.\n4. (menor) A forma do md5. Os caminhos no .md5sums são relativos, então o `cd /` é carga útil, não enfeite.",
      "correcao": "Cartão corrigido — seis passos, nesta ordem:\n\n0. DESARMAR O GREETER E O VIGIA DE UMA VEZ:\n   sudo systemctl isolate multi-user.target\n   (funciona porque o próprio watchdog se cala quando `systemctl get-default` != graphical.target; hoje é graphical.target. Alternativa equivalente: `sudo systemctl stop aurora-greeter-watchdog.service` ANTES de parar o cosmic-greeter.service — nunca depois.)\n\n1. PARAR O SELF-HEAL, senão ele repatcha em até 1h:\n   sudo systemctl stop ritual-aurora-self-heal.timer\n   (e lembrar: qualquer operação de apt re-dispara o self-heal pelo hook 99-ritual-aurora-self-heal.)\n\n2. FREIO + QUARENTENA (as duas, o freio sozinho não basta):\n   sudo touch /var/lib/aurora/cosmic-comp-ws-autobuild-off\n   sudo mv /var/lib/aurora/cosmic-comp-<VER>.aurora-ws /var/lib/aurora/cosmic-comp-<VER>.QUARENTENA\n\n3. RESTAURAR O .orig — E SÓ O .orig. NÃO usar o .pkg-orig, NÃO usar `aurora-cosmic-comp-ws.sh --restore` (ele pega o .pkg-orig, que já vem com o shader dentro):\n   sudo cp -a /var/lib/aurora/cosmic-comp-<VER>.orig /usr/bin/cosmic-comp.rec\n   sudo mv -f /usr/bin/cosmic-comp.rec /usr/bin/cosmic-comp\n   sudo chmod 755 /usr/bin/cosmic-comp\n\n4. CONFERIR, do diretório raiz:\n   cd / && md5sum -c /var/lib/dpkg/info/cosmic-comp.md5sums\n   Tem de dizer SUCESSO nas 5 linhas. (Prova de que a checagem discrimina: rodando agora, com o binário patchado, ela diz `usr/bin/cosmic-comp: FALHOU` e SUCESSO nas outras 4.) Conferir ANTES de subir a sessão — depois que o self-heal voltar, o night light re-patcha e essa linha volta a FALHAR legitimamente.\n\n5. SUBIR:\n   sudo systemctl set-default graphical.target && sudo systemctl isolate graphical.target\n   Ao reativar o self-heal (systemctl start ritual-aurora-self-heal.timer), saber que ele vai recolocar o night light — deixar o timer parado enquanto a causa não estiver entendida.\n\nVERSÃO DE HOJE, para copiar no cartão impresso: <VER> = 0.1~1787767625~24.04~5c93094, md5 do .orig = 468c46ba3bc3466933737b185624863b.\n\nPorta alternativa: ssh (enabled/active, 0.0.0.0:22) — mas isso exige um segundo aparelho na mesma rede E o IP dela anotado no cartão. Anotar o IP fixo/hostname no papel, senão a \"porta alternativa\" é teórica.\n\nO cartão deve datar-se e nomear as linhas de que depende (aurora-cosmic-comp-ws.sh:102, :173, :341, :532; ritual-aurora-self-heal.sh:549, :2639; aurora-greeter-watchdog.sh), porque são scripts da Aurora com auto-commit e vão mudar.",
      "risco_real": "O cartão em si não escreve nada e não quebra nada — é uma página. O risco é o oposto: o cartão como proposto dá FALSA SEGURANÇA em dois cenários reais.\n\n(a) Ela vai ao TTY, para o greeter, e em 10s ele volta por cima (watchdog camada 2), com o compositor quebrado reiniciando em laço enquanto ela digita. Não há perda de dados — o mv é atômico —, mas ela conclui que \"nem o procedimento funciona\", que é o pior momento possível para perder a confiança no papel.\n\n(b) Ela restaura, o desktop volta, ela respira — e uma hora depois a tela quebra de novo, sem causa aparente, porque o self-heal recolocou o shader. Esse é o cenário mais provável do projeto dos sliders, já que o alvo dele é justamente o bloco do shader.\n\nVolta existe e é sólida nos dois casos: TTY (NAutoVTs=6, ela é sudo) e ssh ativo; o .orig da versão corrente está no disco e bate byte a byte com o dpkg; a cópia é atômica, então não há janela de binário truncado. Sem .orig sobraria apt --reinstall, que precisa de rede — não há .deb em cache. Com os seis passos acima, considero o caminho de recuperação documentado e suficiente; sem eles, não."
     }
    },
    {
     "nome": "Fazer o aviso chegar nela pelo `meow doctor`, não pelo notify-send",
     "alvo": "ui",
     "como": "O build grava /var/lib/aurora/cosmic-comp-patches.estado com uma linha por marcador (ok= ou faltando=), e o doctor do MeowSystem lê esse arquivo comparando contra /proc/<pid do cosmic-comp>/exe — a pergunta é sobre o PROCESSO, não sobre o arquivo, pela mesma razão que lib/painel.sh:158 documenta. Duas mensagens distintas: 'FALTA <marcador>' (o build precisa ser refeito) e '<marcador> está no disco mas NÃO na sessão' (vale no próximo login). O notify-send continua onde está, mas ninguém depende dele: medido no próprio script, 22 avisos foram engolidos pelo do_not_disturb entre 27 e 28/08, e ela descobriu o problema pelo painel, dois dias depois.",
     "onde": "aurora-cosmic-comp-ws.sh (fim de cmd_build e cmd_build_auto) + /mnt/Apate/Desenvolvimento/MeowSystem/bin/meow (função nova no doctor)",
     "a_quente": true,
     "custo": "~25 linhas divididas entre os dois repos. O doctor já é rodado uma vez por ciclo pelo Aurora (FRONTEIRA.md:320).",
     "durabilidade": "O estado é regravado a cada build; se o arquivo sumir, o doctor diz 'sem estado gravado' em vez de mentir. Sobrevive a apt upgrade e reboot.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "build-convivencia: o aviso do patch do cosmic-comp chega pelo `meow doctor` (estado gravado em /var/lib/aurora + comparação contra /proc/<pid>/exe), não pelo notify-send",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "MEDIDO AGORA (29/08, uid 1000, sem sudo), tudo que a proposta cita existe e funciona:\n\nCAMINHOS — /home/vitoriamaria/.config/zsh/scripts/aurora-cosmic-comp-ws.sh existe (27493 B), com `cmd_build()` na linha 448 e `cmd_build_auto()` na 481; a cópia instalada /usr/local/sbin/aurora-cosmic-comp-ws.sh é byte-idêntica. /var/lib/aurora existe (root:root 755, legível por ela). /mnt/Apate/Desenvolvimento/MeowSystem/bin/meow tem `cmd_doctor()` na 1185. lib/painel.sh:158 tem exatamente o bloco \"A PERGUNTA É SOBRE O PROCESSO, NUNCA SOBRE O ARQUIVO — 26/08/2026\". docs/FRONTEIRA.md:~320 confirma \"o Aurora roda `meow doctor` (que é read-only) e imprime uma linha\"; a chamada real está em ritual-aurora-self-heal.sh:1982-1999, via `runuser -u vitoriamaria`.\n\n\"A QUENTE\" — vale, e a prova é a máquina dela neste minuto: `readlink /proc/3127/exe` devolve `/usr/bin/cosmic-comp (deleted)`, e mesmo assim `md5sum`, `strings -a` e `grep -a` leem o inode sem sudo. Sessão nasceu 28/08 18:43:47 (23h21 de uptime); /usr/bin/cosmic-comp foi trocado às 20:50:14 — 2h07 DEPOIS. Resultado: disco = 33.571.456 B com `AURORA-COSMIC-WS-PATCH-3.67`; processo vivo = 28.339.864 B, SEM marcador nenhum. O caso \"está no disco mas NÃO na sessão\" está acontecendo há 23 horas e o doctor o detectaria hoje, sem logout. Nada de GLSL/uniform aqui: a proposta não toca no shader, então o critério 3 não se aplica.\n\nO DIAGNÓSTICO DO notify-send CONFERE — está escrito e medido no próprio script: linhas 116-122 (177 avisos \"não tem build patchado\", 22 só da versão de 27/08 22:43, engolidos por `do_not_disturb: true`) e 236-243 (`notify-send -u critical` NÃO aparece com DND no COSMIC, testado com screenshot). O `|| true` do `avisar_usuaria` (linha 250) faz o script nem saber que falhou.\n\nE EXISTE UMA REGRESSÃO SILENCIOSA VIVA QUE ISTO PEGARIA — comparando os artefatos: /var/lib/aurora/cosmic-comp-...31827ed.aurora-ws (26/08) tem AURORA-COSMIC-RADIUS-PATCH-1 **e** WS-PATCH-3.67; o instalado ...5c93094.aurora-ws (28/08) tem só o WS. O patch do raio sumiu no rebuild e ninguém grita, porque `meow_painel_patch_pendente` (lib/painel.sh:175) exige o marcador no disco — que também não está lá — e scripts/painel.sh:191 cai na frase genérica \"sem o patch\", idêntica à de quem nunca teve. Isto é a justificativa da proposta, provada.\n\nPOR QUE NÃO É \"CONFIRMADO\": três defeitos concretos na especificação (ver correção). E um alerta de escopo: esta trilha não entrega NADA do que ela pediu (A slider de temperatura, B textura, C agendamento) — é andaime do build. Não pode ser contada como progresso do pedido.",
      "risco_real": "Nulo para a tela dela, e isso é o ponto forte: o doctor é read-only por contrato (bin/meow:24 \"o `doctor` NUNCA USA sudo, E NUNCA BAIXA NADA\"; :35 \"conferir é leitura\"). Nenhum caminho novo escreve em /usr/bin/cosmic-comp, não recompila, não reinicia painel. Se a leitura de /proc falhar, o pior é uma linha errada na tabela. Não há cenário de \"ela fica sem desktop\" — logo não há recuperação por TTY a documentar.\n\nTerritório: /var/lib/aurora é da Aurora (root), mas só é LIDO pelo Meow — sem violação da TRAVA 1. A escrita real é em ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh, que auto-commita a cada 10 min no repo privado dela; precedente aceito (\"o Aurora pode ser corrigido pelo Meow\"), mas lembre de copiar para /usr/local/sbin (hoje as duas cópias são idênticas; editar só a fonte não muda o que o self-heal roda).\n\nO ÚNICO risco de verdade é regressão do build: qualquer linha nova no fim de `cmd_build`/`cmd_build_auto` roda depois de 4 min de compilação, sob `set -u`, num script cuja falha já deixou `ritual-aurora-self-heal.service` em `failed` 27 vezes seguidas (bloco HOME, linhas 70-84). Grave o estado com `|| true` e NUNCA dentro de um `&&` que decida instalação.\n\nE um risco de sinal, não de máquina: se a linha nova acender \"~~\" todo ciclo por algo que só o logout resolve, ela para de olhar — o mesmo fim do notify-send, por outro canal. É o que a correção abaixo evita.",
      "correcao": "1) O CÓDIGO DE SAÍDA É O QUE DECIDE SE ISTO SERVE OU VIRA RUÍDO. Tabela do runner (bin/meow ~1035): 0=ok, 1=`~~`, 3|4=`--`, resto=erro; `pior_rc` transforma qualquer 1 em \"há divergências\", e o self-heal imprime \"meow: DIVERGENTE (N linhas de ~~)\". \"No disco mas não na sessão\" NÃO tem conserto — `--consertar` não compila (bin/meow:524) nem usa sudo — e está verdadeiro há 23h. Se devolver 1, a Vitória lê \"DIVERGENTE\" de hora em hora até o próximo login, exatamente o ping-pong que bin/meow:811 (chk_prompt) e :832 (chk_fastfetch) já documentam. Então: `FALTA <marcador> no disco` → MEOW_DIVERGENTE (1), porque existe ação humana (`aurora-cosmic-comp-ws.sh --build`); `no disco mas não na sessão` → 4, com a frase nomeando o logout; sem compositor visível (`pgrep -x cosmic-comp` vazio, doctor por SSH/TTY) → 3, nunca \"assume o pior\" nesta sonda. E acrescente a linha no MOTIVO_SEM_FIX (bin/meow:979-984): \"o conserto compila, e o doctor não compila\".\n\n2) O ARQUIVO DE ESTADO NÃO PODE CARREGAR O VEREDITO — só o CONTRATO. O marcador já mora dentro do binário e é a fonte autoritativa (é por isso que o próprio script rejeita hash, linhas 40-43). Um `.estado` gravado no build com \"ok=WS\" sobrevive ao `apt upgrade` que troca /usr/bin/cosmic-comp — e passa a mentir com cara de verdade. A \"durabilidade\" alegada é o defeito. Grave só a LISTA de marcadores esperados; o veredito o doctor deriva na hora, de dois lugares: `strings -a /usr/bin/cosmic-comp` e `grep -a` em /proc/<pid>/exe. Três estados por marcador: ausente nos dois (1), presente no disco e ausente na sessão (4), presente nos dois (0).\n\n3) A LISTA DE ESPERADOS TEM DE SER DECLARADA, NÃO DERIVADA DO BUILD. \"Uma linha por marcador\" só pega o que o build sabe que devia produzir. Prova de que o buraco é real: /usr/local/share/aurora/cosmic-comp-sem-workspace-vazio.patch (mtime 25/08 19:04) declara UM marcador — `static AURORA_WS_PATCH: &str = \"AURORA-COSMIC-WS-PATCH-3.67\"` na linha 144 — e `grep -c AURORA-COSMIC-RADIUS-PATCH` nele devolve 0. O patch do raio nunca entrou na série; foi aplicado à mão uma vez, em 26/08, e evaporou no rebuild de 28/08. Derivar a lista do patch (ou do build) continuaria silenciosa sobre ele. Portanto: array constante no topo do script (`MARCAS_ESPERADAS=(AURORA-COSMIC-WS-PATCH AURORA-COSMIC-RADIUS-PATCH)`), o build grava esse contrato, e o doctor cobra os dois. E `cmd_build`/`cmd_build_auto` devem abortar quando o binário compilado não tem TODOS (hoje o guard das linhas 426 e 513 checa só `$MARCA`).\n\n4) REUSE, NÃO INVENTE. `meow_painel_compositor_clampa` (lib/painel.sh:167) e `meow_painel_patch_pendente` (:175) já são exatamente esta sonda para um marcador. Generalize as duas para receber o marcador como argumento e o novo verificador vira ~10 linhas em bin/meow, não 25 — sem uma segunda implementação de \"processo vs arquivo\" para envelhecer em paralelo.\n\n5) Grave o estado com escrita atômica (tmp + `mv -f`) e `|| true`, e faça o doctor dizer \"sem estado gravado\" quando o arquivo não existir — como a proposta já previu, e é a parte dela que está certa."
     }
    },
    {
     "nome": "Poda do /var/lib/aurora",
     "alvo": "build",
     "como": "Chamar podar_lib_aurora() junto do podar_fontes() já existente, só depois de um build bem-sucedido: apaga todo .aurora-ws que não seja da versão instalada (o --ensure já se recusa a instalar de outra versão, então são peso morto), mantém os 3 .orig mais novos e SEMPRE o da versão instalada, e apaga os .pkg-orig que não sejam da corrente.",
     "onde": "~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh, ao lado de podar_fontes() (:436-445), chamado em :475 e :520",
     "a_quente": true,
     "custo": "~20 linhas. Libera ~370 MB agora e trava o crescimento em ~90 MB.",
     "durabilidade": "Roda a cada build bem-sucedido, ou seja a cada versão nova do cosmic-comp — que nesta máquina é semanal. Nunca toca no .orig da versão instalada, que é a rede de recuperação.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Poda do /var/lib/aurora — chamar podar_lib_aurora() junto do podar_fontes() em ~/.config/zsh/scripts/aurora-cosmic-comp-ws.sh",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": true,
      "porque": "O QUE CONFIRMEI (medido agora, 29/08):\n\n1. Caminhos e linhas EXISTEM e batem. `/home/vitoriamaria/.config/zsh/scripts/aurora-cosmic-comp-ws.sh` tem 553 linhas; `podar_fontes()` abre exatamente em :436 e fecha em :445; as chamadas estão em :475 (cmd_build) e :520 (cmd_build_auto). `LIB=/var/lib/aurora` (:87), `artefato_de()` (:98) e `pkg_orig_de()` (:102). O arquivo instalado em /usr/local/sbin é byte-idêntico à fonte. Nada refutado aqui.\n\n2. \"São peso morto\" — CONFIRMADO para .aurora-ws e .pkg-orig. `cmd_ensure` só olha `artefato_de \"$ver\"` e `pkg_orig_de \"$ver\"` com `ver=$(versao_pacote)` = dpkg da versão INSTALADA (:279-286). `cmd_restore` idem (:530-533). Nenhum caminho de código alcança artefato de outra versão. Varredura ampla em /etc/systemd, /usr/local, /usr/lib/systemd e ~/.config/systemd: os únicos consumidores de /var/lib/aurora são aurora-night-light.py, este script e o self-heal (aurora-fan-ultra usa /var/lib/aurora-fan-ultra, diretório diferente). Confirmado também que cmd_ensure já ANTECIPA o sumiço do artefato — \":302, \"se o artefato sumiu (limpeza de disco), regrava a partir do próprio binário instalado\"\". O desenho tolera poda por construção.\n\n3. \"Vale a quente\" — CONFIRMADO, mas trivialmente. O compositor vivo roda de /usr/bin/cosmic-comp; nada em /var/lib/aurora é lido em runtime. Apagar ali não toca em uniform, shader nem sessão. A pergunta 2/3 do roteiro (GLSL, uniform por frame) não se aplica: esta proposta não encosta em shader. E é justo dizer: ela não entrega NADA de (A) slider de temperatura, (B) textura e-ink, (C) agendamento. É faxina da trilha de build, não funcionalidade.\n\nO QUE DERRUBEI:\n\n4. A CHAMADA EM :475 NÃO FUNCIONA. `cmd_build` se RECUSA a rodar como root (:449-453, \"rode o --build como usuária normal\"), e /var/lib/aurora é `drwxr-xr-x root:root`. Medido nesta máquina: `[ -w /var/lib/aurora ]` é falso e `touch` ali retorna \"Permissão negada\". Unlink exige escrita no DIRETÓRIO, então nem o `.aurora-ws` da versão instalada — que é `vitoriamaria:vitoriamaria`, herdado do `sudo cp -a` — pode ser apagado por ela. Controle positivo em sandbox: `rm -f` em diretório 555 imprime \"Permissão negada\" e sai com 1 (não é silencioso, é barulhento e inútil). `sudo -n true` pede senha, então não há atalho. Metade da proposta, como escrita, é ruído após todo build manual. Só :520 (cmd_build_auto, que exige root em :482) funcionaria.\n\n5. OS NÚMEROS ESTÃO ERRADOS. Contei byte a byte: apaga 11 arquivos = 323,3 MB, não ~370 MB. Restante em binários = 146,6 MB, não ~90 MB. E é inconsistência interna: \"~90 MB\" só fecha guardando UM .orig (33,5 + 28 + 28), não os três que a própria proposta manda guardar (3 × 28 = 84 MB de .orig eternos).\n\n6. OS .orig SÃO DE OUTRO DONO. Não são deste script — quem os cria é `aurora-night-light.py:212` (`BACKUP_DIR/cosmic-comp-<ver>.orig`), e só ele os lê, no `--restore`, sempre da versão corrente. Velhos são de fato inalcançáveis, mas esse invariante mora em OUTRO arquivo e pode derivar sem que este script saiba.\n\nFRONTEIRA: sem travessia. docs/FRONTEIRA.md:58 dá \"binário cosmic-comp (patches de workspace e night light)\" ao Aurora, dono dos dois scripts. Editar a fonte em ~/.config/zsh é o certo (o self-heal:1157 instala de lá para /usr/local/sbin); o auto-commit de 10 min é condição conhecida, não violação.\n\nContexto que REFORÇA a proposta: a raiz está com 80% (12 GB livres de 58 GB) e /var/lib/aurora mora nela. O `podar_fontes` existente só limpa /home, que tem 342 GB livres. Ou seja, a poda ataca o sistema de arquivos que realmente aperta.",
      "risco_real": "Na tela dela, AGORA: nada. Nenhum arquivo de /var/lib/aurora é lido pelo desktop vivo; o compositor roda de /usr/bin/cosmic-comp. Não há cenário em que esta poda apague a sessão dela.\n\nO risco é FUTURO e tem volta em todos os ramos:\n- Perder um .aurora-ws de versão antiga custa, no pior caso, um rebuild de ~4 min. Não é perda de desktop.\n- Perder um .pkg-orig antigo não custa nada: o `--restore` só lê o da versão corrente.\n- CENÁRIO DESTRUTIVO ÚNICO: se `$ver` vier VAZIO, \"não é da versão instalada\" casa com TUDO — some o artefato vivo, o .pkg-orig corrente e todos os .orig. Isso apaga a rede de segurança nomeada em ritual-aurora-self-heal.sh:836. Mesmo assim NÃO fica sem desktop: /usr/bin/cosmic-comp continua intacto no disco; perde-se só o `--restore`, e o último recurso documentado (`apt-get install --reinstall cosmic-comp`) segue valendo. Hoje esse cenário está coberto por acidente — em cmd_build o `compilar` já aborta com `[ -n \"$ver\" ]` (:382) antes de chegar em :475, e cmd_build_auto guarda em :485 — mas nenhuma das duas guardas está DENTRO da função que apaga.\n- Sobrevive a apt upgrade (a poda só reage a build), a reboot (é disco, não estado) e a troca de tema (ortogonal).\n- Recuperação por TTY: sim, e em camadas — `sudo aurora-cosmic-comp-ws.sh --restore` (usa .pkg-orig da versão corrente, preservado), `sudo aurora-night-light.py --restore`, e `sudo apt-get install --reinstall cosmic-comp`. Não há .deb em /var/cache/apt/archives, então o reinstall exige rede — vale registrar, mas não é eliminatório.",
      "correcao": "Seis correções. Nenhuma cara; a (1) é obrigatória ou metade da proposta não roda.\n\n1. TIRAR A CHAMADA DE :475. `cmd_build` é não-root e não pode apagar em /var/lib/aurora. Melhor lugar: DENTRO de `cmd_ensure`, que já é root em todo caminho que escreve em $LIB (cmd_build o chama via `sudo \"$0\" --ensure` em :473; cmd_build_auto e o self-heal o chamam já como root), já calcula `ver`, e já tem a guarda de versão vazia em :280-284 (`return 0`). Bônus: poda também quando nenhum build acontece. Se preferir manter fora, então crie uma entrada só-root `--podar-lib` e chame `sudo \"$0\" --podar-lib` em cmd_build.\n\n2. Guarda explícita na própria função, não herdada: `[ -n \"$1\" ] || return 0` como primeira linha. Versão vazia apagando \"tudo que não é a versão instalada\" é o único ramo que destrói a rede de segurança.\n\n3. Receber `$ver` como argumento, NUNCA chamar `versao_pacote` dentro. Um apt upgrade que caia durante os ~4 min de build faria a função apagar o artefato recém-escrito. É o mesmo contrato que `podar_fontes \"$ver\"` já usa.\n\n4. Comparar por igualdade de nome exato com `artefato_de \"$ver\"` e `pkg_orig_de \"$ver\"`, nunca por prefixo/glob — as versões compartilham prefixo `cosmic-comp-0.1~` e um glob frouxo erra o alvo.\n\n5. Os .orig: ou mover essa parte para `aurora-night-light.py` (dono real, `BACKUP_DIR`, :212), ou deixar comentário nomeando o outro dono e o invariante de que ele só lê o .orig da versão corrente. E guardar UM .orig, não três — três é 84 MB parados que nenhum código alcança, e é o que faz o \"~90 MB\" da proposta não fechar.\n\n6. Espelhar o `AURORA_DRY_RUN` do self-heal (`AURORA_WS_DRY_RUN=1` só imprime), para a primeira execução mostrar os 11 arquivos antes de apagar 323 MB.\n\nNúmeros corrigidos para a proposta: libera 323 MB (não ~370); teto ~147 MB guardando 3 .orig, ou ~90 MB guardando 1 — e é essa última que casa com o texto original."
     }
    },
    {
     "nome": "NÃO usar fork git nem `apt-mark hold` — e por quê",
     "alvo": "build",
     "como": "Fork git: o fonte vem de `apt-get source`, um tarball por versão sem história git, com 698 crates vendorizados e um vendor.tar de 94 MB; manter um fork significaria rebase manual contra um tarball a cada 6 dias, e o rebase falha do mesmo jeito que um patch falha, só que sem mensagem de erro útil. `sed` no script de build: um sed contra Rust que não casa mais SEGUE EM FRENTE em silêncio e produz um compositor que compila e se comporta errado — o modo de falha mais caro possível numa máquina onde o compositor é também o greeter. `apt-mark hold cosmic-comp`: congela o binário, mas o resto do COSMIC continua subindo, e um cosmic-comp fora de fase com o cosmic-session/libcosmic é exatamente a sessão que não sobe que o script inteiro existe para evitar (comentário :63-66). Fica o .patch versionado + série — que é o padrão que os dois repos já usam.",
     "onde": "decisão de arquitetura; o padrão existente está em /mnt/Apate/Desenvolvimento/MeowSystem/patches/ e ~/.config/zsh/patches/",
     "a_quente": false,
     "custo": "Nenhum — é uma escolha, não uma implementação.",
     "durabilidade": "O .patch com contexto falha ALTO e cedo (o `patch` diz qual hunk e em que arquivo), no dry-run, antes de compilar. É a propriedade que garante o fallback seguro: sem efeito, nunca sem desktop.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Trilha \"build-convivencia\": NÃO usar fork git, NÃO usar sed no script de build, NÃO usar `apt-mark hold cosmic-comp` — ficar com .patch versionado + série, o padrão que (segundo a proposta) os dois repos já usam.",
      "veredito": "PLAUSIVEL",
      "a_quente_confirmado": false,
      "porque": "As TRÊS recusas se sustentam. O que NÃO se sustenta é a alternativa que ela propõe no lugar, e é isso que importa.\n\n=== O QUE CONFIRMEI (medido) ===\n\n1. \"Fork git é inviável\": CONFIRMADO. `/home/vitoriamaria/Desenvolvimento/cosmic-comp-patch/cosmic-comp_0.1~1787767625~24.04~5c93094.dsc` diz `Format: 3.0 (native)` — um tarball só, sem .orig + debian.diff. Não existe `.git` na árvore extraída, não existe `debian/patches`, não existe quilt. Não há árvore git local contra a qual rebasear.\n\n2. \"698 crates vendorizados\": EXATO. `ls vendor | wc -l` = 698.\n\n3. \"a cada 6 dias\": BEM CALIBRADO. 12 upgrades do cosmic-comp no `/var/log/apt/history.log*` entre 2026-06-18 e 2026-08-27 = 70 dias / 11 intervalos = 6,4 dias. (E o ritmo acelerou: 08-20, 08-26, 08-27.)\n\n4. \"apt-mark hold cria desalinhamento\": CONFIRMADO na substância, embora a citação de linha esteja errada. O argumento não está em :63-66 (que fala de Nice 19 e das quatro guardas) e sim em :68-71 do `aurora-cosmic-comp-ws.sh`: \"Um cosmic-comp de uma versão com o resto do COSMIC de outra é receita de sessão que não sobe\". `apt-mark`/`hold` não aparecem em lugar nenhum do script — ou seja, isto nunca foi tentado, é raciocínio por analogia. A analogia é boa, mas não é medição.\n\n=== O QUE REFUTEI ===\n\nR1. \"vendor.tar de 94 MB\" — ERRADO POR ~8x. O `vendor.tar` tem 828.119.040 bytes (790 MiB / 829 MB). Os 94 MiB são o `.tar.xz` do pacote-fonte (98.293.984 B). A proposta confundiu os dois — o próprio `podar_fontes` do script diz \"Os `.tar.xz` (94 MB) ficam\", e o número foi colado no objeto errado. O erro puxa a favor da conclusão (é PIOR que o alegado), então não vira o veredito — mas um número errado num documento de decisão vira fato repetido.\n\nR2. \"a série — que é o padrão que os dois repos já usam\" — A SÉRIE NÃO EXISTE. Não há arquivo `series` em `/mnt/Apate/Desenvolvimento/MeowSystem/patches/` nem em `/home/vitoriamaria/.config/zsh/patches/`. Há UM .patch em cada, em repos diferentes, e o `compilar()` (`aurora-cosmic-comp-ws.sh:410-411`) fixa UM caminho, sem laço:\n    patch=\"$PATCH_INSTALADO\"   # /usr/local/share/aurora/cosmic-comp-sem-workspace-vazio.patch\n    [ -f \"$patch\" ] || patch=\"$HOME/.config/zsh/patches/cosmic-comp-sem-workspace-vazio.patch\"\nA proposta está justificando uma escolha pelo padrão existente, e o padrão que ela descreve não existe.\n\nR3. E A CONSEQUÊNCIA JÁ ACONTECEU NESTA MÁQUINA. O `cosmic-comp-raio-clampado.patch` do MeowSystem NÃO ESTÁ NO COMPOSITOR RODANDO:\n  - dry-run contra a árvore de fonte atual: aplica LIMPO (rc=0, \"checking file src/wayland/protocols/corner_radius.rs\", sem \"previously applied\") ⇒ não está aplicado;\n  - `strings -a /usr/bin/cosmic-comp | grep AURORA-COSMIC-.*-PATCH` devolve só `AURORA-COSMIC-WS-PATCH-3.67`. Não há `AURORA-COSMIC-RADIUS-PATCH-1`. Idem no artefato `/var/lib/aurora/cosmic-comp-0.1~1787767625~24.04~5c93094.aurora-ws`.\nEnquanto isso o `forma.sh:114-117` afirma no presente: \"Em 26/08/2026 o cosmic-comp passou a ser buildado com um patch nosso... Com esse patch de pé não existe mais raio que derrube a barra\". O patch foi escrito, commitado (ago 26 22:53) e nunca ligado ao build. Ou seja: o caminho .patch JÁ PERDEU UM PATCH EM SILÊNCIO — que é exatamente o modo de falha que a proposta atribui só ao `sed`.\n  (Alívio: o `forma.sh` guarda pelo MARCADOR, não pela existência do arquivo — \"Sem o marcador... a trava volta a valer\". Então o teto de 8 está de pé e a barra dela não está em risco por isto. O que está errado é a documentação, não o comportamento.)\n\nR4. \"falha ALTO e cedo... no dry-run, antes de compilar\" — NÃO EXISTE DRY-RUN. `grep -n 'dry.run' aurora-cosmic-comp-ws.sh` não devolve nada. A linha 414 é `patch -p1 --forward --batch < \"$patch\"`, sem `--dry-run`. E `patch --forward --batch` aplica os hunks que casam e rejeita os que não casam — não é tudo-ou-nada.\n\nO caminho concreto para um compositor que compila, se diz conforme e está errado:\n  - O patch de workspace mexe em 6 arquivos / 12 hunks. Em `src/shell/mod.rs`, o marcador `static AURORA_WS_PATCH = \"AURORA-COSMIC-WS-PATCH-3.67\"` é o hunk 1 (`@@ -11,6`), logo após o bloco `use` — contexto estabilíssimo. A mudança funcional (`ensure_last_empty` → `if self.workspaces.is_empty()`) é o hunk 3 (`@@ -638,12`), dentro de uma função que o upstream mexe — a guarda #3 do próprio script existe por isso (\"Se o upstream mexer no `ensure_last_empty`, o patch para de aplicar\").\n  - Upstream mexe. Hunk 1 aplica (marcador gravado NA ÁRVORE), hunk 3 rejeita. `patch` volta 1, `compilar` volta 3, nada é instalado. Correto NESSA rodada.\n  - Mas A ÁRVORE FICA MEIO-PATCHADA. Na tentativa seguinte (auto-build permite MAX_TENTATIVAS=3; o `--build` à mão é ilimitado), a guarda de idempotência da linha 408 — `grep -q \"$MARCA\" src/shell/mod.rs` — dá VERDADEIRO, imprime \">> patch já aplicado no fonte\", PULA o patch inteiro, compila, o binário sai com o marcador, a verificação pós-build `grep -aqs \"$MARCA\" \"$bin\"` PASSA, o artefato é guardado, instalado, e o `--ensure` loga \"OK (patch ativo: AURORA-COSMIC-WS-PATCH-3.67)\".\n  - `podar_fontes` só apaga árvores de OUTRAS versões (`[ \"$d\" = \"$(src_dir_de \"$ver_atual\")\" ] && continue`), então a árvore envenenada da versão ATUAL sobrevive entre tentativas.\n  - Bônus de imprevisibilidade: o hunk 2 do mesmo arquivo é `ANIMATION_DURATION 200 → 350`. A mistura do que aplicou e do que não aplicou é arbitrária.\n\nConclusão: a propriedade de segurança que a proposta usa como justificativa — \".patch falha alto, sed falha em silêncio\" — HOJE NÃO EXISTE nesta máquina. O `.patch` também falha em silêncio, uma tentativa depois. As recusas continuam certas; a justificativa precisa ser construída, não citada.",
      "risco_real": "O item em si tem risco ZERO de runtime: é uma escolha de arquitetura, não muda byte nenhum. Não vale a quente e não alega valer — `vale a quente? false` está correto.\n\nO risco real é da TRILHA de build que ela endossa, e ele é contido:\n- Se um build sair errado, o desktop NÃO cai na hora. O processo `cosmic-comp` em execução segura o próprio inode; a troca de `/usr/bin/cosmic-comp` só vale no login seguinte (o script repete \"vale no próximo login\" em três lugares).\n- Existe volta, e ela está verificada: `cmd_restore` (`aurora-cosmic-comp-ws.sh:529`) restaura de `/var/lib/aurora/cosmic-comp-<versão>.pkg-orig`, e o arquivo da versão de hoje EXISTE (28.339.864 B, ago 27). Segunda rede, no próprio código: `sudo apt-get install --reinstall cosmic-comp`. Ambas rodam de TTY (Ctrl+Alt+F3) sem sessão gráfica.\n- A escrita no binário é atômica (`cp` para `.aurora-tmp` + `mv -f`), então não existe cosmic-comp truncado = tela preta.\n- Degradação é suave: sem o patch, o desktop fica perfeito, só volta o workspace vazio.\n\nO risco NÃO contido é o silencioso: um binário meio-patchado passa por TODAS as verificações (marcador presente, `esta_patchado` sim, `marcador_de` igual, self-heal \"OK\") e ela fica com um compositor cujo comportamento ninguém prometeu. Aí não há alarme para disparar — só ela reparando na tela, dias depois, como aconteceu com o \"3\" seco em 24/08.\n\nTerritório: `docs/FRONTEIRA.md:58` dá o binário `cosmic-comp` à Aurora (\"patches de workspace e night light | Aurora | Aurora\"). O `cosmic-comp-raio-clampado.patch` mora em `MeowSystem/patches/` — do outro lado da fronteira, e é justamente o que ficou órfão. Pôr um patch NOVO de shader em `MeowSystem/patches/` repete o erro. Ou o patch vai para `~/.config/zsh/patches/` (lembrando: auto-commit a cada 10 min), ou a linha 58 da FRONTEIRA precisa ser reescrita antes, explicitamente.",
      "correcao": "Manter as três recusas (fork, sed, apt-mark hold) — estão certas. Corrigir a justificativa e CONSTRUIR a propriedade que ela alega existir:\n\n1. Corrigir o número: `vendor.tar` = 828 MB / 790 MiB. Os 94 MiB são o `.tar.xz` do pacote-fonte. São coisas diferentes.\n\n2. Parar de dizer que existe \"série\". Ou criar uma de verdade, ou dizer que hoje há um patch ligado e um órfão. Série de verdade = um arquivo `series` + um laço em `compilar()`:\n     while read -r p; do\n       [ -n \"$p\" ] || continue\n       patch -p1 --forward --batch --dry-run < \"$SERIEDIR/$p\" || return 3\n     done < \"$SERIEDIR/series\"\n   (todo o dry-run ANTES de qualquer aplicação real, para não deixar árvore pela metade)\n\n3. Ligar o `cosmic-comp-raio-clampado.patch` — ou tirar do `forma.sh:114-117` a afirmação de que ele está de pé. Hoje o comentário mente. O guard-by-marker do `forma.sh` está certo e salva a barra dela; a frase é que está errada.\n\n4. Fechar o buraco do meio-patch, que é a correção que realmente importa. Duas linhas em `compilar()`:\n     patch -p1 --forward --batch --dry-run < \"$patch\" || { rm -rf \"$src\"; return 3; }\n     patch -p1 --forward --batch          < \"$patch\" || { rm -rf \"$src\"; return 3; }\n   O `rm -rf \"$src\"` é o essencial: qualquer falha de patch destrói a árvore, e o `apt-get source` da próxima tentativa reextrai limpa do `.tar.xz` que já está no disco. Sem isso, a guarda de idempotência da linha 408 vira a arma.\n\n5. Tornar o marcador confiável: mover o `static AURORA_WS_PATCH` para DENTRO do mesmo hunk da mudança de `ensure_last_empty` (`@@ -638,12`), em vez do hunk `@@ -11,6`. Assim \"marcador presente\" passa a implicar \"correção presente\" — que é o que o self-heal já assume hoje sem ter direito. Um marcador por hunk-grupo funcional, e a verificação pós-build confere TODOS.\n\n6. Se o alvo final for o shader (o pedido dela: slider de temperatura, textura, agendamento), avisar desde já: um patch que mexe em GLSL tem contexto MUITO mais estável que `ensure_last_empty` (o `offscreen.frag` quase não muda), então o risco de rejeição é menor — mas o risco de meio-patch é o mesmo, porque o modo de falha está no script, não no patch. Corrigir o item 4 é pré-requisito de qualquer trilha de build nova, não trabalho opcional.\n\nComandos de recuperação a documentar junto (verificados):\n  sudo /usr/local/sbin/aurora-cosmic-comp-ws.sh --restore\n  sudo apt-get install --reinstall cosmic-comp\n  sudo touch /var/lib/aurora/cosmic-comp-ws-autobuild-off   # freio do auto-build"
     }
    },
    {
     "nome": "Fechar a dívida achada de passagem: o patch de raio, que o README diz aplicado e nunca foi",
     "alvo": "varios",
     "como": "A série resolve sozinha (entra como opt:cosmic-comp-raio-clampado.patch). Mas até o primeiro build com a série, corrigir o README.md:195-197, que hoje afirma um fato falso — e é um fato em que outra pessoa (ou outro frente) vai confiar ao decidir se o compositor clampa. O dry-run aplica limpo hoje, então o conserto é só encanamento.",
     "onde": "/mnt/Apate/Desenvolvimento/MeowSystem/README.md:195; /mnt/Apate/Desenvolvimento/MeowSystem/patches/cosmic-comp-raio-clampado.patch",
     "a_quente": false,
     "custo": "Uma frase no README agora; o efeito real vem no próximo build.",
     "durabilidade": "Depois de entrar na série, é reaplicado a cada versão do cosmic-comp como qualquer outro — e o meow_painel_compositor_clampa() passa a responder verdade, liberando o raio que ela pedir sem teto.",
     "trilha": "build-convivencia",
     "veredito": {
      "solucao": "Fechar a dívida achada de passagem: corrigir o README.md:195-198, que afirma que o `patches/cosmic-comp-raio-clampado.patch` é aplicado pelo `aurora-cosmic-comp-ws.sh --build` — e nunca foi.",
      "veredito": "CONFIRMADO",
      "a_quente_confirmado": false,
      "porque": "Tudo medido nesta máquina, agora (29/08/2026):\n\n1) A FRASE DO README É FALSA — PROVADO POR TRÊS VIAS.\n   a. Código: `/home/vitoriamaria/.config/zsh/scripts/aurora-cosmic-comp-ws.sh:88` fixa\n      `PATCH_INSTALADO=/usr/local/share/aurora/cosmic-comp-sem-workspace-vazio.patch`, e a\n      função `compilar()` (linhas 381-414) aplica ESSE arquivo e só ele — não há laço sobre\n      diretório, não há série, não há `opt:`. `grep -n \"PATCH_INSTALADO|for .*patch|series|opt:\"`\n      devolve exatamente 3 linhas, todas do mesmo arquivo único.\n   b. Disco: `ls /usr/local/share/aurora/` tem apenas `cosmic-comp-sem-workspace-vazio.patch`\n      (+ trackers do qBittorrent). O patch de raio existe SÓ dentro do repo do Meow.\n   c. Binário: `strings -a /usr/bin/cosmic-comp | grep AURORA-COSMIC-` devolve apenas\n      `AURORA-COSMIC-WS-PATCH-3.67`. Zero ocorrência de `AURORA-COSMIC-RADIUS-PATCH`.\n      Ou seja: mesmo o binário patchado no disco não tem o clamp.\n\n2) A REFERÊNCIA CRUZADA CONFIRMA QUE NINGUÉM DO OUTRO LADO FOI AVISADO.\n   `grep -rn \"raio-clampado\"` em `~/.config/zsh/`, `/usr/local/share/aurora/`, `/usr/local/bin/`\n   e no MeowSystem: os 6 hits estão TODOS dentro de `/mnt/Apate/Desenvolvimento/MeowSystem`.\n   E `git log --all -S \"raio-clampado\"` no repo `~/.config/zsh` não devolve NADA — o Aurora\n   nunca soube que esse patch existe. A frase entrou no commit d2730c9 (26/08) descrevendo um\n   acoplamento que nunca foi feito.\n\n3) O PATCH EM SI ESTÁ SÃO — a dívida é só de encanamento, como a proposta diz.\n   `cd ~/Desenvolvimento/cosmic-comp-patch/cosmic-comp-0.1~1787767625~24.04~5c93094 &&\n    patch -p1 --dry-run --forward --batch < .../cosmic-comp-raio-clampado.patch`\n   → `checking file src/wayland/protocols/corner_radius.rs` / RC=0. Aplica limpo contra a\n   MESMA versão que está instalada e rodando (5c93094), embora tenha sido gerado contra\n   31827ed. `cargo 1.98.0` presente, deb-src habilitado (`system.sources`) — o caminho de build\n   existe de fato.\n\n4) NÃO PISA EM TERRITÓRIO ALHEIO. `docs/FRONTEIRA.md:58` dá o binário do cosmic-comp ao Aurora\n   — e a correção proposta não toca nele: é uma frase no README do Meow, dono do próprio doc.\n   Nada é escrito em `~/.config/zsh` (que tem auto-commit de 10 min), nada em `/usr/local`.\n\n5) \"VALE A QUENTE = false\" ESTÁ CERTO, e é irrelevante aqui: é edição de documentação. O\n   efeito no compositor só existiria depois de build + login (o cosmic-comp É o servidor\n   Wayland; trocá-lo a quente fecharia todas as janelas dela).\n\nCONTEXTO QUE ENCONTREI DE QUEBRA (não muda o veredito, mas contradiz a \"durabilidade\" da\nproposta): a sessão VIVA dela roda um cosmic-comp sem marcador NENHUM. `pgrep -x cosmic-comp`\n= 3127, iniciado 28/08 18:43; `/proc/3127/exe` é `(deleted)`, legível (110.176 strings), mesma\nversão 5c93094, e `grep -a AURORA-COSMIC-WS-PATCH` nele dá 0. O binário patchado do WS chegou\nao disco depois (`/var/lib/aurora` e o dir de build têm mtime 28/08 20:46-20:50). Então hoje,\nnesta sessão, nem o patch de workspace está de pé — só valerá no próximo login.",
      "risco_real": "Nenhum risco de desktop. É uma edição de texto em\n`/mnt/Apate/Desenvolvimento/MeowSystem/README.md`, arquivo que nenhum script lê em tempo de\nexecução (conferi: quem decide o clamp é `meow_painel_compositor_clampa()` em `lib/painel.sh:167`,\nque sonda `/proc/PID/exe`, não o README). Não recompila nada, não troca binário, não escreve em\n`~/.config/cosmic`, não reinicia painel. Se sair errado, a volta é `git checkout -- README.md`;\na tela dela nem pisca. Não há caminho por onde isso a deixe sem desktop, então o critério\neliminatório do \"existe volta por TTY?\" nem chega a ser acionado.\n\nO risco REAL é o oposto: deixar a frase como está. Ela (ou uma frente) lê \"aplicado pelo\naurora-cosmic-comp-ws.sh --build\", roda 4 minutos de build, faz logout, e continua sem clamp —\ncom `meow painel teto` dizendo \"sem o patch\" e a barra ainda podendo sumir. Foi exatamente o\nque o histórico já registrou como custo (uma tarde inteira sem barra em 26/08).",
      "correcao": "Aprovo, com DUAS emendas obrigatórias — senão o conserto fica pela metade:\n\n(1) O README NÃO É O ÚNICO LUGAR COM A MENTIRA, E NEM É O PIOR.\n    A mesma instrução falsa é impressa NA TELA DELA, em tempo de execução, por\n    `/mnt/Apate/Desenvolvimento/MeowSystem/scripts/forma.sh`:\n      - linha 194: `meow_info \"  o patch some num 'apt upgrade' do cosmic-comp; refazer: aurora-cosmic-comp-ws.sh --build (~4 min)\"`\n      - linha 497: `meow_info \"  o conserto de raiz é o patch: aurora-cosmic-comp-ws.sh --build (~4 min)\"`\n    Essas duas linhas disparam JUSTAMENTE quando o raio dela é cortado — o momento em que ela\n    vai obedecer a instrução. E o comentário de cabeçalho `forma.sh:115-117` afirma \"Em\n    26/08/2026 o cosmic-comp passou a ser buildado com um patch nosso\", que é falso pelo item 1\n    do \"porquê\". Consertar só o README deixa a mentira exatamente onde ela é lida.\n    O texto certo, enquanto a série não existir, é dizer que o patch está no repo mas NÃO entra\n    em nenhum build hoje — o `aurora-cosmic-comp-ws.sh --build` aplica só o patch de workspace.\n\n(2) A JUSTIFICATIVA DA PROPOSTA ESTÁ ERRADA NUM PONTO, E ELA NÃO PODE VIRAR TEXTO NO README.\n    A proposta diz que com isso \"o `meow_painel_compositor_clampa()` passa a responder verdade\".\n    Não: essa função JÁ responde a verdade hoje. Ela sonda `/proc/PID/exe` do compositor vivo\n    (`lib/painel.sh:167-171`), com o comentário de 26/08 explicando por que a pergunta é sobre o\n    PROCESSO e não sobre o arquivo. Medido agora: ela retorna falso, corretamente, porque não há\n    marcador no processo. Quem mente é o README, não a sonda. A correção do README não muda\n    resposta de função nenhuma e não \"libera o raio sem teto\" — isso só acontece depois de o\n    patch entrar num build de verdade E ela relogar.\n\nEmenda opcional, mas barata: o `--dry-run` limpo é válido para 5c93094, a versão de HOJE. O\npatch foi gerado contra 31827ed. Vale carimbar no README (ou no cabeçalho do .patch) a versão\nem que ele foi conferido pela última vez, porque o cosmic-comp sai quase diário e o\n`corner_radius.rs` é upstream ativo — \"aplica limpo\" tem prazo de validade."
     }
    }
   ]
  }
 ]
}
